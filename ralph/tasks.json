[
 {
  "id": "0.0",
  "model": "sonnet",
  "execution": "bootstrap",
  "description": "BOOTSTRAP (the one task that runs interactively on the host, Michael + principal, because it creates the image and the runner; preflight checklist docs/PREFLIGHT.md must be complete first): docker/Dockerfile + compose.yml for the essaydown-dev image per §4 (Node 22, Rust stable, Tauri Linux deps, webkit2gtk-driver, tauri-driver, xvfb, pandoc, typst, JDK, Claude Code CLI, Codex CLI); poppler-utils, Claude Code CLI, Codex CLI, Grok Build CLI, all versions pinned in docker/versions.env); entrypoints `claude-task`, `claude-review`, `codex-review`, `grok-review` (`grok -p` with the login-state dir mounted read-only); ralph/ralph.sh implementing docs/RUNNER-SPEC.md (task branches, integration transaction, review snapshots, phase close, generated summaries) + stop-check.sh + PROMPT.md (from BUILD-DEFAULTS.md template, phase placeholders filled); ralph/generate-tasks.mjs (PRD §8 → immutable ralph/tasks.json: `execution` defaults, review-set expansion, needsCI gate expansion, targetBranch derivation), ralph/bootstrap.sh and ralph/sync-state.sh (RUNNER-SPEC §6), ralph.sh commands exactly as listed in RUNNER-SPEC §11 (run, retry, resume, resolve-conflict, abandon, plan, plan-retry, plan-abandon, rerun, close, sync-state, status, doctor, admin mark-integrated | admin retry | admin resolve-request) and ralph/test/ conformance suite (§12), scripts/gate.sh consuming `ci` objects (§3) with `--resume a<n>`, `rerun`, `abandon` and `gc` subcommands and ralph/validate-tasks.mjs (plain Node, no deps: raw JSON parses, unique ids, expanded ids unique, dependencies resolve after macro rewriting, execution enum per RUNNER-SPEC §3, needsCI ⇒ ci object, loop-external ⇒ externalRepo, human ⇒ recordTarget or evidence-only, review attempts chain, targetBranch derivation, expanded count, `blockedOnHuman` only on `execution:human`, acyclic); docs/progress.md (generated at reconciliation), docs/progress/journal-main.md, docs/RUNNER-SPEC.md (copied from BUILD-DEFAULTS templates), docs/lessons.md, docs/reviews/TEMPLATE.md seeded from BUILD-DEFAULTS templates; .gitattributes with `merge=union` for docs/lessons.md only; /work/.evidence created and mounted as /logs in every entrypoint; CLAUDE.md and AGENTS.md generated from BUILD-DEFAULTS.md + project specifics, with ralph/check-agent-rules.sh (a `cmp` of both files below line 1); logs/ and .claude/settings.local.json gitignored.",
  "acceptance": "`docker compose build` succeeds on OrbStack; `docker compose run claude-task --version`, `codex-review --version` and `grok-review --version` print versions with credentials mounted read-only and the credential boundary holds inside the container: no environment variable matching `*_API_KEY|GH_TOKEN|GITHUB_TOKEN`, `git config --show-origin --get-all credential.helper` empty, no SSH_AUTH_SOCK, no ~/.ssh or ~/.config/gh mount, and `git ls-remote https://github.com/MLSavage/essaydown-private-authcheck` fails with 'Authentication failed' (a private empty repo Michael creates for this check); `pdfimages -v` runs in the container; `node ralph/validate-tasks.mjs` passes and reports the expanded task count recorded in ralph/EXPECTED_COUNT (generated from PRD §8; 122 at v14: 86 raw tasks + 21 review-set expansion + 15 CI gates; the validator recomputes it); `ralph/bootstrap.sh` runs 0.0's own checks and, only on success, makes the initial commit on phase/0 and initialises `.evidence/state/tasks.json` with 0.0 recorded as passed (ralph/tasks.json has no status field); `ralph/ralph.sh --dry-run` then prints the first unblocked task id (0.1) and the branch it would create (task/0.1); in a disposable fixture repo under /tmp, `git merge` of two branches that each appended a distinct line to a `merge=union` lessons.md merges cleanly with both lines, (the journal test is not needed: implementation is sequential and only lessons.md is union-merged); `ralph/check-agent-rules.sh` exits 0. This task passes its own checks; §9.3 applies from 0.1. `bootstrap.sh` also validates the external clone at /work/.ext/build-defaults (clean tree, refs/heads/main present, `node scripts/validate-defaults.mjs` passes) and writes .evidence/external/build-defaults/state.json with that SHA, and writes .evidence/state/phases.json with the Phase 0 base_main_sha. `ralph/test/` conformance suite (RUNNER-SPEC §12) passes against disposable fixture repos: integration CAS, approval rejection → plan request → rewire, code-failed CI → replacement verifier, transient rerun a2, failed review r0 → r1 chain with candidate resolved from N.verify.r1h, close crash before/after the ref transaction, 4→6→5→terminal phase records, verifier g1 repair then r0→r1→r1.g1 chain, crash injection around the candidate tag, update-ref and state write (doctor names the one admin command, run refuses until clean, wrong admin command refused), an incomplete CI attempt resumed by run_id without a new run, gate.sh gc scope, digest mismatch rejected by consumers, rerun refused after a workflow-file change, validator rejecting gateKind publication, completed-close re-entry, sync-state refusing mutation, validator rejecting each schema violation.",
  "dependencies": [],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.1",
  "model": "sonnet",
  "description": "Scaffold monorepo: pnpm workspaces, packages/core|editor|modes|coach|export, apps/desktop (Tauri 2 + React + Vite), Vitest with @vitest/coverage-v8 and a per-file threshold config, ESLint/Prettier, e2e/web (Playwright) and e2e/shell (WebdriverIO) folders with placeholder configs; tests/agent-rules.test.ts (Vitest) re-asserting CLAUDE.md ≡ AGENTS.md below line 1.",
  "acceptance": "`pnpm install && pnpm test` passes with one placeholder test per package; `pnpm tauri dev` opens a window titled 'Essay Down' (under xvfb in the container); `pnpm coverage` prints a per-file table. Does NOT add editor code or CI.",
  "dependencies": [
   "0.0"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.2",
  "model": "sonnet",
  "description": "CI: .github/workflows/ci.yml running pnpm lint, pnpm test, cargo test on ubuntu-latest, macos-latest, windows-latest; installs a JDK, poppler-utils, and pinned epubcheck and html-validate versions (from docker/versions.env, so container and CI agree) for later phases; caches pnpm and cargo.",
  "acceptance": "ci.yml lints under `actionlint` locally and `act --dryrun` resolves the three jobs; versions in the workflow match docker/versions.env.",
  "dependencies": [
   "0.1"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "ci.yml passes on all three runners; `epubcheck --version`, `npx html-validate --version` and `pdfimages -v` succeed in the workflow log on all three.",
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.2h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "0.2",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "phase": "0",
  "targetBranch": "phase/0",
  "description": "CI gate for 0.2: scripts/gate.sh 0.2h pushes integrated_sha of 0.2 to ci/0.2/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\"] into /logs/ci/0.2/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "ci.yml passes on all three runners; `epubcheck --version`, `npx html-validate --version` and `pdfimages -v` succeed in the workflow log on all three.",
  "dependencies": [
   "0.2"
  ]
 },
 {
  "id": "0.3",
  "model": "opus",
  "description": "Write docs/MARKDOWN-STYLE.md (ATX headings, `-` bullets, `1.` ordered with incrementing numbers, fenced code with ``` and info string preserved, pipe tables with aligned columns, one blank line between blocks, `*emphasis*`/`**strong**`, inline links, images as `![alt](relative/path)`, no hard wrap, exactly one trailing newline) and configure remark-parse/remark-stringify in packages/core/src/{parse,format}.ts with the §4 extension set and byte-preserving handlers for html and yaml nodes.",
  "acceptance": "format(parse(x)) for the 12 examples in MARKDOWN-STYLE.md equals the listed output byte-for-byte (12 assertions); an html node and a yaml block are byte-identical after the trip (2 assertions); `[^1]` parses as text (1 assertion); the front-matter fixtures with quoted, commented, block-scalar, duplicate and malformed forms round-trip byte-identical when no app-owned key changes (5 assertions).",
  "dependencies": [
   "0.1"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.4",
  "model": "sonnet",
  "description": "Fixture corpus part 1: 44 short .md files in fixtures/markdown covering every §6.1 node, nested lists 3 deep, tables with escaped pipes, code fences containing ``` and ~~~, images with spaces in paths, front matter (plain, quoted values, inline comment, block scalar `question: |`, duplicate `title:` key, malformed front matter), raw html block, footnote syntax (as text), CRLF line endings, missing trailing newline, tabs. Each gets a `.canonical.md` sibling from format(parse(x)). fixtures/markdown/index.json lists per file: node types, blockCount, sectionCount, and for each paragraph its 1-based start line in the canonical file.",
  "acceptance": "44 files + 44 canonical siblings present; index.json validates against fixtures/markdown/index.schema.json; a test asserts no supported node type is absent from the union of node types; blockCount for nested-lists.md, table-escaped-pipes.md, code-fence-nested.md, front-matter.md and raw-html.md equals a hand-computed value written in the test (the Phase 0 reviewers independently hand-verify five further fixtures of their choosing).",
  "dependencies": [
   "0.3"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.5",
  "model": "sonnet",
  "description": "Fixture corpus part 2: fixtures/markdown/essay-fixture.md — a ~5,000-word essay with 12 H2/H3 headings, 2 tables, 3 code fences, 2 images, 1 blockquote, lists — supplied by Michael if provided in docs/fixture-essay.md, otherwise written by the agent on a neutral topic (the history of the fountain pen). Add its canonical sibling and index.json entry.",
  "acceptance": "Word count 4,500–5,500; exactly 12 headings; index.json entry present with paragraph line offsets; invariants A/B/C hold for it.",
  "dependencies": [
   "0.4"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.6",
  "model": "opus",
  "description": "Round-trip invariant tests in packages/core: A idempotence, B semantic preservation (positions stripped), C byte fidelity on `.canonical.md` and on opaque nodes. Fix format.ts until green.",
  "acceptance": "`pnpm --filter core test -- roundtrip` runs 135 assertions (3 × 45 fixtures) and passes; CRLF fixture normalizes to LF on parse (asserted).",
  "dependencies": [
   "0.5"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.7",
  "model": "opus",
  "description": "core/hash.ts (unsigned 64-bit FNV-1a as BigInt → zero-padded 13-char base-36) and core/blocks.ts: `blocksOf(root)` with contentIds per §6.1, `sectionsOf(root)`, `replaceBlock`, `moveBlock`, `moveSection`, `setHeadingDepth` (re-depths descendants), all pure.",
  "acceptance": "fnv1a64('') === 0xcbf29ce484222325n and fnv1a64('hello') === 0xa430d84680aabd0bn (published FNV-1a 64 vectors), and their encodings are exactly 13 chars ('hello' → '2hvyo96lq8v0r'); essay-fixture yields sectionCount and blockCount from index.json; two identical paragraphs get ids ending -0 and -1; moveSection(2,0) then format() equals expected/essay-fixture.moved.md (created and committed by this task); setHeadingDepth on an H2 with H3 children yields H3/H4. 100% branch coverage on blocks.ts (coverage threshold set in vitest config).",
  "dependencies": [
   "0.6"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.8",
  "model": "opus",
  "description": "core/sentences.ts: `sentencesOf(paragraph)` via Intl.Segmenter with the §6.1 abbreviation post-filter; `replaceSentence(root, blockId, index, text)` and `reorderSentences(root, blockId, order)` preserving inline marks by offset mapping.",
  "acceptance": "packages/core/test/sentences.cases.json with 30 cases passes under Node, e.g. 'Dr. Smith went home. He slept.' → 2; 'It cost 3.50 dollars. Then more.' → 2; 'See e.g. the chart. Next.' → 2; replaceSentence on 'A **bold** start. And *emphasis* here.' index 0 with 'A quiet start.' → 'A quiet start. And *emphasis* here.' (marks inside the replaced range go with it), and with 'A **quiet** start.' → 'A **quiet** start. And *emphasis* here.'; a link spanning a boundary survives on both sides.",
  "dependencies": [
   "0.7"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.9",
  "model": "opus",
  "description": "core/segment-fallback.ts: rule-based sentence segmenter (terminal punctuation + closing quotes/brackets + whitespace + capital/number, with the same abbreviation list) and `segmenterSelfTest()` which runs 5 canary cases against the runtime Intl.Segmenter and returns which implementation to use.",
  "acceptance": "The fallback passes ≥ 27/30 of sentences.cases.json (the 3 allowed misses are listed in the test); selfTest returns 'intl' under Node ≥ 20 and 'fallback' when `Intl.Segmenter` is stubbed to undefined.",
  "dependencies": [
   "0.8"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.10",
  "model": "opus",
  "description": "core/sidecar.ts: zod schema, `attach(sidecar, root)` implementing the five-step resolution order of §6.2, `refresh(sidecar, root)`, front-matter mirror of title/question with the byte-preservation rule of §6.1.",
  "acceptance": "A variant anchored to a sentence survives changing one word elsewhere in the paragraph; survives a two-character change in the sentence itself; orphans after a complete rewrite; an in-app reorderSentences of two identical sentences moves each variant with its logical sentence; an external swap of the same two sentences (file re-parsed) leaves each variant at its recorded position; the same two cases for two identical H2 headings; missing version → zod error 'sidecar version missing'; `question:` front matter round-trips to topicQuestion; saving with an unchanged topic question leaves the yaml block byte-identical; changing it in-app rewrites only the `question:` line. Front matter with a block-scalar, duplicate or otherwise unsupported app-owned key → FrontMatterUnsupported and the block stays byte-identical on save; quoted values and inline comments are rewritten preserving quoting and comment.",
  "dependencies": [
   "0.9"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.11",
  "model": "opus",
  "description": "core/undo.ts: snapshot undo stack per §6.5 — push(root, sidecar, {coalesceKey, at}), undo(), redo(), coalescing pushes with the same key within 1,000 ms, cap 500 with structural sharing.",
  "acceptance": "20 unit tests: 3 pushes with key 'typing' inside 1 s collapse to one undo step; a 'rewrite' push between them does not coalesce; 600 pushes retain the last 500; undo/redo restore identical (deep-equal) root and sidecar.",
  "dependencies": [
   "0.10"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify",
  "model": "sonnet",
  "description": "Phase 0 verification: freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "0.2h",
   "0.11"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full Phase 0 suite (pnpm lint, pnpm test, cargo test) green on ubuntu, macos and windows at the implementation SHA; logs in /logs/ci/0.verify/accepted/.",
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "0.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "phase": "0",
  "targetBranch": "phase/0",
  "description": "CI gate for 0.verify: scripts/gate.sh 0.verifyh pushes integrated_sha of 0.verify to ci/0.verify/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\"] into /logs/ci/0.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full Phase 0 suite (pnpm lint, pnpm test, cargo test) green on ubuntu, macos and windows at the implementation SHA; logs in /logs/ci/0.verify/accepted/.",
  "dependencies": [
   "0.verify"
  ]
 },
 {
  "id": "0.13",
  "model": "sonnet",
  "description": "Line-ending policy for the three-OS suite (repair after the 0.verifyh GATE-FAILED, ci.yml run 34037147355: ubuntu and macos 431/431 green, windows-latest 99 of 419 failed in 6 files, every failure `\\r\\n` versus `\\n`; cause: `.gitattributes` had no text/eol policy, so `actions/checkout@v4` on Windows converted every LF file to CRLF at checkout, and six tests read fixtures/markdown/**, packages/core/test/fixtures/** and docs/MARKDOWN-STYLE.md byte-for-byte; DECISIONS #018). Replace the whole of `.gitattributes` with exactly these four lines, in this order, nothing more: `* text=auto eol=lf` / `fixtures/** -text` / `packages/core/test/fixtures/** -text` / `docs/lessons.md merge=union` (each on its own line, one trailing newline). The `-text` trees are read byte-for-byte by tests and stay verbatim on every OS (fixtures/markdown/crlf-line-endings.md is the corpus's one CRLF source by design); everything else is LF on every checkout, which is what docs/MARKDOWN-STYLE.md needs and what the next byte-for-byte reader will need. Scope is that one file plus the journal and lessons: no test edits, no workflow edits, no source edits, zero new dependencies (PRD §4). Do not widen or narrow the attribute lines; do not run `git config core.autocrlf` (repo config is shared by every worktree) — use `git -c core.autocrlf=true <cmd>` for the one-command simulation in the acceptance.",
  "acceptance": "`cat .gitattributes` is byte-identical to the four lines in the description. Reproduction first, recorded in the journal: before the change, `rm -r docs/MARKDOWN-STYLE.md fixtures/markdown packages/core/test/fixtures && git -c core.autocrlf=true checkout -- docs/MARKDOWN-STYLE.md fixtures packages/core/test/fixtures` (a Windows-style checkout) followed by `git ls-files --eol docs/MARKDOWN-STYLE.md fixtures packages/core/test/fixtures | grep -c 'w/crlf'` reports more than one path, and `pnpm test` is red. After the change, the same two commands report `w/crlf` for exactly one path, fixtures/markdown/crlf-line-endings.md (`i/crlf`, `attr/-text`), `git ls-files --eol | grep 'i/crlf'` still lists only that path (no renormalisation of tracked content), and `pnpm lint && pnpm test && cargo test` is green on that Windows-style checkout. `git diff --stat` against the branch base touches only `.gitattributes`, `docs/lessons.md` and `docs/progress/journal-main.md`. Honest statement: this container is Linux, so the in-container result is a proxy and not a Windows pass; the only proof is the `0.verify.g1h` gate green on ubuntu, macos and windows, which this task cannot observe and must not claim.",
  "dependencies": [
   "0.verifyh"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify.g1",
  "model": "sonnet",
  "description": "Phase 0 verification, repair 1 of 0.verify after the 0.verifyh GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a); DECISIONS #018): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "0.13"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full Phase 0 suite (pnpm lint, pnpm test, cargo test) green on ubuntu, macos and windows at the implementation SHA; logs in /logs/ci/0.verify.g1h/accepted/ (the gate id; DECISIONS #018).",
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "0.verify.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "phase": "0",
  "targetBranch": "phase/0",
  "description": "CI gate for 0.verify.g1: scripts/gate.sh 0.verify.g1h pushes integrated_sha of 0.verify.g1 to ci/0.verify.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\"] into /logs/ci/0.verify.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full Phase 0 suite (pnpm lint, pnpm test, cargo test) green on ubuntu, macos and windows at the implementation SHA; logs in /logs/ci/0.verify.g1h/accepted/ (the gate id; DECISIONS #018).",
  "dependencies": [
   "0.verify.g1"
  ]
 },
 {
  "id": "0.12.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 0.12 (phase 0). Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build.",
  "acceptance": "/logs/reviews/0/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.g1h"
  ],
  "phase": "0",
  "targetBranch": "phase/0",
  "reviewSet": "0.12",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build."
 },
 {
  "id": "0.12.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 0.12 (phase 0). Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build.",
  "acceptance": "/logs/reviews/0/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.g1h"
  ],
  "phase": "0",
  "targetBranch": "phase/0",
  "reviewSet": "0.12",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build."
 },
 {
  "id": "0.12.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 0.12 (phase 0). Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build.",
  "acceptance": "/logs/reviews/0/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.g1h"
  ],
  "phase": "0",
  "targetBranch": "phase/0",
  "reviewSet": "0.12",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build."
 },
 {
  "id": "0.12.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 0.12 (phase 0): copy the three reports to docs/reviews/phase-0-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-0-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 0.verify.r1 and 0.12.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "0.12.r0a",
   "0.12.r0b",
   "0.12.r0c"
  ],
  "phase": "0",
  "targetBranch": "phase/0",
  "reviewSet": "0.12",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 0. Phase checks: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from logs/ci), invariant table, five further fixtures hand-verified by each reviewer, golden expected/essay-fixture.moved.md hand-verified, fresh-clone cold build."
 },
 {
  "id": "0.14",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F1, Claude findings 1–2 and Sol finding 2, blocker): packages/core/src/sentences.ts `sliceInline` gives a zero-width atomic inline node (an image with empty alt, `![](x.png)`, alone or inside a link) no owner at the end of a paragraph and two owners at offset 0, so `replaceSentence` and `reorderSentences` silently delete or duplicate it; the mark branch's `hi <= lo` test discards zero-width descendants the same way. Decide the ownership rule for width-0 inline nodes at slice endpoints once (an explicit `includeAtTo` argument, true only for the final slice that reaches `plain.length`; drop the `zeroWidth && start === from` special case), traverse wrappers (link, emphasis, strong) that contain them, write the rule in the doc comment, and add the invariant the corpus never had: for every top-level paragraph of every fixture listed in fixtures/markdown/index.json, `format(reorderSentences(root, id, identity))` and `format(replaceSentence(root, id, i, <the sentence's own text>))` equal `format(root)` byte-for-byte. No new dependencies; no edits outside packages/core/src/sentences.ts and packages/core/test/.",
  "acceptance": "Probe cases from the reports pass as tests, each asserting node presence, count and URL, presence and absence: `One. Two. ![](i.png)` with replaceSentence(index 0, 'X.') → `X. Two. ![](i.png)` and reorderSentences(identity) unchanged; `![](i.png) One. Two.` with replaceSentence(index 0, 'X.') → exactly one image; `Hi. ![](x.png)` identity reorder unchanged; a linked empty-alt image `[![](x.png)](https://e.x)` at the end and at the start survives replace and reorder with its URL; an image adjacent to another image survives. The corpus-wide identity invariant passes for every paragraph of every fixture in index.json (count read from the index, not written as a literal). The 30 cases of sentences.cases.json and the existing sentences tests still pass; `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.12.r0d"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.15",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F2, Sol findings 3 and 5, blocker): packages/core/src/sidecar.ts's hand-written front-matter scalar scanner treats malformed scalars as writable (`title: bad: mapping`, `title: \"\\q\"`) and misdecodes valid double-quoted escapes (`\"\\u0041\"` mirrored as `u0041`, not `A`); a replacement value containing a newline is written literally inside single quotes as two physical lines and reported as success. Define the supported grammar for the two app-owned keys (PRD §6.1): plain scalar with no `: ` or ` #` sequence and no leading indicator character; double-quoted with the YAML escapes `\\\\ \\\" \\n \\t \\r \\/ \\b \\f \\0 \\uXXXX \\xXX` decoded and re-encoded; single-quoted with `''`. Everything else (unknown escapes, malformed plain scalars, block or flow indicators, a replacement value containing `\\n` or `\\r` for any quoting style) → `FrontMatterUnsupported` before any edit, root identity and every byte unchanged. No YAML library (PRD §4; zero new dependencies); edits only in packages/core/src/sidecar.ts and packages/core/test/.",
  "acceptance": "Tests assert, for each case, both the outcome and the untouched bytes: `\"\\u0041\"` reads as `A` and re-encodes preserving the quoting; `\"\\q\"` and `bad: mapping` reads report FrontMatterUnsupported and a write leaves the block byte-identical; a multiline replacement (`Line one\\nLine two`, and with `\\r`) is refused for plain, single-quoted and double-quoted titles; the existing sidecar tests (73 at r0) still pass; `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.14"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.16",
  "model": "sonnet",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F3, Claude findings 3 and 10, Sol finding 10, Grok findings 5–6, should-fix required): the green bar `pnpm lint && pnpm test && cargo test` (CLAUDE.md step 2, RUNNER-SPEC §4.3) covers neither `tsc` nor coverage, so packages/core ships red on `pnpm --filter @essaydown/core typecheck` (packages/core/test/sidecar.test.ts:979, TS2339: `variants` on an un-narrowed `orphans[0].entry` union) and 0.7's 100% blocks.ts threshold is never executed. Widen the bar's existing commands, not its command list: root `package.json` `lint` becomes `eslint . && pnpm -r --if-present typecheck` (every package with a `typecheck` script runs `tsc --noEmit`; add the script to packages that lack it), `test` becomes `vitest run --coverage` (thresholds fail the run) with `coverage` kept for the table. Fix the narrowing at sidecar.test.ts:979 with a discriminant check (`list === 'rewrites'`) rather than a cast. Set global per-file thresholds in vitest.config.ts at or just below the values the current `pnpm coverage` run prints (read them from the run; do not copy numbers from this text), keeping blocks.ts at 100. ci.yml needs no change (it already runs `pnpm lint` and `pnpm test`). No new dependencies.",
  "acceptance": "`pnpm lint` fails on a deliberately introduced type error in a scratch file and passes once it is removed (both shown in the journal); `pnpm test` fails when blocks.ts's threshold is lowered below its measurement to a value that cannot be met and passes at the committed values (shown in the journal, then reverted); `pnpm --filter @essaydown/core typecheck` exits 0; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "0.15"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.17",
  "model": "sonnet",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F4 and F5, Claude findings 4–5, Sol finding 8, Grok finding 1, should-fix required): (1) .github/workflows/ci.yml's `merge-logs` job merges the three per-OS `test-logs-<os>` artifacts with the default `separate-directories: false`, so `lint.log`/`test.log`/`cargo-test.log` collide and the accepted, digest-verified artifact holds one OS; set `separate-directories: true` (the merged artifact then holds `test-logs-{ubuntu,macos,windows}-latest/{lint,test,cargo-test}.log`); (2) the runner's conformance suite `ralph/test/run.sh` (RUNNER-SPEC §12) is run only by bootstrap: add a step to the ubuntu leg of the `test` job that runs `bash ralph/test/run.sh` after `cargo test`, its output captured as `ralph-test.log` inside that leg's upload. Do not change the artifact name `test-logs`. Tool versions still come from docker/versions.env. No new dependencies.",
  "acceptance": "ci.yml lints under `actionlint` and `act --list` resolves the jobs; the diff touches only .github/workflows/ci.yml and the journal; the journal states plainly that the three-OS proof and the artifact layout are observed only by the `0.verify.r1h` gate (whose ciAcceptance names the three directories), never from this Linux container.",
  "dependencies": [
   "0.16"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.18",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F6 and F7, Sol finding 6 and Grok finding 2, should-fix required; runner code, conformance tests required): (1) review start (`ralph/lib/run.mjs`, the branch that records /logs/reviews/<phase>/r<k>/) trusts the runtime `integrated_sha` and never consumes the verifier gate's evidence; it must read `/logs/ci/<verifier_id>h/accepted.json` through the existing `readAcceptedCi` helper (digests of every named artifact verified), require its `sha` to equal both `verification_sha` and `implementation_sha`, and refuse to start the attempt (a RalphError naming the gate and the mismatch; `run` exits non-zero, no reviewer started, nothing recorded) when accepted.json is missing or any digest differs; (2) `ralph/lib/gate.mjs` `isVerifyGate` is `/^\\d+\\.verifyh$/`, so `0.verify.g1h` (and every `N.verify.r<k>[.g<n>]h`) never prints `ROTATE-PRINCIPAL` (DECISIONS #015): use the full verifier-gate grammar `/^\\d+\\.verify(?:\\.r\\d+)?(?:\\.g\\d+)?h$/` in the runner and in gate.sh's output path. Conformance tests in ralph/test/conformance.test.mjs through the real entry points (`ralph.sh run`, `gate.sh`): tampered artifact → review refused; intact → review starts; `0.verify.g1h` and `0.verify.r1h` ACCEPT lines followed by `ROTATE-PRINCIPAL`. No new dependencies; RUNNER-SPEC unchanged (both items implement sentences already in §5.1, §11 and §12).",
  "acceptance": "`bash ralph/test/run.sh` green with the new tests present and failing before the fix (shown in the journal: run once with the fix stashed); the existing 61 tests still pass; `pnpm lint && pnpm test && cargo test` green; the diff touches only ralph/lib/run.mjs, ralph/lib/gate.mjs, ralph/test/ and the journal.",
  "dependencies": [
   "0.17"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.19",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F8, Grok finding 3 and Claude finding 11, the #017 doctor gap, should-fix required; runner code, conformance tests required): `ralph.sh doctor` checks a passed task by ancestry only, so a later commit on the phase branch that deletes a passed task's files (DECISIONS #017: `c8b234a` deleted 91 files of 0.4 while `6a9acdc` stayed an ancestor) is invisible; and a crash after `status: passed` but before the candidate tag, worktree and branch are removed leaves debris no check names. Add two read-only doctor checks, each naming its one repair: (1) `principal-commit-deletes <sha> <n> paths` for every commit on the phase branch since `phases.json` `base_main_sha` whose subject does not start with `task(`, `plan(` or `fix(0.` and which deletes any path present in its parent (task and plan commits may delete files; a principal commit that deletes is the #017 shape) — repair is the manual #017 procedure (restore commit); (2) `passed-with-debris <id>` when a `passed` task still has `candidate/<id>`, a worktree or `task/<id>` — repair `admin mark-integrated <id>` (already idempotent; make it so if it is not). Conformance tests: a fixture host commit deleting a passed task's file → doctor reports (1) and `run` refuses; the restore commit clears it; a task commit deleting a file → clean; a passed task with a leftover candidate tag → doctor reports (2) and the admin command clears it. No new dependencies.",
  "acceptance": "`bash ralph/test/run.sh` green with the new tests failing before the fix (journal shows the red run); existing tests still pass; `ralph/ralph.sh doctor` on the real repo at HEAD is clean (the `c8b234a`/`59d2d3e` pair on phase/0 must not be reported: `59d2d3e` restored the paths, so the check compares against the branch head, not the parent alone — state exactly which comparison is implemented); `pnpm lint && pnpm test && cargo test` green; the diff touches only ralph/lib/doctor.mjs, ralph/lib/integrate.mjs if needed, ralph/test/ and the journal.",
  "dependencies": [
   "0.18"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.20",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F9 and F10, Claude finding 6 and Sol finding 11, should-fix required; runner code, conformance tests required): (1) `ralph/lib/util.mjs` `withLock` breaks a stale lock by path (read pid → `kill(pid, 0)` → `unlinkSync`), a TOCTOU in which two processes recovering from the same crash can both hold the lock, and the `finally` unlinks unconditionally; break and take the lock as one operation (rename the stale file to a per-pid name and continue only if the rename succeeded), write a random token into the lock, re-read it after acquisition and abort if it is not yours, and release only when the token read back is yours; (2) `writeAtomic` is documented as fsynced but never calls fsync: fsync the temporary file's descriptor before `rename` and the containing directory after it (RUNNER-SPEC §8.4 needs the close intent durable before the ref transaction). Conformance tests: a stale lock plus a simulated concurrent breaker (a second lock file appearing between check and break) → exactly one holder, the other aborts; release never deletes a lock it does not own; `writeAtomic` with an injected fs records the order write → fsync(fd) → close → rename → fsync(dir). No new dependencies.",
  "acceptance": "`bash ralph/test/run.sh` green with the new tests failing before the fix (journal shows the red run); existing tests still pass; `pnpm lint && pnpm test && cargo test` green; the diff touches only ralph/lib/util.mjs, ralph/test/ and the journal.",
  "dependencies": [
   "0.19"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.21",
  "model": "sonnet",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F11, Claude findings 7 and 12, should-fix required): the 0.1 scaffold left `tauri-plugin-opener` (apps/desktop/src-tauri/Cargo.toml) and `@tauri-apps/plugin-opener` (apps/desktop/package.json) — dependencies outside PRD §4 with no DECISIONS note (§9.4) — and the scaffold's `greet` command with its `invoke_handler` (apps/desktop/src-tauri/src/lib.rs), an IPC surface item outside §6.4's exhaustive list. Remove both packages, the `.plugin(tauri_plugin_opener::init())` registration, `greet` and the handler; set apps/desktop/src-tauri/Cargo.toml `description` to `Essay Down` and `authors` to `[\"Savage Systems\"]`, and apps/desktop/index.html's `<title>` to `Essay Down`. The lockfiles change by removal only (no addition). Leave App.tsx to Phase 1's shell task (backlog entry exists).",
  "acceptance": "`git diff` shows no added dependency in any manifest or lockfile and the two opener packages gone from both lockfiles; `grep -rn opener apps/` is empty; `cargo test` compiles the workspace; `pnpm tauri dev` under xvfb still opens a window titled 'Essay Down' (0.1's check); `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.20"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.22",
  "model": "sonnet",
  "description": "Phase 0 review fix (DECISIONS #review-0-r0 F12 and F13, Sol finding 4, Grok findings 9–10, should-fix required): packages/core/src/parse.ts's comment says line endings are normalised to LF on the way in, but `parse` never rewrites `\\r\\n`; micromark drops CR between blocks (which is all the corpus's CRLF fixture exercises) and keeps it inside multi-line paragraphs and fenced code, so `format` emits mixed endings. Normalise `\\r\\n?` → `\\n` before parsing (whole document, opaque nodes included: invariant C's rule is that output line endings are LF and the 0.6 acceptance says the CRLF fixture normalises on parse). Add unit tests with string literals (not new CRLF fixtures, which the corpus test reserves for the one CRLF source): a CRLF two-line paragraph, a CRLF fenced code block, a CRLF html block and a CRLF yaml block, asserting `\\r` present in the source, absent from every node value and from `format` output. Add two LF corpus fixtures with canonical siblings and index.json entries: `task-list-as-text.md` (`- [ ]`/`- [x]` items stay plain list text, PRD §4/§6.1) and `autolink-literal.md` (a bare URL becomes a link node); the corpus tests read counts from index.json, so no literal changes. No edits to 0.4's fixture files; no new dependencies.",
  "acceptance": "The four CRLF unit tests pass with the presence (source) and absence (values, output) assertions; the two new fixtures satisfy invariants A/B/C (the roundtrip test picks them up from the directory) and their index.json entries validate against index.schema.json; `fixtures-corpus.test.ts` still asserts the three counts equal (read from the directory and the index); `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.21"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify.r1",
  "model": "sonnet",
  "description": "Phase 0 verification for review attempt r1 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. `promote-lessons` is a manual step carried by this task's text (DECISIONS #review-0-r0): append these two lines, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md (identical below line 1; `ralph/check-agent-rules.sh` must stay green): `- Every core operation of the shape f(root, …) → root has a corpus-wide identity test: the no-op argument (identity permutation, a sentence replaced by its own text, an unchanged front-matter value) re-serialises byte-identically for every fixture in fixtures/markdown/index.json.` and `- Code that partitions a string or a node list into adjacent half-open slices states its ownership rule for zero-width items once, in the doc comment, and tests the first, middle and last positions.`",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "0.22"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify.r1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full Phase 0 suite (pnpm lint with typecheck, pnpm test with coverage thresholds, cargo test, and ralph/test/run.sh on ubuntu) green on ubuntu, macos and windows at the implementation SHA; the accepted `test-logs` artifact under /logs/ci/0.verify.r1h/accepted/ (the gate id; DECISIONS #018) holds test-logs-ubuntu-latest/, test-logs-macos-latest/ and test-logs-windows-latest/ each with lint.log, test.log and cargo-test.log (ubuntu also ralph-test.log).",
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify.r1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "0.verify.r1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify.r1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "phase": "0",
  "targetBranch": "phase/0",
  "description": "CI gate for 0.verify.r1: scripts/gate.sh 0.verify.r1h pushes integrated_sha of 0.verify.r1 to ci/0.verify.r1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\"] into /logs/ci/0.verify.r1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full Phase 0 suite (pnpm lint with typecheck, pnpm test with coverage thresholds, cargo test, and ralph/test/run.sh on ubuntu) green on ubuntu, macos and windows at the implementation SHA; the accepted `test-logs` artifact under /logs/ci/0.verify.r1h/accepted/ (the gate id; DECISIONS #018) holds test-logs-ubuntu-latest/, test-logs-macos-latest/ and test-logs-windows-latest/ each with lint.log, test.log and cargo-test.log (ubuntu also ralph-test.log).",
  "dependencies": [
   "0.verify.r1"
  ]
 },
 {
  "id": "0.12.r1a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "0.12",
  "reviewAttempt": "r1",
  "description": "Review attempt r1, claude reviewer, for review set 0.12 (phase 0). Review set for Phase 0, attempt r1 (after the r0 FAIL, DECISIONS #review-0-r0). Phase checks as r0: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from /logs/ci/0.verify.r1h/accepted/, three OS directories); invariant table; five further fixtures hand-verified by each reviewer; golden expected/essay-fixture.moved.md hand-verified; fresh-clone cold build. r1-specific checks: each r0 finding's disposition in DECISIONS #review-0-r0 verified against the tree (fixed, deferred with a backlog line, or recorded), the identity invariant of 0.14 and the front-matter grammar of 0.15 probed independently, and the conformance tests of 0.18–0.20 read for what they actually exercise.",
  "acceptance": "/logs/reviews/0/r1/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.r1h"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.12.r1b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "0.12",
  "reviewAttempt": "r1",
  "description": "Review attempt r1, sol reviewer, for review set 0.12 (phase 0). Review set for Phase 0, attempt r1 (after the r0 FAIL, DECISIONS #review-0-r0). Phase checks as r0: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from /logs/ci/0.verify.r1h/accepted/, three OS directories); invariant table; five further fixtures hand-verified by each reviewer; golden expected/essay-fixture.moved.md hand-verified; fresh-clone cold build. r1-specific checks: each r0 finding's disposition in DECISIONS #review-0-r0 verified against the tree (fixed, deferred with a backlog line, or recorded), the identity invariant of 0.14 and the front-matter grammar of 0.15 probed independently, and the conformance tests of 0.18–0.20 read for what they actually exercise.",
  "acceptance": "/logs/reviews/0/r1/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.r1h"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.12.r1c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "0.12",
  "reviewAttempt": "r1",
  "description": "Review attempt r1, grok reviewer, for review set 0.12 (phase 0). Review set for Phase 0, attempt r1 (after the r0 FAIL, DECISIONS #review-0-r0). Phase checks as r0: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from /logs/ci/0.verify.r1h/accepted/, three OS directories); invariant table; five further fixtures hand-verified by each reviewer; golden expected/essay-fixture.moved.md hand-verified; fresh-clone cold build. r1-specific checks: each r0 finding's disposition in DECISIONS #review-0-r0 verified against the tree (fixed, deferred with a backlog line, or recorded), the identity invariant of 0.14 and the front-matter grammar of 0.15 probed independently, and the conformance tests of 0.18–0.20 read for what they actually exercise.",
  "acceptance": "/logs/reviews/0/r1/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.r1h"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.12.r1d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "0.12",
  "reviewAttempt": "r1",
  "description": "Reconciliation of review attempt r1 for review set 0.12 (phase 0): copy the three reports to docs/reviews/phase-0-r1-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-0-r1 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 0.verify.r2 and 0.12.r2a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "0.12.r1a",
   "0.12.r1b",
   "0.12.r1c"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.23",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r1 G1, G2, G7 and G8; Sol finding 2 and Claude finding 1, blocker; Grok finding 1 and Claude finding 3, should-fix required): packages/core/src/sidecar.ts's front-matter writer still disagrees with its reader about what is representable. (a) Key identity: `KEY_LINE` compares literal key spellings, so a double- or single-quoted spelling of an app-owned key is neither a duplicate of the plain spelling nor found on its own; `writeFrontMatter` then rewrites the plain line beside the quoted duplicate (ok:true, the duplicate stays), or appends a second spelling. Rule: any quoted spelling of `title` or `question` in the block makes that key unsupported (FrontMatterUnsupported, root identity, every byte unchanged) whether it appears alone or beside the plain spelling; the block's other keys stay readable. (b) One quoting predicate: `needsQuoting` tests only tab, newline, `#`, `:` and the leading indicator, and the single-quoted branch of `serializeScalar` escapes only the apostrophe, so a replacement value containing U+0000, another C0 or C1 control, DEL, U+2028, U+2029 or a lone surrogate is written raw with ok:true; invariant A then fails on the written document, and `KEY_LINE` (a dot under the u flag without the s flag) classifies the block as malformed on the next read. Derive every quoting decision from one predicate that owns `encodeDoubleQuoted`'s escape table (plus U+2028, U+2029 and lone surrogates), route the single-quoted branch through it (promote to double-quoted rather than emit raw), and match key lines with an explicit character class instead of a dot. (c) The unchanged-value skip runs after the newline refusal, so a no-op write of a double-quoted value whose decoded form contains a newline (which the reader accepted and classified writable) is refused: run the `entry.value === value` skip first and refuse only a value that would actually be rewritten. (d) The rule promoted into CLAUDE.md names an unchanged front-matter value as a corpus-wide identity case and no test carries it. No YAML library, no new dependencies; edits only in packages/core/src/sidecar.ts and packages/core/test/.",
  "acceptance": "Tests assert both the outcome and the untouched bytes for: a quoted duplicate of `question` beside the plain spelling returns FrontMatterUnsupported with `write.root === root` and the block byte-identical (double-quoted, single-quoted and escaped spellings, alone and paired with the plain one); values containing U+0000, U+0007, U+007F, U+0085, U+2028, U+2029 and a lone surrogate either round-trip through a double-quoted scalar with invariant A holding on the written document or are refused before any edit, never written raw; the inverse property readable(v) === !needsQuoting(v) holds over a value table that includes those code points and the four printable strings the existing test uses; a no-op write of a double-quoted value with a decoded newline returns ok:true with `changed` empty and `write.root === root`; a corpus-wide test over every fixture in fixtures/markdown/index.json that carries a yaml node writes each writable app-owned key back with the value just read and asserts `format` output is byte-identical, and asserts a non-writable key returns FrontMatterUnsupported with root identity (fixture names read from the index, never a literal count); the existing sidecar and front-matter-grammar tests still pass; `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.12.r1d"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.24",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r1 G3, Sol finding 3, should-fix required): packages/core/src/sentences.ts rebuilds slices for an identity permutation and for a sentence replaced by its own Markdown, so an inline mark spanning a sentence boundary is split: `**First sentence. Second sentence.**` reordered by [0, 1] becomes `**First sentence.** **Second sentence.**`, and a link spanning two sentences becomes two links with the intervening space moved out of the link. The corpus-wide identity invariant of 0.14 did not catch it because no indexed fixture carries a strong, emphasis, link or delete mark across a sentence boundary. Do three things: (1) after input validation, an identity permutation and a replacement whose Markdown equals the sentence's own (`sentenceMarkdown`) return a tree that formats byte-identically (short-circuit on the validated no-op, not on a string comparison of the output); (2) add LF fixtures under fixtures/markdown with canonical siblings and index.json entries, at least one paragraph each with strong, emphasis, a link and strikethrough spanning a sentence boundary, plus one with a mark spanning the first boundary and one the last, so invariants A/B/C and the 0.14 identity invariant cover them (counts are read from the index and the directory; tests that read the directory keep working); (3) state in the doc comment the rule for a real replacement or reorder under a spanning mark (which sentence keeps the mark and what happens at the boundary) and test it at the first, middle and last position, choosing the rule that leaves the bytes of every untouched sentence unchanged. No new dependencies; edits only in packages/core/src/sentences.ts, packages/core/test/ and fixtures/markdown/.",
  "acceptance": "Sol's two probes pass as tests: the identity reorder of `**First sentence. Second sentence.**` and the self-replacement of the first sentence of `[First sentence. Second sentence.](https://example.com)` both format byte-identically to the input; the new fixtures appear in fixtures/markdown/index.json with canonical siblings and pass invariants A/B/C and the corpus-wide identity invariant (the roundtrip matrix grows by three per fixture, count read from the index, not a literal); the spanning-mark rule for real edits is tested at the first, middle and last positions with the untouched sentence's bytes asserted unchanged; the 30 cases of sentences.cases.json and the existing sentences and zero-width tests still pass; `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.23"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.25",
  "model": "opus",
  "description": "Phase 0 review fix (DECISIONS #review-0-r1 G4 and G5, Sol finding 1 and Claude finding 6, should-fix required; runner code, conformance tests required): (1) `ralph/lib/util.mjs` `breakStaleLock` renames a lock it judged stale before it compares bytes, so between the rename and the compare the lock path is free: with A holding a live lock, a breaker B that read the crashed holder's bytes earlier renames A's lock away, a third entrant C creates the path and runs its critical section beside A, and B's compare fails too late (Sol's probe, `/logs/reviews/0/r1/sol/lock-probe.mjs` and `lock-probe.log`: third entrant inside the critical section while the live holder is active). Cut automatic stale-lock breaking to a manual procedure, as CLAUDE.md prefers: `withLock` never renames or unlinks a lock it did not create; a lock whose pid is dead makes `withLock` throw a `RalphError` naming the file, the dead pid and the manual recovery (confirm with `pgrep` that no `ralph.sh`, `gate.sh` or admin process exists, then remove `.locks/ralph` by hand), and `ralph.sh doctor` reports `stale-lock <pid>` with that same repair (a read-only check in 0.19's pattern). Delete `breakStaleLock` and the `hooks.beforeBreak` interleaving point; keep the token re-read after acquisition and the token-scoped release; rewrite the doc comment. (2) `writeAtomic` swallows a failed directory fsync in a bare catch, so a close intent written without directory durability leaves no trace: on that failure append one line (`writeAtomic: directory fsync failed <path> <code>`) to the audit log the state layer already writes, before continuing. Conformance tests: a stale lock (dead pid) makes `withLock` refuse with the recovery message and nothing under state/ changes; Sol's three-actor interleaving, reproduced with the fs hooks the suite already owns, shows the live holder's path is never freed and the third entrant waits until timeout; `doctor` lists `stale-lock` and is clean after the manual removal; the injected-fs `writeAtomic` test records the audit line when the directory open or fsync throws. docs/RUNNER-SPEC.md §1 already says stale locks are manual (this reconciliation); do not edit it. No new dependencies; the diff touches only ralph/lib/util.mjs, ralph/lib/doctor.mjs, ralph/test/ and the journal.",
  "acceptance": "`bash ralph/test/run.sh` green with the new tests failing before the fix (the journal shows the red run); the identifier `breakStaleLock` no longer appears under ralph/lib/; the existing lock and writeAtomic tests still pass or are rewritten to the new contract with the reason in the journal; `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.24"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.26",
  "model": "sonnet",
  "description": "Phase 0 review fix (DECISIONS #review-0-r1 G6 and deviation 30, Claude finding 2 and Grok finding 2, should-fix required; runner tests and compose only): (1) `ralph/lib/run.mjs` refuses a review start when `accepted.json`'s `sha` differs from `verification_sha` or from `implementation_sha` (the two guards after `readAcceptedCi`), and the review-start block of `ralph/test/conformance.test.mjs` never reaches them: its legs are a missing `accepted.json` and a tampered artifact, both of which throw earlier inside `readAcceptedCi`. Add a third leg: copy a valid accepted tree, rewrite `accepted.json`'s `sha` to another 40-hex value leaving the artifact digests intact, and assert `ralph.sh run --phase 0` exits non-zero, names the gate and the mismatch, and passes `nothingRecorded()` (nothing under `/logs/reviews/`). (2) `docker/compose.yml` gives every service `RALPH_MAX_TURNS` with default 50; the r1 Claude reviewer needed 96 turns for the r1 task text (two attempts ended on max_turns at 50 and 80) and the runner passes no reviewer budget. Give the three review services (`claude-review`, `codex-review`, `grok-review`) their own variable `RALPH_REVIEW_MAX_TURNS` with default 160, mapped onto the `RALPH_MAX_TURNS` the entrypoints read, leaving the task services at 50. Zero new dependencies; the diff touches only ralph/test/conformance.test.mjs, docker/compose.yml and the journal.",
  "acceptance": "`bash ralph/test/run.sh` green, with the journal showing the new leg red while the two guards are temporarily disabled and green with them restored; `docker compose config` with neither variable set renders `RALPH_MAX_TURNS: 160` for the three review services and `50` for claude-task; existing tests still pass; `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "0.25"
  ],
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify.r2",
  "model": "sonnet",
  "description": "Phase 0 verification for review attempt r2 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. `promote-lessons` is a manual step carried by this task's text (DECISIONS #review-0-r1): append this line, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md (identical below line 1; `ralph/check-agent-rules.sh` must stay green): `- A fix that adds several guards has one test per guard, enumerated from the diff rather than from the acceptance sentences, and the journal names the test that discharges each guard; a rule promoted into this file names, in the promoting task's journal entry, the test that discharges each of its clauses.`",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "0.26"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify.r2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full Phase 0 suite (pnpm lint with typecheck, pnpm test with coverage thresholds, cargo test, and ralph/test/run.sh on ubuntu) green on ubuntu, macos and windows at the implementation SHA; the accepted `test-logs` artifact under /logs/ci/0.verify.r2h/accepted/ (the gate id; DECISIONS #018) holds test-logs-ubuntu-latest/, test-logs-macos-latest/ and test-logs-windows-latest/ each with lint.log, test.log and cargo-test.log (ubuntu also ralph-test.log).",
  "phase": "0",
  "execution": "loop",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.verify.r2h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "0.verify.r2",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/0.verify.r2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs"
   ],
   "cleanupRef": true
  },
  "phase": "0",
  "targetBranch": "phase/0",
  "description": "CI gate for 0.verify.r2: scripts/gate.sh 0.verify.r2h pushes integrated_sha of 0.verify.r2 to ci/0.verify.r2/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\"] into /logs/ci/0.verify.r2/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full Phase 0 suite (pnpm lint with typecheck, pnpm test with coverage thresholds, cargo test, and ralph/test/run.sh on ubuntu) green on ubuntu, macos and windows at the implementation SHA; the accepted `test-logs` artifact under /logs/ci/0.verify.r2h/accepted/ (the gate id; DECISIONS #018) holds test-logs-ubuntu-latest/, test-logs-macos-latest/ and test-logs-windows-latest/ each with lint.log, test.log and cargo-test.log (ubuntu also ralph-test.log).",
  "dependencies": [
   "0.verify.r2"
  ]
 },
 {
  "id": "0.12.r2a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "0.12",
  "reviewAttempt": "r2",
  "description": "Review attempt r2, claude reviewer, for review set 0.12 (phase 0). Review set for Phase 0, attempt r2 (after the r1 FAIL, DECISIONS #review-0-r1). Phase checks as r0 and r1: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from /logs/ci/0.verify.r2h/accepted/, three OS directories); invariant table; five further fixtures hand-verified by each reviewer; golden expected/essay-fixture.moved.md hand-verified; fresh-clone cold build. r2-specific checks: each r1 finding's disposition in DECISIONS #review-0-r1 (G1 to G13) verified against the tree (fixed, deferred with a backlog line, or recorded); the front-matter writer of 0.23 probed independently with control characters, line terminators and quoted key spellings; the spanning-mark rule of 0.24 probed independently; the stale-lock refusal of 0.25 driven through `withLock` and `doctor`; the conformance legs of 0.25 and 0.26 read for what they actually exercise. Budget: the reviewer has 160 turns; the report and status.json must be written by turn 130.",
  "acceptance": "/logs/reviews/0/r2/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.r2h"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.12.r2b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "0.12",
  "reviewAttempt": "r2",
  "description": "Review attempt r2, sol reviewer, for review set 0.12 (phase 0). Review set for Phase 0, attempt r2 (after the r1 FAIL, DECISIONS #review-0-r1). Phase checks as r0 and r1: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from /logs/ci/0.verify.r2h/accepted/, three OS directories); invariant table; five further fixtures hand-verified by each reviewer; golden expected/essay-fixture.moved.md hand-verified; fresh-clone cold build. r2-specific checks: each r1 finding's disposition in DECISIONS #review-0-r1 (G1 to G13) verified against the tree (fixed, deferred with a backlog line, or recorded); the front-matter writer of 0.23 probed independently with control characters, line terminators and quoted key spellings; the spanning-mark rule of 0.24 probed independently; the stale-lock refusal of 0.25 driven through `withLock` and `doctor`; the conformance legs of 0.25 and 0.26 read for what they actually exercise. Budget: the reviewer has 160 turns; the report and status.json must be written by turn 130.",
  "acceptance": "/logs/reviews/0/r2/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.r2h"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.12.r2c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "0.12",
  "reviewAttempt": "r2",
  "description": "Review attempt r2, grok reviewer, for review set 0.12 (phase 0). Review set for Phase 0, attempt r2 (after the r1 FAIL, DECISIONS #review-0-r1). Phase checks as r0 and r1: the ralph/ runner (integration mutex, CI-gate expansion, phase-close, evidence mount) reviewed as hard as packages/core; all tests green on 3 OSes (from /logs/ci/0.verify.r2h/accepted/, three OS directories); invariant table; five further fixtures hand-verified by each reviewer; golden expected/essay-fixture.moved.md hand-verified; fresh-clone cold build. r2-specific checks: each r1 finding's disposition in DECISIONS #review-0-r1 (G1 to G13) verified against the tree (fixed, deferred with a backlog line, or recorded); the front-matter writer of 0.23 probed independently with control characters, line terminators and quoted key spellings; the spanning-mark rule of 0.24 probed independently; the stale-lock refusal of 0.25 driven through `withLock` and `doctor`; the conformance legs of 0.25 and 0.26 read for what they actually exercise. Budget: the reviewer has 160 turns; the report and status.json must be written by turn 130.",
  "acceptance": "/logs/reviews/0/r2/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "0.verify.r2h"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.12.r2d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "0.12",
  "reviewAttempt": "r2",
  "description": "Reconciliation of review attempt r2 for review set 0.12 (phase 0): copy the three reports to docs/reviews/phase-0-r2-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-0-r2 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 0.verify.r3 and 0.12.r3a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "0.12.r2a",
   "0.12.r2b",
   "0.12.r2c"
  ],
  "phase": "0",
  "targetBranch": "phase/0"
 },
 {
  "id": "0.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/0 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; write the close intent; one ref transaction moving main and creating phase/1; then finalise .evidence/closes and the next phase base record.",
  "acceptance": "main == reconciliation_sha of the newest Phase 0 review attempt (verdict PASS); suite green on that commit; branch phase/1 exists at main with its base recorded in phases.json; .evidence/closes written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "0.12.r2d"
  ],
  "nextPhase": "1",
  "nextBranch": "phase/1",
  "phase": "0",
  "sourceBranches": [
   "phase/0"
  ],
  "targetBranch": "main"
 },
 {
  "id": "1.1",
  "model": "opus",
  "description": "ProseMirror schema in packages/editor/src/schema.ts mapping 1:1 to §6.1, `mdastToPM(root)` and `pmToMdast(doc)`; html nodes become a non-editable `raw` node rendering a grey box; yaml is held outside the PM doc and re-attached on serialize.",
  "acceptance": "For every fixture listed in index.json (count read from index.json, never a literal): format(pmToMdast(mdastToPM(parse(x)))) === format(parse(x)) (one assertion per fixture, count read from index.json); the raw-html fixture's html node is byte-identical after the trip.",
  "dependencies": [
   "0.close"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.2",
  "model": "opus",
  "description": "Formatter position map in packages/core/src/positions.ts: `formatWithMap(root)` returns the canonical string plus a map from every mdast node (by path) to its `{startLine, startCol, endLine, endCol}` in that string, and `nodeAt(map, line, col)` for the reverse lookup; built by instrumenting remark-stringify's per-node output rather than re-parsing.",
  "acceptance": "For every fixture listed in index.json (count read from index.json, never a literal), every paragraph's mapped startLine equals index.json's recorded line; nodeAt(map, l, c) for 200 seeded random (l, c) positions returns a node whose range contains (l, c) or null only on blank lines; the map is a pure function of root (two calls deep-equal).",
  "dependencies": [
   "0.close"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.3",
  "model": "opus",
  "description": "Typora-style input rules and keymap: `# ` heading, `- `/`1. ` lists, `> ` blockquote, ``` opens a code block, `**x**`/`*x*`/`` `x` `` convert on close, `---` rule, `|a|b|`+Enter makes a 2-col table, Tab/Shift-Tab list indent, Backspace on an empty list item exits the list. No history plugin (undo comes from the store in 1.6).",
  "acceptance": "e2e/web/editor-input.spec.ts (Playwright, dev route /dev/editor) with 22 scenarios typing keystrokes into a blank doc and asserting the resulting canonical Markdown (e.g. '# Title⏎Hello **world**' → '# Title\\n\\nHello **world**\\n').",
  "dependencies": [
   "1.1"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.4",
  "model": "opus",
  "description": "Reveal-on-active-line decorations: Markdown delimiters for emphasis/strong/code/links/headings are shown only on the block containing the cursor and hidden elsewhere.",
  "acceptance": "Playwright: cursor inside a strong span → DOM contains the literal `**` adjacent to the text; cursor moved to another block → removed; heading `#` marks follow the same rule; 8 assertions.",
  "dependencies": [
   "1.3"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.5",
  "model": "sonnet",
  "description": "CodeMirror 6 source view in packages/editor/src/source.ts with lang-markdown and a highlight theme color-coding 7 token classes: headings (by depth), emphasis/strong, links/images, code, blockquotes, list markers, table delimiters; light and dark via prefers-color-scheme; history extension not installed.",
  "acceptance": "Dev route /dev/source renders essay-fixture; Playwright asserts each of the 7 token classes has a distinct computed color in both light and dark (14 assertions).",
  "dependencies": [
   "1.1"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.6",
  "model": "opus",
  "description": "Document store (Zustand) with the undo stack from 0.11 wired to both views: PM transactions push coalesced 'typing' snapshots; Cmd/Ctrl+Z and Shift+Z bound at store level in PM and CM keymaps.",
  "acceptance": "Playwright: type 3 words quickly then Cmd+Z → all 3 gone; type, wait 1.5 s, type, Cmd+Z → only the second burst gone; redo restores it. 6 assertions.",
  "dependencies": [
   "1.4",
   "1.5"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.7",
  "model": "opus",
  "description": "Source toggle: Cmd/Ctrl+/ swaps PM ↔ CM over the store; cursor mapped by (line, ch) ↔ PM position through the position map from 1.2; a toggle closes the active coalescing group but creates no history entry; unparseable intermediate source (unclosed fence) is allowed and re-parses leniently. Dev-only top bar on /dev/editor: 'Load fixture…' and 'Copy Markdown' (for the Phase 1 human gate).",
  "acceptance": "Playwright: cursor in paragraph 7 sentence 2 of essay-fixture, toggle, CM cursor line equals index.json's paragraph line; edit in source, toggle back, rendered text updated; 200 seeded random toggle+edit operations never throw and end with invariant A holding. Undo test: type a word, toggle to source and back, Cmd/Ctrl+Z → the word is gone in one step (no no-op undo); Cmd/Ctrl+Shift+Z restores it.",
  "dependencies": [
   "1.2",
   "1.6"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.8",
  "model": "sonnet",
  "description": "Outline PROTOTYPE (throwaway, dev route /dev/outline): topic-question field, add/nest/drag list of question strings (dnd-kit), 'Produce' button opening /dev/editor pre-populated with front matter `question: <topic>`, one H2 per top-level question and H3 per nested question, each with its question shown as a muted line above the heading. No persistence.",
  "acceptance": "Playwright: enter 3 questions, nest one, click Produce → canonical Markdown is '---\\nquestion: <topic>\\n---\\n\\n## Q1\\n\\n## Q2\\n\\n### Q2a\\n' and three `.question-hint` elements are visible.",
  "dependencies": [
   "1.3"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.9",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE: Michael runs `pnpm dev`, writes ~1,000 words on /dev/editor, toggles source, copies Markdown and checks it in Typora; plays with /dev/outline; records his verdict and any re-scoping of Outline via `scripts/gate.sh 1.9` (evidence file only; the Phase 1 reconciliation copies it to DECISIONS.md#002-outline-feel).",
  "acceptance": "scripts/gate.sh 1.9 wrote .evidence/human/1.9/a<n>.md (accepted.json on ACCEPT) and it validates (the next reconciliation copies it into DECISIONS.md).",
  "dependencies": [
   "1.7",
   "1.8"
  ],
  "blockedOnHuman": true,
  "gateKind": "observation",
  "recordTarget": "002-outline-feel",
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify",
  "model": "sonnet",
  "description": "Phase 1 verification: freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "1.9"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright) green on all three runners at the implementation SHA; logs in /logs/ci/1.verify/accepted/.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify: scripts/gate.sh 1.verifyh pushes integrated_sha of 1.verify to ci/1.verify/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright) green on all three runners at the implementation SHA; logs in /logs/ci/1.verify/accepted/.",
  "dependencies": [
   "1.verify"
  ]
 },
 {
  "id": "1.11",
  "model": "opus",
  "description": "macOS keymap tests exercise the platform route (repair after the 1.verifyh GATE-FAILED, ci.yml run 34333160626 at 3e98707: ubuntu-latest and windows-latest green on every test file, macos-latest 2 test files and 5 tests failed — `packages/editor/test/store.test.ts` › `undoKeymap (rendered view)` › \"undoes on Ctrl+z\", \"redoes on the shifted-letter route, where the event's key is 'Z'\", \"redoes on the keyCode route, where the event's key is not a letter\", \"is installed by storePlugins, ahead of the editing keymaps\", and `packages/editor/test/toggle.test.ts` › `the chord` › \"Cmd/Ctrl+/ reaches a ProseMirror keymap\" — each `expected false to be true` on the handled boolean; the log is /logs/ci/1.verifyh/a1/workflow.log; DECISIONS #020). Principal reading, to be confirmed from that log and from the library source in node_modules before anything changes, never assumed: the bindings are `Mod-z`, `Mod-Z`, `Shift-Mod-z` (`KEY_NAMES` in packages/editor/src/store.ts) and `Mod-/` (`TOGGLE_KEY` in packages/editor/src/toggle.ts); PRD §6.5 says Cmd on macOS and Ctrl elsewhere, so the chord is right and the tests are wrong. `prosemirror-keymap` resolves `Mod-` from `navigator.platform` (`/Mac|iP(hone|[oa]d)/` → Meta, otherwise Ctrl) in a module-level constant evaluated once at import; Node 22 exposes `navigator.platform` from the host OS (`MacIntel` on the macOS runner, `Linux x86_64` in this container), so the five tests, which fire events with `ctrlKey: true` only, encode the non-Mac route and pass on Linux and Windows by accident of platform (CLAUDE.md: never claim a macOS or Windows pass from inside the container). Fix the tests, not the chord. Inject the platform instead of reading the host's: stub `navigator` before the keymap library is imported (the constant is read at import, so this needs module isolation — `vi.stubGlobal` + `vi.resetModules()` + a dynamic import of `prosemirror-keymap` and of the module under test inside the test, or an equivalent the task shows working in the reproduction); never a literal platform list copied from one run. For each of the four chords (`Mod-z`, `Mod-Z`, `Shift-Mod-z`, `Mod-/`) exercise both routes with presence and absence: on a Mac platform a `metaKey` event is handled and a `ctrlKey` event is not; on a non-Mac platform a `ctrlKey` event is handled and a `metaKey` event is not; the `storePlugins` ordering test follows the same rule. One test per guard, enumerated from the diff, and the journal names the test that discharges each guard. No `.skip`, `.only`, `runIf` or `skipIf` — nothing that leaves a route untested on any OS. The CodeMirror binding tests (`undoKeyBindings`, `toggleKeyBindings`) assert binding names, which are platform-independent: leave them as they are. Scope: the two test files plus the journal and lessons; no source edits, no workflow edits, no config edits, zero new dependencies (PRD §4). These are files of passed tasks 1.6 and 1.7, not their records (DECISIONS #016); the repair is authorised by this task.",
  "acceptance": "Reproduction first, recorded in the journal: before the change, `pnpm test` in this Linux container is green, and the same suite with `navigator.platform` forced to `MacIntel` (a temporary vitest `setupFiles` stub or an equivalent, removed again before the commit) fails exactly the five tests named in the description and no other. After the change, `pnpm lint && pnpm test && cargo test` is green both with the host platform and with the platform forced to `MacIntel`, both runs recorded in the journal, and the forcing stub is not committed. Both routes are present in each of the two files: `grep -c 'ctrlKey: true'` and `grep -c 'metaKey: true'` are each non-zero in packages/editor/test/store.test.ts and in packages/editor/test/toggle.test.ts. Each chord has a handled and an unhandled case on each of the two platforms — the journal lists, per chord, the four guards (Mac + meta handled, Mac + ctrl unhandled, non-Mac + ctrl handled, non-Mac + meta unhandled) with the test name that discharges each, and `grep -c 'toBe(false)'` over the two files is non-zero. `grep -cE '\\.(skip|only)\\(|runIf|skipIf'` is zero in both files. `git diff --stat` against the branch base touches only packages/editor/test/store.test.ts, packages/editor/test/toggle.test.ts, docs/lessons.md and docs/progress/journal-main.md. Honest statement: this container is Linux; a forced `navigator.platform` is a proxy, not a macOS pass; the only proof is the `1.verify.g1h` gate green on ubuntu, macos and windows, which this task cannot observe and must not claim.",
  "dependencies": [
   "1.verifyh"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.12",
  "model": "sonnet",
  "description": "Playwright in CI (repair after the 1.verifyh GATE-FAILED; handoff 009 Finding 4, DECISIONS #019 and #020): `.github/workflows/ci.yml` (task 0.2) runs `pnpm lint`, `pnpm test` and `cargo test` and uploads `test-logs`, but installs no Playwright browser, never runs `e2e/web/` and uploads no `playwright-report`, so `1.verify`'s ciAcceptance (\"Full suite + e2e/web (Playwright) green on all three runners\") could never be met — run 34333160626 records `artifacts: []`, and the gate would have failed on the missing artifact had the macOS unit tests not failed first. Edit `.github/workflows/ci.yml` only. After the `Cargo test` step, on all three runners: (1) install the Playwright browser per OS at the version the lockfile pins — `pnpm --filter @essaydown/e2e-web exec playwright install --with-deps chromium` on Linux, the same without `--with-deps` on macOS and Windows (the browser cache is per runner; never assume it is present; the CLI lives only in e2e/web/node_modules/.bin, which the `--filter … exec` spelling reaches); (2) run every spec file under `e2e/web/` with `pnpm --filter @essaydown/e2e-web exec playwright test`, which starts the Vite `webServer` of e2e/web/playwright.config.ts (`pnpm --filter @essaydown/desktop dev`, `cwd` repo root, port 1420), with the HTML reporter writing e2e/web/playwright-report/ on every outcome and never opening a browser (`PLAYWRIGHT_HTML_OPEN=never` and `--reporter=list,html`, or the equivalent), so that any failing spec fails the step and the job — the failure lives in the job, `ciGate` refuses on `conclusion !== success`, and no artifact-content guard is built; (3) upload `playwright-report-${{ matrix.os }}` with `if: always()`, and in the existing `merge-logs` job merge the three into one artifact named exactly `playwright-report` (a second `actions/upload-artifact/merge@v4` step, `pattern: playwright-report-*`, `separate-directories: true`, `delete-merged: true`), so the report exists on failure too. The report is evidence for the 1.10 reviewers, never the pass condition. Keep the workflow on `push` to `ci/**` only; tool versions stay read from docker/versions.env; `shell: bash` on every new step like the rest of the file; no other step changes. No change to e2e/web/playwright.config.ts unless the reporter cannot be set from the workflow (then the smallest config change, stated in the journal); no manifest or lockfile change; zero new dependencies (PRD §4). Windows: the Vite dev server and the `pnpm --filter` spellings have never run there; the journal states that the Windows job log in /logs/ci/1.verify.g1h/a1/workflow.log is the first thing to read if the gate fails.",
  "acceptance": "`git diff --stat` against the branch base touches only .github/workflows/ci.yml, docs/lessons.md and docs/progress/journal-main.md (plus e2e/web/playwright.config.ts only in the case the description allows, stated in the journal). The workflow parses: `node -e` with the `yaml` package already present under node_modules/.pnpm (no new dependency; state the exact command in the journal) loads it and prints the `test` job's step names, and that list contains, in this order, the `Cargo test` step, a browser-install step, a Playwright step and an upload step named `playwright-report-${{ matrix.os }}` with `if: always()`; the `merge-logs` job has a merge step whose `name` is `playwright-report` and whose `pattern` is `playwright-report-*`; no new step is conditioned on `runner.os` except the two browser-install variants (`--with-deps` on Linux only). In this container, after `pnpm install --frozen-lockfile`, the exact browser-install and test command lines copied from the workflow's Linux steps, run from the repo root: `pnpm --filter @essaydown/e2e-web exec playwright test --list` names every spec file that `find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*'` lists (compared as sets; the count is read from the directory, never written as a literal); the run passes with every spec executed; e2e/web/playwright-report/index.html exists afterwards. Then, to show the failure lives in the job: a temporary spec with one failing assertion (deleted again before the commit; `git status` shows no trace of it) makes the same test command line exit non-zero while e2e/web/playwright-report/index.html is still written. `pnpm lint && pnpm test && cargo test` green. Honest statement: this container is Linux and cannot run GitHub Actions; the in-container run proves the command lines, not the workflow; a green `1.verify.g1h` gate proves the e2e suite and the unit suite ran on all three runners at the integrated SHA, which this task cannot observe and must not claim; the Windows job log is the first unknown.",
  "dependencies": [
   "1.11"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.g1",
  "model": "sonnet",
  "description": "Phase 1 verification, repair 1 of 1.verify after the 1.verifyh GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a); DECISIONS #020): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "1.12"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; logs in /logs/ci/1.verify.g1h/accepted/ (the gate id; DECISIONS #018).",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.g1: scripts/gate.sh 1.verify.g1h pushes integrated_sha of 1.verify.g1 to ci/1.verify.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; logs in /logs/ci/1.verify.g1h/accepted/ (the gate id; DECISIONS #018).",
  "dependencies": [
   "1.verify.g1"
  ]
 },
 {
  "id": "1.10.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation).",
  "acceptance": "/logs/reviews/1/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1",
  "reviewSet": "1.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation)."
 },
 {
  "id": "1.10.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation).",
  "acceptance": "/logs/reviews/1/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1",
  "reviewSet": "1.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation)."
 },
 {
  "id": "1.10.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation).",
  "acceptance": "/logs/reviews/1/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1",
  "reviewSet": "1.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation)."
 },
 {
  "id": "1.10.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r1 and 1.10.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r0a",
   "1.10.r0b",
   "1.10.r0c"
  ],
  "phase": "1",
  "targetBranch": "phase/1",
  "reviewSet": "1.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 1. Phase checks: typing latency p95 with 10k words on /dev/editor (< 16 ms or a follow-up task is inserted at reconciliation)."
 },
 {
  "id": "1.13",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F1 — Claude finding 1 blocker, Sol finding 6 required, Grok finding 1; Michael's 1.9 record in #002-outline-feel, escalated by him to a PRD §6 portability defect): a paragraph or heading edited in the rendered view keeps ProseMirror's text verbatim (`leafToMdast`/`pmToMdast`, packages/editor/src/schema.ts), so an ordinary trailing space before Enter reaches `format`, whose `unsafe` rule escapes a space before a line ending as `&#x20;` — `hello ` + Enter + `world` → `hello&#x20;\\n\\nworld\\n`; `- one ` + Enter → `- one&#x20;\\n`. `parse` already discards these spaces (micromark strips the leading and trailing whitespace of a paragraph line), so the editor hands the serializer a tree no Markdown file parses to, and the copied Markdown is not a `format(parse(x))` fixed point. Fix in `pmToMdast` (schema.ts), so every consumer of the editor's tree agrees with `parse` — not in the serializer's unsafe table (Grok: do not weaken it) and not at the DevEditor copy button: strip exactly the whitespace micromark strips at the boundary of a paragraph, heading or table cell — leading ASCII whitespace of the block's first text run and trailing whitespace of its last — with the ownership rule stated once in the doc comment. Untouched: a text run adjacent to a `break` node (a hard break already serialises correctly), inline code, code blocks, and the opaque `raw`/`html`/`yaml` bytes; a run that becomes empty is dropped, not kept as an empty text node. Then close the blind spot Claude's risk 1 names: every round-trip leg today is seeded from `parse(fixture)`, so no test can reach a tree only the editor builds. Add one corpus leg seeded from the editor's own output — for every fixture in fixtures/markdown/index.json, build `mdastToPM(parse(x))`, apply a typing-shaped ProseMirror transaction that appends a space to the end of every paragraph and heading, and assert that `format(pmToMdast(doc))` is a fixed point of parse∘format (`format(parse(out)) === out`) and equals `format(parse(x))`; the same leg on the unchanged doc is the absence case. Title the leg `editor fixed point` in the test name. Playwright on /dev/editor: type `X`, space, Enter, `Y` — the markdown pane reads `X\\n\\nY\\n`; the absence case (no space) reads the same; a list item typed with a trailing space serialises without the entity; a heading typed with a trailing space likewise. One test per guard, enumerated from the diff (leading strip, trailing strip, break-adjacent untouched, inline code untouched, raw untouched, empty run dropped, table cell), the journal naming the test that discharges each. Scope: packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/*.spec.ts, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "The seven guards each have a named unit test in packages/editor/test/ and the journal lists them; the `editor fixed point` corpus leg is green for every fixture listed in fixtures/markdown/index.json (count from the index, never a literal) and is asserted to be able to fail (one hand-built doc with a trailing space and the strip disabled by a test-local shim, or an equivalent, goes red — recorded in the journal, not committed as a skip); the existing schema-roundtrip and roundtrip suites are unchanged and green; Playwright on /dev/editor: the presence case types `X`, space, Enter, `Y` and the markdown pane is exactly `X\\n\\nY\\n`, the absence case is the same string, the list and heading cases contain no `&#x20;`; `grep -rn '&#x20;' e2e/web packages/editor/test` names only assertions of absence; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.10.r0d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.14",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F2 — Sol finding 1 required): the ProseMirror schema in packages/editor/src/schema.ts defines `toDOM` for every node and mark and no `parseDOM` rule (the comment near line 94 says the native clipboard path is unused; it is not), so the editor's own copy → paste in the rendered view reconstructs the pasted HTML as plain text: `# Heading\\n\\n**bold**\\n` selected, copied and pasted over itself becomes `Heading\\n\\nbold\\n` (Sol's `/report/paste.cjs`, reproduced in docs/reviews/phase-1-r0-sol.md). Add `parseDOM` rules paired one-to-one with each `toDOM` — headings by level, paragraph, emphasis, strong, delete/strikethrough, link (href, title), image (src, alt, title), inline code, code block (language), blockquote, ordered list (start) and bullet list, list item, table/row/cell (alignment), thematic break, hard break — preserving every attribute the schema carries; the opaque `raw` atom round-trips by its stored value (a `data-` attribute or equivalent on its `toDOM`, read back by its rule) and its displayed text is never re-parsed as markup, so a pasted raw node cannot become executable HTML. Unit: a test that enumerates `schema.nodes` and `schema.marks` and asserts every one with a `toDOM` has a `parseDOM`, so a future node cannot be added without its rule; if a DOM is available in vitest without a new dependency, the corpus leg `DOMSerializer` → `DOMParser` → `eq` over every fixture in index.json, else the journal says so and the Playwright corpus leg below carries it. Playwright on /dev/editor: for every fixture in fixtures/markdown/index.json (loaded through the dev bar, count from the index), select all, copy, paste over the selection, and assert the markdown pane is byte-identical before and after; plus explicit cases for a heading, strong/emphasis, a nested list, a table, a fenced code block and a raw HTML block, and one case pasting plain text from the clipboard (set with the Playwright clipboard API) into a paragraph that stays plain. Scope: packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "The enumeration test is present and green; the Playwright self copy/paste corpus leg is green for every fixture in index.json and byte-identical before and after (count from the index, never a literal); the six explicit structure cases and the plain-text case are green; the raw-node case asserts the pasted document contains the same `raw` node value and that no element from its text was created in the DOM; the existing 1.1 schema round-trip suite is unchanged and green; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.13"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.15",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F3 — Sol finding 4 required): `formatWithMap` in packages/core/src/positions.ts derives the ranges of nodes the serializer does not dispatch (table rows and cells) from their descendants' spans, so an empty cell or a wholly empty row has no range, and `nodeAt`/the map cannot resolve paths `0.1`, `0.1.0`, `0.1.1` of `| a | b |\\n| - | - |\\n| | |\\n` — the shape the 1.3 table input rule itself creates (`packages/editor/src/input.ts:179`), so a cursor toggle from the new empty body row has no mapping. Give every row and cell an explicit range from the table layout the serializer writes (delimiter positions), so an empty cell maps to its zero-width point between its delimiters; state the ownership rule for the zero-width cell once in the doc comment (CLAUDE.md's half-open-slice rule) and test the first, middle and last empty cells, a wholly empty body row, a table whose header cells are empty, and the cursor toggle from the newly created empty body row on /dev/editor (Playwright). Add one LF fixture with empty cells to fixtures/markdown/ with its index.json entry (paragraphStartLines and nodeTypes filled the way 0.22 did), so the positions-corpus test's \"every node has a range\" assertion covers cells with no descendants; the map stays pure (no DOM). Scope: packages/core/src/positions.ts, packages/core/test/**, fixtures/markdown/**, e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "Each of the six guards has a named unit test and the journal lists them; `nodeAt` resolves every path of the reproduction table and the new fixture (presence) and still rejects the root and out-of-range paths (absence); the positions-corpus suite is green for every fixture in index.json including the new one; invariants A/B/C green for the new fixture; Playwright: on /dev/editor create a table with the input rule, place the caret in the empty body cell, toggle to source, and the source cursor sits inside that cell's delimiters (line and ch asserted from the canonical text written in the test); `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/core/src/positions.ts, packages/core/test/**, fixtures/markdown/**, e2e/web/**, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.14"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.16",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F4 — Sol findings 2 and 3 required; one class: source ↔ rendered cursor coordinates): (a) `apps/desktop/src/dev/DevEditor.tsx:92` passes the raw CodeMirror (line, ch) to the cursor map built from the canonical serialization, so a source edit that changes the canonical line layout moves the next rendered edit — `alpha\\n\\n\\n\\nbeta` with the cursor before `beta`, toggle, type `X` gives `alpha\\n\\nbetaX\\n`, expected `alpha\\n\\nXbeta\\n`; (b) `packages/editor/src/toggle.ts:233` maps character offsets ignoring escapes and continuation prefixes, so even canonical source lands the cursor on the wrong character — in `a\\*b c` the PM position before `b` maps to source ch 2 (before `*`) instead of ch 3; in `> alpha\\n> beta gamma` the position before `beta` maps to line 2 ch 0 instead of ch 2. Fix: translate the live source cursor through the parsed node and its offset within that node into canonical coordinates before consulting the map (parse the live buffer and map through its own node/offset), and carry the serializer's per-character transformations (escapes, continuation prefixes, marker widths, indentation) in both directions, rather than assuming one source character per text character — if the formatter must export an offset table for this, add it to packages/core/src/positions.ts as a pure function and say so in the journal. Tests assert independently derived locations (Sol's lesson: a round trip through two mutually wrong functions, or an in-bounds check, proves nothing): extra blank lines, a Setext heading (`Alpha\\n=====`) and a `* item` list spelling, an escaped character, a blockquote prefix, a nested-list prefix, an emphasis marker, each with the expected (line, ch) and PM position written by hand from the canonical text; the translation creates no undo entry (the existing toggle/undo test stays green). Playwright: Sol's two reproductions as specs, asserting the exact markdown pane after the typed `X`. Scope: packages/editor/src/toggle.ts, apps/desktop/src/dev/DevEditor.tsx, packages/editor/test/**, e2e/web/**, packages/core/src/positions.ts and packages/core/test/** only for the exported offset table, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "Each of the seven hand-derived cases has a named unit test and the journal lists them with the expected coordinates; both Playwright reproductions are green with the exact expected markdown; the 200-seeded-positions toggle test of 1.7 and the toggle/undo test stay green; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only the files named in the scope, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.15"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.17",
  "model": "opus",
  "description": "Phase 1 follow-up required by task 1.10's phase check (\"typing latency p95 with 10k words on /dev/editor: < 16 ms or a follow-up task is inserted at reconciliation\"; DECISIONS #review-1-r0 F5 — Claude findings 2 and 7, Sol finding 5 required). Measurements at r0, all Linux headless Chromium, Vite dev build: rendered view p95 15.0 / 13.9 / 14.1 ms at 10k words and 26.3 ms at 20k (Claude: keydown → first mutation of the markdown pane, both timestamps in-page, 120 presses); 38.6 / 39.5 ms (Sol: beforeinput → the next timer task after input and observer work); source view p95 40.6 ms at 10k words (Claude, same harness as its rendered run). The two protocols differ by 2.5× on the same tree, so the number is protocol-bound; the cost is not: every rendered doc-changing transaction runs `pmToMdast` over the whole document (`packages/editor/src/store.ts:167`), the /dev/editor snapshot re-runs `format(root)` on every `useSyncExternalStore` read (`apps/desktop/src/dev/DevEditor.tsx:72`), and every source-view keystroke re-parses the whole buffer (`packages/editor/src/toggle.ts:479`; `parse` is 37 ms at 10k words against `format` 0.9 ms). Three changes. (1) Cache the formatted snapshot by root identity so a read whose root is unchanged returns the same string instance without serialising. (2) Commit the source buffer on the coalescing boundary, not per update: schedule `parse` + commit on the `SOURCE_KEY` burst window (or an idle callback bounded by it), the CodeMirror buffer staying authoritative for what the user sees in between, the binding's `shownText` echo guard unchanged, undo grouping unchanged, and a toggle or an unmount flushing the pending commit first so nothing typed is lost (a test for the flush). (3) One Playwright spec, e2e/web/latency.spec.ts, whose doc comment states the protocol: a document generated in-test at 1k and 10k words of agent-written prose (never a personal text), the caret in a mid-document paragraph, N single-character presses after W warm-ups, latency = keydown timestamp to the first mutation of the markdown pane, both in-page; p50/p95 printed to the test output and attached as Playwright annotations, for the rendered view and for the source view. The assertion is a relationship, never a magnitude from one run (CLAUDE.md): p95 at 10k words is within a stated factor of p95 at 1k words, the factor and its reason in the spec's doc comment; the absolute numbers are evidence for the 1.10.r1 reviewers, who judge the 16 ms criterion from them under this protocol — never a CI pass condition on shared runners. Incremental `pmToMdast` is not built here: append the option with the r0 numbers to docs/V1.1-BACKLOG.md. Scope: packages/editor/src/store.ts, packages/editor/src/toggle.ts, apps/desktop/src/dev/DevEditor.tsx, packages/editor/test/**, e2e/web/**, docs/V1.1-BACKLOG.md, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "Unit tests per guard, named in the journal: an unchanged root yields the identical snapshot string instance and a changed root a new one; N rapid source updates inside the burst window produce one store commit and one undo entry, and an update after the window a second; a toggle and an unmount flush the pending source commit; the existing 1.6/1.7 undo and toggle suites stay green. The latency spec runs inside the ordinary `playwright test` invocation (so 1.verify.r1h runs it on all three runners) and is green; the journal records p50/p95 at 1k and 10k words for the rendered and the source view, before and after the changes, measured in this container under the spec's protocol, with the honest statement that they are one machine's numbers; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.16"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.18",
  "model": "sonnet",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F6 — Claude finding 3, Grok risk 3; discharges backlog lines #review-0-r2 H2 and H9, whose revisit trigger \"the Phase 1 task that first calls `writeFrontMatter` from the shell\" fired at task 1.8 without anyone looking): `apps/desktop/src/dev/outline-document.ts:41` writes the Outline topic into `question:` through `writeFrontMatter`, and `needsQuoting` in packages/core/src/sidecar.ts ignores YAML core-schema implicit resolution, so the topics `no`, `true`, `null`, `~`, `123`, `1.5`, `.inf`, `2026-01-01`, `off`, `y` are written bare and a third-party YAML reader (Typora, Obsidian, pandoc) takes them back typed. Add the core-schema implicit resolvers as one clause of `needsQuoting` — booleans `true|false|yes|no|on|off|y|n`, `null|~` and the empty string, integers including `0x`/`0o` forms and signs, floats including exponent forms and `.inf`/`.nan`, ISO dates and timestamps, case-insensitive where YAML 1.1/1.2 are — quoting such values with the grammar task 0.15 defined; extend the value table at packages/core/test/front-matter-boundary.test.ts:231 by one row per resolver (presence: quoted and read back as the same string) and rows that must stay bare (`hello`, `1a`, `no way`, `2026-01-01 essay`), and assert `ok: true` per row before the invariant-A check (H9: the parameterized test near line 203 returns early on `ok: false`, so a regression from escaped to refused stays green today). Playwright outline-produce.spec.ts: a topic `true` produces a quoted `question:` value and the rendered hint shows the string `true`. Scope: packages/core/src/sidecar.ts, packages/core/test/**, e2e/web/outline-produce.spec.ts, docs/lessons.md, the journal. Zero new dependencies (PRD §4); no YAML library.",
  "acceptance": "Every resolver class has a presence row and the bare set an absence row in the value table; each row asserts `ok: true` and invariant A (the written file re-reads to the same value and re-serialises byte-identically); the existing 0.15 grammar tests are unchanged and green; the corpus identity test for `writeFrontMatter` stays green; the Playwright case is green; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/core/src/sidecar.ts, packages/core/test/**, e2e/web/outline-produce.spec.ts, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.17"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.19",
  "model": "sonnet",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F7 — Grok finding 2, Claude finding 4; Michael's decision recorded there): packages/editor/package.json declares `codemirror` `^6.0.2`, the kitchen-sink meta-package, which nothing imports (`grep -rn 'from \"codemirror\"' packages apps` is empty) and which pulls `@codemirror/commands@6.11.0` — CodeMirror's `history` — into pnpm-lock.yaml. That falsifies two comments that document a safety property: `packages/editor/src/source.ts:30-32` (\"`@codemirror/commands` is not a dependency of this package … the surest way to keep it that way is to never import the package it lives in\") and `packages/editor/src/store.ts:40-42` (the same for both editors). The property they protect is PRD §4/§6.5's single snapshot-based undo stack: a later task could import `history` from a package that is legitimately installed and break Cmd/Ctrl+Z with a green suite. Remove the `codemirror` entry; refresh pnpm-lock.yaml by removal only (`pnpm install`, then `git diff pnpm-lock.yaml` shows removed lines only — no addition, no version change of any other package); confirm `@codemirror/commands` is no longer reachable from `@essaydown/editor` (`pnpm why @codemirror/commands` and the lockfile's `packages/editor` importer block; record both in the journal) — if it is still reachable through another declared package, name it in the journal and stop without removing a second package (the reconciliation decides). Then re-verify both comments against the lockfile rather than assuming they became true again: reword each to state what is actually installed, and add one vitest test that reads packages/editor/package.json and asserts `codemirror`, `@codemirror/commands` and `prosemirror-history` are absent from every dependency block, and reads pnpm-lock.yaml's `packages/editor` importer and asserts `@codemirror/commands` is not resolved for it — so the property has a test rather than a comment. The direct `@lezer/highlight` and `@lezer/markdown` entries stay: source.ts imports them by name, which is the correct declaration (relying on them arriving through `@codemirror/lang-markdown` would be a phantom dependency), and the reconciliation commit ratifies them in PRD §4. Scope: packages/editor/package.json, pnpm-lock.yaml, the two comments, one new test file under packages/editor/test/, docs/lessons.md, the journal. No dependency added (PRD §4).",
  "acceptance": "`git diff --stat` against the branch base touches only packages/editor/package.json (one removed line), pnpm-lock.yaml (removed lines only: `git diff pnpm-lock.yaml | grep -c '^+[^+]'` is 0, recorded in the journal), packages/editor/src/source.ts and packages/editor/src/store.ts (comment lines only), one new test file, docs/lessons.md and docs/progress/journal-main.md; `pnpm why @codemirror/commands` output in the journal shows no path from @essaydown/editor; the new test is green and goes red when a scratch copy of the manifest re-adds `codemirror` (recorded, reverted before the commit); every e2e/web spec green in the container (the source view is unchanged); `pnpm lint && pnpm test && cargo test` green.",
  "dependencies": [
   "1.18"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.20",
  "model": "sonnet",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F8 — Michael's rule: a dependency rule caught only at review is a rule with no enforcement point; this is the fourth package to reach a review before anyone asked — `zod` at #review-0-r0 Grok 8, then `@lezer/highlight`, `@lezer/markdown` and `codemirror` at #review-1-r0 — and backlog line `[review-0-r1 (b)]` already named the existing bar scripts as the place, not new machinery): add `scripts/check-deps.mjs` (plain Node, no dependency, the way ralph/generate-tasks.mjs is written) that reads every workspace `package.json` (the globs in pnpm-workspace.yaml plus the root) and every `Cargo.toml` in the Cargo workspace, collects the direct names from `dependencies`, `devDependencies`, `optionalDependencies`, `[dependencies]`, `[dev-dependencies]` and `[build-dependencies]` (workspace-internal `workspace:*` entries excluded), and checks each against the committed allowlist `docs/dependencies.json` — `{ \"npm\": { \"<name>\": \"<PRD §4 row or DECISIONS id that admits it>\" }, \"cargo\": { … } }` — failing, and naming the package, the manifest and the sentence to add, when a direct dependency has no entry, and failing likewise when an allowlist entry names a package no manifest declares, so the list cannot rot. Seed the allowlist from the manifests at this SHA with every entry pointing at its admitting text (the two `@lezer` packages at the PRD §4 Source-editor sentence written by #review-1-r0; `zod` at its §4 row; eslint, prettier, typescript, vitest, `@vitest/coverage-v8`, Playwright, Vite, React, the Tauri CLI/API packages and the `@types/*` packages at the §4 Frontend/Tests/Shell rows). Wire it into the root `lint` script — `eslint . && node scripts/check-deps.mjs && pnpm -r --if-present typecheck` — so it runs on every task's bar and on all three CI runners through the existing `pnpm lint` step: no workflow change, no runner change. Tests (vitest, temp-dir fixtures): an undeclared package fails naming it and its manifest; a stale allowlist entry fails naming it; a `workspace:*` entry is ignored; the real tree passes. Scope: scripts/check-deps.mjs, docs/dependencies.json, package.json (the root `lint` script only), one test file, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "`pnpm lint` runs the check and is green on the real tree (its output line recorded in the journal); mutation: adding a fake package to a workspace manifest in a scratch copy makes `node scripts/check-deps.mjs` exit non-zero naming the package (recorded, reverted before the commit); every allowlist entry names an existing PRD §4 row or DECISIONS id (a test greps docs/PRD.md and docs/DECISIONS.md for each referenced anchor or row label); `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only scripts/check-deps.mjs, docs/dependencies.json, package.json, one test file, docs/lessons.md and docs/progress/journal-main.md; no manifest dependency block or lockfile change.",
  "dependencies": [
   "1.19"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.21",
  "model": "sonnet",
  "description": "Phase 1 review fix (DECISIONS #review-1-r0 F9 — Claude findings 5, 6, 10, 11, Grok finding 3, Sol finding 7's test half; one class: assertions that cannot fail). (a) The source view's undo/redo chords are never dispatched as key events on any platform — packages/editor/test/store.test.ts:423-451 asserts binding-name strings and calls `binding.run` directly, and no e2e presses undo or redo while the source view is focused (e2e/web/editor-toggle.spec.ts:216 toggles back to rendered first); `@codemirror/view` resolves `Mod-` from a module-level platform constant exactly as `prosemirror-keymap` does (the 1.verifyh class, one layer over). Add one e2e in editor-toggle.spec.ts that toggles to source, types two bursts more than a second apart, presses `ControlOrMeta+z` then `ControlOrMeta+Shift+Z` with the source view focused, and asserts the markdown pane after each press. (b) Both redo presses (e2e/web/editor-undo.spec.ts:56, editor-toggle.spec.ts:231) spell `ControlOrMeta+Shift+z`, which Playwright synthesises as `key: \"z\"` with shiftKey — an event no keyboard produces — so `Mod-Z` is never exercised: Claude deleted the `Mod-Z` entries from `undoKeymap` and `undoKeyBindings` and all six tests stayed green. Spell the redo presses `ControlOrMeta+Shift+Z`; keep exactly one `ControlOrMeta+Shift+z` case with a comment naming the keyCode route it covers; then repeat the mutation (delete `Mod-Z` from both tables) and record in the journal which tests go red, reverting before the commit. (c) packages/editor/test/schema-roundtrip.test.ts:57-64 asserts `names.length === Object.keys(index).length` where `names` is `Object.keys(index).sort()` — `x === x` — under a comment claiming it pins the loop's coverage; replace it with an assertion over what the loop produced (collect the names each loop body saw and compare that list to `names`). (d) e2e/web/editor-source.spec.ts:29-43 measures colours on seven probe spans the test appends to `document.body` itself, so a highlighter that assigns no class stays green: for each class the fixture actually produces (heading, link, code, blockquote, list marker, table delimiter — Claude counted live spans for all six) assert a real `.cm-content .essaydown-tok-<name>` node exists and measure the colour on it; keep the probe only for emphasis, which essay-fixture lacks, with a comment saying so. Not in scope, recorded on the backlog: Grok 5's duplicated `keymapOn` helper; depth-specific heading colours (Sol 7). Scope: the named test and spec files, docs/lessons.md, the journal; no source file. Zero new dependencies (PRD §4).",
  "acceptance": "Each of (a)–(d) has a named test and the journal lists them; the mutation record in the journal names at least one test per platform route that went red with `Mod-Z` deleted and states the tree was restored (`git status` clean of the mutation before the commit); the source-view chord e2e is green in the container with the honest statement that only the 1.verify.r1h gate proves it on macOS and Windows; every e2e/web spec green in the container; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/editor/test/**, e2e/web/**, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.20"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.22",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r0 F10; discharges backlog lines #review-0-r2 H3 and H5, whose revisit point was \"the first Phase 1 task that edits packages/core/src/blocks.ts, else the Phase 1 reconciliation appends it as a test-only fix task\" — no Phase 1 task edited blocks.ts): the promoted rule \"every core operation of the shape f(root, …) → root has a corpus-wide identity test\" is discharged for `reorderSentences`, `replaceSentence` and `writeFrontMatter` only; `moveBlock(root, i, i)`, `moveSection(root, i, i)` and `replaceBlock(root, id, ownNode)` have synthetic tests (Claude ran all three over the corpus at r2 and at r0 of this phase: byte-identical today — a missing guard, not a bug). Add the corpus leg for the three: for every fixture in fixtures/markdown/index.json each no-op re-serialises byte-identically, and one non-identity case per operation on one fixture proves the leg can fail. H5: packages/core/test/fixtures-corpus.test.ts:116 builds the node-type union from index.json metadata, so the corpus-coverage claim certifies itself — build the union from the parsed trees and assert each index entry's `nodeTypes` against its own tree, presence and absence. No source change: if a test finds a defect, stop, record it in the journal and lessons and do not fix it (the reconciliation decides). Scope: packages/core/test/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4).",
  "acceptance": "Three identity legs and three non-identity cases are present and green (fixture count from the index, never a literal); the rebuilt node-type assertion is green and goes red when one index entry's `nodeTypes` is altered in a scratch copy (recorded, reverted before the commit); `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/core/test/**, docs/lessons.md and docs/progress/journal-main.md; no source, manifest or lockfile change.",
  "dependencies": [
   "1.21"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r1",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r1 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. `promote-lessons` is a manual step carried by this task's text (DECISIONS #review-1-r0): append this line, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md (identical below line 1; `ralph/check-agent-rules.sh` must stay green): `- Every round-trip family (parser ↔ serializer, mdast ↔ ProseMirror, the position map) has one corpus leg seeded from the writing surface's own output (a ProseMirror doc changed by a typing-shaped transaction, a mode operation's result), not only from parse(fixture); the test title names the leg, and the editor's output is asserted to be a fixed point of parse∘format.` The journal names the test that discharges the clause (the `editor fixed point` leg of 1.13).",
  "acceptance": "pnpm lint (which now includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "1.22"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r1: scripts/gate.sh 1.verify.r1h pushes integrated_sha of 1.verify.r1 to ci/1.verify.r1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r1"
  ]
 },
 {
  "id": "1.10.r1a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r1",
  "description": "Review attempt r1, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r1 (after the r0 FAIL, DECISIONS #review-1-r0). Phase checks as r0: typing latency p95 with 10k words on /dev/editor, now measured under the 1.17 protocol (e2e/web/latency.spec.ts doc comment) and reported with the protocol named — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r1-specific checks: each r0 finding's disposition in DECISIONS #review-1-r0 verified against the tree (fixed, deferred with a backlog line, or recorded); the `editor fixed point` leg of 1.13 and the clipboard round trip of 1.14 probed independently by typing and pasting in a browser, not only by reading the tests; the cursor cases of 1.16 checked against hand-derived positions; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; the 1.9 record in DECISIONS #002-outline-feel read for anything the fix chain should have taken and did not.",
  "acceptance": "/logs/reviews/1/r1/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r1b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r1",
  "description": "Review attempt r1, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r1 (after the r0 FAIL, DECISIONS #review-1-r0). Phase checks as r0: typing latency p95 with 10k words on /dev/editor, now measured under the 1.17 protocol (e2e/web/latency.spec.ts doc comment) and reported with the protocol named — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r1-specific checks: each r0 finding's disposition in DECISIONS #review-1-r0 verified against the tree (fixed, deferred with a backlog line, or recorded); the `editor fixed point` leg of 1.13 and the clipboard round trip of 1.14 probed independently by typing and pasting in a browser, not only by reading the tests; the cursor cases of 1.16 checked against hand-derived positions; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; the 1.9 record in DECISIONS #002-outline-feel read for anything the fix chain should have taken and did not.",
  "acceptance": "/logs/reviews/1/r1/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r1c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r1",
  "description": "Review attempt r1, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r1 (after the r0 FAIL, DECISIONS #review-1-r0). Phase checks as r0: typing latency p95 with 10k words on /dev/editor, now measured under the 1.17 protocol (e2e/web/latency.spec.ts doc comment) and reported with the protocol named — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r1-specific checks: each r0 finding's disposition in DECISIONS #review-1-r0 verified against the tree (fixed, deferred with a backlog line, or recorded); the `editor fixed point` leg of 1.13 and the clipboard round trip of 1.14 probed independently by typing and pasting in a browser, not only by reading the tests; the cursor cases of 1.16 checked against hand-derived positions; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; the 1.9 record in DECISIONS #002-outline-feel read for anything the fix chain should have taken and did not. Grok-specific (DECISIONS #review-1-r0, reviewer-shape record: Grok's read-only vantage is kept on purpose — it finds what manifests and logs show, which the two cold-build reviewers share a blind spot for): Grok reads the tree and /logs only and cites /logs/ci/1.verify.r1h/accepted/ for the cold build; for each r0 finding of Claude and Sol that Grok did not raise or rated lower, state whether a read-only review could have found it and, if so, why Grok's severity differed; and state explicitly the verdict Grok gives if any r0 blocker or required finding is not discharged in the tree.",
  "acceptance": "/logs/reviews/1/r1/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r1d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r1",
  "description": "Reconciliation of review attempt r1 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r1-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r1 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r2 and 1.10.r2a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r1a",
   "1.10.r1b",
   "1.10.r1c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.23",
  "model": "sonnet",
  "description": "Phase 1 review fix (DECISIONS #review-1-r1 G1 — Claude finding 1 blocker, Sol finding 1 required, Grok finding 3; a regression introduced by task 1.17, which is passed and is not edited): `copyMarkdown` in apps/desktop/src/dev/DevEditor.tsx formats `store.getState().document.root` without `sourceBinding.current?.flush()`, so while a source-view burst is pending (1.17 commits the buffer one coalescing window after the last keystroke) the button copies the last committed root and reports \"Copied Markdown\" — Claude: load `alpha\\n`, Cmd+/, type `BETA`, click Copy → clipboard `alpha\\n`; 1.5 s later → `alpha\\nBETA\\n`; Sol's `/report/probes.cjs` the same with `base`/`fresh`. The markdown pane lags for the same window, so the visible cross-check is stale too, and Michael's 1.9 record says he works with that pane always visible. `toggle` (DevEditor.tsx:102) and the unmount flush; the copy button is the reader 1.17's acceptance did not enumerate. Do three things. (1) Enumerate every reader of the store outside the two bindings from the code, not from the task text — `grep -n \"getState()\" apps/desktop/src packages/editor/src` — and list each site in the journal with whether it settles the pending source burst first, so the next reader (Phase 2 autosave, the sidecar writer) starts from the list; the source-view Undo/Redo readers are G2 and belong to task 1.24 — list them, do not fix them here. (2) `copyMarkdown` flushes the source binding before it reads the root (its first statement); the status text is written only after the clipboard write succeeds, as today. (3) Playwright, in e2e/web/editor-clipboard.spec.ts or a new spec: the presence case toggles to source, types inside the window, clicks Copy Markdown at once, and asserts the clipboard equals the typed document with no pause and no toggle; the absence case makes the same copy after the window has elapsed and reads the same string; the rendered-view copy is unchanged. Scope: apps/desktop/src/dev/DevEditor.tsx (the one function), e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1).",
  "acceptance": "The journal lists every `getState()` reader outside `bindProseMirror`/`bindCodeMirror` with its flush status; the presence case (copy inside the burst window) is green and is asserted to be able to fail (the flush removed in a scratch edit turns exactly that case red — recorded, reverted before the commit); the absence case and the existing clipboard corpus are green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only apps/desktop/src/dev/DevEditor.tsx, e2e/web/**, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.10.r1d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.24",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r1 G2 — Sol finding 2 required; the sibling of G1 in the seam task 1.17 opened): the source view's Undo and Redo (`undoKeyBindings`, packages/editor/src/store.ts) run on the store's committed history while the burst being typed is still pending in `bindCodeMirror` (packages/editor/src/toggle.ts), and the binding's `pull` drops the pending text when the undone root arrives. Sol's `/report/cursor-undo.cjs`, two reproductions: (a) fresh route, type `first` in source, Undo at once → the surface still reads `first` and the timer commits it afterwards, so the Undo did nothing; (b) type `first`, wait 1.2 s, type ` second`, Undo then Redo at once → `first\\n`, never `first second\\n` — the pending ` second` is lost for good. Two causes; fix both. First, a history command settles the pending burst before it moves history: flush the binding, then undo or redo, from one seam (the binding installs the source view's undo keymap itself, or `sourceUndoKeymap` takes a `beforeHistory` hook the DevEditor wires to the binding's `flush` — choose, and say why in the doc comment). Second, the deferred commit's `at` is the burst's own keystroke time, recorded in `change`, not `now()` at the moment the timer or a flush fires: with `at: now()` a flush 0.25 s after the previous commit coalesces a burst the user typed 1.2 s after the previous one into the previous entry (which is why (b) removed `first` too), and a copy or toggle flush followed by more typing opens a second undo entry for one burst (1.17's own lesson names this). State the grouping rule once in `bindCodeMirror`'s doc comment: the chain of keystrokes that becomes one commit is the chain that became one coalesced entry before 1.17, whether the commit is timed or flushed. Tests with the injected clock (`now` and `schedule` options of `bindCodeMirror`), one per case, named in the journal: fresh history + Undo inside the window empties the surface; a completed burst, a second burst after more than the window, Undo removes only the second and Redo restores it; a flush inside a burst followed by more typing inside the window yields one undo entry, not two; two bursts separated by more than the window yield two entries whether the second is flushed or timed; a pending edit after an Undo is committed on its own and Redo is then unavailable, as in the rendered view. Playwright in e2e/web/editor-toggle.spec.ts (the 1.21 source-view chord test is the model): the two reproductions above as real chords inside the window, asserting the markdown pane and the surface after each press. Scope: packages/editor/src/toggle.ts, packages/editor/src/store.ts, apps/desktop/src/dev/DevEditor.tsx (wiring only), packages/editor/test/**, e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1).",
  "acceptance": "One unit test per case above, green, each named in the journal against its case; the existing 1.6/1.7/1.17 undo, toggle and burst suites unchanged in meaning and green (a changed expectation is explained in the journal against the grouping rule); the two Playwright reproductions green and asserted able to fail (the flush before history removed in a scratch edit turns them red — recorded, reverted before the commit); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.23"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.25",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r1 G3 — Claude finding 3, Sol finding 3 required, Grok finding 2 — and G4 — Claude finding 2; one class: whitespace micromark normalises that the editor's tree keeps, the F1 family that task 1.13 closed at the block's two ends only). G3: typing anywhere in a paragraph that holds a Markdown soft line break rewrites every line break in it to a space — `alpha\\nbeta gamma\\n\\nsecond para\\n`, caret at the end of the first paragraph, `X` → `alpha beta gammaX\\n\\nsecond para\\n` (Claude); `alpha\\nbeta\\n` + `X` → `alpha betaX\\n` (Sol); `> alpha\\n> beta gamma` + `X` before `beta` → `> alpha Xbeta gamma\\n` (Grok; e2e/web/editor-cursor.spec.ts:89 asserts this as the expected pane). Known since 1.16 (docs/lessons.md [1.16]) and never disposed. Cause: the schema's `preserveWhitespace: \"full\"` on the parseDOM rules (the `INLINE_WHITESPACE` decision, packages/editor/src/schema.ts:151-166) governs the clipboard parse only; prosemirror-view's DOM-change parse (`readDOMChange`, domchange.ts) derives its whitespace mode from the parent node type's `whitespace` property, and `paragraph`, `heading` and `table_cell` say nothing, so the `\\n` in the text node is normalised to a space on the first keystroke. The canonical formatter preserves soft breaks (`format(parse(\"alpha\\nbeta gamma\\n\"))` is byte-identical), so this is the editor rewriting the user's own line layout (PRD §6, invariant C). Fix: `whitespace: \"pre\"` on the three inline-content node specs so both parse paths keep the `\\n` (verify in the browser which of the two settings is still needed and say so in the doc comment, rewritten to name the typing path); a soft line break stays a `\\n` inside a `text` node, as mdast writes it — no new node type. G4: the strip 1.13 added (`trimBlockEnds`) stops at any atom, and its doc comment justifies stopping with \"a hard break already serialises to bytes that parse back\", which is true before the break and false after it: micromark strips the leading whitespace of the continuation line (CommonMark §6.7), so a space typed at the start of the line after a hard break is again a tree no file parses to and `format` writes `&#x20;` — load fixtures/markdown/hard-break.md, caret at the start of the second line, type one space → `First line of the paragraph\\\\\\n&#x20;Second line after a hard break.\\n` (Claude; guard 3 of editor-fixed-point.test.ts asserts the tree and never `format`, so it pins the defect as correct). With soft breaks now surviving typing, the same rule applies on both sides of every `\\n` inside a text run (CommonMark §6.8: spaces at the end of a line and at the start of the next are removed). Enumerate the boundaries from micromark's rules, not from the three reproductions — block start and end (kept from 1.13), after a hard break, before and after each soft line break, a whitespace-only line between two soft breaks (a blank line, hence a paragraph break, in any Markdown file: decide by the fixed point, splitting the block or collapsing the run, and state the decision) — and give each one a guard that asserts the serialised bytes (`format(root)` and `not.toContain(\"&#x20;\")`), first, middle and last positions for the soft-break case; restate the ownership rule once in the doc comment (each soft break owns the whitespace on both its sides; a hard break owns the whitespace after it; the block owns its two ends); inline code, code blocks, `raw` and the opaque bytes stay untouched. Then the corpus legs: if no fixture in fixtures/markdown/index.json holds an ordinary paragraph, a blockquote and a list item each wrapped over two source lines and a hard break followed by an indented continuation line, add `soft-line-breaks.md` (LF, agent-written prose, never a personal text) with its `.canonical.md` and index entry (1.15's pattern); a second `editor fixed point` leg in packages/editor/test/editor-fixed-point.test.ts, titled for typing *inside* a block — for every fixture, insert one letter after the first character of every paragraph's first text run and at the start of the second line of every text run that holds a soft break, and assert `format(pmToMdast(doc))` is a parse∘format fixed point and differs from `format(parse(x))` by exactly the inserted letters; the existing block-end leg unchanged. Playwright on /dev/editor: `alpha\\nbeta gamma` + `X` at the end reads `alpha\\nbeta gammaX\\n`; editor-cursor.spec.ts:89 becomes `> alpha\\n> Xbeta gamma\\n` (the tripwire the 1.16 comment named); a space typed at the end of the first line of a wrapped paragraph and a space typed at the start of the continuation line after a hard break each serialise without `&#x20;`; the absence case (a paragraph with no soft break) is unchanged. Scope: packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, fixtures/markdown/** (additive only), docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1).",
  "acceptance": "One guard test per boundary enumerated above, each asserting `format` bytes, listed in the journal; both `editor fixed point` legs green for every fixture in fixtures/markdown/index.json (count from the index, never a literal) and the new leg asserted able to fail (the strip disabled by a test-local shim goes red — recorded, not committed); the fixture, if added, has its index entry and its canonical file and passes the Phase 0 corpus suites (invariants A, B, C, positions, sidecar); the four Playwright cases green, with `grep -rn '&#x20;' e2e/web packages/editor/test` naming only assertions of absence; the schema-roundtrip, roundtrip, parse-dom and clipboard suites green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.24"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.26",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r1 G5 — Grok finding 1 required, Claude finding 5; G8 — Grok finding 4; and G7's comment — Claude finding 4). (a) `undoKeymap` (packages/editor/src/store.ts, the rendered view) still has no test that reads its own key list: deleting the `Mod-Z` entry leaves every unit test and every e2e green, because `prosemirror-keymap`'s keyCode fallback answers `Shift-Mod-z` with the same command (Claude repeated 1.21's mutation at r1: 1845/1845 and 59/59 stayed green). 1.21 recorded the sibling as a lesson; a lesson line is not a disposition. Add, beside the source view's `undoKeyBindings` \"binds the same three names\" test in packages/editor/test/store.test.ts, one test asserting that `Object.keys(undoKeymap(store)).sort()` equals the sorted list of names `undoKeyBindings(store)` binds (one list for both views, read from the table, never a literal); repeat the mutation (delete the `Mod-Z` entry from `undoKeymap` only) and record in the journal that exactly this test goes red and no other, reverting before the commit. (b) e2e/web/editor-clipboard.spec.ts:99 asserts `checked === Object.keys(index).length` where `checked` counts a loop over `Object.keys(index).sort()` — `x === x`, the class 1.21 removed from schema-roundtrip.test.ts under F9(c): collect the names the loop actually loaded and compare that list to `names`. (c) vitest.config.ts:14's coverage `include` is `packages/*/src/**` only, so `apps/desktop/src/**` (the dev routes, where r0's F3 and F4 and r1's G1 landed) is measured at zero and reported as nothing: add one comment above the glob stating what it deliberately excludes and which suite covers it instead (the e2e/web Playwright specs), so \"not measured\" is a decision in the file; the include itself changes at Phase 2 planning (backlog line, hard stop 2.verify). No source file. Scope: packages/editor/test/store.test.ts, e2e/web/editor-clipboard.spec.ts, vitest.config.ts (a comment only; no threshold, no glob change), docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1).",
  "acceptance": "The `undoKeymap` key-list test is present and green and the journal records the mutation (which single test went red); the clipboard spec's count assertion compares the loaded names to `names` and is green; `git diff vitest.config.ts` is comment lines only; `pnpm lint && pnpm test && cargo test` green (coverage thresholds unchanged and holding); every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the four scope files, docs/V1.1-BACKLOG.md (only if a line is added) and docs/progress/journal-main.md; no source, manifest or lockfile change.",
  "dependencies": [
   "1.25"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.27",
  "model": "opus",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r1 G6 — Sol finding 4 required): the rule task 1.verify.r1 promoted into CLAUDE.md/AGENTS.md names three round-trip families — parser ↔ serializer, mdast ↔ ProseMirror, the position map — and the journal discharged it with 1.13's `editor fixed point` leg, which exercises the first two and never calls `formatWithMap`, `nodeAt` or `cursorMap`; packages/core/test/positions-corpus.test.ts seeds every case from `parse(sourceOf(name))`. Add the position-map leg over editor output, in packages/editor/test (the editor depends on core, never the reverse), titled for the leg: for every fixture in fixtures/markdown/index.json, `doc = mdastToPM(parse(x))`, apply the typing-shaped transactions the existing legs use (the block-end space of 1.13 and the in-block letter of 1.25 — reuse their helpers, do not copy them), `root2 = pmToMdast(doc2)`, `{ text, map } = formatWithMap(root2)`, and assert: `text === format(root2)` and `format(parse(text)) === text`; when `text` equals `format(parse(x))` (the block-end leg, whose space is stripped) the map deep-equals `formatWithMap(parse(x)).map`; every paragraph's mapped `startLine` equals index.json's `paragraphStartLines` for the in-block leg too (a letter adds no line); 200 seeded `nodeAt(map, l, c)` positions return a node whose range contains (l, c) or null only on a blank line (the 1.2 clause, now over editor-seeded roots); and, for the in-block leg, `cursorMap(root2, doc2).toSource(pos)` at the position after each inserted letter names the line and column where `text` holds that letter (the map's consumer, seeded from the editor). One negative case proves the leg can fail (a hand-built doc whose map is checked against a deliberately shifted line). Scope: packages/editor/test/**, docs/lessons.md, the journal; no source file — if the leg finds a defect, stop, record it in the journal and lessons with a docs/V1.1-BACKLOG.md line carrying a revisit trigger and a hard stop, and do not fix it (the reconciliation decides). Zero new dependencies (PRD §4).",
  "acceptance": "The editor-output position-map leg is green for every fixture listed in the index (count from the index, never a literal), its title names the leg and the family, the journal names it as the test that discharges the position-map clause of the promoted rule, and the negative case is green; `pnpm lint && pnpm test && cargo test` green; `git diff --stat` against the branch base touches only packages/editor/test/**, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no source, manifest or lockfile change.",
  "dependencies": [
   "1.26"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r2",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r2 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. `promote-lessons` is a manual step carried by this task's text (DECISIONS #review-1-r1): append this line, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md (identical below line 1; `ralph/check-agent-rules.sh` must stay green): `- Every reader of the document store that is not an editing surface (copy, undo/redo, toggle, unmount, save, export) settles the pending source burst before it reads, and a new reader's test is an action inside the coalescing window; a read that can lag the buffer by a window is a stale-read defect, not a timing quirk.` The journal names the tests that discharge the clause (1.23's copy-inside-the-window case and 1.24's undo-inside-the-window cases). Second, the procedure change Michael decided at #review-1-r1 (nine of ten failed first attempts in Phase 1 were `NO-JOURNAL` at the turn cap, each costing one of three attempts and a manual restart): in the `## Every iteration` list of both files, replace step 2 and step 3 with these three steps, verbatim, renumbering the list so it reads 1–6: `2. Before implementing, append a stub journal entry to `docs/progress/journal-main.md` — `- [<id>] <ISO> Task: <the task's first sentence>. Status: In progress.` — and commit it as `wip(<id>): journal stub`, so an attempt cut off at the turn cap leaves a checkpoint the runner reads as a journal entry instead of nothing (a capped attempt still counts toward the three; RUNNER-SPEC §4.3).`, `3. Implement, then verify every acceptance sentence. From task 0.1 onward `pnpm lint && pnpm test && cargo test` must be green (root `Cargo.toml` is a workspace so `cargo test` runs from the root). Never `.skip()` a test to get green; file the gap in the journal instead.`, `4. Complete your own stub in place, using the template in `docs/progress.md` (never a commit SHA; never edit `docs/progress.md`, it is generated; never touch another task's line — the integrated commit appends exactly one line per attempt). Append to `docs/lessons.md` (`[<id>] <ISO> LESSON: <root cause> → <do instead>`) when you learned something; never edit an old line.`; the former steps 4 and 5 become 5 and 6 unchanged. The journal names `ralph/check-agent-rules.sh` and `tests/agent-rules.test.ts` as the tests that discharge the identical-files clause.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "1.27"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r2h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r2h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r2",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r2: scripts/gate.sh 1.verify.r2h pushes integrated_sha of 1.verify.r2 to ci/1.verify.r2/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r2/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r2h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r2"
  ]
 },
 {
  "id": "1.28",
  "model": "opus",
  "description": "Phase 1 verification-gate repair, test-only (repair after the 1.verify.r2h GATE-FAILED, ci.yml run 34499653269 at 6d9345c: ubuntu-latest and macos-latest green on every unit test and every e2e/web spec; windows-latest green on every unit test and on every e2e/web spec except three, all in `e2e/web/editor-clipboard.spec.ts` › `Copy Markdown flushes a pending source burst before it reads the store` — \"presence: copying inside the coalescing window includes the just-typed text, with no pause and no toggle\", \"absence: copying after the coalescing window has elapsed reads the same committed text\", \"the rendered view's copy is unchanged: every keystroke there is already committed, so an immediate copy needs no flush\" — each `expect(clipboard).toBe(\"<word>\\n\")` receiving `<word>` plus an invisible trailing character before the newline; the log is /logs/ci/1.verify.r2h/a1/workflow.log, step `test (windows-latest)` › `Playwright (e2e/web)`; DECISIONS #021). Principal reading, to be confirmed from that log before anything changes, never assumed: the three tests task 1.23 added read the OS clipboard back through `navigator.clipboard.readText()` after `copyMarkdown` (apps/desktop/src/dev/DevEditor.tsx) wrote it with `navigator.clipboard.writeText(text)`; on Windows, Chromium hands plain text to the system clipboard with LF converted to CRLF, so the read returns the written word followed by a carriage return and a newline where the test expects the newline alone. The app wrote the right bytes; the assertion encoded one platform's clipboard convention (the [plan.1.verifyh.r0] lesson: a test that passes on one OS by accident of platform is not a pass), and the spec's own doc comment already says why the OS clipboard is not what a copy case should go through (one shared resource across parallel workers on three OSes). Fix the tests, not `copyMarkdown`. Assert what the app handed the clipboard API, not what the OS did with it: install a spy before the page's scripts run (`page.addInitScript`, before `page.goto`) that replaces `navigator.clipboard.writeText` with a function recording its argument on the window and resolving, and have `clickCopyMarkdown` return the recorded string; the `\"Copied Markdown\"` status assertion stays, so the app's real path (flush → format → writeText → status) is still the thing exercised, and the `clipboard-read`/`clipboard-write` permission grant becomes unnecessary in these three tests (remove it there). Do not normalise the carriage return away in the assertion and do not branch on `process.platform`: the first hides a stray carriage return the app might one day write, the second encodes a platform list. The plain-text paste case (\"a paragraph takes the clipboard's characters, not the markup they spell\") keeps the real clipboard, because pasting foreign content is the case where the source has to be outside the editor; leave it and the `DataTransfer` round-trip cases untouched. State the rule once in the describe block's doc comment: a copy test asserts the string the app wrote; a paste test of foreign content is the one that needs the real clipboard; the carriage return is named in words there, never as an escape in code. Scope: e2e/web/editor-clipboard.spec.ts plus docs/lessons.md and the journal; no source edits, no config edits, no workflow edits, zero new dependencies (PRD §4). The spec is a file of passed task 1.23, not its record (DECISIONS #016); the repair is authorised by this task.",
  "acceptance": "Reproduction first, recorded in the journal: before the change, `grep -c 'navigator.clipboard.readText' e2e/web/editor-clipboard.spec.ts` is non-zero and each of the three named tests reads the OS clipboard through it; the three Playwright diffs from /logs/ci/1.verify.r2h/a1/workflow.log (step `test (windows-latest)` › `Playwright (e2e/web)`) are quoted in the journal as the reproduction, because this Linux container cannot run Windows. After the change, in e2e/web/editor-clipboard.spec.ts: `grep -c 'navigator.clipboard.readText'` is zero; `grep -c 'addInitScript'` is non-zero; `grep -c 'navigator.clipboard.writeText'` is non-zero (the spy names the function it replaces, and the foreign-paste case still writes the real clipboard); `grep -c 'process.platform'` is zero; `grep -c '\\\\r'` is zero (the carriage return is named in words in the doc comment, never normalised in code); `grep -cE '\\\\.(skip|only)\\\\(|runIf|skipIf'` is zero. The three tests are still able to fail for G1: with `sourceBinding.current?.flush();` removed from `copyMarkdown` in a scratch edit (never staged; restored with `git checkout -- apps/desktop/src/dev/DevEditor.tsx` before the commit, `git status` clean of it), the presence case is red and the absence and rendered-view cases stay green, recorded in the journal with the three test names; and the recorded string is asserted byte-exact (`toBe(\"alpha\\n\")` and its two siblings, no normalisation). Every e2e/web spec green in the container (`pnpm --filter @essaydown/e2e-web exec playwright install chromium` first, lessons 1.3/1.4/1.12), including the three named tests; `pnpm lint && pnpm test && cargo test` green; `npx prettier --check e2e/web/editor-clipboard.spec.ts` clean; `git diff --stat` against the branch base touches only e2e/web/editor-clipboard.spec.ts, docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change. Honest statement: this container is Linux; the spy makes the assertion independent of any OS clipboard convention, but the only proof of a Windows pass is the `1.verify.r2.g1h` gate green on ubuntu, macos and windows, which this task cannot observe and must not claim.",
  "dependencies": [
   "1.verify.r2h"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r2.g1",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r2, repair 1 of 1.verify.r2 after the 1.verify.r2h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #021): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. The promote-lessons step and the CLAUDE.md/AGENTS.md procedure change carried by 1.verify.r2 are already integrated at 6d9345c and are not repeated; `ralph/check-agent-rules.sh` must still exit 0.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0.",
  "dependencies": [
   "1.28"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r2.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r2.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r2.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r2.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r2.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r2.g1: scripts/gate.sh 1.verify.r2.g1h pushes integrated_sha of 1.verify.r2.g1 to ci/1.verify.r2.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r2.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r2.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r2.g1"
  ]
 },
 {
  "id": "1.10.r2a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r2",
  "description": "Review attempt r2, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r2 (after the r1 FAIL, DECISIONS #review-1-r1). Phase checks as r1: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r2.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r2-specific checks: each r1 finding's disposition in DECISIONS #review-1-r1 (G1–G12) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r0 disposition (F1–F16) re-confirmed; Copy Markdown and the source view's Undo/Redo probed in a browser inside the coalescing window (1.23, 1.24); typing inside a wrapped paragraph, inside a wrapped blockquote and after a hard break probed in a browser (1.25), and the `&#x20;` family searched for beyond the three r1 reproductions; the position-map leg of 1.27 read for which functions it actually calls; the `undoKeymap` key-list test of 1.26 read against the mutation 1.21 and Claude recorded; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired.",
  "acceptance": "/logs/reviews/1/r2/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r2.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r2b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r2",
  "description": "Review attempt r2, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r2 (after the r1 FAIL, DECISIONS #review-1-r1). Phase checks as r1: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r2.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r2-specific checks: each r1 finding's disposition in DECISIONS #review-1-r1 (G1–G12) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r0 disposition (F1–F16) re-confirmed; Copy Markdown and the source view's Undo/Redo probed in a browser inside the coalescing window (1.23, 1.24); typing inside a wrapped paragraph, inside a wrapped blockquote and after a hard break probed in a browser (1.25), and the `&#x20;` family searched for beyond the three r1 reproductions; the position-map leg of 1.27 read for which functions it actually calls; the `undoKeymap` key-list test of 1.26 read against the mutation 1.21 and Claude recorded; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired.",
  "acceptance": "/logs/reviews/1/r2/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r2.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r2c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r2",
  "description": "Review attempt r2, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r2 (after the r1 FAIL, DECISIONS #review-1-r1). Phase checks as r1: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r2.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r2-specific checks: each r1 finding's disposition in DECISIONS #review-1-r1 (G1–G12) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r0 disposition (F1–F16) re-confirmed; Copy Markdown and the source view's Undo/Redo probed in a browser inside the coalescing window (1.23, 1.24); typing inside a wrapped paragraph, inside a wrapped blockquote and after a hard break probed in a browser (1.25), and the `&#x20;` family searched for beyond the three r1 reproductions; the position-map leg of 1.27 read for which functions it actually calls; the `undoKeymap` key-list test of 1.26 read against the mutation 1.21 and Claude recorded; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired. Grok-specific (DECISIONS #review-1-r0 reviewer-shape record; #review-1-r1 shared-severity count): Grok reads the tree and /logs only and cites /logs/ci/1.verify.r2.g1h/accepted/ for the cold build; for each r1 finding of Claude and Sol that Grok did not raise or rated lower (G1 in particular, rated nit against a blocker), state whether a read-only review could have found it and why the severity differed; and state explicitly the verdict Grok gives if any r1 blocker or required finding is not discharged in the tree.",
  "acceptance": "/logs/reviews/1/r2/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r2.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r2d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r2",
  "description": "Reconciliation of review attempt r2 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r2-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r2 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r3 and 1.10.r3a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r2a",
   "1.10.r2b",
   "1.10.r2c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.29",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r2 H1 — Claude finding 1 blocker; H5 — Claude finding 5; H6 — Claude finding 6; the F1/G4 family in `stripUnparsableWhitespace`, packages/editor/src/schema.ts, a file of passed tasks 1.13 and 1.25, whose records are not edited): the strip's ownership rule stops at every atom and says nothing about an atom that is a block's **last** node, so a `hard_break` left at the end of a paragraph or heading reaches `format`, which writes it as a bare trailing `\\` that `parse` reads back as a literal backslash — `format(parse(\"one\\\\\\n\"))` is `\"one\\\\\\\\\\n\"` and the `break` node is gone. Reproduction (Claude, Chromium at c9408ee): seed `hard-break.md`'s shape through the source view, `Ctrl+End`, three `Backspace` presses — the markdown pane shows `\"one\\\\\\n\"`, `writeText` receives exactly that, the status reads \"Copied Markdown\", and the copied file does not round-trip (PRD §7's gate criterion). A heading whose last node is a `hard_break` is worse: `\"H\\\\\\n\\n\"` reparses as a paragraph, the block type lost (H5). Every one of 1.25's eleven boundary guards asserts whitespace, never an atom, and both corpus legs (packages/editor/test/typing-legs.ts) only ever `insertText`, so a tree that needs a deletion to build is outside every guard. Do four things. (1) Extend the ownership rule, stated once in the doc comment: the block's end takes the trailing whitespace of its last inline node **and drops a trailing `hard_break`**, repeatedly, until the last node is neither a whitespace-only run nor a break — a break with nothing after it is not a line break (CommonMark §6.7: a backslash at a paragraph's end is a literal backslash), dropped the way a run trimmed to nothing is dropped; a block that is only a break becomes an empty block, whatever the empty block's existing fixed point is (state it). (2) The cell clause (H6): a GFM cell cannot hold a line ending (the row would end), so inside a `table_cell` a line-ending run collapses to one space rather than to `\\n`; the cell's own two ends stay boundary 11. (3) Guards in packages/editor/test/editor-fixed-point.test.ts, one per position, each asserting `format(root)` bytes and `format(parse(format(root))) === format(root)`: a break as the block's last node, a block that is only a break, a break followed only by whitespace — each on `paragraph` and on `heading` (the heading's guard pins the block type separately); a line ending inside a cell serialises without `&#xA;`. (4) The class (DECISIONS #review-1-r2, Claude's lesson 2): a third `editor fixed point` corpus leg in the same file, titled for **deletion**, that for every fixture in fixtures/markdown/index.json applies deletion-shaped transactions (`tr.delete`, the call a `Backspace` makes) — delete the whole inline content after every `hard_break`, so the break becomes its block's last node; delete the last character of every block's last text run, so a run can empty and the block end can land on an atom or an inline-code run — and asserts `format(pmToMdast(doc))` is a parse∘format fixed point; the existing two legs unchanged; the journal states how many fixtures each deletion reached (from the loop, never a literal). Playwright on /dev/editor, in e2e/web/editor-soft-line-breaks.spec.ts or a new spec: the reproduction above as a case that deletes rather than types — after the three `Backspace` presses the markdown pane reads `one\\n` and the spy-recorded copy equals it; a heading variant; and, for H6, a multi-line plain-text paste into a table cell (the route the typing keymap does not consume) asserting the pane holds one space and no `&#xA;` — if no browser route reaches a cell's line ending, say so in the journal and keep the unit guard. Scope: packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "One guard per position enumerated above, each asserting `format` bytes and the fixed point, listed in the journal; the deletion leg green for every fixture in the index (count from the index, never a literal) and asserted able to fail (the trailing-break drop disabled by a test-local shim goes red on at least one fixture — recorded, not committed; if no fixture in the index ends a block with a hard break after the deletion, the journal says which guard carries the claim instead); the existing eleven guards and both typing legs green and unchanged in meaning; the Playwright cases green, with `grep -rn '&#xA;' e2e/web packages/editor/test` naming only assertions of absence; the schema-roundtrip, roundtrip, parse-dom and clipboard suites green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.10.r2d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.30",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r2 H8 — Sol finding 2, filed against docs/V1.1-BACKLOG.md:44 and reconciled as a required should-fix: the r1 deferral of the `&#x20;` family's emphasis-edge member rested on the claim that it is unreachable while Phase 1 binds no mark-toggle command, and the claim is false). Reproduction (Sol, `/report/probe.mjs`, ordinary keyboard events): load `a *b* c`, put the rendered caret immediately after `b`, type a space → the pane reads `a *b&#x20;*&#x20;c`. Marks come from input rules and from every loaded file, and a caret at the end of an emphasised word sits inside the mark, so this is a one-keystroke route in the rendered view to the byte family Michael escalated at r0 (DECISIONS #review-1-r0 F1). Cause: `emphasis[text(\"b \")]` is a tree no Markdown file parses to — a closing `*` must be right-flanking (CommonMark §6.2: not preceded by Unicode whitespace) — and the serializer's `unsafe` table faithfully spells the difference as a character reference. Fix, in `stripUnparsableWhitespace` (packages/editor/src/schema.ts; a file of passed tasks 1.13 and 1.25, whose records are not edited) or a sibling it calls: the mark-edge clause of the ownership rule, stated once in the doc comment — ASCII whitespace at the outer edge of an `emphasis`, `strong` or `strikethrough` run belongs **outside** the mark: a marked run's leading whitespace is moved before the mark and its trailing whitespace after it, merged into the unmarked neighbour where there is one (so `text(\"a \"), em(\"b \"), text(\"c\")` becomes `text(\"a \"), em(\"b\"), text(\" c\")`); a run that is whitespace-only loses those marks; nested marks give up the whitespace from every mark whose edge it touches; a link's text keeps its whitespace (`[b ](u)` is a link) and inline code stays opaque; the moved whitespace then falls under the existing line and block rules, so the clause runs first. Enumerate the edges from §6.2's flanking rules, not from the reproduction: leading edge, trailing edge, both, whitespace-only, for each of the three marks, plus a nested case (`strong` inside `emphasis`, whitespace at the shared edge) and the link absence case — one guard per case in packages/editor/test/editor-fixed-point.test.ts, first, middle and last positions in the block for the trailing-edge case, each asserting `format(root)` bytes, `not.toContain(\"&#x20;\")` and the parse∘format fixed point. A fourth `editor fixed point` corpus leg in the same file, titled for the mark edge: for every fixture in fixtures/markdown/index.json, insert one space at the end of every text node carrying `emphasis`, `strong` or `strikethrough` (inside the mark — `insertText` with the node's marks, the shape a caret inside the mark produces), and assert the output is a parse∘format fixed point, contains no `&#x20;`, and differs from `format(parse(x))` only by spaces; the existing legs unchanged. Playwright on /dev/editor: Sol's reproduction as a case — `a *b* c` loaded, caret after `b`, one space → the pane reads `a *b* c\\n`; the absence case (a space typed after the closing delimiter, outside the mark) unchanged; and one case that types `*word*` from nothing (the input rule) followed by a space, asserting the pane reads `*word*\\n` whichever tree the input rule leaves the caret in. Scope: packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "One guard per edge-and-mark case enumerated above, each asserting `format` bytes, the absence of `&#x20;` and the fixed point, listed in the journal; the mark-edge leg green for every fixture in the index (count from the index, never a literal), non-vacuous (the journal states how many marked runs the corpus holds, from the loop), and asserted able to fail (the clause disabled by a test-local shim goes red — recorded, not committed); the existing guards and the other three legs green and unchanged in meaning; the three Playwright cases green, with `grep -rn '&#x20;' e2e/web packages/editor/test` naming only assertions of absence; the schema-roundtrip, roundtrip, parse-dom and clipboard suites green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.29"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.31",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r2 H2 — Sol finding 1 blocker; the seam of passed tasks 1.17 and 1.24, whose records are not edited): a flush inside a continuous source typing burst splits undo history when the continuation outlasts one coalescing window. Reproduction (Sol, `/report/probe.mjs` and `probe.log`): blank /dev/editor, toggle to source, type `a`, click Copy Markdown, return the caret to the source view, type `bcdef` at 300 ms per character, press Ctrl+Z at once → `a` remains (Ctrl+Shift+Z restores `abcdef`); the same burst without the Copy (`control.mjs`) undoes to empty. Sol's keystroke times: 242, 308, 612, 924, 1237, 1543 ms — every adjacent gap under 314 ms, the two commits' keystroke times 1,300 ms apart. Cause: `bindCodeMirror` (packages/editor/src/toggle.ts) keeps one time for the pending segment, `pendingAt`, its **latest** keystroke, and `push` (packages/core/src/undo.ts) decides continuity by one distance, `at - present.at` — the previous entry's last keystroke against the segment's last keystroke — so the adjacent keystrokes between them are invisible, and 1.24's grouping rule (the chain of keystrokes that becomes one commit is the chain that became one coalesced entry before 1.17) holds only for a segment shorter than the window; 1.24's case 3 stops its continuation at 200 ms and never reaches this. Fix: the pending segment carries both its first and its last keystroke time; continuity is decided between the segment's **first** keystroke and the previous entry's last, and the entry keeps the **last** for the next comparison. Thread it through the contract rather than around it: `PushOptions` (core) gains `from` — the earliest keystroke of the batch, defaulting to `at` — and `push` merges when `from` is within the window of the present entry's `at` (a clock that went backwards still starts a new step); the store's `commit` passes it through; the binding records the first keystroke time when a segment opens and the latest on every `change`, and its commit carries both; the rendered view and every other caller are unchanged (`from` defaults to `at`). Restate the grouping rule in the module comment (it is stated once; make it true again: continuity is a property of adjacent keystrokes, so a segment is continuous with the previous entry when its first keystroke is, however long the segment runs). Explicit closure on a toggle and on a history command (`endCoalescing`, `beforeHistory`) is preserved. Tests with the injected clock (`now` and `schedule` options), one per case, named in the journal: (a) a commit, then a continuation whose total duration exceeds the window while every adjacent gap is under it, ending by the timer — one undo entry; (b) the same continuation ending by a flush — one entry; (c) a continuation whose first keystroke lands more than a window after the previous commit — two entries, whether timed or flushed (the genuine-gap absence case); (d) the 1.24 cases unchanged and green; (e) a core test in packages/core/test for `from`: presence (merges when `from` is inside the window and `at` is outside) and absence (`from` omitted behaves as before). Playwright in e2e/web/editor-toggle.spec.ts (the 1.24 reproductions are the model): Sol's reproduction as real chords with `delay: 300` — after Ctrl+Z the pane and the surface are empty; after Ctrl+Shift+Z they read `abcdef\\n`. G9's revisit trigger fires here (DECISIONS #review-1-r1 G9: the first task that changes `bindCodeMirror`'s commit path after 1.24): add the third reported line to e2e/web/latency.spec.ts's source-view measurement — `commit: p50/p95, n=<bursts>`, the deferred whole-buffer `parse` at the burst boundary — evidence only, no threshold, reported the way the existing two lines are. Scope: packages/core/src/undo.ts, packages/core/test/**, packages/editor/src/store.ts, packages/editor/src/toggle.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "One unit test per case (a)–(e), green, each named in the journal against its case; the Playwright reproduction green and asserted able to fail (the `from` carried by the binding's commit replaced by its `at` in a scratch edit turns it red — recorded, reverted before the commit); the 1.6/1.7/1.17/1.24 undo, toggle and burst suites unchanged in meaning and green (a changed expectation is explained in the journal against the grouping rule); the latency spec prints the `commit` line for the source view at both sizes and the journal quotes it; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change.",
  "dependencies": [
   "1.30"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.32",
  "model": "opus",
  "description": "Phase 1 review fix, tests and one comment (DECISIONS #review-1-r2 H3 — Claude finding 2; H4 — Claude finding 3, a consequence of #021's scope decision; H9 — Grok finding 1). (a) H3: the flush `toggle` performs before it reads the store (apps/desktop/src/dev/DevEditor.tsx:102, `sourceBinding.current?.flush()`) has no test that fails without it — Claude replaced the line with a comment and the full e2e/web suite stayed 68/68 — and found no behavioural difference either, which points at the line being redundant with `SourceBinding.destroy`'s own `commitPending()` on the mode-change effect cleanup (the cleanup runs before the rendered view is built), while the comment at :98-101 asserts an ordering (the pending commit before `toggleMode` closes the group) that the line may no longer be the cause of. `toggle` is the first reader named by the rule 1.verify.r2 promoted into CLAUDE.md (every reader of the store that is not an editing surface settles the pending source burst before it reads, and a new reader's test is an action inside the coalescing window) and the one of the four readers without such a case. Do this in order and record each step: add the toggle-inside-the-window case to e2e/web/editor-toggle.spec.ts — toggle to source, type inside the window, toggle back with no wait, assert the rendered document and the markdown pane hold the burst; then run the mutation against both candidate guards, separately — first `:102` removed, then `destroy`'s `commitPending()` removed — and record which cases go red under each; look for the ordering case the comment claims (a burst typed in source inside the window, a toggle at once, a keystroke in the rendered view at once, one Undo: if the group closed before the burst was committed, the burst and the keystroke could share an entry) and add it if it distinguishes the two; keep `:102` only if some case fails without it, otherwise rewrite its comment to say that `destroy`'s flush on the effect cleanup is what settles the burst and that this call is the reader's own statement of the rule — no other source change. The journal names the test that discharges the toggle clause of the promoted rule. (b) H4: `installWriteTextSpy` is the first statement of `openEditor` in e2e/web/editor-clipboard.spec.ts, the only route to `clickCopyMarkdown`, so every click of Copy Markdown in the suite goes to a stub that always resolves and nothing exercises the real clipboard write; in this container the unspied click fails with `Copy failed: the browser refused clipboard access` unless the permission is granted, so the `catch` branch is reachable in CI's environment and covered by nothing. Keep the three spy cases byte-exact as they are (#021, 1.28); add one case that grants `clipboard-read` and `clipboard-write`, opens the editor **without** the spy (a second opener or an option on `openEditor`, never a spy that is installed and then bypassed), types a word, clicks Copy Markdown, and asserts only that the status becomes \"Copied Markdown\" and that `navigator.clipboard.readText()` **contains** the word — platform-independent (a carriage return does not break \"contains\"; no normalisation, no `process.platform`, no carriage-return escape in code — 1.28's grep guards stay at their counts except that `readText` now appears in exactly this one case, named in its doc comment), still red if `writeText` rejects. Extend the describe block's doc comment: a copy test asserts the string the app wrote; one case asserts the real write resolves and the OS holds the word; a paste test of foreign content is the one that needs the real clipboard for its source. (c) H9: packages/editor/test/position-map-editor-leg.test.ts:180 asserts `names.length === Object.keys(index).length` where `names` is `Object.keys(index).sort()` — `x === x`, the class 1.21 (F9c) and 1.26 (G8) removed from the neighbouring files; drop the tautology and leave `expect(names.length).toBeGreaterThan(0)` and the `blockEndChecked`/`inBlockChecked` collectors as the coverage claim. Scope: apps/desktop/src/dev/DevEditor.tsx (the one line and its comment only), e2e/web/**, packages/editor/test/position-map-editor-leg.test.ts, docs/lessons.md, the journal. Zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The toggle-inside-the-window case is present and green and the journal records both mutations with the cases each turned red (or that neither guard alone turns any case red, in which case the comment is rewritten as described and `git diff apps/desktop/src/dev/DevEditor.tsx` is comment lines only); the real-clipboard case is green and asserted able to fail (the `writeText` call replaced by a rejecting one in a scratch edit turns exactly that case red — recorded, reverted before the commit), with `grep -c 'navigator.clipboard.readText' e2e/web/editor-clipboard.spec.ts` equal to 1, `grep -c 'process.platform'` 0 and `grep -c '\\\\r'` 0; the position-map leg's count assertion is gone and its collectors are green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md; no manifest or lockfile change. Honest statement: this container is Linux; the real-clipboard case proves the write resolves on one OS, and the `1.verify.r3h` gate is the only proof on macOS and Windows, which this task cannot observe and must not claim.",
  "dependencies": [
   "1.31"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r3",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r3 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. `promote-lessons` is a manual step carried by this task's text (DECISIONS #review-1-r2): append this line, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md (identical below line 1; `ralph/check-agent-rules.sh` must stay green): `- A fix for one member of a stated invariant asserts the invariant over the corpus and enumerates instances only as named guards on top of it; a corpus leg that claims to be seeded from the writing surface's own output includes at least one destructive transaction (a deletion, a split, a join), because the trees a parser cannot produce are usually reached by taking something away.` The journal names the tests that discharge the clause (1.29's deletion leg and 1.30's mark-edge leg in packages/editor/test/editor-fixed-point.test.ts, by title). No procedure change this attempt. Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "1.32"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r3/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r3h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r3h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r3",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r3/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r3: scripts/gate.sh 1.verify.r3h pushes integrated_sha of 1.verify.r3 to ci/1.verify.r3/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r3/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r3h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r3"
  ]
 },
 {
  "id": "1.33",
  "model": "opus",
  "description": "Phase 1 verification-gate repair, test-only (repair after the 1.verify.r3h GATE-FAILED, ci.yml run 34563964087 at 32552e0: ubuntu-latest and windows-latest green on every unit test and every e2e/web spec, 79 Playwright cases each; macos-latest green on every unit test and on every e2e/web case except one — `e2e/web/editor-mark-edge.spec.ts` › `a space typed at a mark edge never reaches the Markdown as a character reference` › \"H8 (Sol's reproduction): `a *b* c`, the caret after `b`, one space — the space lands inside the mark and the pane is clean\" — whose first `expectClean(page, \"a *b*  c\\n\")` (the call at spec line 78) timed out receiving `a *b* c\\n`, the seed's own bytes unchanged; the log is /logs/ci/1.verify.r3h/a1/workflow.log, step `test (macos-latest)` › `Playwright (e2e/web)`; DECISIONS #022). Principal reading, to be confirmed from that log and the spec before anything changes, never assumed: the case places the caret with `page.keyboard.press(\"ControlOrMeta+Home\")` — Meta+Home on macOS — and three `ArrowRight`. On ubuntu and windows Ctrl+Home is Blink's document-start motion; on macOS a Home, End or modifier chord in a contenteditable is resolved by the OS's key-binding layer (Cocoa scrolls on Home and End, document motion is Cmd+Arrow), so the chord did not put the caret at the document start, the three `ArrowRight` had nothing to traverse from where `seedFromSource`'s click had left the caret (the paragraph's end), the space was typed after `c`, and the trailing-whitespace clause of `stripUnparsableWhitespace` removed it — exactly the received bytes. The assertion meant to catch that, `expect(page.locator(\".ProseMirror em\")).toHaveText(\"b \")`, cannot: `toHaveText` normalises whitespace, so `<em>b</em>` satisfies it; and the pane assertion `a *b*  c` is the same bytes whether the space was typed inside the mark (moved out by the clause) or outside it after the delimiter, so on no runner did the case prove that the keystroke built Sol's tree. Two defects, both in the test; the app is not implicated (the clause of 1.30 is exercised by the unit guards and the corpus leg in packages/editor/test/editor-fixed-point.test.ts). Fix both, in e2e/web/editor-mark-edge.spec.ts only. (1) Place the caret through motions Blink alone decides, which the Linux container therefore proves for every OS: arrow keys — in a one-line block `ArrowUp` reaches the block's start and `ArrowDown` its end in Blink on every OS (Sol's route with the OS-decided key removed: /logs/reviews/1/r2/sol/probe.mjs line 6 is a bare `Home`, three `ArrowRight`, a typed space, on Linux), then the `ArrowRight`/`ArrowLeft` counts the cases already use; if the container shows `ArrowUp`/`ArrowDown` not reaching the block's edges, the fallback is a click at a computed point of the `<em>`'s bounding box (its last pixel puts the caret after `b`), stated in the doc comment. Never a `Home` or `End` key and never a `ControlOrMeta+` chord in the rendered view; state the rule once in the file's doc comment in words. The absence case's `ControlOrMeta+End` (spec line 101) is the same construct and is replaced the same way — it passed on macOS only because the seed's click had already left the caret at the end. (2) Every assertion on the `<em>`'s text is byte-exact: `textContent()` compared with `toBe(\"b \")` in the H8 case (the space went inside the mark; the document keeps it there and the clause runs on the way out) and `toBe(\"b\")` in the absence case, and, for the input-rule case that by its title accepts either tree, an explicit regex naming both (`/^word ?$/` after the space, `toBe(\"word\")` after the `x`) — never `toHaveText`, which normalises whitespace and is the assertion that let the miss through. Do not branch on `process.platform`, do not `.skip`, do not widen any poll timeout, do not touch `stripUnparsableWhitespace` or DevEditor. Scope: e2e/web/editor-mark-edge.spec.ts plus docs/lessons.md and the journal; no source edits, no config edits, no workflow edits, zero new dependencies (PRD §4). The same construct in the rendered view of two other spec files (editor-soft-line-breaks.spec.ts lines 85 and 107, editor-trailing-break.spec.ts lines 125 and 138) is outside this task's scope and already on docs/V1.1-BACKLOG.md (the #022 line); the bare `Home`/`End` in the source view (editor-cursor.spec.ts, editor-toggle.spec.ts) is CodeMirror's own keymap and not a member; leave those files untouched, and a defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit (DECISIONS #review-1-r1). The spec is a file of passed task 1.30, not its record (DECISIONS #016); the repair is authorised by this task. Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Reproduction first, recorded in the journal: before the change, `grep -c 'ControlOrMeta+Home' e2e/web/editor-mark-edge.spec.ts` is 2 and `grep -c 'ControlOrMeta+End' e2e/web/editor-mark-edge.spec.ts` is 1, and the macOS Playwright diff from /logs/ci/1.verify.r3h/a1/workflow.log (step `test (macos-latest)` › `Playwright (e2e/web)`: expected `a *b*  c`, received `a *b* c`, the poll timed out) is quoted in the journal as the reproduction, because this Linux container cannot run macOS. The tightened assertion is shown able to fail in the container: with the H8 case's caret placement replaced in a scratch edit by a placement at the paragraph's end (the position the macOS run typed at), the byte-exact `<em>` assertion and the pane assertion of the H8 case are both red — recorded in the journal with the case's title, reverted before the commit (`git status` clean of it). After the change, in e2e/web/editor-mark-edge.spec.ts: `grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"' e2e/web/editor-mark-edge.spec.ts` is zero; `grep -c 'em\")).toHaveText' e2e/web/editor-mark-edge.spec.ts` is zero; `grep -c 'process.platform' e2e/web/editor-mark-edge.spec.ts` is zero; `grep -cE '\\.(skip|only)\\(|runIf|skipIf' e2e/web/editor-mark-edge.spec.ts` is zero; the H8 case asserts byte-exact that the `<em>`'s text is `b ` after the space and the absence case that it is `b`; the pane assertions `a *b*  c\\n`, `a *b* c\\n` and `a *b*  c\\n`, the `not.toContain(\"&#x20;\")` checks and the `format(parse(pane))` fixed point are unchanged; no timeout option is added. Every e2e/web spec green in the container (`pnpm --filter @essaydown/e2e-web exec playwright install chromium` first, lessons 1.3/1.4/1.12), including the three cases of this file; `pnpm lint && pnpm test && cargo test` green; `npx prettier --check e2e/web/editor-mark-edge.spec.ts` clean; `git diff --stat` against the branch base touches only e2e/web/editor-mark-edge.spec.ts, docs/lessons.md and docs/progress/journal-main.md (docs/V1.1-BACKLOG.md only if a line is added); no manifest or lockfile change. Honest statement: this container is Linux; the repaired route uses only motions Blink decides, but the only proof of a macOS pass is the `1.verify.r3.g1h` gate green on ubuntu, macos and windows, which this task cannot observe and must not claim.",
  "dependencies": [
   "1.verify.r3h"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r3.g1",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r3, repair 1 of 1.verify.r3 after the 1.verify.r3h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #022): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. The promote-lessons step carried by 1.verify.r3 (the invariant-over-the-corpus rule appended to CLAUDE.md and AGENTS.md) is already integrated at 32552e0 and is not repeated; `ralph/check-agent-rules.sh` must still exit 0 and `tests/agent-rules.test.ts` must still pass. No procedure change this attempt. Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "1.33"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r3.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r3.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r3.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r3.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r3.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r3.g1: scripts/gate.sh 1.verify.r3.g1h pushes integrated_sha of 1.verify.r3.g1 to ci/1.verify.r3.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r3.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r3.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r3.g1"
  ]
 },
 {
  "id": "1.10.r3a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r3",
  "description": "Review attempt r3, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r3 (after the r2 FAIL, DECISIONS #review-1-r2). Phase checks as r2: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, now including the source view's `commit` line (1.31) — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r3.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r3-specific checks: each r2 finding's disposition in DECISIONS #review-1-r2 (H1–H9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; in a browser: a paragraph and a heading whose tail is deleted back to a hard break (1.29), a multi-line paste into a table cell (1.29), a space typed at the end of an emphasised word and `*word*` typed then a space (1.30), Copy inside a source burst that continues past one window at 300 ms per character then Undo (1.31), a toggle inside the window followed by a rendered keystroke and Undo (1.32), the real-clipboard copy case (1.32); the deletion and mark-edge corpus legs read for what they actually delete and insert; the `from`/`at` contract in packages/core/src/undo.ts read against the grouping rule in packages/editor/src/toggle.ts; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and, the r2 class (DECISIONS #review-1-r2, Claude's risk 1): any further tree the editor can build that `parse` cannot produce, searched for beyond the reproductions the fix tasks name.",
  "acceptance": "/logs/reviews/1/r3/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r3.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r3b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r3",
  "description": "Review attempt r3, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r3 (after the r2 FAIL, DECISIONS #review-1-r2). Phase checks as r2: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, now including the source view's `commit` line (1.31) — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r3.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r3-specific checks: each r2 finding's disposition in DECISIONS #review-1-r2 (H1–H9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; in a browser: a paragraph and a heading whose tail is deleted back to a hard break (1.29), a multi-line paste into a table cell (1.29), a space typed at the end of an emphasised word and `*word*` typed then a space (1.30), Copy inside a source burst that continues past one window at 300 ms per character then Undo (1.31), a toggle inside the window followed by a rendered keystroke and Undo (1.32), the real-clipboard copy case (1.32); the deletion and mark-edge corpus legs read for what they actually delete and insert; the `from`/`at` contract in packages/core/src/undo.ts read against the grouping rule in packages/editor/src/toggle.ts; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and, the r2 class (DECISIONS #review-1-r2, Claude's risk 1): any further tree the editor can build that `parse` cannot produce, searched for beyond the reproductions the fix tasks name.",
  "acceptance": "/logs/reviews/1/r3/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r3.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r3c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r3",
  "description": "Review attempt r3, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r3 (after the r2 FAIL, DECISIONS #review-1-r2). Phase checks as r2: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, now including the source view's `commit` line (1.31) — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r3.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r3-specific checks: each r2 finding's disposition in DECISIONS #review-1-r2 (H1–H9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; in a browser: a paragraph and a heading whose tail is deleted back to a hard break (1.29), a multi-line paste into a table cell (1.29), a space typed at the end of an emphasised word and `*word*` typed then a space (1.30), Copy inside a source burst that continues past one window at 300 ms per character then Undo (1.31), a toggle inside the window followed by a rendered keystroke and Undo (1.32), the real-clipboard copy case (1.32); the deletion and mark-edge corpus legs read for what they actually delete and insert; the `from`/`at` contract in packages/core/src/undo.ts read against the grouping rule in packages/editor/src/toggle.ts; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and, the r2 class (DECISIONS #review-1-r2, Claude's risk 1): any further tree the editor can build that `parse` cannot produce, searched for beyond the reproductions the fix tasks name. Grok-specific (DECISIONS #review-1-r0 reviewer-shape record; #review-1-r2 reviewer record): Grok reads the tree and /logs only and cites /logs/ci/1.verify.r3.g1h/accepted/ for the cold build; for each r2 finding of Claude and Sol that Grok did not raise or rated lower (H1 and H2 in particular — two blockers in the tree while Grok's verdict was PASS — and H7, rated nit against a should-fix), state whether a read-only review could have found it from the tree (`schema.ts`'s ownership rule at an atom that ends a block; `pendingAt` as one time against `push`'s one distance) and why the severity differed; and state explicitly the verdict Grok gives if any r2 blocker or required finding is not discharged in the tree.",
  "acceptance": "/logs/reviews/1/r3/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r3.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r3d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r3",
  "description": "Reconciliation of review attempt r3 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r3-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r3 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r4 and 1.10.r4a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r3a",
   "1.10.r3b",
   "1.10.r3c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.34",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r3 I1 — Claude finding 1 blocker; I8 — Claude 6 / Sol 4 / Grok 1 nit; the F1/G4/H1/H8 family in `stripUnparsableWhitespace`, packages/editor/src/schema.ts, a file of passed tasks 1.13, 1.25, 1.29 and 1.30, whose records are not edited): a `hard_break` that is the last node of an `emphasis`/`strong`/`delete` run **with unmarked text after it** reaches `format` — `giveUpEdges` (schema.ts, the mark-edge clause) stops its trailing-edge scan at any non-text node and 1.29's end-of-block loop sees only the block's last node — so the serializer writes the break's line ending as `\\&#xA;` inside the delimiters and escapes the next character. Reproduced by the reconciliation headlessly through the Copy button's own conversion: `parse(\"*a\\\\\\nb* c\\n\")`, delete `b` → `*a\\&#xA;*&#x63;\\n` (emphasis) and `**a\\&#xA;**&#x63;\\n` (strong), each a fixed point whose parse holds no `break` and whose text now contains the six characters `&#xA;`; strikethrough `~~a\\\\\\nb~~ c\\n` → `~~a\\\\\\n~~c\\n`, not a fixed point; the setext member `*a\\\\\\nb* c\\n===\\n` → `*a\\&#xA;*&#x63;\\n===============\\n`, not a fixed point. Claude's browser route (Chromium at f90ed50): load `*roses are red\\` newline `violets are blue* — anon`, caret after `blue`, Backspace to the break → pane and `writeText` `*roses are red\\&#xA;*— anon\\n`, status \"Copied Markdown\". The deletion leg (packages/editor/test/typing-legs.ts) deletes to the *block's* end, so the break is always the block's last node and never a run's last node with text after it, and no fixture holds a break inside a mark. Do five things. (1) Extend the mark-edge clause of the ownership rule, stated once in the doc comment: a `hard_break` at either edge of a flanking run leaves the mark the way a whitespace-only run does — in `giveUpEdges`, at either edge, a break gives up the mark (`node.mark(without(node))`) and the scan continues — so `[em(a), em(break), text(\" c\")]` becomes `[em(a), break, text(\"c\")]` → `*a*\\` newline `c`, which `parse` reads as `emphasis[a], break, text(c)`; the leading case (`[text(\"x \"), em(break), em(b)]` → `x \\` newline `*b*`) the same way; a break that is then the block's last node falls to 1.29's clause as before. (2) Guards in packages/editor/test/editor-fixed-point.test.ts, one per position and mark, each asserting `format(root)` bytes, the parse∘format fixed point, that `parse(format(root))` still holds a `break` (count equal to the input's), and that no text node of it matches `/&#x?[0-9a-fA-F]+;/`: a break as a flanking run's last node before unmarked text, in a paragraph and in a setext heading; before another mark's run; as the run's first node; two breaks at the edge; on each of `emphasis`, `strong` and `delete`. (3) The class: the deletion leg gains a second range set that, for every `hard_break` inside a marked run, deletes from the break's end to the end of that *marked run* (the mark's extent, not the block's), so the break becomes the run's last node with text after it; every leg's `not.toContain(\"&#x20;\")` / `not.toContain(\"&#xA;\")` widened to `not.toMatch(/&#x?[0-9a-fA-F]+;/)` (the family has produced `&#x20;`, `&#xA;`, `&#x63;`, `&#x78;`, `&#xA0;`); the journal states how many fixtures the new ranges reached (from the loop, never a literal). (4) A fixture fixtures/markdown/hard-break-in-emphasis.md (agent-written, a break inside emphasis followed by unmarked text, its `.canonical.md` sibling and its index.json entry with nodeTypes), so the leg reaches the tree and the identity corpus covers it; the fixtures-corpus test's counts are read from the index. (5) I8: drop `expect(names.length).toBe(Object.keys(index).length)` at editor-fixed-point.test.ts (the `x === x` line; keep `toBeGreaterThan(0)`). Playwright, in e2e/web/editor-trailing-break.spec.ts or a new spec: Claude's reproduction as a case that deletes (the pane reads `*roses are red*\\` newline `— anon\\n` or the canonical bytes the rule produces — state them — with no `&#`, and the spy-recorded copy equals it), the caret placed only through motions Blink decides (ArrowUp/ArrowDown, counted arrows; never Home/End or a ControlOrMeta chord — DECISIONS #022). Scope: packages/editor/src/schema.ts, packages/editor/test/**, fixtures/markdown/** (the one new fixture, its sibling, the index entry), e2e/web/**, docs/lessons.md, the journal. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "One guard per position and mark enumerated above, each asserting bytes, the fixed point, the surviving `break` and the absence of any numeric character reference, listed in the journal; the deletion leg's new ranges green for every fixture in the index (count from the index, never a literal) and asserted able to fail (the atom clause disabled by a test-local shim goes red on `hard-break-in-emphasis.md` at least — recorded, not committed); the widened regex in every leg (`grep -c '&#x20;' packages/editor/test/*.ts` names only guards of absence or seeds); the tautology gone (`grep -rn 'names.length).toBe(Object.keys' packages apps e2e` is empty); the new fixture present with its canonical sibling and index entry and byte-identical through every identity leg; the existing guards and the four legs green and unchanged in meaning; the Playwright case green with the caret placed by Blink-only motions (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"'` on any file this task adds is 0); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.10.r3d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.35",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r3 I3 — Claude finding 2, should-fix required, a regression introduced by 1.30; I4 — Claude finding 3, pre-existing since 1.13; `stripUnparsableWhitespace` and `giveUpEdges` in packages/editor/src/schema.ts, a file of passed tasks whose records are not edited): link-marked text is not opaque to the strip's trims where the link's own edge is the trim's edge. (I3) The mark-edge clause splits a **link-marked** run at a flanking mark's edge and the split-off whitespace keeps the `link` mark, so a link with edge whitespace inside emphasis/strong/strikethrough — a tree `parse` produces from valid Markdown — comes back trimmed with a second, whitespace-only link invented: reproduced by the reconciliation with no transaction at all (`mdastToPM` → `pmToMdast` → `format`): `*a [b ](u)* c` → `*a [b](u)*[ ](u) c`; `**a [b ](u)** c` → `**a [b](u)**[ ](u) c`; `x *[ b](u)*` → `x [ *b*](u)`; in a cell `| *[b ](u)* |` → `| [*b*](u) |`; byte-identical at c9408ee (before 1.30). Cause: CommonMark's flanking rule reads the delimiter's actual neighbour, which is `)` or `[` when the link's edge coincides with the run's edge — the whitespace inside the link text is not at a flanking edge — but ProseMirror's flat mark set (`[link, emphasis]` on one node) cannot say which mark is outermost, and the clause assumed the flanking mark. (I4) The end-of-block loop and the line-start trim drop a link's edge whitespace at a **block** boundary: `see [the essay ](u)` → `see [the essay](u)`; `[ b](u)` → `[b](u)`; `# [b ](u)` → `# [b](u)` (reproduced the same way; micromark keeps `[b ](u)`, so these are silent rewrites of the user's link text, fixed points but not identity). Do three things. (1) The link predicate, stated once in the doc comment beside the ownership rule: in `giveUpEdges`, when the edge node carries `link` and the node beyond the run on that side does not carry the same link (the link's edge coincides with the run's edge), the scan stops there — the delimiter's neighbour is the bracket, nothing has to move; when the link continues past the run (`[*b* c](u)`, the existing guard) the space still moves within the link, as now. In the end-of-block loop and the line-start trim, a text node carrying `link` stops the trim (the block's end is then the link's `)`); inline code stays opaque as before. (2) Guards in packages/editor/test/editor-fixed-point.test.ts, each asserting `format(root)` equals the input's canonical bytes and that `parse(format(root))` holds exactly one `link`: `*[b ](u)*`, `*[ b](u)*`, `*a [b ](u)* c` at the first, middle and last positions of the block, the strong and strikethrough twins, the cell; `[b ](u)` last and `[ b](u)` first in a paragraph, a heading and a cell; the `[*b* c](u)` guard unchanged. (3) The class: a fifth corpus leg in typing-legs.ts, titled for the **link edge**, that types a space at the end of every **link-marked** text node over every fixture (the `link` mark is not inclusive, so the leg passes the node's marks to `insertText` explicitly, as the mark-edge leg does) and asserts the fixed point, the link count and no numeric character reference; a fixture fixtures/markdown/link-in-emphasis.md (agent-written: a link with edge whitespace inside emphasis, a bare link with edge whitespace at a block end, with its canonical sibling and index entry) so the legs reach the trees. Playwright on /dev/editor: Claude's reproduction (load `*a [b ](u)* c`, type one letter, the pane and the spy's copy hold exactly one link and the canonical bytes) with the caret placed only through motions Blink decides (DECISIONS #022). Scope: packages/editor/src/schema.ts, packages/editor/test/**, fixtures/markdown/** (the one new fixture, its sibling, the index entry), e2e/web/**, docs/lessons.md, the journal. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every guard enumerated above green and listed in the journal against its case; the link-edge leg green for every fixture in the index (count from the index, never a literal) and asserted able to fail (the link predicate disabled by a test-local shim goes red on `link-in-emphasis.md` at least — recorded, not committed); the new fixture byte-identical through every identity leg; the existing guards and legs green and unchanged in meaning (the `[*b* c](u)` case in particular); the Playwright case green with Blink-only caret motion; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.34"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.36",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r3 I2 — Sol finding 1 blocker; `blockToMdast` in packages/editor/src/schema.ts, the block conversion of passed task 1.1, whose record is not edited): a `list_item` keeps its parsed `spread` after an edit adds a second paragraph to it, and the enclosing `list` keeps its own, so `format` writes the item as tight (one newline before the nested list) while the blank line two paragraphs need makes `parse` read the item as loose and write a blank line before the nested list too — Copy Markdown puts bytes on the clipboard that are not a parse∘format fixed point and reports \"Copied Markdown\". Reproduced by the reconciliation headlessly (the two-paragraph slice `prosemirror-view` builds from a two-line plain-text paste, replaced at the end of the first paragraph of fixtures/markdown/nested-lists.md): `- Top level oneX\\n\\n  Y\\n  - Second level one\\n    - Third level one\\n  - Second level two\\n- Top level two\\n`, whose `format(parse(·))` is `- Top level oneX\\n\\n  Y\\n\\n  - Second level one…`; Sol's real-clipboard log holds the same two strings. Sol's fuzz probe (11,955 single transactions over the 55 fixtures) finds the same class at its first 25 mismatches, all `split` inside a list item's paragraph. The route: a plain-text paste of two or more lines into a list item (Enter is `splitListItem`, which opens a new item, not a second paragraph; the paste fitter places the second paragraph inside the item). A parse-time structural attribute outlived the structure it described — a block-level class no inline leg can reach. Do four things. (1) In `blockToMdast`, derive `spread` for `list_item` and for `list` from the edited children the way `parse` will derive it from the bytes `format` writes, stated once in the doc comment: an item holding two adjacent children that the serializer must separate by a blank line (a paragraph followed by a paragraph is the certain case; confirm every other pair against `parse(format(·))` in a test rather than assume it) is spread; a list's `spread` follows `mdast-util-from-markdown`'s rule for a list holding a spread item (confirm it the same way — read the two strings the reconciliation recorded: the reparse of Sol's bytes keeps `- Second level two\\n- Top level two` without a blank line, so do not assume the list flips); an item and a list that parse would read as tight stay tight; the node attrs are untouched (the derivation is in the conversion, so the rendered view keeps its parsed attrs). (2) The identity corpus (CLAUDE.md): every fixture in the index, parsed and converted with no transaction, still serialises byte-identically — in particular every tight and every loose list fixture. (3) Guards in packages/editor/test/editor-fixed-point.test.ts or a new file under packages/editor/test/, each asserting bytes and the fixed point: a second paragraph pasted into a tight item, top-level and nested (Sol's shape); a tight item whose second child is a nested list, still tight; a loose list unchanged; a tight list unchanged; a split of an item's paragraph by `tr.split` (the fuzz shape); the presence and absence of the blank line before a nested list after each. (4) The class: a corpus leg in typing-legs.ts titled for **the list split**, that for every fixture splits every list item's first paragraph at its end and separately replaces its end with the two-paragraph slice, and asserts the fixed point over the corpus; the journal states how many fixtures hold a list (from the loop). Playwright on /dev/editor, in a new spec or e2e/web/editor-trailing-break.spec.ts (which already pastes plain text): load nested-lists.md, click the first paragraph's end (caret placed only by Blink-decided motions, DECISIONS #022), paste `X\\nY` as plain text, Copy Markdown — the spy's bytes equal the pane and are a fixed point of parse∘format, and the item's structure is what the paste built (two paragraphs then the nested list). Scope: packages/editor/src/schema.ts, packages/editor/test/**, e2e/web/**, docs/lessons.md, the journal. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every guard enumerated above green and listed in the journal against its case, with the two `parse(format(·))` confirmations recorded as tests, not comments; the list-split leg green for every fixture in the index (count from the index, never a literal) and asserted able to fail (the derivation replaced by the parsed attr under a test-local shim goes red on `nested-lists.md` at least — recorded, not committed); every identity leg green with every fixture byte-identical; the Playwright case green; the schema-roundtrip, roundtrip, parse-dom, clipboard and positions suites green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.35"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.37",
  "model": "opus",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r3 I5 — Sol finding 2, should-fix required; packages/editor/test/position-map-editor-leg.test.ts, a file of passed tasks 1.27 and 1.32, whose records are not edited): the position-map family's editor-seeded coverage is insertion-only — both legs seed from `typeSpaceAtEveryBlockEnd` and `typeInsideEveryBlock` — while the rule 1.verify.r3 promoted into CLAUDE.md (\"a corpus leg that claims to be seeded from the writing surface's own output includes at least one destructive transaction\") names every round-trip family, and 1.verify.r3's journal discharged it with two legs of the neighbouring fixed-point family only (G6's class: a rule discharged at a neighbour). Do two things. (1) A third leg in that file, titled for **deletion**, that for every fixture in fixtures/markdown/index.json applies `deleteAtEveryBlockEnd` (typing-legs.ts; and the list-split leg 1.36 added, if it is present at this task's base — say which in the journal) to the live ProseMirror document, converts the changed document with `pmToMdast`, passes the root through `formatWithMap`, `nodeAt` and `cursorMap`, and asserts: the map's line for every block agrees with the block's position in the output bytes; `nodeAt` of every mapped position returns the block that owns it; the cursor placed at the end of every changed block maps to the position the output bytes put it at; and `format(root)` is a parse∘format fixed point. (2) Non-vacuous reach, from the loop and never a literal: the count of fixtures the deletion changed is asserted `> 0`, and the presence/absence pair — a fixture the leg leaves unchanged yields the same map as the insertion-free conversion. The leg is asserted able to fail (a map with its lines shifted by one under a test-local shim goes red — the shape of the existing \"the leg can fail\" case — recorded, not committed). The journal names this test as the one that discharges the position-map clause of the promoted rule (CLAUDE.md: a promoted rule names the test that discharges each of its clauses), so the discharge record is complete at every family the rule names. No source change; no fixture change. Scope: packages/editor/test/position-map-editor-leg.test.ts, packages/editor/test/typing-legs.ts (only if a helper is needed), docs/lessons.md, the journal. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The deletion leg present and green for every fixture in the index (count from the index, never a literal), titled for the leg, calling `formatWithMap`, `nodeAt` and `cursorMap` (`grep -c` of each on the file grows by at least one); the reach assertion `> 0` and the presence/absence pair present; the able-to-fail mutation recorded in the journal with the assertion it turned red; the journal names the discharging test by title; the existing two legs and the \"the leg can fail\" case unchanged; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.36"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.38",
  "model": "opus",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r3 I6 — Claude finding 5, Sol finding 3 (Required), Grok finding 2; the #022 revisit trigger fired): four rendered-view caret placements still use `ControlOrMeta+Home`/`ControlOrMeta+End`, the construct #022 showed is resolved by Cocoa on macOS and 1.33 removed from e2e/web/editor-mark-edge.spec.ts: e2e/web/editor-soft-line-breaks.spec.ts:85 (G4's first-line case: `ControlOrMeta+Home` then five `ArrowRight`, asserting \"bytes unchanged\", which a space typed at the paragraph's end also satisfies — so on macos-latest the case proves nothing about the first line) and :107, e2e/web/editor-trailing-break.spec.ts:125 and :138 (`ControlOrMeta+End` after a click that already leaves the caret at the document end). Grep at the reconciliation: exactly these four under e2e/web/, plus three bare `Home`/`End` presses in the **source** view (e2e/web/editor-cursor.spec.ts:36, e2e/web/editor-toggle.spec.ts:359 and :436), which are CodeMirror's own keymap, handled in JS before the OS layer, and are not members. Do three things. (1) Replace the four with motions Blink alone decides (1.33's route: `ArrowUp`/`ArrowDown` reach a one-line block's edges on every OS, then counted `ArrowRight`/`ArrowLeft`; or a click at a computed point of the target's box), the rule already stated in editor-mark-edge.spec.ts's doc comment restated once per file. (2) The :85 case asserts its caret precondition before it types — the caret's text offset in the first line, read from the DOM selection (`document.getSelection()` anchor offset and node text, byte-exact) — so a wrong-end placement fails the location assertion even when the pane bytes are unchanged; the other three assert the caret is at the block's end the same way where their bytes cannot tell. (3) The guard #022 promised, in tests/ in the shape of packages/editor/test/no-codemirror-history.test.ts (a test that fails the build): every file under e2e/web/ contains no `ControlOrMeta+Home`, `ControlOrMeta+End`, `Control+Home|End` or `Meta+Home|End` press, and a bare `\"Home\"`/`\"End\"` press appears only at an allowlisted site — the three source-view sites listed in the test by file with the reason (CodeMirror keymap) — so a fourth use fails; the test reads the directory (count from it, never a literal file list beyond the allowlist). Able to fail, recorded in the journal: (a) the :85 case with its placement replaced by one `ArrowDown` (the paragraph's end) goes red on the location assertion in the container; (b) the guard goes red on a scratch spec line containing the chord. Both reverted in the same tool call. Honest statement in the journal: the container is Linux; the only proof of the macOS pass is the 1.verify.r4h gate. Scope: e2e/web/editor-soft-line-breaks.spec.ts, e2e/web/editor-trailing-break.spec.ts, tests/** (the guard), docs/lessons.md, the journal. No source change; no `process.platform`; no `.skip`/`.only`; no timeout widened. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "`grep -rcE 'ControlOrMeta\\+(Home|End)' e2e/web/` is 0 in every file; `grep -cE '\"(Home|End)\"' e2e/web/*.ts` totals exactly the three allowlisted source-view sites; the guard test present, green, and recorded red on the scratch chord; the :85 case's caret-offset assertion present and recorded red on the paragraph's-end mutation; the four cases green in the container with Blink-only motions; `grep -rn 'process.platform' e2e/web tests` empty; `grep -rcE '\\.(skip|only)\\('` on the touched files 0; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.37"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r4",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r4 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r4 chain — three candidates weighed at DECISIONS #review-1-r3, two promoted and one recorded. (a) Promote, into the `## Every iteration` list of both CLAUDE.md and AGENTS.md (steps 2 and 4, one sentence appended verbatim to each, identical in both files): each attempt appends its own `- [<id>] ` journal line; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]; the runner's stop-check counts journal lines per attempt). Discharged by the runner's stop-check conformance test in ralph/test/ that counts journal lines per attempt — name it by title in the journal. (b) Promote, as the last bullet of `## Code rules` in both files, verbatim: \"An e2e places the rendered caret only through motions Blink alone decides — arrow keys (`ArrowUp`/`ArrowDown` reach a one-line block's edges on every OS, then counted `ArrowRight`/`ArrowLeft`) or a click at a computed point — never a Home/End key or a `ControlOrMeta+` chord in a contenteditable, because Cocoa resolves those before Blink sees them; a mark's text is asserted byte-exact with `textContent` and `toBe`, never a normalising matcher; a case whose bytes cannot tell two trees apart asserts the tree (DECISIONS #022).\" Discharged by 1.33's in-file grep guards (editor-mark-edge.spec.ts) and 1.38's tree-wide guard in tests/ — name both by title in the journal. (c) Record, do not promote: the `[0.0]` doctor lesson (a doctor remedy is read against gate.mjs before anyone runs it; every conformance scenario that ends in a new state ends with a doctor-clean assertion) addresses the principal and the runner's conformance suite, which this task cannot edit; it lands in the boundary `fix(runner)` DECISIONS #023 scopes and in docs/PRINCIPAL.md at the boundary — the journal says so in one sentence. `ralph/check-agent-rules.sh` must exit 0 and tests/agent-rules.test.ts must pass after the edit. No other procedure change this attempt. Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes; the journal names the discharging test for each clause of (a) and (b) and records (c).",
  "dependencies": [
   "1.38"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r4/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r4h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r4h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r4",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r4/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r4: scripts/gate.sh 1.verify.r4h pushes integrated_sha of 1.verify.r4 to ci/1.verify.r4/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r4/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r4h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r4"
  ]
 },
 {
  "id": "1.39",
  "model": "opus",
  "description": "Phase 1 verification-gate repair, test-only (repair after the 1.verify.r4h GATE-FAILED, ci.yml run 34685866034 at 4c133e0: macos-latest green on every unit test and all 83 e2e/web cases; ubuntu-latest and windows-latest green on every unit test and on every e2e/web case except one, the same on both — `e2e/web/editor-soft-line-breaks.spec.ts:119` › \"G4: a space typed at the end of the first line of a wrapped paragraph carries no entity\" — red on the caret precondition at spec line 135, expected `{ kind: \"text\", text: \"alpha\\nbeta gamma\", offset: 5 }`, received offset 8 on ubuntu and 16 on windows, the poll timing out before the space was typed; the log is /logs/ci/1.verify.r4h/a1/workflow.log, steps `test (ubuntu-latest)` and `test (windows-latest)` › `Playwright (e2e/web)`; DECISIONS #024). Principal reading, to be confirmed from the log and the spec before anything changes, never assumed: task 1.38 placed the caret with `toDocumentStart(page, 2)` — `ArrowUp` once per visual line — on the doc comment's claim that `ArrowUp` on a block's first visual line reaches the editable root's start on every OS, then five `ArrowRight`. In a block that renders more than one line a vertical arrow is a visual-line motion whose landing depends on the caret's x, the wrapping and the font metrics: on ubuntu the two presses left the caret at offset 3 (the x of the seed's caret, one line up), on windows at 11 (still on the second line), and the five `ArrowRight` added five; the container and macOS happened to land at 0. 1.33's rule holds for a one-line block only. The location assertion did its job. The app is not implicated. Fix, in e2e/web/editor-soft-line-breaks.spec.ts only: (1) every caret placement in the file reaches its position by counted horizontal arrows from an anchor the case asserts first — the block's end, where `seedFromSource`'s click (and the `.ProseMirror` click in the hard-break case) leaves the caret on every OS: the first-line case asserts `{ kind: \"text\", text: \"alpha\\nbeta gamma\", offset: \"alpha\\nbeta gamma\".length }` and then presses `ArrowLeft` `\"beta gamma\".length + 1` times (the LF is one character in the text node under `white-space: pre-wrap`) before its existing offset-5 assertion; the hard-break case keeps its end-of-block assertion and its 31 `ArrowLeft` and loses the `toDocumentEnd(page, 2)` before them; `toDocumentStart`/`toDocumentEnd` are removed from the file. (2) The doc comment's caret-placement paragraph is rewritten to state the rule once: in the rendered view a position is reached by counted horizontal arrows from an asserted anchor; a vertical arrow only in a one-line block; never a Home or End key or a modifier chord (the chord named in prose only, lesson [1.30]); \"Blink-only\" means decided from the document's characters, not merely handled by Blink. (3) Able to fail, recorded in the journal and reverted in the same tool call: the first-line case with its `ArrowLeft` count reduced by one goes red on the location assertion (received offset 6, expected 5) in the container. Do not branch on `process.platform`, do not `.skip`, do not widen any poll timeout, do not touch `e2e/web/editor-trailing-break.spec.ts` (its two `toDocumentEnd` uses are the `[plan.1.verify.r4h.r0]` backlog line — outside this scope), `tests/no-caret-chords-in-e2e.test.ts`, any source file or DevEditor. Scope: e2e/web/editor-soft-line-breaks.spec.ts plus docs/lessons.md and the journal; no source edits, no config edits, no workflow edits, zero new dependencies (PRD §4). The spec is a file of passed tasks 1.25 and 1.38, not their record (DECISIONS #016); the repair is authorised by this task. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Reproduction first, recorded in the journal: before the change, `grep -c 'toDocumentStart\\|toDocumentEnd' e2e/web/editor-soft-line-breaks.spec.ts` is at least 4 (the two definitions and the two uses), and the ubuntu and windows Playwright diffs from /logs/ci/1.verify.r4h/a1/workflow.log (expected offset 5, received 8 and 16, the poll timed out at line 135) are quoted in the journal as the reproduction, because this Linux container cannot run those runners' geometry. After the change: `grep -c 'toDocumentStart\\|toDocumentEnd\\|ArrowUp\\|ArrowDown' e2e/web/editor-soft-line-breaks.spec.ts` is 0; the first-line case asserts the end-of-block anchor (offset 16 in `alpha\\nbeta gamma`) before its `ArrowLeft` count and offset 5 after it; the hard-break case still asserts its end-of-block anchor before its `ArrowLeft` count; `grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"' e2e/web/editor-soft-line-breaks.spec.ts` is 0; `grep -c 'process.platform'` is 0; `grep -cE '\\.(skip|only)\\(|runIf|skipIf'` is 0; no timeout option added; the shortened-count mutation recorded in the journal with the assertion it turned red. Every e2e/web spec green in the container (`pnpm --filter @essaydown/e2e-web exec playwright install chromium` first, lessons 1.3/1.4/1.12), including all five cases of this file; `tests/no-caret-chords-in-e2e.test.ts` green and unchanged; `pnpm lint && pnpm test && cargo test` green; `npx prettier --check e2e/web/editor-soft-line-breaks.spec.ts` clean; `git diff --stat` against the branch base touches only e2e/web/editor-soft-line-breaks.spec.ts, docs/lessons.md and docs/progress/journal-main.md (docs/V1.1-BACKLOG.md only if a line is added); no manifest or lockfile change. Honest statement: this container is Linux; the repaired route uses only counted character motions from an asserted anchor, but the only proof of an ubuntu-latest and windows-latest pass is the `1.verify.r4.g1h` gate green on all three runners, which this task cannot observe and must not claim.",
  "dependencies": [
   "1.verify.r4h"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r4.g1",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r4, repair 1 of 1.verify.r4 after the 1.verify.r4h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #024): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. The promote-lessons step carried by 1.verify.r4 (the journal-line-per-attempt sentence in `## Every iteration` and the Blink-only caret rule in `## Code rules` of CLAUDE.md and AGENTS.md) is already integrated at 4c133e0 and is not repeated; `ralph/check-agent-rules.sh` must still exit 0 and `tests/agent-rules.test.ts` must still pass. No procedure change this attempt. Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes.",
  "dependencies": [
   "1.39"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r4.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r4.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r4.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r4.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r4.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r4.g1: scripts/gate.sh 1.verify.r4.g1h pushes integrated_sha of 1.verify.r4.g1 to ci/1.verify.r4.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r4.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r4.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r4.g1"
  ]
 },
 {
  "id": "1.10.r4a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r4",
  "description": "Review attempt r4, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r4 (after the r3 FAIL, DECISIONS #review-1-r3). Phase checks as r3: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r4.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r4-specific checks: each r3 finding's disposition in DECISIONS #review-1-r3 (I1–I10) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; in a browser: a Backspace inside an emphasised verse loaded from a file (1.34), a keystroke in a paragraph holding a link with edge whitespace inside emphasis and one holding a link with edge whitespace at a block end (1.35), a two-line plain-text paste into a list item then Copy Markdown (1.36), the four repaired caret placements and the guard's ability to fail (1.38); the deletion, mark-edge, link-edge, list-split and position-map legs read for what they actually delete, split and insert (1.34–1.37); the `spread` derivation in `blockToMdast` read against `mdast-util-from-markdown`'s rule; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; the principal entries written during the r3 set (#023) are in the tree at this SHA — read them as inputs; and, the r3 class (DECISIONS #review-1-r3, Claude's risk 1 and Sol's risk 1): any further tree the editor can build that `parse` cannot produce, inline (nestings the corpus lacks) or block-level (a parsed attribute after a structural edit), searched for beyond the reproductions the fix tasks name.",
  "acceptance": "/logs/reviews/1/r4/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r4.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r4b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r4",
  "description": "Review attempt r4, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r4 (after the r3 FAIL, DECISIONS #review-1-r3). Phase checks as r3: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r4.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r4-specific checks: each r3 finding's disposition in DECISIONS #review-1-r3 (I1–I10) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; in a browser: a Backspace inside an emphasised verse loaded from a file (1.34), a keystroke in a paragraph holding a link with edge whitespace inside emphasis and one holding a link with edge whitespace at a block end (1.35), a two-line plain-text paste into a list item then Copy Markdown (1.36), the four repaired caret placements and the guard's ability to fail (1.38); the deletion, mark-edge, link-edge, list-split and position-map legs read for what they actually delete, split and insert (1.34–1.37); the `spread` derivation in `blockToMdast` read against `mdast-util-from-markdown`'s rule; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; the principal entries written during the r3 set (#023) are in the tree at this SHA — read them as inputs; and, the r3 class (DECISIONS #review-1-r3, Claude's risk 1 and Sol's risk 1): any further tree the editor can build that `parse` cannot produce, inline (nestings the corpus lacks) or block-level (a parsed attribute after a structural edit), searched for beyond the reproductions the fix tasks name. Sol's r3 probes (paste-probe.mjs, fuzz.mjs, history-probe.mjs) are under /logs/reviews/1/r3/sol/ — re-run the paste probe and the fuzz probe at this SHA and report the mismatch count beside r3's 25-of-25 in nested-lists.md.",
  "acceptance": "/logs/reviews/1/r4/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r4.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r4c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r4",
  "description": "Review attempt r4, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r4 (after the r3 FAIL, DECISIONS #review-1-r3). Phase checks as r3: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r4.g1h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r4-specific checks: each r3 finding's disposition in DECISIONS #review-1-r3 (I1–I10) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; in a browser: a Backspace inside an emphasised verse loaded from a file (1.34), a keystroke in a paragraph holding a link with edge whitespace inside emphasis and one holding a link with edge whitespace at a block end (1.35), a two-line plain-text paste into a list item then Copy Markdown (1.36), the four repaired caret placements and the guard's ability to fail (1.38); the deletion, mark-edge, link-edge, list-split and position-map legs read for what they actually delete, split and insert (1.34–1.37); the `spread` derivation in `blockToMdast` read against `mdast-util-from-markdown`'s rule; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; the principal entries written during the r3 set (#023) are in the tree at this SHA — read them as inputs; and, the r3 class (DECISIONS #review-1-r3, Claude's risk 1 and Sol's risk 1): any further tree the editor can build that `parse` cannot produce, inline (nestings the corpus lacks) or block-level (a parsed attribute after a structural edit), searched for beyond the reproductions the fix tasks name. Additionally, as at r3 (DECISIONS #review-1-r2, #review-1-r3 reviewer record): for each r3 blocker or required finding Grok did not raise — I1 (the backlog line's reachability claim cited in Grok's own gate row as closing the family, falsifiable from typing-legs.ts's ranges beside the [1.10.r2d] lesson), I2 (a parsed attribute copied through blockToMdast with no rule that re-derives it), I3 and I5 — state whether a read-only review could have found it from the tree and which severity it could have rated it, and why the severity of I6 differed (nit against should-fix Required); a read-only reviewer treats a deferral's reachability claim as a hypothesis to test against the legs' own ranges, never as a disposition.",
  "acceptance": "/logs/reviews/1/r4/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r4.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r4d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r4",
  "description": "Reconciliation of review attempt r4 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r4-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r4 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r5 and 1.10.r5a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r4a",
   "1.10.r4b",
   "1.10.r4c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.40",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r4 J1 — Claude finding 1 blocker, Sol finding 1 blocker; packages/core/src/format.ts, a file of passed Phase 0 tasks whose records are not edited — the first Phase 1 fix in packages/core, a new task per DECISIONS #016): the serializer half of the flanking property. `toMarkdownExtensions()` takes `gfmStrikethroughToMarkdown()` as-is, and its `handleDelete` (mdast-util-gfm-strikethrough/lib/index.js) writes `~~`…`~~` with none of the `encodeInfo` logic mdast-util-to-markdown's own `emphasis`/`strong` handlers have (lib/handle/emphasis.js, lib/util/encode-info.js — read both in node_modules first), so a `delete` run whose edge character is punctuation directly beside unmarked alphanumeric text serialises to `~~` delimiters GFM can neither open nor close (micromark-extension-gfm-strikethrough/lib/syntax.js: `_close = !before || before === punctuation && Boolean(after)`): `format` writes bytes whose parse holds no `delete`, Copy Markdown reports \"Copied Markdown\", and the rendered view still shows the strike. Reproduced by the reconciliation headlessly through the Copy button's own conversion (`mdastToPM` → one `tr.delete` → `pmToMdast` → `format`): `Alpha ~~beta.~~ gamma delta\\n` minus the space before `gamma` → `Alpha ~~beta.~~gamma delta\\n`, whose `format(parse(·))` is `Alpha \\~\\~beta.\\~\\~gamma delta\\n`; `x ~~a.~~ 1` minus its space → `x ~~a.~~1` (digit outside), `x ~~`a`~~ b` → `x ~~`a`~~b` (inline code inside), `Alpha ~~(beta)~~ gamma` minus the space before `~~(` → `Alpha~~(beta)~~ gamma` (the opening side) — none a fixed point, every one parsing to zero `delete` nodes; Sol's `~~a [b ](u)~~ c\\n` minus the space before `c` → `~~a [b ](u)~~c\\n` → `\\~\\~a [b ](u)\\~\\~c\\n` (the link's `)` is the delimiter's neighbour because `outermostMark` writes the strikethrough outside a link with edge whitespace, as 1.35 intends); fixtures/markdown/link-in-emphasis.md minus the one space after `](essay.md)~~` → not a fixed point, `delete` count 1 → 0. The emphasis and strong twins come back as `Alpha *beta.*&#x67;amma delta\\n` and `Alpha **beta.**&#x67;amma delta\\n` — fixed points whose parse keeps the mark — because the built-in handlers encode the outside letter. The parser's own tree is not safe either: `parse(\"~~a.~~&#x62;\\n\")` holds one `delete` and `format` of it is `~~a.~~b\\n`, which reparses to none — a Phase 0 invariant-A gap the corpus has no fixture for. The editor's strip (`giveUpEdges`, schema.ts) is not the cause and is not touched: it moves whitespace and breaks, and there is nothing to move. Do three things. (1) A `delete` handler in format.ts beside `opaqueHandlers()`, registered so it replaces the extension's (read how `remark-stringify` merges `handlers` from `toMarkdownExtensions` and options, and confirm by a test that the app's handler is the one that runs), that mirrors emphasis.js's shape for the `~` marker: after `containerPhrasing`, classify the last inner character against `info.after`'s first and the first inner character against `info.before`'s last with `encodeInfo`'s own table (letter outside + punctuation inside → encode the outside character as a numeric character reference, the `outside` case, by setting `state.attentionEncodeSurroundingInfo` the way emphasis.js does; whitespace inside → encode inside as the built-ins do; letter inside → nothing), stated once in the doc comment together with the ownership sentence: the serializer, not the editor's tree, owns delimiter validity against the neighbour outside the mark. Keep the tracker/`peek` behaviour of the extension so `unsafe` patterns and `~` escaping are unchanged for every other tree. (2) Guards in packages/core/test (the format/roundtrip suite, or a new `strikethrough-flanking.test.ts`), one per side (opening, closing) × neighbour class outside (letter, digit, punctuation, whitespace) × inner class at the edge (punctuation, letter, inline code, link) — enumerated from `encode-info.js`'s table, not from the reproductions —, each asserting `format(root)`'s bytes, the parsed `delete` count of those bytes, and the parse∘format fixed point; the reproductions above appear among them by construction; plus the invariant-A case (`parse(\"~~a.~~&#x62;\\n\")` → `~~a.~~&#x62;\\n`, a fixed point). Asserted able to fail: a test-local formatter built from `stringifyOptions()` and the three extensions without the new handler goes red on the closing-side letter case at least — recorded in the journal, not committed. (3) A fixture `fixtures/markdown/strikethrough-punctuation.md` (`~~a.~~ b`, `a ~~(b)~~`, and the tree of `~~a.~~&#x62;`) with its canonical sibling and index entry, so the identity corpus and every corpus leg cover the shape (count from the index, never a literal); every existing fixture byte-identical through the identity corpus (the handler changes bytes only where the old ones did not parse back). Scope: packages/core/src/format.ts, packages/core/test/**, fixtures/markdown/** (the new fixture, its canonical sibling, index.json), docs/lessons.md, the journal. No editor change, no e2e change (task 1.41 carries the leg and the browser reproductions), no config or workflow edits, zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one command that prints the timestamp — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every guard enumerated above green and listed in the journal against its (side, neighbour class, inner class) triple, each asserting bytes, the parsed `delete` count and the fixed point; the invariant-A case green; the able-to-fail mutation recorded with the case it turned red; the app's handler shown to be the one that runs (a test, not a comment); the new fixture present with its canonical sibling and index entry and byte-identical through every identity leg; every other fixture byte-identical (the identity corpus green, count from the index); `grep -n 'handleDelete\\|gfmStrikethroughToMarkdown' packages/core/src/format.ts` shows the registration; the schema-roundtrip, roundtrip, editor-fixed-point and positions suites green unchanged; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.10.r4d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.41",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r4 J1 — the editor leg and the browser reproductions of the blocker task 1.40 fixed in packages/core/src/format.ts; packages/editor/test/typing-legs.ts and packages/editor/test/editor-fixed-point.test.ts, files of passed tasks whose records are not edited): none of the five corpus legs (`deleteAtEveryBlockEnd`, `deleteToEveryMarkedRunEnd`, `typeSpaceInsideEveryMarkedRun`, `typeSpaceAtEveryLinkEnd`, the list split/paste pair) deletes the unmarked whitespace *between* a flanking-marked run and its neighbour — the one-keystroke route both r4 reviewers found (a Backspace over the space after `~~beta.~~`, or after `~~…](essay.md)~~` in fixtures/markdown/link-in-emphasis.md). Do two things. (1) A corpus leg titled for **the mark's neighbour** in typing-legs.ts, imported by editor-fixed-point.test.ts in the shape of the existing legs: for every fixture in fixtures/markdown/index.json (count from the index, never a literal), for every maximal run of every flanking mark (`emphasis`, `strong`, `delete`), delete the whitespace character immediately outside the run on each side where one exists (two transactions per run, applied separately), and, for the certain case, a variant that first types `.` at the run's end with the run's marks and then deletes the whitespace after it; each result converted with `pmToMdast` and asserted to be a parse∘format fixed point whose parsed tree keeps as many `delete`/`emphasis`/`strong` nodes as the input tree had, with no text node holding a numeric character reference other than the ones the serializer's encoding writes for the outside character (assert the decoded text equals the editor's text). Non-vacuous reach from the loop: the count of runs deleted beside is asserted `> 0` and the count of fixtures changed `> 0`, and the presence/absence pair (a fixture with no flanking run yields the unchanged document). Asserted able to fail, recorded in the journal and not committed: the deletion replaced by a no-op goes red on the reach assertion, and the leg run against `format` with 1.40's handler swapped for the package's under a test-local formatter (the shape 1.40 recorded) goes red on `strikethrough-punctuation.md` or `link-in-emphasis.md` at least. (2) Two Playwright cases in a new e2e/web/editor-strikethrough-neighbour.spec.ts, on `/dev/editor` with the `writeText` spy of 1.28 and the real `<del>` count: (a) Claude's route — load `Alpha ~~beta.~~ gamma delta\\n` through the fixture input, click at the paragraph's end and assert the caret's text anchor and offset (`{ text: \" gamma delta\", offset: 12 }`), press `ArrowLeft` eleven times and assert `{ text: \" gamma delta\", offset: 1 }`, one `Backspace`, Copy Markdown → the pane and the spy receive `Alpha ~~beta.~~&#x67;amma delta\\n` (or whatever byte-exact string 1.40's handler writes — read it from 1.40's guard, then assert it with `toBe`), that string a fixed point of `format(parse(·))` computed in the page, and its parse holding one `delete`; (b) Sol's route — load `~~a [b ](u)~~ c\\n`, click the end, assert `{ text: \" c\", offset: 2 }`, one `ArrowLeft` asserted at offset 1, `Backspace`, Copy Markdown, the same three assertions. Caret placement only by a click and counted horizontal arrows from an asserted anchor (CLAUDE.md's rule; `tests/no-caret-chords-in-e2e.test.ts` guards it). Scope: packages/editor/test/typing-legs.ts, packages/editor/test/editor-fixed-point.test.ts, e2e/web/editor-strikethrough-neighbour.spec.ts, docs/lessons.md, the journal. No source change, no fixture change, no config or workflow edits, zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one command that prints the timestamp — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The neighbour leg present and green for every fixture in the index (count from the index, never a literal), titled for the leg, with the reach assertions and the presence/absence pair; both able-to-fail mutations recorded in the journal with the assertion each turned red; the two Playwright cases green in the container with the caret placed by a click and counted `ArrowLeft` only (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowUp|ArrowDown' e2e/web/editor-strikethrough-neighbour.spec.ts` is 0), each asserting the copied bytes with `toBe`, the fixed point and the parsed `delete` count; the existing legs and guards green and unchanged in meaning; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.40"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.42",
  "model": "sonnet",
  "description": "Phase 1 review fix (DECISIONS #review-1-r4 J2 — Claude finding 2 should-fix; `needsBlankLine` in packages/editor/src/schema.ts, the block conversion of passed task 1.36, whose record is not edited): the pair *paragraph then setext heading* is missing from the paragraph clause. mdast-util-to-markdown/lib/join.js (read it in node_modules) writes a blank line after a paragraph before a heading `formatHeadingAsSetext` (lib/util/format-heading-as-setext.js) chooses — depth ≤ 2 whose text holds a `\\r?\\n|\\r` in any literal or a `break` — whatever the item's flag says, exactly as for two paragraphs, so a tight item `[paragraph(\"a\"), heading(depth 1, \"h\\nx\"), list]` serialises as `- a\\n\\n  h\\n  x\\n  =\\n  - i\\n` while `parse` reads the item as spread and writes `…=\\n\\n  - i\\n` — reproduced by the reconciliation headlessly (`fixed: false`); I2's class, one pair short. packages/editor/test/list-spread.test.ts's `KINDS.heading` is ATX depth 2 with `text(\"h\")` and no break, so its 225 pairs cannot see it. Route: the heading input rule typed at the start of a hard-wrapped second paragraph of a list item that has a nested list after it — reachable, not ordinary. Do two things. (1) In `needsBlankLine`'s paragraph clause, `right.type === \"heading\"` and the same predicate `formatHeadingAsSetext` uses (depth ≤ 2, non-empty text, a line ending in any literal descendant or a `break`), stated in the doc comment's pair list as item 1's fourth member; the rule stays confirmed against `parse(format(·))` by the enumeration, never assumed. (2) The two setext variants added to `KINDS` (a depth-1 heading with `\\n` in its text; a depth-2 heading with a `break`), so the pair test asserts them by construction; one guard with the nested list present and one absent, each asserting `format(root)`'s bytes and the fixed point; the existing 225 pairs and every identity leg green with every fixture byte-identical. Scope: packages/editor/src/schema.ts (`needsBlankLine` and its doc comment only), packages/editor/test/list-spread.test.ts, docs/lessons.md, the journal. No other source change, no fixture change, no config or workflow edits, zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one command that prints the timestamp — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The two `KINDS` entries present and the pair enumeration green over them (its pair count grows from the loop, never a literal); the two guards green and listed in the journal; the clause's able-to-fail recorded (the clause disabled by a test-local shim goes red on the setext pair — recorded, not committed); every identity leg green with every fixture byte-identical; the list-split and paste legs green unchanged; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.41"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.43",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r4 J3 — Claude finding 3 should-fix, Sol finding 2 should-fix Required; the `[1.37]` backlog line's trigger fired; `cursorMap.toSource` in packages/editor/src/toggle.ts, a file of passed tasks 1.7 and 1.27 whose records are not edited, and packages/editor/test/position-map-editor-leg.test.ts, a file of passed tasks 1.27, 1.32 and 1.37): `toSource` answers a position that a block's correspondence covers but no inline child does with the block's *start* (`innermostAt` finds the block, a block has no spelling table, so the `range.startLine` branch fires), so after a Backspace that leaves a paragraph ending in a `hard_break` (the strip drops the trailing break, the mdast block is narrower than the editor's), or inside the empty paragraph a split opens in a list item (`isPlaceholder` skips it, the item's correspondence still covers it), `Cmd/Ctrl+/` puts the source caret at the paragraph's (or item's) first column instead of after the text before it. Reproduced by the reconciliation headlessly: `abc\\\\` newline `def`, `def` deleted → the editor's paragraph is `abc` + `hard_break`, the mdast paragraph `abc`, and `toSource` of the position after the break is `{ line: 1, ch: 0 }`; Sol's browser probe: load `abc\\\\` newline `def`, click the end (asserted `def`, offset 3), Backspace ×3, toggle to source, type `X` → the pane is `Xabc\\n`, not `abcX\\n`. Task 1.37 pinned both shapes as `it.fails` (\"known defect\") and deferred the fix on its test-only scope; the reconciliation takes it now. Do three things. (1) In `toSource`, the no-spelling branch for a block correspondence answers `afterLastNode(entries, map, pos)` — the last inline node ending at or before `pos` — instead of the block's start: `3:2`-shaped answers for shape (a), the end of the paragraph before the placeholder for shape (b); the existing atom behaviour (`delimiterPosition`, a position on a mark's delimiters, a node answered with its own start when it is the innermost *inline* node) unchanged, stated in the doc comment as the third clause of the mapping. (2) In position-map-editor-leg.test.ts: the width branch and the split collector the `[1.37]` backlog line names deleted, both shapes folded into clause 3 of the deletion leg as ordinary passing assertions (the cursor placed after a dropped break and inside a split item's empty paragraph maps to the byte end of what precedes it), the `it.fails` case removed — the suite then reports no expected failure — and the reach assertions kept (`blocksEndingPastCorrespondence > 0`, `splitParagraphsPastCorrespondence > 0`, now as ordinary counts of shapes the clause covered). Asserted able to fail, recorded in the journal and not committed: `:351`'s old answer restored under a test-local shim goes red on shape (a) and on shape (b). (3) Sol's reproduction as a Playwright case in e2e/web/editor-toggle.spec.ts (or a new spec if the file's fixture helpers do not fit): load `abc\\\\` newline `def\\n`, click the paragraph's end and assert `{ text: \"def\", offset: 3 }`, Backspace ×3, `ControlOrMeta+/` to source (the toggle chord, not a caret motion — the guard allows it), type `X`, Copy Markdown → the pane and the spy `abcX\\n`; the split-item sibling asserted headless in (2) only. Scope: packages/editor/src/toggle.ts (`toSource` and its doc comment), packages/editor/test/position-map-editor-leg.test.ts, one e2e/web spec, docs/lessons.md, the journal. No other source change, no config or workflow edits, zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one command that prints the timestamp — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "`grep -c 'it.fails' packages/editor/test/position-map-editor-leg.test.ts` is 0 and `pnpm test`'s summary shows no expected fail; the deletion leg green for every fixture in the index with both shapes asserted in clause 3 and the reach counts `> 0`; the able-to-fail mutation recorded with the two shapes it turned red; the Playwright case green in the container with the caret placed by a click asserted first (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"' ` on the touched spec is 0); the cursor, toggle and 200-seeded-position suites green unchanged; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.42"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.44",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r4 J4 — Claude finding 4 should-fix; J5 — Claude finding 5 nit; tests/no-caret-chords-in-e2e.test.ts, a file of passed task 1.38, whose record is not edited): (J4) `CHORD = /\\b(ControlOrMeta|Control|Meta)\\+(Home|End)\\b/g` does not match `Shift+End`, `Alt+Home` or `ControlOrMeta+Shift+End`, and `BARE_PRESS` (`/\"(Home|End)\"/g`) does not either — confirmed by the reconciliation with the regex against the three spellings (all false; `ControlOrMeta+Home` true) — so a fifth use spelled with another modifier is green in the container and unproven on macOS, the gap the guard exists to close (DECISIONS #022: Cocoa resolves those chords before Blink sees them). (J5) The allowlist's stated reason (\"CodeMirror's keymap handles the key in JS before the OS layer\", `standardKeymap` in the doc comment) is false: `@codemirror/commands` is not installed (`pnpm why @codemirror/commands` is empty, by 1.19's design — packages/editor/src/source.ts says so in its own doc comment) and source.ts installs no keymap, so the three source-view `Home`/`End` presses are Blink's native line motion in CodeMirror's contenteditable, the same path the rendered view has, proven by the three-OS gate (green on macos-latest at 1.verify.r4.g1h a1) and by e2e/web/editor-cursor.spec.ts's `toColumn` landing. Do two things. (1) `CHORD` becomes any modifier chain ending in Home or End in the quoted key-name form `keyboard.press` takes — `/\"(?:[A-Za-z]+\\+)+(Home|End)\"/g` — and one negative case in the file feeds the regex the three spellings above plus `ControlOrMeta+Home` and expects each to match, and feeds it `\"Home\"` and `\"ArrowLeft\"` and expects no match; the guard's ability to fail re-recorded in the journal on a scratch spec pressing `Shift+End` under e2e/web/ (deleted in the same tool call, not committed). (2) The two `reason` strings and the file's doc comment reworded to the true mechanism: \"Blink's native line motion in CodeMirror's contenteditable (no keymap is installed — `pnpm why @codemirror/commands` is empty); proven by the three-OS gate\", with no claim about a keymap anywhere in the file (`grep -c 'keymap' tests/no-caret-chords-in-e2e.test.ts` is 0). Scope: tests/no-caret-chords-in-e2e.test.ts, docs/lessons.md, the journal. No e2e change, no source change, no config or workflow edits, zero new dependencies (PRD §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one command that prints the timestamp — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The negative case present and green (the three spellings matched, the bare and arrow names not); the scratch-spec mutation recorded red in the journal; `grep -c 'keymap' tests/no-caret-chords-in-e2e.test.ts` is 0; the allowlist counts unchanged (`editor-cursor.spec.ts` 1, `editor-toggle.spec.ts` 2, or the counts 1.43 leaves — read from the tree, never assumed); every existing case of the file green; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.43"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r5",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r5 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r5 chain — three candidates weighed at DECISIONS #review-1-r4, two promoted and one recorded. (a) Promote, as a bullet of `## Code rules` in both CLAUDE.md and AGENTS.md, verbatim and identical in both files: \"A fix for a mark-edge or delimiter defect reads the serializer's handler for that mark before touching the editor's tree — both halves of every fixed-point family are owned, the tree the editor builds and the bytes the serializer writes for it — and every mark-edge guard is enumerated over both neighbour classes CommonMark §6.2 names (whitespace and punctuation) on every flanking mark (DECISIONS #review-1-r4 J1).\" Discharged by 1.40's side × neighbour class × inner class guards in packages/core/test and 1.41's neighbour leg in packages/editor/test — name both by title in the journal. (b) Promote, as the next bullet, verbatim: \"An enumeration that mirrors a third-party rule derives its kind table from that rule's own branches, read in the installed package, never from the type list — a pair the table lacks is a pair the enumeration cannot fail on (DECISIONS #review-1-r4 J2).\" Discharged by 1.42's setext entries in `KINDS` of packages/editor/test/list-spread.test.ts — name it by title in the journal. (c) Record, do not promote: Sol's r4 lesson that a test-only task's scope is not a deferral reason for a product defect on the phase's user path addresses the reconciliation, not the coding agent (the agent's rule — a backlog line with a trigger and a hard stop — already stands); it lands in docs/PRINCIPAL.md at the boundary; the journal says so in one sentence. `ralph/check-agent-rules.sh` must exit 0 and tests/agent-rules.test.ts must pass after the edit. No other procedure change this attempt. Each attempt appends its own `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one command that prints the timestamp — an earlier attempt's line stays as it is; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes; the journal names the discharging test for each clause of (a) and (b) and records (c).",
  "dependencies": [
   "1.44"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r5/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r5h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r5h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r5",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r5/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r5: scripts/gate.sh 1.verify.r5h pushes integrated_sha of 1.verify.r5 to ci/1.verify.r5/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r5/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r5h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r5"
  ]
 },
 {
  "id": "1.10.r5a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r5",
  "description": "Review attempt r5, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r5 (after the r4 FAIL, DECISIONS #review-1-r4; the r4 set's third report was derived, DECISIONS #025 — read that entry as an input and name, in your inputs line, every file you read under /logs/reviews/). Phase checks as r4: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r5h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r5-specific checks: each r4 finding's disposition in DECISIONS #review-1-r4 (J1–J9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the `delete` handler in packages/core/src/format.ts (1.40) read against mdast-util-to-markdown's `encodeInfo` table and GFM's flanking rule, and shown to be the handler that runs; every other handler `toMarkdownExtensions()` takes as-is (tables, autolink literals) walked the same way; the neighbour leg (1.41) read for what it actually deletes and where; the setext predicate (1.42) read against the installed `format-heading-as-setext.js`; `toSource`'s third clause (1.43) read for the atom cases it must keep; in a browser: a Backspace over the space after a struck run ending in punctuation and after one ending in a link, then Copy Markdown (1.41); a deletion that leaves a paragraph ending in a hard break, then the toggle and a typed character (1.43); the widened chord guard's ability to fail on a `Shift+End` press (1.44); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and the r4 class (DECISIONS #review-1-r4, Claude's risks 1 and 2): any further bytes the serializer writes for a tree `parse` produces or the editor builds that do not parse back, inline or block-level, searched for beyond the reproductions the fix tasks name.",
  "acceptance": "/logs/reviews/1/r5/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r5h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r5b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r5",
  "description": "Review attempt r5, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r5 (after the r4 FAIL, DECISIONS #review-1-r4; the r4 set's third report was derived, DECISIONS #025 — read that entry as an input and name, in your inputs line, every file you read under /logs/reviews/). Phase checks as r4: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r5h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r5-specific checks: each r4 finding's disposition in DECISIONS #review-1-r4 (J1–J9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the `delete` handler in packages/core/src/format.ts (1.40) read against mdast-util-to-markdown's `encodeInfo` table and GFM's flanking rule, and shown to be the handler that runs; every other handler `toMarkdownExtensions()` takes as-is (tables, autolink literals) walked the same way; the neighbour leg (1.41) read for what it actually deletes and where; the setext predicate (1.42) read against the installed `format-heading-as-setext.js`; `toSource`'s third clause (1.43) read for the atom cases it must keep; in a browser: a Backspace over the space after a struck run ending in punctuation and after one ending in a link, then Copy Markdown (1.41); a deletion that leaves a paragraph ending in a hard break, then the toggle and a typed character (1.43); the widened chord guard's ability to fail on a `Shift+End` press (1.44); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and the r4 class (DECISIONS #review-1-r4, Claude's risks 1 and 2): any further bytes the serializer writes for a tree `parse` produces or the editor builds that do not parse back, inline or block-level, searched for beyond the reproductions the fix tasks name. Sol's r4 probes (strike-probe.mjs, cursor-probe.mjs, extra-probe.mjs, the r3 fuzz) are under /logs/reviews/1/r4/sol/ — re-run the fuzz and the strike probe at this SHA and report the mismatch count beside r4's one in link-in-emphasis.md.",
  "acceptance": "/logs/reviews/1/r5/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r5h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r5c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r5",
  "description": "Review attempt r5, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r5 (after the r4 FAIL, DECISIONS #review-1-r4; the r4 set's third report was derived, DECISIONS #025 — read that entry as an input and name, in your inputs line, every file you read under /logs/reviews/). Phase checks as r4: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r5h/accepted/, three OS directories in both artifacts); fresh-clone cold build. r5-specific checks: each r4 finding's disposition in DECISIONS #review-1-r4 (J1–J9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the `delete` handler in packages/core/src/format.ts (1.40) read against mdast-util-to-markdown's `encodeInfo` table and GFM's flanking rule, and shown to be the handler that runs; every other handler `toMarkdownExtensions()` takes as-is (tables, autolink literals) walked the same way; the neighbour leg (1.41) read for what it actually deletes and where; the setext predicate (1.42) read against the installed `format-heading-as-setext.js`; `toSource`'s third clause (1.43) read for the atom cases it must keep; in a browser: a Backspace over the space after a struck run ending in punctuation and after one ending in a link, then Copy Markdown (1.41); a deletion that leaves a paragraph ending in a hard break, then the toggle and a typed character (1.43); the widened chord guard's ability to fail on a `Shift+End` press (1.44); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and the r4 class (DECISIONS #review-1-r4, Claude's risks 1 and 2): any further bytes the serializer writes for a tree `parse` produces or the editor builds that do not parse back, inline or block-level, searched for beyond the reproductions the fix tasks name. Additionally (DECISIONS #025, #review-1-r4 reviewer record): this reviewer's r4 report was written after reading its siblings' finished reports and was counted as derived; for r5 the reviewer states in its inputs line every path read under /logs/reviews/, reads no sibling directory, and, for each r4 blocker or required finding (J1, J3), states whether a read-only review could have found it from the tree — J1 from `handleDelete` read against `encode-info.js` and GFM's `_close` rule, J3 from `toggle.ts`'s no-spelling branch beside the `it.fails` case — and which severity it would have rated, on its own reading.",
  "acceptance": "/logs/reviews/1/r5/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r5h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r5d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r5",
  "description": "Reconciliation of review attempt r5 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r5-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r5 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r6 and 1.10.r6a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r5a",
   "1.10.r5b",
   "1.10.r5c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.45",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r5 K1 — Claude finding 1 blocker, Sol finding 1 blocker; packages/core/src/format.ts, a file of passed Phase 0 tasks and of task 1.40, whose records are not edited — a new task per DECISIONS #016): the astral (non-BMP) neighbour of a flanking mark is written as a lone-surrogate character reference. `mdast-util-to-markdown/lib/util/container-phrasing.js` (2.1.2; read it in node_modules first, with lib/handle/emphasis.js, lib/handle/root.js, lib/util/encode-info.js and micromark-util-classify-character/index.js) rewrites a child's output after its handler returned at exactly two places: the first UTF-16 unit of the child after an attention run (`if (encodeAfter && encodeAfter === value.slice(0, 1)) value = encodeCharacterReference(encodeAfter.charCodeAt(0)) + value.slice(1)`) and the last unit of the child before it (`results[results.length - 1].slice(0, -1) + encodeCharacterReference(before.charCodeAt(0))`), where `after` is the first unit of the next child's output (a speculative `handle`/`peek` call, `.charAt(0)`) and `before` the last unit of the previous one. A character outside the BMP directly outside `*`, `**` or `~~` whose inner edge is punctuation or whitespace is therefore encoded one unit wide: `x ~~a.~~😀` → `x ~~a.~~&#xD83D;\\uDE00\\n`, `😀~~(a)~~ x` → `\\uD83D&#xDE00;~~(a)~~ x\\n`, `x ~~a ~~😀` → `x ~~a&#x20;~~&#xD83D;\\uDE00\\n`; the same with `*` and `**` (the built-in handlers share the assembler) and with the supplementary letters 𐐀 (U+10400) and 𝕒 (U+1D552); every one reparses to a text node holding U+FFFD, so Copy Markdown reports \"Copied Markdown\" with the character destroyed, and the 1.7 toggle carries `&#xD83D;` into the document (Claude's typed route: `Alpha *wow!*` then `😀 delta`, the emphasis input rule closing the mark, no deletion; Sol's: the r4 Backspace route on `~~a.~~ 😀`). Invariant A fails on the parser's own tree: `parse(\"~~a.~~&#x1F600;\\n\")` holds one `delete` and `format` writes `~~a.~~&#xD83D;\\uDE00\\n` (also `*a.*&#x1F600;`, `**a.**&#x1F600;`, `&#x1F600;~~(a)~~`, `&#x1F600;*(a)*`). Reproduced by the reconciliation headlessly for three marks × three astral characters × both sides × the punctuation and whitespace inner edges — 27 trees, none a fixed point — and unreachable by every corpus leg because no fixture holds a non-BMP character. The decision to encode is right and is not changed: micromark classifies one UTF-16 unit as well (`micromark-util-classify-character` tests `String.fromCharCode(code)` against `\\p{P}|\\p{S}`; a lone surrogate is neither whitespace nor punctuation, so it is \"other\"), so a raw astral neighbour never lets the run form — `parse(\"~~a.~~😀\\n\")`, `parse(\"😀~~(a)~~\\n\")` and `parse(\"*a.*😀\\n\")` hold no mark — while the code-point reference does (`parse(\"~~a.~~&#x1F600;\\n\")` holds one `delete` whose neighbour text is `😀`). Only the width of the encoding is wrong. So: do not classify by code point (it would call 😀 punctuation — `\\p{So}` — decide not to encode, and write bytes the parser cannot close), do not change `classifyCharacter`, `encodeInfo` or `handleDelete`'s decisions, and do not replace the built-in `emphasis`/`strong` handlers. Do three things. (1) In format.ts, wrap `state.containerPhrasing` once per serialization, installed on the first dispatched node — always `root` — in the shape positions.ts's `patchIndentLines` uses: an app `root` handler registered beside `opaqueHandlers()` that installs the wrapper on the `State` it receives (idempotently, a marker on the state) and then does what lib/handle/root.js does — `containerPhrasing` when any child is phrasing per `mdast-util-phrasing`'s list read in node_modules (`break`, `delete`, `emphasis`, `footnote`, `footnoteReference`, `image`, `imageReference`, `inlineCode`, `inlineMath`, `link`, `linkReference`, `mdxJsxTextElement`, `mdxTextExpression`, `strong`, `text`, `textDirective` — `html` is NOT in it, upstream calls html ambiguous, and a root holding block html must go through `containerFlow`; the reconciliation's prototype listed `html` and turned raw-html.md and the opaque-node tests red), else `containerFlow`, both called as methods of `state` (`containerPhrasingBound` reads `this`). `mdast-util-to-markdown` and `mdast-util-phrasing` are not direct dependencies of packages/core and are not added (a manifest line is a lockfile change): the four lines and the type list are mirrored, the list stated in the doc comment, one guard pins a root of direct phrasing children and one a root holding a block `html` node. The wrapper calls the original bound to `state` and widens every lone-surrogate reference the original wrote at a child boundary to the code-point reference of the pair it split — `&#xD83D;` followed by the raw low surrogate U+DE00 becomes `&#x1F600;`, and the raw high surrogate U+D83D followed by `&#xDE00;` becomes `&#x1F600;` — the only two forms the two rewrites above can produce (`safe()` and the handlers' own inside-whitespace encoding never encode a surrogate: safe.js encodes only the ASCII characters its `unsafe` patterns match), stated once in the doc comment as the app-owned half of the encoding: the handlers and `encodeInfo` decide which side to encode, in the parser's own code-unit terms; the app writes what they decided as a whole scalar. The reference's hexadecimal is upper-case with no padding, as encode-character-reference.js writes it. `formatWithMap` inherits the wrapper because it builds on `createFormatter()` (its `configuredHandlers` drops `root` from the instrumented set, so the app's `root` stays the one that runs; confirm by one test that `formatWithMap(parse(\"~~a.~~&#x1F600;\\n\")).text` is a fixed point); the map's `unresolved` for that node is task 1.46's, not this task's — leave positions.ts, positions-corpus.test.ts and its pin alone, and add no fixture (task 1.47 adds `astral-neighbour.md` once the map can place its neighbour, because the position-map deletion leg puts an encoded neighbour at a block's end and would go red on it before 1.46). (2) Guards in packages/core/test/strikethrough-flanking.test.ts, extended to all three marks: astral outside — one symbol (😀 U+1F600) and one letter (𐐀 U+10400) — × inner class at the edge (punctuation, whitespace, inline code, link) × side (opening, closing) × mark (`delete`, `emphasis`, `strong`), each asserting `format(root)`'s bytes with `toBe`, that the bytes parse to exactly one mark of that type, that the parsed tree's text values joined equal the built tree's (the instrument that sees U+FFFD; not a decoded-bytes comparison), and the parse∘format fixed point; the letter-inside cell as the absence case (no encoding, the raw astral kept: `x ~~a~~😀`); both sides at once (`😀~~(a.)~~😀`); the five invariant-A sources above, each formatting to itself; the BMP guards of 1.40 unchanged. Asserted able to fail, recorded in the journal and not committed: a test-local formatter without the wrapper (the `stringifyOptions()` + extensions shape 1.40 recorded) goes red on the closing-side astral case with the lone-surrogate bytes. (3) One Playwright case in a new e2e/web/editor-astral-neighbour.spec.ts on `/dev/editor` with the `writeText` spy of 1.28 (copy the helpers of editor-strikethrough-neighbour.spec.ts; a spec file is self-contained): Claude's typed route — click the empty editor, `keyboard.type(\"Alpha *wow!*\")` (the emphasis input rule closes the mark: assert one `<em>` whose `textContent` is `wow!` first, with `toBe`), then `keyboard.type(\"😀 delta\")`, Copy Markdown → the pane and the spy receive `Alpha *wow!*&#x1F600; delta\\n` (read the bytes from the new guard, assert with `toBe`), that string a fixed point of `format(parse(·))` computed in the page, its parse's text values joined equal to the paragraph's `textContent` (`Alpha wow!😀 delta`), and `&#xD` absent from the pane. Caret placement only by the click and typing — no arrow, no Home/End key, no chord (`tests/no-caret-chords-in-e2e.test.ts` guards the file). Scope: packages/core/src/format.ts, packages/core/test/**, e2e/web/editor-astral-neighbour.spec.ts, docs/lessons.md, the journal. No editor change, no positions.ts change, no fixture change, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 chain's turn caps were spent on (lessons [1.41], [1.42]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every guard enumerated above green and listed in the journal against its (mark, side, inner class, neighbour) tuple, each asserting bytes, one mark, the parsed text values equal to the tree's and the fixed point; the absence case and the both-sides case green; the five invariant-A sources format to themselves; the able-to-fail mutation recorded with the lone-surrogate bytes it produced; the `formatWithMap` fixed-point test green; the root-of-phrasing and root-with-block-html guards green; `grep -c 'containerPhrasing' packages/core/src/format.ts` is at least 2 and `grep -cE 'codePointAt|fromCodePoint|0x10000' packages/core/src/format.ts` at least 1 (the widening is by code point); `classifyCharacter`, `encodeInfo` and `handleDelete` unchanged (`git diff` shows no change inside those three functions); the Playwright case green in the container with the copied bytes asserted by `toBe`, the fixed point and the text equality (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowUp|ArrowDown|ArrowLeft|ArrowRight' e2e/web/editor-astral-neighbour.spec.ts` is 0); the schema-roundtrip, roundtrip, fixtures-corpus, editor-fixed-point and positions suites green with their meaning unchanged and every fixture byte-identical (the identity corpus green, count from the index; `grep -lE '&#xD[89AB]' fixtures/markdown/*.canonical.md` prints nothing); the positions suite's one expected fail — 1.40's pin — stays, so `pnpm test`'s summary shows `1 expected fail` (this task does not touch it; 1.46 removes it); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.10.r5d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.46",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r5 K2 — Claude finding 2, Sol finding 2 and Grok finding 1, should-fix Required by all three; the `[1.40, found outside scope]` backlog line's trigger fired, its revisit being the r5 reconciliation; `placeChildren` in packages/core/src/positions.ts, the position map of passed task 1.2, whose record is not edited, and the pin task 1.40 left in packages/core/test/positions-corpus.test.ts): a text child whose edge character `containerPhrasing` rewrote after the child's handler returned is not found by `container.indexOf(child.value, cursor)` (positions.ts:455), so it is reported in `unresolved`, `cursorMap` (packages/editor/src/toggle.ts) has no range and no spelling table for it, `nodeAt` over its bytes answers the paragraph, and `toSource` of a caret inside the neighbour falls to `afterLastNode` — the end of the preceding run, inside its closing delimiter. Reproduced by the reconciliation headlessly at 3dd9c4b: `formatWithMap(parse(\"x *a.*&#x62;\\n\")).map.unresolved` is `[\"0.2\"]`, `~~a.~~&#x62;` gives `[\"0.1\"]`, `&#x62;*.a* x` gives `[\"0.0\"]` (both sides, all three marks; `x *a.* b` is empty); through `cursorMap(pmToMdast(mdastToPM(parse(·))), doc)` on `Alpha ~~beta.~~&#x67;amma delta` every ProseMirror position from 13 (inside `gamma`) to 23 (the end) answers `{ line: 1, ch: 13 }` and `toRendered` of that answers 12, and on `x *a.*&#x62;` the position after `b` (6) answers `{ line: 1, ch: 5 }` — in a browser, toggle then type `X` puts the keystroke inside the marked run (`Alpha ~~beta.X~~gamma delta`, Claude; `x *a.X*b`, Sol). After task 1.45 the rewrite is a whole scalar (`&#x1F600;` for 😀), so the neighbour of an astral character is the same shape with a two-unit character. Do four things. (1) In `placeChildren` (or `wrapHandle`, whichever keeps the lookup in one place — state which in the doc comment): when `indexOf` misses a `text` child, retry the value with its first code point replaced by that code point's numeric character reference (`&#x` + the code point in upper-case hexadecimal, no padding, `;` — the form encode-character-reference.js and 1.45's wrapper write), then with its last code point replaced, then with both — the only rewrites `container-phrasing.js` and the wrapper make, enumerated in the doc comment with the reason —, and a one-character value with the whole value as the reference; the lookup bounded from `cursor` as before, so a later repeated literal is never mistaken for this child (Sol's rule); the found emission's length is the rewritten string's. (2) `spellingOffsets` already reads a reference for a BMP character (`spellingEnd` compares `String.fromCodePoint(code)` with one UTF-16 unit); make it place a surrogate pair written as one reference, with the ownership rule stated once in `SpellingTable`'s doc comment and tested at the first, middle and last position of a text node: the leading unit owns the whole reference and the trailing unit is a zero-width item at the reference's end (its `start` and `end` both the reference's end), so `toSource` of the position between the two units — a position the editor never offers, ProseMirror counting units — answers the reference's end and every other position of the node is monotone; a raw surrogate pair in plain text keeps its two one-unit spellings as today. Rewrite the doc comment's sentence \"A character whose spelling is empty cannot arise\" to name this one case. (3) Delete `OUTSIDE_ENCODED_NEIGHBOUR` and the `it.fails` case in positions-corpus.test.ts so `strikethrough-punctuation.md` runs through the ordinary \"maps every node of the tree, and nothing is unresolved\" case (count from the index, never a literal); unit guards in packages/core/test (positions.test.ts or a new file) for `x *a.*&#x62;`, `x **a.**&#x62;`, `~~a.~~&#x62;`, `&#x62;*.a* x`, `~~a.~~&#x1F600;`, `x &#x10400;*(a)*`, `&#x1F600;~~(a.)~~&#x1F600;` (both sides, three marks, one BMP and two astral, a one-character neighbour and a longer one), each asserting `unresolved` empty, the neighbour's range covering the reference, `nodeAt` inside the reference answering the text node, and the spelling table's entries for the encoded character and its neighbours. Asserted able to fail, recorded in the journal and not committed: the retry disabled goes red on the corpus case for `strikethrough-punctuation.md`. (4) In packages/editor/test/position-map-editor-leg.test.ts, a clause in the deletion leg — seeded by `deleteBesideEveryMarkedRun` of typing-legs.ts (imported, unchanged) over every fixture in the index, plus one hand-seeded document `mdastToPM(parse(\"~~a.~~&#x1F600;\\n\"))` so the astral shape is asserted before 1.47's fixture joins the index — asserting, for every text node the deletion left directly against a flanking mark, that `toSource` is monotone over the node's positions (excluding the position between a pair's units) and that `toRendered(toSource(p))` is `p` for each, with the reach count of such nodes asserted `> 0` and `strikethrough-punctuation.md` named among the fixtures reached; and one Playwright case in a new e2e/web/editor-toggle-encoded-neighbour.spec.ts (helpers copied from editor-strikethrough-neighbour.spec.ts and editor-toggle-trailing-break.spec.ts): load `Alpha ~~beta.~~ gamma delta\\n`, click the paragraph's end and assert the anchor `{ text: \" gamma delta\", offset: 12 }`, `ArrowLeft` ×11 asserted at offset 1, one `Backspace` (the 1.41 route; the pane `Alpha ~~beta.~~&#x67;amma delta\\n`), `ArrowRight` ×3 asserted at `{ text: \"gamma delta\", offset: 3 }` (the text node's name read from the DOM after the Backspace, not assumed — assert whatever the DOM reports and record it in the journal), `ControlOrMeta+/` (the toggle chord, allowed — the guard bans Home/End chords only), type `X`, toggle back, Copy Markdown → the spy's bytes `Alpha ~~beta.~~&#x67;amXma delta\\n` with `toBe`, a fixed point, one `delete`, and the rendered paragraph's `textContent` `Alpha beta.gamXma delta`. Caret placement only by a click asserted first and counted horizontal arrows (DECISIONS #022, #024). Scope: packages/core/src/positions.ts, packages/core/test/**, packages/editor/test/position-map-editor-leg.test.ts, e2e/web/editor-toggle-encoded-neighbour.spec.ts, docs/lessons.md, the journal. No change to toggle.ts, typing-legs.ts, format.ts or any fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 chain's turn caps were spent on (lessons [1.41], [1.42]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "`grep -c 'OUTSIDE_ENCODED_NEIGHBOUR\\|it.fails' packages/core/test/positions-corpus.test.ts` is 0; every fixture in the index green through \"maps every node of the tree, and nothing is unresolved\" (count from the index); the unit guards above green, each listed in the journal against its source string; the spelling rule for a surrogate pair tested at the first, middle and last position; the able-to-fail mutation recorded with the fixture it turned red; the deletion-leg clause green over every fixture with its reach count `> 0` and `strikethrough-punctuation.md` reached, and the hand-seeded astral document green; the Playwright case green in the container with the anchor asserted before the arrows, the copied bytes asserted by `toBe`, the fixed point, the `delete` count and the `textContent` (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowUp|ArrowDown' e2e/web/editor-toggle-encoded-neighbour.spec.ts` is 0); the cursor, toggle, 200-seeded-position and editor-fixed-point suites green unchanged; `pnpm test`'s summary shows no expected fail (1.37's pin went at 1.43 and 1.40's goes here — this file-level grep and the suite total agree only from this task on); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.45"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.47",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r5 K1's corpus half and K4 — Claude finding 4 should-fix; fixtures/markdown/**, packages/editor/test/typing-legs.ts and packages/editor/test/editor-fixed-point.test.ts, files of passed tasks whose records are not edited): the corpus reaches the astral shape 1.45 fixed and 1.46 mapped, and the mark's-neighbour legs assert the text they were asked to. Do three things. (1) A fixture fixtures/markdown/astral-neighbour.md of exactly three paragraphs — `~~a.~~&#x1F600;`, `x &#x10400;*(a)*`, `**a.**&#x1D552; 😀` (a closing-side symbol neighbour, an opening-side letter neighbour, a closing-side letter neighbour followed by a raw astral in plain text; the second paragraph starts with `x ` because the letter leg types after a block's first character and its \"delete the letters, get the canonical back\" model cannot predict a letter that lands flush against a mark and is encoded — a backlog line records that model limit with a trigger) — with its canonical sibling (`format(parse(·))`, byte-identical to the source) and its index.json entry, so the identity corpus, every corpus leg and the position-map family cover the shape (count from the index, never a literal). The reconciliation ran the scratch suite with this fixture at 3dd9c4b plus 1.45's prototype and found two instruments the fixture reaches: the position-map deletion leg puts the encoded neighbour last in the third paragraph (1.46's clause, green once 1.46 is in), and `typeInsideEveryBlock` inserted its letter between the two units of a surrogate pair. (2) In typing-legs.ts, every leg that chooses a position inside a text run or deletes one character does so by code point, never between the two units of a surrogate pair — `typeInsideEveryBlock` types after the block's first *code point* (`[...text][0].length`), as `deleteAtEveryBlockEnd` and `deleteBesideEveryMarkedRun` already read a character with `[...text]`; state the rule once in the file's doc comment (\"one character is one code point\"), and pin it with one guard in editor-fixed-point.test.ts on a hand-built paragraph whose first character is an astral one, asserting the letter is inserted after both units (its position and the fixed point), with the able-to-fail recorded in the journal (the old `first + 1` goes red on that guard). (3) The assertion 1.41's text named and the legs lack (K4): in both mark's-neighbour legs (`deleteBesideEveryMarkedRun`, `punctuateThenDeleteAfterEveryMarkedRun`) and in `expectNoForbiddenEntity`'s corpus callers, per inline block, `textValues(parse(out))` joined equals the ProseMirror block's `textContent` — the comparison that sees U+FFFD (`decodeReferences` would not: `String.fromCodePoint(0xD83D)` and a raw low surrogate concatenate back to the emoji); with the fixture in the index the legs reach the astral shape, and the reach assertion names `astral-neighbour.md` among the fixtures whose text was compared. Asserted able to fail, recorded in the journal and not committed: the assertion run against `format` with 1.45's wrapper disabled under a test-local formatter goes red on `astral-neighbour.md`. Scope: fixtures/markdown/astral-neighbour.md, fixtures/markdown/astral-neighbour.canonical.md, fixtures/markdown/index.json, packages/editor/test/typing-legs.ts, packages/editor/test/editor-fixed-point.test.ts, docs/V1.1-BACKLOG.md (the letter-leg model line), docs/lessons.md, the journal. No source change, no e2e change, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 chain's turn caps were spent on (lessons [1.41], [1.42]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg, and reaching the deletion leg's encoded-neighbour clause and the mark's-neighbour legs (each reach assertion names `astral-neighbour.md`); every other fixture byte-identical (count from the index); `grep -c 'first + 1' packages/editor/test/typing-legs.ts` is 0 and the astral-first guard green with its mutation recorded; the text-equality assertion present in both neighbour legs and green over every fixture, its mutation recorded red on `astral-neighbour.md`; `pnpm test`'s summary shows no expected fail; the backlog line for the letter leg's model present with a trigger and hard stop `2.verify`; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.46"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.48",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r5 K5 — Claude finding 5 nit and Grok finding 2 nit; K6 — Claude finding 6 nit, the quote-form half; K8 — Grok finding 3 nit; packages/core/test/strikethrough-flanking.test.ts, packages/editor/test/list-spread.test.ts and tests/no-caret-chords-in-e2e.test.ts, files of passed tasks 1.40, 1.42 and 1.44, whose records are not edited): three uncovered cells. (K5, first half) `encodeInfo`'s whitespace-outside × whitespace-inside branch (packages/core/src/format.ts:94, `{ inside: true, outside: true }`, encode-info.js:66–68) is the file's one uncovered line: strikethrough-flanking.test.ts's \"whitespace inside the run\" describe covers letter-outside and punctuation-outside only. Add the third guard, the tree `paragraph(text(\"a \"), del(text(\" b\")), text(\" c\"))`, asserting `format(root)`'s bytes with `toBe` (read the bytes the built-in `emphasis` handler writes for the same tree with `*` first, and assert the `~~` twin — both inside and outside whitespace encoded on the opening side, nothing on the closing side where the inner edge is a letter), one `delete` in the parse, and the fixed point; the coverage line for format.ts then shows no uncovered line (read it from the run, record it in the journal). (K5, second half) No `KINDS` entry of list-spread.test.ts feeds `isSetextHeading` a heading that fails its depth clause with a line ending present (packages/editor/src/schema.ts:711, the `depth > 2` branch, unexecuted): add one entry — an ATX heading of depth 3 whose text holds `\\n` (J7's shape) — so the pair enumeration grows by one row and one column (its count grows from the loop, never a literal), and one named guard asserting that a tight item `[paragraph, heading(depth 3, \"h\\nx\"), list]` gets no blank line before the heading (the bytes and the fixed point; the depth-1 twin already asserts the blank line). (K6, quote forms) `CHORD` and `BARE_PRESS` in tests/no-caret-chords-in-e2e.test.ts match the double-quoted key-name form only; widen both to any of `'`, `\"` and the backtick around the key name (the two quotes must match: a capture group and a backreference, or three alternatives), keep the allowlist counts unchanged (read them from the tree), and add one negative case per quote form for each regex (`'Shift+End'`, a backtick-quoted `Shift+End`, `'Home'`, a backtick-quoted `End` each matched; `\"ArrowLeft\"` not); re-record the guard's ability to fail on a scratch spec pressing a single-quoted `Shift+End` (deleted in the same tool call). The identifier-argument form (`press(key)` with the name in a `const`) is not taken — the `[review-1-r5 K6]` backlog line says why — so do not add it. (K8) The per-file `it` title in the \"no e2e/web file places the caret with a modifier chord on Home or End\" describe still names `ControlOrMeta, Control or Meta`; retitle it to the class `CHORD` matches (\"has no modifier chord ending in Home or End\"), and check the file's doc comment and every other title against the widened regex. Scope: packages/core/test/strikethrough-flanking.test.ts, packages/editor/test/list-spread.test.ts, tests/no-caret-chords-in-e2e.test.ts, docs/lessons.md, the journal. No source change, no fixture change, no e2e change, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 chain's turn caps were spent on (lessons [1.41], [1.42]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The whitespace × whitespace guard green with its bytes listed in the journal and the format.ts coverage line recorded with no uncovered line; the depth-3 `KINDS` entry present, the pair enumeration green over it with its count grown by the loop, and the named guard green; `CHORD` and `BARE_PRESS` matching the three quote forms with one negative case each green, the allowlist counts unchanged, the scratch-spec mutation recorded red; `grep -c 'ControlOrMeta, Control or Meta' tests/no-caret-chords-in-e2e.test.ts` is 0; every existing case of the three files green; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.47"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r6",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r6 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r6 chain — three candidates weighed at DECISIONS #review-1-r5, two promoted and one recorded. (a) Promote, as a bullet of `## Code rules` in both CLAUDE.md and AGENTS.md, verbatim and identical in both files: \"Every character classification in the app's serializer handlers is made in the parser's own terms — one UTF-16 unit, as `micromark-util-classify-character` reads it — and every character reference the app writes encodes a whole scalar (`codePointAt`), never a code unit; every neighbour-class enumeration carries one astral member (a non-BMP symbol and a non-BMP letter) beside its ASCII one, because a lone-surrogate reference decodes to U+FFFD silently (DECISIONS #review-1-r5 K1).\" Discharged by 1.45's astral guards (mark × side × inner class × neighbour) in packages/core/test/strikethrough-flanking.test.ts and 1.47's `astral-neighbour.md` in the index — name both by title in the journal. (b) Promote, as the next bullet, verbatim: \"A position map that instruments handler output learns every rewrite the parent makes after a handler returns, and its corpus case asserting that nothing is unresolved is the guard that names which rewrites exist — a new encoding in a handler is a change to that map (DECISIONS #review-1-r5 K2).\" Discharged by 1.46's ordinary \"maps every node of the tree, and nothing is unresolved\" case over every fixture in the index with the pin gone, and its deletion-leg clause in packages/editor/test/position-map-editor-leg.test.ts — name both by title in the journal. (c) Record, do not promote: Grok's r5 lesson that a read-only review's first two reads are the handler walk and the pin's neighbouring branch addresses reviewers (the r6 rows carry it); Claude's and Sol's r5 lessons on mid-chain decisions outside the tree and on dispositioning a pinned defect by its user-visible consequence address the principal and land in docs/PRINCIPAL.md at the boundary; the journal says so in one sentence. `ralph/check-agent-rules.sh` must exit 0 and tests/agent-rules.test.ts must pass after the edit. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal (the 1.verify.r5 journal said 23 where the directory held 20 — DECISIONS #review-1-r5 K7). No other procedure change this attempt. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 chain's turn caps were spent on (lessons [1.41], [1.42]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; `pnpm test`'s summary shows no expected fail; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes; the journal names the discharging test for each clause of (a) and (b) and records (c).",
  "dependencies": [
   "1.48"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r6/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r6h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r6h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r6",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r6/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r6: scripts/gate.sh 1.verify.r6h pushes integrated_sha of 1.verify.r6 to ci/1.verify.r6/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r6/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r6h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r6"
  ]
 },
 {
  "id": "1.10.r6a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r6",
  "description": "Review attempt r6, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r6 (after the r5 FAIL, DECISIONS #review-1-r5; all three r5 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r5: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r6h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r6-specific checks: each r5 finding's disposition in DECISIONS #review-1-r5 (K1–K9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the `containerPhrasing` wrapper and the app `root` handler in packages/core/src/format.ts (1.45) read against the installed lib/util/container-phrasing.js (its two rewrites), lib/handle/root.js and mdast-util-phrasing's list, and shown to be installed for `format` and for `formatWithMap` (the test, not a comment), with `classifyCharacter`, `encodeInfo` and `handleDelete` confirmed unchanged from 1.40; `placeChildren`'s retry and the surrogate-pair spelling rule in packages/core/src/positions.ts (1.46) read against those two rewrites and the code-point reference 1.45 writes, and the pin confirmed gone with every fixture in the index resolved; typing-legs.ts (1.47) read for where each leg cuts (one character is one code point) and the mark's-neighbour legs read for the text-equality assertion; the three cells of 1.48 read against encode-info.js, format-heading-as-setext.js and the widened `CHORD`; in a browser: the typed route `Alpha *wow!*` then an emoji, Copy Markdown (1.45); a Backspace over the space before an astral neighbour of a struck run, Copy Markdown (Sol's r5 route); the toggle-then-type route into the encoded neighbour (1.46); the widened chord guard's ability to fail on a single-quoted `Shift+End` (1.48); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and the r5 class (DECISIONS #review-1-r5, Claude's risks 1 and 2, Sol's 1 and 2): any character reference the serializer writes that does not decode to the tree's text, and any node `formatWithMap` leaves unresolved or `toSource` answers non-monotonically, for a tree `parse` produces or the editor builds, searched for beyond the reproductions the fix tasks name with at least one non-BMP seed per neighbour class and per inline position class (a mark's edge, a link's edge, a cell, a heading, a soft break).",
  "acceptance": "/logs/reviews/1/r6/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r6h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r6b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r6",
  "description": "Review attempt r6, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r6 (after the r5 FAIL, DECISIONS #review-1-r5; all three r5 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r5: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r6h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r6-specific checks: each r5 finding's disposition in DECISIONS #review-1-r5 (K1–K9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the `containerPhrasing` wrapper and the app `root` handler in packages/core/src/format.ts (1.45) read against the installed lib/util/container-phrasing.js (its two rewrites), lib/handle/root.js and mdast-util-phrasing's list, and shown to be installed for `format` and for `formatWithMap` (the test, not a comment), with `classifyCharacter`, `encodeInfo` and `handleDelete` confirmed unchanged from 1.40; `placeChildren`'s retry and the surrogate-pair spelling rule in packages/core/src/positions.ts (1.46) read against those two rewrites and the code-point reference 1.45 writes, and the pin confirmed gone with every fixture in the index resolved; typing-legs.ts (1.47) read for where each leg cuts (one character is one code point) and the mark's-neighbour legs read for the text-equality assertion; the three cells of 1.48 read against encode-info.js, format-heading-as-setext.js and the widened `CHORD`; in a browser: the typed route `Alpha *wow!*` then an emoji, Copy Markdown (1.45); a Backspace over the space before an astral neighbour of a struck run, Copy Markdown (Sol's r5 route); the toggle-then-type route into the encoded neighbour (1.46); the widened chord guard's ability to fail on a single-quoted `Shift+End` (1.48); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and the r5 class (DECISIONS #review-1-r5, Claude's risks 1 and 2, Sol's 1 and 2): any character reference the serializer writes that does not decode to the tree's text, and any node `formatWithMap` leaves unresolved or `toSource` answers non-monotonically, for a tree `parse` produces or the editor builds, searched for beyond the reproductions the fix tasks name with at least one non-BMP seed per neighbour class and per inline position class (a mark's edge, a link's edge, a cell, a heading, a soft break). Sol's r5 probes (serializer-probe.mjs, map-probe.mjs, unicode-browser.mjs, the r3 fuzz) are under /logs/reviews/1/r5/sol/ — re-run the serializer probe and the map probe at this SHA and report the mismatch and unresolved counts beside r5's 54 and the `x *a.X*b` route.",
  "acceptance": "/logs/reviews/1/r6/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r6h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r6c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r6",
  "description": "Review attempt r6, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r6 (after the r5 FAIL, DECISIONS #review-1-r5; all three r5 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r5: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r6h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r6-specific checks: each r5 finding's disposition in DECISIONS #review-1-r5 (K1–K9) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the `containerPhrasing` wrapper and the app `root` handler in packages/core/src/format.ts (1.45) read against the installed lib/util/container-phrasing.js (its two rewrites), lib/handle/root.js and mdast-util-phrasing's list, and shown to be installed for `format` and for `formatWithMap` (the test, not a comment), with `classifyCharacter`, `encodeInfo` and `handleDelete` confirmed unchanged from 1.40; `placeChildren`'s retry and the surrogate-pair spelling rule in packages/core/src/positions.ts (1.46) read against those two rewrites and the code-point reference 1.45 writes, and the pin confirmed gone with every fixture in the index resolved; typing-legs.ts (1.47) read for where each leg cuts (one character is one code point) and the mark's-neighbour legs read for the text-equality assertion; the three cells of 1.48 read against encode-info.js, format-heading-as-setext.js and the widened `CHORD`; in a browser: the typed route `Alpha *wow!*` then an emoji, Copy Markdown (1.45); a Backspace over the space before an astral neighbour of a struck run, Copy Markdown (Sol's r5 route); the toggle-then-type route into the encoded neighbour (1.46); the widened chord guard's ability to fail on a single-quoted `Shift+End` (1.48); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired; and the r5 class (DECISIONS #review-1-r5, Claude's risks 1 and 2, Sol's 1 and 2): any character reference the serializer writes that does not decode to the tree's text, and any node `formatWithMap` leaves unresolved or `toSource` answers non-monotonically, for a tree `parse` produces or the editor builds, searched for beyond the reproductions the fix tasks name with at least one non-BMP seed per neighbour class and per inline position class (a mark's edge, a link's edge, a cell, a heading, a soft break). Additionally (DECISIONS #review-1-r5 reviewer record, Grok's own r5 lesson): the handler walk and the pin's neighbouring branch are the first two reads — read the wrapper against container-phrasing.js's two rewrites and `placeChildren`'s retry against them before anything else, and state in the report which of K1 and K2 a read-only review could have found from the tree at 3dd9c4b and which severity it would have rated, on its own reading; the r5 report did not see K1 because a read-only walk has no non-BMP seed — say what read would have reached it.",
  "acceptance": "/logs/reviews/1/r6/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r6h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r6d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r6",
  "description": "Reconciliation of review attempt r6 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r6-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r6 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r7 and 1.10.r7a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r6a",
   "1.10.r6b",
   "1.10.r6c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.49",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r6 L1 — Sol finding 1 blocker, Grok finding 1 blocker; packages/core/src/format.ts, a file of passed tasks 1.40 and 1.45, whose records are not edited — a new task per DECISIONS #016): an astral (non-BMP) character that is the closing neighbour of one flanking run and the opening neighbour of the next is written as two lone-surrogate references. `mdast-util-to-markdown/lib/util/container-phrasing.js` (2.1.2) makes its two post-handler rewrites on the *same* text child when that child sits between two runs whose inner edges are punctuation: `encodeAfter` encodes the child's first UTF-16 unit after the left run and `encodingInfo.before` encodes its last unit before the right run, so a one-character astral child comes out as `&#xD83D;&#xDE00;` — a form task 1.45's `SPLIT_PAIR` does not match, because it accepts only a reference beside a *raw* mate. Reproduced by the reconciliation headlessly at c2f6526 in a detached scratch worktree: `format(parse(\"*a.*&#x1F600;*(b)*\\n\"))` is `*a.*&#xD83D;&#xDE00;*(b)*\\n`, whose parse holds `a.` U+FFFD U+FFFD `(b)`, for every mark pair of `*`, `**`, `~~` (nine pairs, mixed pairs included) × 😀 (U+1F600), 𐐀 (U+10400), 𝕒 (U+1D552), in a paragraph, a heading, a table cell, a link's text, a blockquote and a list item — 27 + 5 trees, none a fixed point, every one leaving the neighbour `unresolved` in `formatWithMap` (path `0.1` in a paragraph) because the map's retry (task 1.46) names the code-point reference `&#x1F600;` and not the two-unit form; the BMP twin `*a.*&#x62;*(b)*` and the whitespace-inner twin `x *a *&#x1F600;* b* y` are fixed points (the second keeps the raw emoji: no encoding is decided for it). Sol's browser route (`/logs/reviews/1/r6/sol/between-browser.mjs`, its log beside it): load `*a.* 😀 *(b)*`, delete the two spaces around the emoji with ordinary caret motion, Copy Markdown — the rendered text still shows the emoji, the status reports success, the clipboard holds the corrupt form. The 1.45 \"both sides\" guard (`strikethrough-flanking.test.ts:394–401`, `😀~~(a.)~~😀`) is two different emoji, each split once, which `SPLIT_PAIR` handles; no guard puts one scalar between two runs. Prototyped by the reconciliation and taken (the suite 3,298 green with it): add a third alternative to `SPLIT_PAIR` — a high-surrogate reference (`&#xD[89AB]..;`) directly followed by a low-surrogate reference (`&#xD[C-F]..;`) — and widen it with the same code-point arithmetic `widenSplitSurrogateReferences` already uses; nothing else changes: `classifyCharacter`, `encodeInfo`, `handleDelete`, the root handler and the wrapper's installation stay as 1.45 left them, and the doc comments of `SPLIT_PAIR` and `widenSplitSurrogateReferences` are rewritten to name three forms, derived from the two rewrites of container-phrasing.js and their join on one two-unit child (DECISIONS #review-1-r4 J2: from the branches, read in node_modules). With the bytes whole, `placeChildren`'s existing one-code-point candidate (positions.ts, task 1.46) finds the neighbour — assert it, change no code in positions.ts. Do three things. (1) The regex and its callback in format.ts as above. (2) Guards in packages/core/test/strikethrough-flanking.test.ts, a new describe \"one astral character between two flanking runs\": every ordered pair of `emphasis`, `strong`, `delete` (nine) × the three astral characters above, each tree `paragraph(mark1(text(\"a.\")), text(c), mark2(text(\"(b)\")))`, asserting `format(root)`'s bytes with `toBe` (the code-point reference between the two runs), that the bytes parse to exactly two marks of the named types, that the parsed tree's text values joined equal the built tree's (the instrument that sees U+FFFD), the parse∘format fixed point, and `formatWithMap(root).map.unresolved` empty; the same tree once in each of a heading, a table cell, a link's text, a blockquote and a list item (one pair, one character each — the wrapper is on `state.containerPhrasing` and the cell's assembler calls it, `mdast-util-gfm-table/lib/index.js`); the absence cases: the BMP twin (`&#x62;`, one reference, unchanged) and the whitespace-inner twin (the raw emoji kept); the 1.45 both-sides guard unchanged; the five new sources `*a.*&#x1F600;*(b)*`, `**a.**&#x10400;**(b)**`, `~~a.~~&#x1D552;~~(b)~~`, `**a.**&#x1F600;~~(b)~~`, `# *a.*&#x1F600;*(b)*` added to the invariant-A list (each formatting to itself). Asserted able to fail, recorded in the journal and not committed: the third alternative removed goes red on the pair guards with the two-reference bytes it produces (record them). (3) A fixture fixtures/markdown/astral-between-runs.md of exactly three blocks — the paragraph `*a.*&#x1F600;*(b)*`, the paragraph `**a.**&#x10400;~~(b)~~`, the heading `# ~~a.~~&#x1D552;~~(b)~~` — with its canonical sibling (`format(parse(·))`, byte-identical to the source) and its index.json entry, so the identity corpus, every corpus leg and the position-map family (\"maps every node of the tree, and nothing is unresolved\", the rule promoted at 1.verify.r6) cover the shape; count from the index, never a literal. (4) One Playwright case in a new e2e/web/editor-astral-between-runs.spec.ts on `/dev/editor` with the `writeText` spy of 1.28 (helpers copied from editor-astral-neighbour.spec.ts and editor-toggle-encoded-neighbour.spec.ts; a spec file is self-contained): Sol's route — load `*a.* 😀 *(b)*\\n` through the fixture-file input, click the paragraph, `ArrowUp` (a one-line block's start), counted `ArrowRight` to the anchor asserted as `{ text: \" 😀 \", offset: 1 }` (assert whatever the DOM reports and record it in the journal), `Backspace`, then the anchor asserted again, `ArrowRight` past the emoji (two UTF-16 units are one caret step in Blink — assert the offset, do not assume it), `Delete`, Copy Markdown → the pane and the spy receive `*a.*&#x1F600;*(b)*\\n` (`toBe`), that string a fixed point of `format(parse(·))` computed in the page, its parse's text values joined equal to the paragraph's `textContent` (`a.😀(b)`), two `<em>` elements, and `&#xD` absent from the pane. Caret placement only by the click, `ArrowUp` and counted horizontal arrows with the anchor asserted before every count (DECISIONS #022, #024); no Home/End key, no chord (`tests/no-caret-chords-in-e2e.test.ts` guards the file). Scope: packages/core/src/format.ts, packages/core/test/**, fixtures/markdown/astral-between-runs.md, fixtures/markdown/astral-between-runs.canonical.md, fixtures/markdown/index.json, e2e/web/editor-astral-between-runs.spec.ts, docs/lessons.md, the journal. No positions.ts change, no editor change, no other fixture change, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The pair guards green for every ordered pair of the three marks × the three astral characters and listed in the journal against their (mark1, mark2, character) tuple, each asserting bytes, two marks, the parsed text values equal to the tree's, the fixed point and `unresolved` empty; the five position-class guards green; both absence cases and the 1.45 both-sides guard green; the five new invariant-A sources format to themselves; the able-to-fail mutation recorded with the two-reference bytes it produced; `grep -c 'D\\[C-F\\]' packages/core/src/format.ts` is at least 2 (the third alternative present); `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and `installSurrogateWidening` unchanged (`git diff` shows no change inside those five functions) and `git diff --stat` shows no change to packages/core/src/positions.ts; the fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg and resolved by the ordinary \"maps every node of the tree, and nothing is unresolved\" case (count from the index; `grep -lE '&#xD[89AB]' fixtures/markdown/*.canonical.md` prints nothing); the Playwright case green in the container with the anchor asserted before each count, the copied bytes asserted by `toBe`, the fixed point, the text equality and the `<em>` count (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowDown' e2e/web/editor-astral-between-runs.spec.ts` is 0); the schema-roundtrip, roundtrip, fixtures-corpus, editor-fixed-point and positions suites green with their meaning unchanged and every other fixture byte-identical; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.10.r6d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.50",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r6 L2 — Claude finding 1 blocker; packages/core/src/format.ts, a file of passed tasks 1.40, 1.45 and 1.49, whose records are not edited — a new task per DECISIONS #016): an autolink literal whose URL holds a backslash is written in the `<…>` autolink form with the backslash doubled, and every `parse ∘ format` doubles it again — invariant A fails on the parser's own tree. Read first, in node_modules: `mdast-util-to-markdown/lib/handle/link.js` (the autolink branch hides the construct stack — `state.stack = []` — and writes `state.containerPhrasing(node)` between `<` and `>`), `lib/util/format-link-as-autolink.js` (the decision: `!state.options.resourceLink`, a url, no title, one text child equal to the url, a protocol, no control character, space or angle bracket), `lib/util/safe.js` and `lib/unsafe.js` (the text child still goes through `safe()`, which doubles a backslash before ASCII punctuation — \"typical escapes are handled in `safe`\" — and applies every `unsafe` entry without an `inConstruct`, although inside an autolink no escape works: CommonMark §6.4 reads the bytes literally). Reproduced by the reconciliation headlessly at c2f6526: `parse(\"see https://x.y\\\\ end\\n\")` holds `link{url: \"https://x.y\\\\\"}` and `format` writes `see <https://x.y\\\\\\\\> end\\n`, whose parse holds `url: \"https://x.y\\\\\\\\\"`, then eight backslashes, then sixteen; the same for `https://en.wikipedia.org/wiki/Foo\\\\_bar` (the hand-escaped underscore many writers put in URLs); `www.x.y\\\\` is a fixed point because the built-in falls back to the resource form `[www.x.y\\\\\\\\](http://www.x.y\\\\\\\\)` (the text and the url differ), and the plain `https://example.com/a_b` autolink is a fixed point. In the browser (Claude's route): load `see https://en.wikipedia.org/wiki/Foo\\\\_bar end`, the rendered link's `href` is `…/Foo\\\\_bar`, the pane and Copy Markdown hand over `<https://en.wikipedia.org/wiki/Foo\\\\\\\\_bar>` with status \"Copied Markdown\" — the user's URL changed silently. The corpus's one autolink (`autolink-literal.md`) has no such character. Prototyped by the reconciliation and taken (the suite green with it): an app-owned fallback that keeps the autolink form exactly when it can carry the url and takes the resource form otherwise, per node — `[https://x.y\\\\\\\\](https://x.y\\\\\\\\)` is a fixed point whose parse holds the original url (backslash escapes work in a link destination and in a label), as the `www.` case already shows; `resourceLink: true` globally is NOT taken (it would rewrite the autolink of `autolink-literal.md` and every canonical sibling holding one; docs/MARKDOWN-STYLE.md rule 8 keeps the autolink \"when the text is the destination\", which this fallback preserves whenever the form can spell the destination). Do three things. (1) In format.ts, wrap the configured `link` handler once per serialization in the shape `installSurrogateWidening` uses (installed from the app `root` handler on the `State` it receives, a marker on the state, the original called bound to `state`; `mdast-util-to-markdown` is not a direct dependency of packages/core, so the built-in is reached through `state.handlers.link` and never imported): the wrapper calls the original; when the result is the autolink form (it starts with `<`) and the bytes between `<` and `>` are not byte-identical to `node.url`, it calls the original again with `state.options.resourceLink` set to `true` for that call only and restored in a `finally`, so the resource form is written for that node — the one option `formatLinkAsAutolink` reads; the wrapper's `peek` answers `[` when the fallback will be taken and the original's `peek` otherwise (the previous sibling's `after` classification reads it; both `<` and `[` are punctuation, state that in the doc comment). State the criterion once in the doc comment: the autolink form is kept exactly when it round-trips the url byte for byte, because nothing inside `<…>` can be escaped. `formatWithMap` inherits the wrapper through the app `root` handler as it inherits the widening (`configuredHandlers` captures the built-in link before the root runs and the root wraps the instrumented handler at serialization time) — confirm with one test that `formatWithMap(parse(\"see https://x.y\\\\ end\\n\"))` is a fixed point with `unresolved` empty and that the link's text child has a spelling table in which the backslash's spelling is the two-character escape. (2) Guards in a new packages/core/test/autolink-literal.test.ts: enumerate the characters `safe()` rewrites with the construct stack hidden — read lib/util/safe.js's backslash rule and every lib/unsafe.js entry without an `inConstruct`, list them in the file's doc comment with the line read — and for each build the url `https://x.y/` + the character + `z` (and the trailing-position twin `https://x.y` + the character): assert `format(parse(·))`'s bytes with `toBe`, the parse∘format fixed point, that the parse holds exactly one `link` whose `url` equals the source url, and the text values joined equal to the tree's; the members the autolink form carries unchanged are the absence cases (the plain `https://example.com/a_b` autolink among them, its bytes still `<…>`); the backslash before punctuation (`x.y\\\\` before the closing `>`), `\\\\_`, and the `www.` form are named guards; the five sources `see https://x.y\\\\ end`, `see https://en.wikipedia.org/wiki/Foo\\\\_bar end`, `see www.x.y\\\\ end`, `see https://example.com/a_b end`, `<https://example.com/a_b>` added to the invariant-A list of strikethrough-flanking.test.ts or asserted as a fixed point here (state which in the journal). Asserted able to fail, recorded in the journal and not committed: the wrapper disabled goes red on the backslash guard with the doubled bytes it produces. (3) A fixture fixtures/markdown/autolink-backslash.md of exactly two paragraphs — `see https://x.y\\\\ end` and `see https://en.wikipedia.org/wiki/Foo\\\\_bar end` — with its canonical sibling (`format(parse(·))`: the resource form, byte-identical on a second pass) and its index.json entry, so the identity corpus, every corpus leg and the position-map family cover the shape (count from the index, never a literal); no Playwright case (the editor builds no autolink; the route is a loaded file and the corpus leg is the instrument — say so in the journal). Scope: packages/core/src/format.ts, packages/core/test/**, fixtures/markdown/autolink-backslash.md, fixtures/markdown/autolink-backslash.canonical.md, fixtures/markdown/index.json, docs/lessons.md, the journal. No positions.ts change, no editor change, no e2e change, no other fixture change, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every enumerated character's guard green and listed in the journal against the `safe()`/`unsafe.js` line that names it, each asserting bytes, the fixed point, one `link` with the source url and the text equality; the absence cases green with the autolink form kept; the backslash, `\\\\_` and `www.` guards green; the five invariant-A sources format to themselves; the able-to-fail mutation recorded with the doubled bytes it produced; the `formatWithMap` guard green with the escape's spelling asserted; `grep -c 'resourceLink' packages/core/src/format.ts` is at least 2; `classifyCharacter`, `encodeInfo`, `handleDelete`, `widenSplitSurrogateReferences` and `SPLIT_PAIR` unchanged (`git diff` shows no change inside them) and `git diff --stat` shows no change to packages/core/src/positions.ts; the fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg and resolved by \"maps every node of the tree, and nothing is unresolved\" (count from the index); `fixtures/markdown/autolink-literal.canonical.md` byte-identical and every other fixture byte-identical; the schema-roundtrip, roundtrip, fixtures-corpus, editor-fixed-point and positions suites green with their meaning unchanged; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.49"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.51",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r6 L3 — Claude finding 3 should-fix, Sol finding 2 should-fix Required; L4 — Claude finding 2 should-fix Required; packages/core/src/positions.ts, the position map of passed task 1.2 and of task 1.46, whose records are not edited): two node classes the map cannot place a caret in. (L3) `rewrittenEmissions` (positions.ts:562) says its three candidates are \"the whole enumeration\" of the parent's rewrites, but `mdast-util-to-markdown/lib/util/container-phrasing.js` (2.1.2; read it in node_modules first) has a third branch, lines 60–72: when the next child is `html`, the previous child's trailing line ending is replaced by a space (`results[results.length - 1].replace(/(\\r?\\n|\\r)$/, ' ')`) — so a text child ending in a soft line break before inline html is `unresolved`, and every caret in it answers the paragraph's start. Reproduced by the reconciliation headlessly at c2f6526: `formatWithMap(parse(\"alpha beta\\n<span>x</span> gamma\\n\")).map.unresolved` is `[\"0.0\"]` (the bytes `alpha beta <span>x</span> gamma\\n`, a fixed point), `alpha\\n<i>beta</i>` `[\"0.0\"]`, `*a*\\n<b>x</b>` `[\"0.1\"]`, `alpha <i>beta</i>` (no break) empty; through `cursorMap(pmToMdast(mdastToPM(parse(·))), doc)` on the first every ProseMirror position 1–11 answers `toSource` `1:0` and `toRendered` of it `0`; Sol's browser route (`/logs/reviews/1/r6/sol/html-map-browser.mjs`): `alpha\\n<i>beta</i>`, the rendered caret asserted at `{ text: \"alpha\\n\", offset: 5 }`, toggle, type `X` → the clipboard `Xalpha <i>beta</i>\\n` instead of `alphaX <i>beta</i>\\n`. positions.test.ts:306 already exercises this rewrite for a `break` child, so the file contradicted its own doc comment, and no fixture holds inline html (`raw-html.md` is block html). (L4) `inlineCode` is a value-bearing node with no spelling table, so `toSource` answers `range.startCol` for every position strictly inside a code span and `toRendered` of the span's end (when the span is a block's last node) answers its start. Reproduced headlessly: on `a `+\"`cd`\"+` b` the inverse `toRendered(toSource(p)) === p` holds at every text position except the one between `c` and `d` (it answers the position before `c`); on `# h `+\"`c`\"+`` it fails inside the span and at the heading's end (`toSource` answers the end of the line, `toRendered` of it the span's start); the plain paragraph `a cd b` is a perfect inverse; Claude's typed route: `see `+\"`foo`\"+` (the input rule closes the span, caret after it), `ControlOrMeta+/`, type ` bar` → pane and Copy Markdown `see  bar`+\"`foo`\"+`\\n` — the keystrokes landed before the code span. Do four things. (1) `rewrittenEmissions`: a fourth candidate — the value with its trailing line ending replaced by one space — tried when the child's next sibling is `html` (pass the sibling or a flag from `placeChildren`; state which in the doc comment); rewrite the doc comment to derive the enumeration from container-phrasing.js's three branches with their line numbers as read (the two edge encodings, the eol-before-html replacement), and to name the three forms task 1.49's wrapper widens (a reference beside a raw mate, both orders, and two adjacent references) — DECISIONS #review-1-r4 J2: from the branches, never from the type list; the spelling table for the rewritten child records the line ending's spelling as the one space at the same offset (one unit for one unit, so every offset before and after it is unchanged), the rule stated once in `SpellingTable`'s doc comment beside the surrogate rule. (2) A spelling table for `inlineCode` in `placeChildren`: its emission is the fence, optional padding, the value one UTF-16 unit per character, the padding, the fence (read `lib/handle/inline-code.js` for when the padding is written and how a value holding a backtick sequence lengthens the fence); the fence and the padding are owned by no character (a position before the first character maps to the offset after the opening fence and padding; the position after the last character to the offset before the closing padding and fence), the rule stated once in `SpellingTable`'s doc comment, so `toSource`/`toRendered` use the table exactly as they do for `text` (toggle.ts reads `spellings[path]` and needs no change — confirm, and if a change is needed stop and write the conflict to the journal, because toggle.ts is task 1.52's scope). (3) Unit guards in packages/core/test/positions-encoded-neighbour.test.ts (or a new file): the three html sources above each asserting `unresolved` empty, the text node's range covering its bytes with the space, the spelling of the line ending as the space, and `nodeAt` inside the node answering it; the no-break control unchanged; a `break` child before html (the positions.test.ts:306 shape) unchanged; inline code: `a `+\"`cd`\"+` b`, `# h `+\"`c`\"+``, a padded value (`` ` a` ``), a value holding a backtick (a two-backtick fence), an astral value (`` `😀` ``), each asserting the spelling table's entries at the first, middle and last position and the fence and padding owned by no character; and in packages/editor/test/toggle.test.ts one case per source above asserting `toRendered(toSource(p)) === p` for every text position `p` of the ProseMirror doc (every position whose parent is a textblock, computed from the doc, never a literal) and `toSource` monotone over them. Asserted able to fail, recorded in the journal and not committed: the fourth candidate removed goes red on the html corpus fixture below; the `inlineCode` table removed goes red on the toggle inverse case for `a `+\"`cd`\"+` b`. (4) A fixture fixtures/markdown/inline-html.md of exactly three paragraphs — `alpha beta\\n<span>x</span> gamma` (a soft break before an inline tag), `*a*\\n<b>x</b>` (inside a mark's neighbourhood), `alpha <i>beta</i> gamma` (no break) — with its canonical sibling (`format(parse(·))`: the space written for the line ending on the first two, byte-identical on a second pass) and its index.json entry, so the identity corpus, every corpus leg and \"maps every node of the tree, and nothing is unresolved\" cover the shape (count from the index, never a literal; the promoted rule's corpus case is the guard that names which rewrites exist, and this fixture is the member it names for the third branch); and one Playwright case in a new e2e/web/editor-toggle-inline-html.spec.ts on `/dev/editor` with the `writeText` spy of 1.28 (helpers copied from editor-toggle-encoded-neighbour.spec.ts, the 200 ms tick before the chord included — the `[1.46, found outside scope]` line): load `alpha\\n<i>beta</i>\\n`, click the paragraph, `ArrowUp`, counted `ArrowRight` to the anchor asserted as `{ text: \"alpha\\n\", offset: 5 }` (assert whatever the DOM reports and record it), `ControlOrMeta+/`, type `X`, toggle back, Copy Markdown → the spy's bytes `alphaX <i>beta</i>\\n` with `toBe`, a fixed point, and the rendered paragraph's `textContent` starting `alphaX`. Caret placement only by a click, `ArrowUp` and counted horizontal arrows with the anchor asserted first (DECISIONS #022, #024). Scope: packages/core/src/positions.ts, packages/core/test/**, packages/editor/test/toggle.test.ts, fixtures/markdown/inline-html.md, fixtures/markdown/inline-html.canonical.md, fixtures/markdown/index.json, e2e/web/editor-toggle-inline-html.spec.ts, docs/lessons.md, the journal. No change to toggle.ts, format.ts or any other fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The three html guards and the inline-code guards green and listed in the journal against their source strings, each asserting `unresolved` empty, the range, the spelling entries at first, middle and last position and `nodeAt`; the two absence cases green; the toggle inverse cases green with the position set computed from the doc; both able-to-fail mutations recorded with the case each turned red; the `rewrittenEmissions` doc comment naming container-phrasing.js's three branches by line and the wrapper's three forms (`grep -c 'html' packages/core/src/positions.ts` grown from the branch base, read both numbers into the journal); `SpellingTable`'s doc comment stating the line-ending-as-space rule and the inline-code ownership rule; `git diff --stat` shows no change to packages/editor/src/toggle.ts or packages/core/src/format.ts; the fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg and resolved by \"maps every node of the tree, and nothing is unresolved\" (count from the index); every other fixture byte-identical; the Playwright case green in the container with the anchor asserted before the arrows, the copied bytes asserted by `toBe`, the fixed point and the `textContent` (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowDown' e2e/web/editor-toggle-inline-html.spec.ts` is 0); the cursor, toggle, 200-seeded-position, positions and editor-fixed-point suites green with their meaning unchanged; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.50"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.52",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r6 L5 — Claude finding 4 should-fix; and the corpus half of L4/L3 — Claude's risk 2, Sol's risk 2; packages/editor/src/toggle.ts, the cursor map of passed tasks 1.7, 1.37, 1.43, whose records are not edited): the position at the end of a table cell's text maps back to the row's start, and the inverse `toRendered(toSource(p)) === p` is asserted only for mark neighbours, one plain paragraph and one cell interior. Reproduced by the reconciliation headlessly at c2f6526 on `| h1 | h2 |\\n| -- | -- |\\n| ab | z |`: the end of `h1` (ProseMirror position 5) answers `toSource` `1:4` and `toRendered` of it 1; the end of `h2` (9) → `1:9` → 1; the end of `ab` (15) → `3:4` → 11; the end of `z` (18) → `3:8` → 11 — every cell's end lands at its row's first position, header row included, while every interior position is the identity; the cause: `toSource` of a cell's end is the column before the cell's padding (correct), `nodeAt` there answers the `tableRow` (the text's range is exclusive of that column, the row's is not), and `delimiterPosition` (toggle.ts:441) returns `entry.pmStart` for anything that is not a mark. Claude's browser route: caret at the end of `zq` (asserted `{ text: \"zq\", offset: 2 }`), toggle, toggle back → caret `{ text: \"ab\", offset: 0 }`, type `X` → `| Xab | zq |` — the keystroke lands in a different cell. toggle.test.ts:251–274 asserts the inverse only at an interior cell position. Do three things. (1) In `toRendered`, when `found` is a `tableCell`, `tableRow` or `table` and the position is at or past a cell's `endCol` on that cell's line, answer that cell's last descendant's `pmEnd` (an empty cell: its own `pmEnd`), the rule `afterLastLine` already applies at a paragraph's end, stated once in `delimiterPosition`'s or a new helper's doc comment with the ownership rule for the padding column (the row owns its delimiters and padding, the cell's end position belongs to the cell); `toSource` is unchanged. (2) The corpus property leg — the invariant the fixes of 1.46, 1.51 and this task are members of, asserted once over the corpus with the members as named guards on top (CLAUDE.md's rule): in packages/editor/test/toggle.test.ts (or a new position-map-inverse.test.ts), for every fixture in fixtures/markdown/index.json (count from the index), build `doc = mdastToPM(parse(fixture)).doc`, `root = pmToMdast(mdastToPM(parse(fixture)))`, `map = cursorMap(root, doc)`, and for every position `p` whose `doc.resolve(p).parent.isTextblock` (computed from the doc, never a literal; the positions between blocks are not caret positions) assert `map.toRendered(map.toSource(p)) === p` and that `toSource` is monotone in `p` within each textblock, with the reach count of positions asserted `> 0` per fixture and the fixtures reached named (the test title names the leg: \"cursor map: toRendered ∘ toSource is the identity over every text position of every fixture\"); a second leg seeded from the writing surface's own output — the same assertion after `deleteAtEveryBlockEnd` and `deleteBesideEveryMarkedRun` of typing-legs.ts (imported, unchanged) over every fixture, the destructive transactions CLAUDE.md asks for. Named guards on top: the table document above (every cell end, header row included), `a `+\"`cd`\"+` b` and `# h `+\"`c`\"+`` (task 1.51's spelling table), `alpha beta\\n<span>x</span> gamma` (1.51's fourth candidate), `*a.*&#x1F600;*(b)*` (1.49), each asserting the inverse over every text position. Asserted able to fail, recorded in the journal and not committed: the cell-end clause disabled goes red on the table guard and on the corpus leg for the table fixtures (name them). (3) Two Playwright cases in a new e2e/web/editor-toggle-cell-end.spec.ts on `/dev/editor` with the `writeText` spy of 1.28 (helpers copied from editor-toggle-encoded-neighbour.spec.ts, the 200 ms tick before the chord included): (a) Claude's route — load `| h1 | h2 |\\n| -- | -- |\\n| ab | zq |\\n`, click the `zq` cell, `ArrowUp` then counted `ArrowRight` to the anchor asserted as `{ text: \"zq\", offset: 2 }`, `ControlOrMeta+/`, toggle back, the anchor asserted unchanged, type `X`, Copy Markdown → the spy's bytes hold `| ab | zqX |` (`toBe` on the whole string), a fixed point; (b) Claude's inline-code route — click the empty editor, `keyboard.type(\"see `+\"`foo`\"+`\")` (assert one `<code>` whose `textContent` is `foo` first), `ControlOrMeta+/`, type ` bar`, toggle back, Copy Markdown → `see `+\"`foo`\"+` bar\\n` (`toBe`), a fixed point, the paragraph's `textContent` `see foo bar`. Caret placement only by a click, `ArrowUp`, counted horizontal arrows with the anchor asserted first, and typing (DECISIONS #022, #024). Scope: packages/editor/src/toggle.ts, packages/editor/test/**, e2e/web/editor-toggle-cell-end.spec.ts, docs/lessons.md, the journal. No change to positions.ts, format.ts, typing-legs.ts or any fixture, no config or workflow edits, zero new dependencies (PRD §4). A position class the corpus leg turns red that is not one of the members named here is a defect found outside this scope: a docs/V1.1-BACKLOG.md line with the fixture and position named, a revisit trigger and a hard stop, and the leg asserts it as a named expected member (`it.fails` with the fixture named) rather than being narrowed — stop and write the conflict to the journal if that would leave `pnpm test` with an expected fail (DECISIONS #028). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The cell-end clause green on the table guard for every cell end, header row included; the corpus inverse leg green over every fixture in the index with its reach count `> 0` per fixture and the fixtures named, and the destructive-transaction leg green over every fixture, both titles naming the leg; the named member guards green (the table, both inline-code sources, the inline-html source, the astral-between-runs source); the able-to-fail mutation recorded with the guards and fixtures it turned red; `git diff --stat` shows no change to packages/core/src/positions.ts, packages/core/src/format.ts or packages/editor/test/typing-legs.ts; both Playwright cases green in the container with the anchors asserted, the copied bytes asserted by `toBe` and the fixed points (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowDown' e2e/web/editor-toggle-cell-end.spec.ts` is 0); the cursor, toggle, 200-seeded-position and editor-fixed-point suites green with their meaning unchanged; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.51"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.53",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r6 L6 — Claude finding 5 should-fix; packages/editor/src/toggle.ts, the cursor map of passed tasks 1.7, 1.37, 1.43 and 1.52, whose records are not edited): the two views disagree about the same caret at a marked run's end. `innermostAt` (toggle.ts:260, \"a boundary belongs to the later node\") maps a rendered caret at the end of a run whose last character is punctuation (`*wow!*`, `~~beta.~~`) to the source position *after* the closing delimiter, so a letter typed there in the source view un-forms the run (CommonMark §6.2: `~~beta.~~X` cannot close) and the toggle back shows the delimiters as escaped literal text, while the same caret typed in the rendered view extends the run (ProseMirror's inclusive marks). Reproduced by the reconciliation headlessly at c2f6526: on `Alpha ~~beta.~~ gamma` the inverse `toRendered(toSource(p)) === p` holds at every text position (this is not an inverse defect and task 1.52's corpus leg stays green through it), and `toSource` of the position after `beta.` (12) answers the column after `~~` — Claude's browser route: caret at the run's end (DOM anchor `{ text: \"beta.\", offset: 5 }`), toggle, `X`, toggle → pane `Alpha \\\\~\\\\~beta.\\\\~\\\\~X gamma\\n`, rendered text `Alpha ~~beta.~~X gamma`; the same caret typed in the rendered view gives `Alpha ~~beta.X~~ gamma\\n`. Read first, in node_modules: `prosemirror-model`'s `ResolvedPos.marks()` (dist/index.js, the method's whole body): inside a text node the node's marks; at a boundary the marks of the node *before* (the node after only at the parent's start), minus every mark with `inclusive: false` absent from the other side — that is the rule the rendered view types by, and `packages/editor/src/schema.ts` gives `link` `inclusive: false` and `inline_code` `code: true` (read its spec for what ProseMirror does with a code mark at a boundary). Do three things. (1) In `toSource`, at a boundary between two inline nodes of the entered block (a position equal to one node's `pmEnd` and the next node's `pmStart`), choose the node whose ProseMirror marks equal `doc.resolve(pos).marks()` (the doc is in reach — `cursorMap(root, doc)`) — the earlier node when the marks at the boundary are its marks, the later node otherwise — so the source caret lands where a typed character would take the same marks the rendered view gives it; state the rule once in `innermostAt`'s doc comment, derived from `ResolvedPos.marks()` and never re-implemented as a hand list of mark types, and keep the block's start (no node before) and the block's end as they are. The inverse over every text position stays the identity (a boundary is one ProseMirror position whichever side the source caret takes) — 1.52's corpus leg is the guard, unchanged. (2) Guards in packages/editor/test/toggle.test.ts, a describe \"a boundary between marked and unmarked text is resolved by `$pos.marks()`\": for each of `emphasis`, `strong`, `delete` × a punctuation edge and a letter edge × the run's end and the run's start (the position after `Alpha ` before `~~beta.~~`, where `marks()` gives the unmarked side), assert `toSource`'s column against the expected side (inside the closing delimiter at the end, before the opening delimiter at the start), and — the semantic instrument — that inserting `X` in the source at that column and formatting the reparsed tree gives the same bytes as inserting `X` at `pos` in the ProseMirror doc with `doc.resolve(pos).marks()` and running it through `pmToMdast` and `format` (the two views agree; the mark count survives at the end, the run is not extended at the start); a `link` end (inclusive false: the caret leaves the link on both sides) and an `inline_code` end as the two non-inclusive members; the inverse asserted over every text position of each document. Asserted able to fail, recorded in the journal and not committed: the rule reverted to \"the later node\" goes red on the punctuation-edge end cases with the escaped-delimiter bytes it produces. (3) One Playwright case in a new e2e/web/editor-toggle-run-end.spec.ts on `/dev/editor` with the `writeText` spy of 1.28 (helpers copied from editor-toggle-encoded-neighbour.spec.ts, the 200 ms tick before the chord included): load `Alpha ~~beta.~~ gamma\\n`, click the paragraph, `ArrowUp`, counted `ArrowRight` to the anchor asserted as `{ text: \"beta.\", offset: 5 }` (assert whatever the DOM reports and record it), `ControlOrMeta+/`, type `X`, toggle back, Copy Markdown → the spy's bytes `Alpha ~~beta.X~~ gamma\\n` with `toBe`, a fixed point, one `<del>` whose `textContent` is `beta.X`, no backslash in the pane. Caret placement only by a click, `ArrowUp` and counted horizontal arrows with the anchor asserted first (DECISIONS #022, #024). Scope: packages/editor/src/toggle.ts, packages/editor/test/toggle.test.ts, e2e/web/editor-toggle-run-end.spec.ts, docs/lessons.md, the journal. No change to positions.ts, format.ts, schema.ts or any fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The boundary guards green for every (mark, edge class, side) tuple and listed in the journal, each asserting the column and the two-view byte agreement; the `link` and `inline_code` end cases green; the inverse green over every text position of each guard document and 1.52's corpus leg green unchanged; the able-to-fail mutation recorded with the escaped-delimiter bytes it produced; `innermostAt`'s doc comment naming `ResolvedPos.marks()` (`grep -c 'marks()' packages/editor/src/toggle.ts` at least 1); `git diff --stat` shows no change to packages/core/src/positions.ts, packages/core/src/format.ts or packages/editor/src/schema.ts; the Playwright case green in the container with the anchor asserted, the copied bytes asserted by `toBe`, the fixed point, the `<del>` text and no backslash (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowDown' e2e/web/editor-toggle-run-end.spec.ts` is 0); the cursor, toggle, 200-seeded-position and editor-fixed-point suites green with their meaning unchanged; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.52"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.54",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r6 L7 — Sol finding 3 should-fix Required, Claude finding 6 should-fix; L8 — Claude finding 7 should-fix, the corpus-caller half; e2e/web/*.spec.ts and packages/editor/test/editor-fixed-point.test.ts, files of passed tasks whose records are not edited): the gate's own instrument fires counted arrow presses blind, and the instrument that sees U+FFFD is in two corpus legs only. (L7) Every `press(page, key, times)` helper under e2e/web/ (eight editor specs: editor-break-in-mark, editor-link-edge, editor-mark-edge, editor-soft-line-breaks, editor-strikethrough-neighbour, editor-toggle-encoded-neighbour, editor-toggle-trailing-break, editor-trailing-break; count from the directory with `grep -l 'async function press' e2e/web/*.spec.ts`, never a literal — latency.spec.ts's `press(page, count)` is a timing loop and is not touched) fires `keyboard.press` `times` times without reading the caret between presses. The 1.verify.r6h gate's a1 (ci.yml run 34735711543 at c2f6526, windows-latest) failed G4 of editor-soft-line-breaks.spec.ts:111 one press short — the location assertion at line 133 read offset 6 where 11 `ArrowLeft` from the asserted anchor at 16 should leave 5 — and Sol reproduced the same failure once on Linux in a full single-worker run overlapping a cold compilation (`/logs/reviews/1/r6/sol/e2e.log:108–139`); a2 and Sol's isolated rerun passed 88/88 (DECISIONS #029, #023's criterion: transient, no code change; this task is the repair of the instrument, not of the product). Rewrite each spec's `press` helper so that after every press it waits for the DOM caret to move: read the caret with the spec's own `caret(page)` helper before the press and `expect.poll` it to differ afterwards (the anchor text or the offset), the spec's existing anchor assertions before and after the count unchanged — the anchor-and-count route of DECISIONS #024 stays the route, each step now synchronised; a spec whose count crosses a node boundary (the anchor text changes, offset resets) is covered by \"differs\", state that in the helper's doc comment; keep the helper self-contained per spec (a spec file imports nothing from a sibling). Assert able to fail, recorded in the journal and not committed: a scratch copy of one spec with the poll removed and a `times` one too many turns its location assertion red the same way a1 did (the received offset recorded). (L8) The assertion 1.47's description named for `expectNoForbiddenEntity`'s corpus callers in packages/editor/test/editor-fixed-point.test.ts — the twenty callers the two mark's-neighbour legs do not cover (count them from the file) — was narrowed to the two legs because the space-typing legs' editor text holds whitespace the strip drops (DECISIONS #review-1-r6 L8 records the re-reading under 1.47); give every caller the instrument in the form that is immune to that: per inline block, `textValues(parse(out))` joined with every ASCII whitespace run removed (`replace(/[ \\t\\n]+/g, \"\")`) equals the ProseMirror block's `textContent` with the same removal — the comparison still sees U+FFFD and any lost non-whitespace character, and it is a new assertion in each caller, so run it once over the corpus first and record in the journal every fixture and leg it turns red before deciding: a red that is a real defect (a character lost or replaced) is a defect found outside this scope — a docs/V1.1-BACKLOG.md line with the fixture, leg and bytes named, a revisit trigger and a hard stop, and the assertion asserts it as a named expected member rather than being narrowed; stop and write the conflict to the journal if that would leave `pnpm test` with an expected fail (DECISIONS #028). Assert able to fail, recorded in the journal and not committed: the assertion run against a formatter with task 1.49's third `SPLIT_PAIR` form disabled goes red on `astral-between-runs.md` in the typing legs that reach it (name them). Scope: e2e/web/editor-break-in-mark.spec.ts, e2e/web/editor-link-edge.spec.ts, e2e/web/editor-mark-edge.spec.ts, e2e/web/editor-soft-line-breaks.spec.ts, e2e/web/editor-strikethrough-neighbour.spec.ts, e2e/web/editor-toggle-encoded-neighbour.spec.ts, e2e/web/editor-toggle-trailing-break.spec.ts, e2e/web/editor-trailing-break.spec.ts (and any other spec the grep lists, named in the journal), packages/editor/test/editor-fixed-point.test.ts, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md, the journal. No source change, no fixture change, no change to typing-legs.ts, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every `press` helper under e2e/web/ except latency.spec.ts's polls the caret after each press (`grep -c 'expect.poll' ` on each listed spec grown from the branch base, both numbers in the journal; `grep -L 'caret(page)' ` over the listed specs prints nothing), the existing anchor assertions unchanged, the scratch mutation recorded red with the received offset; every `expectNoForbiddenEntity` caller carries the whitespace-removed text equality (the caller count read from the file and every caller listed in the journal against its leg), green over every fixture in the index, its mutation recorded red on `astral-between-runs.md`; `git diff --stat` shows no change under packages/*/src, fixtures/ or packages/editor/test/typing-legs.ts; `pnpm test`'s summary shows no expected fail; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.53"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r7",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r7 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r7 chain — four candidates weighed at DECISIONS #review-1-r6, two promoted and two recorded. (a) Promote, as a bullet of `## Code rules` in both CLAUDE.md and AGENTS.md, verbatim and identical in both files: \"An enumeration of rewrites that can each fire on one character includes the shortest value on which two of them fire on the same character — one scalar shared by two delimiter runs — and a neighbour-class enumeration includes the cell where one outside character is the closing neighbour of one run and the opening neighbour of the next (DECISIONS #review-1-r6 L1).\" Discharged by 1.49's \"one astral character between two flanking runs\" guards in packages/core/test/strikethrough-flanking.test.ts and `astral-between-runs.md` in the index — name both by title in the journal. (b) Promote, as the next bullet, verbatim: \"The cursor-map family asserts `toRendered(toSource(p)) === p` and the monotonicity of `toSource` over every text position of every fixture through the editor, seeded from parse(fixture) and from a destructive transaction, and every position class that is not the identity is a named guard on top of that leg, never a doc-comment rule (DECISIONS #review-1-r6 L3–L5).\" Discharged by 1.52's corpus inverse leg and its destructive-transaction leg in packages/editor/test — name both by title in the journal. (c) Record, do not promote: Claude's r6 lesson that a backlog deferral whose trigger is \"the task that adds the next X\" never fires for a defect inside an X already installed addresses the principal's backlog hygiene (the `[review-1-r4, Claude risk 1]` line is corrected at #review-1-r6) and lands in docs/PRINCIPAL.md at the boundary; Sol's r6 lessons that monotonicity accepts a constant wrong map and that a passing rerun is not a diagnosis are carried by (b) and by 1.54's instrument repair; the journal says so in one sentence. (d) Record, do not promote: Grok's r6 lesson on the first read of a flanking review addresses reviewers (the r7 rows carry it). `ralph/check-agent-rules.sh` must exit 0 and tests/agent-rules.test.ts must pass after the edit. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal (1.47's journal said 21 where the directory held 22 — DECISIONS #review-1-r6 L10). No other procedure change this attempt. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time, then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5 and r6 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; `pnpm test`'s summary shows no expected fail; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes; the journal names the discharging test for each clause of (a) and (b) and records (c) and (d).",
  "dependencies": [
   "1.54"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r7/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r7h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r7h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r7",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r7/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r7: scripts/gate.sh 1.verify.r7h pushes integrated_sha of 1.verify.r7 to ci/1.verify.r7/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r7/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r7h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r7"
  ]
 },
 {
  "id": "1.55",
  "model": "opus",
  "description": "Phase 1 verification-gate repair, test-only (repair after the 1.verify.r7h GATE-FAILED, ci.yml run 35629907462 at fd03e40: windows-latest 91 passed / 2 failed on e2e/web, ubuntu-latest and macos-latest 93/93, every unit, lint and cargo step green on all three; DECISIONS #035): two spec defects, neither in the product. (A) `e2e/web/editor-astral-between-runs.spec.ts:144` (task 1.49): `formatParseInPage` imports the core module inside the page through Vite's `/@fs` route with a path built by `fileURLToPath` — on Windows the path is `D:\\a\\…\\index.ts`, the URL becomes `/@fsD:/a/essaydown/…` (no slash before the drive letter) and `page.evaluate` fails with `TypeError: Failed to fetch dynamically imported module`. The sibling `e2e/web/editor-astral-neighbour.spec.ts` (task 1.45, green on all three runners at every gate since r6h) asserts the same fixed point in the test process: `expect(format(parse(pane))).toBe(pane)` with `format` and `parse` imported from `../../packages/core/src/index.js` — imports this spec already has at its top. Do exactly that: delete `formatParseInPage`, `CORE_INDEX` and the `fileURLToPath` import, and assert the fixed point of the copied bytes and of the pane with the test-process `format(parse(·))`; nothing else in the file changes (the route, the anchors, the `toBe` on the copied bytes, the text equality and the `<em>` count stand). (B) `e2e/web/editor-soft-line-breaks.spec.ts:145`, the G4 case (tasks 1.38, 1.39, 1.54): task 1.54's per-step poll in `press` did what it was written for — it caught the press that never moves the DOM caret, `ArrowLeft` at the count's last step, crossing the soft line break from offset 6 (the start of `beta gamma`) to offset 5 (after `alpha`, before the LF), on windows-latest for the second gate in a row (1.verify.r6h a1 read offset 6 after the blind count, DECISIONS #029; Sol's r6 review reproduced it once on Linux under load). The container cannot observe that runner; two readings fit the evidence and the fix task does not decide between them — it chooses a route that is right under both and states in the journal which received value at the next gate discriminates them: (i) the leftward crossing of the LF is the one step Blink drops on that platform (a 5 would then be reached by any route that does not cross the break leftwards); (ii) Blink there reports the caret at the end of the first line as offset 6 (a rightward count within the first line would also read 6 at its last step). Replace the count in G4 by: `ArrowLeft` × `\"beta gamma\".length` (10) from the asserted block-end anchor, asserted at `{ kind: \"text\", text: \"alpha\\nbeta gamma\", offset: 6 }` (the start of the second line, no break crossed); one `ArrowUp` asserted at `{ …, offset: 0 }` — a vertical motion from the line's left edge lands at the first line's start on every OS (the x is the left edge, not a metric; the assertion is the proof, DECISIONS #024's rule that every vertical motion is followed by a landing assertion); then `ArrowRight` × `\"alpha\".length` (5) within the first line, asserted at `{ …, offset: 5 }` — the existing precondition, unchanged in meaning. Then type the space and add the discriminator the bytes cannot give (DECISIONS #022: a case whose bytes cannot tell two trees apart asserts the tree): the rendered text node after the keystroke is `\"alpha \\nbeta gamma\"` (the space before the LF — the end of the first line, not the start of the second and not the paragraph's end), read through `caret(page).text`; the existing assertions that the pane stays `alpha\\nbeta gamma\\n` and holds no `&#x20;` stand. The `press` helper itself (1.54's two forms) is not changed. Asserted able to fail, recorded in the journal and not committed: the rights count shortened by one turns the offset-5 assertion red; a space typed at the block's end instead turns the text-node discriminator red (`alpha\\nbeta gamma ` — record the received value). Honest statement in the journal: the container is Linux, the only proof for windows-latest is the gate, and under reading (ii) the gate will read 6 at the rights count's assertion — the next planning commit then adopts the discriminator alone with the DOM offset asserted as one of the two spellings Blink gives that caret. Scope: e2e/web/editor-astral-between-runs.spec.ts, e2e/web/editor-soft-line-breaks.spec.ts, docs/lessons.md, the journal. No source, fixture, unit-test, config or workflow change; zero new dependencies (PRD §4). `tests/no-caret-chords-in-e2e.test.ts` guards both files (no Home/End key, no `ControlOrMeta+` chord; `ArrowUp` is allowed and followed by its assertion). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps; CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the mismatch is mechanical and not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "`grep -c '@fs' e2e/web/editor-astral-between-runs.spec.ts` is 0 and `grep -c 'fileURLToPath' e2e/web/editor-astral-between-runs.spec.ts` is 0, `grep -c 'format(parse(' e2e/web/editor-astral-between-runs.spec.ts` is at least 1, and that spec is green in the container with every other assertion unchanged; in `e2e/web/editor-soft-line-breaks.spec.ts` the G4 case asserts the anchor at offset 16, then offset 6 after ten `ArrowLeft`, offset 0 after one `ArrowUp`, offset 5 after five `ArrowRight`, and the text node `alpha \\nbeta gamma` after the space (`grep -c 'alpha \\\\nbeta gamma' e2e/web/editor-soft-line-breaks.spec.ts` at least 1), the pane bytes unchanged and no `&#x20;`; both mutations recorded in the journal with their received values; the journal states the two readings and the received value at the gate that discriminates them; `git diff --stat` against the branch base touches only the two spec files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md (`grep -c` of the diff for `packages/` and `fixtures/` is 0); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container (count from the directory); `tests/no-caret-chords-in-e2e.test.ts` green; no manifest or lockfile change.",
  "dependencies": [
   "1.verify.r7h"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r7.g1",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r7, repair 1 of 1.verify.r7 after the 1.verify.r7h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #035): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. No procedure change and no code, test, fixture or config edit. The promote-lessons step carried by `1.verify.r7` (DECISIONS #review-1-r6 candidates (a) and (b)) is integrated at fd03e40 and is not repeated. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on. A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps; CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the mismatch is mechanical and not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only docs/lessons.md and the journal.",
  "dependencies": [
   "1.55"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r7.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r7.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r7.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r7.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r7.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r7.g1: scripts/gate.sh 1.verify.r7.g1h pushes integrated_sha of 1.verify.r7.g1 to ci/1.verify.r7.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r7.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r7.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r7.g1"
  ]
 },
 {
  "id": "1.56",
  "model": "sonnet",
  "description": "Phase 1 verification-gate repair, test-only (repair after the 1.verify.r7.g1h GATE-FAILED, ci.yml run 35685069545 at 5a39abd: windows-latest 92 passed / 1 failed on e2e/web, ubuntu-latest and macos-latest 93/93, every unit, lint and cargo step green on all three; DECISIONS #036): one case, `e2e/web/editor-soft-line-breaks.spec.ts` G4 (\"a space typed at the end of the first line of a wrapped paragraph carries no entity\"), for the third gate in a row on that runner and by a third route. The log this time (workflow.log, the assertion at spec line 164): the ten `ArrowLeft` were asserted at offset 6 and the `ArrowUp` at offset 0, and the five `ArrowRight` that followed ended at offset **11** — the caret was back at 6 before the first right, i.e. ProseMirror reverted the native `ArrowUp` it had not yet observed when the next key arrived (the class the `[1.46, found outside scope]` backlog line records: PM reads a native caret move from `selectionchange`, dispatched in the rendering update after the key, and the container and the other two runners happen to be past that step before the next key lands). Every arrow route to this position has now failed there: the blind eleven lefts (1.verify.r6h a1, #029), the polled eleven lefts (1.verify.r7h a1, #035), up-then-rights (this gate). Do exactly this, in that one case and nowhere else: (1) place the caret at the end of the first line by **a click at a computed point** (the route CLAUDE.md and DECISIONS #024 allow beside arrows, and the one that involves no second key while a first is still being observed — a click is one native event, and ProseMirror syncs its selection from the DOM on `mouseup`): in `page.evaluate`, find the paragraph's text node whose data is `alpha\\nbeta gamma`, build a `Range` over its character 4–5 (the last `a` of `alpha`), take `getBoundingClientRect()` and return `{ x: rect.right - 1, y: rect.top + rect.height / 2 }`; `page.mouse.click(x, y)`; then assert the caret as the DOM reports it — `kind: \"text\"`, `text: \"alpha\\nbeta gamma\"`, and the offset recorded in the journal from the container run (expect `\"alpha\".length`, 5; if the container reports 6 stop and write it to the journal, do not decide); keep the block-end anchor assertion that precedes the route (it is the seed's own check) and delete the ten-left / `ArrowUp` / five-right steps with their assertions and the comment that argued for them, replacing the comment with the log evidence above in three sentences. (2) Keep task 1.55's discriminator exactly: after `keyboard.type(\" \")` the text node reads `\"alpha \\nbeta gamma\"` (the space before the LF — that assertion, not the offset, is what proves the space went to the first line's end: a space at DOM offset 6 would read `\"alpha\\n beta gamma\"`), the pane bytes unchanged, no `&#x20;`. (3) Nothing else changes: the `press` helper (1.54's two forms), the other cases, `caret`, `seedFromSource`. Asserted able to fail, recorded in the journal and not committed: the click point moved to the paragraph's end (`rect.right` of the last character of `gamma`) turns the text-node discriminator red with `alpha\\nbeta gamma ` (record the received value). Add one evidence line to docs/V1.1-BACKLOG.md under the `[1.46, found outside scope]` class naming this gate's received value (11 after five rights from an asserted 0) as the third instance of a native motion reverted before the next key on windows-latest, hard stop unchanged. Honest statement in the journal: the container is Linux; the only proof for windows-latest is the gate. Scope: e2e/web/editor-soft-line-breaks.spec.ts, docs/V1.1-BACKLOG.md (the one evidence line), docs/lessons.md, the journal. No source, fixture, unit-test, config or workflow change; zero new dependencies (PRD §4). `tests/no-caret-chords-in-e2e.test.ts` guards the file (no Home/End key, no `ControlOrMeta+` chord). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps; CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the mismatch is mechanical and not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "In `e2e/web/editor-soft-line-breaks.spec.ts` the G4 case places the caret by `page.mouse.click` at a point computed from the text node's `Range` rect (`grep -c 'mouse.click' e2e/web/editor-soft-line-breaks.spec.ts` at least 1; `grep -c 'ArrowUp' e2e/web/editor-soft-line-breaks.spec.ts` is 0), asserts the caret's kind, text and the recorded offset, then the text node `alpha \\nbeta gamma` after the space (`grep -c 'alpha \\\\nbeta gamma' e2e/web/editor-soft-line-breaks.spec.ts` at least 1), the pane bytes unchanged and no `&#x20;`; the mutation recorded in the journal with its received value; the backlog evidence line present; `git diff --stat` against the branch base touches only that spec, docs/V1.1-BACKLOG.md, docs/lessons.md and docs/progress/journal-main.md (`grep -c` of the diff for `packages/` and `fixtures/` is 0); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container (count from the directory); `tests/no-caret-chords-in-e2e.test.ts` green; no manifest or lockfile change.",
  "dependencies": [
   "1.verify.r7.g1h"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r7.g2",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r7, repair 2 of 1.verify.r7 after the 1.verify.r7.g1h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #036): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. No procedure change and no code, test, fixture or config edit. The promote-lessons step carried by `1.verify.r7` is integrated at fd03e40 and is not repeated. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps; CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the mismatch is mechanical and not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only docs/lessons.md and the journal.",
  "dependencies": [
   "1.56"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r7.g2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r7.g2h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r7.g2h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r7.g2",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r7.g2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r7.g2: scripts/gate.sh 1.verify.r7.g2h pushes integrated_sha of 1.verify.r7.g2 to ci/1.verify.r7.g2/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r7.g2/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r7.g2h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r7.g2"
  ]
 },
 {
  "id": "1.10.r7a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r7",
  "description": "Review attempt r7, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r7 (after the r6 FAIL, DECISIONS #review-1-r6; all three r6 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r6: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r7.g2h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r7-specific checks: each r6 finding's disposition in DECISIONS #review-1-r6 (L1–L11) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the third `SPLIT_PAIR` form in packages/core/src/format.ts (1.49) read against the installed lib/util/container-phrasing.js (its three branches, lines read) as the join of its two edge encodings on one two-unit child, and shown to keep `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and the wrapper's installation unchanged; the `link` wrapper (1.50) read against lib/handle/link.js, lib/util/format-link-as-autolink.js, lib/util/safe.js and lib/unsafe.js, its criterion (the autolink form kept exactly when the bytes between `<` and `>` equal `node.url`) checked against every character `safe()` rewrites with the stack hidden, and `autolink-literal.canonical.md` confirmed byte-identical; `rewrittenEmissions`'s fourth candidate and the `inlineCode` spelling table in packages/core/src/positions.ts (1.51) read against container-phrasing.js's third branch and lib/handle/inline-code.js, with the doc comment's enumeration derived from the branches; the cell-end rule and the corpus inverse leg (1.52) read for the position set being computed from the doc and for the destructive-transaction seed; the boundary rule (1.53) read against prosemirror-model's `ResolvedPos.marks()` in node_modules and shown never to re-implement it as a list of mark types; every `press` helper under e2e/web/ (1.54) read for the per-step caret poll, and the whitespace-removed text equality read in every `expectNoForbiddenEntity` caller; in a browser: Sol's r6 route (`*a.* 😀 *(b)*`, the two spaces deleted, Copy Markdown) and Grok's typed twin; a loaded `see https://en.wikipedia.org/wiki/Foo\\_bar end` copied (1.50 — the resource form, the url unchanged in the `href`); the toggle-then-type routes into a soft break before inline html (1.51), a table cell's end and after an inline code span (1.52), and a punctuation-edged run's end (1.53); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the two r6 deferrals are recorded there and are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text serialises with `https\\://` escapes the app's own parser undoes — a representation decision deferred to the Phase 2 file-open task with hard stop `2.verify`) and L11 (two adjacent same-mark attention runs from a file are not a fixed point — hard stop `2.verify`); and the r6 class (DECISIONS #review-1-r6, Claude's risks 1 and 2, Sol's 1 and 2, Grok's 2): any character reference the serializer writes that does not decode to the tree's text, any node `formatWithMap` leaves unresolved, any text position where `toRendered(toSource(p))` is not `p` or the two views take a typed character differently, for a tree `parse` produces or the editor builds — searched beyond the reproductions the fix tasks name, with one non-BMP seed per neighbour class and per inline position class, one seed per installed third-party handler's delimiter characters (a table cell holding `|`, a code span holding a backtick at its edge, a URL holding each character `safe()` rewrites), and one seed per pair of parent rewrites that can act on one character.",
  "acceptance": "/logs/reviews/1/r7/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r7.g2h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r7b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r7",
  "description": "Review attempt r7, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r7 (after the r6 FAIL, DECISIONS #review-1-r6; all three r6 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r6: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r7.g2h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r7-specific checks: each r6 finding's disposition in DECISIONS #review-1-r6 (L1–L11) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the third `SPLIT_PAIR` form in packages/core/src/format.ts (1.49) read against the installed lib/util/container-phrasing.js (its three branches, lines read) as the join of its two edge encodings on one two-unit child, and shown to keep `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and the wrapper's installation unchanged; the `link` wrapper (1.50) read against lib/handle/link.js, lib/util/format-link-as-autolink.js, lib/util/safe.js and lib/unsafe.js, its criterion (the autolink form kept exactly when the bytes between `<` and `>` equal `node.url`) checked against every character `safe()` rewrites with the stack hidden, and `autolink-literal.canonical.md` confirmed byte-identical; `rewrittenEmissions`'s fourth candidate and the `inlineCode` spelling table in packages/core/src/positions.ts (1.51) read against container-phrasing.js's third branch and lib/handle/inline-code.js, with the doc comment's enumeration derived from the branches; the cell-end rule and the corpus inverse leg (1.52) read for the position set being computed from the doc and for the destructive-transaction seed; the boundary rule (1.53) read against prosemirror-model's `ResolvedPos.marks()` in node_modules and shown never to re-implement it as a list of mark types; every `press` helper under e2e/web/ (1.54) read for the per-step caret poll, and the whitespace-removed text equality read in every `expectNoForbiddenEntity` caller; in a browser: Sol's r6 route (`*a.* 😀 *(b)*`, the two spaces deleted, Copy Markdown) and Grok's typed twin; a loaded `see https://en.wikipedia.org/wiki/Foo\\_bar end` copied (1.50 — the resource form, the url unchanged in the `href`); the toggle-then-type routes into a soft break before inline html (1.51), a table cell's end and after an inline code span (1.52), and a punctuation-edged run's end (1.53); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the two r6 deferrals are recorded there and are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text serialises with `https\\://` escapes the app's own parser undoes — a representation decision deferred to the Phase 2 file-open task with hard stop `2.verify`) and L11 (two adjacent same-mark attention runs from a file are not a fixed point — hard stop `2.verify`); and the r6 class (DECISIONS #review-1-r6, Claude's risks 1 and 2, Sol's 1 and 2, Grok's 2): any character reference the serializer writes that does not decode to the tree's text, any node `formatWithMap` leaves unresolved, any text position where `toRendered(toSource(p))` is not `p` or the two views take a typed character differently, for a tree `parse` produces or the editor builds — searched beyond the reproductions the fix tasks name, with one non-BMP seed per neighbour class and per inline position class, one seed per installed third-party handler's delimiter characters (a table cell holding `|`, a code span holding a backtick at its edge, a URL holding each character `safe()` rewrites), and one seed per pair of parent rewrites that can act on one character. Sol's r6 probes (between-browser.mjs, between-probe.mjs, extended-probe.mjs, html-map-probe.mjs, html-map-browser.mjs, the r5 serializer and map probes) are under /logs/reviews/1/r6/sol/ — re-run the extended probe and the html map probe at this SHA and report the fixed-point-failure, parsed-text-mismatch and unresolved counts beside r6's 351 / 351 / 351 and the html probe's four of five.",
  "acceptance": "/logs/reviews/1/r7/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r7.g2h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r7c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r7",
  "description": "Review attempt r7, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r7 (after the r6 FAIL, DECISIONS #review-1-r6; all three r6 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r6: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r7.g2h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r7-specific checks: each r6 finding's disposition in DECISIONS #review-1-r6 (L1–L11) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the third `SPLIT_PAIR` form in packages/core/src/format.ts (1.49) read against the installed lib/util/container-phrasing.js (its three branches, lines read) as the join of its two edge encodings on one two-unit child, and shown to keep `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and the wrapper's installation unchanged; the `link` wrapper (1.50) read against lib/handle/link.js, lib/util/format-link-as-autolink.js, lib/util/safe.js and lib/unsafe.js, its criterion (the autolink form kept exactly when the bytes between `<` and `>` equal `node.url`) checked against every character `safe()` rewrites with the stack hidden, and `autolink-literal.canonical.md` confirmed byte-identical; `rewrittenEmissions`'s fourth candidate and the `inlineCode` spelling table in packages/core/src/positions.ts (1.51) read against container-phrasing.js's third branch and lib/handle/inline-code.js, with the doc comment's enumeration derived from the branches; the cell-end rule and the corpus inverse leg (1.52) read for the position set being computed from the doc and for the destructive-transaction seed; the boundary rule (1.53) read against prosemirror-model's `ResolvedPos.marks()` in node_modules and shown never to re-implement it as a list of mark types; every `press` helper under e2e/web/ (1.54) read for the per-step caret poll, and the whitespace-removed text equality read in every `expectNoForbiddenEntity` caller; in a browser: Sol's r6 route (`*a.* 😀 *(b)*`, the two spaces deleted, Copy Markdown) and Grok's typed twin; a loaded `see https://en.wikipedia.org/wiki/Foo\\_bar end` copied (1.50 — the resource form, the url unchanged in the `href`); the toggle-then-type routes into a soft break before inline html (1.51), a table cell's end and after an inline code span (1.52), and a punctuation-edged run's end (1.53); `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the two r6 deferrals are recorded there and are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text serialises with `https\\://` escapes the app's own parser undoes — a representation decision deferred to the Phase 2 file-open task with hard stop `2.verify`) and L11 (two adjacent same-mark attention runs from a file are not a fixed point — hard stop `2.verify`); and the r6 class (DECISIONS #review-1-r6, Claude's risks 1 and 2, Sol's 1 and 2, Grok's 2): any character reference the serializer writes that does not decode to the tree's text, any node `formatWithMap` leaves unresolved, any text position where `toRendered(toSource(p))` is not `p` or the two views take a typed character differently, for a tree `parse` produces or the editor builds — searched beyond the reproductions the fix tasks name, with one non-BMP seed per neighbour class and per inline position class, one seed per installed third-party handler's delimiter characters (a table cell holding `|`, a code span holding a backtick at its edge, a URL holding each character `safe()` rewrites), and one seed per pair of parent rewrites that can act on one character. Additionally (DECISIONS #review-1-r6 reviewer record; Grok's own r6 lesson): the first read of a flanking review is the assembler's rewrites, then the handler, then the pin's neighbouring branch — read container-phrasing.js's three branches against `SPLIT_PAIR`'s three forms and `rewrittenEmissions`'s four candidates before anything else, and state in the report, on your own reading, which of L1–L6 a read-only review could have found from the tree at c2f6526 and which severity it would have rated; your r6 report found L1 and named the html branch as a risk without a finding — say what read would have made it a finding.",
  "acceptance": "/logs/reviews/1/r7/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r7.g2h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r7d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r7",
  "description": "Reconciliation of review attempt r7 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r7-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r7 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r8 and 1.10.r8a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r7a",
   "1.10.r7b",
   "1.10.r7c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.57",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r7 M1 — Claude finding 1 blocker; packages/core/src/format.ts, a file of passed tasks 1.40, 1.45, 1.49 and 1.50, whose records are not edited — a new task per DECISIONS #016): the app's surrogate widening runs over the *joined* output of every phrasing parent, so bytes that are not a text child's are rewritten. `installSurrogateWidening` (format.ts:283–291) applies `SPLIT_PAIR`'s three forms to the whole string `containerPhrasing` returns, and the third form (task 1.49, two adjacent lone-surrogate references) is pure ASCII, so the literal bytes `&#xD83D;&#xDE00;` inside a verbatim leaf are rewritten to `&#x1F600;`. Reproduced by the reconciliation headlessly at 5618078 in a detached scratch worktree: ``format(parse(\"`&#xD83D;&#xDE00;`\\n\"))`` is `` `&#x1F600;`\\n `` (the code's value changes — invariant B fails; A holds from the second pass), the same for ``see `&#xD83D;&#xDE00;` here``, for inline html `a <span title=\"&#xD83D;&#xDE00;\">b</span> c` (an `html` node, opaque by PRD §6.1), for a table cell holding the span, for the span inside an emphasis (`*ab `+\"`&#xD83D;&#xDE00;`\"+` cd*`) and inside a link's text; in the browser (Claude's R1) the rendered `<code>` shows the user's bytes while the pane, the clipboard and \"Copied Markdown\" carry the rewritten ones. NOT a member: a link's destination or title holding those bytes — the parser decodes a lone-surrogate reference there to U+FFFD before the serializer runs, so the tree never holds them (do not write guards for them). NOT a member either: a text child — `&amp;#xD83D;&amp;#xDE00;` is written `\\&#xD83D;\\&#xDE00;`, each `&` escaped by `safe()`, which no form matches — the absence guard. Read first, in node_modules: `mdast-util-to-markdown/lib/util/container-phrasing.js` (2.1.2) whole — the look-ahead through `state.handle.handlers[type]` (lines 43–55), the eol-before-html replacement of the previous result (60–80), `encodeAfter` on the current value's first unit (88–94) and `encodingInfo.before` on the previous result's last unit (100–115), the two encodings that fire on text children only because every other handler's output begins and ends with punctuation, which `encode-info.js` never encodes; `lib/handle/emphasis.js` and `strong.js` for the inner-edge encodings a mark handler makes on its own `between` after its `containerPhrasing` returned (the app's `handleDelete` mirrors them); and `rewrittenEmissions` in packages/core/src/positions.ts, the enumeration of the same rewrites the map already holds. The fix, prototyped by the reconciliation with `pnpm lint` green and the whole unit suite green with the fixture below in the index: the wrapper widens per child, positionally, never over the join. (1) During the original `containerPhrasing` call, record each direct child's handler output by wrapping `state.handle` for that call: the wrapper calls the original and pushes `{ node, value }` when the `parent` argument is this parent (a nested call's children are its own), the zwitch function's own properties (`handlers`, `invalid`, `unknown`) copied onto the wrapper with `Object.assign` so the assembler's look-ahead still resolves, the original restored in a `finally`. (2) Walk the join with a cursor: for each recorded child, find at the cursor which form the assembler can have rewritten its output into (`startsWith`, the plain output first): for a `text` child the plain value, the first UTF-16 unit as its `&#x…;` reference, the last unit, both, and each of those with a trailing line ending replaced by one space when the next child is `html`; for every other child the plain output, plus the eol-as-space form for a `break` before `html` (task 1.58 decides that pair's bytes; this task leaves them as the parent wrote them). (3) Widen only where a rewrite fired: for a text child whose first unit was encoded, `SPLIT_PAIR`'s two head forms (a high-surrogate reference before its raw low unit; two adjacent references) anchored at the slice's start; whose last unit was encoded, the two tail forms (a raw high unit before its low reference; two adjacent references) anchored at the slice's end; for an `emphasis`, `strong` or `delete` child, the head forms right after the opening marker and the tail forms right before the closing marker (marker length by node type — 1, 2, 2 — never read from the bytes, since `***a***` is a strong holding an emphasis). Nothing else in the join is touched. (4) A child the walk cannot locate leaves the rest of the join as the parent wrote it, never widened blind — the map's corpus case (\"maps every node of the tree, and nothing is unresolved\") is the guard that no such child exists, because `locateEmission` enumerates the same forms; say so in the doc comment. Rewrite the doc comments of `SPLIT_PAIR`, `widenSplitSurrogateReferences` and the wrapper: the three forms are applied at the positions the parent's two rewrites and the handlers' inner-edge encodings touch, derived from the lines read (DECISIONS #review-1-r4 J2: from the branches, never from the type list). Guards in packages/core/test/strikethrough-flanking.test.ts (or a new file beside it): a code span, an inline html node, a code span inside an emphasis, a code span inside a link's text and a table cell holding a code span, each holding the literal `&#xD83D;&#xDE00;` and, for the code span, the lone twins `&#xD83D;` and `&#xDE00;` too, each asserting `format(parse(·))` byte-identical with `toBe`, the node's `value` unchanged through `parse(format(·))`, and `formatWithMap`'s `unresolved` empty; the escaped-text absence guard above; the 1.45 guards, the 27 pair guards and the 5 position classes of 1.49 unchanged and green (the astral neighbour after a run, before a run, between two runs, and each mark's inner edges still widened — read `grep -c 'between two flanking runs' packages/core/test/strikethrough-flanking.test.ts` at the branch base and at the end into the journal, equal). Asserted able to fail, recorded in the journal and not committed: the text-head widening made unmatchable in place (append `(?!)` to the head form, lesson [1.49]) goes red on 1.49's pair guards and stays green on the code-span guards; the per-child walk replaced by the whole-join widening goes red on the code-span guard. The fixture fixtures/markdown/code-span-references.md — exactly the four paragraphs the reconciliation ran through every corpus leg, byte for byte: ``see `&#xD83D;&#xDE00;` here``, `a <span title=\"&#xD83D;&#xDE00;\">bb</span> cc`, ``*ab `&#xD83D;&#xDE00;` cd* ef``, ``\\`&#xD83D;\\` alone and \\`&#xDE00;\\` alone`` (the last written with single backticks around each reference, no backslashes: the bytes are `` `&#xD83D;` alone and `&#xDE00;` alone ``) — a fixed point, with its canonical sibling (identical) and its index.json entry (nodeTypes emphasis, html, inlineCode, paragraph, root, text; blockCount 4; sectionCount 0; paragraphStartLines 1, 3, 5, 7), so the identity corpus, every corpus leg and \"maps every node of the tree, and nothing is unresolved\" cover the shape (count from the index, never a literal). One instrument outside format.ts trips on this fixture by construction and is named here so it is not a conflict: `expectNoForbiddenEntity` in packages/editor/test/editor-fixed-point.test.ts reads `textValues`, which includes `inlineCode` values, so a code span holding a literal reference is read as a leaked entity; change that one check to read `text` values only (give `textValues` an `only: \"text\"` argument or a sibling), with a comment saying an `inlineCode` value is verbatim (PRD §6.1) and the parser never decodes inside a code span, and leave 1.54's `expectTextMatchesEditorIgnoringWhitespace` reading both — the reconciliation probed that with this one change every leg of that file is green on the fixture. One Playwright case in a new e2e/web/editor-copy-code-span-references.spec.ts on `/dev/editor` (helpers copied from editor-toggle-inline-html.spec.ts: the `writeText` spy of 1.28, `load`, the Copy button): load ``see `&#xD83D;&#xDE00;` here\\n``, the rendered `<code>`'s `textContent` `&#xD83D;&#xDE00;` with `toBe`, Copy Markdown → the spy's bytes ``see `&#xD83D;&#xDE00;` here\\n`` with `toBe` and `format(parse(copied))` equal to it (the test-process fixed point, lesson [1.55]); no caret motion. Scope: packages/core/src/format.ts, packages/core/test/**, packages/editor/test/editor-fixed-point.test.ts (that one check and `textValues` only), fixtures/markdown/code-span-references.md, fixtures/markdown/code-span-references.canonical.md, fixtures/markdown/index.json, e2e/web/editor-copy-code-span-references.spec.ts, docs/lessons.md, the journal. No change to positions.ts, toggle.ts, schema.ts, typing-legs.ts or any other fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The five presence guards and the absence guard green and listed in the journal against their source strings, each asserting the bytes with `toBe`, the value and `unresolved` empty; the 1.45 and 1.49 guards green unchanged (the `grep -c` above equal at the base and the end, both numbers in the journal); both mutations recorded with the cases each turned red and the cases that stayed green; the wrapper's doc comment naming container-phrasing.js's four regions by line and the per-child rule (`grep -c 'state.handle' packages/core/src/format.ts` at least 2); the fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg and resolved by \"maps every node of the tree, and nothing is unresolved\" (count from the index); every other fixture byte-identical; `git diff --stat` shows no change to packages/core/src/positions.ts, packages/editor/src/toggle.ts, packages/editor/src/schema.ts or packages/editor/test/typing-legs.ts, and the diff of packages/editor/test/editor-fixed-point.test.ts touches only `expectNoForbiddenEntity` and `textValues` (`git diff --stat` on that file under 15 lines changed, the number in the journal); the Playwright case green in the container with the `textContent` and the copied bytes asserted by `toBe` and the fixed point (`grep -cE 'ArrowUp|ArrowDown|ControlOrMeta\\+(Home|End)|\"(Home|End)\"' e2e/web/editor-copy-code-span-references.spec.ts` is 0); the cursor, toggle, positions and editor-fixed-point suites green with their meaning unchanged. `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `grep -c 'it.fails' packages/core/test/strikethrough-flanking.test.ts packages/editor/test/editor-fixed-point.test.ts` prints 0 for each; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.10.r7d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.58",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r7 M2 — Claude finding 2 blocker, Sol finding 1 blocker; packages/core/src/format.ts and packages/core/src/positions.ts, files of passed tasks 1.45, 1.49, 1.51 and 1.57, whose records are not edited): a hard `break` before inline `html` is written as a backslash and a space, and the break is lost. `mdast-util-to-markdown/lib/util/container-phrasing.js` (2.1.2) lines 60–80 replace the *previous result's* trailing line ending by one space before an `html` child whatever node wrote it, so the `break` handler's `\\\\`+LF becomes `\\\\ `, which no parser reads as a break. Reproduced by the reconciliation headlessly at 5618078: `format(parse(\"alpha\\\\\\n<b>x</b> y\\n\"))` and the two-space form `alpha  \\n<b>x</b> y` both give `alpha\\\\ <b>x</b> y\\n`, whose parse holds `text \"alpha\\\\ \"`, `html`, … and no `break` (invariant B fails on the parser's own tree; a literal backslash the user never wrote is rendered text; A holds from the second pass) — the same after `*a*`, a code span, a link, an inline html node, inside a blockquote (`> alpha\\\\`+LF+`> <b>x</b> y`) and a list item; positions.ts:664–667 names this exact branch for a `break` child as \"stays unresolved\" and packages/core/test/positions.test.ts:323 pins the corrupt bytes as they are. Sol's browser route (`/logs/reviews/1/r7/sol/hardbreak-browser.mjs`, its log beside it): load `alpha\\\\`+LF+`<i>beta</i>`, one `<br>` rendered, Copy Markdown reports success and hands over `alpha\\\\ <i>beta</i>`, break count 1 → 0. The fix, prototyped by the reconciliation with `pnpm lint` green and the whole unit suite green with the fixture below in the index (the two pins below red until they are rewritten): in task 1.57's per-child walk, a `break` child whose next sibling is `html` and whose output the assembler rewrote (the eol-as-space form found at the cursor) is written as its own output — the line ending kept — exactly when the pair reparses to itself, and as one space with no backslash otherwise. The criterion is the round trip itself, never a hand list of tag names (lesson [1.10.r6d]): `parse(\"x\" + breakOutput + htmlValue + \" y\\n\")` holds exactly one paragraph whose children include a `break` directly followed by an `html` — `format.ts` imports `parse` from `./parse.js`, which imports nothing from `format.ts` (no cycle; confirm by reading its imports). When the html value can start an html block (CommonMark §4.6 conditions 1–6, which the parser decides: `<div>`, `<!-- -->`, `<script>`, `<?x?>`, `<![CDATA[x]]>`) keeping the line ending would turn the next line into an html block, so the break is written as one space — a documented loss on the docs/V1.1-BACKLOG.md line `[#030 product, a soft or hard break before inline html]`, never a stray byte; the html child's bytes are never changed. In positions.ts change only the `rewrittenEmissions` doc comment's last sentences: a `break` before `html` written with its line ending kept is found verbatim by `locateEmission` and resolved; the space form (the block-capable html) is not its value's spelling and stays unresolved, named — K2's rule (a new encoding in a handler is a change to that map); no code change in positions.ts is expected — confirm, and if placement needs one, stop and write the conflict to the journal. Two pins of the old bytes yield, named here so they are not conflicts: packages/core/test/positions.test.ts:323 (\"is reported in unresolved with its subtree, and its siblings keep their ranges\", expecting `a\\\\ <b>x</b>\\n`) and packages/core/test/positions-html-eol-inline-code.test.ts:158 (\"a `break` child before html … is unchanged: still unresolved\") — each becomes: the bytes `a\\\\`+LF+`<b>x</b>\\n` with `toBe`, `unresolved` empty, the break's range covering `\\\\`+LF and its siblings placed; and each file gains the space-form member (a `break` before `<div>x</div>`): the bytes `a <div>x</div>\\n`, the break unresolved, the siblings placed. Guards in packages/core/test (a new file or beside the html-eol guards): both source forms of the break (backslash+LF; two spaces+LF) × `<i>`, `<span>`, `<b>`, `<!-- c -->` as the html × the break after plain text, after `*ab*`, after a code span, after a link, after an html node, inside a blockquote and inside a list item — each asserting the bytes with `toBe`, the `break` count of `parse(out)` equal to the tree's, `format(parse(out))` equal to `out` and `unresolved` empty; the block-capable members `<div>x</div>`, `<!-- c -->` (a comment CAN start a block: condition 2 — put it in this group, not the one above), `<script>x</script>`, `<?x?>`, `<![CDATA[x]]>` each written with one space and no backslash, a fixed point, the loss stated in the case title; the soft-break-before-html cases of 1.51 unchanged (`alpha\\n<i>beta</i>` still `alpha <i>beta</i>`). Asserted able to fail, recorded in the journal and not committed: the criterion inverted (always the space) goes red on the kept guards with the break count 0; always kept goes red on the `<div>` member (an html block appears in the reparse). The fixture fixtures/markdown/hard-break-before-inline-html.md — exactly the three top-level paragraphs the reconciliation ran through every corpus leg: `alpha\\\\`+LF+`<i>beta</i> gamma`, `*ab*\\\\`+LF+`<b>xy</b> zw`, `first line\\\\`+LF+`<span>xy</span> and more` (each line break a backslash then LF; two-character runs at the mark, text after every atom) — a fixed point, its canonical sibling identical, its index.json entry (nodeTypes break, emphasis, html, paragraph, root, text; blockCount 3; sectionCount 0; paragraphStartLines 1, 4, 7). No container member: a hard break inside a blockquote or list item is a cursor-map member outside this task (the `[1.10.r7d, hard break inside a container]` backlog line; do not add one). One Playwright case in a new e2e/web/editor-copy-hard-break-before-html.spec.ts on `/dev/editor` (helpers copied from editor-toggle-inline-html.spec.ts): Sol's route — load `alpha\\\\`+LF+`<i>beta</i>\\n`, one `br` inside the rendered paragraph (`locator(\"p br\").count()` 1), Copy Markdown → the spy's bytes `alpha\\\\`+LF+`<i>beta</i>\\n` with `toBe`, `parse(copied)` holding exactly one `break` (count it), `format(parse(copied))` equal to it; no caret motion. Scope: packages/core/src/format.ts, packages/core/src/positions.ts (the doc comment only, unless the conflict rule above), packages/core/test/**, fixtures/markdown/hard-break-before-inline-html.md, fixtures/markdown/hard-break-before-inline-html.canonical.md, fixtures/markdown/index.json, e2e/web/editor-copy-hard-break-before-html.spec.ts, docs/lessons.md, the journal. No change to toggle.ts, schema.ts, typing-legs.ts or any other fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The kept guards green for every (source form, html tag, left neighbour) tuple and listed in the journal, each asserting the bytes, the break count, the fixed point and `unresolved` empty; the block-capable members green with one space and the loss named; the 1.51 soft-break cases unchanged; the two named pins rewritten and green with the kept bytes and the space-form member beside each; both mutations recorded with the cases each turned red; the criterion stated in the wrapper's doc comment as the round trip (`grep -c 'parse(' packages/core/src/format.ts` at least 1); `git diff --stat` shows the only hunk in packages/core/src/positions.ts inside the `rewrittenEmissions` doc comment (or the journal's conflict record), and no change to packages/editor/src/toggle.ts, packages/editor/src/schema.ts or packages/editor/test/typing-legs.ts; the fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg and resolved by \"maps every node of the tree, and nothing is unresolved\" (count from the index); every other fixture byte-identical; the Playwright case green in the container with the `br` count, the copied bytes by `toBe`, the break count and the fixed point (`grep -cE 'ArrowUp|ArrowDown|ControlOrMeta\\+(Home|End)|\"(Home|End)\"' e2e/web/editor-copy-hard-break-before-html.spec.ts` is 0); the cursor, toggle, positions and editor-fixed-point suites green with their meaning unchanged. `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `grep -c 'it.fails' packages/core/test/positions.test.ts packages/core/test/positions-html-eol-inline-code.test.ts` prints 0 for each; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.57"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.59",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r7 M4 — Sol finding 2 should-fix Required; M8 — Claude finding 7 nit; packages/core/src/positions.ts, the position map of passed tasks 1.2, 1.46 and 1.51, whose records are not edited): the `inlineCode` spelling table reads the base handler and not the configured one. `mdast-util-gfm-table/lib/index.js` (2.0.0) lines 291–298, `inlineCodeWithTable`, wraps `defaultHandlers.inlineCode` and, when `state.stack` includes `tableCell`, writes every `|` of the output as `\\\\|`; `inlineCodeSpelling` (positions.ts:704) compares the inner string one UTF-16 unit per unit, so a cell's `` `a\\\\|b` `` (value `a|b`) fails the length check, gets no table while `map.unresolved` stays empty, and the cursor map falls to the node's start. Reproduced by the reconciliation headlessly at 5618078: on `| h |`+LF+`| - |`+LF+`` | `a\\\\|b` | `` the inverse `toRendered(toSource(p)) === p` fails at exactly the two positions inside the span (9 → 8, 10 → 8), holds everywhere else, and holds on the same span outside a table (`a `+\"`a\\\\|b`\"+` c`, value `a\\\\|b`, unit for unit); Sol's browser route (`/logs/reviews/1/r7/sol/pipe-browser.mjs`): the caret after `a` (asserted `{ text: \"a|b\", offset: 1 }`), toggle to source and back, type `X` → the code becomes `Xa|b`, not `aX|b`; Sol's 1,571-tree probe found 16 inverse failures, all this family. The fix, prototyped by the reconciliation with `pnpm lint` green and the whole unit suite green with the fixture below in the index: `placeChildren` threads an `insideTable` flag through its recursion (true below a `table` emission — the extension never dispatches `tableRow` or `tableCell` through `state.handle`, so a `table` emission's children are the cells' inline nodes; read positions.ts's `placeTableGrid` comment), and `inlineCodeSpelling(value, written, insideTable)` accepts, inside a table, `\\\\|` as the two-unit spelling of a `|` — the pipe owns both units, `starts[i]` at the backslash and `ends[i]` after the pipe, the shape the character-reference rule already has — with the length and padding checks counting the pipes; outside a table a `\\\\|` in the written string is two characters of the value, as before. State the rule once in `SpellingTable`'s inline-code paragraph with the extension's file and lines (DECISIONS #review-1-r4 J2: from the configured handler's branches, read in the installed package, never from the base handler). (M8) Correct the false comment at positions.ts:561–563 in `placeChildren` — \"a `text` child is dispatched exactly once (it has `peek`)\" — to the truth read in container-phrasing.js lines 43–55: `lib/handle/text.js` has no `peek`, the look-ahead calls the handler from `state.handle.handlers`, so `wrapHandle` records a second emission for the look-ahead and `lastPerNode` is what keeps one emission per node and makes `children[index + 1]` the next dispatched child. Unit guards in packages/core/test/positions-html-eol-inline-code.test.ts (or a new file): inside a table cell `` `a\\\\|b` ``, `` `x\\\\|y\\\\|z` ``, a padded value holding a pipe, an astral value with a pipe (`` `😀\\\\|b` ``), each asserting the table's entries at the first, middle and last position (the pipe's two-unit spelling, the fence and padding owned by no character) and `unresolved` empty; the absence guard outside a table (`a `+\"`a\\\\|b`\"+` c`: the backslash is the value's own character, unit for unit); and in packages/editor/test/toggle.test.ts one case per source asserting `toRendered(toSource(p)) === p` for every text position of the doc (computed from the doc, never a literal) and `toSource` monotone. Asserted able to fail, recorded in the journal and not committed: the pipe branch removed goes red on the in-table guards and the inverse case, and stays green on the absence guard. The fixture fixtures/markdown/table-inline-code-pipe.md — exactly the lines the reconciliation ran through every corpus leg: `| Term | Meaning |`, `| --- | --- |`, `` | `a\\\\|b` xy | a pipe inside code | ``, `` | plain \\\\| pipe | `x\\\\|y\\\\|z` zw | `` — with its canonical sibling (`format(parse(·))`: the columns padded; byte-identical on a second pass) and its index.json entry (nodeTypes inlineCode, root, table, tableCell, tableRow, text; blockCount 4 — the table and its three rows, as `blocksOf` counts; sectionCount 0; paragraphStartLines empty). The two characters of text after each code span are deliberate: no cell ends in a code span, because the block-final code-span end is task 1.60's rule and the deletion leg's byte clause fails on a block that ends in a code span until 1.60 (probed by the reconciliation) — keep the shape. One Playwright case in a new e2e/web/editor-toggle-cell-code-pipe.spec.ts on `/dev/editor` (helpers copied from editor-toggle-cell-end.spec.ts, its caret placement inside a cell and the 200 ms tick before the chord included — the `[1.46, found outside scope]` line): Sol's route — load `| h |`+LF+`| - |`+LF+`` | `a\\\\|b` | ``+LF, place the caret after `a` in the code span by the cell-end spec's route (a click in the cell, then the counted arrows from an anchor asserted first — assert whatever the DOM reports and record it, the anchor `{ text: \"a|b\", offset: 1 }`), `ControlOrMeta+/` twice (to source and back), type `X`, Copy Markdown → the spy's bytes' third line `` | `aX\\\\|b` | `` (assert the whole string with `toBe`, computed as `format(parse(·))` of the expected source in the test process), a fixed point, the rendered `<code>`'s `textContent` `aX|b`. Caret placement only by a click, `ArrowUp`/`ArrowDown` and counted horizontal arrows with the anchor asserted first (DECISIONS #022, #024). Scope: packages/core/src/positions.ts, packages/core/test/**, packages/editor/test/toggle.test.ts, fixtures/markdown/table-inline-code-pipe.md, fixtures/markdown/table-inline-code-pipe.canonical.md, fixtures/markdown/index.json, e2e/web/editor-toggle-cell-code-pipe.spec.ts, docs/lessons.md, the journal. No change to format.ts, toggle.ts, schema.ts, typing-legs.ts or any other fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The in-table guards and the absence guard green and listed in the journal against their source strings, each asserting the entries at first, middle and last position and `unresolved` empty; the inverse cases green with the position set computed from the doc; the mutation recorded with the cases it turned red and the one it left green; `SpellingTable`'s doc comment naming `mdast-util-gfm-table`'s wrapper by file and line (`grep -c 'inlineCodeWithTable' packages/core/src/positions.ts` at least 1); the `placeChildren` comment corrected (`grep -c 'it has .peek.' packages/core/src/positions.ts` is 0); `git diff --stat` shows no change to packages/core/src/format.ts, packages/editor/src/toggle.ts, packages/editor/src/schema.ts or packages/editor/test/typing-legs.ts; the fixture present with its canonical sibling and index entry, byte-identical through every identity leg and every corpus leg and resolved by \"maps every node of the tree, and nothing is unresolved\" (count from the index); every other fixture byte-identical; the Playwright case green in the container with the anchor asserted, the copied bytes by `toBe`, the fixed point and the `textContent` (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"' e2e/web/editor-toggle-cell-code-pipe.spec.ts` is 0); the cursor, toggle, positions, position-map and editor-fixed-point suites green with their meaning unchanged. `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `grep -c 'it.fails' packages/core/test/positions-html-eol-inline-code.test.ts packages/editor/test/toggle.test.ts` prints 0 for each; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.58"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.60",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r7 M5 — Claude finding 4 should-fix; packages/editor/src/toggle.ts, the cursor map of passed tasks 1.7, 1.37, 1.43, 1.52 and 1.53, and apps/desktop/src/dev/DevEditor.tsx, whose records are not edited): at a block-final `inline_code` end the two views take a typed character differently — the last tuple of L6 that task 1.53's rule does not reach. `inline_code` in packages/editor/src/schema.ts:469 sets no `inclusive`, so ProseMirror's default applies and `doc.resolve(end).marks()` at the end of a block-final code span is `[inline_code]`: the rendered view types into the span, while `isLeafEnd` (toggle.ts:508–520, task 1.52) answers the column after the closing fence and the source view types outside it. Reproduced by the reconciliation headlessly at 5618078: on `see `+\"`foo`\"+``, `# see `+\"`foo`\"+``, `- see `+\"`foo`\"+`` and `> see `+\"`foo`\"+`` a letter inserted at `end` with `$pos.marks()` and run through `pmToMdast` and `format` gives ``see `fooX` `` while `X` inserted in the source at `toSource(end)` gives ``see `foo`X``; mid-block (`see `+\"`foo`\"+` x`) the views agree. The guard at packages/editor/test/toggle.test.ts:1542–1548 pins the column only (\"keeps task 1.52's answer\") with no two-view instrument, and the 1.52 (b) Playwright route (editor-toggle-cell-end.spec.ts:179) agrees only because the input rule's `removeStoredMark` (packages/editor/src/input.ts:85) cleared the stored mark one keystroke earlier — the editor types by `storedMarks ?? $pos.marks()` (`Transaction.insertText`, read in node_modules/prosemirror-state's dist) and the map reads the doc alone. Read first: `ResolvedPos.marks()` and `Transaction.insertText` in node_modules, and `innermostAt`'s doc comment (1.53's rule). The fix, prototyped by the reconciliation with `pnpm lint` green and the whole unit suite green save the two pins named below: (1) `toSource(pos, storedMarks = null)` — a second, optional argument on `CursorMap.toSource`: the marks a character typed at `pos` receives are `storedMarks ?? $pos.marks()` (a `typedMarks` helper, `null` outside a textblock); `inlineBoundary` takes those marks instead of reading `$pos.marks()` itself, so 1.53's boundary rule is unchanged when no stored marks are given. (2) `isLeafEnd`'s block-end branch applies to a leaf without a table (an atom) and to an `inlineCode` whose typed marks do not include `schema.marks.inline_code`; an `inlineCode` at a block end whose typed marks include it is answered by its spelling table — the last offset, before the closing fence — exactly as mid-block. State the rule in `isLeafEnd`'s and `cursorMap`'s clause-3 doc comments, replacing \"a space typed there is plain text, as it is in the editor\", which is false for an inclusive mark. (3) The dev route's toggle (DevEditor.tsx, the `cursorMap(root, pm.state.doc).toSource(pm.state.selection.head)` call) passes `pm.state.storedMarks` as the second argument, so the caret right after the input rule closed a span still lands after the fence — the 1.52 (b) route unchanged and green. Two pins of the old answer yield, named here so they are not conflicts: packages/editor/test/toggle.test.ts:1546 (\"an `inline_code` run at its block's end keeps task 1.52's answer, after the closing fence\") becomes a two-view guard — with no stored marks, the column before the fence and the two views' bytes equal (``see `fooX` ``: `X` inserted in ProseMirror with `$pos.marks()` through `pmToMdast` and `format`, against `X` inserted in the source at the mapped column and reparsed); with stored marks `[]` the column after the fence and both views ``see `foo`X`` (ProseMirror inserting with the stored marks); and packages/editor/test/position-map-editor-leg.test.ts:741 (the hand-seeded `a `+\"`cd`\"+`` guard, \"answered with that leaf's end in the bytes\") with its `byteEndOf` oracle's inline-code branch: a block whose reparsed last leaf is an `inlineCode` is answered before the closing fence (the position of the value's end, `position.end.column` minus the closing fence's width — derive it from the reparsed node's bytes, never a literal), the atom branch unchanged — the deletion leg's byte clause (the character before the cursor is the block's last character) then holds for a block ending in a code span, which it did not before (probed by the reconciliation: latent because no fixture ends a block in a code span; 1.59's fixture was shaped not to). Guards in toggle.test.ts, a describe \"a block-final inline code span follows the marks a typed character takes\": for a paragraph, a heading, a list item, a blockquote and a table cell ending in a code span, both routes — no stored marks (the click route) and stored marks `[]` (the input-rule route) — each asserting `toSource`'s column and the two-view byte agreement (the instrument of 1.53's matrix), and the inverse `toRendered(toSource(p)) === p` over every text position of each document; 1.52's corpus inverse leg and its destructive leg green unchanged. Asserted able to fail, recorded in the journal and not committed: the block-end branch reverted to \"every leaf\" goes red on the no-stored-marks cases with the ``see `foo`X`` bytes it produces; the stored-marks argument ignored goes red on the stored-marks cases. One Playwright case in a new e2e/web/editor-toggle-code-span-end.spec.ts on `/dev/editor` (helpers copied from editor-toggle-cell-end.spec.ts, the `writeText` spy of 1.28 and the 200 ms tick before the chord; the click-at-a-measured-point helper copied from editor-soft-line-breaks.spec.ts, task 1.56): load ``see `foo`\\n``, click at the point measured from the end of the `foo` text node's own Range rect, anchor asserted `{ text: \"foo\", offset: 3 }` (assert whatever the DOM reports and record it), `ControlOrMeta+/`, type `X`, toggle back, Copy Markdown → the spy's bytes ``see `fooX`\\n`` with `toBe`, a fixed point, the rendered `<code>`'s `textContent` `fooX`; the existing 1.52 (b) case in editor-toggle-cell-end.spec.ts stays as it is and green (the stored-marks route). Caret placement only by a click at a computed point, `ArrowUp`/`ArrowDown` and counted horizontal arrows with the anchor asserted first (DECISIONS #022, #024). Scope: packages/editor/src/toggle.ts, packages/editor/test/toggle.test.ts, packages/editor/test/position-map-editor-leg.test.ts (the named guard and `byteEndOf` only), apps/desktop/src/dev/DevEditor.tsx (the one argument), e2e/web/editor-toggle-code-span-end.spec.ts, docs/lessons.md, the journal. No change to positions.ts, format.ts, schema.ts, input.ts, typing-legs.ts or any fixture, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The ten guards (five block kinds × two routes) green and listed in the journal, each asserting the column and the two-view byte agreement, with the inverse over every text position of each document; the two named pins rewritten and green; 1.52's corpus inverse leg and destructive leg green unchanged; both mutations recorded with the bytes each produced; `isLeafEnd`'s doc comment naming `storedMarks ?? $pos.marks()` (`grep -c 'storedMarks' packages/editor/src/toggle.ts` at least 2, and at least 1 in apps/desktop/src/dev/DevEditor.tsx); `git diff --stat` shows no change to packages/core/src/positions.ts, packages/core/src/format.ts, packages/editor/src/schema.ts, packages/editor/src/input.ts or packages/editor/test/typing-legs.ts, and the diff of position-map-editor-leg.test.ts confined to `byteEndOf` and the named guard; the Playwright case green in the container with the anchor asserted, the copied bytes by `toBe`, the fixed point and the `textContent` (`grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"|ArrowLeft|ArrowRight' e2e/web/editor-toggle-code-span-end.spec.ts` is 0) and editor-toggle-cell-end.spec.ts unchanged (`git diff --stat` on it empty); the cursor, toggle, 200-seeded-position, position-map and editor-fixed-point suites green with their meaning unchanged. `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `grep -c 'it.fails' packages/editor/test/toggle.test.ts packages/editor/test/position-map-editor-leg.test.ts` prints 0 for each; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.59"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.61",
  "model": "sonnet",
  "description": "Phase 1 review fix, test-only (DECISIONS #review-1-r7 M3 (c) — Claude finding 3 should-fix Required, the record half; M6 — Claude finding 5 should-fix; packages/editor/test/editor-fixed-point.test.ts and packages/editor/test/position-map-inverse.test.ts, files of passed tasks whose records are not edited): task 1.54 was integrated from an attempt that never finished (`transcriptHasDone` matched the promise echoed from docs/lessons.md in a tool result; DECISIONS #review-1-r7 M3), so the counts and the caller list its acceptance says are \"recorded in the journal\" are recorded nowhere, and one caller is missing the instrument. Do three things. (1) The list-item paste leg in editor-fixed-point.test.ts (the `pastedOut` assertion near line 1649, inside the `for (const name of names)` loop of the list-split leg) carries no `expectTextMatchesEditorIgnoringWhitespace`; add it beside the leg's `expectNoForbiddenEntity`, in the same form the other callers use; count the callers of `expectNoForbiddenEntity` in the file (`grep -c 'expectNoForbiddenEntity(' packages/editor/test/editor-fixed-point.test.ts`, the definition excluded) and the callers of `expectTextMatchesEditorIgnoringWhitespace`, and list every caller against its leg in the journal — the record 1.54 never wrote; run the assertion once over the corpus and, if it turns a fixture red, follow the rule below (a named member and a backlog line, never a narrowing). (2) In position-map-inverse.test.ts the two destructive-leg `it`s (the `describe` over `deleteAtEveryBlockEnd` and `deleteBesideEveryMarkedRun`) get a per-test timeout of 30 seconds in the form vitest's installed version documents (read node_modules/vitest's README or types for `test(name, fn, timeout)` / the options object), with a comment naming why: the essay-fixture leg ran 3,401 / 2,930 / 1,364 ms on the accepted ubuntu / windows / macos runners of gate 1.verify.r7.g2h and 5,051 ms once in a cold build (Claude's r7 review), against vitest's 5,000 ms default; never a `.skip`, never a narrowing of the position set. (3) Record 1.54's evidence in your journal line, cited from the transcripts and never re-run: the per-spec `grep -c 'expect.poll'` counts of the twelve `press` helpers (count them now from e2e/web/*.spec.ts, read into the journal), the scratch-spec mutation's received offset (4 for 5, `/logs/tasks/1.54/3.a3-capped.log`) and where the format.ts mutation is recorded (the same transcript; the runner's recovery commit swept it onto the branch and the retry restored it — DECISIONS #034, handoff 028). The format.ts mutation is NEVER re-run in this task (no source change of any kind). Scope: packages/editor/test/editor-fixed-point.test.ts, packages/editor/test/position-map-inverse.test.ts, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md, the journal. No source change, no fixture change, no change to typing-legs.ts, no config or workflow edits, zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "Every `expectNoForbiddenEntity` caller in editor-fixed-point.test.ts carries `expectTextMatchesEditorIgnoringWhitespace` (both `grep -c` counts in the journal, equal once the definitions are excluded; every caller listed against its leg), green over every fixture in the index; the two destructive-leg `it`s carry the 30 s timeout with the comment (`grep -c '30_000\\|30000' packages/editor/test/position-map-inverse.test.ts` at least 2) and no `.skip` (`grep -c '\\.skip(' ` on both files is 0); the journal carries the twelve per-spec `expect.poll` counts and the two mutations' citations; `git diff --stat` shows no change under packages/*/src, apps/, e2e/, fixtures/ or packages/editor/test/typing-legs.ts. `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container; `grep -c 'it.fails' packages/editor/test/editor-fixed-point.test.ts packages/editor/test/position-map-inverse.test.ts` prints 0 for each; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md and docs/progress/journal-main.md.",
  "dependencies": [
   "1.60"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r8",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r8 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r8 chain — six candidates weighed at DECISIONS #review-1-r7, three promoted and three recorded. (a) Promote, as a bullet of `## Code rules` in both CLAUDE.md and AGENTS.md, verbatim and identical in both files: \"A post-processor over a serializer's joined output is applied per child, over the slice the rewritten child occupies and at the positions the parent's rewrites touch, never over the join, and its guard set carries one member per node class whose bytes are literal — a code span, inline html, a link's destination and title — holding the exact bytes the post-processor matches (DECISIONS #review-1-r7 M1).\" Discharged by 1.57's per-child walk and its verbatim-leaf guards in packages/core/test — name them by title in the journal. (b) Promote, as the next bullet, verbatim: \"When an enumeration of a parent's rewrites names a branch for a node class, the same guard asserts that branch's bytes on the parser's own tree — the node count after parse(format(·)) equal to the tree's — because a rewrite the map cannot follow is usually one the parser cannot read (DECISIONS #review-1-r7 M2).\" Discharged by 1.58's break-count guards — name them by title. (c) Promote, as the next bullet, verbatim: \"A two-view guard at a mark's end runs both routes — the input rule then the toggle, and a click then the toggle — and the toggle hands the editor's stored marks to the cursor map, because the editor inserts with `storedMarks ?? $pos.marks()` and a map that reads the doc alone proves one route only (DECISIONS #review-1-r7 M5).\" Discharged by 1.60's two-route guards in packages/editor/test/toggle.test.ts — name them by title. (d) Record, do not promote: the DONE-sentinel and lesson-line rule (M3 (a), (b)) addresses the runner and the principal — the boundary `fix(runner)` and docs/PRINCIPAL.md; (e) record, do not promote: Sol's r7 lesson that spelling rules derive from the configured handler is J2's promoted rule applied to an extension wrapper, carried by 1.59's doc comment; (f) record, do not promote: Claude's r7 lesson on pairwise sweeps addresses reviewers (the r8 rows carry it); the journal says so in one sentence each. `ralph/check-agent-rules.sh` must exit 0 and tests/agent-rules.test.ts must pass after the edit. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal (DECISIONS #review-1-r6 L10). No other procedure change this attempt. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r7 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; `grep -c 'it.fails' packages/core/test/*.ts packages/editor/test/*.ts` prints 0 for each file; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes; the journal names the discharging test for each clause of (a), (b) and (c) and records (d), (e) and (f).",
  "dependencies": [
   "1.61"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r8/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r8h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r8h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r8",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r8/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r8: scripts/gate.sh 1.verify.r8h pushes integrated_sha of 1.verify.r8 to ci/1.verify.r8/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r8/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r8h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r8"
  ]
 },
 {
  "id": "1.62",
  "model": "sonnet",
  "description": "Phase 1 verification-gate repair (repair after the 1.verify.r8h GATE-FAILED, ci.yml run 35755695990 at b7f45a5: ubuntu-latest 97 passed / 1 failed and windows-latest 96 passed / 2 failed on e2e/web, macos-latest 98/98, every unit, lint and cargo step green on all three; DECISIONS #037): two of the r8 chain's new specs fail at the click that seeds the caret, before the toggle chord ever runs — `e2e/web/editor-toggle-code-span-end.spec.ts` line 127 (task 1.60; ubuntu-latest and windows-latest: expected `{ text: \"foo\", offset: 3 }`, received `{ text: null, offset: 4 }`) and `e2e/web/editor-toggle-cell-code-pipe.spec.ts` line 167 in `seedWithCaretAfterA` (task 1.59; windows-latest only: expected `{ text: \"a|b\", offset: 0 }`, received `{ text: null, offset: 0 }`). Cause, read in the installed prosemirror-view 1.42.3 (`node_modules/.pnpm/prosemirror-view@1.42.3/node_modules/prosemirror-view/dist/index.js`): a caret at either edge of a code span sits at a reveal-widget boundary — task 1.4's `.essaydown-delimiter` backticks (`packages/editor/src/reveal.ts`: the opening widget has a positive `side`, the closing one a negative `side`, and prosemirror-view gives every widget `contentEditable = \"false\"`) — and that one document position has two DOM spellings. Blink's hit test anchors the selection in the `<code>` text node (`foo`/3, `a|b`/0); ProseMirror's own `NodeViewDesc.domFromPos` (line 922) spells the same position on the parent element after the closing widget (`<p>`/4, the value both runners received) or before the opening widget (`<td>`/0, the windows value — and the spelling the pipe spec's second case already asserts after the toggle back, where ProseMirror wrote the DOM). The two are not equivalent to ProseMirror (`scanFor` returns false at a `contentEditable == \"false\"` child), so which one the poll reads depends on whether the view re-synced the DOM selection after the click (`handlers.focus` schedules `selectionToDOM` 20 ms after focus when the observer's `currentSelection` differs from the DOM's, line 3904; `setSelection` at line 1025 re-spells whenever the DOM is not equivalent) — a race the 5 s poll cannot settle, because both spellings are stable and both resolve to the same document position. The precondition asserted a DOM spelling that belongs to whichever of Blink and ProseMirror wrote the DOM last; what the toggle reads is the editor's selection (`toggle` in `apps/desktop/src/dev/DevEditor.tsx` hands `pm.state.selection.head` and `pm.state.storedMarks` to `toSource`). Do exactly this. (1) Reproduce first: in the container run `e2e/web/node_modules/.bin/playwright test editor-toggle-code-span-end editor-toggle-cell-code-pipe` once before any change and record each case's outcome and received value in the journal (both were green in the container at tasks 1.59 and 1.60 and red on the gate's ubuntu-latest; record what the container shows, do not decide from it). (2) A selection readout on the dev bar, `apps/desktop/src/dev/DevEditor.tsx`: a `<span className=\"dev-editor-status\" data-testid=\"selection\" ref={…} />` beside `mode` and `status`, with no React state behind it (a state update per transaction would sit on the 1.17 latency path; the readout is written imperatively into the span through the ref), written by a small ProseMirror plugin defined in that file and added to the rendered view's `plugins` list — `new Plugin({ view: () => ({ update: (view) => write(view.state), destroy: () => clear() }) })` with `write` also called once from the plugin's `view` constructor — so that every `updateState` counts, including the store pulls `bindProseMirror` makes with `updateState` directly (a fixture load moves the selection without a dispatch). The span's text is `JSON.stringify` of `{ before, empty, marks, stored }`: `before` is `$head.parent.textBetween(0, $head.parentOffset)` (the text of the caret's own textblock before the caret, `null` when `$head.parent.isTextblock` is false), `empty` is `state.selection.empty`, `marks` is `$head.marks().map((mark) => mark.type.name)`, `stored` is `state.storedMarks === null ? null : state.storedMarks.map((mark) => mark.type.name)` — the values `toSource` and the two-view guard of DECISIONS #review-1-r7 M5 depend on. The source view leaves it empty. The plugin's doc comment names this task and the reason in two sentences. `Plugin` is imported from `prosemirror-state`, already a dependency of the app. (3) `e2e/web/editor-toggle-code-span-end.spec.ts`: keep `clickAfter` and its point unchanged; replace the `caret` poll at line 127 with a poll on the readout equal to `{ before: \"see foo\", empty: true, marks: [\"inline_code\"], stored: null }` (`before` proves the end of the span byte for byte, `marks` proves the click route's `$pos.marks()`, `stored: null` proves no stored marks — M5's click route); delete the now-unused `caret` helper; keep the 200 ms tick and its comment. (4) `e2e/web/editor-toggle-cell-code-pipe.spec.ts`: in `seedWithCaretAfterA` replace `clickCodeStart` and the one `press(page, \"ArrowRight\", 1)` with one click at a point measured from the `a|b` text node's own `Range` rect over its character 1, the pipe (`range.setStart(node, 1)`, `range.setEnd(node, 2)`, `{ x: rect.left + 1, y: rect.top + rect.height / 2 }`, the text node found by a `TreeWalker` under `.ProseMirror code` as `clickAfter` finds its node) — an interior text position, which has one DOM spelling, and no key after a click — then poll the readout equal to `{ before: \"a\", empty: true, marks: [\"inline_code\"], stored: null }`; in the second case replace the `{ text: null, offset: 0 }` poll after the toggle back with the readout equal to `{ before: \"\", empty: true, marks: [\"inline_code\"], stored: null }` and reword its comment to say the position is the span's start as `canonicalCursor` answers it; delete the unused `caret`, `press` and `clickCodeStart` helpers and the doc-comment sentences that argued for the arrow; keep the 200 ms tick. In both specs the readout helper has one shape, copied per file as the other helpers are: `function selection(page: Page)` returning `JSON.parse` of `getByTestId(\"selection\")`'s `textContent`. Everything after the precondition in both specs — the chord, the typed `X`, the bytes by `toBe`, the fixed point, the `textContent` and the copy — is unchanged. (5) Asserted able to fail, recorded in the journal and not committed: in the span-end spec move the click point to `rect.left + 1` of `foo`'s first character — the readout reads `before: \"see \"` and the poll fails (record the received value). (6) One evidence line in docs/V1.1-BACKLOG.md under the `[1.46, found outside scope]` class: this gate's two received values are a second race of the same family — not the observer reading a native motion late, but the DOM selection re-spelled by `selectionToDOM` after a click at a widget boundary, with the `domFromPos` derivation above in two sentences — hard stop unchanged. Honest statement in the journal: the container is Linux and its Chrome passed both specs at 1.59 and 1.60; the only proof for the three runners is the gate. Scope: apps/desktop/src/dev/DevEditor.tsx (the readout and its plugin only; `toggle`, the bindings and the bar's other controls unchanged), e2e/web/editor-toggle-code-span-end.spec.ts, e2e/web/editor-toggle-cell-code-pipe.spec.ts, docs/V1.1-BACKLOG.md (the one evidence line), docs/lessons.md, the journal. No change to packages/**, fixtures/**, any file under apps/desktop/src outside `dev/DevEditor.tsx`, config or workflow files; `tests/no-caret-chords-in-e2e.test.ts` guards both specs (no Home/End key, no `ControlOrMeta+` chord for the caret); zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps; CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the mismatch is mechanical and not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "`apps/desktop/src/dev/DevEditor.tsx` renders `data-testid=\"selection\"` once (`grep -c 'data-testid=\"selection\"' apps/desktop/src/dev/DevEditor.tsx` is 1), its writer reads `textBetween`, `marks()` and `storedMarks` (`grep -c 'textBetween' apps/desktop/src/dev/DevEditor.tsx` at least 1, `grep -c 'storedMarks' apps/desktop/src/dev/DevEditor.tsx` at least 2) and no React state was added (`grep -c 'useState' apps/desktop/src/dev/DevEditor.tsx` is 3, as at the branch base); both specs poll the readout (`grep -c 'getByTestId(\"selection\")' e2e/web/editor-toggle-code-span-end.spec.ts` and the same for `e2e/web/editor-toggle-cell-code-pipe.spec.ts` at least 1 each) and read no DOM selection (`grep -c 'getSelection' <each spec>` is 0); `before: \"see foo\"` present in the span-end spec and `before: \"a\"` and `before: \"\"` in the pipe spec (`grep -c` at least 1 each); the span-end spec's click unchanged (`grep -c 'mouse.click' e2e/web/editor-toggle-code-span-end.spec.ts` is 1) and the pipe spec free of arrow keys (`grep -cE 'ArrowRight|ArrowLeft' e2e/web/editor-toggle-cell-code-pipe.spec.ts` is 0; `grep -cE 'ControlOrMeta\\+(Home|End)|\"(Home|End)\"' <each spec>` is 0); the reproduction run and the mutation recorded in the journal with their received values; the backlog evidence line present; `git diff --stat` against the branch base touches only apps/desktop/src/dev/DevEditor.tsx, the two specs, docs/V1.1-BACKLOG.md, docs/lessons.md and docs/progress/journal-main.md (`grep -c` of the diff's file list for `packages/`, `fixtures/` and `.github/` is 0); `e2e/web/latency.spec.ts` green in the container; `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container (count from the directory); `tests/no-caret-chords-in-e2e.test.ts` green; no manifest or lockfile change.",
  "dependencies": [
   "1.verify.r8h"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r8.g1",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r8, repair 1 of 1.verify.r8 after the 1.verify.r8h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #037): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. No procedure change and no code, test, fixture or config edit. The promote-lessons step carried by `1.verify.r8` is integrated at b7f45a5 and is not repeated. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print DONE — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps; CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the mismatch is mechanical and not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; `grep -c 'it.fails' packages/core/test/*.ts packages/editor/test/*.ts` prints 0 for each file; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only docs/lessons.md and the journal.",
  "dependencies": [
   "1.62"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r8.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r8.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r8.g1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r8.g1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r8.g1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r8.g1: scripts/gate.sh 1.verify.r8.g1h pushes integrated_sha of 1.verify.r8.g1 to ci/1.verify.r8.g1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r8.g1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r8.g1h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r8.g1"
  ]
 },
 {
  "id": "1.10.r8a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r8",
  "description": "Review attempt r8, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r8 (after the r7 FAIL, DECISIONS #review-1-r7; all three r7 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r7: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r8.g1h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r8-specific checks: each r7 finding's disposition in DECISIONS #review-1-r7 (M1–M11) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r6 (L1–L11), r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the per-child walk in packages/core/src/format.ts (1.57) read against the installed lib/util/container-phrasing.js (the look-ahead, the eol-before-html branch, the two edge encodings, lines read), lib/handle/emphasis.js and strong.js (the inner-edge encodings), and shown to record each direct child's output through `state.handle`, to widen only at a text slice's encoded edge and inside a mark's markers, and to leave a code span, inline html, an image and a link's destination and title untouched — with `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and `installAutolinkFallback` unchanged; the break-before-html criterion (1.58) read as the round trip itself and shown never to hold a list of tag names, its two rewritten pins and the space-form member read; the `insideTable` flag and the two-unit pipe spelling in positions.ts (1.59) read against mdast-util-gfm-table's `inlineCodeWithTable` (lib/index.js, lines read) and the corrected `placeChildren` comment read against container-phrasing.js's look-ahead; the typed-marks rule in toggle.ts (1.60) read against prosemirror-state's `Transaction.insertText` and prosemirror-model's `ResolvedPos.marks()` in node_modules, the stored-marks argument traced from apps/desktop/src/dev/DevEditor.tsx, and the `byteEndOf` oracle's inline-code branch read; the paste leg's assertion and the destructive legs' timeout (1.61) read; in a browser: Claude's r7 R1 (a loaded ``see `&#xD83D;&#xDE00;` here``, Copy — the bytes unchanged) and Sol's r7 routes (the loaded `alpha\\\\`+LF+`<i>beta</i>` copied with its break; `| `+\"`a\\\\|b`\"+` |` with the caret after `a`, toggle twice, `X` → `aX|b`), the block-final code-span end by both routes (a click at the end of `foo` then toggle and `X`; the input rule then toggle and ` bar`), and the r7 routes of 1.49–1.53 re-driven; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the recorded deferrals are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text — Phase 2 file-open, hard stop `2.verify`), L11 with its M10 member (adjacent attention runs and a GFM literal glued to a run — upstream, `2.verify`), `[1.52, code block interior]` (the inverse's named exclusion, `2.verify`), `[1.53, a raw source keystroke before a punctuation-edged run]` (`2.verify`), `[#030 product, a soft or hard break before inline html]` (the soft-break space and the block-capable hard-break loss — Phase 2 file-open, `2.verify`), `[1.10.r7d, hard break inside a container]` (the caret after a hard break inside a blockquote or list item maps to the paragraph's start — found by the reconciliation, `2.verify`), `[review-1-r7 M7]` (`link.peek`'s three serialisations, `2.verify`) and `[1.46]` with its fourth, Linux instance; and the r7 class (DECISIONS #review-1-r7, Claude's risks 1–3, Sol's 1–2, Grok's 1–2): any byte the app's post-processing touches that is not a text child's encoded edge or a mark's inner edge; any rewrite the assembler makes on a non-text child; any configured extension handler whose escapes a spelling table does not read (the table's `\\\\|`, the autolink literal's `unsafe` set, the strikethrough's `~`); any position where `toRendered(toSource(p))` is not `p` or the two views take a typed character differently under either route (stored marks cleared, or inherited) — searched beyond the reproductions the fix tasks name, with Claude's r7 method (ordered pairs and triples of installed inline node kinds, the failing seeds reported by class), one seed per configured handler's delimiter characters inside its enabling context and outside it, and one non-BMP seed per neighbour class and per inline position class.",
  "acceptance": "/logs/reviews/1/r8/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r8.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r8b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r8",
  "description": "Review attempt r8, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r8 (after the r7 FAIL, DECISIONS #review-1-r7; all three r7 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r7: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r8.g1h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r8-specific checks: each r7 finding's disposition in DECISIONS #review-1-r7 (M1–M11) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r6 (L1–L11), r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the per-child walk in packages/core/src/format.ts (1.57) read against the installed lib/util/container-phrasing.js (the look-ahead, the eol-before-html branch, the two edge encodings, lines read), lib/handle/emphasis.js and strong.js (the inner-edge encodings), and shown to record each direct child's output through `state.handle`, to widen only at a text slice's encoded edge and inside a mark's markers, and to leave a code span, inline html, an image and a link's destination and title untouched — with `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and `installAutolinkFallback` unchanged; the break-before-html criterion (1.58) read as the round trip itself and shown never to hold a list of tag names, its two rewritten pins and the space-form member read; the `insideTable` flag and the two-unit pipe spelling in positions.ts (1.59) read against mdast-util-gfm-table's `inlineCodeWithTable` (lib/index.js, lines read) and the corrected `placeChildren` comment read against container-phrasing.js's look-ahead; the typed-marks rule in toggle.ts (1.60) read against prosemirror-state's `Transaction.insertText` and prosemirror-model's `ResolvedPos.marks()` in node_modules, the stored-marks argument traced from apps/desktop/src/dev/DevEditor.tsx, and the `byteEndOf` oracle's inline-code branch read; the paste leg's assertion and the destructive legs' timeout (1.61) read; in a browser: Claude's r7 R1 (a loaded ``see `&#xD83D;&#xDE00;` here``, Copy — the bytes unchanged) and Sol's r7 routes (the loaded `alpha\\\\`+LF+`<i>beta</i>` copied with its break; `| `+\"`a\\\\|b`\"+` |` with the caret after `a`, toggle twice, `X` → `aX|b`), the block-final code-span end by both routes (a click at the end of `foo` then toggle and `X`; the input rule then toggle and ` bar`), and the r7 routes of 1.49–1.53 re-driven; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the recorded deferrals are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text — Phase 2 file-open, hard stop `2.verify`), L11 with its M10 member (adjacent attention runs and a GFM literal glued to a run — upstream, `2.verify`), `[1.52, code block interior]` (the inverse's named exclusion, `2.verify`), `[1.53, a raw source keystroke before a punctuation-edged run]` (`2.verify`), `[#030 product, a soft or hard break before inline html]` (the soft-break space and the block-capable hard-break loss — Phase 2 file-open, `2.verify`), `[1.10.r7d, hard break inside a container]` (the caret after a hard break inside a blockquote or list item maps to the paragraph's start — found by the reconciliation, `2.verify`), `[review-1-r7 M7]` (`link.peek`'s three serialisations, `2.verify`) and `[1.46]` with its fourth, Linux instance; and the r7 class (DECISIONS #review-1-r7, Claude's risks 1–3, Sol's 1–2, Grok's 1–2): any byte the app's post-processing touches that is not a text child's encoded edge or a mark's inner edge; any rewrite the assembler makes on a non-text child; any configured extension handler whose escapes a spelling table does not read (the table's `\\\\|`, the autolink literal's `unsafe` set, the strikethrough's `~`); any position where `toRendered(toSource(p))` is not `p` or the two views take a typed character differently under either route (stored marks cleared, or inherited) — searched beyond the reproductions the fix tasks name, with Claude's r7 method (ordered pairs and triples of installed inline node kinds, the failing seeds reported by class), one seed per configured handler's delimiter characters inside its enabling context and outside it, and one non-BMP seed per neighbour class and per inline position class. Sol's r7 probes (extended-probe.mjs, handler-probe.mjs, html-map-probe.mjs, hardbreak-browser.mjs, pipe-browser.mjs, between-browser.mjs, between-typed-browser.mjs, url-browser.mjs, with their logs and result files) are under /logs/reviews/1/r7/sol/ — re-run the extended probe, the handler probe and the html map probe at this SHA and report the fixed-point-failure, parsed-text-mismatch, unresolved and inverse-failure counts beside r7's 0 / 0 / 0, 16 and 0 of 5.",
  "acceptance": "/logs/reviews/1/r8/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r8.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r8c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r8",
  "description": "Review attempt r8, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r8 (after the r7 FAIL, DECISIONS #review-1-r7; all three r7 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r7: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r8.g1h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r8-specific checks: each r7 finding's disposition in DECISIONS #review-1-r7 (M1–M11) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r6 (L1–L11), r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the per-child walk in packages/core/src/format.ts (1.57) read against the installed lib/util/container-phrasing.js (the look-ahead, the eol-before-html branch, the two edge encodings, lines read), lib/handle/emphasis.js and strong.js (the inner-edge encodings), and shown to record each direct child's output through `state.handle`, to widen only at a text slice's encoded edge and inside a mark's markers, and to leave a code span, inline html, an image and a link's destination and title untouched — with `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot` and `installAutolinkFallback` unchanged; the break-before-html criterion (1.58) read as the round trip itself and shown never to hold a list of tag names, its two rewritten pins and the space-form member read; the `insideTable` flag and the two-unit pipe spelling in positions.ts (1.59) read against mdast-util-gfm-table's `inlineCodeWithTable` (lib/index.js, lines read) and the corrected `placeChildren` comment read against container-phrasing.js's look-ahead; the typed-marks rule in toggle.ts (1.60) read against prosemirror-state's `Transaction.insertText` and prosemirror-model's `ResolvedPos.marks()` in node_modules, the stored-marks argument traced from apps/desktop/src/dev/DevEditor.tsx, and the `byteEndOf` oracle's inline-code branch read; the paste leg's assertion and the destructive legs' timeout (1.61) read; in a browser: Claude's r7 R1 (a loaded ``see `&#xD83D;&#xDE00;` here``, Copy — the bytes unchanged) and Sol's r7 routes (the loaded `alpha\\\\`+LF+`<i>beta</i>` copied with its break; `| `+\"`a\\\\|b`\"+` |` with the caret after `a`, toggle twice, `X` → `aX|b`), the block-final code-span end by both routes (a click at the end of `foo` then toggle and `X`; the input rule then toggle and ` bar`), and the r7 routes of 1.49–1.53 re-driven; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the recorded deferrals are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text — Phase 2 file-open, hard stop `2.verify`), L11 with its M10 member (adjacent attention runs and a GFM literal glued to a run — upstream, `2.verify`), `[1.52, code block interior]` (the inverse's named exclusion, `2.verify`), `[1.53, a raw source keystroke before a punctuation-edged run]` (`2.verify`), `[#030 product, a soft or hard break before inline html]` (the soft-break space and the block-capable hard-break loss — Phase 2 file-open, `2.verify`), `[1.10.r7d, hard break inside a container]` (the caret after a hard break inside a blockquote or list item maps to the paragraph's start — found by the reconciliation, `2.verify`), `[review-1-r7 M7]` (`link.peek`'s three serialisations, `2.verify`) and `[1.46]` with its fourth, Linux instance; and the r7 class (DECISIONS #review-1-r7, Claude's risks 1–3, Sol's 1–2, Grok's 1–2): any byte the app's post-processing touches that is not a text child's encoded edge or a mark's inner edge; any rewrite the assembler makes on a non-text child; any configured extension handler whose escapes a spelling table does not read (the table's `\\\\|`, the autolink literal's `unsafe` set, the strikethrough's `~`); any position where `toRendered(toSource(p))` is not `p` or the two views take a typed character differently under either route (stored marks cleared, or inherited) — searched beyond the reproductions the fix tasks name, with Claude's r7 method (ordered pairs and triples of installed inline node kinds, the failing seeds reported by class), one seed per configured handler's delimiter characters inside its enabling context and outside it, and one non-BMP seed per neighbour class and per inline position class. Additionally (DECISIONS #review-1-r7 reviewer record; your own r7 independence section): your r7 report was PASS with two blockers in the tree — the join widening rewriting a code span's literal bytes (`format.ts:283–291` against `container-phrasing.js`'s two encodings, which fire on text children only, and `SPLIT_PAIR`'s pure-ASCII third form) and the break before html written `\\\\ ` (lines 60–80 against the break handler's `\\\\`+LF, pinned as-is at positions.test.ts:323) — the third of five counted attempts with that shape; state in the report, on your own reading, which read of the tree at 5618078 would have found each from the diff and the installed package without a build, and which severity it would have rated; then apply that read to the r8 chain first.",
  "acceptance": "/logs/reviews/1/r8/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r8.g1h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r8d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r8",
  "description": "Reconciliation of review attempt r8 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r8-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r8 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r9 and 1.10.r9a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r8a",
   "1.10.r8b",
   "1.10.r8c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.63",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r8 N1 — Sol finding 1 should-fix Required, with the link-edge members the reconciliation found (N1's block-start half); packages/editor/src/toggle.ts, the cursor map of passed tasks 1.7, 1.37, 1.43, 1.52, 1.53 and 1.60, and packages/editor/test/position-map-editor-leg.test.ts, a file of passed tasks 1.37 and 1.60, whose records are not edited — a new task per DECISIONS #016): at a block's edge the source caret is placed by the innermost text's spelling table, never by the marks a typed character takes, so the two views disagree wherever those marks leave an enclosing mark. The cause, read in the tree: `typedMarks` (toggle.ts:274–283) already computes `storedMarks ?? $pos.marks()` — prosemirror-state's `Transaction.insertText` rule (1.4.4, dist/index.js:627–644) —, `inlineBoundary` (:297–309) is `null` at a block's start and end (no node on one side), and `toSource` (:492–518) then asks `isLeafEnd` (:560–571), which returns `false` for a `text` node, so the answer is `spellingPoint(table, pos - inside.pmStart)` (:506): the innermost text's own end, inside every enclosing delimiter, whatever the typed marks say. 1.60 applied the typed-marks rule to the one node type that exposed it (`inlineCode`, M5) and not to the marks. Members, every one reproduced headlessly by the reconciliation at 970bb5c: `see *foo*` typed in the rendered view — the input rule's `removeStoredMark` leaves stored marks `[]` (input.ts:77–86) — then the toggle and `X`: rendered `see *foo*X`, source `see *fooX*`; strong `see **foo**X` vs `see **fooX**`; delete `see ~~foo~~X` vs `see ~~fooX~~`; the same in a heading, a list item and a table cell, and with an astral content `see *f😀*`; the click route (marks inherited, `[emphasis]`) agrees in every case and 1.60's inline-code member agrees on both routes — the controls; over the corpus, 256 block ends × 2 routes, eight block-final marked runs in six fixtures disagree on the `[]` route (astral-between-runs.md ×3, astral-neighbour.md, hard-break-in-emphasis.md, spanning-mark-last-boundary.md, spanning-mark-strikethrough.md, strikethrough-punctuation.md) and none on the click route. The other edge and the other mark, found by the reconciliation's sweep: `link` is `inclusive: false` (schema.ts:447), so at a block that starts or ends in a link the typed marks are `[]` on BOTH routes — link-in-emphasis.md paragraph 9 `[A bare link whose text begins with a space](essay.md) opens…` at the block's start: rendered `X[A bare…`, source `[XA bare…`; paragraph 7 `…see [the essay ](essay.md)` at the block's end: rendered `…](essay.md)X`, source `…[the essay X](essay.md)`. One invariant, and this task asserts it over the corpus with the instances as named guards on top (CLAUDE.md `## Code rules`): at a block's edge, where no inline boundary exists, the source caret is outside every enclosing mark the typed marks do not carry and inside every one they do — from the innermost mark outward, stopping at the first mark the typed marks carry (a nested `*foo **bar**` end with stored `[emphasis]`: after `**`, before `*`, `*foo **bar**X*`); an atom's and an inline-code run's own rule (`isLeafEnd`, 1.52/1.60) is unchanged and runs first for that leaf. The reconciliation's prototype — one loop over the entries enclosing the innermost leaf at the edge, comparing each mark entry's `node.type` with the typed marks' `type.name` (the schema's mark names equal the mdast node types: `emphasis`, `strong`, `delete`, `link`, `inline_code`) and answering the outermost left mark's range end (or start) — turned every member above green with toggle.test.ts and position-map-inverse.test.ts unchanged and exactly one pin red; it is the shape, not a mandate: read `innermostAt` (:317–330) and `delimiterPosition` (:531–558, the `toRendered` side, which already answers a closing delimiter with the position after the mark) before deciding. Do exactly this. (1) The rule in `toSource`, with its doc comment naming the invariant, both edges, the nesting order and prosemirror-state's `insertText` by file and line; `typedMarks`, `inlineBoundary` and `isLeafEnd` unchanged in signature. (2) Guards in packages/editor/test/toggle.test.ts beside 1.60's family (\"a block-final inline code span follows the marks a typed character takes\"), one `describe` titled for the invariant, enumerating: mark kinds `emphasis`, `strong`, `delete`, `link` × routes stored `null` (the click) and stored `[]` (the input rule) × edges start and end × block kinds paragraph, heading, list item, blockquote, table cell, plus one astral content per mark kind and the nested member above (stored `[emphasis]`), plus inline code as the unchanged control — each asserting the column, the two views' bytes by `toBe` (`typedInRendered` against `typedInSource`, the file's own helpers), the parse of the rendered bytes holding the run unchanged, and the inverse over every text position of the document (`assertInverseEverywhere`); the position set computed from the doc (`lastTextblockEnd` and its start twin), never a literal. (3) The corpus leg, in the same file: for every fixture in fixtures/markdown/index.json, every textblock's start and end, both routes, the two views' bytes equal — with ONE named exclusion, positively bounded (DECISIONS #032): on the `[]` route at a block end whose last character is punctuation the rendered view writes the letter as `&#x58;` (the encoded-neighbour rule, tasks 1.45/1.49) while the raw source keystroke after the closing delimiter dissolves the run (`a ~~(b)~~` → `a \\~\\~(b)\\~\\~X`) — the `[1.53, a raw source keystroke before a punctuation-edged run]` backlog class on its after side (DECISIONS #review-1-r8 N8, the member sentence already on that line); the leg asserts that every excluded position is such a run (last character punctuation by `Intl`-free ASCII/Unicode-punctuation test, the rendered bytes ending in `&#x58;`) and that at least one exists (the reconciliation counted eight), never `it.fails`, never a narrowing; fenced code interiors are outside the leg as in 1.52's. (4) The one pin that yields, named here so it is not a conflict: packages/editor/test/position-map-editor-leg.test.ts:511–532 `byteEndOf` (the deletion leg's oracle) has an inline-code branch (1.60) and no link branch, and its assertion at :832 (\"cursor at the end of paragraph 3\", link-in-emphasis.md) expects `{ line: 7, ch: 76 }` — inside the link text — where the invariant answers `{ line: 7, ch: 87 }`, after `)`: give `byteEndOf` a link branch (a link-final leaf's end is the end of its `](destination)` tail — read the link handler and `installAutolinkFallback` in packages/core/src/format.ts for the autolink `<…>` form and cover it if a fixture ends a block in one, else say so in the journal) with the rationale in its comment; the byte clause is otherwise unchanged and the leg stays green over every fixture. (5) One Playwright spec, e2e/web/editor-toggle-mark-end.spec.ts, Sol's route: in the rendered view type `see *foo*` (the input rule closes the run), poll the 1.62 readout (`getByTestId(\"selection\")`, the `selection` helper copied from e2e/web/editor-toggle-code-span-end.spec.ts) equal to `{ before: \"see foo\", empty: true, marks: [\"emphasis\"], stored: [] }`, `ControlOrMeta+/`, type `X`, the pane equal to `see *foo*X\\n` by `toBe`, the fixed point, toggle back, one `<em>` whose `textContent` is `foo`; a second case, the click route: load `see *foo*`, `clickAfter(page, \"foo\")` (1.62's helper copied), readout `{ before: \"see foo\", empty: true, marks: [\"emphasis\"], stored: null }`, the 200 ms tick with its comment, the chord, `X`, `see *fooX*\\n`. No Home/End key, no `ControlOrMeta+` chord for the caret, no DOM selection read (DECISIONS #022, #024, #037; `tests/no-caret-chords-in-e2e.test.ts` guards the file). (6) Asserted able to fail, recorded in the journal and not committed: with the rule reverted, every `[]`-route guard of (2) and the corpus leg red at the eight members (record the received columns). Scope: packages/editor/src/toggle.ts, packages/editor/test/toggle.test.ts, packages/editor/test/position-map-editor-leg.test.ts (the oracle's link branch and the one assertion only), e2e/web/editor-toggle-mark-end.spec.ts (new), docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md, the journal. No change to packages/core/**, packages/editor/src/schema.ts, apps/**, fixtures/**, any other spec, config or workflow files; no fixture added; zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r8 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The invariant's guard family green and listed in the journal (mark kind × route × edge × block kind, the astral members, the nested member, the inline-code control), each asserting the column, the two views' byte agreement and the inverse over every text position; the corpus leg green over every fixture in the index with its one exclusion positively bounded (both sides of the clause reached, the count of excluded positions read from the run into the journal, never a literal in the test); the `byteEndOf` link branch and the :832 assertion rewritten with the rationale (`grep -c 'byteEndOf' packages/editor/test/position-map-editor-leg.test.ts` at least 2; `grep -c 'link' packages/editor/test/position-map-editor-leg.test.ts` at least 1 more than at the branch base, both numbers in the journal); the reverted-rule mutation recorded with the guards it turned red; `toSource`'s doc comment naming both edges and `insertText` by file and line (`grep -c 'insertText' packages/editor/src/toggle.ts` at least 2; `grep -c 'storedMarks' packages/editor/src/toggle.ts` at least 2, unchanged); e2e/web/editor-toggle-mark-end.spec.ts present with both routes (`grep -c 'getByTestId(\"selection\")' e2e/web/editor-toggle-mark-end.spec.ts` at least 2, `grep -c 'getSelection' e2e/web/editor-toggle-mark-end.spec.ts` is 0, `grep -c 'see \\*foo\\*X' e2e/web/editor-toggle-mark-end.spec.ts` at least 1, `grep -c 'see \\*fooX\\*' e2e/web/editor-toggle-mark-end.spec.ts` at least 1); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container, their number in the journal read from the directory; `grep -c 'it.fails' packages/editor/test/toggle.test.ts packages/editor/test/position-map-editor-leg.test.ts` prints 0 for each; `grep -c '\\.skip(' ` on both is 0; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files.",
  "dependencies": [
   "1.10.r8d"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.64",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r8 N2 — Sol finding 2 should-fix Required; packages/editor/src/toggle.ts (`correspondences`, `walkInline`, `walkBlock` and the two directions' leaf arithmetic, the cursor map of passed tasks 1.7, 1.37, 1.43, 1.52, 1.53, 1.60 and 1.63) and packages/editor/src/schema.ts (`stripUnparsableWhitespace`, the conversion of passed tasks 1.1, 1.13, 1.29 and 1.30), whose records are not edited — a new task per DECISIONS #016): inline correspondences derive live ProseMirror positions from the normalised tree's widths, so every character after whitespace the conversion drops is mapped one place off, in both directions, and the inverse holds while both are wrong. The cause, read in the tree: `walkInline` (toggle.ts:164–178) starts at the block's content start and advances by `inlineWidth` of each mdast node (:135–150 — the value's length, a mark's children's sum, one for an atom); `stripUnparsableWhitespace` (schema.ts:905–940, called at :567, :573, :624 with `LINE_ENDING` or `CELL_LINE_ENDING`) has already dropped `LEADING_WHITESPACE` (:759) at a line start (except a block-initial link's), `TRAILING_WHITESPACE` (:760) at the block's end, collapsed every `LINE_ENDING_RUN` (:770) to one line ending, dropped runs that became empty and trailing `hard_break`s, while `unmarkEdgeWhitespace` moved edge whitespace out of marks — and the live document still holds every one of those characters. Reproduced headlessly by the reconciliation at 970bb5c: `a `+\"`cd`\"+` b`, the first character deleted, live text ` `+\"`cd`\"+` b` (the readout's `before: \" c\"`), caret between `c` and `d`: `toSource` answers column 3 — rendered `\\`cXd\\` b`, source `\\`cdX\\` b`; the same for plain text (`cXd b` vs `cdX b`), emphasis, strong and a list item; `toRendered(toSource(p)) === p` at every one (both directions share the false start). Over the corpus, the first character of every textblock deleted (248 blocks in the 65 fixtures): 54 blocks lose their lead, and at every text position of those blocks the two views disagree (4,374 of 4,374 positions checked in them); the six disagreements outside them are members of recorded classes (`[1.53]`'s before side on an astral neighbour, L9's autolink literal, and N1's link-edge members that 1.63 fixes). Sol's constraints, kept: the serializer's forbidden whitespace is not restored and the live buffer is not touched — the loss is decided (tasks 1.13/1.29/1.30); the correspondence is what is wrong. Do exactly this. (1) Export from packages/editor/src/schema.ts one function that performs `stripUnparsableWhitespace`'s normalisation AND returns, for every kept output node, the live position of each of its characters (a kept-character map: output offset → live position, plus the live range each output node covers) — and make `stripUnparsableWhitespace` a projection of it, so the two cannot disagree (one implementation of every branch: the line-start rule, the block-initial link exception, the line-ending run, the empty run, the block-end loop, the trailing break); its doc comment states the ownership rule for a live position that sits inside dropped whitespace ONCE (the position maps to the next kept character; at the block's end, to the block's end) — the rule tested at the first, middle and last dropped character (CLAUDE.md `## Code rules`). (2) `correspondences` in toggle.ts consumes it: `walkBlock` hands each textblock's ProseMirror inline children and its line ending to that function, and every inline correspondence's `pmStart`/`pmEnd` are live positions; the leaf-interior arithmetic — `pos - inside.pmStart` in `toSource` (:506, the spelling-table index) and `entry.pmStart + spellingIndex(…)` in `toRendered` (:522) — translates through the kept-character map in both directions, and `inlineWidth` is deleted or reduced to the atom case (say which in the journal). A document with no dropped whitespace has identical correspondences to today's — the guard that the change is a no-op there is the existing corpus inverse leg and toggle.test.ts's whole inverse family, green unchanged. (3) Guards in packages/editor/test/toggle.test.ts, one `describe` titled for the rule, enumerating whitespace classes — a lead at the block's start, a lead after a hard break, a trailing run at the block's end, a line-ending run with spaces on both sides (a paragraph's `LINE_ENDING` and a cell's `CELL_LINE_ENDING`), a whitespace-only run between two nodes, a dropped trailing hard break — × node classes — plain text, marked text, inline code, a link's text, a run beside an atom — × containers — paragraph, heading, list item, blockquote, table cell — with one astral member; each seeded from a typing-shaped or deleting transaction on `mdastToPM(parse(·))` (never a hand-built ProseMirror doc), asserting the two views' bytes by `toBe` at the positions on both sides of the dropped whitespace (`typedInRendered` against `typedInSource` on `format(pmToMdast(live))`), the inverse over every text position, and the dropped-whitespace ownership rule at its three positions. (4) The corpus leg `deleteAtEveryBlockStart` in packages/editor/test/position-map-inverse.test.ts beside the two destructive legs (the 30 s per-test timeout in 1.61's form and comment): for every fixture in the index, the first character of every textblock outside a fenced code block deleted, and at every text position of that block the two views' bytes equal AND the inverse holds — a third destructive transaction in the family (CLAUDE.md: a corpus leg seeded from the writing surface's own output includes a destructive transaction); the leg records how many blocks lost their lead and asserts at least one did and at least one did not (both sides of the clause; the reconciliation's sweep: 54 of 248 — a count read from the run, never a literal in the test); named exclusions positively bounded (DECISIONS #032): the `[1.53]` members (a raw source keystroke before or after a punctuation-edged run, the rendered bytes carrying `&#x58;`) and `[review-1-r6 L9]`'s member (a character typed inside an autolink literal's text, the rendered bytes a `[text](url)` link) — each excluded position asserted to be of its class, never `it.fails`, never a narrowing. (5) One Playwright spec, e2e/web/editor-toggle-stripped-lead.spec.ts, Sol's route: load `a `+\"`cd`\"+` b`, click at the left edge of the paragraph's first character measured from its text node's own `Range` rect (1.62's measured-click shape; an interior text position with one DOM spelling), poll the readout equal to `{ before: \"\", empty: true, marks: [], stored: null }`, press `Delete`, poll `before: \"\"` with the pane equal to `\\`cd\\` b\\n`, click at the left edge of `d` inside the `<code>` text node (measured as the pipe click in e2e/web/editor-toggle-cell-code-pipe.spec.ts), poll `{ before: \" c\", empty: true, marks: [\"inline_code\"], stored: null }`, the 200 ms tick with its comment, `ControlOrMeta+/`, type `X`, the pane equal to `\\`cXd\\` b\\n` by `toBe`, the fixed point; a second case the strong twin `a **cd** b` → `**cXd** b\\n`. No Home/End key, no `ControlOrMeta+` chord for the caret, no DOM selection read (DECISIONS #022, #024, #037). (6) Asserted able to fail, recorded in the journal and not committed: the width arithmetic restored in `walkInline` — the corpus leg red at the stripped-lead blocks and the (3) lead guards red (record the received columns). Pins: the reconciliation ran no prototype of this shape; the legs named in (2) assert positions of documents with no dropped whitespace, which the change leaves as they are — a pin that turns red is a defect of the implementation, not a pin to rewrite: stop, journal it with the position and both values, and do not edit the pin in this task. Scope: packages/editor/src/toggle.ts, packages/editor/src/schema.ts (the export and the projection only; `pmToMdast`'s output bytes unchanged for every fixture — the schema-roundtrip and corpus fixed-point tests green unchanged), packages/editor/test/toggle.test.ts, packages/editor/test/position-map-inverse.test.ts, e2e/web/editor-toggle-stripped-lead.spec.ts (new), docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md, the journal. No change to packages/core/**, apps/**, fixtures/**, any other spec, config or workflow files; no fixture added; zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r8 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The exported correspondence function present in schema.ts and consumed in toggle.ts (its name in the journal; `grep -c '<name>' packages/editor/src/schema.ts` at least 2 and `grep -c '<name>' packages/editor/src/toggle.ts` at least 1, the name substituted), `stripUnparsableWhitespace` a projection of it (one body for the branches; the journal says how), the dropped-whitespace ownership rule stated once in its doc comment and tested at three positions; the whitespace × node × container guards green and listed in the journal with the astral member, each asserting the two views' bytes and the inverse; the corpus leg `deleteAtEveryBlockStart` green over every fixture (`grep -c 'deleteAtEveryBlockStart' packages/editor/test/position-map-inverse.test.ts` at least 2; `grep -c '30_000\\|30000' packages/editor/test/position-map-inverse.test.ts` at least 3), both sides of its lead clause reached with the counts in the journal, its exclusions positively bounded; the existing corpus inverse legs and toggle.test.ts's inverse family green unchanged; the restored-arithmetic mutation recorded with the cases it turned red; e2e/web/editor-toggle-stripped-lead.spec.ts present with both cases (`grep -c 'getByTestId(\"selection\")' e2e/web/editor-toggle-stripped-lead.spec.ts` at least 2, `grep -c 'getSelection' e2e/web/editor-toggle-stripped-lead.spec.ts` is 0, `grep -c 'cXd' e2e/web/editor-toggle-stripped-lead.spec.ts` at least 2); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container, their number in the journal read from the directory; `grep -c 'it.fails' packages/editor/test/toggle.test.ts packages/editor/test/position-map-inverse.test.ts` prints 0 for each; `grep -c '\\.skip(' ` on both is 0; `git diff --stat` shows no change under packages/core, apps/, fixtures/ or any other spec; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files.",
  "dependencies": [
   "1.63"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.65",
  "model": "opus",
  "description": "Phase 1 review fix (DECISIONS #review-1-r8 N3 — Claude finding 1 should-fix; packages/core/src/format.ts, the per-child widening walk of passed task 1.57, and packages/core/src/positions.ts, `formatWithMap`'s `unresolved` of passed tasks 1.2, 1.46, 1.51 and 1.59, whose records are not edited — a new task per DECISIONS #016): the walk's give-up path is unguarded — `locateChild`'s `return undefined` (format.ts:431–432, the only uncovered line in the tree at the r8 gate, coverage lines 99.94 %) reached from `widenSplitSurrogateReferences` (:515, `if (located === undefined) break;`) silently leaves children i+1…n unwidened, and the doc comment (:500–503) discharges the branch by saying `locateEmission` in positions.ts enumerates the same forms so the map's corpus case guards it — false as written: `locateEmission` (positions.ts:635–652) finds a child with `container.indexOf(candidate.text, cursor)`, a forward search that succeeds at ANY offset at or after the cursor, and offers a non-text child one candidate, its raw handler value, while `locateChild` requires `joined.startsWith(form.text, cursor)` AT the cursor and carries head/tail-encoded and eol-as-space forms; the more permissive enumeration is the guard, so the corpus case stays green while the walk gives up. Reproduced by the reconciliation as unreachable today: with the `break` replaced by a throw, the whole unit suite at 970bb5c (44 files, 3,947 tests, every fixture, parse(fixture) and format(parse(·))) ran with zero throws; Claude's 11,560 swept trees plus the 65 corpus files against an instrumented counter: 0 give-ups. A missing guard, not a live defect — the class M2's promoted rule names (a rewrite the map cannot follow is one the parser cannot read), and Claude's risk 3: the two paths that resolve a surprise by doing less, quietly, this one and `applyEdits`' overlap skip (:475, `if (edit.start < at) continue`). Do exactly this. (1) The walk records its give-up per call, never in module-level state (CLAUDE.md `packages/core` rules: pure functions with injected state): read how `handleRoot` (:669–706) installs the recorder on the serializer's `state` for that `toMarkdown` call (`installSurrogateWidening`, :567–590) and record the give-up there — the parent's path or node and the index and type of the child the walk could not locate — or thread it back from the walk as a return value; `format` keeps today's bytes (the walk stops; nothing is widened blind). (2) `formatWithMap` (positions.ts:194) reports each recorded give-up as an `unresolved` entry (the map's existing instrument, a path string naming the parent and the child index; the `PositionMap` doc comment at :34–40 updated) — read `formatWithMap` first to confirm the widening walk runs under it as it does under `format` (the same extension list from :701); if it does not, make it so and say so in the journal. The corpus case \"maps every node of the tree, and nothing is unresolved\" (packages/core/test/positions-corpus.test.ts) then covers the walk for every fixture; confirm it also runs over `format(parse(·))` of each fixture (Claude's second leg) and, if it does not, add that leg beside it. (3) A hand-built guard that reaches the branch: export the walk (or a thin test-only entry) and call it with a `RecordedChild` whose value is absent from the join — the give-up recorded, the join returned unchanged, the children before it widened and the ones after it untouched (the walk's contract stated in its doc comment) — so format.ts:432 is covered by a test that asserts the behaviour, not by a mutation alone. (4) The same treatment for `applyEdits`' overlap skip (:475): either an overlapping edit is recorded the same way and reported through `unresolved`, or one paragraph in the journal names why two edits cannot overlap by construction and the test that shows it — decide from the code, record the decision in the doc comment. (5) The doc comment at :495–503 rewritten: the instrument (`unresolved`) is the guard; the sentence naming `locateEmission` as the equivalent enumeration deleted; the two enumerations' difference (`indexOf` vs `startsWith`, the candidate sets) stated in one sentence so the next reader does not rebuild the false argument. (6) Asserted able to fail, recorded in the journal and not committed: remove the eol-as-space form from `emissionForms` (:370–406) — the corpus case red on the fixture whose walk needs it (record which fixture and which path), and the hand-built guard of (3) red with the give-up entry absent. Bytes: `format(parse(x))` byte-identical for every fixture in the index (the corpus identity and fixed-point cases green unchanged) — no widening decision changes. Scope: packages/core/src/format.ts, packages/core/src/positions.ts, packages/core/test/positions-corpus.test.ts (a leg if needed), one new or extended test file under packages/core/test for the hand-built guard, docs/V1.1-BACKLOG.md (only if a line is added), docs/lessons.md, the journal. No change to packages/editor/**, apps/**, e2e/**, fixtures/**, config or workflow files; no fixture added; zero new dependencies (PRD §4). Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r8 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "The give-up recorded per call and reported through `formatWithMap`'s `unresolved` (the hand-built guard green: the entry present, the join unchanged, the widening before the give-up present and after it absent — presence and absence); the corpus case green over every fixture in the index for parse(fixture) and for format(parse(·)) with the walk under it (the journal names the test titles); `applyEdits`' overlap skip decided and recorded (either an `unresolved` entry with its guard, or the by-construction argument with its test named); the doc comment no longer names `locateEmission` as the guard (`grep -c 'locateEmission' packages/core/src/format.ts` is 0) and names `unresolved` (`grep -c 'unresolved' packages/core/src/format.ts` at least 1); the removed-form mutation recorded with the fixture and path it turned red; the coverage report shows no uncovered line in packages/core/src/format.ts (the line read from the run into the journal); every fixture's `format(parse(x))` byte-identical (the corpus identity cases green unchanged); `pnpm lint && pnpm test && cargo test` green; every e2e/web spec green in the container, their number in the journal read from the directory; `grep -c 'it.fails' packages/core/test/*.ts` prints 0 for each file; `git diff --stat` shows no change under packages/editor, apps/, e2e/ or fixtures/; no manifest or lockfile change; zero new dependencies (PRD §4). `git diff --stat` against the branch base touches only the scope files.",
  "dependencies": [
   "1.64"
  ],
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r9",
  "model": "sonnet",
  "description": "Phase 1 verification for review attempt r9 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r9 chain — six candidates weighed at DECISIONS #review-1-r8, three promoted and three recorded. (a) Promote, as a bullet of `## Code rules` in both CLAUDE.md and AGENTS.md, after the three r8 bullets (M1, M2, M5), verbatim and identical in both files: \"A cursor rule derived from the marks a typed character takes is applied at both edges of a block to every mark the schema declares — emphasis, strong, delete, link and inline code alike — placing the source caret outside each enclosing mark those marks do not carry and inside each they do, and its guard set enumerates every mark kind × both routes (stored marks cleared, inherited) × both edges × every textblock kind with an astral member, because a rule scoped to the node type that exposed it is one route and one kind short (DECISIONS #review-1-r8 N1).\" Discharged by 1.63's invariant family in packages/editor/test/toggle.test.ts and its corpus leg — name them by title in the journal. (b) Promote, as the next bullet, verbatim: \"A correspondence between the editor's document and the tree the converter makes of it is anchored on the live document's own characters, never on widths read from the normalised tree, and its corpus leg deletes the first character of every textblock and asserts two-view insertion equivalence at every position of that block beside the inverse, because two directions built from one wrong offset table agree with each other while both are wrong (DECISIONS #review-1-r8 N2).\" Discharged by 1.64's `deleteAtEveryBlockStart` leg in packages/editor/test/position-map-inverse.test.ts and its whitespace guards — name them by title. (c) Promote, as the next bullet, verbatim: \"A branch whose safety argument is that a second implementation enumerates the same cases is discharged by an instrument the corpus case reads — the branch records its give-up where the map reports `unresolved`, and a mutation that removes one case turns the corpus red — never by a doc-comment sentence, and the uncovered line in the coverage report is where that pair lives (DECISIONS #review-1-r8 N3).\" Discharged by 1.65's hand-built give-up guard and the corpus case under packages/core/test — name them by title. (d) Record, do not promote: Claude's r8 lesson that an allowlist citing a PRD row certifies the name and never the version addresses `scripts/check-deps.mjs` at the phase boundary (DECISIONS #review-1-r8 N5, Michael's OK); (e) record, do not promote: Claude's r8 lesson that a human observation gate certifies the SHA it was recorded at addresses the principal — docs/PRINCIPAL.md at the boundary and N4's ask; (f) record, do not promote: Claude's r8 lesson that a sweep states which axes it varied addresses reviewers (the r9 rows carry it); Sol's two r8 lessons are (a) and (b). The journal says so in one sentence each. `ralph/check-agent-rules.sh` must exit 0 and tests/agent-rules.test.ts must pass after the edit. The journal states the number of spec files under e2e/web/ counted from the directory (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`), never a literal (DECISIONS #review-1-r6 L10). No other procedure change this attempt. Each attempt appends a NEW `- [<id>] ` journal line to docs/progress/journal-main.md, stubbed first by one `printf` that prints the timestamp (`printf -- '- [%s] %s Task: …' <id> \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"`) and completed in place before the commit that prints DONE — an earlier attempt's line is never edited; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lessons [1.29], [1.41]). Run the suite once: after the implementation is complete, `pnpm lint && pnpm test && cargo test` and the e2e/web suite each one time (`e2e/web/node_modules/.bin/playwright install chromium` first, lesson [1.50]), then complete the stub, commit and print the DONE promise for this task — the runner re-runs the whole suite on the squashed candidate (RUNNER-SPEC §4), and a second verification pass in the same attempt is what the r5–r8 chains' turn caps were spent on (lessons [1.41], [1.42], [1.47], [1.54]). A defect found outside this scope gets a docs/V1.1-BACKLOG.md line with a revisit trigger and a hard stop in this task's commit, never only a lesson or a test comment (DECISIONS #review-1-r1); a class this task's new assertions turn red on a fixture outside its scope is filed the same way and asserted as a named, positively bounded member, never as `it.fails` and never by narrowing (DECISIONS #032). Procedure note (DECISIONS #review-1-r2 H7): this task's generated `PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration steps (implement, then one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list is the one that stands — the wrapper's own line 1 says CLAUDE.md outranks it, the mismatch is mechanical (commit granularity), and it is not a conflict to stop on — so stub the journal, commit, implement, complete the stub.",
  "acceptance": "pnpm lint (which includes `node scripts/check-deps.mjs`), pnpm test, cargo test green in the container; `grep -c 'it.fails' packages/core/test/*.ts packages/editor/test/*.ts` prints 0 for each file; every e2e/web spec green in the container, their number in the journal read from the directory; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change and touches only CLAUDE.md, AGENTS.md, docs/lessons.md and the journal; `ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passes; the journal names the discharging test for each clause of (a), (b) and (c) and records (d), (e) and (f).",
  "dependencies": [
   "1.65"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r9/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r9h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "phase": "1",
  "execution": "loop",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.verify.r9h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "1.verify.r9",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/1.verify.r9/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "playwright-report"
   ],
   "cleanupRef": true
  },
  "phase": "1",
  "targetBranch": "phase/1",
  "description": "CI gate for 1.verify.r9: scripts/gate.sh 1.verify.r9h pushes integrated_sha of 1.verify.r9 to ci/1.verify.r9/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"playwright-report\"] into /logs/ci/1.verify.r9/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/web (Playwright: every spec file under e2e/web/ including the 1.17 latency spec, count from the directory; a failing spec fails the job, DECISIONS #019) green on all three runners at the implementation SHA; a green gate proves both suites ran on ubuntu, macos and windows at the integrated SHA; the accepted `test-logs` and `playwright-report` artifacts under /logs/ci/1.verify.r9h/accepted/ (the gate id; DECISIONS #018) each hold three OS directories.",
  "dependencies": [
   "1.verify.r9"
  ]
 },
 {
  "id": "1.10.r9a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "reviewSet": "1.10",
  "reviewAttempt": "r9",
  "description": "Review attempt r9, claude reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r9 (after the r8 FAIL, DECISIONS #review-1-r8; all three r8 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r8: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r9h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r9-specific checks: each r8 finding's disposition in DECISIONS #review-1-r8 (N1–N8) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r7 (M1–M11), r6 (L1–L11), r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the block-edge rule in packages/editor/src/toggle.ts (1.63) read against prosemirror-state's `Transaction.insertText` and prosemirror-model's `ResolvedPos.marks()` in node_modules and against every mark's `inclusive` flag in packages/editor/src/schema.ts, shown to apply at both edges to every mark kind including `link` and nested marks, with `typedMarks`, `inlineBoundary` and `isLeafEnd` unchanged in signature, and the `byteEndOf` oracle's link branch read; the live correspondence (1.64) read against every branch of `stripUnparsableWhitespace` in schema.ts (the line-start rule, the block-initial link exception, the line-ending run, the empty run, the block-end loop, the trailing break, `unmarkEdgeWhitespace`'s moved whitespace) — each branch matched to a guard, the ownership rule for a dropped position stated once — and both directions' leaf arithmetic in toggle.ts shown to translate through it; the give-up instrument (1.65) read against `formatWithMap` in packages/core/src/positions.ts (the walk shown to run under it) and the hand-built give-up guard read, with `applyEdits`' overlap skip decided; the three promote bullets present exactly once in each of CLAUDE.md and AGENTS.md; in a browser: Sol's r8 routes (`see *foo*` typed so the input rule closes the run, the toggle, `X` → `see *foo*X`; `a `+\"`cd`\"+` b` loaded, the first character deleted, a click between `c` and `d`, the toggle, `X` → `\\`cXd\\` b`), 1.60's and 1.62's routes and the r8 routes of 1.57–1.59 re-driven; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the recorded deferrals are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text — Phase 2 file-open, hard stop `2.verify`), L11 with its M10 member (`2.verify`), `[1.52, code block interior]` (`2.verify`), `[1.53, a raw source keystroke before a punctuation-edged run]` with its N8 after-side member (`2.verify`), `[#030 product, a soft or hard break before inline html]` (`2.verify`), `[1.10.r7d, hard break inside a container]` (`2.verify`), `[review-1-r7 M7]` (`2.verify`), `[1.46]` with its two races (#037), `[1.59, found outside scope]` (`canonicalCursor`'s inline-code interior, `2.verify`), `[review-1-r8 N5]` (React 19 vs PRD §4, Michael's ratification at the boundary) and `[review-1-r8 N7]` (`ci.yml`'s merge step, the next `.g<n>` repair or the boundary); and the r8 class (DECISIONS #review-1-r8, Claude's risks 1–3, Sol's 1–3, Grok's 1–3): any block-edge position where the typed marks and the enclosing delimiters disagree for any mark kind, nesting included; any live position a normalisation moves — leading, trailing, line-ending runs, `unmarkEdgeWhitespace`'s moved edge whitespace, a dropped empty run, a dropped trailing hard break — in either direction, with two-view insertion equivalence as the oracle beside the inverse; any give-up path that resolves a surprise by doing less quietly; searched beyond the reproductions the fix tasks name, with Claude's method (ordered pairs and triples of installed inline node kinds, the failing seeds reported by class) and Sol's (destructive transactions removing leading content before retained runs, the two views compared), the report stating which axes each sweep varied and which it held fixed (Claude's r8 lesson), one non-BMP seed per neighbour class and per inline position class.",
  "acceptance": "/logs/reviews/1/r9/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r9h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r9b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "reviewSet": "1.10",
  "reviewAttempt": "r9",
  "description": "Review attempt r9, sol reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r9 (after the r8 FAIL, DECISIONS #review-1-r8; all three r8 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r8: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r9h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r9-specific checks: each r8 finding's disposition in DECISIONS #review-1-r8 (N1–N8) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r7 (M1–M11), r6 (L1–L11), r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the block-edge rule in packages/editor/src/toggle.ts (1.63) read against prosemirror-state's `Transaction.insertText` and prosemirror-model's `ResolvedPos.marks()` in node_modules and against every mark's `inclusive` flag in packages/editor/src/schema.ts, shown to apply at both edges to every mark kind including `link` and nested marks, with `typedMarks`, `inlineBoundary` and `isLeafEnd` unchanged in signature, and the `byteEndOf` oracle's link branch read; the live correspondence (1.64) read against every branch of `stripUnparsableWhitespace` in schema.ts (the line-start rule, the block-initial link exception, the line-ending run, the empty run, the block-end loop, the trailing break, `unmarkEdgeWhitespace`'s moved whitespace) — each branch matched to a guard, the ownership rule for a dropped position stated once — and both directions' leaf arithmetic in toggle.ts shown to translate through it; the give-up instrument (1.65) read against `formatWithMap` in packages/core/src/positions.ts (the walk shown to run under it) and the hand-built give-up guard read, with `applyEdits`' overlap skip decided; the three promote bullets present exactly once in each of CLAUDE.md and AGENTS.md; in a browser: Sol's r8 routes (`see *foo*` typed so the input rule closes the run, the toggle, `X` → `see *foo*X`; `a `+\"`cd`\"+` b` loaded, the first character deleted, a click between `c` and `d`, the toggle, `X` → `\\`cXd\\` b`), 1.60's and 1.62's routes and the r8 routes of 1.57–1.59 re-driven; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the recorded deferrals are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text — Phase 2 file-open, hard stop `2.verify`), L11 with its M10 member (`2.verify`), `[1.52, code block interior]` (`2.verify`), `[1.53, a raw source keystroke before a punctuation-edged run]` with its N8 after-side member (`2.verify`), `[#030 product, a soft or hard break before inline html]` (`2.verify`), `[1.10.r7d, hard break inside a container]` (`2.verify`), `[review-1-r7 M7]` (`2.verify`), `[1.46]` with its two races (#037), `[1.59, found outside scope]` (`canonicalCursor`'s inline-code interior, `2.verify`), `[review-1-r8 N5]` (React 19 vs PRD §4, Michael's ratification at the boundary) and `[review-1-r8 N7]` (`ci.yml`'s merge step, the next `.g<n>` repair or the boundary); and the r8 class (DECISIONS #review-1-r8, Claude's risks 1–3, Sol's 1–3, Grok's 1–3): any block-edge position where the typed marks and the enclosing delimiters disagree for any mark kind, nesting included; any live position a normalisation moves — leading, trailing, line-ending runs, `unmarkEdgeWhitespace`'s moved edge whitespace, a dropped empty run, a dropped trailing hard break — in either direction, with two-view insertion equivalence as the oracle beside the inverse; any give-up path that resolves a surprise by doing less quietly; searched beyond the reproductions the fix tasks name, with Claude's method (ordered pairs and triples of installed inline node kinds, the failing seeds reported by class) and Sol's (destructive transactions removing leading content before retained runs, the two views compared), the report stating which axes each sweep varied and which it held fixed (Claude's r8 lesson), one non-BMP seed per neighbour class and per inline position class. Sol's r8 probes (extended-probe.mjs, handler-probe.mjs, html-map-probe.mjs, adjacency-probe.mjs, delimiter-probe.mjs, marks-probe.mjs, marks-astral-probe.mjs, trim-map-probe.mjs, marks-browser.mjs, trim-browser.mjs, with their logs and result files) are under /logs/reviews/1/r8/sol/ — re-run the extended, handler, html-map, marks and trim-map probes at this SHA and report the fixed-point-failure, parsed-text-mismatch, unresolved, inverse-failure and two-view-mismatch counts beside r8's 0 / 0 / 0, 6, 0 of 5 and 9 of 48.",
  "acceptance": "/logs/reviews/1/r9/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r9h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r9c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "reviewSet": "1.10",
  "reviewAttempt": "r9",
  "description": "Review attempt r9, grok reviewer, for review set 1.10 (phase 1). Review set for Phase 1, attempt r9 (after the r8 FAIL, DECISIONS #review-1-r8; all three r8 reports were independent — DECISIONS #025's condition met by the parallel start; read #025 as an input and name, in your inputs line, every file you read under /logs/reviews/, and read no sibling directory). Phase checks as r8: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol (e2e/web/latency.spec.ts doc comment), reported with the protocol named and the isolated single-worker run stated separately from any contended or CI number, including the source view's `commit` line — the 16 ms judgement is the reviewer's, from the numbers, not CI's; all tests and every e2e/web spec green on 3 OSes (from /logs/ci/1.verify.r9h/accepted/, three OS directories in both artifacts; the spec-file count read from the directory); fresh-clone cold build. r9-specific checks: each r8 finding's disposition in DECISIONS #review-1-r8 (N1–N8) verified against the tree (fixed, deferred with a backlog line, or recorded), and each r7 (M1–M11), r6 (L1–L11), r5 (K1–K9), r4 (J1–J9), r3 (I1–I10), r2 (H1–H9), r1 (G1–G12) and r0 (F1–F16) disposition re-confirmed; the block-edge rule in packages/editor/src/toggle.ts (1.63) read against prosemirror-state's `Transaction.insertText` and prosemirror-model's `ResolvedPos.marks()` in node_modules and against every mark's `inclusive` flag in packages/editor/src/schema.ts, shown to apply at both edges to every mark kind including `link` and nested marks, with `typedMarks`, `inlineBoundary` and `isLeafEnd` unchanged in signature, and the `byteEndOf` oracle's link branch read; the live correspondence (1.64) read against every branch of `stripUnparsableWhitespace` in schema.ts (the line-start rule, the block-initial link exception, the line-ending run, the empty run, the block-end loop, the trailing break, `unmarkEdgeWhitespace`'s moved whitespace) — each branch matched to a guard, the ownership rule for a dropped position stated once — and both directions' leaf arithmetic in toggle.ts shown to translate through it; the give-up instrument (1.65) read against `formatWithMap` in packages/core/src/positions.ts (the walk shown to run under it) and the hand-built give-up guard read, with `applyEdits`' overlap skip decided; the three promote bullets present exactly once in each of CLAUDE.md and AGENTS.md; in a browser: Sol's r8 routes (`see *foo*` typed so the input rule closes the run, the toggle, `X` → `see *foo*X`; `a `+\"`cd`\"+` b` loaded, the first character deleted, a click between `c` and `d`, the toggle, `X` → `\\`cXd\\` b`), 1.60's and 1.62's routes and the r8 routes of 1.57–1.59 re-driven; `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` run in the cold build; docs/V1.1-BACKLOG.md read for any revisit trigger that has fired — the recorded deferrals are not new findings unless their premise is false: L9 (URL-shaped text the editor holds as text — Phase 2 file-open, hard stop `2.verify`), L11 with its M10 member (`2.verify`), `[1.52, code block interior]` (`2.verify`), `[1.53, a raw source keystroke before a punctuation-edged run]` with its N8 after-side member (`2.verify`), `[#030 product, a soft or hard break before inline html]` (`2.verify`), `[1.10.r7d, hard break inside a container]` (`2.verify`), `[review-1-r7 M7]` (`2.verify`), `[1.46]` with its two races (#037), `[1.59, found outside scope]` (`canonicalCursor`'s inline-code interior, `2.verify`), `[review-1-r8 N5]` (React 19 vs PRD §4, Michael's ratification at the boundary) and `[review-1-r8 N7]` (`ci.yml`'s merge step, the next `.g<n>` repair or the boundary); and the r8 class (DECISIONS #review-1-r8, Claude's risks 1–3, Sol's 1–3, Grok's 1–3): any block-edge position where the typed marks and the enclosing delimiters disagree for any mark kind, nesting included; any live position a normalisation moves — leading, trailing, line-ending runs, `unmarkEdgeWhitespace`'s moved edge whitespace, a dropped empty run, a dropped trailing hard break — in either direction, with two-view insertion equivalence as the oracle beside the inverse; any give-up path that resolves a surprise by doing less quietly; searched beyond the reproductions the fix tasks name, with Claude's method (ordered pairs and triples of installed inline node kinds, the failing seeds reported by class) and Sol's (destructive transactions removing leading content before retained runs, the two views compared), the report stating which axes each sweep varied and which it held fixed (Claude's r8 lesson), one non-BMP seed per neighbour class and per inline position class. Additionally (DECISIONS #review-1-r8 reviewer record; your own r8 independence section): your r8 report was PASS with no finding while two should-fix marked Required were in the tree, both reproduced by the reconciliation — the block-end branch answering the innermost text's spelling-table end for every mark while `isLeafEnd` (toggle.ts:560–571 at 970bb5c) consulted the typed marks for `inlineCode` only (DECISIONS #review-1-r7 M5's rule applied to one node type), and `walkInline` (toggle.ts:164–178) accumulating mdast widths while `stripUnparsableWhitespace` (schema.ts:905–940) drops characters the live document keeps — state in the report, on your own reading, which read of the tree at 970bb5c would have found each from the diff and the installed package without a build, and which severity it would have drawn; then apply that read to the r9 chain first.",
  "acceptance": "/logs/reviews/1/r9/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "1.verify.r9h"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.10.r9d",
  "model": "opus",
  "execution": "interactive-principal",
  "reviewSet": "1.10",
  "reviewAttempt": "r9",
  "description": "Reconciliation of review attempt r9 for review set 1.10 (phase 1): copy the three reports to docs/reviews/phase-1-r9-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-1-r9 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 1.verify.r10 and 1.10.r10a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "1.10.r9a",
   "1.10.r9b",
   "1.10.r9c"
  ],
  "phase": "1",
  "targetBranch": "phase/1"
 },
 {
  "id": "1.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/1 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; write the close intent; one ref transaction moving main and creating phase/2; then finalise .evidence/closes and the next phase base record.",
  "acceptance": "main == reconciliation_sha of the newest Phase 1 review attempt (verdict PASS); suite green on that commit; branch phase/2 exists at main with its base recorded in phases.json; .evidence/closes written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "1.10.r9d"
  ],
  "nextPhase": "2",
  "nextBranch": "phase/2",
  "phase": "1",
  "sourceBranches": [
   "phase/1"
  ],
  "targetBranch": "main"
 },
 {
  "id": "2.1",
  "model": "sonnet",
  "description": "Shell e2e harness: e2e/shell with WebdriverIO + @wdio/tauri-service; ci.yml job `e2e-shell` runs it via tauri-driver on ubuntu (with xvfb + webkit2gtk-driver) and windows (msedgedriver), and via the WDIO Tauri plugin's embedded provider on macos marked continue-on-error until Phase 6.",
  "acceptance": "The smoke spec passes in the container under xvfb via tauri-driver; the `e2e-shell` job is defined for ubuntu, windows and macOS (macOS continue-on-error) and actionlint passes.",
  "dependencies": [
   "1.close"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/2.1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "The smoke spec passes on the ubuntu and windows runners; the macOS result is recorded in the job summary.",
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "2.1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/2.1/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell"
   ],
   "cleanupRef": true
  },
  "phase": "2",
  "targetBranch": "phase/2",
  "description": "CI gate for 2.1: scripts/gate.sh 2.1h pushes integrated_sha of 2.1 to ci/2.1/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"e2e-shell\"] into /logs/ci/2.1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "The smoke spec passes on the ubuntu and windows runners; the macOS result is recorded in the job summary.",
  "dependencies": [
   "2.1"
  ]
 },
 {
  "id": "2.2",
  "model": "sonnet",
  "description": "Rust commands open_folder, list_tree (recursive, `.md` only, ignores dotfiles, node_modules, and `.icloud` placeholders which are listed as `{cloudOnly:true}`), read_doc, write_doc (temp + atomic rename, preserves mode), read_sidecar, write_sidecar. Capability file grants fs and asset-protocol scope only to the chosen folder, persisted with tauri-plugin-persisted-scope; CSP per §6.4; `WorkspaceRoot` authority per §6.4 enforced by every path-taking command.",
  "acceptance": "cargo test: write_doc to a permission-denied dir returns io::ErrorKind::PermissionDenied (Windows variant uses an ACL-denied temp dir under #[cfg(windows)]); no temp file remains after success or failure; list_tree on fixtures/markdown returns exactly the number of `.md` files counted by the test itself, sorted by name; a `x.md.icloud` file is reported cloudOnly; the §6.4 path suite: relative-inside → Ok; `..` escape, symlink-out, parent-symlink, Windows junction, case-folded escape on case-insensitive filesystems, export output outside → PathOutsideWorkspace; any absolute path → InvalidPath; the empty path (the root itself) → Ok for list_tree and watch_folder and InvalidPath for every other command. Capability file contains no `**` scope. A test asserts the plugin registration order in main.rs has tauri-plugin-fs before tauri-plugin-persisted-scope.",
  "dependencies": [
   "2.1h"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.3",
  "model": "sonnet",
  "description": "Rust commands new_file (`Untitled-N.md` with `# Untitled-N`), rename_file (doc + sidecar + assets/<stem> together, rewriting every relative image URL in the document that points into assets/<oldstem>/ to assets/<newstem>/, with rollback of all three on partial failure), delete_to_trash (trashes doc + sidecar + assets/<stem> together after a confirm dialog listing the three) behind a `TrashBackend` trait (real impl uses the `trash` crate; tests inject a recording fake), reveal_in_folder.",
  "acceptance": "cargo test: rename with sidecar and assets present renames all three and the document's image URLs now reference assets/<newstem>/ (asserted on the re-read Markdown); rename where the target exists returns AlreadyExists and changes nothing; a simulated failure moving the assets dir rolls back the doc and sidecar names; delete_to_trash calls the fake once per existing item (doc, sidecar, assets dir).",
  "dependencies": [
   "2.2"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.4",
  "model": "sonnet",
  "description": "File tree sidebar (left): open folder, new file, rename (F2 / context menu), delete to trash, click to open, cloudOnly files shown greyed with a tooltip 'Not downloaded on this device'; last folder and file restored on launch.",
  "acceptance": "WebdriverIO: create → rename a.md→b.md with sidecar → both renamed, assets/a → assets/b; relaunch restores the same file; clicking a cloudOnly entry shows the tooltip and does not error.",
  "dependencies": [
   "2.3"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.5",
  "model": "opus",
  "description": "Autosave + external-change handling: debounce 500 ms, save canonical Markdown + refreshed sidecar; notify watcher emits fs:changed; changed file open and not dirty → silent reload; dirty → banner 'Changed on disk' with Reload / Keep mine; partial writes from sync tools (file shrinks then grows within 2 s) are debounced before reload; first-open non-canonical banner per §6.1.",
  "acceptance": "WebdriverIO: edit, wait 600 ms, file on disk is canonical; external append while not dirty → visible within 1 s; while dirty → banner, Keep mine overwrites, Reload discards; truncate-then-rewrite externally within 1 s triggers one reload, not two; opening the CRLF fixture shows the banner once and saving yields its .canonical.md byte-for-byte.",
  "dependencies": [
   "2.2"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.6",
  "model": "sonnet",
  "description": "Images: paste/drag → save_image → `![](assets/<docstem>/<file>)`; rendered view resolves relative paths against the document directory and renders through convertFileSrc; broken paths render a labelled placeholder.",
  "acceptance": "WebdriverIO: paste a 1×1 PNG → file exists, Markdown has the relative path, <img> naturalWidth 1 inside the real webview; open a doc referencing missing.png → placeholder 'Image not found: missing.png'; a doc in a sub-folder with `../shared/img.png` renders; two pastes within the same second produce two distinct files.",
  "dependencies": [
   "2.2"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.7",
  "model": "sonnet",
  "description": "Settings: JSON in the platform config dir, schema {theme:'system', typewriterScroll:false, coach:{provider:null, baseUrl:'', model:''}} (no key field), zod-validated with defaults, get_settings/set_settings, minimal dialog (Cmd/Ctrl+,); `set_coach_key`/`has_coach_key`/`clear_coach_key` commands per §6.4 backed by the `keyring` crate with an in-memory backend under test and `ESSAYDOWN_COACH_KEY` env override; when the OS store is locked or unavailable, `has_coach_key` reports the status and the settings dialog shows 'Credential store unavailable — set ESSAYDOWN_COACH_KEY or unlock your keychain'.",
  "acceptance": "Corrupt settings file → defaults + one logged warning; typewriterScroll persists across relaunch; after set_coach_key the settings JSON on disk contains no key material (grep for the value returns nothing), has_coach_key returns {present:true, backend:'available'} and no IPC response ever contains the value (asserted on the recorded IPC log); env var wins over the store; a backend returning an error yields backend:'unavailable'. Does NOT render any coach UI.",
  "dependencies": [
   "2.2"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.8",
  "model": "sonnet",
  "description": "Scripted 10-minute robustness session (e2e/shell/robustness.spec.ts) against a folder containing `.icloud` placeholders and a Syncthing `.stfolder`, exercising open/edit/rename/external-change/image paste in a loop, with the webview console and Rust log captured.",
  "acceptance": "Spec passes in the container with zero unhandled promise rejections and zero Rust panics in the captured logs.",
  "dependencies": [
   "2.4",
   "2.5",
   "2.6",
   "2.7"
  ],
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.9",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE: Michael installs the macOS debug build fetched by the 2.verify CI gate (/logs/ci/2.verify/accepted/EssayDown-debug-<sha>.dmg, SHA named in .evidence/human/2.9/a<n>.md (accepted.json on ACCEPT)), opens his real synced folder on the MacBook Pro, edits a document, edits the same document in Typora, and confirms both directions survive.",
  "acceptance": "scripts/gate.sh 2.9 records the artifact SHA and the result in .evidence/human/2.9/a<n>.md (accepted.json on ACCEPT).",
  "dependencies": [
   "2.verifyh"
  ],
  "blockedOnHuman": true,
  "gateKind": "observation",
  "recordTarget": "006-mac-sync-smoke",
  "phase": "2",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.verify",
  "model": "sonnet",
  "description": "Phase 2 verification: freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "2.8"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/2.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "macos-debug-dmg"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + e2e/shell smoke and robustness specs green on ubuntu and windows (macOS recorded) at the implementation SHA; a macOS debug .dmg built by the workflow at that SHA is fetched to /logs/ci/2.verify/accepted/EssayDown-debug-<sha>.dmg.",
  "phase": "2",
  "execution": "loop",
  "targetBranch": "phase/2"
 },
 {
  "id": "2.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "2.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/2.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "macos-debug-dmg"
   ],
   "cleanupRef": true
  },
  "phase": "2",
  "targetBranch": "phase/2",
  "description": "CI gate for 2.verify: scripts/gate.sh 2.verifyh pushes integrated_sha of 2.verify to ci/2.verify/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"e2e-shell\",\"macos-debug-dmg\"] into /logs/ci/2.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + e2e/shell smoke and robustness specs green on ubuntu and windows (macOS recorded) at the implementation SHA; a macOS debug .dmg built by the workflow at that SHA is fetched to /logs/ci/2.verify/accepted/EssayDown-debug-<sha>.dmg.",
  "dependencies": [
   "2.verify"
  ]
 },
 {
  "id": "2.10.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 2.10 (phase 2). Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean.",
  "acceptance": "/logs/reviews/2/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "2.verifyh",
   "2.9"
  ],
  "phase": "2",
  "targetBranch": "phase/2",
  "reviewSet": "2.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean."
 },
 {
  "id": "2.10.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 2.10 (phase 2). Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean.",
  "acceptance": "/logs/reviews/2/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "2.verifyh",
   "2.9"
  ],
  "phase": "2",
  "targetBranch": "phase/2",
  "reviewSet": "2.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean."
 },
 {
  "id": "2.10.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 2.10 (phase 2). Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean.",
  "acceptance": "/logs/reviews/2/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "2.verifyh",
   "2.9"
  ],
  "phase": "2",
  "targetBranch": "phase/2",
  "reviewSet": "2.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean."
 },
 {
  "id": "2.10.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 2.10 (phase 2): copy the three reports to docs/reviews/phase-2-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-2-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 2.verify.r1 and 2.10.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "2.10.r0a",
   "2.10.r0b",
   "2.10.r0c"
  ],
  "phase": "2",
  "targetBranch": "phase/2",
  "reviewSet": "2.10",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 2. Phase checks: WorkspaceRoot path-escape suite present and green; robustness log clean."
 },
 {
  "id": "2.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/2 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; write the close intent; one ref transaction moving main and creating phase/3; then finalise .evidence/closes and the next phase base record.",
  "acceptance": "main == reconciliation_sha of the newest Phase 2 review attempt (verdict PASS); suite green on that commit; branch phase/3 exists at main with its base recorded in phases.json; .evidence/closes written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "2.10.r0d"
  ],
  "nextPhase": "3",
  "nextBranch": "phase/3",
  "phase": "2",
  "sourceBranches": [
   "phase/2"
  ],
  "targetBranch": "main"
 },
 {
  "id": "3.1",
  "model": "opus",
  "description": "Mode bar (Outline / Produce / Rewrite / Reorder, Cmd/Ctrl+1–4); store holds root, sidecar, cursor position; all mode mutations go through core pure functions and push one undo snapshot each.",
  "acceptance": "WebdriverIO: switching modes 100× never changes format(root); an injected store mutation (test-only `window.__essaydown.dispatch(moveBlock(...))`) followed by Cmd/Ctrl+Z restores the exact prior Markdown and sidecar.",
  "dependencies": [
   "2.close"
  ],
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.2",
  "model": "opus",
  "description": "Outline mode per §6.3, re-scoped by payload.verdictText in /logs/human/1.9/accepted.json (Michael's Outline verdict; copied to DECISIONS.md#002-outline-feel at the Phase 1 reconciliation): section tree with drag-to-reorder and drag-to-nest, question fields, topic question field, 'New question'.",
  "acceptance": "WebdriverIO on essay-fixture: set a question on section 3 → sidecar has it, Markdown unchanged; drag section 5 under section 2 → Markdown equals expected/essay-fixture.nested.md (created here); add 'Why now?' → new '## Why now?' heading anchored; delete the sidecar and reopen → headings with empty question fields.",
  "dependencies": [
   "3.1",
   "1.9"
  ],
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.3",
  "model": "sonnet",
  "description": "Produce mode per §6.3.",
  "acceptance": "WebdriverIO: sidebars display:none, editor width ≤ 68ch, heading 2's question text present in `.question-hint`; typewriterScroll keeps the caret within ±40 px of vertical center across 30 typed lines; Esc returns to the previous mode.",
  "dependencies": [
   "3.1"
  ],
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.4",
  "model": "opus",
  "description": "Rewrite mode per §6.3, persisting variants/history/chosen in the sidecar; sidebar auto-scrolls to the cursor's sentence; 'Unattached' section for orphans with drag-to-reattach.",
  "acceptance": "WebdriverIO on essay-fixture paragraph 4: add 2 variants to sentence 2, choose variant 2 → Markdown equals expected/essay-fixture.rewrite.md (created here) with inline emphasis preserved; Cmd/Ctrl+Z reverts sentence and `chosen`; relaunch → variants still anchored; edit elsewhere in the paragraph → still anchored; retype the sentence entirely → card in 'Unattached'; drag it onto sentence 1 → re-anchored.",
  "dependencies": [
   "3.1"
  ],
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.5",
  "model": "opus",
  "description": "Reorder mode per §6.3 with mouse and keyboard (Space pick up, arrows, Space drop).",
  "acceptance": "WebdriverIO: sentence 3 → 1 in paragraph 4 → expected/essay-fixture.reorder-sentence.md (created here) with marks intact; paragraph 2 → 4 in section 3 → expected/essay-fixture.reorder-para.md; keyboard-only section move equals the mouse result; each is one undo step, and a Reorder drag followed by Cmd/Ctrl+Z restores the exact prior Markdown and sidecar.",
  "dependencies": [
   "3.1"
  ],
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.6",
  "model": "sonnet",
  "description": "Author e2e/shell/one-workflow.spec.ts implementing §3 steps 1–6 on a fresh temp folder and fresh Untitled file (step 7 added in 4.4, step 8 in 5.2): open folder, outline 3 questions, produce 2 paragraphs under each, rewrite one sentence, reorder two sentences, toggle source and back, assert final Markdown against expected/one-workflow.md (created here).",
  "acceptance": "Spec passes in the container (Linux) against expected/one-workflow.md.",
  "dependencies": [
   "3.2",
   "3.3",
   "3.4",
   "3.5"
  ],
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.verify",
  "model": "sonnet",
  "description": "Phase 3 verification: freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "3.6"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/3.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + one-workflow steps 1–6 green on ubuntu and windows (macOS recorded) at the implementation SHA; logs in /logs/ci/3.verify/accepted/.",
  "phase": "3",
  "execution": "loop",
  "targetBranch": "phase/3"
 },
 {
  "id": "3.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "3.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/3.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell"
   ],
   "cleanupRef": true
  },
  "phase": "3",
  "targetBranch": "phase/3",
  "description": "CI gate for 3.verify: scripts/gate.sh 3.verifyh pushes integrated_sha of 3.verify to ci/3.verify/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"e2e-shell\"] into /logs/ci/3.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + one-workflow steps 1–6 green on ubuntu and windows (macOS recorded) at the implementation SHA; logs in /logs/ci/3.verify/accepted/.",
  "dependencies": [
   "3.verify"
  ]
 },
 {
  "id": "3.7.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 3.7 (phase 3). Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified.",
  "acceptance": "/logs/reviews/3/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "3.verifyh"
  ],
  "phase": "3",
  "targetBranch": "phase/3",
  "reviewSet": "3.7",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified."
 },
 {
  "id": "3.7.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 3.7 (phase 3). Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified.",
  "acceptance": "/logs/reviews/3/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "3.verifyh"
  ],
  "phase": "3",
  "targetBranch": "phase/3",
  "reviewSet": "3.7",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified."
 },
 {
  "id": "3.7.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 3.7 (phase 3). Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified.",
  "acceptance": "/logs/reviews/3/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "3.verifyh"
  ],
  "phase": "3",
  "targetBranch": "phase/3",
  "reviewSet": "3.7",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified."
 },
 {
  "id": "3.7.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 3.7 (phase 3): copy the three reports to docs/reviews/phase-3-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-3-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 3.verify.r1 and 3.7.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "3.7.r0a",
   "3.7.r0b",
   "3.7.r0c"
  ],
  "phase": "3",
  "targetBranch": "phase/3",
  "reviewSet": "3.7",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 3. Phase checks: one-workflow green on Linux and Windows (logs/ci); goldens from 3.2/3.4/3.5/3.6 hand-verified."
 },
 {
  "id": "3.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/3 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; write the close intent; one ref transaction moving main and creating phase/4; then finalise .evidence/closes and the next phase base record.",
  "acceptance": "main == reconciliation_sha of the newest Phase 3 review attempt (verdict PASS); suite green on that commit; branch phase/4 exists at main with its base recorded in phases.json; .evidence/closes written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "3.7.r0d"
  ],
  "nextPhase": "4",
  "nextBranch": "phase/4",
  "phase": "3",
  "sourceBranches": [
   "phase/3"
  ],
  "targetBranch": "main"
 },
 {
  "id": "4.0",
  "model": "sonnet",
  "description": "SPIKE prep (a normal task on task/4.0; its CI gate pushes the integrated 4.0 SHA to the immutable ref ci/4.0/a<n>): add a disposable workflow .github/workflows/spike-pdf.yml that on all three runners installs the pinned pandoc/typst, exports essay-fixture.md (with its relative images) via `pandoc -f gfm --resource-path=<docdir> --pdf-engine=typst`, and on failure tries the fallback (copy/resolve images into a temp Typst root); uploads PDFs, `pdfimages -list` output and logs as artifacts.",
  "acceptance": "spike-pdf.yml passes actionlint; the Linux leg runs in the container and uploads its artifacts to a local path.",
  "dependencies": [
   "3.close"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/4.0/a{n}",
   "workflow": "spike-pdf.yml",
   "trigger": "push",
   "artifactNames": [
    "spike-pdf"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "spike-pdf.yml ran on all three runners at the integrated 4.0 SHA on ref ci/4.0/a<n>; artifacts are in /logs/ci/4.0/accepted/; run.json records the expanded ref and it was deleted after fetch.",
  "phase": "4",
  "execution": "loop",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.0h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "4.0",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/4.0/a{n}",
   "workflow": "spike-pdf.yml",
   "trigger": "push",
   "artifactNames": [
    "spike-pdf"
   ],
   "cleanupRef": true
  },
  "phase": "4",
  "targetBranch": "phase/4",
  "description": "CI gate for 4.0: scripts/gate.sh 4.0h pushes integrated_sha of 4.0 to ci/4.0/a{n}, writes run.json, waits for spike-pdf.yml, fetches [\"spike-pdf\"] into /logs/ci/4.0/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "spike-pdf.yml ran on all three runners at the integrated 4.0 SHA on ref ci/4.0/a<n>; artifacts are in /logs/ci/4.0/accepted/; run.json records the expanded ref and it was deleted after fetch.",
  "dependencies": [
   "4.0"
  ]
 },
 {
  "id": "4.0r",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "SPIKE decision: read /logs/ci/4.0/accepted/ and write docs/DECISIONS.md#004-pdf-pipeline naming the pipeline that embedded both images on each OS; delete .github/workflows/spike-pdf.yml from the phase branch in the same commit.",
  "acceptance": "DECISIONS.md#004 states the pipeline per OS with the `pdfimages -list` lines pasted; spike-pdf.yml absent; no other product file changed.",
  "dependencies": [
   "4.0h"
  ],
  "phase": "4",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.1",
  "model": "sonnet",
  "description": "scripts/fetch-sidecars.ts reading scripts/sidecars.lock.json (exact pandoc and typst versions, per-platform URLs and SHA-256s, committed by this task): download and verify 8 archives — pandoc (linux x86_64 tarball, windows zip, macOS .pkg arm64, macOS .pkg x86_64 with payload extraction) and typst (linux, windows, macOS arm64, macOS x86_64) — extracting 8 pre-lipo artifacts: `pandoc-x86_64-unknown-linux-gnu`, `pandoc-x86_64-pc-windows-msvc.exe`, `typst-…` likewise, plus four macOS slices under scripts/.cache/sidecars/ (gitignored; never under src-tauri/binaries/).",
  "acceptance": "`pnpm fetch-sidecars` produces exactly 8 verified artifacts with matching checksums (4 platform binaries + 4 macOS slices); `pandoc --version` and `typst --version` run for the host OS's binary or slice; a tampered checksum aborts with a non-zero exit.",
  "dependencies": [
   "4.0r"
  ],
  "phase": "4",
  "execution": "loop",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.2",
  "model": "sonnet",
  "description": "macOS universal sidecars: `lipo -create` the two pandoc slices and the two typst slices into `pandoc-universal-apple-darwin` and `typst-universal-apple-darwin`; place all 6 target-suffixed files in src-tauri/binaries/ (slices stay in scripts/.cache/, gitignored, outside the distributables); configure exactly two externalBin entries, `binaries/pandoc` and `binaries/typst` (Tauri picks the file with the current target-triple suffix); scope `shell:allow-spawn` to those two sidecars with validated argument shapes (`-f gfm`, `--standalone`, `--resource-path=<inside workspace>`, `-o <inside workspace>`, `--pdf-engine=typst`, `-t <format>`), nothing else.",
  "acceptance": "Six distributables present with exactly two externalBin base entries; the cargo shell-scope test rejects `--lua-filter` and any path outside the workspace; `pnpm tauri build` succeeds in the container and the Linux app contains the Linux binaries; no `slices` directory under src-tauri/binaries.",
  "dependencies": [
   "4.1"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/4.2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "tauri-build"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "`pnpm tauri build` on macos-latest and windows-latest includes the platform binaries; on macOS `lipo -info` reports both architectures for both universal sidecars and `pandoc --version` runs from inside the built .app; on Windows `pandoc --version` runs from inside the built app (workflow log in /logs/ci/4.2/accepted/).",
  "phase": "4",
  "execution": "loop",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.2h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "4.2",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/4.2/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "tauri-build"
   ],
   "cleanupRef": true
  },
  "phase": "4",
  "targetBranch": "phase/4",
  "description": "CI gate for 4.2: scripts/gate.sh 4.2h pushes integrated_sha of 4.2 to ci/4.2/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"tauri-build\"] into /logs/ci/4.2/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "`pnpm tauri build` on macos-latest and windows-latest includes the platform binaries; on macOS `lipo -info` reports both architectures for both universal sidecars and `pandoc --version` runs from inside the built .app; on Windows `pandoc --version` runs from inside the built app (workflow log in /logs/ci/4.2/accepted/).",
  "dependencies": [
   "4.2"
  ]
 },
 {
  "id": "4.3",
  "model": "sonnet",
  "description": "packages/export argument builders + Rust `export` command + File → Export menu (DOCX / HTML / Other pandoc format string): `-f gfm --standalone --resource-path=<docdir>`, stderr streamed to a progress toast, reveal_in_folder on success.",
  "acceptance": "Integration test exports essay-fixture: DOCX validated with pinned tooling only (`unzip -t` from versions.env + `fast-xml-parser` pinned in packages/export/package.json): the ZIP is readable, `word/document.xml`, `word/styles.xml` and `[Content_Types].xml` exist, exactly 12 paragraphs carry Heading styles, two `w:tbl` elements exist, and `word/media/` holds 2 images; HTML passes html-validate with the committed config; a doc with a missing image exports with a warning toast, not a failure.",
  "dependencies": [
   "4.2h"
  ],
  "phase": "4",
  "execution": "loop",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.4",
  "model": "sonnet",
  "description": "PDF (per DECISIONS #004 pipeline) and EPUB export; append §3 step 7 to e2e/shell/one-workflow.spec.ts.",
  "acceptance": "In the container: PDF has ≥ 3 pages, 2 embedded images (`pdfimages -list`), and `pdftotext` output contains every one of the 12 heading titles, the code-block text, and one cell from each of the 2 tables; EPUB passes epubcheck with 0 errors and its XHTML contains the 12 headings; HTML passes html-validate and contains the 12 headings and both `<table>` elements; one-workflow steps 1–7 pass under xvfb.",
  "dependencies": [
   "4.3"
  ],
  "phase": "4",
  "execution": "loop",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.verify",
  "model": "sonnet",
  "description": "Phase 4 verification: freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "4.4"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/4.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "export-validation"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + one-workflow steps 1–7 green on ubuntu and windows (macOS recorded) at the implementation SHA; export validators (pdfimages, pdftotext, epubcheck, html-validate) green on all three; logs in /logs/ci/4.verify/accepted/.",
  "phase": "4",
  "execution": "loop",
  "targetBranch": "phase/4"
 },
 {
  "id": "4.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "4.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/4.verify/a{n}",
   "workflow": "ci.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "export-validation"
   ],
   "cleanupRef": true
  },
  "phase": "4",
  "targetBranch": "phase/4",
  "description": "CI gate for 4.verify: scripts/gate.sh 4.verifyh pushes integrated_sha of 4.verify to ci/4.verify/a{n}, writes run.json, waits for ci.yml, fetches [\"test-logs\",\"e2e-shell\",\"export-validation\"] into /logs/ci/4.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + one-workflow steps 1–7 green on ubuntu and windows (macOS recorded) at the implementation SHA; export validators (pdfimages, pdftotext, epubcheck, html-validate) green on all three; logs in /logs/ci/4.verify/accepted/.",
  "dependencies": [
   "4.verify"
  ]
 },
 {
  "id": "4.5.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 4.5 (phase 4). Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed).",
  "acceptance": "/logs/reviews/4/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "4.verifyh"
  ],
  "phase": "4",
  "targetBranch": "phase/4",
  "reviewSet": "4.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed)."
 },
 {
  "id": "4.5.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 4.5 (phase 4). Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed).",
  "acceptance": "/logs/reviews/4/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "4.verifyh"
  ],
  "phase": "4",
  "targetBranch": "phase/4",
  "reviewSet": "4.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed)."
 },
 {
  "id": "4.5.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 4.5 (phase 4). Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed).",
  "acceptance": "/logs/reviews/4/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "4.verifyh"
  ],
  "phase": "4",
  "targetBranch": "phase/4",
  "reviewSet": "4.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed)."
 },
 {
  "id": "4.5.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 4.5 (phase 4): copy the three reports to docs/reviews/phase-4-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-4-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 4.verify.r1 and 4.5.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "4.5.r0a",
   "4.5.r0b",
   "4.5.r0c"
  ],
  "phase": "4",
  "targetBranch": "phase/4",
  "reviewSet": "4.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 4. Phase checks: Claude and Sol re-run the export validators in their scratch copy; Grok verifies the captured commands, tool versions, hashes and outputs in /logs/ci/4.verify/accepted/ (structural DOCX validation stands in for an office-suite smoke test; none is installed)."
 },
 {
  "id": "4.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/4 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; write the close intent; one ref transaction moving main and creating phase/6; then finalise .evidence/closes and the next phase base record.",
  "acceptance": "main == reconciliation_sha of the newest Phase 4 review attempt (verdict PASS); suite green on that commit; branch phase/6 exists at main with its base recorded in phases.json; .evidence/closes written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "4.5.r0d"
  ],
  "targetBranch": "main",
  "sourceBranches": [
   "phase/4"
  ],
  "nextPhase": "6",
  "nextBranch": "phase/6",
  "phase": "4"
 },
 {
  "id": "5.0",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Verify, against the pinned Claude Code CLI version in docker/versions.env (`claude --version`), the exact arguments for: no tools (`--tools \"\"`), no MCP servers, and NO settings sources of any kind (user, project, local), plus stream-json partial-message streaming; record the exact argv and the minimum version in docs/DECISIONS.md#007-claude-cli-sandbox with the help-text lines pasted.",
  "acceptance": "DECISIONS.md#007 exists with the exact argv array and the `claude --help` excerpts proving each flag; a fixture `tests/fixtures/claude-cli-argv.json` contains that argv for 5.3c's tests.",
  "dependencies": [
   "2.7",
   "6.close"
  ],
  "phase": "5",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.1",
  "model": "opus",
  "description": "packages/coach: OpenAI-compatible chat-completions client (streaming SSE) via Rust `coach_complete` (reqwest; no CORS); prompt templates for 'coach this sentence' and 'coach this paragraph' taking the sentence, its paragraph, the section question, and the topic question; system prompt forbids replacement text and requires 2–4 questions.",
  "acceptance": "Unit tests with a mock server: tokens stream in order; 401 → 'Coach endpoint rejected the key'; ECONNREFUSED → 'Coach endpoint unreachable at <url>'; prompt template snapshots.",
  "dependencies": [
   "5.0"
  ],
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.2",
  "model": "sonnet",
  "description": "Coach UI: 'Ask the coach' on each Rewrite sentence card and paragraph header ONLY when settings.coach.provider !== null; streamed answers under the card; pin to sidecar coach[]; settings dialog gains Base URL / Model / API key and 'Test connection' (GET /v1/models). Append §3 step 8 to one-workflow behind a mock endpoint.",
  "acceptance": "WebdriverIO: with provider null no element with data-testid^=coach- exists anywhere and the proxy log shows zero outbound connections during a 5-minute scripted session; with the mock endpoint the button appears, streams 3 questions, pinning persists across relaunch; Test connection shows the model name.",
  "dependencies": [
   "5.1"
  ],
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.3a",
  "model": "opus",
  "description": "Question-only enforcement filter: keep only lines ending in '?', drop any line with Dice ≥ 0.6 to the user's sentence, re-ask once with a stricter prompt if < 2 survive, then show 'The coach couldn't form a question here.' Ship scripts/coach-eval.ts which runs 20 essay-fixture sentences against a configured endpoint and prints a pass-rate table.",
  "acceptance": "15 filter unit cases pass (including a disguised rewrite phrased as a question, 'Would you say: <rewrite>?', which is dropped); coach-eval runs against the mock server and prints the table.",
  "dependencies": [
   "5.2"
  ],
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.3b",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE: Michael runs scripts/coach-eval.ts against Ollama on the MacBook Pro with a ~8B and a 70B+ open-weight model and pastes the tables plus notes on any leaked rewrites into `scripts/gate.sh 5.3b` (run 1; evidence file only, copied to DECISIONS.md#003-coach-prompts at reconciliation). Pass threshold per tier: ≥ 90% two-question yield and 0% leaks.",
  "acceptance": "scripts/gate.sh 5.3b wrote .evidence/human/5.3b/a<n>.md (accepted.json on ACCEPT) and it validates (the next reconciliation copies it into DECISIONS.md).",
  "dependencies": [
   "5.3a"
  ],
  "blockedOnHuman": true,
  "gateKind": "observation",
  "recordTarget": "003-coach-prompts",
  "phase": "5",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.3d",
  "model": "opus",
  "description": "Conditional prompt tuning: read /logs/human/5.3b/accepted.json and its payload.tierResults; if run 1 passed both tiers, write 'no tuning needed' to docs/coach-eval/run-1.md and pass; otherwise revise the prompt templates (one round) targeting the failing tier, with the filter unit tests still green.",
  "acceptance": "Either docs/coach-eval/run-1.md says 'no tuning needed', or the prompt template snapshot changed and the 15 filter cases still pass.",
  "dependencies": [
   "5.3b"
  ],
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.3e",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE: if tuning happened, Michael re-runs coach-eval (run 2) and pastes the tables into `scripts/gate.sh 5.3e`; then records the outcome (evidence file only): both tiers pass / one tier passes (other model listed as unsupported in settings) / neither passes and v1 ships with the coach disabled ('Coach unavailable in this build'). If no tuning happened, he records the outcome from run 1.",
  "acceptance": "scripts/gate.sh 5.3e wrote .evidence/human/5.3e/a<n>.md (accepted.json on ACCEPT) and it validates (the next reconciliation copies it into DECISIONS.md).",
  "dependencies": [
   "5.3d"
  ],
  "blockedOnHuman": true,
  "gateKind": "observation",
  "recordTarget": "003-coach-prompts",
  "phase": "5",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.3c",
  "model": "sonnet",
  "description": "`claude-cli` CoachProvider, CONTEXT-ONLY: settings gain provider {kind:'openai-compatible'|'claude-cli'} and, for claude-cli, `cliPath` (auto-detected via `which claude`) and `contextFiles` (user-selected files from the writing-room repo, e.g. its CLAUDE.md and chosen skill .md files). Per request the app reads the selected files itself and inlines their contents into the system prompt, creates an EMPTY temp dir, and spawns the CLI with exactly the argv recorded in DECISIONS.md#007 (from 5.0: headless stream-json with partial messages, no tools, no MCP servers, no settings sources of any kind) with cwd = that temp dir, so no project or user hooks, skills, MCP servers or slash commands can load; text deltas come from `content_block_delta` events; the 5.3a filter applies; the temp dir is deleted after the call. Full agentic writing-room access is explicitly out of scope for v1.",
  "acceptance": "Unit tests with a fake `claude` script on PATH emitting recorded stream-json: deltas stream in order; fragmented and malformed lines handled as before; the fake asserts it received exactly the argv in tests/fixtures/claude-cli-argv.json and that cwd is a temp dir containing only the selected context files; an integration test runs with HOME pointed at a fixture home containing a malicious user-level `~/.claude/settings.json` hook, an MCP config and a plugin, plus a fixture writing-room repo with a project-level hook and skill, and asserts after a coach call that no file outside the temp dir was created or modified (directory hashes before/after for HOME and the repo), no child process other than `claude` was spawned (process-tree capture), and the temp dir is gone; exit code 1 with 'Not logged in' → mapped error; version below the pin → 'Claude Code CLI too old'.",
  "dependencies": [
   "5.3a",
   "5.0"
  ],
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "post.1",
  "model": "opus",
  "description": "Retrospective proposal: read docs/lessons.md, docs/reviews/*, docs/progress/journal-*.md and the per-task metrics; write docs/RETRO.md with the §10.4 metrics table and a proposed diff to BUILD-DEFAULTS.md (new defaults, removed defaults, changed thresholds), one line of evidence per change, every hunk carrying a stable id `H-<n>`. The reviewer table tabulates unique findings per reviewer per phase and the severity each reviewer assigned to findings two or more raised (from the reviewer citations in every `DECISIONS.md#review-N-r<k>` entry and the reports they cite), never verdicts per reviewer: RUNNER-SPEC §5.4 has no vote, so a verdict was never the signal (DECISIONS #review-1-r0, Michael's rule).",
  "acceptance": "docs/RETRO.md exists with the metrics table and a unified diff block against BUILD-DEFAULTS.md in which every hunk has an `Evidence:` line.",
  "dependencies": [
   "5.3e",
   "5.3c"
  ],
  "targetBranch": "phase/5",
  "phase": "5",
  "execution": "loop"
 },
 {
  "id": "post.2",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE: Michael accepts or rejects each hunk of the RETRO.md diff by id via `scripts/gate.sh post.2` (observation gate: the record is complete when every H-<n> has an ACCEPT or REJECT decision in the payload — per-hunk REJECTs do not reject the gate; the Phase 5 reconciliation copies it to DECISIONS.md#009-retro-approval).",
  "acceptance": "scripts/gate.sh post.2 wrote .evidence/human/post.2/a<n>.md (accepted.json on ACCEPT) with exactly one decision per H-<n> in RETRO.md and it validates.",
  "dependencies": [
   "post.1"
  ],
  "blockedOnHuman": true,
  "targetBranch": "phase/5",
  "gateKind": "observation",
  "recordTarget": "009-retro-approval",
  "phase": "5"
 },
 {
  "id": "post.3",
  "model": "sonnet",
  "execution": "loop-external",
  "description": "(loop-external per RUNNER-SPEC §7; externalRepoId build-defaults at /work/.ext/build-defaults, target refs/heads/main, verify `node scripts/validate-defaults.mjs`; its journal entry lives in /logs/external/build-defaults/) reads payload.hunkDecisions from /logs/human/post.2/accepted.json and Apply the accepted hunks to the local clone of `MLSavage/build-defaults` under /work/.ext/build-defaults (cloned by Michael onto the bind mount), bump its version and retro log, commit there.",
  "acceptance": "The build-defaults clone has a new local commit whose diff equals the ACCEPT hunks; version and retro-log lines updated.",
  "dependencies": [
   "post.2"
  ],
  "targetBranch": "phase/5",
  "externalRepoId": "build-defaults",
  "externalRepoPath": "/work/.ext/build-defaults",
  "externalTargetRef": "refs/heads/main",
  "externalVerify": "node scripts/validate-defaults.mjs",
  "phase": "5"
 },
 {
  "id": "post.4",
  "model": "sonnet",
  "description": "(on phase/5) Copy the approved BUILD-DEFAULTS.md from the build-defaults clone into this repo and commit.",
  "acceptance": "This repo's BUILD-DEFAULTS.md is byte-identical to the build-defaults commit.",
  "dependencies": [
   "post.3"
  ],
  "targetBranch": "phase/5",
  "phase": "5",
  "execution": "loop"
 },
 {
  "id": "5.version",
  "model": "sonnet",
  "description": "Set all version fields to 0.2.0 with the equality test; update docs/RELEASE-NOTES-v0.2.0.md draft from Phase 5 DECISIONS entries (coach + retrospective).",
  "acceptance": "Version test asserts 0.2.0 everywhere; RELEASE-NOTES-v0.2.0 draft exists.",
  "dependencies": [
   "post.4"
  ],
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.verify",
  "model": "sonnet",
  "description": "Phase 5 verification: freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs.",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "5.version"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/5.verify/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + one-workflow steps 1–8 (step 8 via the mock coach endpoint) green on all three runners at the integrated 5.verify SHA (= implementation_sha); release-candidate.yml built the 4 installers + SHA256SUMS.txt at that SHA with version 0.2.0 (final-package gate; artifacts in /logs/ci/5.verify/accepted/).",
  "phase": "5",
  "execution": "loop",
  "targetBranch": "phase/5"
 },
 {
  "id": "5.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "5.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/5.verify/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "phase": "5",
  "targetBranch": "phase/5",
  "description": "CI gate for 5.verify: scripts/gate.sh 5.verifyh pushes integrated_sha of 5.verify to ci/5.verify/a{n}, writes run.json, waits for release-candidate.yml, fetches [\"test-logs\",\"e2e-shell\",\"installers\",\"SHA256SUMS.txt\"] into /logs/ci/5.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + one-workflow steps 1–8 (step 8 via the mock coach endpoint) green on all three runners at the integrated 5.verify SHA (= implementation_sha); release-candidate.yml built the 4 installers + SHA256SUMS.txt at that SHA with version 0.2.0 (final-package gate; artifacts in /logs/ci/5.verify/accepted/).",
  "dependencies": [
   "5.verify"
  ]
 },
 {
  "id": "5.4.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 5.4 (phase 5). Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9).",
  "acceptance": "/logs/reviews/5/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "5.verifyh"
  ],
  "phase": "5",
  "targetBranch": "phase/5",
  "reviewSet": "5.4",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9)."
 },
 {
  "id": "5.4.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 5.4 (phase 5). Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9).",
  "acceptance": "/logs/reviews/5/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "5.verifyh"
  ],
  "phase": "5",
  "targetBranch": "phase/5",
  "reviewSet": "5.4",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9)."
 },
 {
  "id": "5.4.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 5.4 (phase 5). Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9).",
  "acceptance": "/logs/reviews/5/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "5.verifyh"
  ],
  "phase": "5",
  "targetBranch": "phase/5",
  "reviewSet": "5.4",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9)."
 },
 {
  "id": "5.4.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 5.4 (phase 5): copy the three reports to docs/reviews/phase-5-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-5-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 5.verify.r1 and 5.4.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions); the disabled outcome is a valid pass.",
  "dependencies": [
   "5.4.r0a",
   "5.4.r0b",
   "5.4.r0c"
  ],
  "phase": "5",
  "targetBranch": "phase/5",
  "reviewSet": "5.4",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 5 (coach + retrospective, v0.2.0). Phase checks: proxy-log proof of zero network calls while provider === null; if /logs/human/5.3e/accepted.json payload.coachOutcome is 'disabled', verify the disabled state and skip the red-team; otherwise red-team 10 prompts through each enabled provider, all filtered; retrospective diff reviewed for evidence lines; RELEASE-NOTES-v0.2.0 finalised at reconciliation. The reconciliation writes docs/RETRO-CARRYFORWARD.md with Phase 5's class-level lessons (the retrospective's evidence cutoff precedes this review; RUNNER-SPEC §9)."
 },
 {
  "id": "5.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/5 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; release checks — every version field == 0.2.0, the approved product candidate (accepted attempt of the newest passing attempt's verifier gate, via its accepted.json) has sha == that attempt's verification_sha, an ancestor of the proposed commit differing only by the docs allowlist, tag object v0.2.0 created; write the close intent; one ref transaction moving main, the tag; terminal close (no nextPhase): finalise .evidence/closes/5.json only.",
  "acceptance": "main == reconciliation_sha of the newest Phase 5 review attempt (verdict PASS); suite green on that commit; all version fields == 0.2.0; tag v0.2.0 points at main; the candidate sha equals the newest passing attempt's verification_sha and is an ancestor; closes/<N>.json follows the v1 schema with tag_object_oid, tag_target_sha and refs[]; no phase record written (terminal); .evidence/closes/5.json written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "5.4.r0d"
  ],
  "targetBranch": "main",
  "sourceBranches": [
   "phase/5"
  ],
  "releaseVersion": "0.2.0",
  "phase": "5"
 },
 {
  "id": "6.1",
  "model": "sonnet",
  "description": "Release workflows + rc1: set all version fields (tauri.conf.json, Cargo.toml, package.json) to 0.1.0-rc1 with the equality test; release.yml on the final tag v* (publishes) and release-candidate.yml on push to ci/** refs (uploads installers as artifacts, publishes nothing) — both build: macOS universal .dmg, Linux .AppImage + .deb, Windows .msi with sidecars, SHA256SUMS.txt; docs/INSTALL.md with Gatekeeper (`xattr -dr com.apple.quarantine`), SmartScreen, and Arch (`fuse2` for AppImage) notes.",
  "acceptance": "Version test asserts 0.1.0-rc1 everywhere; release.yml and release-candidate.yml pass actionlint; a local `pnpm tauri build` in the container produces the Linux .AppImage and .deb and SHA256SUMS.txt verifies against them; INSTALL.md present.",
  "dependencies": [
   "4.close"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.1/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "release-candidate.yml ran at the integrated 6.1 SHA on ref ci/6.1/a<n> and produced 4 installers + SHA256SUMS.txt as artifacts (rc1); each installer installs and launches on its runner (macOS `open`, Linux AppImage exec, Windows `msiexec /qn`) and writes a launch marker file; artifacts under /logs/ci/6.1/accepted/ and accepted.json records the producer run_id, run URL, SHA and the SHA-256 of every artifact (RUNNER-SPEC §1). No tag is created.",
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.1h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "6.1",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.1/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "phase": "6",
  "targetBranch": "phase/6",
  "description": "CI gate for 6.1: scripts/gate.sh 6.1h pushes integrated_sha of 6.1 to ci/6.1/a{n}, writes run.json, waits for release-candidate.yml, fetches [\"installers\",\"SHA256SUMS.txt\"] into /logs/ci/6.1/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "release-candidate.yml ran at the integrated 6.1 SHA on ref ci/6.1/a<n> and produced 4 installers + SHA256SUMS.txt as artifacts (rc1); each installer installs and launches on its runner (macOS `open`, Linux AppImage exec, Windows `msiexec /qn`) and writes a launch marker file; artifacts under /logs/ci/6.1/accepted/ and accepted.json records the producer run_id, run URL, SHA and the SHA-256 of every artifact (RUNNER-SPEC §1). No tag is created.",
  "dependencies": [
   "6.1"
  ]
 },
 {
  "id": "6.2",
  "model": "sonnet",
  "description": "Cross-platform measurement workflow: add .github/workflows/measure.yml, triggered by push to ci/** refs, that reads `ci-inputs/measure.json` from the tree — a file THIS TASK writes and commits (gates never commit; RUNNER-SPEC §3) from /logs/ci/6.1/accepted.json: producer run_id, producer SHA, per-artifact SHA-256 — downloads the rc1 installers from that exact producer run via the GitHub API, verifies the hashes, then on all three runners installs them, runs one-workflow against the installed app, runs sentences.cases.json inside each real webview via the WDIO harness, captures per-OS screenshots of essay-fixture in rendered view, and uploads all of it plus a producer-manifest artifact. Validated locally (actionlint; the Linux leg runs in the container against artifacts copied from /logs/ci/6.1/accepted/).",
  "acceptance": "measure.yml passes actionlint; ci-inputs/measure.json is part of this task's commit and a test asserts it matches /logs/ci/6.1/accepted.json (run_id, sha, every artifact digest); the Linux leg runs in the container against the accepted rc1 artifacts and produces the artifact types; a unit test asserts the workflow fails when a hash in ci-inputs/measure.json mismatches.",
  "dependencies": [
   "6.1h"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.2/a{n}",
   "workflow": "measure.yml",
   "trigger": "push",
   "artifactNames": [
    "one-workflow-results",
    "segmenter-results",
    "screenshots",
    "producer-manifest"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "measure.yml ran on all three runners at the integrated 6.2 SHA (which already contains ci-inputs/measure.json) on ref ci/6.2/a<n>, downloaded the rc1 installers from the producer run named in ci-inputs/measure.json with matching hashes (producer-manifest artifact records run id, SHA and hashes), and its artifacts are under /logs/ci/6.2/accepted/.",
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.2h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "6.2",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.2/a{n}",
   "workflow": "measure.yml",
   "trigger": "push",
   "artifactNames": [
    "one-workflow-results",
    "segmenter-results",
    "screenshots",
    "producer-manifest"
   ],
   "cleanupRef": true
  },
  "phase": "6",
  "targetBranch": "phase/6",
  "description": "CI gate for 6.2: scripts/gate.sh 6.2h pushes integrated_sha of 6.2 to ci/6.2/a{n}, writes run.json, waits for measure.yml, fetches [\"one-workflow-results\",\"segmenter-results\",\"screenshots\",\"producer-manifest\"] into /logs/ci/6.2/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "measure.yml ran on all three runners at the integrated 6.2 SHA (which already contains ci-inputs/measure.json) on ref ci/6.2/a<n>, downloaded the rc1 installers from the producer run named in ci-inputs/measure.json with matching hashes (producer-manifest artifact records run id, SHA and hashes), and its artifacts are under /logs/ci/6.2/accepted/.",
  "dependencies": [
   "6.2"
  ]
 },
 {
  "id": "6.2m",
  "model": "sonnet",
  "execution": "replan",
  "description": "Measurement analysis (a RE-PLANNING GATE, not a fix task): from /logs/ci/6.2/accepted/ write docs/PLATFORM-NOTES.md listing every divergence (fonts, scrollbars, drag ghosts, segmenter diffs) with a proposed fix and size; treat the rc1 captures as diagnostic inputs only (kept in /logs/ci/6.2/accepted/, not committed). Emits DONE 6.2m; after integration the runner emits `REPLAN 6.2r` (RUNNER-SPEC §2).",
  "acceptance": "PLATFORM-NOTES.md exists with the divergence table; segmenter results per webview recorded; no fix was attempted in this task.",
  "dependencies": [
   "6.2h"
  ],
  "replanTarget": "6.2r",
  "phase": "6",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.2r",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Re-planning (principal engineer, interactive): convert PLATFORM-NOTES.md (from 6.2m) into explicit tasks 6.2.1 … 6.2.n appended to PRD §8 and ralph/tasks.json, each with its own acceptance criterion, each depending on 6.2r; make 6.3 depend on all of them. If there are zero divergences, record that and make 6.3 depend on 6.2r alone.",
  "acceptance": "PRD §8 and tasks.json contain the 6.2.x tasks (or the explicit zero-divergence note); `node ralph/validate-tasks.mjs` passes.",
  "dependencies": [
   "6.2m"
  ],
  "phase": "6",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.3",
  "model": "sonnet",
  "description": "Final cross-platform verification: layout parity assertions added to one-workflow (heading count, editor column width, no horizontal overflow); generate candidate per-OS baseline PNGs of essay-fixture from the post-fix build (via the CI gate, which builds the post-fix app in the same workflow run rather than consuming rc artifacts) into e2e/shell/baselines/candidate/.",
  "acceptance": "Parity assertions pass in the container; Linux candidate baseline generated locally.",
  "dependencies": [
   "6.2r"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.3/a{n}",
   "workflow": "measure.yml",
   "trigger": "push",
   "artifactNames": [
    "one-workflow-results",
    "screenshots",
    "build-manifest"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "measure.yml ran on all three runners at the integrated 6.3 SHA; parity assertions green on all three; candidate baselines for macOS and Windows fetched to /logs/ci/6.3/accepted/.",
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.3h",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "6.3",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.3/a{n}",
   "workflow": "measure.yml",
   "trigger": "push",
   "artifactNames": [
    "one-workflow-results",
    "screenshots",
    "build-manifest"
   ],
   "cleanupRef": true
  },
  "phase": "6",
  "targetBranch": "phase/6",
  "description": "CI gate for 6.3: scripts/gate.sh 6.3h pushes integrated_sha of 6.3 to ci/6.3/a{n}, writes run.json, waits for measure.yml, fetches [\"one-workflow-results\",\"screenshots\",\"build-manifest\"] into /logs/ci/6.3/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "measure.yml ran on all three runners at the integrated 6.3 SHA; parity assertions green on all three; candidate baselines for macOS and Windows fetched to /logs/ci/6.3/accepted/.",
  "dependencies": [
   "6.3"
  ]
 },
 {
  "id": "6.3v",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE (approval; outcomes ACCEPT/REJECT): Michael views the three candidate baselines from /logs/ci/6.3/accepted/ and accepts or rejects each via `scripts/gate.sh 6.3v`.",
  "acceptance": "scripts/gate.sh 6.3v wrote .evidence/human/6.3v/a<n>.md (accepted.json on ACCEPT) with ACCEPT or REJECT per OS and an overall outcome (ACCEPT only if all three accepted); on REJECT the runner records a plan-gate request (RUNNER-SPEC §2) and 6.3a stays blocked until an attempt with outcome ACCEPT exists.",
  "dependencies": [
   "6.3h"
  ],
  "blockedOnHuman": true,
  "gateKind": "approval",
  "recordTarget": "008-visual-baselines",
  "outcomes": {
   "ACCEPT": "continue",
   "REJECT": "plan-gate"
  },
  "phase": "6",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.3a",
  "model": "sonnet",
  "description": "Promote the candidate baselines whose payload.baselineDecisions entry is ACCEPT in /logs/human/6.3v/accepted.json to e2e/shell/baselines/<os>/ and add the per-OS 0.5% pixel-delta assertion to one-workflow.",
  "acceptance": "Approved PNGs committed under baselines/<os>/; Linux comparison passes in the container.",
  "dependencies": [
   "6.3v"
  ],
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.3b",
  "model": "sonnet",
  "description": "Release candidate 2: bump version fields to 0.1.0-rc2 (tauri.conf.json, Cargo.toml, package.json) and validate they agree (no tag; the CI gate packages the integrated 6.3b SHA from an immutable ci ref).",
  "acceptance": "A test asserts all version fields equal 0.1.0-rc2.",
  "dependencies": [
   "6.3a"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.3b/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "release-candidate.yml ran at the integrated 6.3b SHA on ref ci/6.3b/a<n> and produced 4 installers + SHA256SUMS.txt (version 0.1.0-rc2) as artifacts under /logs/ci/6.3b/accepted/; the run SHA equals the integrated 6.3b SHA. No tag is created.",
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.3bh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "6.3b",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.3b/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "phase": "6",
  "targetBranch": "phase/6",
  "description": "CI gate for 6.3b: scripts/gate.sh 6.3bh pushes integrated_sha of 6.3b to ci/6.3b/a{n}, writes run.json, waits for release-candidate.yml, fetches [\"installers\",\"SHA256SUMS.txt\"] into /logs/ci/6.3b/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "release-candidate.yml ran at the integrated 6.3b SHA on ref ci/6.3b/a<n> and produced 4 installers + SHA256SUMS.txt (version 0.1.0-rc2) as artifacts under /logs/ci/6.3b/accepted/; the run SHA equals the integrated 6.3b SHA. No tag is created.",
  "dependencies": [
   "6.3b"
  ]
 },
 {
  "id": "6.4",
  "model": "opus",
  "execution": "human",
  "description": "HUMAN GATE: Michael installs the rc2 build from the artifacts under /logs/ci/6.3b/accepted/ on the MacBook Pro, the Mini, the Omarchy machine, and his son's Windows machine and runs the one workflow by hand on each.",
  "acceptance": "scripts/gate.sh 6.4 wrote .evidence/human/6.4/a<n>.md (accepted.json on ACCEPT) and it validates (the next reconciliation copies it into DECISIONS.md).",
  "dependencies": [
   "6.3bh"
  ],
  "blockedOnHuman": true,
  "gateKind": "observation",
  "recordTarget": "005-v1-acceptance",
  "phase": "6",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.verify",
  "model": "sonnet",
  "description": "Phase 6 verification: freeze dependencies (no lockfile change), run the full suite in the container, (release-candidate.yml already exists from 6.1) so the CI gate is the final-package gate: installers built from the exact product tree that will be tagged (the tagged commit adds only the review documents; docs/PUBLISH.md's manual check compares the packages rebuilt from the tagged commit against the digests in this gate's accepted.json).",
  "acceptance": "pnpm lint, pnpm test, cargo test green in the container; `git diff --stat` against the previous task shows no dependency-manifest or lockfile change.",
  "dependencies": [
   "6.6"
  ],
  "needsCI": true,
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.verify/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "ciAcceptance": "Full suite + one-workflow steps 1–7 green on all three runners at the integrated 6.verify SHA (= implementation_sha); release-candidate.yml built the 4 installers + SHA256SUMS.txt at that SHA with version 0.1.0 (the final-package gate; artifacts under /logs/ci/6.verify/accepted/; the tagged commit will differ from this SHA only by review documents).",
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.verifyh",
  "model": "sonnet",
  "execution": "human",
  "blockedOnHuman": true,
  "gateKind": "ci",
  "outcomes": {
   "ACCEPT": "continue",
   "GATE-FAILED": "plan-gate"
  },
  "recordPolicy": "evidence-only",
  "gateFor": "6.verify",
  "ci": {
   "refType": "ci",
   "refTemplate": "ci/6.verify/a{n}",
   "workflow": "release-candidate.yml",
   "trigger": "push",
   "artifactNames": [
    "test-logs",
    "e2e-shell",
    "installers",
    "SHA256SUMS.txt"
   ],
   "cleanupRef": true
  },
  "phase": "6",
  "targetBranch": "phase/6",
  "description": "CI gate for 6.verify: scripts/gate.sh 6.verifyh pushes integrated_sha of 6.verify to ci/6.verify/a{n}, writes run.json, waits for release-candidate.yml, fetches [\"test-logs\",\"e2e-shell\",\"installers\",\"SHA256SUMS.txt\"] into /logs/ci/6.verify/a<n>/, asserts ciAcceptance, writes accepted.json on ACCEPT (RUNNER-SPEC §2, §3).",
  "acceptance": "Full suite + one-workflow steps 1–7 green on all three runners at the integrated 6.verify SHA (= implementation_sha); release-candidate.yml built the 4 installers + SHA256SUMS.txt at that SHA with version 0.1.0 (the final-package gate; artifacts under /logs/ci/6.verify/accepted/; the tagged commit will differ from this SHA only by review documents).",
  "dependencies": [
   "6.verify"
  ]
 },
 {
  "id": "6.5.r0a",
  "model": "claude-opus",
  "execution": "reviewer",
  "reviewer": "claude",
  "description": "Review attempt r0, claude reviewer, for review set 6.5 (phase 6). Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md.",
  "acceptance": "/logs/reviews/6/r0/claude/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "6.verifyh"
  ],
  "phase": "6",
  "targetBranch": "phase/6",
  "reviewSet": "6.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md."
 },
 {
  "id": "6.5.r0b",
  "model": "sol",
  "execution": "reviewer",
  "reviewer": "sol",
  "description": "Review attempt r0, sol reviewer, for review set 6.5 (phase 6). Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md.",
  "acceptance": "/logs/reviews/6/r0/sol/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "6.verifyh"
  ],
  "phase": "6",
  "targetBranch": "phase/6",
  "reviewSet": "6.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md."
 },
 {
  "id": "6.5.r0c",
  "model": "grok",
  "execution": "reviewer",
  "reviewer": "grok",
  "description": "Review attempt r0, grok reviewer, for review set 6.5 (phase 6). Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md.",
  "acceptance": "/logs/reviews/6/r0/grok/report.md and status.json exist and pass the docs/reviews/TEMPLATE.md check (canonical review-set acceptance, PRD §8 conventions).",
  "dependencies": [
   "6.verifyh"
  ],
  "phase": "6",
  "targetBranch": "phase/6",
  "reviewSet": "6.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md."
 },
 {
  "id": "6.5.r0d",
  "model": "opus",
  "execution": "interactive-principal",
  "description": "Reconciliation of review attempt r0 for review set 6.5 (phase 6): copy the three reports to docs/reviews/phase-6-r0-{claude,sol,grok}.md, append accepted human records by recordTarget into DECISIONS.md, write DECISIONS.md#review-6-r0 with verdict PASS|FAIL, class-level lessons, regenerated docs/progress.md; on FAIL append fix tasks, 6.verify.r1 and 6.5.r1a/b/c/d in the same planning commit (RUNNER-SPEC §5.4).",
  "acceptance": "Canonical review-set acceptance (§8 conventions).",
  "dependencies": [
   "6.5.r0a",
   "6.5.r0b",
   "6.5.r0c"
  ],
  "phase": "6",
  "targetBranch": "phase/6",
  "reviewSet": "6.5",
  "reviewAttempt": "r0",
  "reviewSetDescription": "Review set for Phase 6 (whole-release review at the implementation SHA: diff from the Phase 6 base SHA, rc2 installers from /logs/ci/6.3b/accepted/ and the final-package installers from /logs/ci/6.verify/accepted/, INSTALL.md, PLATFORM-NOTES.md, RELEASE-NOTES, Michael's #005 notes). Blockers from Michael's manual runs or any reviewer require fix tasks and a replacement review set; only nits and explicitly approved non-blocking findings enter docs/V1.1-BACKLOG.md."
 },
 {
  "id": "6.6",
  "model": "sonnet",
  "description": "Version and draft release notes: set all version fields (tauri.conf.json, Cargo.toml, package.json) to 0.1.0 with a test asserting equality; write docs/RELEASE-NOTES-v0.1.0.md as a draft from DECISIONS.md entries for Phases 0–4 and 6 so far (Phase 6 dispositions and the final backlog are added by the 6.5 reconciliation, a docs-only change).",
  "acceptance": "Version test asserts 0.1.0 everywhere; RELEASE-NOTES draft exists and lists every DECISIONS entry by id.",
  "dependencies": [
   "6.4"
  ],
  "phase": "6",
  "execution": "loop",
  "targetBranch": "phase/6"
 },
 {
  "id": "6.close",
  "model": "opus",
  "execution": "runner",
  "description": "Phase close (runner, transactional and idempotent, RUNNER-SPEC §8): preconditions — phase/6 head == reconciliation_sha of the newest attempt with verdict PASS; only docs/DECISIONS/tasks.json/EXPECTED_COUNT changed since implementation_sha; main == phases.json base_main_sha; run the full suite on the proposed commit in a detached worktree; release checks — every version field == 0.1.0, the approved product candidate (accepted attempt of the newest passing attempt's verifier gate, via its accepted.json) has sha == that attempt's verification_sha, an ancestor of the proposed commit differing only by the docs allowlist, tag object v0.1.0 created; write the close intent; one ref transaction moving main, the tag and creating phase/5 (nextPhase 5); then finalise .evidence/closes and the next phase base record.",
  "acceptance": "main == reconciliation_sha of the newest Phase 6 review attempt (verdict PASS); suite green on that commit; all version fields == 0.1.0; tag v0.1.0 points at main; the candidate sha equals the newest passing attempt's verification_sha and is an ancestor; closes/<N>.json follows the v1 schema with tag_object_oid, tag_target_sha and refs[]; branch phase/5 exists at main with its base recorded in phases.json; .evidence/closes written after the ref transaction; a re-run of the close is a no-op.",
  "dependencies": [
   "6.5.r0d"
  ],
  "targetBranch": "main",
  "sourceBranches": [
   "phase/6"
  ],
  "nextPhase": "5",
  "nextBranch": "phase/5",
  "releaseVersion": "0.1.0",
  "phase": "6"
 }
]
