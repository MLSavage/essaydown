A paragraph the author wrapped by hand, so its source holds a soft
line break in the middle and another one before the closing
sentence of the block.

> A quoted paragraph wrapped the same way, so the blockquote
> carries a soft line break of its own.

- A list item wrapped over two source lines, so the item's
  paragraph holds a soft line break like the ones above.
- A second item on a single line, as the absence case.

A line that ends in a hard break,\
   an indented continuation line whose leading spaces the parser drops.
