# Phase 1 review — Grok

Reviewer: Grok 4.6. Inputs: docs/PRD.md §7/§8 phase 1, §9, docs/RUNNER-SPEC.md, docs/lessons.md, docs/DECISIONS.md (#025, #review-1-r8 N1–N8, #039, #002-outline-feel, #review-1-r7 M1–M11, #review-1-r6 L1–L11, #019–#038), docs/V1.1-BACKLOG.md, `git diff 052e206aa8314ee1e4e454668a0496b3b96f4128...287c884e316cd8e39efc3ee0c0954b2b9430a24f` (attempt r9), `git diff 970bb5cd060e5cf226b5fa8fa51ce2113d4b62d6...287c884e316cd8e39efc3ee0c0954b2b9430a24f` (r9 chain), /logs/ci/1.verify.r9.g1h/accepted/ (this attempt's verifier; verifier_id `1.verify.r9.g1`), /logs/ci/1.verify.r9.g1h/{accepted.json,a1/result.json,a1/run.json}, /logs/ci/{1.verify.g1h,1.verify.r1h,1.verify.r2.g1h,1.verify.r3.g1h,1.verify.r4.g1h,1.verify.r5h,1.verify.r6h,1.verify.r7.g2h,1.verify.r8.g1h}/accepted/, /logs/ci/1.verify.r9h/a1/ (GATE-FAILED predecessor), /logs/human/1.9/accepted.json + a2.md, /logs/tasks/, /logs/state/summary.md. Paths read under /logs/reviews/: `/logs/reviews/1/r9/{implementation_sha,phase_base_sha,verification_sha,verifier_id}` (metadata only); directory listing of `/logs/reviews/1/r9/` (names only: `claude/`, `grok/`, `sol/`, the four metadata files); directory listing of `/logs/reviews/1/` (attempt-directory names only); `/logs/reviews/1/r8/grok/report.md` (prior attempt's own grok file); `/logs/reviews/1/r8/{claude,sol}/status.json` (verdict counts only); `/logs/reviews/1/r7/grok/report.md` (prior own grok, header). Sibling directories `/logs/reviews/1/r9/{claude,sol}/` were not opened. Cold build: cited /logs/ci/1.verify.r9.g1h/accepted/ (Grok). Commands run: `git rev-parse HEAD`; `git log --oneline` / `git diff --stat` of `052e206…287c884` and of `970bb5c…287c884`; python `digestDir` (ralph/lib/util.mjs algorithm: `${rel}\0${sha256File}\n`) over accepted `test-logs` and `playwright-report`; greps of format/positions/schema/toggle/store/DevEditor, e2e specs, lockfile, CLAUDE.md/AGENTS.md, backlog, secret patterns, skip/only/`it.fails`; read of installed `prosemirror-state@1.4.4/dist/index.js` `insertText` 627–644 and `prosemirror-model@1.25.11/dist/index.js` `ResolvedPos.marks()` 944–967 (host path `/Users/mlsavage/Developer/essaydown/node_modules/.pnpm/…`; the snapshot worktree has no `node_modules`). No build, no tests, no `pnpm why`. No browser: the 1.63–1.66 probes named in the r9 task text are discharged from the tree plus the three-OS Playwright logs, not from an independent drive. Isolated `playwright test latency.spec.ts --workers=1` was not run (no scratch copy).

## Gate table

| Criterion (from PRD §7) | Result (pass / fail / unverifiable) | Evidence (command + output line, or file:line) |
|---|---|---|
| Vite `/dev/editor` with the dev-only "Load fixture…" / "Copy Markdown" bar | pass | `apps/desktop/src/main.tsx:11` routes `/dev/editor`. `DevEditor.tsx:283-312` renders `load-fixture`, `copy-markdown`, and the 1.62 `data-testid="selection"` readout. `copyMarkdown` (`DevEditor.tsx:264-277`) flushes, then writes `format(store.getState().document.root)` and sets status `"Copied Markdown"`. |
| Michael writes ~1,000 words in the rendered view | pass (count not recorded) | `/logs/human/1.9/a2.md` body "Rendered editor"; `accepted.json` attempt 2, `outcome: ACCEPT`. Word count is still not in the record (#002, F16; backlog line for 2.9). Observed SHA `ca1cd3d` is an ancestor of `287c884`. N4's ask for a `1.9.r1` at this SHA is Michael's, recorded on `[repair-1.9 (c)]`; not re-raised. |
| Toggles to source | pass | `a2.md` "Source toggle": shortcut is Cmd+/. Playwright `editor-toggle.spec.ts` uses `ControlOrMeta+/`. `DevEditor.tsx:151-154` passes `pm.state.selection.head` and `pm.state.storedMarks` into `toSource`. workflow.log: ubuntu `102 passed (1.1m)`, macos `102 passed (2.9m)`, windows `102 passed (1.5m)`. |
| Copied Markdown round-trips (Typora) | pass | M1/M2 remain closed (per-child `widenSplitSurrogateReferences`; `breakSurvivesBeforeHtml` is the round trip). `code-span-references.test.ts` and `hard-break-before-html.test.ts` in the 4572. Playwright `editor-copy-code-span-references.spec.ts` and `editor-copy-hard-break-before-html.spec.ts` inside the 102/102 × 3 OS run. a2.md Typora check is the 1.9 record of F1, not these trees. L9 stays on the backlog, premise true. |
| Outline prototype tried; verdict recorded (DECISIONS #002) | pass | `/logs/human/1.9/accepted.json` `payload.verdictText` copied to `docs/DECISIONS.md` `#002-outline-feel`. Three re-scopes stay Phase 3 inputs. Playwright `outline-produce.spec.ts` in the 102/102 × 3 OS run. |
| Typing latency p95 with 10k words on `/dev/editor` (< 16 ms or a follow-up task at reconciliation) | pass (follow-up taken; isolated not rerun here) | Protocol: `e2e/web/latency.spec.ts` doc comment (keydown → markdown-pane mutation for rendered; keydown → `.cm-content` for source; source `commit` line, G9/1.31). Isolated `--workers=1` not rerun (no scratch). This SHA's accepted CI workflow.log, 120 presses, 2 workers: **rendered 10k pane p95 ubuntu 22.1 / macos 25.7 / windows 26.1 ms**; source 10k surface p95 1.1 / 2.2 / 1.5 ms (pane n=0 during a burst); source 10k **commit** p95 55.9 / 96.0 / 72.6 ms (n=5 bursts). The PRD alternative (“or a follow-up task”) was taken at r0 (1.17); G10 names the quiet single-worker run as the incremental-`pmToMdast` trigger's input. I do not re-open F5 as required. |
| Full suite green on 3 OSes at the implementation SHA | pass | `/logs/ci/1.verify.r9.g1h/accepted.json` `sha` = `287c884e316cd8e39efc3ee0c0954b2b9430a24f` = `/logs/reviews/1/r9/implementation_sha` = `verification_sha` = `git rev-parse HEAD`. Gate attempt **a1** (run 35968837053, ref `ci/1.verify.r9.g1/a1`). `result.json` `conclusion: success`. Vitest `4572 passed` / 45 files / **no expected fail** on ubuntu, macos, windows. cargo `0 passed; 0 failed; 0 ignored` × desktop_lib, desktop, doc-tests on all three. lint.log on all three: `eslint . && node scripts/check-deps.mjs && pnpm -r --if-present typecheck`; `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`; six `typecheck: Done`. ubuntu `ralph-test.log`: `# tests 83 / # pass 83 / # fail 0`. Prior gate `1.verify.r9h` GATE-FAILED (DECISIONS #039, essay-fixture block-edge timeout); this verifier is the first repair. |
| e2e/web Playwright on all three runners (1.verify.r9.g1 ciAcceptance) | pass | workflow.log: `102 passed` on ubuntu (1.1m), macos (2.9m), windows (1.5m). Spec-file count from the directory: **32** (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*'`), including `editor-toggle-mark-end.spec.ts` (1.63) and `editor-toggle-stripped-lead.spec.ts` (1.64), plus `tests/placeholder.spec.ts`. Merged artifact has `playwright-report-{ubuntu,macos,windows}-latest/index.html`. |
| 1.1 schema 1:1; corpus `format(pmToMdast(mdastToPM(parse(x)))) === format(parse(x))`; raw-html bytes | pass | Index keys = **65**. ubuntu test.log: 45 files, 4572 passed, no expected fail. `editor-fixed-point.test.ts` includes the `editor fixed point` typing legs and 1.61's paste-leg `expectNoForbiddenEntity`. |
| 1.2 `formatWithMap` / `nodeAt`; every paragraph startLine; 200 seeded positions; map is pure | pass | `positions-corpus.test.ts` `it.each(names)` over every index key, including the 1.65 second leg `"%s: maps every node of format(parse(·))'s own tree, and nothing is unresolved"` (`:126-133`). `grep` of `it.fails` under `packages/` empty. Inverse family `position-map-inverse.test.ts` three legs, `DESTRUCTIVE_LEG_TIMEOUT_MS` / `START_LEG_TIMEOUT_MS` = `30_000`. |
| 1.3–1.8 Playwright acceptances | pass | Inside the 102/102 × 3 OS run: 22 input scenarios, 8 reveal, live source colours, 6 undo, toggle+200 seeded, outline Produce, plus r1–r9 specs (the r8 set plus `editor-toggle-mark-end.spec.ts`, `editor-toggle-stripped-lead.spec.ts`). |
| 1.11 macOS keymap tests: both platforms, presence and absence, four chords, no skip | pass | `store.test.ts` `PLATFORMS`; `toggle.test.ts` the same shape. macos-latest vitest 4572/4572, no expected fail. `grep` of `e2e/web` + `packages` + `apps` + `tests` for `.skip(` / `.only(` / `it.fails` / `runIf` / `skipIf`: empty. |
| 1.12 Playwright in `ci.yml` on all three runners | pass | workflow.log browser-install + `Playwright (e2e/web)` on ubuntu/macos/windows; merge-logs `playwright-report` with three OS directories. |
| Fresh-clone cold build | pass (cited CI) | `/logs/ci/1.verify.r9.g1h/accepted/`: `pnpm install --frozen-lockfile` then lint / test / cargo / Playwright / ubuntu `bash ralph/test/run.sh` on ubuntu-latest, macos-latest, windows-latest at `287c884`. Artifact `test-logs` digest `16f18837fe735af47eff6ef4f5a1ca2f8e477ce86079b7d9778d365c1611ffe2` / 159280 bytes / 10 files MATCH `digestDir` of `/logs/ci/1.verify.r9.g1h/accepted/test-logs`. Artifact `playwright-report` digest `deb5c8db73186f9410219668192830e58d1c73f56754ea163d7c416b99de3e1f` / 2313105 bytes / 3 files MATCH. `accepted` holds the same trees as `a1`. |
| §9.4 verify tasks: no lockfile change; no API key in tree | pass | `git diff --stat 970bb5c..287c884` touches no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock`. Secret-pattern walk of the working tree outside `docs/` (`sk-ant-`, `ANTHROPIC_API_KEY=`, `OPENAI_API_KEY=`, `xai-`, `ghp_`, `github_pat_`, `AKIA`): empty. I9 redaction present at `docs/reviews/phase-0-r0-claude.md:26` (`<owner address>`). |
| r9 check: N1–N8 dispositions against the tree | pass | **N1 fixed** (1.63: `markEdgePoint` `toggle.ts:846-866`, guard family `toggle.test.ts:1706` mark kind × route × edge × block kind + astral + nested + inline-code control, corpus leg `toggle.test.ts:1903` with N8 and the block-start inline-code exclusion positively bounded, `byteEndOf` link branch `position-map-editor-leg.test.ts:537-576`, Playwright `editor-toggle-mark-end.spec.ts` both routes through the 1.62 readout). **N2 fixed** (1.64: `keptCharacters` `schema.ts:979-1071` is the one implementation; `stripUnparsableWhitespace` is its `.nodes` projection `:904-909`; `walkInline` `:193-221` places through `liveOf`/`liveEndOf`; whitespace guards `toggle.test.ts:2127`; `deleteAtEveryBlockStart` corpus `position-map-inverse.test.ts:493`; Playwright `editor-toggle-stripped-lead.spec.ts`). **N3 fixed** (1.65: `locateChild` `:465-483` `undefined` records `unlocatable-child`; `applyEdits` `:531-546` records a non-duplicate overlap; `formatWithMap` reports through `buildMap` `positions.ts:1054-1062`; hand-built guard `format-widening-giveup.test.ts:58`; corpus unresolved empty). **N4** recorded, Michael's call (`[repair-1.9 (c)]`). **N5** backlog `[review-1-r8 N5]`, `apps/desktop/package.json` still `react ^19.1.0`, premise true. **N6** taken at r8d (DONE-sentinel restated). **N7** backlog `[review-1-r8 N7]`, `ci.yml:187` `playwright-report` merge step still has no `if: always()`, premise true. **N8** member on `[1.53]`, 1.63's corpus exclusion 1, premise true. |
| r9 check: block-edge rule (1.63) vs `insertText` / `ResolvedPos.marks()` / `inclusive` | pass | Installed `prosemirror-state` 1.4.4 `insertText` (`dist/index.js:627-644`): `let marks = this.storedMarks; if (!marks) … marks = $from.marks()`. Installed `ResolvedPos.marks()` (`prosemirror-model` 1.25.11 `dist/index.js:944-967`): at a boundary the node before's, less `inclusive: false` marks the other side lacks. `typedMarks` (`toggle.ts:440-449`) is that expression; signature unchanged vs `970bb5c`. `inlineBoundary` (`:464-475`) and `isLeafEnd` (`:749-760`) signatures unchanged. `isLeafEnd` still returns false for `text` and still consults typed marks only for `inline_code` (the leaf's own rule, 1.52/1.60). `markEdgePoint` (`:846-866`) walks `enclosingMarks` innermost-first, stops at the first carried mark, answers the outermost uncarried mark's range end or start. Schema marks: `link` `inclusive: false` (`schema.ts:447`); `strong`/`emphasis`/`delete`/`inline_code` set no `inclusive` (default true, `:466-473`). `DevEditor.tsx:151-154` still passes `storedMarks`. |
| r9 check: live correspondence (1.64) vs every `stripUnparsableWhitespace` branch | pass | `keptCharacters` (`schema.ts:979-1071`) is the strip: line-start trim `:1004-1006` (`atLineStart && !(atBlockStart && isLinked(node)) ? dropLeadingWhitespace`); block-initial link exception the `isLinked` conjunct; line-ending run `collapseLineEndings` `:1085-1098` (first character of the run kept); empty run `:1007` `if (kept.text === "") continue`; block-end loop `:1015-1038` (trailing `hard_break` pop, whitespace-only pop, `TRAILING_WHITESPACE` of the last run, `isLinked` last-run stop); `unmarkEdgeWhitespace` run first `:990` (no characters added or removed; live offsets are its output's widths). Ownership for a dropped position stated once (`:961-970`): next kept character, else the block's end; first/middle/last of a three-character run in the whitespace guards. `walkInline` `:209-217` and `leafOffset`/`leafPosition` `:413-423` translate through the map. `blockEdge` `:777-791` reads kept content, not editor nodes. |
| r9 check: give-up instrument (1.65) vs `formatWithMap` / `applyEdits` | pass | `formatWithMap` (`positions.ts:217-243`) reads `wideningGiveUps` off this call's `State` and passes them to `buildMap`. `buildMap` (`:1054-1062`) reports an `unlocatable-child` as that child and every later sibling, an `overlapping-edit` as that child. `applyEdits` (`format.ts:531-546`) drops a duplicate of the two `SPLIT_PAIR` anchors silently (`duplicates`) and records any other overlap. Hand-built: `format-widening-giveup.test.ts:58` (unlocatable middle child, earlier sibling widened, later sibling untouched), `:137` (different-bytes overlap recorded), `:126` (duplicate silent). Corpus: `positions-corpus.test.ts:103` and `:126` `unresolved` empty over every index key. |
| r9 check: three promote bullets exactly once in CLAUDE.md and AGENTS.md | pass | `CLAUDE.md:56-58` and `AGENTS.md:56-58` identical: N1 cursor-rule bullet (discharged by `toggle.test.ts` `describe("at a block's edge the caret is outside every enclosing mark the typed marks do not carry")` and `describe("the block-edge rule over the corpus")`); N2 live-correspondence bullet (discharged by the whitespace guards and `deleteAtEveryBlockStart`); N3 give-up-instrument bullet (discharged by `format-widening-giveup.test.ts:58` and the corpus unresolved case). `grep -c` of each `DECISIONS #review-1-r8 N{1,2,3}` is 1 and 1. |
| r9 check: browser routes (Sol's r8 `see *foo*` and stripped-lead; 1.60/1.62; 1.57–1.59) | pass (from the tree + three-OS logs; no independent drive) | Sol's N1: `editor-toggle-mark-end.spec.ts` input-rule then toggle then `X` → `see *foo*X`; click then toggle → `see *fooX*`; readout `{ before: "see foo", … stored: [] }` / `stored: null`. Sol's N2: `editor-toggle-stripped-lead.spec.ts` load `` a `cd` b ``, delete first character, click between `c` and `d`, toggle, `X` → `` `cXd` b `` (strong twin the same). 1.60/1.62: `editor-toggle-code-span-end.spec.ts` still polls `{ before: "see foo", marks: ["inline_code"], stored: null }`. 1.57–1.59 copy/pipe specs remain in the 102/102 × 3 OS run. Chord-guard: `tests/no-caret-chords-in-e2e.test.ts` part of the 4572; the two new specs use `getByTestId("selection")` and no `Home`/`End` key. |
| r9 check: `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` | pass (cited CI + lockfile; `pnpm why` not run) | `grep @codemirror/commands pnpm-lock.yaml` empty. lint.log on all three OS: `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`. `packages/editor/test/no-codemirror-history.test.ts` in the 4572. `source.ts` installs no keymap (1.19). |
| r9 check: backlog revisit triggers | pass, none fired | L9 (`BACKLOG.md:85`) and L11 with M10 (`:86`, `:97`) premises hold. `[1.52, code block interior]` (`:91`) inverse-leg exclusion, trigger not fired. `[1.53]` with N8 (`:92`) still pinned, 1.63's exclusion 1; hard stop `2.verify`. `[#030 product]` (`:93`) hard stop `2.verify`. `[1.10.r7d, hard break inside a container]` (`:94`) trigger not fired as a new fixture; 1.64's whitespace guards bound the same position (`toggle.test.ts:2234-2270`) without adding a fixture. `[review-1-r7 M7]` (`:95`) `link.peek`, hard stop `2.verify`. `[1.46]` (`:82-83`, `:102`) two races, hard stop `2.verify`. `[1.59]` (`:101`) `canonicalCursor` (`toggle.ts:984`) still refines only a `text` node; hard stop `2.verify`. `[review-1-r8 N5]` (`:103`) React 19 vs PRD §4, `^19.1.0` still; `[review-1-r8 N7]` (`:104`) `ci.yml:187` still without `if: always()`. `[1.63]` (`:109`) block-start inline-code, three positions, exclusion 2 of the block-edge corpus; `[1.64]` (`:111`) block-final mark whose last child is a link, `deleteAtEveryBlockStart` exclusion; `[1.65]` (`:113`) `format.ts:297` still the only uncovered statement of that file (ubuntu coverage `format.ts` `297`). `[review-1-r9, CI timing]` (`:115`) filed after 1.66; remaining `packages/editor/test` corpus loops are strided or bounded per fixture. F11 `App.tsx:12` `invoke("greet", { name })` still present, hard stop `2.verify`. |
| r9 check: r8 class searched beyond the named reproductions | pass on the named families; remaining members are the backlog lines above | **Axes varied (static; no scratch, no generated trees):** mark kind ∈ {emphasis, strong, delete, link} × route ∈ {`null`, `[]`} × edge ∈ {start, end} × block kind ∈ {paragraph, heading, list item, blockquote, table cell} (the 1.63 matrix, `toggle.ts:1706-1807`); astral content 😀 and 𐐀 *between* run edges on both routes and both edges (`:1809-1827`); nested `*foo **bar***` with stored `[emphasis]` at the end (`:1829-1852`); inline-code as the control the enclosing-mark rule does not touch (`:1854`). Strip branches × node/container (1.64 whitespace guards: line-start, block-initial link, line-ending run, emptied run, block-end loop, trailing break, `unmarkEdgeWhitespace`); `deleteAtEveryBlockStart` over every textblock of every index key, two-view bytes beside the inverse. Give-up: unlocatable text and html children, first-child give-up, eol-as-space form located, duplicate overlap silent, different-bytes overlap recorded. **Held fixed:** no ordered-pair/triple generation over the nine installed inline kinds (text, emphasis, strong, delete, inlineCode, link, image, html, break) — no scratch; no independent browser drive; neighbour-class cells other than the 1.63 astral-between-edges members are the fixtures already in the index. **One non-BMP seed per neighbour class / inline position class:** the 1.63 matrix carries 😀 (symbol) and 𐐀 (letter) inside every mark kind; earlier fixtures (`astral-neighbour.md`, `astral-between-runs.md`) remain in the corpus legs. No unnamed block-edge disagreement: `enclosingMarks` (`toggle.ts:174`) is exactly the four `isMark` types; `inline_code` is the leaf rule (start twin is `[1.63]`); image/html/break are atoms (`isLeafEnd` `!hasTable` → true). No unnamed live-position disagreement: every strip branch is a named guard; the indented-container hard-break position is `[1.10.r7d]`, bounded in the 1.64 guards, not a new class. No unnamed quiet give-up: `format.ts` uncovered statement is `:297` (`[1.65]`). |
| Independent read of r8 N1 and N2 (DECISIONS #review-1-r8 grok row; apply that read to the r9 chain first) | — | A read-only review **could** have found **N1** at `970bb5c` without a build or a browser: `isLeafEnd` (`toggle.ts:560-571` at that SHA) returns false for `text` and consults typed marks only for `inline_code`; `toSource` then answers the innermost text's spelling-table end, inside every enclosing delimiter. Installed `insertText` (`dist/index.js:627-644`) inserts with `storedMarks ?? $from.marks()`, so after `see *foo*` the input rule's `removeStoredMark` leaves `[]` and the next character is plain — `see *foo*X` rendered, `see *fooX*` through the source. `schema.ts:447` `link` `inclusive: false` is the same branch's other edge, on both routes, with no input rule. Severity: **should-fix** (a routine toggle changes the formatting of the next character; the documents remain fixed points; not silent wrong clipboard). **N2** **could** have been found from the same tree without a build: `walkInline` (`toggle.ts:164-178` at that SHA) accumulated mdast widths from the block's content start, while `stripUnparsableWhitespace` (`schema.ts:905-940`) dropped leading whitespace the live document keeps — after one deleted lead every correspondence is off by the dropped width in both directions and `toRendered(toSource(p)) === p` holds. Severity: **should-fix**. That is the read r8 did not make (r8 named M5's inline-code family as closed and L4/L5 as closed, and stopped). **Applied to r9:** `isLeafEnd` is still the leaf-end rule for `inline_code` only (`:749-760`); the enclosing-mark answer is now `markEdgePoint` (`:846-866`), which is the rule r8 would have asked `isLeafEnd` to be. `walkInline` (`:209-217`) no longer uses mdast widths as live distances. The two remaining members of those classes that this read still finds are the ones the chain named: `[1.63]` (`isLeafEnd` has no start twin — a bare `inline_code` first on the `[]` route, three positions, corpus exclusion 2) and `[1.64]` (`markEdgePoint` returns `null` when the innermost enclosing mark is carried and the last child is a `link`, so the older end rule answers past the closing delimiter of the outer mark). Both premises hold (the corpus exclusions still disagree; the 4572 is green). No further member of either class is in `toggle.ts` at `287c884` that those two lines do not already name. |
| r7 dispositions M1–M11 verified against the tree | pass | M1 **fixed** (per-child walk). M2 **fixed** (round-trip repair). M3 **recorded** (N6; paste-leg; Linux `[1.46]`). M4 **fixed** (`insideTable`). M5 **fixed for inline code** (1.60) **and for every other mark at both edges** (N1/1.63); the start twin of the leaf rule is `[1.63]`, not a reopening of M5. M6 **fixed** (30 s); 1.66 put the block-edge corpus on the same budget. M7 **deferred**. M8 **taken**. M9 **taken**. M10 **deferred** on L11. M11 **deferred**; 1.64 bounds the position without adding a fixture. |
| r6 L1–L11, r5 K1–K9, r4 J1–J9, r3 I1–I10, r2 H1–H9, r1 G1–G12, r0 F1–F16 re-confirmed | pass | L1–L8 closed as at r8; L9/L11 deferred, premises true. L4/L5's width member was N2, now the kept-character map; the code-block sibling stays `[1.52]`. K1–K2 closed. J1 closed. J3 remaining: `[1.52]`, `[1.59]`, `[1.63]`, `[1.64]`. I1–I10 hold; I7 this gate a1 success (the r9h failure was a per-test budget, #039). H1–H6, H8 closed; H7/H9 runner/backlog. G1 `DevEditor.tsx:269` flush; G2 source undo flush; G4 click. F1 family closed for the named members; F7 no `@codemirror/commands` in the lockfile; F11 `App.tsx:12` still `invoke("greet")`. |
| Promote-lessons carried by 1.verify.r9 | pass | See the three-bullets row. 1.verify.r9.g1 did not repeat the step (task text). |

## Test counts and coverage

Vitest: 4572 passed / 0 failed / 0 expected fail across 45 files, all three OS (`/logs/ci/1.verify.r9.g1h/accepted/test-logs/test-logs-{ubuntu,macos,windows}-latest/test.log` summary lines). cargo test: 0 passed / 0 failed / 0 ignored × desktop_lib, desktop, doc-tests, all three OS (Rust surface is Phase 2). e2e: 102 passed / 0 failed on ubuntu, macos, windows (workflow.log Playwright steps); runner conformance 83/83 ubuntu only. Coverage (v8, all three OS identical): statements 99.52 % / branches 97.22 % / functions 100 % / lines 100 %. `format.ts` 99.5/99.03/100/100 (uncovered `297`, `breakSurvivesBeforeHtml`'s non-paragraph guard — `[1.65]`). `positions.ts` 100/97.59/100/100. `toggle.ts` 98.38/94.59/100/100 (uncovered includes `canonicalCursor`'s non-text fallback — `[1.59]`). `schema.ts` 100/98.64/100/100 (`722-723` `image.alt ?? ""`). Delta vs Phase 0 close (progress.md r2, 99.45 / 96.98 / 100 / 100): +0.07 / +0.24 / 0 / 0. Delta vs r8 (`1.verify.r8.g1h` 99.53 / 97.75 / 100 / 99.94): −0.01 / −0.53 / 0 / +0.06 (new branches in `toggle.ts` / `schema.ts` `keptCharacters`; `format.ts:432` and `:515` of r8 are now covered by 1.65). Isolated latency `--workers=1` not rerun. Fixture index: 65 keys. Spec files: 32.

## Findings (≤ 20, most severe first)

None.

## Three riskiest things

1. The `[1.46]` class (a native caret motion reverted by ProseMirror before the next synthetic key, and a second race: `domFromPos` vs Blink at a reveal-widget boundary, #037) has failed four gates by four routes. G4 is a click; the 1.62/1.63/1.64 specs read `data-testid="selection"`. Specs that still wait 200 ms so a chord can follow an arrow, and still poll `document.getSelection()`, remain the trigger (`BACKLOG.md:82-83, :102`).
2. `toRendered(toSource(p)) === p` does not hold inside a fenced code block (`position-map-inverse.test.ts`; `code-fence-basic.md`, `code-fence-nested.md`, `essay-fixture.md`). A toggle from inside a fence lands on the fence line. Named exclusion, hard stop `2.verify` (`BACKLOG.md:91`).
3. Three remaining cursor-map members, all named, all hard-stopped at `2.verify`: `canonicalCursor` still refines only a `text` node (`toggle.ts:984`; `[1.59]`); `isLeafEnd` has no start twin, so a bare `inline_code` run first on the `[]` route disagrees (`[1.63]`); `markEdgePoint`'s carried branch returns `null` when a block-final mark's last child is a `link`, and the older end rule answers past the outer closing delimiter (`[1.64]`). Independent of those, at the start of a punctuation-edged run the two views still take a typed letter differently (`[1.53]` / N8). Copy of the rendered form still round-trips.

## Class-level lessons (for docs/lessons.md)

- LESSON: a read-only review that stops at "isLeafEnd uses typed marks" without asking which node types that function returns false for will miss every other mark at a block edge — at `970bb5c` `isLeafEnd` returned false for `text`, so the spelling-table end was the answer for emphasis, strong, delete and link; installed `insertText` 627–644 and `schema.ts:447` `inclusive: false` are the whole of N1, with no browser and no transaction.
- LESSON: `walkInline` adding mdast widths from the block's content start while `stripUnparsableWhitespace` drops characters the live document keeps is N2 in one subtraction — the inverse over that table is the proof both directions share the error, not the proof the correspondence is right; the instrument is two-view insertion after a lead is stripped.
- LESSON: a per-test budget copied from a container timing of a whole-document per-position oracle does not transfer to GitHub's runners (8–12× here) — the oracle that survives is the block alone (`blockAlone`: `doc.cut` keeps open ancestors, one `format` per block, the document bridged by a constant shift keyed by the block's own position beside its block-local line), and the family's 30_000 ms, never a multiple of one machine's number.
