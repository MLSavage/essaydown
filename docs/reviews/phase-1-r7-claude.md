# Phase 1 review — Claude

Reviewer: Claude Opus 5 (`claude-opus-5`), review attempt r7, task 1.10.r7a. Inputs: docs/PRD.md §7/§8 phase 1 (the 1.49–1.56, 1.verify.r7[.g1|.g2], 1.10.r7a rows read from ralph/tasks.json), docs/RUNNER-SPEC.md, docs/lessons.md, docs/DECISIONS.md (#025, #review-1-r6 L1–L11, #035, #036 in the tree; #030–#034 read through `git show handoff/028:docs/DECISIONS.md` as #035 directs), docs/V1.1-BACKLOG.md, `git diff 052e206aa8314ee1e4e454668a0496b3b96f4128...56180784fe4eeb5b289be07117f9403f17c332b5` (attempt r7; the r7 chain read as `c2f6526..HEAD`), /logs/ci/1.verify.r7.g2h/{accepted.json,accepted/,a1/workflow.log}, /logs/ci/1.verify.r7h/ and /logs/ci/1.verify.r7.g1h/ (directory listings; both GATE-FAILED, no accepted/), /logs/human/1.9/accepted.json, /logs/tasks/1.54/{1.log,1.a1-capped.log,2.a2-capped-nostub.log,3.a3-capped.log} (read in full for finding 3), the result lines of /logs/tasks/{1.51,1.52,1.53,1.55,1.56}/*.log. Under /logs/reviews/ I read exactly the four metadata files of `/logs/reviews/1/r7/` (`implementation_sha`, `phase_base_sha`, `verification_sha`, `verifier_id`) and listed the attempt directory's entries by name (`ls`: each sibling held only a `transcript.log` at start); I opened no file under `grok/` or `sol/` (DECISIONS #025). Installed sources read in `/scratch/claude/node_modules`: `mdast-util-to-markdown@2.1.2` `lib/util/container-phrasing.js` (whole), `lib/util/encode-info.js`, `lib/handle/emphasis.js`, `lib/handle/text.js`, `lib/unsafe.js` (every entry without `inConstruct` enumerated by a script), `lib/util/safe.js:100–160`; `prosemirror-model`'s `ResolvedPos.marks()` as the toggle.ts doc comment restates it, checked against the editor's schema (`inline_code` has no `inclusive` field). Cold build: scratch local clone at `/scratch/claude`, HEAD `5618078`, `pnpm install --frozen-lockfile` (699 packages, lockfile unchanged), `git status --porcelain` empty at exit. Commands run: `pnpm install --frozen-lockfile`; `pnpm lint`; `pnpm test` (twice); `cargo test`; `node scripts/check-deps.mjs`; `pnpm why @codemirror/commands`; `e2e/web/node_modules/.bin/playwright install chromium`; `playwright test --workers=1` (the whole e2e/web directory, then `latency.spec.ts` alone); `bash ralph/check-agent-rules.sh`; the artifact digests recomputed with `ralph/lib/util.mjs`'s `digestDir`; eight throwaway vitest files under `packages/{core,editor}/test/zz-probe*.test.ts` and one throwaway Playwright spec `e2e/web/zz-review-probe.spec.ts` (a 13,824-seed pairwise adjacency sweep over nine inline node kinds, a 25-seed handler-delimiter sweep, three literal-reference probes, a block-end two-view probe, and seven browser routes on `/dev/editor`), all deleted before exit.

## Gate table

| Criterion (from PRD §7) | Result (pass / fail / unverifiable) | Evidence (command + output line, or file:line) |
|---|---|---|
| The Vite dev route with the dev-only "load fixture / copy Markdown" bar | pass | `/dev/editor` served by `pnpm --filter @essaydown/desktop dev` (e2e/web/playwright.config.ts:14–20); the fixture-file input and Copy Markdown button exercised in seven browser routes (probe spec, all seven ran); `playwright test --workers=1` → `93 passed (1.5m)`, 26 spec files (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' \| wc -l` = 26). |
| Michael writes ~1,000 words in the rendered view and toggles to source | pass on the record / unverifiable for the count | /logs/human/1.9/accepted.json: `"outcome": "ACCEPT"`, attempt 2, 2026-09-09; the payload carries the Outline verdict only, no word count (F16, unchanged since r0). |
| The copied Markdown round-trips | **fail** | Two new families, both on the parser's own tree, both through the bar's load route, both reported "Copied Markdown". (1) A code span or inline html holding the literal bytes `&#xD83D;&#xDE00;` is copied as `&#x1F600;`: headless `format(parse("see \`&#xD83D;&#xDE00;\` here\n"))` → ``see `&#x1F600;` here\n`` (`unresolved: ["0.1"]`); browser R1/R2/R3: rendered `<code>` `textContent` `&#xD83D;&#xDE00;`, pane and `writeText` spy ``see `&#x1F600;` here\n``, status `Copied Markdown` (finding 1, packages/core/src/format.ts:203–212). (2) A hard break before inline html is written `\ `: `format(parse("alpha\\\n<b>x</b> y\n"))` → `alpha\ <b>x</b> y\n`, whose parse holds `text "alpha\\ "` and no `break` — the break is lost and a literal backslash enters the text; the two-space form `alpha  \n<b>x</b>` the same; 190 of the 196 failing seeds of a 13,824-seed pairwise sweep are this class (finding 2). |
| Michael tries the Outline prototype and records his verdict (DECISIONS #002) | pass | /logs/human/1.9/accepted.json `payload.verdictText` present (three things to settle before it leaves prototype); recorded at #002 per the r0–r6 reconciliations. |
| Phase check: typing latency p95 with 10k words on /dev/editor under the 1.17 protocol | pass on a quiet machine | Protocol: e2e/web/latency.spec.ts doc comment (agent-written 1 k / 10 k-word document, 120 keystrokes per size, p95 of pane and surface, five source bursts for `commit`). Isolated single-worker run of the spec alone: rendered 10 k words pane p95 **9.9 ms** (surface 9.6), 1 k words 1.5 ms; source `commit` line 10 k p95 **57.5 ms** (1 k: 28.8). Full single-worker run of all 26 specs: rendered 10 k pane p95 10.3 ms, `commit` 57.8 ms. CI (contended, evidence only, workflow.log): rendered 10 k pane p95 23.1 (windows) / 13.6 (macos) / 30.1 (ubuntu) ms; `commit` 77.2 / 73.5 / 83.2 ms. The 16 ms judgement from the isolated numbers: rendered pane passes; the source `commit` line (the 25× decision of #review-1-r1) is evidence only. |
| Phase check: all tests and every e2e/web spec green on 3 OSes at the implementation SHA | pass | /logs/ci/1.verify.r7.g2h/accepted.json: run 35688068381, `sha` `56180784…`, ref `ci/1.verify.r7.g2/a1`; a1/workflow.log:85 `* [new ref] 56180784… -> origin/ci/1.verify.r7.g2/a1`; digests recomputed with `digestDir`: `test-logs` `738f676a…` 125,916 bytes / 10 files, `playwright-report` `331ce1f1…` 2,199,061 bytes / 3 files — both MATCH accepted.json; three OS directories in each artifact; test.log per OS `Test Files 42 passed (42)`, `Tests 3754 passed (3754)`, no expected fail; `cargo-test.log` 0/0 ×3 per OS; workflow.log `Playwright (e2e/web)` `93 passed` on windows-latest, macos-latest, ubuntu-latest; `verification_sha == implementation_sha`, `verifier_id 1.verify.r7.g2`. |
| Phase check: fresh-clone cold build | pass, with one red first run | `pnpm install --frozen-lockfile` exit 0; `pnpm lint` exit 0 (`check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`, every typecheck Done); `pnpm test` run 1: `Tests 1 failed \| 3753 passed (3754)` — `position-map-inverse.test.ts:277` essay-fixture after deleteAtEveryBlockEnd, `Error: Test timed out in 5000ms` (5,051 ms, cold transform cache, 23.8 s total); run 2: `3754 passed (3754)` in 7.8 s; `cargo test` exit 0 (0 tests, three targets). Finding 5. |
| Phase check: zero dependencies beyond PRD §4 | pass | `check-deps` 61 + 4 allowlisted in the cold build and on all three runners (lint.log); `pnpm why @codemirror/commands` prints nothing; `git diff --stat c2f6526..HEAD -- package.json pnpm-lock.yaml packages/*/package.json apps/*/package.json Cargo.toml Cargo.lock` empty. |
| r7 checks: the r6 dispositions L1–L11 against the tree | L1, L2, L3, L5, L7 fixed; L4 and L6 fixed except one shared tuple; L8 half met; L9–L11 recorded with premises intact | L1: `grep -c 'D\[C-F\]' format.ts` = 2, the 27 pair guards + 5 position classes green, `astral-between-runs.md` in the index (62 entries), Sol's route (R4: `*a.*&#x1F600;*(b)*\n`, two `<em>`) and Grok's typed twin (R5, same bytes) green — but the third form is the cause of finding 1. L2: `grep -c resourceLink format.ts` = 5, the 13 `unsafe.js` entries without `inConstruct` are all `atBreak` (verified by script), R6: `href` `…/Foo\_bar`, copied `see [https://en.wikipedia.org/wiki/Foo\\\_bar](https://en.wikipedia.org/wiki/Foo\\_bar) end\n`, its parse's url `…/Foo\_bar`, fixed point; `autolink-literal.canonical.md` unchanged in `c2f6526..HEAD`. L3: `rewrittenEmissions` fourth candidate (positions.ts:668–702) matches container-phrasing.js:60–80 as read; e2e 66 green. L4: the `inlineCode` table (positions.ts:704–730) read against `inline-code.js`; mid-block two-view guard green (toggle.test.ts:1520) — the block-end tuple is finding 4. L5: `containerEnd` (toggle.ts:581) and the corpus inverse leg with positions from `doc.descendants`/`isTextblock` (position-map-inverse.test.ts:98–113) and the two destructive legs (:271–272); e2e 63/64 green. L6: `innermostAt` by `Mark.sameSet($pos.marks(), before.marks)` (toggle.ts:304–319), no mark-type list; e2e 67 green; finding 4 is the same class at a block end. L7: 13 `press` helpers (12 editor specs + latency's timing loop) poll `caret(page)` to differ after each press. L8: nine corpus callers of `expectNoForbiddenEntity` carry `expectTextMatchesEditorIgnoringWhitespace`; the list-item paste leg at editor-fixed-point.test.ts:1649 does not (finding 3). L9/L11: backlog lines 85–86, premises hold (L11 gains a member, finding 9). L10 recorded. |
| r7 checks: r0–r5 dispositions (F, G, H, I, J, K) re-confirmed | confirmed as recorded, one reopened | K1 (the wrapper widens whole scalars): reopened by finding 1 — the third form now widens bytes that are not a text child's. K2's promoted rule (the corpus case names which rewrites exist): the `break`-before-html branch is named at positions.ts:664–667 as "stays unresolved" and its bytes were never asserted — finding 2. J3/K2/L4/L5 (the "answers some start" family): `[1.52, code block interior]` recorded positively (position-map-inverse.test.ts:212–219). The rest as r6 recorded; no regression found in `c2f6526..HEAD` outside the findings below. |

## Test counts and coverage

Vitest (cold build at `5618078`): 42 files; run 1 **3753 passed / 1 failed** (a 5,000 ms timeout, finding 5), run 2 **3754 passed / 0 failed**, no expected fail. cargo test: 0/0/0 (vacuous; Rust surface is Phase 2). e2e/web: 26 spec files, **93 passed** single-worker in the container (1.5 m), and 93/93 on each of ubuntu, macos, windows at the accepted a1 run (a1 of 1.verify.r7h failed 2 and a1 of 1.verify.r7.g1h failed 1 on windows, both G4 of editor-soft-line-breaks — repaired by 1.55 and 1.56 as #035/#036 record). Coverage at `5618078`: statements 99.65 %, branches 97.85 %, functions 100 %, lines 100 %; `format.ts` 100/100/100/100, `positions.ts` 100/97.83/100/100 (uncovered branches 216, 573, 577, 718), `toggle.ts` 98.9/96.6/100/100. Delta vs main (`052e206`, from the r6 report's baseline: 21 files / 729 tests, 99.45 / 96.98 / 100 / 100): +21 files, +3,025 tests, statements +0.20 pt, branches +0.87 pt; vs r6 (`c2f6526`, 3,298 tests, 99.68 / 97.9): +3 files, +456 tests, statements −0.03 pt, branches −0.05 pt.

## Findings (≤ 20, most severe first)

Severity, rated by the consequence for the gate's criterion and never by the size of the fix (DECISIONS #review-1-r1): **blocker** — a gate criterion is not met, or wrong output reaches the user silently (example: Copy Markdown puts stale text on the clipboard and reports "Copied"); **should-fix** — a defect or a missing guard the phase should not close with, while the criterion still holds or the path is not the gate's own instrument (example: a redo chord bound under two names with no test that reads the binding table, so deleting one name stays green); **nit** — wording, citations, style, or an assertion with no behaviour behind it (example: `expect(checked).toBe(Object.keys(index).length)` where both sides are the same list). Mark a should-fix **Required** when the reconciliation should fail without it.

1. **blocker** — packages/core/src/format.ts:203–212 (`SPLIT_PAIR`, the third alternative of task 1.49), :233–260, :283–291 — the widening runs over the *joined* output of every phrasing parent, so the ASCII bytes `&#xD83D;&#xDE00;` inside an `inlineCode` or inline `html` child (verbatim, never a rewrite of the parent's) are rewritten to `&#x1F600;`: `format(parse("\`&#xD83D;&#xDE00;\`\n"))` → `` `&#x1F600;`\n `` (invariant B fails: the code's value changes; `formatWithMap` leaves `0.0` unresolved), `a <span title="&#xD83D;&#xDE00;">b</span> c` → `title="&#x1F600;"` (an `html` node, which PRD §6.1 says is opaque and byte-identical), a table cell the same; in the browser the rendered `<code>` shows the user's bytes while the pane, the clipboard and "Copied Markdown" carry the rewritten ones, for a loaded file (R1, R3) and for a typed span (R2). The first two forms (1.45) need a raw lone surrogate beside a reference and are unreachable from valid UTF-8; the third is pure ASCII, reachable from any file — the fix of L1 is what opened it. Fix: widen per child, never the join — during the original `containerPhrasing` call, hook `state.handle` for this parent's direct non-`text` children (every non-text handler's output begins with punctuation — `` ` ``, `<`, `[`, `!`, `\`, a marker — which `encode-info.js:73–80` never encodes, so it appears verbatim in the join), locate those outputs left to right with `indexOf` from a cursor as `locateEmission` does, and apply `widenSplitSurrogateReferences` only to the gaps between them (the text children's slices, the only bytes the parent rewrites); guards: a code span, an inline html node, a link title and a link destination each holding the literal `&#xD83D;&#xDE00;` (and the lone `&#xD83D;` / `&#xDE00;` twins), each byte-identical through `format(parse(·))` with the node's value unchanged and `unresolved` empty, plus the L1 pair guards unchanged; a fixture `code-span-references.md` in the index so every corpus leg meets the shape.

2. **blocker** — packages/core/src/format.ts:283–291 (`installSurrogateWidening`, the app's one hook on `containerPhrasing`) / `mdast-util-to-markdown/lib/util/container-phrasing.js:66–75` — a hard `break` before inline `html` is written as a backslash and a space: the branch replaces the *previous result's* trailing line ending with a space whatever node wrote it, and the `break` handler's `\`+LF becomes `\ `, which no parser reads as a break — `format(parse("alpha\\\n<b>x</b> y\n"))` and `format(parse("alpha  \n<b>x</b> y\n"))` both give `alpha\ <b>x</b> y\n`, whose parse holds `text "alpha\\ "`, `html`, `text "x"`, … and no `break` (invariant B fails on the parser's own tree; a literal backslash the user never wrote is now rendered text; A holds from the second pass). The same after an emphasis, a code span, a link, an image, an autolink and another html node (the 13,824-seed sweep: 190 of its 196 reds are this class; the rest are findings 9 and the recorded L11). positions.ts:664–667 names this exact branch for a `break` child as "stays unresolved" (the map) and positions.test.ts:306 exercises it — the bytes were never asserted (K2's promoted rule: the corpus case that says nothing is unresolved is the guard that names which rewrites exist; this rewrite was named and its bytes left unread). Reachable from a loaded file through the bar; silent. Fix: in the wrapper, before calling the original, handle the pair (`break`, `html`) itself: when the html child cannot start an html block — CommonMark §4.6 conditions 1–6 read from `micromark-extension-…/html-flow`'s start branches (`<script|pre|style|textarea`, `<!--`, `<?`, `<!X`, `<![CDATA[`, and the block-level tag names of condition 6); condition 7 can never interrupt a paragraph — keep the line ending (the join then reads `\`+LF+`<b>`, a fixed point that parses back to the `break`); otherwise write one space and no backslash (a documented loss, never a stray byte) and file the lossy case on the backlog; guards for both source forms of the break × `<i>`, `<span>`, `<!-- -->`, `<div>`, each asserting the bytes with `toBe`, `breaksIn(parse(out))` equal to the tree's, the fixed point and `unresolved` empty; a fixture `hard-break-before-inline-html.md` in the index (probe it against invariant B and every index-walking leg first, lesson [1.51]).

3. **should-fix, Required** — ralph/lib/integrate.mjs:189–192 (`transcriptHasDone`), docs/lessons.md:452–453 (and :441, :450), docs/progress/journal-main.md:344 — task 1.54 was integrated from an attempt that never finished: `transcriptHasDone` is a substring search over the whole JSONL transcript, the two principal lesson lines for 1.54 carry the literal `<promise>DONE 1.54</promise>` ("print `<promise>DONE 1.54</promise>`"), the retry's attempt 1 read docs/lessons.md (tool results 1–3 and 20 of /logs/tasks/1.54/1.log echo those lines), and the attempt ended `error_max_turns` at 51 with no DONE of its own: its one e2e run (tool 33) was **91 passed / 2 failed** — `editor-soft-line-breaks.spec.ts:126` G4 read offset **11** where 5 was expected, on Linux, in the container, and `editor-break-in-mark.spec.ts:163` I1 timed out in the new poll — it then edited `editor-break-in-mark.spec.ts` until that one case passed (tool 46) and was cut; the last commit on `wip/1.54` is the runner's `wip(1.54): recovery of uncommitted changes` (`b6d9e86`); the journal stub is still `Status: In progress` (the third `- [1.54] ` line), so the counts, the caller list and both mutations the acceptance says are "recorded in the journal" are recorded nowhere, and "every `expectNoForbiddenEntity` caller carries the whitespace-removed text equality" is not met: the list-item paste leg (packages/editor/test/editor-fixed-point.test.ts:1649, `pastedOut`, inside the `for (const name of names)` loop at :1627) has no `expectTextMatchesEditorIgnoringWhitespace`. The product is green at the final SHA (CI), so the criterion holds; the record is false, the runner's stop-check accepted a red attempt, and the one piece of evidence that the G4 revert reproduces on Linux under a parallel run — which contradicts #035's "the container cannot observe that runner" and #036's "the container and the other two runners happen to be past that step" — never reached the two planning commits. Fix: (a) manual procedure now and the boundary `fix(runner)`: `transcriptHasDone` parses each JSONL line and searches only `type === "assistant"` text blocks; `branchHasDone` is already commit-message-scoped; a conformance test with a transcript whose tool result echoes the promise; (b) a lesson line never carries the literal promise — write "the DONE promise" (grep docs/lessons.md for `<promise>DONE` and rewrite the three lines in the next planning commit's lessons section, not in place); (c) the reconciliation records 1.54's missing completion under 1.54, adds the paste leg's assertion in a test-only task, and adds the Linux instance (offset 11 after eleven polled lefts, /logs/tasks/1.54/1.log tool 33) to the `[1.46, found outside scope]` line so the class is no longer recorded as windows-only.

4. **should-fix** — packages/editor/src/toggle.ts:508–520 (`isLeafEnd`) and :443–448 (the `cursorMap` doc comment: "a space typed there is plain text, as it is in the editor") — at a block-final `inline_code` end the two views take a typed character differently, the L6 class at the one tuple 1.53's rule does not reach: `inline_code` has no `inclusive: false` (schema.ts:469–473), so `doc.resolve(end).marks()` is `[inline_code]` and the rendered view types into the span, while `toSource(end)` answers the column after the closing fence and the source view types outside it. Headless on `see \`foo\`\n`: rendered `see \`fooX\`\n`, source `see \`foo\`X\n`; the same in a heading, a list item and a blockquote. Browser R7: a click at the end of `foo` then `X` → clipboard ``see `fooX`\n``; the same click, `ControlOrMeta+/`, `X`, toggle back → ``see `foo`X\n``. The guard at toggle.test.ts:1542–1548 pins the column only ("keeps task 1.52's answer") with no two-view instrument, and the 1.52 (b) Playwright route agrees only because the input rule's `removeStoredMark` (input.ts:85) cleared the stored mark one keystroke earlier — the editor types by `storedMarks ?? $pos.marks()` (`Transaction.insertText`), and the map reads the doc alone. Fix: `isLeafEnd`'s block-end branch applies to atoms only (`!hasTable`); an `inlineCode` at a block end is answered by its table's last offset, before the fence, as it is mid-block; `toggle` (apps/desktop) hands `state.storedMarks` to `toSource` as an optional override of `$pos.marks()` so the post-input-rule caret still lands after the fence; the guard at :1542 becomes a two-view guard (`typedInRendered` = `typedInSource`) for both routes, and the 1.52 (b) spec adds the click route.

5. **should-fix** — packages/editor/test/position-map-inverse.test.ts:277 — the essay-fixture destructive leg has no timeout of its own and runs at 68 % of vitest's 5,000 ms default on the slowest accepted runner (3,401 ms ubuntu, 2,930 ms windows, 1,364 ms macos) and past it on the first cold run here (5,051 ms → `Test timed out in 5000ms`, the only red of the cold build; 3.1 s for the whole file on the warm second run). The gate's own instrument is one slow runner away from a transient rerun (#023's class, the third of the phase). Fix: give the two destructive legs a per-`it` timeout sized to their reach (`it(…, { timeout: 30_000 })`, or `testTimeout` in vitest.config.ts with a comment naming this leg), never a `.skip`; optionally sweep `essay-fixture.md` per textblock so no single `it` carries 970+ positions.

6. **nit** — packages/core/src/format.ts:355–359 — `link.peek` serialises the whole link (children, `safe()`, the nested `containerPhrasing`) to answer one character, on every look-ahead of every link, and the real call serialises it again (a third time on fallback); under `formatWithMap` each peek records and discards a full emission subtree. Correct (the decision is the same either way), but three serialisations per link. Fix: ask `originalPeek` first and serialise only when it answers `<`.

7. **nit** — packages/core/src/positions.ts:561–563 — the comment "a `text` child is dispatched exactly once (it has `peek`)" is false: `lib/handle/text.js` has no `peek`, so `container-phrasing.js:45–55` calls the text handler itself for the look-ahead and every text child is dispatched twice; `lastPerNode` (:881) is what makes `children[index + 1]` the next dispatched child. Fix: state the real reason.

8. **nit** — docs/V1.1-BACKLOG.md — the product backlog line DECISIONS #030 Decision 1 promises ("a user's soft break before inline html becomes a space on save … **backlog line** … hard stop `2.verify`") exists neither in the tree nor on `handoff/028` (`grep -n 'type 7\|on save\|soft break before' docs/V1.1-BACKLOG.md` empty on both); the behaviour is live (`*a*\n<b>x</b> y` → `*a* <b>x</b> y`, a text value changed) and recorded only inside a DECISIONS entry on a handoff ref. Fix: the r7 reconciliation writes the line with #030's trigger and hard stop.

9. **nit** — packages/core/src/format.ts (the built-in `emphasis` handler, upstream) — the parser's own tree for `p *a.*https://x.y/b**a.** q` (`emphasis[text "a.", emphasis[link, text "."]]`, the GFM literal having swallowed `**a`) is written `p *a.*<https://x.y/b**a>.** q`, whose parse dissolves the inner run, and the second pass writes `.\*\* q` — invariant A fails on the parser's own tree; a URL glued to a `**` run with no space, exotic. Fix: a member on the `[review-1-r6 L11]` line (attention resolution vs the serializer's escaping, upstream), same trigger and hard stop.

## Three riskiest things

1. The app's serializer corrections are string surgery on the joined output of `containerPhrasing` (`widenSplitSurrogateReferences`) and re-runs of a handler under a flipped option (the link fallback): each fix that widened the regex widened the bytes it can touch, and finding 1 is L1's fix rewriting a code span. The join is owned by upstream, its three post-handler rewrites are enumerated (positions.ts), but the app owns no per-child boundary in the join — until it does (finding 1's fix), every new form is a new class of bytes rewritten that are not the text's, and finding 2 shows a rewrite named for the map and never read for the bytes.
2. The runner integrated a task on a DONE it did not print (finding 3): the sentinel that gates integration is a substring of a transcript that echoes files, and the phase's lessons now carry three copies of it. The same attempt held the only Linux reproduction of the G4 revert; the two planning commits that followed reasoned from "windows-only". The gate's instrument (G4, four CI rounds) is placed by a click now, but the `[1.46]` class — ProseMirror reverting a native caret move before the next key — is a product question the chain has not answered, and the cold build's one red (finding 5) is the next transient rerun waiting.
3. The cursor map derives the boundary side from `$pos.marks()` but the editor types by `storedMarks ?? $pos.marks()`, and `isLeafEnd` overrides the marks at a block-final code span (finding 4): the two-view agreement 1.53 states is a doc-level rule for a state-level behaviour, and the code-block interior (`[1.52, code block interior]`, 1,074 positions on three fixtures) is still the one place a toggle lands the caret outside the block it left.

## Class-level lessons (for docs/lessons.md)

- LESSON: a regex post-processor over the *joined* output of a phrasing parent owns every child's bytes — the verbatim leaves (`inlineCode`, `html`, a link's destination and title) included — so a form added for one text-child rewrite (`&#xD83D;&#xDE00;`) rewrote a code span's literal value → a fix that post-processes serializer output is applied per child, over the slice the rewritten child occupies, and its guard set carries one member per node class whose bytes are literal (a code span, inline html, a destination, a title) holding the exact bytes the regex matches.
- LESSON: a third-party rewrite named "for the map" (`break` before `html` "stays unresolved") was enumerated by line number and never read for the bytes it writes, and the bytes destroy the node (`\`+LF → `\ `) → when an enumeration of a parent's rewrites names a branch for a node class, the same guard asserts that branch's bytes on the parser's own tree (the node count after `parse(format(·))` equal to the tree's), because a rewrite the map cannot follow is usually a rewrite the parser cannot read.
- LESSON: the runner's DONE detection is a substring search over a transcript that echoes every file the agent reads, and three principal lesson lines carry the literal promise, so a capped, red attempt integrated as done and its journal stub never completed → the sentinel is detected only in assistant text (parse the JSONL), and a lesson or journal line refers to "the DONE promise" by description, never by its bytes; a reviewer reads the integrated attempt's result line (`subtype`, `num_turns`) before trusting its journal.
- LESSON: a two-view caret rule derived from `ResolvedPos.marks()` is incomplete, because the editor inserts with `storedMarks ?? $pos.marks()` and the map reads the doc alone; a Playwright route that types right after an input rule (stored mark cleared) proves the rule for that route only → a two-view guard at a mark's end runs both routes (the rule-then-toggle and the click-then-toggle) and the toggle hands the stored marks to the map.
- LESSON: a corpus sweep that builds one tree per case (the r6 lesson) still misses the *pair* of node classes a rewrite joins (`break`, `html`); a 13,824-seed pairwise sweep over nine inline kinds found the class in one run → the reviewer's sweep enumerates ordered pairs and triples of installed inline node kinds, not neighbour classes of one kind, and reports the failing seeds by class.
