# Phase 1 review — Claude

Reviewer: Claude Opus 5 (`claude-opus-5`), reviewer task `1.10.r4a`, attempt r4. Inputs: docs/PRD.md §7 gate row / §8 Phase 1 / §9 / §10.5, docs/RUNNER-SPEC.md, docs/lessons.md, docs/DECISIONS.md (#002, #019–#024, #review-1-r0/r1/r2/r3), `git diff 052e206aa8314ee1e4e454668a0496b3b96f4128...6c0e601a49609036f02678436db4b380d340efb7` (126 files, +20,352/−77) and its r3→r4 leg `f90ed50..6c0e601` (34 files, +3,517/−94: 1.10.r3d, 1.34, 1.35, 1.36, 1.37, 1.38, 1.verify.r4, plan.1.verify.r4h.r0, 1.39, 1.verify.r4.g1), `/logs/reviews/1/r4/{phase_base_sha,verifier_id,verification_sha,implementation_sha}`, `/logs/ci/1.verify.r4.g1h/{accepted.json,accepted/}` (this attempt's verifier gate) and `/logs/ci/{1.verify.g1h,1.verify.r1h,1.verify.r2.g1h,1.verify.r3.g1h}/accepted/`, `/logs/human/1.9/accepted.json`, `/logs/tasks/{1.34,1.35,1.36,1.37,1.38,1.verify.r4,1.39,1.verify.r4.g1}/*.log`. Cold build: scratch local clone `/scratch/claude` at `6c0e601a…` (`pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm test`, `cargo test`, then `pnpm --filter @essaydown/e2e-web exec playwright install chromium` and every `e2e/web` spec). Commands run: the cold build; `pnpm why @codemirror/commands` (no output, exit 0); `node scripts/check-deps.mjs` (`61 npm + 4 cargo direct dependencies, all allowlisted`); `bash ralph/check-agent-rules.sh` (exit 0); `node ralph/generate-tasks.mjs --check` (`164 raw + 21 review-set + 26 CI gates = 211 expanded; byte-identical`); `node ralph/validate-tasks.mjs` (`OK 211`); `bash ralph/test/run.sh` (83/83); artifact digests recomputed with the runner's own `digestDir` (`ralph/lib/util.mjs:90`); `playwright test latency.spec.ts --workers=1` isolated; six throwaway vitest probe files (a 22,800-transaction random fuzz over every fixture, a **61,682-transaction exhaustive sweep of every keystroke-shaped single transaction at every inline position of every fixture** — Backspace, Delete, word-Backspace, Enter, Tab, Shift-Tab, a typed letter/space/period/asterisk/tilde with the position's marks, a two-line plain paste with the position's marks —, an 11,060-pair sweep of the same, and hand-built shapes), one throwaway Playwright spec (two cases on `/dev/editor`, `writeText` spied per 1.28), one guard mutation (a scratch spec with three chords under `e2e/web/`), one spec mutation (1.39's `ArrowLeft` count reduced by one); all deleted, `git status` clean, `coverage/` removed.

## Gate table

| Criterion (from PRD §7) | Result (pass / fail / unverifiable) | Evidence (command + output line, or file:line) |
|---|---|---|
| Vite dev route carries the dev-only "load fixture / copy Markdown" bar | pass | `apps/desktop/src/dev/DevEditor.tsx` `data-testid="fixture-file"`, `"copy-markdown"`, `"status"` (unchanged since r3, `git diff f90ed50..6c0e601 -- apps/` empty); my probe spec loaded two files through the input and read the pane and the spy |
| Michael writes ~1,000 words in the rendered view and toggles to source | pass on the record / unverifiable for the count | `/logs/human/1.9/accepted.json` `outcome ACCEPT`, `attempt 2`; payload holds `verdictText` only — no word count (r0 F16, unchanged at r1–r3) |
| **The copied Markdown round-trips** | **fail** | Finding 1, reproduced on `/dev/editor` in Chromium at `6c0e601`: load `Alpha ~~beta.~~ gamma delta`, click at the paragraph's end, eleven `ArrowLeft` (caret asserted `{text: " gamma delta", offset: 1}`), one `Backspace` → pane and `writeText` both receive `Alpha ~~beta.~~gamma delta\n`, status "Copied Markdown"; `format(parse(·))` of that is `Alpha \~\~beta.\~\~gamma delta\n` — the strikethrough is literal tildes on reparse. Headless, the same tree from the corpus's own `link-in-emphasis.md` (1.35's fixture) by one `Delete`/`Backspace` over the space after `~~…](essay.md)~~`. Every other one of the 61,682 keystroke-shaped single transactions and 11,060 pairs in my sweep is a fixed point; the r3 reproductions (I1 verse, I2 list paste, I3/I4 link edges) are all fixed at this SHA — verified by the guards, the legs, the three new specs and my own re-run of each shape |
| Michael tries the Outline prototype and records his verdict (DECISIONS #002) | pass | `/logs/human/1.9/accepted.json` `payload.verdictText`; `docs/DECISIONS.md:223` `#002-outline-feel`; `e2e/web/outline-produce.spec.ts` green here and on three runners |
| Full suite and every `e2e/web` spec green on three OSes at the implementation SHA | pass | `/logs/ci/1.verify.r4.g1h/accepted.json` `attempt 1`, `run_id 34687540740`, `ref ci/1.verify.r4.g1/a1`, `sha 6c0e601a…`; `accepted/result.json` `"conclusion": "success"`; `accepted/workflow.log` `Running 83 tests` → `83 passed` on macos-latest (10:09:20), ubuntu-latest (10:09:56), windows-latest (10:12:20); `accepted/test-logs/test-logs-<os>-latest/test.log` `Test Files 37 passed`, `Tests 2798 passed | 1 expected fail (2799)` on each OS; `cargo-test.log` `0 passed; 0 failed` ×3 on each; `lint.log` ends in `apps/desktop typecheck: Done` on each |
| Artifact digests match `accepted.json` | pass | Recomputed with `digestDir`: `test-logs` `ca4fafe860be…` 116,413 bytes / 10 files; `playwright-report` `2d476ac1fc02…` 2,020,449 bytes / 3 files — both MATCH. Three OS directories in each |
| `verification_sha == implementation_sha`, verifier recorded | pass | `/logs/reviews/1/r4/implementation_sha`, `verification_sha` both `6c0e601a49609036f02678436db4b380d340efb7`; `verifier_id` `1.verify.r4.g1`; `accepted/run.json` `sha` the same; `phase_base_sha` `052e206a…`; `git rev-parse HEAD` in the worktree the same |
| Zero dependencies beyond PRD §4 | pass | `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted` (cold build and all three `lint.log`s); `git diff --stat f90ed50..6c0e601` touches no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock`; `pnpm why @codemirror/commands` empty; `packages/editor/test/no-codemirror-history.test.ts` green |
| No API key, nothing personal, in the tree (§9 non-negotiable) | pass | `grep -rEn 'sk-ant-\|AKIA…\|ghp_…\|xoxb-\|-----BEGIN'` over the working tree (`node_modules`/`.git` excluded): only prose in prior review copies; `grep -rl '<owner address literal>'` empty (I9 redaction present at `docs/reviews/phase-0-r0-claude.md`); `.claude/` holds `settings.json` and `commands/` only |
| Phase check: typing latency p95 at 10k words, 1.17 protocol | pass on a quiet machine | Isolated `latency.spec.ts --workers=1` in the container (10 cores): **rendered 10000 words pane p95 9.8 ms** (p50 3.5, n=120/120), surface p95 9.5 ms; rendered 1000 pane p95 1.3 ms. Source surface p95 0.5 ms, `pane` `n=0 of 120` by design (G9). `commit` line (1.31): source 1000 words commit p95 18.0 ms; source 10000 words commit p50 51.5 ms, p95 56.1 ms (n=5 bursts) — evidence only. Contended run (5 workers, whole suite): rendered 10k pane p95 10.0 ms, commit p95 55.7 ms. CI runners (accepted a1, reported separately, not judged): rendered 10k pane p95 24.7 (ubuntu) / 23.9 (windows) / 12.9 ms (macos); source 10k commit p95 75.0 / 72.3 / 91.6 ms. The 16 ms judgement on the isolated number: met, by my reading |
| `CLAUDE.md ≡ AGENTS.md`, promote-lessons applied (RUNNER-SPEC §5.5) | pass | `bash ralph/check-agent-rules.sh` → `OK`; `diff <(tail -n +2 CLAUDE.md) <(tail -n +2 AGENTS.md)` empty; the journal-line sentence is in steps 2 and 4 of `## Every iteration` and the Blink-only caret rule is the last `## Code rules` bullet (`git diff --stat 04517a8..4c133e0` = `AGENTS.md` +3/−2, `CLAUDE.md` +3/−2, journal +1); the 1.verify.r4 journal names the discharging tests. `ralph/PROMPT.md` still carries the pre-1.verify.r2 steps (H7, as decided) |
| Task graph intact (RUNNER-SPEC §2, §3) | pass | `generate-tasks --check` → 211 expanded, byte-identical; `validate-tasks` OK 211; `ralph/test/run.sh` 83/83; `1.10.r4a/b/c` depend on `1.verify.r4.g1h` per #024's rewire |
| r4 check: I1–I10 dispositions verified against the tree | pass, with the class reopened | I1 → 1.34 (`schema.ts:995–1046` `giveUpEdges` break clauses; 15 positional guards + nested + seed in `editor-fixed-point.test.ts`; `deleteToEveryMarkedRunEnd` `typing-legs.ts:267`; `fixtures/markdown/hard-break-in-emphasis.md` + index entry; `editor-break-in-mark.spec.ts`; `*a\` newline `b* c` minus `b` → `*a*\` newline `c`, fixed point, break survives — re-run headless and in the browser spec). I2 → 1.36 (`schema.ts:646` `listItemSpread`, `:677` `needsBlankLine`; `list-spread.test.ts` 225 pair confirmations against `parse(format(·))`; the list-split and paste legs; `editor-list-paste.spec.ts`; Sol's two strings re-run — fixed point; one pair the enumeration lacks, finding 2). I3/I4 → 1.35 (`schema.ts:920` `linkEdgeStops`, `:871–875` block-start link, `:893` block-end link, `:1167` `outermostMark`; the eight I3/I4 strings and the two I1 strings byte-identical through `mdastToPM → pmToMdast → format` in my probe; `link-in-emphasis.md`; `editor-link-edge.spec.ts` 2 cases). I5 → 1.37 (`position-map-editor-leg.test.ts:542` deletion leg calling `formatWithMap`/`nodeAt`/`cursorMap`; reach and presence/absence asserted; the `it.fails` known-defect case at `:703` — finding 3). I6 → 1.38 + 1.39 (`grep -rcE 'ControlOrMeta\+(Home|End)' e2e/web/` 0 in every file; `grep -cE '"(Home|End)"'` = cursor 1 + toggle 2; `tests/no-caret-chords-in-e2e.test.ts` 44 cases green and red on a scratch chord; the `:119` G4 case's location assertion red on a count shortened by one — offset 6 vs 5 — in my mutation). I7 → `DECISIONS.md:363` `#023` in the tree. I8 → `grep -rn 'names.length).toBe(Object.keys' packages apps e2e tests` empty. I9 → redaction present. I10 → `V1.1-BACKLOG.md:60` re-pointed line |
| r4 check: H1–H9, G1–G12, F1–F16 re-confirmed | pass | The r3 table's citations re-checked at this SHA (`schema.ts` end-of-block loop now `:881–901`; `undo.ts` `withinWindow`; `toggle.ts:612–672`; `DevEditor.tsx:215` flush in `copyMarkdown`; `store.test.ts` binding-table test; `vitest.config.ts:14–17`; `check-deps` in root `lint`; `parse-dom.test.ts` enumeration; `sidecar.ts` `needsQuoting`); every guard file of those tasks is in the 37 green files; F16 still deferred; nothing I ran contradicts an r0–r3 disposition |
| r4 check: browser reproductions | pass for the four named; fail beyond them | 1.34 verse Backspace (`editor-break-in-mark.spec.ts`), 1.35 link-edge keystrokes (`editor-link-edge.spec.ts` ×2), 1.36 two-line paste + Copy (`editor-list-paste.spec.ts`), 1.38/1.39 caret placements (`editor-soft-line-breaks.spec.ts` ×5, `editor-trailing-break.spec.ts` ×4) — 83/83 here and on three runners; beyond them: finding 1 (one Backspace over the space after a struck run ending in punctuation) |
| r4 check: the legs read for what they delete, split and insert | pass, with the gap named | `deleteAtEveryBlockEnd` (`typing-legs.ts:165`) to the block's end; `deleteToEveryMarkedRunEnd` (`:267`) to the marked run's end after every break inside a flanking mark; `typeSpaceAtEveryLinkEnd` (`:325`) one space with the node's own marks; `splitEveryListItemParagraph` / `pasteIntoEveryListItemParagraph` (`:371`, `:402`) at every item's first paragraph's end; the position-map deletion leg runs the first and the split. None deletes the *whitespace between* a marked run and its unmarked neighbour, which is finding 1's route; no fixture holds a flanking mark whose edge is punctuation followed by a space and a letter except through the link in `link-in-emphasis.md` |
| r4 check: `spread` derivation vs `mdast-util-from-markdown` | pass | `lib/index.js:308–351` (`firstBlankLineIndex` set only at `containerBalance === 0`, item `_spread` when it precedes the tail) and `:340`/`:380` (`listSpread` only from a blank line between items) agree with `schema.ts:590–593` (list flag copied) and `:646` (item flag derived); `list-spread.test.ts:216` confirms a tight list holding a spread item stays tight |
| r4 check: backlog revisit triggers | none fired | `V1.1-BACKLOG.md:7–67` read; the r4-pointed lines (I8, I6) are dispositioned; the `[1.37]` line (`:66`) and the `[plan.1.verify.r4h.r0]` line (`:67`) point at Phase 2 planning — finding 3 and finding 6 concern them |

## Test counts and coverage

Vitest (cold build, container): **2798 passed / 0 failed / 1 expected fail (`it.fails`, task 1.37)**, 37 files — identical to all three CI legs of the accepted a1 (`Tests 2798 passed | 1 expected fail (2799)` on ubuntu, macos, windows). `cargo test`: **0 passed / 0 failed / 0 ignored** across the three targets — still vacuous; the Rust surface is Phase 2. e2e: **83 passed / 0 failed** (`e2e/web`, 17 spec files counted from the directory plus the `tests/` placeholder) in the container (5 workers, 26.2 s) and 83/83 on each of the three runners. Runner conformance: 83/83.

Coverage delta vs `main` (phase base `052e206`, from `/logs/ci/0.verify.r2h/accepted/test-logs/test-logs-macos-latest/test.log`): 99.45 / 96.98 / 100 / 100 over 729 tests in 21 files → **99.66 / 97.91 / 100 / 100** over 2799 tests in 37 files (stmts / branches / funcs / lines; the same four numbers in the ubuntu `test.log` of the accepted gate). Within the phase, r3 → r4 moves 2196 → 2799 tests (+603: 1.34–1.37 and the list-spread pair enumeration). `schema.ts`, `input.ts`, `store.ts`, `undo.ts` at 100 % on every column; `toggle.ts` 99.1 / 95.55 (uncovered `…51,390-392,447`). `apps/desktop/src/**` still excluded from the measurement by design (G7).

## Findings (≤ 20, most severe first)

Severity, rated by the consequence for the gate's criterion and never by the size of the fix (DECISIONS #review-1-r1): **blocker** — a gate criterion is not met, or wrong output reaches the user silently (example: Copy Markdown puts stale text on the clipboard and reports "Copied"); **should-fix** — a defect or a missing guard the phase should not close with, while the criterion still holds or the path is not the gate's own instrument (example: a redo chord bound under two names with no test that reads the binding table, so deleting one name stays green); **nit** — wording, citations, style, or an assertion with no behaviour behind it (example: `expect(checked).toBe(Object.keys(index).length)` where both sides are the same list). Mark a should-fix **Required** when the reconciliation should fail without it.

1. **blocker** — `packages/core/src/format.ts:54` (`gfmStrikethroughToMarkdown()` taken as-is) and `packages/editor/src/schema.ts:943` (`FLANKING_MARKS` lists `delete` beside `emphasis`/`strong`, but the mark-edge clause reads whitespace and breaks only) — a `delete` run whose edge character is **punctuation** directly adjacent to unmarked alphanumeric text serialises to `~~` delimiters that `micromark-extension-gfm-strikethrough` cannot close or open (`lib/syntax.js:137–138`: `_close = !before || before === punctuation && Boolean(after)` — false when the inside is punctuation and the outside a letter), so `format` writes bytes whose parse holds no `delete`; `mdast-util-gfm-strikethrough`'s `handleDelete` (`lib/index.js:89–101`) has none of the `encodeInfo` logic `mdast-util-to-markdown`'s `emphasis`/`strong` handlers have, which is why the same tree with `*` comes back as `**a.**&#x62;` (a fixed point) and with `~~` as `~~a.~~b` (not one). Reproduced headless for the closing and opening sides and for the inner classes punctuation, inline code and link (`~~a.~~b`, `a~~.b~~`, `x ~~`a`~~b`, `~~[a\`↵`](u)~~c` and `link-in-emphasis.md`'s `~~…[a link whose text ends in a space ](essay.md)~~and` — a link with edge whitespace or a break inside a struck run, which `runHasEdgeWhitespace` at `schema.ts:1155` writes with the strikethrough outermost, so `)` is the delimiter's neighbour) and on `/dev/editor` in Chromium: `Alpha ~~beta.~~ gamma delta` loaded, caret after the space (asserted), one `Backspace` → pane and `writeText` `Alpha ~~beta.~~gamma delta\n`, status "Copied Markdown", `format(parse(·))` = `Alpha \~\~beta.\~\~gamma delta\n`; the rendered view still shows `beta.` struck, so nothing tells the writer. Deleting the space after a struck sentence (or before a struck run that opens with a quote or bracket) is an ordinary essay action; my sweep reaches the tree from the corpus's own `link-in-emphasis.md` with one `Delete` or `Backspace`. Note the parser's own tree for `~~a.~~&#x62;` (`delete[a.], text(b)`) is not a fixed point of `parse`∘`format` either — a Phase 0 invariant-A gap the corpus has no fixture for. Fix: a `delete` handler in `format.ts` beside `opaqueHandlers()` that mirrors `mdast-util-to-markdown/lib/handle/emphasis.js:33–48`: after `containerPhrasing`, if the last inner character is punctuation and `info.after`'s first character is neither whitespace nor punctuation, append `encodeCharacterReference(info.after.charCodeAt(0))` (the `outside` case) — symmetrically for the opening side with `info.before` — so the bytes read `~~a.~~&#x62;` and `&#x61;~~.b~~`, both of which parse back to the tree (verified); one guard per side × neighbour class (letter, digit, punctuation, whitespace) × inner class (punctuation, letter, inline code, link), each asserting bytes and the fixed point; a fixture `strikethrough-punctuation.md` (`~~a.~~ b`, `a ~~(b)~~`) so the identity corpus covers the shape; a corpus leg titled for **the mark's neighbour** that deletes the whitespace on each side of every flanking-marked run (and, for the certain case, types `.` at the run's end first) over every fixture, asserting the fixed point and the mark count; the Playwright reproduction above. `packages/core` is Phase 0 territory, so a new task (#016).

2. **should-fix** — `packages/editor/src/schema.ts:678–681` (`needsBlankLine`, the paragraph clause) — the pair *paragraph then setext heading* is missing: `mdast-util-to-markdown/lib/join.js:26–35` writes a blank line after a paragraph before a heading `formatHeadingAsSetext` chooses (depth ≤ 2 with a `\n` or a `break` in its text) whatever the item's flag says, exactly as for two paragraphs, so a tight item `[paragraph, heading("h\nx", depth 1), list]` serialises as `- a\n\n  h\n  x\n  =\n  - i\n` while `parse` reads the item as spread and writes a blank line before `- i` too — I2's class, one pair short. Reproduced headless for the `\n` and the `break` members (`fixed: false`); the enumeration in `list-spread.test.ts:101` (`KINDS`) has the ATX heading only, so its 225 pairs cannot see it. Route: `# ` typed at the start of a hard-wrapped second paragraph of a list item that has a nested list after it — reachable, not ordinary. Fix: in the paragraph clause, `right.type === "heading" && right.depth <= 2 && headingHasLineBreak(right)` (a `\n` in any text descendant or a `break` — the same predicate `format-heading-as-setext.js` uses); add the two setext variants to `KINDS` so the pair test asserts them by construction; one guard with the nested list present and absent.

3. **should-fix** — `packages/editor/src/toggle.ts:351` — `toSource` answers a position that a block's correspondence covers but no inline child does with the block's *start* (`range.startLine`), so after a Backspace that leaves a paragraph ending in a `hard_break`, or inside the empty paragraph a split opens in a list item, `Cmd/Ctrl+/` puts the source caret at the paragraph's (or item's) first column instead of after the text before it. Pinned by 1.37 as `it.fails` (`position-map-editor-leg.test.ts:703`) and deferred to Phase 2 planning by a test-only task (`V1.1-BACKLOG.md:66`). A wrong caret on the toggle is 1.7's acceptance, not the copy criterion, and the `it.fails` shape is the opposite of a `.skip` (it goes red when fixed) — but the defect is one line: replace `:351` with `return afterLastNode(entries, map, pos)` (the last inline node ending at or before `pos`, which is `3:2` for shape (a) and `1:10` for shape (b)), delete the width branch and the split collector the backlog line names, and fold both shapes into clause 3. Not Required; a reconciliation decision, not a task agent's.

4. **should-fix** — `tests/no-caret-chords-in-e2e.test.ts:30` — `CHORD = /\b(ControlOrMeta|Control|Meta)\+(Home|End)\b/` does not match `Shift+End`, `Alt+Home` or `ControlOrMeta+Shift+End`, and `BARE_PRESS` (`:33`) does not either, so a scratch spec pressing all three passed the guard in my run (only the bare `ControlOrMeta+Home` line turned it red). `Shift+End` and `Cmd+Shift+End` are resolved by the same OS layer #022 describes (`MoveToEndOfDocumentAndModifySelection` on macOS), so a fifth use spelled with Shift is green in the container and unproven on macOS — the gap the guard exists to close. Fix: `CHORD = /"(?:[A-Za-z]+\+)+(Home|End)"/g` (any modifier chain ending in Home or End, in the quoted key-name form `keyboard.press` takes), and one negative case in the file that feeds the regex the three spellings.

5. **nit** — `tests/no-caret-chords-in-e2e.test.ts:42–49` and `DECISIONS.md` #review-1-r3 I6 — the allowlist's stated reason ("CodeMirror's keymap handles the key in JS before the OS layer", `standardKeymap`) is false: `@codemirror/commands` is not installed (`pnpm why` empty, by 1.19's design) and `source.ts` installs no keymap, so the three source-view `Home`/`End` presses are Blink's native contenteditable line motion, the same path the rendered view has. They are proven by the gate (green on macos-latest at this attempt's a1, and `editor-cursor.spec.ts:35` `toColumn` would land the `X` at the line's end if `Home` only scrolled), not by a keymap. Fix: reword the two `reason` strings to "Blink's native line motion in CodeMirror's contenteditable; proven by the three-OS gate, no keymap is installed", and the file's doc comment likewise.

6. **nit** — `docs/V1.1-BACKLOG.md:67` — the `[plan.1.verify.r4h.r0]` line says the vertical-arrow construct "remains" at two sites in `editor-trailing-break.spec.ts`; the same construct is at `e2e/web/editor-break-in-mark.spec.ts:126` (`ArrowDown` ×2 in a block that renders two visual lines — the verse with its `<br>`) and `e2e/web/editor-link-edge.spec.ts:143`, `:163` (one-line blocks, inside the promoted rule). All three carry a `textBeforeCaret` precondition and passed on three runners, so they are proven for this geometry; the line's grep was incomplete. Fix: name all four sites in the line so the revisit trigger covers them.

7. **nit** — `packages/editor/src/input.ts:119` (the heading input rule) — `### ` typed at the start of a hard-wrapped paragraph (`alpha\nbeta gamma`, the soft line break typing preserves since 1.25) makes a depth-3 heading whose text holds a `\n`, which `format` can only spell as `### alpha&#xA;beta gamma` (depth 1–2 become setext, which is fine); a fixed point that decodes to the same tree, so no criterion is touched, but the entity is what the writer sees in the source view. Fix: a backlog line (Phase 3's heading tasks: the conversion to a heading of depth ≥ 3 replaces line endings and breaks by a space, the way `CELL_LINE_ENDING` does for cells) with a trigger.

8. **nit** — `docs/progress/journal-main.md:281` — 1.37's attempt-1 stub has no timestamp (`- [1.37]  Task: …`, two spaces where the ISO goes); the template in `docs/progress.md` requires it and the runner's per-attempt count read it anyway.

9. **nit** — turn budget, evidence only (`/logs/tasks/{1.34,1.35,1.36,1.37}/1.log` `"num_turns":51,"subtype":"error_max_turns"`; each `2.log` `success` at 26 / 20 / 25 / 36): four of the five opus fix tasks in the r4 chain capped at attempt 1 under the stub rule and passed at attempt 2; 1.38 (35), 1.verify.r4 (40), 1.39 (18), 1.verify.r4.g1 (26) passed at attempt 1. With the r3 chain's three of six, that is seven of eleven opus fix tasks at 49–51 turns on a1 — input to the conditional 80-turn line (`V1.1-BACKLOG.md:64`), no decision here.

## Three riskiest things

1. **The serializer is an unowned half of the fixed-point property.** Every Phase 1 fix (F1, G3/G4, H1, H8, I1, I3/I4) landed in the editor's strip on the assumption that `format` is right about flanking; finding 1 shows one GFM handler has no flanking logic at all, and the parser's own tree for `~~a.~~&#x62;` is not a fixed point — a Phase 0 invariant with no fixture behind it. Tables (`|`, `\n` in cells), autolink literals and inline code were clean in my sweep; strikethrough was the only extension handler that fails, but nothing in the tree reads the handlers, so the next extension added (footnotes, task lists are excluded by PRD) reopens the question.

2. **The enumerations are as complete as their kind tables, and the property test that would replace them is still a Phase 2 planning line.** Finding 2 is a pair the 225-case enumeration cannot express; finding 1 is a neighbour class (punctuation) the mark-edge guards never varied. A committed sweep of every keystroke-shaped single transaction at every position of every fixture (61,682 transactions, under five minutes in the container) found finding 1 in one run and confirmed everything else the four attempts fixed one member at a time; the `[review-1-r2, Claude risk 1]` backlog line defers exactly this to Phase 2, while the gate criterion it discharges is Phase 1's.

3. **The caret-placement story rests on inference from one failing run.** #024 reads the ubuntu/windows offsets (8, 16) as a visual-line landing at the seed's x; a one-line block's `ArrowUp` reached column 0 on the same runners in `editor-link-edge.spec.ts` at this gate, and `ArrowDown` ×2 in a two-line block reached the end in `editor-break-in-mark.spec.ts` on all three. The anchor-and-count route is robust regardless, but the rule now in CLAUDE.md ("`ArrowUp`/`ArrowDown` reach a one-line block's edges on every OS") and the two-line uses that remain (finding 6) are proven only by the gate's geometry, and `tests/no-caret-chords-in-e2e.test.ts` cannot see a vertical arrow (or a Shift chord, finding 4).

## Class-level lessons (for docs/lessons.md)

- LESSON: CommonMark §6.2's flanking rules read punctuation as well as whitespace, and only `mdast-util-to-markdown`'s own `emphasis`/`strong` handlers encode for them; an extension handler (`~~`) writes the delimiters blind, so an edit that puts punctuation inside a mark's edge next to a letter outside it produces bytes the parser cannot close → before assuming the editor's strip is the place to fix a mark-edge defect, read the serializer's handler for that mark for its flanking logic, and enumerate every mark-edge guard over both neighbour classes (whitespace, punctuation) on all three flanking marks.
- LESSON: an enumeration that mirrors a third-party rule (`join.js`, `prepareList`) is complete only when its kind table is derived from that rule's own branches (paragraph→paragraph, →definition, →setext heading), not from the mdast type list — a pair the table lacks is a pair the 225 cases cannot fail on.
- LESSON: a sweep of every keystroke-shaped single transaction (Backspace, Delete, Enter, Tab, a typed letter/space/punctuation with the position's marks, a two-line paste) at every inline position of every fixture, asserting the `parse`∘`format` fixed point, runs in minutes and finds the member the next attempt would otherwise find by hand → commit it as the property leg the moment a second member of a class appears, not at the next phase's planning.
- LESSON: a guard's allowlist states a reason that must be checked against the manifest and the source (a keymap from a package `pnpm why` cannot find is not the reason a press is safe); a false reason survives every green run and misleads the next repair's scope decision.
- LESSON: a caret-motion regex guard enumerates the modifier *chain* (`(?:[A-Za-z]+\+)+`), not the modifier names it has seen; the spelling the next spec uses is the one the alternation lacks.
