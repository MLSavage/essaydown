# Phase 1 review — Grok

Reviewer: Grok 4.6. Inputs: docs/PRD.md §7/§8 phase 1, docs/RUNNER-SPEC.md, docs/lessons.md, `git diff 052e206aa8314ee1e4e454668a0496b3b96f4128...f90ed508ae49e4d00d503bcbb0b5fce6b748e933` (attempt r3), /logs/ci/1.verify.r3.g1h/accepted/ (this attempt's verifier; verifier_id `1.verify.r3.g1`), /logs/ci/1.verify.r3.g1h/a1/ (transient GATE-FAILED, no accepted.json; abandoned as superseded by a2), /logs/ci/1.verify.r3h/a1/ (GATE-FAILED macos Home chord, DECISIONS #022), /logs/ci/1.verify.r2.g1h/accepted/, /logs/ci/1.verify.r1h/accepted/, /logs/ci/1.verify.g1h/accepted/, /logs/human/1.9/accepted.json + a2.md, /logs/tasks/, docs/DECISIONS.md #review-1-r0, #review-1-r1, #review-1-r2, #002-outline-feel, #021, #022. Cold build: cited /logs/ci/1.verify.r3.g1h/accepted/ (Grok). Commands run: `git rev-parse HEAD`; `git log --oneline 052e206…f90ed50`; `git diff --stat 052e206…f90ed50`; `git show --stat` of `f90ed50` / `440472e` / `32552e0`; python `digestDir` (ralph/lib/util.mjs algorithm) over accepted `test-logs` and `playwright-report`; greps of schema/store/toggle/undo/DevEditor, e2e specs, lockfile, CLAUDE.md/AGENTS.md, backlog, secret patterns, skip/only, `process.platform`. No build, no tests. No browser: the 1.29/1.30/1.31/1.32 probes named in the r3 task text are discharged from the tree plus the three-OS Playwright logs, not from an independent drive. Isolated `playwright test latency.spec.ts --workers=1` was not run (no scratch copy).

## Gate table

| Criterion (from PRD §7) | Result (pass / fail / unverifiable) | Evidence (command + output line, or file:line) |
|---|---|---|
| Vite `/dev/editor` with the dev-only "Load fixture…" / "Copy Markdown" bar | pass | `apps/desktop/src/main.tsx:11` routes `/dev/editor`. `DevEditor.tsx:229-248` renders both buttons (`data-testid` `load-fixture`, `copy-markdown`). `copyMarkdown` (`DevEditor.tsx:210-223`) flushes, then writes `format(store.getState().document.root)`. |
| Michael writes ~1,000 words in the rendered view | pass (count not recorded) | `/logs/human/1.9/a2.md` body "Rendered editor"; `accepted.json` attempt 2, `outcome: ACCEPT`. Word count is still not in the record (#002, F16; backlog line for 2.9). Observed SHA `ca1cd3d` is an ancestor of `f90ed50`. |
| Toggles to source | pass | `a2.md` "Source toggle": shortcut is Cmd+/. Playwright `editor-toggle.spec.ts` uses `ControlOrMeta+/`. workflow.log: ubuntu `79 passed (1.0m)`, macos `79 passed (45.5s)`, windows `79 passed (1.0m)`. |
| Copied Markdown round-trips (Typora) | pass | `a2.md`: Typora renders the copied file correctly. F1/G4/H1 family is `stripUnparsableWhitespace` (`schema.ts:737-770`); ownership rule at `:668-732` now drops a trailing `hard_break` (`:700-710`) and moves ASCII whitespace off a flanking mark (`:673-691`, `unmarkEdgeWhitespace` `:806`). H1 e2e `editor-trailing-break.spec.ts:115-130` deletes the continuation of `one\\\ntwo` and copies `one\n`. H8 e2e `editor-mark-edge.spec.ts:80-110` types a space inside `*b*` and asserts pane `a *b*  c\n` with byte-exact `emText` `"b "` and no `&#x20;`. Real-clipboard case `editor-clipboard.spec.ts:337-345` (`readText` count in that file is 1). Remaining members (atom last inside a mark mid-block; Unicode whitespace at a mark edge; arbitrary splits/joins/pastes) are named backlog lines, not residues of the r2 reproductions. |
| Outline prototype tried; verdict recorded (DECISIONS #002) | pass | `/logs/human/1.9/accepted.json` `payload.verdictText` copied to `docs/DECISIONS.md` `#002-outline-feel`. Three re-scopes stay Phase 3 inputs; editor/layout wishes stay Phase 2. Playwright `outline-produce.spec.ts:63` (`question: "true"` quoted). G11's bold-span line is on the backlog. |
| Typing latency p95 with 10k words on `/dev/editor` (< 16 ms or a follow-up task at reconciliation) | pass (follow-up taken; 16 ms not met on contended CI rendered pane) | Protocol: `e2e/web/latency.spec.ts:1-50` (keydown → markdown-pane mutation for rendered; keydown → `.cm-content` for source; source `commit` line added by 1.31, G9). Isolated `--workers=1` not rerun here (no scratch). Prior isolated numbers under that protocol: r2 Claude 9.2 ms / Sol 9.8 ms rendered 10k pane. This SHA's accepted CI workflow.log, 120 presses, 2 workers on all three: **rendered 10k pane p95 ubuntu 26.4 ms / windows 20.7 ms / macos 14.3 ms**; source 10k surface p95 1.1 / 1.1 / 1.3 ms (pane n=0 during a burst); source 10k **commit** p95 70.7 / 59.9 / 60.1 ms (n=5 bursts). The PRD alternative (“or a follow-up task”) was taken at r0 (1.17); G10 names the quiet single-worker run as the incremental-`pmToMdast` trigger's input. I do not re-open F5 as required. |
| Full suite green on 3 OSes at the implementation SHA | pass | `/logs/ci/1.verify.r3.g1h/accepted.json` `sha` = `f90ed508ae49e4d00d503bcbb0b5fce6b748e933` = `/logs/reviews/1/r3/implementation_sha` = `verification_sha` = `git rev-parse HEAD`. `result.json` `conclusion: success`, run 34573685490, ref `ci/1.verify.r3.g1/a2`. Vitest `2196 passed (2196)` / 35 files on ubuntu, macos, windows. cargo `0 passed; 0 failed; 0 ignored` × desktop_lib, desktop, doc-tests on all three. lint.log on all three: `eslint . && node scripts/check-deps.mjs && pnpm -r --if-present typecheck`; `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`; six `typecheck: Done`. ubuntu `ralph-test.log`: `# tests 83 / # pass 83 / # fail 0`. a1 of this gate (run 34572776730) failed windows Playwright `locator.setInputFiles` timeout 30s; abandoned as a transient rerun superseded by a2 (`abandoned.json`). |
| e2e/web Playwright on all three runners (1.verify.r3.g1 ciAcceptance; DECISIONS #019, #022) | pass | workflow.log: `Running 79 tests` then `79 passed` on ubuntu (1.0m, 2 workers), macos (45.5s, 2 workers), windows (1.0m, 2 workers). Fourteen spec files under `e2e/web/` (count from the directory, including `latency.spec.ts`, `editor-mark-edge.spec.ts`, `editor-trailing-break.spec.ts`). Merged artifact has `playwright-report-{ubuntu,macos,windows}-latest/index.html`. 1.verify.r3h a1 (run 34563964087 at `32552e0`) GATE-FAILED: macos 1 H8 case red on `ControlOrMeta+Home`; no artifacts fetched. Repair 1.33 + this gate. |
| 1.1 schema 1:1; corpus `format(pmToMdast(mdastToPM(parse(x)))) === format(parse(x))`; raw-html bytes | pass | Index keys = 55. ubuntu test.log `schema-roundtrip.test.ts (60 tests)`; `editor-fixed-point.test.ts (324 tests)` including the `editor fixed point` typing legs, the **deletion** leg (`:810`) and the **mark-edge** leg (`:830`). |
| 1.2 `formatWithMap` / `nodeAt`; every paragraph startLine; 200 seeded positions; map is pure | pass | `positions-corpus.test.ts (390 tests)`; editor-seeded family `position-map-editor-leg.test.ts (113 tests)` titled for the leg, calling `formatWithMap`, `nodeAt`, `cursorMap`. |
| 1.3–1.8 Playwright acceptances | pass | Inside the 79/79 × 3 OS run: 22 input scenarios, 8 reveal, live source colours, 6 undo, toggle+200 seeded, outline Produce, plus r1–r3 specs (trailing-space, clipboard including G1 spy cases and H4 real write, cursor, empty-cell, latency including commit line, source-view undo, G2/H2 reproductions, soft-line-breaks, trailing-break, mark-edge, toggle-inside-window). |
| 1.11 macOS keymap tests: both platforms, presence and absence, four chords, no skip | pass | `store.test.ts` `PLATFORMS`; `toggle.test.ts` the same shape. macos-latest vitest 2196/2196. `grep` of `e2e/web` + `packages` + `apps` for `.skip(` / `.only(` / `runIf` / `skipIf` / `process.platform`: empty. |
| 1.12 Playwright in `ci.yml` on all three runners | pass | workflow.log browser-install + `Playwright (e2e/web)` on ubuntu/macos/windows; merge-logs `playwright-report` with three OS directories. |
| Fresh-clone cold build | pass (cited CI) | `/logs/ci/1.verify.r3.g1h/accepted/`: `pnpm install --frozen-lockfile` then lint / test / cargo / Playwright / ubuntu `bash ralph/test/run.sh` on ubuntu-latest, macos-latest, windows-latest at `f90ed50`. Artifact `test-logs` digest `b44ce58b9dfe372f5f08caa499b60385d2868f02c954b36d2a862d57a4a5dab2` / 109134 bytes / 10 files MATCH `digestDir` of `/logs/ci/1.verify.r3.g1h/accepted/test-logs`. Artifact `playwright-report` digest `4fd80a07a7ae715a847543ad7cd4e1cc6c7d89c59502126f29554f3f93a5ebbc` / 1974541 bytes / 3 files MATCH. `accepted` symlink → `a2`. |
| §9.4 verify tasks: no lockfile change; no API key in tree | pass | `git show f90ed50`: `docs/progress/journal-main.md` only. `git show 32552e0`: `AGENTS.md`, `CLAUDE.md`, journal only. `git show 440472e`: mark-edge spec + docs only. Secret-pattern walk of the working tree (`sk-ant-`, `ANTHROPIC_API_KEY=`, `OPENAI_API_KEY=`, `xai-`, `ghp_`, `github_pat_`, `AKIA`): empty outside docs that name the patterns. |
| r2 dispositions H1–H9 verified against the tree | pass | H1 `schema.ts:700-710` + `:754-769` drop a trailing `hard_break`; guards `editor-fixed-point.test.ts:286-373` (last node, only-a-break, break+whitespace, two breaks, heading type survives); deletion leg `typing-legs.ts:153-188` (`tr.delete` after every break, then last char of every last run) titled at `editor-fixed-point.test.ts:810`; reach guards `:861-876` include `hard-break.md`; e2e `editor-trailing-break.spec.ts:115`. H2 `PushOptions.from` `undo.ts:86-94`; `push` `:177-182` (`from ?? at` vs `present.at`); `pendingFrom`/`pendingAt` `toggle.ts:619-671`; injected-clock cases `toggle.test.ts:848-1016` (a/b/c/d); `undo.test.ts:160-187`; e2e `editor-toggle.spec.ts:341-370` (`bcdef` at 300 ms). H3 toggle flush `DevEditor.tsx:102` kept; inside-window case `editor-toggle.spec.ts:385-406`; ordering case `:420` (Copy then `bc` then toggle: one Undo empties). H4 `openEditorWithRealClipboard` `editor-clipboard.spec.ts:270-275`; real case `:337-345` (`toContain("delta")`); three spy cases untouched; `readText` count 1. H5 heading guard `:294-300`. H6 cell clause `schema.ts:716-719`; guard `:404-426`. H7 runner chore, backlog, hard stop `2.1`; r3-chain texts name CLAUDE.md's stub-first list. H8 `unmarkEdgeWhitespace` `schema.ts:806`; seed `editor-fixed-point.test.ts:740-757` builds `emphasis[text("b ")]`; mark-edge leg `typing-legs.ts:212-223` + `:830`; e2e `editor-mark-edge.spec.ts` ArrowUp/ArrowDown, byte-exact `emText`, no Home/End. H9 tautology dropped at `position-map-editor-leg.test.ts:178-182`. |
| r1 dispositions G1–G12 re-confirmed | pass | G1 `DevEditor.tsx:215` flush + spy cases `editor-clipboard.spec.ts:294-326`; attestation restored as H4. G2 source undo flush `DevEditor.tsx:165`; reopened as H2 and closed. G3 `whitespace: INLINE_PRE` on paragraph/heading/table_cell; soft breaks survive. G4 hard-break continuation; family reopened as H1/H5/H6 and closed. G5 `store.test.ts:439-446` key-list equality, never a literal. G6 `position-map-editor-leg.test.ts` describe title names the family; both legs call `formatWithMap`/`nodeAt`/`cursorMap`. G7 comment `vitest.config.ts:14-17` + backlog. G8 `checkedNames` still in clipboard spec. G9 commit line `latency.spec.ts:31-40`; printed on all three OS. G10 isolated vs CI reported separately. G11 backlog. G12 PRD §4 Rust crates row names `serde_json`; Shell row names `tauri-build`. |
| r0 dispositions F1–F16 re-confirmed | pass | F1 confirmed; family members reopened at r1 (G3/G4) and r2 (H1/H5/H6/H8) are now closed in the strip. F2 `parse-dom.test.ts` (25 tests). F3 empty-cell fixture + map. F4 hand-derived cursor cases + `editor-cursor.spec.ts`. F5 1.17 + latency spec + backlog. F6 `outline-produce.spec.ts:63`. F7 `packages/editor/package.json` has no `codemirror`; lockfile grep for `@codemirror/commands` empty; `no-codemirror-history.test.ts`. F8 `check-deps` in lint on all three OS. F9 (a)–(d) plus G5/H9 siblings. F10 identity legs. F11–F16 remain nits/backlog as recorded (`App.tsx` still `invoke("greet")`, hard stop `2.verify`). |
| Deletion and mark-edge corpus legs: what they actually delete and insert | pass | Deletion (`typing-legs.ts:153-188`): (1) `tr.delete` of the inline content after every `hard_break` in paragraph/heading/table_cell, so every break becomes last in its block; (2) `tr.delete` of the last code point of every such block's last text run. Asserts `breaksIn(root) === 0`, `format(parse(out)) === out`, no `&#x20;` / `&#xA;` (`editor-fixed-point.test.ts:810-828`). Mark-edge (`typing-legs.ts:212-223`): `insertText(" ")` at the end of every text node carrying `emphasis`/`strong`/`delete`. Seed at `:740-757` is Sol's tree `paragraph(text("a "), em("b "), text(" c"))` → `"a *b*  c\n"`. Presence: `typed.doc.eq(doc)` iff `runs === 0`; reach guard `:852`. |
| `from`/`at` contract in `undo.ts` against the grouping rule in `toggle.ts` | pass | `undo.ts:86-94`: `from` is the earliest keystroke of a batch, defaulting to `at`; the entry keeps `at` as the latest. `push` `:177-182`: `withinWindow = from >= present.at && from - present.at <= coalesceWindowMs`. `toggle.ts:581-600` grouping rule (adjacent keystrokes); `:619-671` records `pendingFrom` when the segment opens and `pendingAt` on every `change`; `commitPending` hands both to `commit`. Explicit closure on toggle and history is unchanged (`:599-600`). |
| Copy Markdown and source Undo/Redo inside the coalescing window; H2 long continuation; H3 toggle | pass (from tree + CI; this reviewer has no browser) | Presence copy `editor-clipboard.spec.ts:295-304`. H2 reproduction `editor-toggle.spec.ts:341-370`. H3 inside-window `:385-406` and ordering `:420`. All in the 79/79 × 3 OS run. |
| Space at a mark edge and `*word*` then a space (1.30); H8 caret is Blink-only | pass | `editor-mark-edge.spec.ts:23-36` (never Home/End/a modifier chord for rendered caret motion; byte-exact `textContent`). H8 `:90-99` ArrowUp + three ArrowRight, `emText === "b "`, pane `a *b*  c\n`. Absence `:113-129`. Input-rule `:132-151` (`toMatch(/^word ?$/)`). `grep -cE 'ControlOrMeta\+(Home\|End)\|" (Home\|End)"'` on that file is 0. macos-latest: H8 is `✓` in a2 workflow.log. |
| `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` in the cold build | pass (read-only equivalent) | No scratch clone: lockfile has no `@codemirror/commands` / `codemirror` / `prosemirror-history`. `packages/editor/package.json` dependency blocks contain none of the three. CI lint on all three OS: `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`. |
| #002-outline-feel: anything the fix chain should have taken and did not | pass | The `&#x20;` defect is F1/1.13 + G4/1.25 + H1/1.29 + H8/1.30. Outline re-scopes remain Phase 3. List spacing, background, word-count, one centre pane, file tree, `Cmd+/` help-text remain Phase 2. G11 carries the bold-span observation. No unfixed Phase 1 gate defect in the record. |
| Backlog revisit triggers that have fired | one nit; two decisions recorded | `[1.32]` tautology in `editor-fixed-point.test.ts:763` — trigger "the next review attempt's reconciliation" has fired (finding 1). `[1.30]` Unicode whitespace at a mark edge — trigger asks this reconciliation to decide: **do not widen** the clause to CommonMark §6.2's Zs class in Phase 1; moving U+00A0 changes what Option+Space typed on purpose, and `&#xA0;` is a fixed point (1.30's reason is not false). `[plan.1.verify.r3h.r0]` four remaining rendered-view Home/End chords — trigger is "the r3 reconciliation if a reviewer raises it" (finding 2). G9's trigger fired in 1.31 (commit line present). G7/App.tsx hard stop remains `2.verify`. Incremental-`pmToMdast` trigger still reads a quiet single-worker 10k pane p95; no r3 isolated rerun exists here. |
| r2 class (Claude risk 1): further trees the editor can build that `parse` cannot produce, beyond the named reproductions | pass (remaining members already named) | Walked the ownership rule (`schema.ts:668-732`) against CommonMark §6.2 / §6.7 and the Phase 1 keymap. Trailing `hard_break` and ASCII mark-edge whitespace are stripped. Mid-block `hard_break` is kept (`editor-fixed-point.test.ts:365-373`). A `hard_break` last *inside* a flanking mark, mid-block, is the 1.30 backlog line (Shift-Enter unbound; rich-text `<br>` paste only). Unicode whitespace at a mark edge is the other 1.30 line. Multi-line paste into a cell restructures the table (1.29 backlog, not a round-trip defect). Arbitrary splits/joins/structural pastes are the Phase 2 planning line. No new keystroke-reachable member under Phase 1's bound commands. |
| r2 Claude/Sol findings Grok missed or rated lower (DECISIONS #review-1-r2; H1, H2, H7) | recorded | **H1 (Claude blocker; Grok did not raise).** A read-only review **could** have found it from the tree: the r2 ownership comment said an atom stops the strip and said nothing about an atom that *is* the block's last node (`schema.ts` at `c9408ee`); both corpus legs only `insertText`; `hard-break.md` is in the index; CommonMark §6.7 reads a trailing `\` as a literal backslash. I treated 1.25's eleven whitespace guards as closing the family. Severity: read-only could have rated **should-fix Required** (missing guard on a family the phase claimed closed). Blocker needed Claude's three-Backspace copy of non-round-tripping bytes with status "Copied Markdown". **H2 (Sol blocker; Grok did not raise).** A read-only review **could** have found it from the tree: `pendingAt` was one time; `push` compared one distance (`at - present.at`); 1.24's case 3 stopped at 200 ms. A flush then a continuation longer than the window is last-vs-last, not adjacent-vs-adjacent. I verified the short G2 reproductions and did not ask what happens when the continuation outlasts a window. Severity: the contract mismatch is **should-fix Required** from the tree; blocker needed Sol's user-visible split. **H7 (Claude should-fix, Grok nit).** Read-only found it (the wrapper's step list vs CLAUDE.md). Severity differed because the template rates by consequence for the **gate's criterion**; the wrapper is a runner file, not the product gate. Reconciliation took it as a boundary `fix(runner)`, which agrees with the product rating. |
| Verdict if any r2 blocker or required finding is not discharged | would be FAIL; none remain undischarged | H1–H6 and H8 each have a commit `task(1.29)`…`task(1.33)` on this SHA and matching tests in the 2196/79 green suites on three OSes. H3's `:102` is the live guard (ordering case). H4's real write is in the 79/79 run including windows-latest. H7 deferred as a runner chore (hard stop `2.1`). H9 dropped in the cited file; the sibling tautology is finding 1, a nit. |

## Test counts and coverage

Vitest: 2196 passed / 0 failed / 35 files (ubuntu, macos, windows at 1.verify.r3.g1). Breakdown from `/logs/ci/1.verify.r3.g1h/accepted/test-logs/test-logs-ubuntu-latest/test.log`: sidecar 73, positions 55, store 46, toggle 129, front-matter-boundary 125, undo 36, reveal 90, editor-fixed-point 324, sentences-spanning-marks 24, schema 50, parse-dom 25, sentences 91, input 59, blocks 45, front-matter-grammar 73, position-map-editor-leg 113, segment-fallback 21, sentences-zero-width 72, check-deps 64, fixtures-corpus 7, source 10, positions-corpus 390, opaque-roundtrip 9, no-codemirror-history 10, schema-roundtrip 60, parse-line-endings 4, hash 7, agent-rules 1, markdown-style 13, roundtrip 165, plus five package placeholders. cargo test: 0 passed / 0 failed / 0 ignored × desktop_lib, desktop, doc-tests on all three OS (vacuous; DECISIONS #review-0-r1 G13, unchanged; Rust surface is Phase 2). e2e/web Playwright: 79 passed / 0 failed on ubuntu, macos and windows (14 spec files; count from the directory). ralph/test/run.sh (ubuntu only): 83 passed / 0 failed. Coverage at implementation SHA (ubuntu table; macos/windows match): 99.64% stmts / 97.73% branches / 100% funcs / 100% lines. `editor/src` 99.7/98.4/100/100 (`schema.ts`/`input.ts`/`reveal.ts`/`source.ts`/`store.ts` 100; `toggle.ts` 99.1/95.55). `core/src` 99.6/97.3/100/100 (`undo.ts` 100 all columns; `positions.ts` 100/98.33, uncovered 182,464). Thresholds hold. Coverage delta vs phase base / main (`052e206`, Phase 0 r2 table): 99.45/96.98/100/100 → 99.64/97.73/100/100. vs r2 (`1.verify.r2.g1`): 99.63/97.65/100/100 → 99.64/97.73/100/100.

1.verifyh a1 GATE-FAILED (macos keymap; run 34333160626; no artifacts). 1.verify.g1h was r0's verifier. 1.verify.r1h was r1's. 1.verify.r2h a1 GATE-FAILED (windows clipboard CRLF). 1.verify.r2.g1h was r2's. 1.verify.r3h a1 GATE-FAILED (macos Home chord; run 34563964087; no artifacts). 1.verify.r3.g1h a1 transient windows `setInputFiles` timeout; a2 is this attempt's verifier.

## Findings (≤ 20, most severe first)

1. **nit** — `packages/editor/test/editor-fixed-point.test.ts:763` — `expect(names.length).toBe(Object.keys(index).length)` is `x === x`: `names` is `Object.keys(index).sort()` (`:62`). The real guards are the per-fixture `it` cases and the reach collectors (`:852-897`). Same class as F9(c) / G8 / H9, which 1.21, 1.26 and 1.32 removed from the neighbouring files; 1.32 grepped the tree, filed this hit as `[1.32]` on the backlog, and the revisit trigger ("the next review attempt's reconciliation") has fired. Fix: drop the length assertion (keep `toBeGreaterThan(0)`).

2. **nit** — `e2e/web/editor-soft-line-breaks.spec.ts:85` (and `:107`; `e2e/web/editor-trailing-break.spec.ts:125`, `:138`) — four remaining rendered-view caret placements through `ControlOrMeta+Home`/`ControlOrMeta+End`, the construct DECISIONS #022 removed from `editor-mark-edge.spec.ts`. On macOS those chords are not document motions; the G4 first-line case (`:85`, assertion "bytes unchanged") is satisfied by a space typed at the paragraph's end, so on macos-latest it proves nothing about the first line. The other three still work because the seed's click already leaves the caret at the document end. Unit guards and the ubuntu/windows legs still cover G4. Backlog line `[plan.1.verify.r3h.r0]` invited this attempt to raise it; hard stop `2.verify`. Fix: replace the four with Blink-only `ArrowUp`/`ArrowDown` as 1.33 did, and add a suite-level grep guard over `e2e/web/` that fails the build on `Home`/`End`/`ControlOrMeta+Home|End` in a rendered-view spec.

## Three riskiest things

1. Rendered typing is still O(document) (`pmToMdast` per transaction). Under the 1.17 protocol the contended CI runners report 10k-word pane p95 26.4 / 20.7 / 14.3 ms (ubuntu / windows / macos, 2 workers); r2 isolated runs were 9.2–9.8 ms. Incremental conversion is on the backlog with a quiet-single-worker trigger. Phase 2 adds autosave and a watcher to the same path. Source `commit` p95 at 10k is 60–71 ms (n=5 bursts) — the number autosave will wait on.

2. Every new reader of the store that is not an editing surface must flush. Copy, undo/redo, toggle and unmount now do, each with an inside-the-window case; the markdown pane still lags a source burst by design. Phase 2 autosave is the next reader. The promoted rule names the test shape (an action inside the window); a save path that only tests after the window will miss G1 again.

3. The image-of-`parse` invariant is asserted for typed, deleted and marked shapes, not for arbitrary splits, joins, structural moves or pastes. Remaining named members: a `hard_break` last inside a flanking mark mid-block (rich-text paste; Shift-Enter unbound); Unicode whitespace at a mark edge (Option+Space). A property-shaped corpus over random ProseMirror transactions is the Phase 2 planning line beside the stated-invariant sweep.

## Class-level lessons (for docs/lessons.md)

- LESSON: a read-only review that treats "the family is closed because the last fix's guards are green" will miss the next member whose construction (a deletion, a caret inside a mark) is outside those guards → walk the ownership rule against the CommonMark clauses it cites, including the atoms the rule says stop the strip, and ask which trees `insertText` cannot build (H1 at r2; Grok's lane).
- LESSON: `pendingAt` as one time against `push`'s one distance is the same class as `copyMarkdown` without `flush`: the contract is in the tree, the short-path tests are green, the long continuation is the case that distinguishes them → when a grouping rule says "adjacent keystrokes", the test must include a continuation longer than the window with every neighbouring gap shorter than it (H2 at r2; Grok's lane).
- LESSON: a tautology-class grep that lists every hit still leaves the out-of-scope instance in the tree until a later reconciliation names it → the reconciliation of the attempt that files the backlog line is the drop, not a later "or any task that edits the file" (H9 → `[1.32]` → this attempt's finding 1).
