# Phase 1 review — Grok

Reviewer: Grok 4.6. Inputs: docs/PRD.md §7/§8 phase 1, §9, docs/RUNNER-SPEC.md, docs/lessons.md, docs/DECISIONS.md (#025, #review-1-r6 L1–L11, #review-1-r5 K1–K9, #002-outline-feel, #019–#036), docs/V1.1-BACKLOG.md, `git diff 052e206aa8314ee1e4e454668a0496b3b96f4128...56180784fe4eeb5b289be07117f9403f17c332b5` (attempt r7), /logs/ci/1.verify.r7.g2h/accepted/ (this attempt's verifier; verifier_id `1.verify.r7.g2`), /logs/ci/1.verify.r7.g2h/{accepted.json,a1/result.json,a1/run.json}, /logs/ci/{1.verify.g1h,1.verify.r1h,1.verify.r2.g1h,1.verify.r3.g1h,1.verify.r4.g1h,1.verify.r5h,1.verify.r6h}/accepted/, /logs/human/1.9/accepted.json + a2.md, /logs/tasks/, /logs/state/summary.md. Paths read under /logs/reviews/: `/logs/reviews/1/r7/{implementation_sha,phase_base_sha,verification_sha,verifier_id}` (metadata only); directory listing of `/logs/reviews/1/r7/` (names only); `/logs/reviews/1/r6/{verdict,grok/report.md,grok/status.json}` (prior attempt's own grok files, not a sibling of this run). Sibling directories `/logs/reviews/1/r7/{claude,sol}/` were not opened. Cold build: cited /logs/ci/1.verify.r7.g2h/accepted/ (Grok). Commands run: `git rev-parse HEAD`; `git log --oneline` / `git diff --stat` of `052e206…5618078`; python `digestDir` (ralph/lib/util.mjs algorithm: `${rel}\0${sha256File}\n`) over accepted `test-logs` and `playwright-report`; greps of format/positions/schema/toggle/store/DevEditor, e2e specs, lockfile, CLAUDE.md/AGENTS.md, backlog, secret patterns, skip/only; read of installed `mdast-util-to-markdown@2.1.2/lib/{util/container-phrasing.js,handle/{root,link,inline-code}.js,util/format-link-as-autolink.js}` and `prosemirror-model@1.25.11/dist/index.js` `marks()`. No build, no tests, no `pnpm why`. No browser: the 1.49–1.54 probes named in the r7 task text are discharged from the tree plus the three-OS Playwright logs, not from an independent drive. Isolated `playwright test latency.spec.ts --workers=1` was not run (no scratch copy).

## Gate table

| Criterion (from PRD §7) | Result (pass / fail / unverifiable) | Evidence (command + output line, or file:line) |
|---|---|---|
| Vite `/dev/editor` with the dev-only "Load fixture…" / "Copy Markdown" bar | pass | `apps/desktop/src/main.tsx` routes `/dev/editor`. `DevEditor.tsx:229-250` renders both buttons (`data-testid` `load-fixture`, `copy-markdown`). `copyMarkdown` (`DevEditor.tsx:210-223`) flushes, then writes `format(store.getState().document.root)` and sets status `"Copied Markdown"`. |
| Michael writes ~1,000 words in the rendered view | pass (count not recorded) | `/logs/human/1.9/a2.md` body "Rendered editor"; `accepted.json` attempt 2, `outcome: ACCEPT`. Word count is still not in the record (#002, F16; backlog line for 2.9). Observed SHA `ca1cd3d` is an ancestor of `5618078`. |
| Toggles to source | pass | `a2.md` "Source toggle": shortcut is Cmd+/. Playwright `editor-toggle.spec.ts` uses `ControlOrMeta+/`. workflow.log: ubuntu `93 passed (1.2m)`, macos `93 passed (1.8m)`, windows `93 passed (1.4m)`. |
| Copied Markdown round-trips (Typora) | pass | Finding 1 of r6 (L1) is closed: `SPLIT_PAIR` (`format.ts:203-213`) names the two-reference join; `widenSplitSurrogateReferences` (`:233-260`) widens it to one code-point reference. `strikethrough-flanking.test.ts:561-680` nine ordered mark pairs × three astral characters, five position classes, BMP and whitespace-inner absence, five invariant-A sources; `formatWithMap` `unresolved` empty. Playwright `editor-astral-between-runs.spec.ts:141-188` (Sol's load-then-delete, Copy, `toBe`, `format(parse(·))`, `textValuesOf` vs `textContent`) inside the 93/93 × 3 OS run. L2 closed: `installAutolinkFallback` (`format.ts:335-361`) keeps `<…>` only when `autolinkCarriesUrl`; `autolink-literal.test.ts:61-96` enumerates `safe.js:147` and every `unsafe.js` / gfm-table `atBreak` character; `autolink-backslash.md` canonical is the resource form; `autolink-literal.canonical.md` remains `See <https://example.com/pens> for details.` (not globally `resourceLink: true`). a2.md Typora check is the 1.9 record of F1, not these trees. L9 (URL-shaped text the editor holds as text) stays a representation decision on the backlog, premise true. |
| Outline prototype tried; verdict recorded (DECISIONS #002) | pass | `/logs/human/1.9/accepted.json` `payload.verdictText` copied to `docs/DECISIONS.md` `#002-outline-feel`. Three re-scopes stay Phase 3 inputs; editor/layout wishes stay Phase 2. Playwright `outline-produce.spec.ts` in the 93/93 × 3 OS run. G11's bold-span line is on the backlog. |
| Typing latency p95 with 10k words on `/dev/editor` (< 16 ms or a follow-up task at reconciliation) | pass (follow-up taken; isolated not rerun here) | Protocol: `e2e/web/latency.spec.ts` doc comment (keydown → markdown-pane mutation for rendered; keydown → `.cm-content` for source; source `commit` line, G9/1.31). Isolated `--workers=1` not rerun (no scratch). This SHA's accepted CI workflow.log, 120 presses, 2 workers: **rendered 10k pane p95 ubuntu 30.1 / macos 13.6 / windows 23.1 ms**; source 10k surface p95 1.5 / 0.9 / 1.4 ms (pane n=0 during a burst); source 10k **commit** p95 83.2 / 73.5 / 77.2 ms (n=5 bursts). The PRD alternative (“or a follow-up task”) was taken at r0 (1.17); G10 names the quiet single-worker run as the incremental-`pmToMdast` trigger's input (r5 isolated 9.8 / 10.4 ms). I do not re-open F5 as required. |
| Full suite green on 3 OSes at the implementation SHA | pass | `/logs/ci/1.verify.r7.g2h/accepted.json` `sha` = `56180784fe4eeb5b289be07117f9403f17c332b5` = `/logs/reviews/1/r7/implementation_sha` = `verification_sha` = `git rev-parse HEAD`. Gate attempt **a1** (run 35688068381, ref `ci/1.verify.r7.g2/a1`). `result.json` `conclusion: success`. Vitest `3754 passed` / 42 files / **no expected fail** on ubuntu, macos, windows. cargo `0 passed; 0 failed; 0 ignored` × desktop_lib, desktop, doc-tests on all three. lint.log on all three: `eslint . && node scripts/check-deps.mjs && pnpm -r --if-present typecheck`; `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`; six `typecheck: Done`. ubuntu `ralph-test.log`: `# tests 83 / # pass 83 / # fail 0`. Prior gates `1.verify.r7h` and `1.verify.r7.g1h` GATE-FAILED (DECISIONS #035, #036); this verifier is the second repair. |
| e2e/web Playwright on all three runners (1.verify.r7.g2 ciAcceptance) | pass | workflow.log: `93 passed` on ubuntu (1.2m), macos (1.8m), windows (1.4m). Spec-file count from the directory: 26 (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*'`), including `editor-astral-between-runs.spec.ts`, `editor-toggle-inline-html.spec.ts`, `editor-toggle-cell-end.spec.ts`, `editor-toggle-run-end.spec.ts`, plus `tests/placeholder.spec.ts`. Merged artifact has `playwright-report-{ubuntu,macos,windows}-latest/index.html`. G4 on windows is the click route (`editor-soft-line-breaks.spec.ts:160-181`), green at a1. |
| 1.1 schema 1:1; corpus `format(pmToMdast(mdastToPM(parse(x)))) === format(parse(x))`; raw-html bytes | pass | Index keys = **62** (includes `astral-between-runs.md`, `autolink-backslash.md`, `inline-html.md`). ubuntu test.log: 42 files, 3754 passed, no expected fail. `editor-fixed-point.test.ts` includes the `editor fixed point` typing legs, the deletion / mark-edge / link-edge / list-split / mark's-neighbour legs, and L8's `expectTextMatchesEditorIgnoringWhitespace` (`:488`, called from the corpus callers at `:1447`–`:1611`). |
| 1.2 `formatWithMap` / `nodeAt`; every paragraph startLine; 200 seeded positions; map is pure | pass | `positions-corpus.test.ts` `it.each(names)("%s: maps every node of the tree, and nothing is unresolved")` over every index key; `grep` of `OUTSIDE_ENCODED_NEIGHBOUR` / `it.fails` under `packages/` empty. Editor-seeded family `position-map-editor-leg.test.ts` titled for the leg. `rewrittenEmissions` (`positions.ts:668-693`) names first / last / both code-point references and the html-eol fourth candidate. |
| 1.3–1.8 Playwright acceptances | pass | Inside the 93/93 × 3 OS run: 22 input scenarios, 8 reveal, live source colours, 6 undo, toggle+200 seeded, outline Produce, plus r1–r7 specs (trailing-space, clipboard, cursor, empty-cell, latency including commit line, source-view undo, soft-line-breaks with 1.56 click for G4, trailing-break, mark-edge, toggle-inside-window, break-in-mark, link-edge, list-paste, strikethrough-neighbour, toggle-trailing-break, astral-neighbour, toggle-encoded-neighbour, astral-between-runs, toggle-inline-html, toggle-cell-end, toggle-run-end). |
| 1.11 macOS keymap tests: both platforms, presence and absence, four chords, no skip | pass | `store.test.ts` `PLATFORMS`; `toggle.test.ts` the same shape. macos-latest vitest 3754/3754, no expected fail. `grep` of `e2e/web` + `packages` + `apps` + `tests` for `.skip(` / `.only(` / `runIf` / `skipIf`: empty. |
| 1.12 Playwright in `ci.yml` on all three runners | pass | workflow.log browser-install + `Playwright (e2e/web)` on ubuntu/macos/windows; merge-logs `playwright-report` with three OS directories. |
| Fresh-clone cold build | pass (cited CI) | `/logs/ci/1.verify.r7.g2h/accepted/`: `pnpm install --frozen-lockfile` then lint / test / cargo / Playwright / ubuntu `bash ralph/test/run.sh` on ubuntu-latest, macos-latest, windows-latest at `5618078`. Artifact `test-logs` digest `738f676a5570ef5fc145c7b536f88a43593e4fe0637219dc765c09efb4361130` / 125916 bytes / 10 files MATCH `digestDir` of `/logs/ci/1.verify.r7.g2h/accepted/test-logs`. Artifact `playwright-report` digest `331ce1f1fbe4fa36c9994e8926e28238a1f277d3b0eccb51310ece3aa768b28a` / 2199061 bytes / 3 files MATCH. `accepted` holds the same trees as `a1`. |
| §9.4 verify tasks: no lockfile change; no API key in tree | pass | `git show 5618078` (1.verify.r7.g2): journal only. `git diff --stat 218b2a9..5618078` touches no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock`. Secret-pattern walk of the working tree (`sk-ant-`, `ANTHROPIC_API_KEY=`, `OPENAI_API_KEY=`, `xai-`, `ghp_`, `github_pat_`, `AKIA`): empty outside docs that name the patterns. I9 redaction present at `docs/reviews/phase-0-r0-claude.md:26` (`<owner address>`). |
| r7 check: third `SPLIT_PAIR` form vs `container-phrasing.js` (three branches, lines read); `classifyCharacter`, `encodeInfo`, `handleDelete`, `handleRoot`, wrapper installation unchanged by 1.49 | pass | Installed `container-phrasing.js` 2.1.2: lines 88–94 `encodeAfter` (first UTF-16 unit), 100–115 `encodingInfo.before` (last unit of the previous result), 60–80 trailing line ending before `html`. The join of 88–94 and 100–115 on one two-unit child is `&#xD83D;&#xDE00;`, the third alternative at `format.ts:209-210`. `git show 6d78458` `handleRoot` still only `installSurrogateWidening` then phrasing/flow (`format.ts` at 1.49); 1.50 adds `installAutolinkFallback` beside it (`:369-373`). `classifyCharacter` / `encodeInfo` / `handleDelete` function bodies `UNCHANGED` vs 1.40 (`a049570`). Wrapper still `original.call(state, …)` then `widenSplitSurrogateReferences` (`:287-289`); `state.astralWidened` marker (`:284-285`). |
| r7 check: `link` wrapper vs `link.js`, `format-link-as-autolink.js`, `safe.js`, `unsafe.js`; criterion; `autolink-literal.canonical.md` | pass | Installed `link.js:28-45` hides `state.stack`, writes `containerPhrasing` between `<` and `>`; `format-link-as-autolink.js:16-33` keeps that form when text === url (or `mailto:`+text) and the url has no `\0- <>`. `autolinkCarriesUrl` (`format.ts:299-303`) is that round trip. Fallback sets `resourceLink` for one call (`:342-349`). `autolink-literal.test.ts:61-96` one row per character `safe()` rewrites with the stack hidden (`\` presence; `# ) * + - . < = > [ _ \` ~ \| :` absence / resource). Canonical sibling of `autolink-literal.md` is still the autolink form (byte-identical to what format wrote before 1.50; source is the GFM literal, not a 1.50 rewrite). |
| r7 check: `rewrittenEmissions` fourth candidate and `inlineCode` spelling table vs the third branch and `inline-code.js` | pass | `rewrittenEmissions` (`positions.ts:630-693`) derives four candidates from the three branches plus the wrapper's three forms; branch 3 × branch 1 is the head form with its trailing line ending replaced (`:686-690`). `inlineCodeSpelling` (`:704-728`) reads fence / optional padding / value / padding / fence from `inline-code.js:14-68`; first, middle and last positions in `positions-html-eol-inline-code.test.ts`. Doc comment `positions.ts:81-92` states fence and padding owned by no character. |
| r7 check: cell-end rule and corpus inverse leg (1.52); position set from the doc; destructive-transaction seed | pass | `position-map-inverse.test.ts:11-47` two legs, positions computed from each doc; deletion-leg seed from `typing-legs.ts`. Named exclusion for `code_block` interiors (`CODE_BLOCK_MEMBERS`, backlog `[1.52, code block interior]`, hard stop `2.verify`). Playwright `editor-toggle-cell-end.spec.ts:136-177` types `X` into `zq`, not `ab`. |
| r7 check: boundary rule (1.53) vs `ResolvedPos.marks()`; never a list of mark types | pass | `innermostAt` (`toggle.ts:304-317`) chooses `Mark.sameSet(boundary.marks, boundary.before.marks) ? ending : best`. `inlineBoundary` (`:275-283`) is `doc.resolve(pos).marks()`. Installed `prosemirror-model` `dist/index.js:944-967`: inside a text node the node's marks; at a boundary the node before's marks less `inclusive: false` marks the other side lacks. No mark-type list in `toggle.ts`. `toggle.test.ts:1374` matrix; Playwright `editor-toggle-run-end.spec.ts:144-188` `Alpha ~~beta.X~~ gamma`. |
| r7 check: every `press` helper under `e2e/web/` polls; whitespace-removed text equality in every `expectNoForbiddenEntity` caller | pass | Counted-press helpers (`editor-astral-between-runs.spec.ts:80-85` and the siblings) poll `caret` after every press. `expectTextMatchesEditorIgnoringWhitespace` (`editor-fixed-point.test.ts:488-495`) is the corpus-caller instrument; ten call sites in that file's corpus legs. Exact `expectTextMatchesEditor` remains on the two mark's-neighbour legs (L8's re-reading). |
| r7 check: browser routes (Sol's `*a.* 😀 *(b)*`; Grok's typed twin; 1.50 loaded `Foo\_bar`; 1.51–1.53 toggle-then-type) | pass (from the tree + three-OS logs; no independent drive) | Sol's route is `editor-astral-between-runs.spec.ts:141-188` (load, Backspace, Delete, Copy, `toBe`, no `&#xD`). Grok's typed twin is not a second spec: the unit guards build the same two-mark tree (`strikethrough-flanking.test.ts:536-572`) and the e2e reaches it by a destructive edit; input-rule typing from blank is not separately driven. 1.50 has no Playwright case (task text: the editor builds no autolink; the corpus leg is the instrument); `autolink-backslash.md` is the loaded `Foo\_bar` source. `editor-toggle-inline-html.spec.ts`, `editor-toggle-cell-end.spec.ts`, `editor-toggle-run-end.spec.ts` in the 93/93 × 3 OS run. Chord-guard: `grep` of Home/End in the new specs empty of caret chords. |
| r7 check: `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` | pass (cited CI + lockfile; `pnpm why` not run) | `grep @codemirror/commands pnpm-lock.yaml` empty. lint.log on all three OS: `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`. `packages/editor/test/no-codemirror-history.test.ts` green. `source.ts` installs no keymap (1.19). |
| r7 check: backlog revisit triggers | pass, none fired | L9 (`BACKLOG.md:85`) and L11 (`:86`) premises hold: URL-shaped text is a valid escaped representation, adjacent same-mark runs are unreachable from the editor; hard stop `2.verify`. `[1.52, code block interior]` (`:91`) is the inverse-leg exclusion, trigger not fired. `[1.53, a raw source keystroke before a punctuation-edged run]` (`:92`) is the two-view byte disagreement at a run's *start*, pinned in the 1.53 matrix, hard stop `2.verify`. `[1.46, found outside scope]` (`:82-83`) gained a third-instance evidence line from #036; G4 now a click, hard stop `2.verify`. `[review-1-r5 K6]` identifier half (`:78`) hard stop `2.verify`. `[review-1-r5, Claude risk 1]` one-mark astral positions (`:79`) hard stop `2.verify`. Vertical-arrow third site (`:70`) hard stop `2.verify`. J7 heading-entity (`:71`) hard stop `3.verify`. F11 `App.tsx:12` `invoke("greet", { name })` still present, hard stop `2.verify`. |
| r7 check: r6 class — a character reference that does not decode to the tree's text, a node left unresolved, `toRendered(toSource(p)) !== p` or the two views taking a typed character differently, with a non-BMP seed per neighbour class and per inline position class, one seed per installed handler delimiter, one seed per pair of parent rewrites | pass on the named families; remaining members are the backlog lines above | Neighbour-class ASCII/astral at one mark and between two marks: `strikethrough-flanking.test.ts` (1.45 + 1.49). Pair of parent rewrites on one character: `SPLIT_PAIR`'s third form and `rewrittenEmissions`' head+eol candidate (`positions.ts:659-662`). Delimiter seeds: table `\|` (`table-escaped-pipes.md` in the index), inline-code backtick (`inline-code.js` padding + `inlineCodeSpelling` guards), URL characters `safe()` rewrites (`autolink-literal.test.ts` `ROWS`). Code-block interior and the run-start two-view bytes are named exclusions, not silent U+FFFD. |
| Independent read of r6 L1–L6 (DECISIONS #review-1-r6 grok row; first read = assembler's rewrites, then the handler, then the pin's neighbouring branch) | — | A read-only review **could** have found **L1** from the tree at `c2f6526` without a transaction or a second non-BMP fixture: `container-phrasing.js:91-94` and `:103-111` each encode one UTF-16 unit, and `SPLIT_PAIR` there named only a reference beside a *raw* mate, so one two-unit child between two punctuation-edged runs is `&#xD83D;&#xDE00;`. Severity: **blocker**. That is the read r6 made. **L2**: **could** have been found the same way by opening `link.js:28-45` (stack hidden) and `safe.js`'s backslash rule against `parse("see https://x.y\\ end\\n")` — no browser, no non-BMP seed. Severity: **blocker**. r6 did not open that file. **L3**: r6 named `container-phrasing.js:60-72` as risk 2 without a finding. The read that makes it a finding is not a browser route: `rewrittenEmissions` at `c2f6526` enumerated only first/last/both code-point references; a text child whose handler returned `alpha\\n` is rewritten to `alpha ` before an `html` sibling; none of those three candidates is that string; `formatWithMap(parse("alpha\\n<i>beta</i>\\n"))` reports `unresolved ["0.0"]`. Severity: **should-fix Required**. **L4**: **could** — `positions.ts` gave a spelling table only to `text`; `inline-code.js` writes fences the map never entered, so `toSource` of a caret inside `` `cd` `` answers the span's start. Severity: **should-fix**. **L5**: **could** — `delimiterPosition` at a cell's end answered the row; `toRendered(toSource(p))` at that end is the row's first position. Severity: **should-fix**. **L6**: **could** — `innermostAt`'s "a boundary belongs to the later node" vs `ResolvedPos.marks()` (`dist/index.js:944-967`, the node *before* at an inclusive run's end). Severity: **should-fix**. |
| r6 dispositions L1–L11 verified against the tree | pass | L1 **fixed** (third `SPLIT_PAIR` form, pair guards, fixture, Sol's Playwright). L2 **fixed** (per-node resource fallback, `ROWS` from `safe()`/`unsafe.js`, fixture, `formatWithMap` case). L3 **fixed** (fourth candidate, `inline-html.md`, toggle-then-type Playwright). L4 **fixed** (`inlineCodeSpelling`, first/middle/last). L5 **fixed** (cell-end rule, corpus inverse, two Playwright cases). L6 **fixed** (`$pos.marks()`, matrix, run-end Playwright). L7 **fixed** (per-step caret poll; G4 later rerouted around the `[1.46]` class by 1.55/1.56, test-only). L8 **half taken** (whitespace-removed equality in corpus callers; the two neighbour legs keep exact equality). L9 **deferred**, premise true. L10 **recorded**. L11 **deferred**, premise true. |
| r5 K1–K9, r4 J1–J9, r3 I1–I10, r2 H1–H9, r1 G1–G12, r0 F1–F16 re-confirmed | pass | K1 closed for one mark and for two (L1). K2 closed for the two edge encodings and for the html-eol branch (L3). K3–K9 as at r6. J1 BMP and astral one-mark and two-run closed. J3's remaining node classes are L4/L5 (taken) and the code-block sibling (backlog). J6 trigger not fired (the three vertical-arrow sites still green on this gate; G4 no longer uses them). I1 `giveUpEdges`; I2 `listItemSpread`; I3/I4 `linkEdgeStops`; I5 deletion leg; I6 chord guard; I7 this gate a1 success (the r7h / r7.g1h failures were spec routes, #035/#036); I8 tautology grep empty; I9 redaction present; I10 ASCII policy. H1–H6, H8 closed; H7/H9 runner/backlog. G1 `DevEditor.tsx:215` flush; G2 source undo flush; G3 `INLINE_PRE`; G4–G6 closed; G7–G12 backlog or comments. F1 family closed for whitespace/breaks/link edges and for flanking `delete`/`*`/`**` including the two-run astral join; F2 `parseDOM`; F3 empty-cell map; F4 cursor; F5 1.17; F6 quoting; F7 no `codemirror` / no `@codemirror/commands` in the lockfile; F8 `check-deps`; F9–F10 identity/guards; F11–F16 nits/backlog (`App.tsx:12` still `invoke("greet")`). |
| Promote-lessons carried by 1.verify.r7 | pass | `CLAUDE.md:51-52` and `AGENTS.md` identical: two-rewrite / two-run-neighbour bullet (discharged by `strikethrough-flanking.test.ts` pair describe and `astral-between-runs.md` in the index); cursor-map inverse / monotonicity bullet (discharged by `position-map-inverse.test.ts` two legs). Grok's r6 lessons recorded, not promoted (`[1.verify.r7]` journal). |

## Test counts and coverage

Vitest: 3754 passed / 0 failed / 0 expected fail across 42 files, all three OS (`/logs/ci/1.verify.r7.g2h/accepted/test-logs/test-logs-{ubuntu,macos,windows}-latest/test.log` summary lines). cargo test: 0 passed / 0 failed / 0 ignored × desktop_lib, desktop, doc-tests, all three OS (Rust surface is Phase 2). e2e: 93 passed / 0 failed on ubuntu, macos, windows (workflow.log Playwright steps); runner conformance 83/83 ubuntu only. Coverage (v8, all three OS identical): statements 99.65 % / branches 97.85 % / functions 100 % / lines 100 %. `format.ts` 100/100/100/100. Uncovered: `positions.ts:216,573,577,718`, `sentences.ts` / `sidecar.ts` pre-existing, `schema.ts:722-723` (`image.alt ?? ""`), `toggle.ts:…73,552-554,658`. Delta vs Phase 0 close (progress.md r2, 99.45 / 96.98 / 100 / 100): +0.20 / +0.87 / 0 / 0. Delta vs r6 (`1.verify.r6h` 99.68 / 97.9 / 100 / 100): −0.03 / −0.05 / 0 / 0 (new branches in `positions.ts` / `toggle.ts`). Isolated latency `--workers=1` not rerun.

## Findings (≤ 20, most severe first)

None.

## Three riskiest things

1. The `[1.46]` class (a native caret motion reverted by ProseMirror before the next synthetic key on windows-latest) has now failed three gates by three routes on one visual position (blind lefts, polled lefts, up-then-rights). G4 is a click; `editor-toggle-encoded-neighbour.spec.ts:176`, `editor-toggle-inline-html.spec.ts:165`, `editor-toggle-cell-end.spec.ts:156` and `editor-toggle-run-end.spec.ts:171` still wait 200 ms so a chord can follow an arrow. The first red landing assertion, or a chord in the same frame as an arrow, is the trigger (`BACKLOG.md:82-83`).
2. `toRendered(toSource(p)) === p` does not hold inside a fenced code block (`position-map-inverse.test.ts:36-47`; `code-fence-basic.md`, `code-fence-nested.md`, `essay-fixture.md`). A toggle from inside a fence lands on the fence line. Named exclusion, hard stop `2.verify` (`BACKLOG.md:91`).
3. At the *start* of a punctuation-edged run the two views still take a typed letter differently (`BACKLOG.md:92`): rendered writes `Alpha &#x58;*(beta.)* gamma`, source writes bytes that un-form the run. The 1.53 matrix pins both; Copy of the rendered form still round-trips. Hard stop `2.verify`.

## Class-level lessons (for docs/lessons.md)

- LESSON: naming a parent rewrite in a risk list is not a finding until the retry table is shown not to contain the string that rewrite writes — `rewrittenEmissions` at `c2f6526` had first/last/both code-point references and not `alpha ` for a child whose handler returned `alpha\n`, which is already `unresolved ["0.0"]` on `parse("alpha\n<i>beta</i>\n")` with no browser and no transaction → the first read of a flanking review walks each branch of `container-phrasing.js` onto one concrete emission and asks whether `locateEmission` can find it.
- LESSON: a read-only review that closes `handleDelete` against `emphasis.js` and never opens `link.js:28-45` misses the autolink backslash (stack hidden, `safe()` still doubles `\`, nothing inside `<…>` decodes) — L2 was in the tree at `c2f6526` on `parse("see https://x.y\\ end\n")` → after the assembler's three branches, the next read is every installed handler that hides the construct stack or writes a delimiter the app does not own.
- LESSON: an enumeration of mark × neighbour that builds one run per case cannot fail on a neighbour two runs share, and a wrapper whose regex names each rewrite alone cannot fail on their concatenation → the shortest value on which two rewrites fire on the same character is a required cell, not an extra example.
