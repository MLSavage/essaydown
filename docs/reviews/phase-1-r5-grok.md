# Phase 1 review — Grok

Reviewer: Grok 4.6. Inputs: docs/PRD.md §7/§8 phase 1, §9, docs/RUNNER-SPEC.md, docs/lessons.md, docs/DECISIONS.md (#review-1-r4, #002-outline-feel, #019–#025), docs/V1.1-BACKLOG.md, `git diff 052e206aa8314ee1e4e454668a0496b3b96f4128...3dd9c4b042bf14f9a3a21d1b12a5de79bc4588be` (attempt r5), /logs/ci/1.verify.r5h/accepted/ (this attempt's verifier; verifier_id `1.verify.r5`), /logs/ci/{1.verify.g1h,1.verify.r1h,1.verify.r2.g1h,1.verify.r3.g1h,1.verify.r4.g1h}/accepted/, /logs/ci/1.verify.r5h/{accepted.json,a1/result.json,a1/run.json}, /logs/human/1.9/accepted.json + a2.md, /logs/tasks/, /logs/state/summary.md. Paths read under /logs/reviews/: `/logs/reviews/1/r5/{implementation_sha,phase_base_sha,verification_sha,verifier_id}` (metadata only); `/logs/reviews/1/r4/grok/report.md` (prior attempt, derived per #025 — not used as evidence). Sibling directories `/logs/reviews/1/r5/{claude,sol}/` were not opened. Cold build: cited /logs/ci/1.verify.r5h/accepted/ (Grok). Commands run: `git rev-parse HEAD`; `git log --oneline` / `git diff --stat` of `052e206…3dd9c4b` and of `6c0e601…3dd9c4b` and of `d871506..3dd9c4b`; python `digestDir` (ralph/lib/util.mjs algorithm) over accepted `test-logs` and `playwright-report`; greps of format/schema/toggle/store/DevEditor, e2e specs, lockfile, CLAUDE.md/AGENTS.md, backlog, secret patterns, skip/only, `process.platform`, `it.fails`, `toDocumentEnd`; read of installed `mdast-util-to-markdown/lib/{util/encode-info.js,handle/emphasis.js,configure.js,join.js,util/format-heading-as-setext.js}`, `mdast-util-gfm-strikethrough/lib/index.js` `handleDelete`, `micromark-extension-gfm-strikethrough/lib/syntax.js` `_close`/`_open`, `mdast-util-gfm-autolink-literal/lib/index.js` `gfmAutolinkLiteralToMarkdown`, `mdast-util-gfm-table/lib/index.js` `gfmTableToMarkdown`, `micromark-util-classify-character`. No build, no tests, no `pnpm why`. No browser: the 1.40–1.44 probes named in the r5 task text are discharged from the tree plus the three-OS Playwright logs, not from an independent drive. Isolated `playwright test latency.spec.ts --workers=1` was not run (no scratch copy).

## Gate table

| Criterion (from PRD §7) | Result (pass / fail / unverifiable) | Evidence (command + output line, or file:line) |
|---|---|---|
| Vite `/dev/editor` with the dev-only "Load fixture…" / "Copy Markdown" bar | pass | `apps/desktop/src/main.tsx` routes `/dev/editor`. `DevEditor.tsx:229-250` renders both buttons (`data-testid` `load-fixture`, `copy-markdown`). `copyMarkdown` (`DevEditor.tsx:210-223`) flushes, then writes `format(store.getState().document.root)` and sets status `"Copied Markdown"`. |
| Michael writes ~1,000 words in the rendered view | pass (count not recorded) | `/logs/human/1.9/a2.md` body "Rendered editor"; `accepted.json` attempt 2, `outcome: ACCEPT`. Word count is still not in the record (#002, F16; backlog line for 2.9). Observed SHA `ca1cd3d` is an ancestor of `3dd9c4b`. |
| Toggles to source | pass | `a2.md` "Source toggle": shortcut is Cmd+/. Playwright `editor-toggle.spec.ts` uses `ControlOrMeta+/`. workflow.log: ubuntu `86 passed (53.0s)`, macos `86 passed (2.2m)`, windows `86 passed (1.2m)`. |
| Copied Markdown round-trips (Typora) | pass | Finding 1 is the map of the encoded neighbour, not the bytes. `copyMarkdown` is `format` of the store root (`DevEditor.tsx:216`). `format.ts:160-166` registers `{ handlers: { delete: handleDelete } }` after `gfmStrikethroughToMarkdown()`; `configure.js:75-78` `Object.assign`s handlers in extension order, so the app handler runs — `strikethrough-flanking.test.ts:233-235` asserts `state.handlers.delete === handleDelete`. `handleDelete` (`format.ts:126-149`) mirrors `emphasis.js:19-58`'s `encodeInfo` use for `~`. GFM close (`syntax.js:137-138`) is `_close = !before \|\| before === 2 && Boolean(after)` (`2` = punctuation): a punctuation-edge `delete` beside a letter now encodes the letter (`strikethrough-flanking.test.ts:243` `Alpha ~~beta.~~&#x67;amma delta\n`, parse holds one `delete`; `:181-189` invariant A on `~~a.~~&#x62;`). e2e `editor-strikethrough-neighbour.spec.ts:133` and `:156` (Claude's and Sol's one-Backspace routes, Copy spy) green on all three OS (workflow.log ubuntu 2645/2647, macos 4738/4739, windows 6319/6321). a2.md Typora check is the 1.9 record of F1, not this tree. |
| Outline prototype tried; verdict recorded (DECISIONS #002) | pass | `/logs/human/1.9/accepted.json` `payload.verdictText` copied to `docs/DECISIONS.md` `#002-outline-feel`. Three re-scopes stay Phase 3 inputs; editor/layout wishes stay Phase 2. Playwright `outline-produce.spec.ts` in the 86/86 × 3 OS run. G11's bold-span line is on the backlog. |
| Typing latency p95 with 10k words on `/dev/editor` (< 16 ms or a follow-up task at reconciliation) | pass (follow-up taken; isolated not rerun here) | Protocol: `e2e/web/latency.spec.ts` doc comment (keydown → markdown-pane mutation for rendered; keydown → `.cm-content` for source; source `commit` line, G9/1.31). Isolated `--workers=1` not rerun (no scratch). This SHA's accepted CI workflow.log, 120 presses, 2 workers: **rendered 10k pane p95 ubuntu 17.1 / macos 19.1 / windows 27.0 ms**; source 10k surface p95 0.9 / 2.7 / 1.7 ms (pane n=0 during a burst); source 10k **commit** p95 46.0 / 120.8 / 88.7 ms (n=5 bursts). The PRD alternative (“or a follow-up task”) was taken at r0 (1.17); G10 names the quiet single-worker run as the incremental-`pmToMdast` trigger's input (r4 isolated 9.8 / 10.1 ms). I do not re-open F5 as required. |
| Full suite green on 3 OSes at the implementation SHA | pass | `/logs/ci/1.verify.r5h/accepted.json` `sha` = `3dd9c4b042bf14f9a3a21d1b12a5de79bc4588be` = `/logs/reviews/1/r5/implementation_sha` = `verification_sha` = `git rev-parse HEAD`. `result.json` `conclusion: success`, run 34705334786, ref `ci/1.verify.r5/a1`. Vitest `3061 passed \| 1 expected fail (3062)` / 38 files on ubuntu, macos, windows. cargo `0 passed; 0 failed; 0 ignored` × desktop_lib, desktop, doc-tests on all three. lint.log on all three: `eslint . && node scripts/check-deps.mjs && pnpm -r --if-present typecheck`; `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`; six `typecheck: Done`. ubuntu `ralph-test.log`: `# tests 83 / # pass 83 / # fail 0`. The remaining expected fail is 1.40's pin (`positions-corpus.test.ts:114`), finding 1. |
| e2e/web Playwright on all three runners (1.verify.r5 ciAcceptance) | pass | workflow.log: `86 passed` on ubuntu (53.0s), macos (2.2m), windows (1.2m). Nineteen spec files under `e2e/web/` (count from the directory, including `latency.spec.ts`, `editor-strikethrough-neighbour.spec.ts`, `editor-toggle-trailing-break.spec.ts`). Merged artifact has `playwright-report-{ubuntu,macos,windows}-latest/index.html`. |
| 1.1 schema 1:1; corpus `format(pmToMdast(mdastToPM(parse(x)))) === format(parse(x))`; raw-html bytes | pass | Index keys = 58 (includes `strikethrough-punctuation.md`). ubuntu test.log: 38 files, 3061 passed + 1 expected fail. `editor-fixed-point.test.ts` includes the `editor fixed point` typing legs, the **deletion** leg, the **mark-edge** leg, the **link-edge** leg, the **list-split** leg and the **mark's neighbour** legs (`:1466`, `:1487`). |
| 1.2 `formatWithMap` / `nodeAt`; every paragraph startLine; 200 seeded positions; map is pure | pass, with the encoded-neighbour hole | `positions-corpus.test.ts`; editor-seeded family `position-map-editor-leg.test.ts` titled for the leg, calling `formatWithMap`, `nodeAt`, `cursorMap`; deletion leg titled for **deletion**. The fixture `strikethrough-punctuation.md` is excluded from "maps every node" and pinned `it.fails` at `:114` — finding 1. |
| 1.3–1.8 Playwright acceptances | pass | Inside the 86/86 × 3 OS run: 22 input scenarios, 8 reveal, live source colours, 6 undo, toggle+200 seeded, outline Produce, plus r1–r5 specs (trailing-space, clipboard, cursor, empty-cell, latency including commit line, source-view undo, soft-line-breaks with 1.39 anchor-and-count, trailing-break, mark-edge, toggle-inside-window, break-in-mark, link-edge, list-paste, strikethrough-neighbour, toggle-trailing-break). |
| 1.11 macOS keymap tests: both platforms, presence and absence, four chords, no skip | pass | `store.test.ts` `PLATFORMS`; `toggle.test.ts` the same shape. macos-latest vitest 3061/3061 + 1 expected fail. `grep` of `e2e/web` + `packages` + `apps` for `.skip(` / `.only(` / `runIf` / `skipIf` / `process.platform`: empty. |
| 1.12 Playwright in `ci.yml` on all three runners | pass | workflow.log browser-install + `Playwright (e2e/web)` on ubuntu/macos/windows; merge-logs `playwright-report` with three OS directories. |
| Fresh-clone cold build | pass (cited CI) | `/logs/ci/1.verify.r5h/accepted/`: `pnpm install --frozen-lockfile` then lint / test / cargo / Playwright / ubuntu `bash ralph/test/run.sh` on ubuntu-latest, macos-latest, windows-latest at `3dd9c4b`. Artifact `test-logs` digest `6cae902bf70171786ecb11d459e4c00f5efbe761ec66a710af30b267c42f0994` / 114823 bytes / 10 files MATCH `digestDir` of `/logs/ci/1.verify.r5h/accepted/test-logs`. Artifact `playwright-report` digest `d99c767de487526a3633c59d0fc88c2127a4f3d72892fef95b8c03042311244f` / 2055521 bytes / 3 files MATCH. `accepted` holds the same trees as `a1`. |
| §9.4 verify tasks: no lockfile change; no API key in tree | pass | `git show 3dd9c4b`: `AGENTS.md`, `CLAUDE.md`, `docs/progress/journal-main.md` only. `git diff --stat d871506..3dd9c4b` touches no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock`. Secret-pattern walk of the working tree (`sk-ant-`, `ANTHROPIC_API_KEY=`, `OPENAI_API_KEY=`, `xai-`, `ghp_`, `github_pat_`, `AKIA`): empty outside docs that name the patterns. I9 redaction present at `docs/reviews/phase-0-r0-claude.md:26` (`<owner address>`). |
| r5 check: `delete` handler vs `encode-info.js` and GFM `_close`; shown to be the handler that runs | pass | App `encodeInfo` (`format.ts:85-99`) is the `*` column of `encode-info.js:41-82` (letter outside + punctuation inside → `{inside:false, outside:true}`; letter + whitespace → both; punctuation + whitespace → inside only). `handleDelete` (`format.ts:126-149`) is `emphasis.js:19-58` with marker `~`. Registration: `toMarkdownExtensions` (`format.ts:160-166`) pushes the extension then `{ handlers: { delete: handleDelete } }`; `configure.js:43-45` `map` is `Object.assign`; test `:233-235`. Guards: `strikethrough-flanking.test.ts:117-160` side × neighbour class (letter, digit, punctuation, whitespace) × inner class (punctuation, letter, inline code, link), plus whitespace-inside (`:162-178`) and invariant A (`:180-189`). |
| r5 check: every other handler `toMarkdownExtensions()` takes as-is | pass | `gfmTableToMarkdown()` (`mdast-util-gfm-table/lib/index.js:152-184`): `unsafe` for `\r`/`\n`/`|`/`:`/`-` in cells and at breaks; handlers `inlineCode` (pipe unescape in cells), `table`, `tableCell`, `tableRow` — no `delete`, no attention flanking. `gfmAutolinkLiteralToMarkdown()` (`mdast-util-gfm-autolink-literal/lib/index.js:50-76`): `unsafe` for `@`/`.`/`:` only, **no handlers**, cannot overwrite `delete`. `gfmStrikethroughToMarkdown()` is taken for `unsafe` + tracker/`peek`; its `handleDelete` (`lib/index.js:89-101`) is the one 1.40 replaced. |
| r5 check: neighbour leg (1.41) — what it deletes and where | pass | `deleteBesideEveryMarkedRun` (`typing-legs.ts:457-501`): for every maximal `emphasis`/`strong`/`delete` run in a paragraph, heading or cell, one `tr.delete` of the adjacent text sibling's first/last whitespace code point, collected then applied back-to-front. `punctuateThenDeleteAfterEveryMarkedRun` (`:524-560`): `.` inserted at the run's end with the run's marks, then the following whitespace deleted. Corpus legs `editor-fixed-point.test.ts:1466` / `:1487` titled for **the mark's neighbour**. e2e `editor-strikethrough-neighbour.spec.ts` (click at paragraph end asserted, counted `ArrowLeft`, one `Backspace`, Copy spy, `toBe` bytes, one `delete` in the parse) green × 3 OS. The leg does not call `formatWithMap` / `cursorMap` — finding 1's hole. |
| r5 check: setext predicate (1.42) vs installed `format-heading-as-setext.js` | pass | `needsBlankLine` (`schema.ts:685-699`) treats `right.type === "heading" && isSetextHeading(right)` as the paragraph/blockquote/list/table partner. `isSetextHeading` (`:710-730`) mirrors `format-heading-as-setext.js:14-33` with `state.options.setext` dropped (`stringifyOptions` `setext: false`): depth ≤ 2, non-empty `toString` (image `alt` counted, `:721-722`), line ending in a literal `value` (`/\r|\n/`) or a `break`. `list-spread.test.ts:121-130` two setext `KINDS`; pairwise sweep `:191`; four named `clause (setext heading)` cases `:257-294`. `join.js:26-35` is the rule being mirrored. |
| r5 check: `toSource` third clause (1.43) atom cases kept | pass | `cursorMap.toSource` (`toggle.ts:396-411`): spelling table (clause 1); `isEntered` (`:243-257` — paragraph/heading/tableCell/blockquote/listItem/list/table/tableRow only) then `lastNodeEnd` bounded by `inside.pmStart` (clause 2, `:405-407`); else the node's own start (`:409`) — inline atoms and unentered leaf blocks. `afterLastNode` (`:301-307`) only when `innermostAt` is null. Deletion leg `position-map-editor-leg.test.ts:594-663` folds both past-correspondence shapes as ordinary assertions; `grep -c 'it.fails' packages/editor/test/position-map-editor-leg.test.ts` = 0. e2e `editor-toggle-trailing-break.spec.ts:108` (click, Backspace ×3, `ControlOrMeta+/`, type `X`, pane `abcX\n`) green × 3 OS (ubuntu 2653, macos 4741, windows 6323). |
| r5 check: chord guard (1.44) able to fail on `Shift+End` | pass (from the test; no scratch mutation) | `CHORD = /"(?:[A-Za-z]+\+)+(Home|End)"/g` (`tests/no-caret-chords-in-e2e.test.ts:38`). Describe `:94-108` feeds `"Shift+End"`, `"Alt+Home"`, `"ControlOrMeta+Shift+End"`, `"ControlOrMeta+Home"` (count 1) and `"Home"`, `"ArrowLeft"` (count 0). Journal [1.44] records a scratch spec with `press("Shift+End")` turning the per-file `it` red, deleted in the same tool call. CI: `tests/no-caret-chords-in-e2e.test.ts (50 tests)` green on all three OS. Allowlist reasons (`:47-57`) name Blink's native line motion, not a keymap; `grep -c 'keymap' tests/no-caret-chords-in-e2e.test.ts` = 0. |
| r5 check: `pnpm why @codemirror/commands` and `node scripts/check-deps.mjs` | pass (cited CI + lockfile; `pnpm why` not run) | `grep @codemirror/commands pnpm-lock.yaml` empty. lint.log on all three OS: `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`. `packages/editor/test/no-codemirror-history.test.ts` (10 tests) green. `source.ts` installs no keymap (1.19). |
| r5 check: backlog revisit triggers | pass, one fired | `[1.40, found outside scope]` (`docs/V1.1-BACKLOG.md:77`): revisit is "the r5 reconciliation dispositions it"; hard stop `1.close`; the `it.fails` case itself — finding 1. `[1.37]` cursor-map line dispositioned by J3/1.43 (`BACKLOG.md:69`). Vertical-arrow third site (`:70`) hard stop `2.verify`, trigger not fired (no red landing, file unedited). J7 heading-entity (`:71`) hard stop `3.verify`. Serializer-handler class (`:72`) hard stop = next extension. F11 `App.tsx:12` `invoke("greet", { name })` still present, hard stop `2.verify`. |
| r5 check: further bytes `format` writes for a tree `parse` produces or the editor builds that do not parse back | pass on the walk | Table and autolink handlers have no flanking table (above). Built-in `emphasis`/`strong` already encode (`editor-fixed-point.test.ts:1659-1672` `x *a.*&#x62;`). Identity corpus includes `strikethrough-punctuation.md`. No second extension delimiter failed the handler walk. The remaining hole is the map of those encoded bytes, not the bytes — finding 1. |
| r4 dispositions J1–J9 verified against the tree | pass, J1's map half reopened | J1 **fixed** in `format.ts` (handler + 40 flanking tests + neighbour legs + two Playwright Copy cases). J2 **fixed** (`schema.ts:685-699`, two setext `KINDS`, four named clauses). J3 **fixed** for the two shapes 1.37 pinned (`toggle.ts:405-407`, deletion-leg ordinary assertions, Playwright toggle case). J4 **fixed** (`CHORD` any modifier chain). J5 **fixed** (reason strings). J6 backlog, third site named, hard stop `2.verify`. J7 backlog, hard stop `3.verify`. J8 recorded, journal append-only. J9 evidence only. The `[1.40]` map pin is the class J1 opened on the serializer's neighbour — finding 1. |
| Independent read of r4 J1 and J3 (DECISIONS #025 / r5 grok row) | — | A read-only review **could** have found J1 from the tree: `gfmStrikethroughToMarkdown`'s `handleDelete` (`lib/index.js:89-101`) writes `~~`…`~~` with none of `emphasis.js`'s `encodeInfo`; GFM `_close` (`syntax.js:138`) is false when `before` is punctuation and `after` is a letter — `format` of that tree is not a fixed point. Severity I would have rated: **blocker** (Copy Markdown of the one-Backspace tree reports "Copied" of bytes whose parse holds no `delete`). A read-only review **could** have found J3 from the tree: `toSource`'s no-spelling branch on an entered block answered `range.startLine`, next to 1.37's `it.fails`. Severity I would have rated: **should-fix Required** (Backspace then the 1.7 toggle is a Phase 1 action; Sol's type-`X` route). Neither finding is copied from a r5 sibling report. |
| r3 dispositions I1–I10 re-confirmed | pass | I1 `giveUpEdges` break clauses; I2 `listItemSpread`/`needsBlankLine` (setext pair closed as J2); I3/I4 `linkEdgeStops`; I5 deletion leg (J3 folded); I6 chord guard (J4/J5); I7 `#023` in the tree, this gate a1 success; I8 tautology grep under `packages`/`apps`/`e2e`/`tests` empty; I9 redaction present; I10 ASCII policy, Phase 3. |
| r2 H1–H9, r1 G1–G12, r0 F1–F16 re-confirmed | pass | H1–H6, H8 closed in the strip/undo/e2e; H7/H9 runner/backlog. G1 `DevEditor.tsx:215` flush; G2 source undo flush; G3 `INLINE_PRE`; G4–G6 closed; G7–G12 backlog or comments. F1 family closed for whitespace/breaks/link edges and for `delete` flanking (J1); F2 `parseDOM`; F3 empty-cell map; F4 cursor; F5 1.17; F6 quoting; F7 no `codemirror` / no `@codemirror/commands` in the lockfile; F8 `check-deps`; F9–F10 identity/guards; F11–F16 nits/backlog (`App.tsx:12` still `invoke("greet")`, hard stop `2.verify`). |
| Promote-lessons carried by 1.verify.r5 | pass | `CLAUDE.md:47-48` and `AGENTS.md` identical: serializer-handler / mark-edge enumeration bullet (discharged by `strikethrough-flanking.test.ts` side×neighbour×inner and the neighbour legs); kind-table-from-the-rule bullet (discharged by the two setext `KINDS` and the pairwise sweep). Sol's test-only-is-not-a-deferral lesson recorded, not promoted (`[1.verify.r5]` journal). |

## Test counts and coverage

Vitest: 3061 passed / 0 failed / 1 expected fail (3062) across 38 files, all three OS (`/logs/ci/1.verify.r5h/accepted/test-logs/test-logs-{ubuntu,macos,windows}-latest/test.log` summary lines). cargo test: 0 passed / 0 failed / 0 ignored × desktop_lib, desktop, doc-tests, all three OS (Rust surface is Phase 2). e2e: 86 passed / 0 failed on ubuntu, macos, windows (workflow.log Playwright steps); runner conformance 83/83 ubuntu only. Coverage (v8, all three OS identical): statements 99.56 % / branches 97.66 % / functions 100 % / lines 100 %. Uncovered: `format.ts:94` (whitespace-outside × whitespace-inside of `encodeInfo`; finding 2), `positions.ts:182,464`, `sentences.ts` / `sidecar.ts` pre-existing, `schema.ts:711,722-723`, `toggle.ts:409,448-450,505`. Delta vs Phase 0 close (progress.md r2, 99.45 / 96.98 / 100 / 100): +0.11 / +0.68 / 0 / 0. Delta vs r4 (`1.verify.r4.g1h` 99.66 / 97.91 / 100 / 100): −0.10 / −0.25 / 0 / 0, the new `encodeInfo` branches in `format.ts`. Isolated latency `--workers=1` not rerun.

## Findings (≤ 20, most severe first)

1. **should-fix Required** — `packages/core/src/positions.ts:455-456` — `placeChildren` looks each child's emission up with `container.indexOf(child.value, cursor)` and skips the child when `containerPhrasing` has already rewritten that value (`encodeAfter` / `attentionEncodeSurroundingInfo`: a letter beside a punctuation edge of an attention run becomes `&#x62;` while the child's emission is still `b`), so `formatWithMap` reports the text node in `unresolved` (`positions.ts:676-677`) and `nodeAt` over those bytes answers the paragraph; `toSource` of a caret *inside* that neighbour then falls through `toggle.ts:409-411` to `afterLastNode`, which cannot see the unresolved node and answers the end of the preceding run. The `[1.40, found outside scope]` backlog line's revisit is this review (`docs/V1.1-BACKLOG.md:77`, hard stop `1.close`); the pin is `positions-corpus.test.ts:114` `it.fails` over `OUTSIDE_ENCODED_NEIGHBOUR = ["strikethrough-punctuation.md"]`. Copy Markdown of the J1 tree is a fixed point (e2e `editor-strikethrough-neighbour.spec.ts:133/:156` assert the spy's bytes and one `delete`) and the 1.7 toggle at offset 0 of the neighbour happens to coincide with the run's end, but a caret inside `gamma` after `Alpha ~~beta.~~&#x67;amma delta` maps to the closing `~~`, so Cmd+/ then a typed character inserts at the strike, not in the neighbour — the same Phase 1 action J3 required a fix for, now on the tree 1.40's handler exists to write. 1.41's neighbour legs (`editor-fixed-point.test.ts:1466/:1487`) assert bytes and mark counts and never call `formatWithMap`/`cursorMap`. Fix: in `placeChildren` (or `wrapHandle`), when `indexOf` misses a `text` child, retry the value with its first and/or last character replaced by its `&#x…;` reference — the only two rewrites `container-phrasing.js` makes — and record that spelling in the node's `SpellingTable`; drop `OUTSIDE_ENCODED_NEIGHBOUR` and the `it.fails` case so the fixture is an ordinary "maps every node" row; add a Playwright case on the J1 seed (click, counted `ArrowLeft` to offset 0, Backspace, `ArrowRight` once into the neighbour, `ControlOrMeta+/`, type `X`) asserting the pane is not `Alpha ~~beta.~~X&#x67;amma delta`.

2. **nit** — `packages/core/src/format.ts:94` — `encodeInfo`'s whitespace-outside × whitespace-inside branch (`{inside: true, outside: true}`, `encode-info.js:66-68`) has no test and is the uncovered line of `format.ts` (coverage 98.07 % stmts / 95.45 % branches). `strikethrough-flanking.test.ts:162-178` covers letter-outside and punctuation-outside against a whitespace edge, not whitespace-outside. The editor's strip makes the tree rare; the table 1.40 copied still has the cell. Fix: one guard `paragraph(text("a "), del(text(" b")), text(" c"))` asserting both inside whitespace and the outside letters encode, matching the built-in handlers.

3. **nit** — `tests/no-caret-chords-in-e2e.test.ts:112` — the per-file `it` title still says "has no ControlOrMeta, Control or Meta chord with Home or End" after `CHORD` was widened to any modifier chain (`:38`, describe `:94`). A `Shift+End` hit would fail the assertion and the title would name the wrong class. Fix: retitle to "has no modifier chord ending in Home or End".

## Three riskiest things

1. `formatWithMap` / `cursorMap` still do not own the serializer's encoded neighbour. Every later reader of the map (Phase 2 save, autosave, a second toggle) will see a paragraph where a text node is, on the exact tree 1.40 now writes on one Backspace. Finding 1 is the member; the class is any `containerPhrasing` rewrite `placeChildren` cannot spell.
2. The committed legs still only delete, type, or split the ranges they name. 1.41's neighbour leg is the targeted J1 member and does not ask the map; a five-minute keystroke sweep remains a Phase 2 planning cost (`BACKLOG.md:73`, hard stop `2.verify`).
3. Rendered-view caret placement still has three vertical-arrow sites (`editor-trailing-break.spec.ts:84` `toDocumentEnd` used at H1/H5; `editor-break-in-mark.spec.ts:126` `ArrowDown` ×2 in a verse block). All passed on three runners this gate; the first red landing assertion is the trigger (`BACKLOG.md:70`).

## Class-level lessons (for docs/lessons.md)

- LESSON: a serializer fix that encodes a neighbour so the delimiter can form moves the defect from the bytes into the position map, and a corpus leg that asserts only `format(parse(out)) === out` (and a Playwright case that Copies without toggling) stays green — the `it.fails` pin is the only failing instrument → a flanking-encode fix task names `formatWithMap.unresolved` and one `toSource` of a caret inside the encoded neighbour as acceptance, beside the bytes; a pin whose revisit is the next review is a Required finding at that review, not a note.
- LESSON: a read-only review can find a flanking defect from `handleDelete` read against `encode-info.js` and GFM `_close` without running a transaction, and can find a cursor-map defect from `toSource`'s no-spelling branch beside an `it.fails` — both r4 Required/blocker findings were in the tree as a missing table, not only in a browser → the handler walk and the pin's neighbouring branch are the first two reads of a review that claims independence, not a sweep.
- LESSON: an `encodeInfo` port that enumerates "one guard per side × neighbour × inner class" and then a separate "whitespace inside" describe that names two of the three outside classes leaves the third cell (`format.ts:94`) as the file's only uncovered line → the table in `encode-info.js` (the 3×3, including D/nothing) is the enumeration, and coverage of the ported function is the check that every cell has a test.
