# journal-main.md — Essay Down (append-only; RUNNER-SPEC §10)

One entry per iteration, keyed `- [<task-id>] <ISO>`; never edited, never pruned, never carries its own commit SHA. Template in docs/progress.md. `/logs/state/summary.md` (generated) shows the last five; `docs/progress.md` is regenerated only in reconciliation commits.

- [0.0] 2026-09-05T16:32:39Z Task: bootstrap — image, entrypoints, generator/validator, gate.sh, ralph.sh, conformance suite, bootstrap, agent rules. Status: Complete. Files: docker/, ralph/, scripts/, CLAUDE.md, AGENTS.md, .gitattributes, .gitignore. Tests: conformance suite ; validator 122 tasks. Iterations used: 1 (interactive, principal + Michael). First attempt passed: y. Tool calls: n/a. Reviewer overturned: pending (Phase 0 review set). Notes: versions DEBIAN_CODENAME=bookworm NODE_VERSION=22.22.1 PNPM_VERSION=11.25.0 RUST_TOOLCHAIN=1.98.1 TAURI_DRIVER_VERSION=2.0.6 PANDOC_VERSION=3.11 TYPST_VERSION=0.15.1 EPUBCHECK_VERSION=5.3.0 HTML_VALIDATE_VERSION=11.13.0 CLAUDE_CODE_VERSION=2.1.261 CODEX_VERSION=0.153.4 GROK_VERSION=1.0.13 . Checks (quoted from ralph/bootstrap.sh):
    == docker compose build
        #12 DONE 289.5s
        #13 [ 8/13] RUN . /etc/essaydown/versions.env     && mkdir -p /opt/grok-home /opt/grok/bin     && HOME=/opt/grok-home GROK_BIN_DIR=/opt/grok/bin bash -c 'curl -fsSL https://x.ai/cli/install.sh | bash -s "$0"' "$GROK_VERSION"     && chmod -R a+rX /opt/grok-home /opt/grok     && ln -sf /opt/grok/bin/grok /usr/local/bin/grok     && HOME=/opt/grok-home grok --version
        #13 1.497 Installing Grok 1.0.13 (linux-aarch64)...
        #13 1.500   Downloading grok 1.0.13...
        #13 73.51   Binary linked to /opt/grok/bin/grok and /opt/grok/bin/agent.
        #13 73.52 Grok 1.0.13 installed to /opt/grok/bin/grok
        #13 73.52   Symlinked /usr/local/bin/grok -> /opt/grok/bin/grok
        #13 73.52   Symlinked /usr/local/bin/agent -> /opt/grok/bin/agent
        #13 73.53   Updated /opt/grok/bin in PATH in /opt/grok-home/.bashrc.
        #13 73.53 
        #13 73.53 Run 'grok' or 'agent' to get started!
        #13 73.53 grok 1.0.13 (5e9a58528b76)
        #13 DONE 73.6s
        #14 [ 9/13] RUN git config --system user.name "essaydown-agent"     && git config --system user.email "agent@essaydown.invalid"     && git config --system --add safe.directory '*'     && git config --system init.defaultBranch main     && git config --system pull.rebase true
        #14 DONE 0.1s
        #15 [10/13] COPY entrypoints/ /usr/local/bin/
        #15 DONE 0.0s
        #16 [11/13] RUN chmod 0755 /usr/local/bin/claude-task /usr/local/bin/claude-review /usr/local/bin/codex-review                /usr/local/bin/grok-review /usr/local/bin/boundary-check /usr/local/bin/essaydown-common.sh
        #16 DONE 0.1s
        #17 [12/13] WORKDIR /work
        #17 DONE 0.0s
        #18 [13/13] RUN mkdir -p /home/agent/.claude /home/agent/.codex /home/agent/.grok /home/agent/.cargo /home/agent/.local/share/pnpm
        #18 DONE 0.1s
        #19 exporting to image
        #19 exporting layers
        #19 exporting layers 0.7s done
        #19 writing image sha256:495338f3c9740067a20aead35ab1e136d7c91f75c41e9dbbf55c821199ef0dd2 done
        #19 naming to docker.io/library/essaydown-dev:0.0 done
        #19 DONE 0.7s
        #20 resolving provenance for metadata file
        #20 DONE 0.0s
         Image essaydown-dev:0.0 Built 
    PASS docker compose build
    == docker compose run claude-task --version
         Container essaydown-claude-task-run-1c00608f894c Creating 
         Container essaydown-claude-task-run-1c00608f894c Created 
        2.1.261 (Claude Code)
    PASS docker compose run claude-task --version
    == docker compose run codex-review --version
         Container essaydown-codex-review-run-f5b299673100 Creating 
         Container essaydown-codex-review-run-f5b299673100 Created 
        codex-cli 0.153.4
    PASS docker compose run codex-review --version
    == docker compose run grok-review --version
         Container essaydown-grok-review-run-cd4dd0cd480e Creating 
         Container essaydown-grok-review-run-cd4dd0cd480e Created 
        grok 1.0.13 (5e9a58528b76)
    PASS docker compose run grok-review --version
    == credential boundary (boundary-check → BOUNDARY-OK; includes pdfimages -v)
         Container essaydown-boundary-check-run-d6cf660de021 Creating 
         Container essaydown-boundary-check-run-d6cf660de021 Created 
        == versions
        node --version         v22.22.1
        pnpm --version         11.25.0
        cargo --version        cargo 1.98.1 (797e8a9bc 2026-08-05)
        rustc --version        rustc 1.98.1 (48a229cea 2026-09-01)
        pandoc --version       pandoc 3.11
        typst --version        typst 0.15.1 (9dfd3a08)
        epubcheck --version    EPUBCheck v5.3.0
        pdfimages -v           pdfimages version 22.12.0
        pdftotext -v           pdftotext version 22.12.0
        java -version          openjdk version "17.0.20.1" 2026-08-18
        html-validate --version html-validate-11.13.0
        claude --version       2.1.261 (Claude Code)
        codex --version        codex-cli 0.153.4
        grok --version         grok 1.0.13 (5e9a58528b76)
        git --version          git version 2.39.5
        tauri-driver           MISSING (USAGE: tauri-driver [FLAGS] [OPTIONS])
        == poppler-utils (apt) 22.12.0-2+deb12u3
        == environment
        PASS no *_API_KEY / GH_TOKEN / GITHUB_TOKEN in env
        PASS no SSH_AUTH_SOCK
        == git credential helpers
        PASS git config --show-origin --get-all credential.helper is empty
        == mounts
        PASS /home/agent/.ssh absent
        PASS /home/agent/.config/gh absent
        PASS /root/.ssh absent
        PASS no ~/.ssh or ~/.config/gh in /proc/self/mounts
        == private remote
        remote: Repository not found.
        fatal: Authentication failed for 'https://github.com/MLSavage/essaydown-private-authcheck/'
        PASS git ls-remote of the private authcheck repo failed (rc=128)
        == pdfimages
        PASS pdfimages -v runs: pdfimages version 22.12.0
        BOUNDARY-OK
    PASS credential boundary (boundary-check → BOUNDARY-OK; includes pdfimages -v)
    == node ralph/validate-tasks.mjs (EXPECTED_COUNT 122)
        validate-tasks: OK 122 expanded tasks (EXPECTED_COUNT 122), phases 0,1,2,3,4,5,6, acyclic, byte-identical to PRD §8
    PASS node ralph/validate-tasks.mjs (EXPECTED_COUNT 122)
    == ralph/test/run.sh conformance suite (RUNNER-SPEC §12)
          ...
        # Subtest: validator rejects: phase without an initial verifier
        ok 39 - validator rejects: phase without an initial verifier
          ---
          duration_ms: 0.082375
          type: 'test'
          ...
        # Subtest: validator rejects: phase without a close
        ok 40 - validator rejects: phase without a close
          ---
          duration_ms: 0.075333
          type: 'test'
          ...
        # Subtest: validator rejects: unknown model
        ok 41 - validator rejects: unknown model
          ---
          duration_ms: 0.076708
          type: 'test'
          ...
        # Subtest: validator rejects: shell-unsafe id
        ok 42 - validator rejects: shell-unsafe id
          ---
          duration_ms: 0.075083
          type: 'test'
          ...
        # Subtest: review attempt chain must be linear and the close must depend on the newest d
        ok 43 - review attempt chain must be linear and the close must depend on the newest d
          ---
          duration_ms: 0.157375
          type: 'test'
          ...
        1..43
        # tests 59
        # suites 0
        # pass 59
        # fail 0
        # cancelled 0
        # skipped 0
        # todo 0
        # duration_ms 77399.785333
    PASS ralph/test/run.sh conformance suite (RUNNER-SPEC §12)
    == ralph/check-agent-rules.sh
        check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)
    PASS ralph/check-agent-rules.sh
    == merge=union lessons.md merges two branches cleanly (fixture repo)
        Auto-merging docs/lessons.md
        merge=union: both lines present after merging a into b
        # lessons
        - [b] LESSON: line from b
        - [a] LESSON: line from a
    PASS merge=union lessons.md merges two branches cleanly (fixture repo)
    == external clone .ext/build-defaults (clean, refs/heads/main present, validate-defaults.mjs passes)
        validate-defaults: OK version 1.11.0, 12 sections, 12 retro entries
        external build-defaults at 8573e0ecf117000c669a07a11fa3ab5140cdaa36 (refs/heads/main)
    PASS external clone .ext/build-defaults (clean, refs/heads/main present, validate-defaults.mjs passes)
    == journal entry + task(0.0) commit

- [0.1] 2026-09-06T06:18:56Z Task: scaffold monorepo (pnpm workspaces, packages/core|editor|modes|coach|export, apps/desktop Tauri 2 + React + Vite, Vitest + @vitest/coverage-v8 with per-file thresholds, ESLint/Prettier, e2e/web Playwright + e2e/shell WebdriverIO placeholders, tests/agent-rules.test.ts). Status: Complete. Files: eslint.config.js, vitest.config.ts, .gitignore (this iteration); rest of the scaffold already present from prior wip commits (pnpm-workspace.yaml, packages/*, apps/desktop, e2e/*, tests/agent-rules.test.ts). Tests: pnpm test 6/6 passed (one placeholder test per package + agent-rules test); cargo test 0/0/0 (no Rust tests yet, workspace compiles clean); pnpm lint clean; pnpm coverage prints a per-file table (100% on the 5 placeholder files) after adding coverage.reporter [["text",{skipFull:false}],"html"] and test.reporters ["default"] to vitest.config.ts — the bare reporter:["text"] config from a prior attempt printed only the summary with an empty per-file table; pnpm tauri dev under Xvfb :99 opened a window verified titled "Essay Down" via a throwaway Xlib probe compiled with system gcc/libX11 (no wmctrl/xdotool available in the container and no root to install one), then the tauri/vite/cargo processes and Xvfb were killed. Iterations used: 1 (this session; four prior attempts recorded in docs/lessons.md left the scaffold mostly built but never reached DONE). First attempt passed: n (this is the attempt that completed it). Tool calls: ~20. Notes: coverage/ (vitest html output) had been committed by a prior wip commit — removed with `git rm -r --cached coverage` and gitignored plus excluded in eslint.config.js; nothing else pending for 0.1's acceptance. Next task should not add editor code or CI per 0.1's scope note.

- [0.2] 2026-09-06T06:28:46Z Task: CI workflow .github/workflows/ci.yml (pnpm lint, pnpm test, cargo test on ubuntu-latest/macos-latest/windows-latest; installs JDK via actions/setup-java, poppler-utils, and pinned epubcheck/html-validate versions read from docker/versions.env; caches pnpm store and cargo via actions/cache; triggers only on push to ci/** per CLAUDE.md; test-logs artifact assembled from per-OS uploads via actions/upload-artifact/merge). Status: Blocked. Files: .github/workflows/ci.yml (new), docs/DECISIONS.md (#013-actionlint-gap), docs/lessons.md. Tests: pnpm lint clean; pnpm test 6/6 passed; cargo test 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container. `versions in the workflow match docker/versions.env` verified by direct comparison (NODE_VERSION, PNPM_VERSION, RUST_TOOLCHAIN, EPUBCHECK_VERSION, HTML_VALIDATE_VERSION); YAML hand-checked (no tabs, consistent 2-space indent) since no YAML parser is available without adding one. Iterations used: 1. First attempt passed: n. Tool calls: ~30. Notes: acceptance's other clause — "ci.yml lints under actionlint locally and act --dryrun resolves the three jobs" — is NOT verified: neither tool is in the essaydown-dev image (docker/Dockerfile, task 0.0), no version is pinned for either in docker/versions.env, and Docker itself is unavailable in-container so act could not run even if fetched. Adding either tool is a new pinned dependency, which CLAUDE.md gates on a DECISIONS note *and Michael's OK*; a loop task has no channel to get that OK, so none was added (see docs/DECISIONS.md #013-actionlint-gap for the manual procedure). Next iteration/principal: either get Michael to run actionlint+act on his host and report back, or explicitly authorize pinning them into docker/versions.env + docker/Dockerfile in a follow-up task, then re-verify 0.2's full acceptance and print DONE.

- [0.2] 2026-09-06T06:32:00Z Task: CI workflow .github/workflows/ci.yml (attempt 2, re-orientation). Status: Blocked (unchanged from attempt 1). Files: none changed this iteration. Tests: pnpm lint clean; pnpm test 6/6 passed; cargo test 0/0/0 (workspace compiles) — re-verified green in-container. Iterations used: 2. First attempt passed: n. Tool calls: ~10. Notes: re-checked for anything that would unblock the remaining acceptance clause ("ci.yml lints under actionlint locally and act --dryrun resolves the three jobs") — /logs/human does not exist (no accepted.json for 0.2 or 0.2h), docker/versions.env and docker/Dockerfile are unchanged (no actionlint/act pin), docs/DECISIONS.md #013-actionlint-gap already records the finding and manual procedure accurately. ci.yml itself (150 lines) is unchanged and still matches its task description: pnpm lint/test + cargo test on 3 OSes, JDK/poppler/epubcheck/html-validate installed and versions read from docker/versions.env (verified again by direct comparison), triggers only on push to ci/**, per-OS logs merged into one test-logs artifact. No new dependency was added (CLAUDE.md requires a DECISIONS note *and Michael's OK* for that, and a loop task has no channel to obtain the OK). Next iteration: nothing changes here until either Michael runs actionlint/act on his own host and reports the result, or gives explicit OK (via an interactive-principal task) to pin actionlint+act into docker/versions.env + docker/Dockerfile — do not re-attempt this from a loop task; re-read DECISIONS #013 first instead of re-deriving the same conclusion from scratch.

- [0.2] 2026-09-06T06:33:00Z Task: CI workflow .github/workflows/ci.yml (attempt 3, re-orientation). Status: Blocked (unchanged from attempts 1-2). Files: none changed this iteration. Tests: pnpm lint clean; pnpm test 6/6 passed; cargo test 0/0/0 (workspace compiles, no Rust tests yet) — re-verified green in-container. Iterations used: 3. First attempt passed: n. Tool calls: ~8. Notes: per attempt 2's own instruction, did not re-derive the actionlint/act conclusion from scratch — read docs/DECISIONS.md #013-actionlint-gap directly, confirmed docker/Dockerfile and docker/versions.env still have no actionlint/act reference and /logs/human/0.2/accepted.json still does not exist, so nothing has changed that would unblock the remaining acceptance clause ("ci.yml lints under actionlint locally and act --dryrun resolves the three jobs"). ci.yml itself is unchanged from attempt 1 and still matches its task description. No new dependency added (CLAUDE.md requires a DECISIONS note *and Michael's OK*, unobtainable from a loop task). Next iteration: same as attempt 2 — nothing changes here until Michael runs actionlint/act on his own host and reports the result, or an interactive-principal task gets his explicit OK to pin actionlint+act into docker/versions.env + docker/Dockerfile. A fourth loop attempt on this task will find the identical state; consider routing 0.2 to a human gate or principal task instead of another loop attempt.

- [0.2] 2026-09-06T06:37:30Z Task: CI workflow .github/workflows/ci.yml (attempt 4, re-verification after image fix). Status: Complete. Files: none changed this iteration (ci.yml unchanged since attempt 1; docker image and DECISIONS.md were updated by a principal commit between attempts, not by this agent). Tests: `actionlint .github/workflows/ci.yml` exits 0, no findings; `act --list -W .github/workflows/ci.yml` lists both jobs (test, merge-logs) for the push trigger — warns about no Docker socket but does not fail, consistent with DECISIONS #014's in-container reading (matrix expansion and the 3-OS proof happen at the real `0.2h` CI gate, not here); versions in ci.yml (NODE_VERSION, PNPM_VERSION, RUST_TOOLCHAIN, EPUBCHECK_VERSION, HTML_VALIDATE_VERSION, all read via `docker/versions.env` sourced into $GITHUB_ENV rather than hardcoded) match docker/versions.env by construction. `pnpm lint` clean; `pnpm test` 6/6 passed; `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container. Iterations used: 4 (3 prior attempts blocked on the actionlint/act gap per DECISIONS #013; the gap was resolved by `fix(image) befc9b2` + DECISIONS #014, a principal action, before this attempt started). First attempt passed: n. Tool calls: ~10. Notes: task 0.2's acceptance is now fully met; no remaining work. The next task should not re-touch ci.yml, docker/Dockerfile or docker/versions.env for actionlint/act — they are pinned and installed as of this phase.

- [0.2] 2026-09-06T06:45:15Z Task: CI workflow .github/workflows/ci.yml (attempt 5, final re-verification after conflict resolution). Status: Complete. Files: none changed this iteration — a principal commit (de84774) had removed conflict markers a bad cherry-pick left in docs/DECISIONS.md (both the agent's #013-actionlint-gap entry and the principal's #014-act-dryrun entry landed; #014 restates #013's finding as resolved), and a prior wip commit (de1b0cf) had already re-verified once. This iteration independently confirmed: scanned the whole tree for `<<<<<<<`/`=======`/`>>>>>>>` markers (none found), `git status` clean. Tests: `actionlint .github/workflows/ci.yml` exits 0 with no findings; `act --list -W .github/workflows/ci.yml` lists both jobs (test, merge-logs) for the push trigger (Docker-socket warning only, per DECISIONS #014's in-container reading — the 3-OS proof is the real `0.2h` CI gate); every `docker/versions.env` key ci.yml reads (NODE_VERSION, PNPM_VERSION, RUST_TOOLCHAIN, EPUBCHECK_VERSION, HTML_VALIDATE_VERSION) matches by construction (workflow sources the file directly via `$GITHUB_ENV`, cross-checked by grep). `pnpm lint` clean; `pnpm test` 6/6 passed; `cargo test` 0/0/0 (workspace compiles, no Rust tests yet). Iterations used: 5. First attempt passed: n. Tool calls: ~10. Notes: task 0.2's acceptance is fully met and independently re-verified; printing DONE.

- [0.3] 2026-09-06T07:08:10Z Task: docs/MARKDOWN-STYLE.md (11 numbered style rules + 12 machine-read examples) and the canonical parser/formatter in packages/core/src/{parse,format}.ts — remark-parse + remark-frontmatter(yaml) + the three individual GFM extensions of PRD §4 (table, strikethrough, autolink-literal; footnotes and task lists deliberately absent), remark-stringify configured to the style doc, and byte-preserving `html`/`yaml` handlers for the opaque set of PRD §6.1. Status: Complete. Files: docs/MARKDOWN-STYLE.md (new), packages/core/src/parse.ts (new), packages/core/src/format.ts (new), packages/core/src/index.ts (re-exports), packages/core/test/markdown-style.test.ts (new), packages/core/test/opaque-roundtrip.test.ts (new), packages/core/test/fixtures/front-matter/{quoted,commented,block-scalar,duplicate,malformed}.md (new), packages/core/package.json + pnpm-lock.yaml (PRD §4 remark/mdast/micromark deps). Tests: `pnpm lint` clean; `pnpm test` 28/28 passed (22 of them new in packages/core); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container. Acceptance mapped one-to-one: 12 assertions read the twelve examples out of docs/MARKDOWN-STYLE.md by fence extraction (plus a guard asserting the numbers are exactly 1..12, so the count comes from the doc and not from the test) and compare `format(parse(input))` to the listed output byte-for-byte; 2 assertions cover an html node and a yaml block being byte-identical after the trip (node value before, node value after, and the whole file unchanged); 1 assertion covers `[^1]` reaching the tree as plain `text` with no footnote node anywhere; 5 assertions round-trip the quoted, commented, block-scalar, duplicate and malformed front-matter fixtures byte-identical (plus a guard asserting exactly those five files are present). Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: (1) the front-matter fixtures live in packages/core/test/fixtures/front-matter/, NOT in fixtures/markdown/ — task 0.4 owns that directory and its acceptance counts exactly 44 files with 44 `.canonical.md` siblings, so putting five sibling-less files there would have broken it; 0.4 should write its own front-matter fixtures in fixtures/markdown/ and may reuse the bodies here. (2) The `yaml` handler has one branch not exercised by the five fixtures: empty front matter (`---\n---`) is emitted without the blank line that a naive `---\n${value}\n---` would produce; verified by hand this iteration, and 0.4 should add an empty-front-matter fixture so the branch is covered by the corpus. (3) `packages/core` gained two type-declaration devDependencies, `@types/mdast` (the AST PRD §4 mandates) and `@types/node` (needed because packages/core/tsconfig.json includes `test`, and the tests read the style doc and fixtures from disk); both are declarations for things §4 already names, neither ships any runtime code, and `pnpm --filter @essaydown/core typecheck` is clean. (4) The processors are built per call in `createParser`/`createFormatter` rather than held at module level, per the PRD §9 "no module-level config" rule; if 0.6's invariant suite on 45 fixtures turns out slow, the fix is an injected processor argument, not a module-level singleton. (5) `pnpm format` (prettier) was already failing on 26 pre-existing files before this task and is not part of the green bar or of ci.yml; docs/MARKDOWN-STYLE.md is deliberately not prettier-formatted, because prettier rewrites `*emphasis*` to `_emphasis_` and would corrupt the twelve examples the test reads.

- [0.4] 2026-09-06T07:16:49Z Task: Fixture corpus part 1 — 44 short .md files in fixtures/markdown covering every §6.1 node type, nested lists 3 deep, a table with escaped pipes, code fences whose content contains ``` and ~~~, an image with spaces in its path, six front-matter variants (plain, quoted, inline comment, block scalar, duplicate `title:` key, malformed, plus empty front matter per 0.3's lesson), a raw html block, footnote syntax (parsed as text), CRLF line endings, a missing trailing newline, and tabs. Each source file gets a `.canonical.md` sibling written by `format(parse(x))`; fixtures/markdown/index.json records per file nodeTypes, blockCount, sectionCount and each paragraph's 1-based start line in the canonical file, validated against a new fixtures/markdown/index.schema.json. Status: Complete. Files: fixtures/markdown/*.md (44, new), fixtures/markdown/*.canonical.md (44, new), fixtures/markdown/index.json (new), fixtures/markdown/index.schema.json (new), packages/core/test/fixtures-corpus.test.ts (new). Tests: `pnpm lint` clean; `pnpm test` 34/34 passed (6 new in fixtures-corpus.test.ts); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container. Acceptance mapped one-to-one: one test confirms exactly 44 source files each with a `.canonical.md` sibling (and exactly 44 canonical files, no strays); one test validates index.json's shape and keys against index.schema.json's required fields; one test recomputes `format(parse(source))` for every fixture and compares it byte-for-byte to its committed `.canonical.md`; one test re-parses every canonical file and checks index.json's `paragraphStartLines` against the actual paragraph node positions; one test asserts the union of all `nodeTypes` across the corpus equals exactly the 21 node types §6.1 restricts the AST to (root, heading, paragraph, text, emphasis, strong, inlineCode, delete, link, image, list, listItem, blockquote, code, thematicBreak, table, tableRow, tableCell, break, html, yaml — no more, no fewer); one test asserts the five named blockCounts (nested-lists.md=6, table-escaped-pipes.md=4, code-fence-nested.md=2, front-matter.md=3, raw-html.md=2) against a hand-computed value written in the test with the arithmetic shown in a comment (Block = direct child of root, plus each listItem, plus each tableRow, per §6.1). Iterations used: 1. First attempt passed: y. Tool calls: ~45. Notes: (1) index.json and its schema are hand-validated in the test (no new dependency) rather than via a JSON-Schema library — PRD §4 names no such library and this is not a `verify` task, so none was added; the test's assertions mirror the schema's own `required`/type constraints instead of reusing an external validator. (2) Followed 0.3's lesson and added front-matter-empty.md (`---\n---`) to exercise the one yaml-handler branch the 0.3 fixtures didn't cover, swapping out a redundant link-autolink.md (a bracketed autolink is core CommonMark, not the §4 gfm-autolink-literal extension, and `link` nodes are already covered by link-inline.md and mixed-inline-marks.md) to keep the count at exactly 44. (3) A canonical file can legitimately differ from its source's outer syntax while preserving content: code-fence-nested.md's two fences (content containing ``` and ~~~ respectively) both come out as backtick fences of different lengths chosen by remark-stringify to avoid ambiguity with the content — this is correct per invariant B, not a bug. (4) Fixture generation and index.json computation were done with one temporary Node script run via `node --experimental-strip-types` (Node 22 native TS stripping, no tsx/ts-node needed) that imported `packages/core/src/{parse,format}.ts` directly; the script was deleted after use and is not part of the committed corpus. (5) `pnpm install` had to be re-run in this worktree (node_modules was empty even though pnpm-lock.yaml was already up to date) before the generator script could resolve packages/core's dependencies — worktrees do not share node_modules with the host checkout or other worktrees.
- [0.5] 2026-09-06T09:04:55Z Task: Fixture corpus part 2 — fixtures/markdown/essay-fixture.md, a ~5,000-word essay on the history of the fountain pen (docs/fixture-essay.md does not exist, so agent-written per the task's fallback), with 12 H2/H3 headings, 2 tables, 3 code fences, 2 images, 1 blockquote, and both an ordered and an unordered list. Status: Complete. Files: fixtures/markdown/essay-fixture.md (new), fixtures/markdown/essay-fixture.canonical.md (new, `format(parse(source))`), fixtures/markdown/index.json (one entry added), packages/core/test/fixtures-corpus.test.ts (two `toHaveLength(44)` literals replaced with the sourceNames/canonical/index relationship, per DECISIONS #016 — the file's own authorization, read from docs/lessons.md `[0.4]`). Tests: `pnpm lint` clean; `pnpm test` 34/34 passed (fixtures-corpus.test.ts's corpus-shape tests now derive their totals from the directory instead of asserting 44, and pass at 45); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container. Acceptance mapped one-to-one: word count 4,803 (both `wc -w` and a script's whitespace-split count agree, source and canonical identical) is inside 4,500–5,500; heading count is exactly 12 (8 `##` + 4 `###`, confirmed by grep and by the parsed tree's `sectionCount`); index.json's new entry carries `paragraphStartLines` (57 offsets) alongside `nodeTypes`, `blockCount` (88: root's 65 direct children + 13 listItem + 10 tableRow) and `sectionCount` (12, one per heading node) computed programmatically from the parsed canonical tree, not hand-counted; invariants A (idempotence: `format(parse(canonical)) === canonical`), B (semantic preservation: `parse(source)` deep-equals `parse(canonical)` with positions stripped), and C (byte fidelity: `format(parse(canonical)) === canonical`, no opaque html/yaml nodes in this fixture) all verified true by a throwaway script (not committed — 0.6 owns the committed round-trip suite per its own task text) before writing index.json. Iterations used: 1. First attempt passed: y. Tool calls: ~25. Notes: (1) the DECISIONS #016 test-file edit is the only touch to a passed task's file, and it is the exact edit #016 pre-authorizes — no other line of fixtures-corpus.test.ts changed. (2) generated the canonical file and the index entry with one temporary `node --experimental-strip-types` script importing `packages/core/src/{parse,format}.ts` directly, deleted after use, per the `[0.4]` lesson; `pnpm install` was run once first per the worktree-node_modules lesson. (3) sectionCount counts every heading node regardless of depth (matches the existing multiple-sections.md fixture's precedent: 1 H1 + 2 H2 + 1 H3 = sectionCount 4), not just top-level headings, despite the schema description's wording — 0.6 or a later task should tighten that wording if it is ever read literally. (4) 0.6 (`3 × 45 fixtures`, depends on 0.5) can now proceed: the corpus is 45 files/canonicals/index-entries, all three counts equal.

- [0.6] 2026-09-06T09:13:25Z Task: Round-trip invariant tests in packages/core — A idempotence, B semantic preservation (positions stripped), C byte fidelity on every `.canonical.md` and on the opaque (`html`/`yaml`) set, over all 45 fixtures of fixtures/markdown; plus a `test` script on `@essaydown/core` so the acceptance command runs. Status: Complete. Files: packages/core/test/roundtrip.test.ts (new, 135 tests = 3 invariants x 45 fixtures, the fixture list read from the directory rather than hardcoded per DECISIONS #016), packages/core/package.json (adds the `test` script; no dependency change). format.ts and parse.ts needed no fix — all three invariants already held on all 45 fixtures, verified before writing the suite and again by it. Tests: `pnpm --filter core test -- roundtrip` passes; `packages/core/test/roundtrip.test.ts (135 tests)` in the run output, all passed. `pnpm lint` clean; `pnpm --filter core typecheck` clean; `pnpm test` 169/169 passed (135 new); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container. Acceptance mapped one-to-one: (a) the command runs and passes, and the round-trip file reports exactly 135 tests, 3 per fixture over the 45 sources the directory holds (the run's grand total is 144 because vitest's positional filter `roundtrip` is a substring match and also selects 0.3's pre-existing packages/core/test/opaque-roundtrip.test.ts, 9 tests — that file is not renamed, see Notes); (b) CRLF normalisation is asserted inside invariant C: `expect(source.includes("\r\n")).toBe(name === "crlf-line-endings.md")` establishes that exactly one fixture is a real CRLF document and the other 44 are not, then every node value of `parse(source)`, the formatter output and the committed canonical are all asserted CR-free. Iterations used: 1. First attempt passed: y. Tool calls: ~15. Notes: (1) `pnpm run` in pnpm 11 forwards the `--` separator verbatim, and vitest's CLI parser puts everything after `--` in a passthrough bucket instead of treating it as a file filter, so a plain `"test": "vitest run --root ../.."` script would silently ignore the `roundtrip` argument and run the whole 169-test suite; the script is therefore `sh -c '[ "$1" = -- ] && shift; exec vitest run --root ../.. "$@"' sh`, which drops the leading `--` and forwards the rest. It is only ever invoked by hand — ci.yml runs `pnpm lint && pnpm test` at the root and never this script — so its `sh` dependency does not reach the Windows CI leg. (2) The 9 extra tests come from opaque-roundtrip.test.ts, a passed task's (0.3) file; renaming it would make the filter select only this suite, but that rename is compelled by nothing in the green bar and DECISIONS #016 only licenses edits the green bar or the task text compels, so it was not done. The acceptance's "135 assertions (3 x 45 fixtures)" is met exactly by roundtrip.test.ts's own reported count. (3) Invariant A is asserted as a genuine fixed point — `format(parse(format(parse(source)))) === format(parse(source))` — rather than only against the committed golden, so it holds independently of whether the golden is correct; C is what compares against the golden. (4) The suite was negative-checked: flipping `bullet: "-"` to `"*"` in format.ts turns 12 of the 135 red, so the matrix is not vacuous. (5) Invariant C reads index.json's `nodeTypes` to decide whether a fixture is expected to contain opaque nodes, so both the presence case (the 7 front-matter fixtures and raw-html.md) and the absence case (the other 37) are asserted rather than a hardcoded list of names.
- [0.7] 2026-09-06T09:24:30Z Task: packages/core/src/hash.ts (unsigned 64-bit FNV-1a as BigInt, masked to 64 bits each step, over UTF-8 bytes; `toBase36` zero-pads to 13 chars; `contentHash`) and packages/core/src/blocks.ts (`blocksOf` with contentIds per PRD §6.1, `sectionsOf`, `replaceBlock`, `moveBlock`, `moveSection`, `setHeadingDepth` re-depthing descendants), all pure. Status: Complete. Files: packages/core/src/hash.ts (new), packages/core/src/blocks.ts (new), packages/core/src/index.ts (re-exports), packages/core/test/hash.test.ts (new, 7 tests), packages/core/test/blocks.test.ts (new, 39 tests), fixtures/markdown/expected/essay-fixture.moved.md (new golden, created and committed by this task per PRD §8), vitest.config.ts (glob-keyed 100% coverage threshold for packages/core/src/blocks.ts). No dependency change. Tests: `pnpm lint` clean; `pnpm test` 215/215 passed (46 new); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet); `pnpm --filter @essaydown/core typecheck` clean; `pnpm coverage` 100% statements/branches/functions/lines on blocks.ts (and, as it happens, on every file) with the threshold enforced. Acceptance mapped one-to-one: `fnv1a64('')` is 0xcbf29ce484222325n and `fnv1a64('hello')` is 0xa430d84680aabd0bn, both encodings are exactly 13 chars and 'hello' encodes to '2hvyo96lq8v0r'; `blocksOf(parse(essay-fixture.canonical.md)).length` equals index.json's blockCount (88) and `sectionsOf(...).length` equals its sectionCount (12), both read from fixtures/markdown/index.json rather than written into the test; a document with three identical paragraphs and one different one yields ids `<h>-0`, `<other>-0`, `<h>-1`, `<h>-2`; `format(moveSection(root, 2, 0))` is byte-identical to fixtures/markdown/expected/essay-fixture.moved.md; `setHeadingDepth` on the H2 section with an H3 child yields depths [3, 4, 2] (the following H2 untouched) and the H3/H4 Markdown; 100% branch coverage on blocks.ts with the threshold in vitest.config.ts. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: (1) the golden was hand-verified independently of blocks.ts: `expected/essay-fixture.moved.md` is byte-identical to `{sed 1,2p; sed 23,36p; sed 3,22p; sed 37,$p} essay-fixture.canonical.md`, i.e. a pure textual line-range move of section 2 (`### The Patents of the Early Fountain Pen Age`, canonical lines 23-36) to the front, so the golden does not merely restate whatever moveSection did; the test also asserts the golden's lines are a permutation of the canonical's and that the two files differ. (2) The coverage threshold was negative-checked: running `vitest run --coverage packages/core/test/hash.test.ts` alone fails with four `does not meet "packages/core/src/blocks.ts" threshold (100%)` errors, so the glob key really is matched and enforced and is not silently ignored. Glob-keyed thresholds replace the global ones for the files they match, which is why all four metrics are pinned at 100 there rather than falling back to the repo-wide 0. (3) §6.1 defines contentId as `fnv1a64(normalizedText)` but never defines normalizedText; the reading taken here (documented in blocks.ts) is: plain text of the block (value nodes verbatim, image alt text, a hard break as a space, everything else the concatenation of children), then whitespace runs collapsed to a single space, trimmed, NFC-normalised. That makes an id survive a re-wrap or re-indent of the same prose, which is what §6.2 anchoring needs. If a later task needs a different normalisation (e.g. case folding for the Sorensen-Dice step) it should extend this, not fork it. (4) `moveBlock(root, from, to)` addresses `root.children` indices only - the "top-level block index" that §6.2 anchors refresh against - because a listItem or tableRow cannot legally become a child of root; `replaceBlock` is addressed by contentId and does reach nested blocks, and refuses to put anything but a listItem where a listItem was, or anything but a tableRow where a tableRow was. (5) `moveSection` throws when the target section lies inside the moved section (a section cannot be moved into itself) and is a no-op when from === to; it never touches depths, because PRD §7 makes an Outline nesting drag `setHeadingDepth` + `moveSection` so each half is a separate undo-able intent. (6) `setHeadingDepth` only needs an upper-bound check: every heading inside a section is strictly deeper than the section's own heading, so a shift that keeps the section heading in 1-6 can only ever run a descendant off the bottom (past 6), never off the top - the code and a test both say so. (7) `fixtures/markdown/expected/` is a directory, so 0.4's corpus test (which filters `readdirSync` entries by `.md` suffix) is unaffected and still sees exactly 45 sources, 45 canonicals and 45 index entries. (8) hash.ts uses the `TextEncoder` and blocks.ts the `structuredClone` globals; both are standard cross-platform JavaScript (not DOM), typed via the `@types/node` devDependency 0.3 already added, and `tsc --noEmit` is clean.

- [0.8] 2026-09-06T09:39:41Z Task: packages/core/src/sentences.ts — `segmentSentences`/`sentencesOf` (Intl.Segmenter in sentence granularity plus the §6.1 abbreviation post-filter: e.g., i.e., Dr., Mr., Mrs., Ms., vs., etc., single-capital initials and decimal numbers never end a sentence), `replaceSentence(root, blockId, index, text)` and `reorderSentences(root, blockId, order)` preserving inline marks by mapping sentence offsets back through the paragraph's inline children; plus packages/core/test/sentences.cases.json, the 30-case conformance corpus. Status: Complete. Files: packages/core/src/sentences.ts (new), packages/core/test/sentences.cases.json (new, 30 cases), packages/core/test/sentences.test.ts (new, 91 tests), packages/core/src/index.ts (re-exports appended). No dependency change. Tests: `pnpm lint` clean; `pnpm --filter @essaydown/core typecheck` clean; `pnpm test` 306/306 passed (91 new); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet); `pnpm coverage` still green, blocks.ts unchanged at 100% and sentences.ts at 98.3% statements / 92.6% branches (uncovered lines are the defensive branches for nodes outside the §6.1 set). Acceptance mapped one-to-one: the case file holds exactly 30 cases with unique ids, each run twice — once through `segmentSentences(text)` and once through `sentencesOf(paragraph)` — and each case's expected value is the full list of sentence strings, so the count and the boundaries are both pinned; a guard test asserts the three cases the acceptance spells out are present at 2 sentences each ('Dr. Smith went home. He slept.', 'It cost 3.50 dollars. Then more.', 'See e.g. the chart. Next.'); `replaceSentence` on 'A **bold** start. And *emphasis* here.' index 0 formats to 'A quiet start. And *emphasis* here.' with 'A quiet start.' and to 'A **quiet** start. And *emphasis* here.' with 'A **quiet** start.'; a link spanning a boundary ('See [the pen. It is](https://example.com/pen) here.') survives on both sides — a reorder yields two link nodes with the same url carrying 'It is' and 'the pen.', and replacing either sentence leaves the other half's link intact. Iterations used: 1. First attempt passed: y. Tool calls: ~25. Notes: (1) The 30 cases are plain text with no Markdown, because 0.9's fallback and 6.2's in-webview harness run the same file; each case carries an `id` and a `rule` sentence saying which §6.1 rule it pins. ICU-quirk cases were deliberately kept out: Node 22's Intl.Segmenter splits '"Stop!" she said.' after the closing quote and 'He said (rightly!) that it works.' after the paren, because a lowercase continuation is not part of §6.1's post-filter list — the list was implemented exactly as written, no lowercase-continuation rule was added, and those two shapes are therefore not in the corpus. (2) `abbrev-etc` is recorded as ONE sentence ('Bring paper, ink, blotters, etc. Then start.'): §6.1 says etc. does not split, so a sentence that really ends in etc. is joined to its successor. Stated limit, asserted, not a bug. (3) Sentence ranges are trimmed, so the whitespace between two sentences belongs to neither; on both replace and reorder that gap is re-emitted outside every mark (a `break` in it is kept as a node). Leaving it inside produced '[ ](url)' and an emphasis whose trailing space the formatter escaped as '&#x20;'. (4) `sentencesOf(paragraph, {blockId, segment})`: `segment` is the injection point 0.9's `segmenterSelfTest()` result plugs into (a `SegmentFn` returns candidate segment starts; the §6.1 post-filter runs over them), and `ABBREVIATIONS` is exported so 0.9 uses the same list rather than a second one. Default `blockId` is the id the paragraph would have as the first occurrence of its own text; callers inside a document pass `blocksOf(root)`'s contentId. (5) Sentence operations are refused on anything but a top-level paragraph (§6.1 Rewrite/Reorder v1 scope). A paragraph inside a list item or blockquote is not a Block at all, so it has no contentId to pass — the reachable rejections are top-level non-paragraphs and listItem/tableRow blocks, and the test uses those. (6) Negative-checked: disabling the abbreviation post-filter turns 12 of the 91 tests red, and ignoring `order` in reorderSentences turns 5 red. A whole-essay test also reorders every top-level paragraph of essay-fixture into its own order and asserts the document comes back byte-identical.

- [0.9] 2026-09-06T09:47:22Z Task: packages/core/src/segment-fallback.ts — the rule-based fallback segmenter of PRD §4 (`fallbackSegment`, a drop-in `SegmentFn`: one or more terminal punctuation marks, any closing quotes/brackets, whitespace, then optional opening quotes/brackets and a capital letter or a digit; `fallbackSentences(text)` runs it through the same §6.1 abbreviation post-filter `segmentSentences` applies to Intl.Segmenter) plus `segmenterSelfTest()`, which runs the five `SEGMENTER_CANARIES` against the runtime's own Intl.Segmenter and returns 'intl' or 'fallback'. Status: Complete. Files: packages/core/src/segment-fallback.ts (new), packages/core/test/segment-fallback.test.ts (new, 21 tests), packages/core/src/index.ts (re-exports appended). No dependency change; no edit to any passed task's file. Tests: `pnpm lint` clean; `pnpm --filter @essaydown/core typecheck` clean; `pnpm test` 327/327 passed (21 new); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet); `pnpm coverage` 100% statements/branches/functions/lines on segment-fallback.ts, blocks.ts still 100% and its threshold still enforced. Acceptance mapped one-to-one: the fallback passes 30/30 of packages/core/test/sentences.cases.json — the acceptance allows three misses, the implementation needs none, so the test's `ALLOWED_MISSES` list is empty and the miss set is asserted by equality (`expect(missed).toEqual(ALLOWED_MISSES)`) with `ALLOWED_MISSES.length <= 3` and `passed >= 27` asserted alongside, so a future rule change that loses a case goes red until the loss is listed and explained; `segmenterSelfTest()` returns 'intl' on this runtime (the test first asserts `process.versions.node` major >= 20 and `typeof Intl.Segmenter === 'function'`, so the claim is anchored to the Node >= 20 the acceptance names) and 'fallback' with `Intl.Segmenter` assigned `undefined`, restored in an `afterEach`. Iterations used: 1. First attempt passed: y. Tool calls: ~18. Notes: (1) The fallback needs no abbreviation logic of its own: requiring a capital or a digit to open the next sentence already keeps `See e.g. the chart.` and `Cats vs. dogs …` whole (a lowercase continuation is never a candidate), and the boundaries that do get proposed (`Dr. Smith`, `Mrs. Halloway`, `etc. Then`, `J. R. R.`) are rejected by the §6.1 post-filter inside `segmentSentences`, which reads 0.8's exported `ABBREVIATIONS` — one list, not two, and a test walks `ABBREVIATIONS` to prove each entry is honoured through the fallback. (2) Requiring whitespace after the terminator is what makes decimals and URLs (`3.50`, `example.com/a.b`) unbreakable before the post-filter ever runs. (3) `segmenterSelfTest` reaches the runtime's Intl.Segmenter through `segmentSentences(text)`'s default parameter rather than by exporting `intlSegment` from sentences.ts, so no passed task's file was touched; it is also why the self-test measures the whole §6.1 pipeline (segmenter + post-filter + trimming), which is what a caller actually depends on. (4) Beyond the two states the acceptance names, the self-test is asserted to return 'fallback' for a constructor that segments too finely, too coarsely, at the right count but the wrong offsets, and one that throws on construction — the last is the shape a webview with no ICU sentence data would present in Phase 6. (5) The canaries are five verbatim copies (plain-two, abbrev-dr, decimal-mid, question-mark, no-terminator) of cases in sentences.cases.json, and a test asserts id, text and expected sentences still match the case file, so the src-side copies (a src file cannot read a test fixture) cannot drift from the corpus 6.2 runs inside each webview. (6) Negative-checked: reducing the boundary rule to `/[.!?…]+\s+/gu` (no closing-delimiter run, no capital/digit requirement) turns 5 of the 21 tests red, including the conformance count. (7) No coverage threshold was added for segment-fallback.ts (0.7's glob entry for blocks.ts is untouched); the file happens to be at 100% on all four metrics.
- [0.10] 2026-09-06T10:11:55Z Task: packages/core/src/sidecar.ts — the zod sidecar schema, `attach(sidecar, root)` implementing the five-step resolution order of §6.2, `refresh(sidecar, root)`, and the front-matter mirror of `title`/`question` under the byte-preservation rule of §6.1. Status: Complete. Files: packages/core/src/sidecar.ts (new, 1021 lines), packages/core/test/sidecar.test.ts (new, 73 tests), packages/core/src/index.ts (re-exports appended), packages/core/package.json + pnpm-lock.yaml (adds `zod`, the dependency this task's own text and PRD §5 line 134 "zod-enforced" name — see Notes (2)). Tests: `pnpm lint` clean; `pnpm test` 400/400 passed across 15 files (73 new in sidecar.test.ts); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container on this branch's HEAD. Acceptance mapped one-to-one, each sentence to a named test: a variant anchored to a sentence "survives changing one word elsewhere in the paragraph" and "survives a two-character change in the sentence itself" (steps 1 and 4 respectively) and "orphans after a complete rewrite, keeping the text it was written against"; "an in-app reorder moves each variant with its logical sentence" and "an external swap of the same two sentences leaves each variant at its recorded position" over a document holding two byte-identical sentences; the same pair for two identical H2 headings ("an in-app Outline move takes each question with its logical heading", "an external swap of the same two sections leaves each question at its recorded position"); "rejects a sidecar with no version with the message the acceptance names" asserts the zod issue message is exactly `sidecar version missing` (and a sibling test pins a *present but wrong* version to a different message, so the custom message is not a catch-all); "round-trips \`question:\` into topicQuestion"; "leaves the block byte-identical when the topic question did not change"; "rewrites only the question line when it changed in-app" (asserted by splitting the yaml before and after into lines and requiring every line except the `question:` one to be identical, not just by a substring check); a parameterised test returns `FrontMatterUnsupported` and leaves the block byte-identical for a block scalar (`|` and `>-`), a duplicated app-owned key and the other unsupported shapes; "preserves quoting style and the inline comment" plus "escapes inside the quoting style it found" cover the quoted-value and comment rewrite. Iterations used: 3. First attempt passed: n. Tool calls: 9 (this iteration). Notes: (1) attempts 1 and 2 wrote the implementation and the suite; attempt 2 had the suite green at its turn 46 and then spent its remaining turns on branch-coverage measurement that no acceptance sentence of 0.10 asks for, hitting the turn cap with nothing journaled or committed — the runner's recovery commit 0724f69 preserved that work, and attempt 3 verified it from HEAD (lint, 400 tests, cargo), ticked the acceptance sentences against the existing tests and journaled. Nothing was rewritten this iteration. (2) `zod` is a new runtime dependency of `@essaydown/core`. It is not in the PRD §4 table, but it is named by this task's own description and acceptance in PRD §8 ("zod schema", "missing version → zod error") and by PRD §5 line 134 ("zod-enforced"), so it is PRD-requested rather than an agent's choice; no `docs/DECISIONS.md` entry was written for it, because a DECISIONS note under CLAUDE.md's dependency rule needs Michael's OK and a `loop` task can never obtain one, and inventing one attributed to him is forbidden. A later task or the Phase 0 reconciliation should add `zod` to the PRD §4 dependency table so the pin lives in one place. (3) No coverage threshold was added for sidecar.ts: the 100% glob threshold in vitest.config.ts is 0.7's and keyed to packages/core/src/blocks.ts alone, and 0.10's acceptance says nothing about coverage. (4) No passed task's file was edited. (5) The two duplicate cases turn on the difference between an in-app operation (which carries the anchor with the logical item, because the operation says which index moved where) and an external re-parse (which cannot, because the two texts are byte-identical and the file records no evidence of the swap) — the tests state that limit explicitly rather than pretending the external case can be resolved, per §6.2's honest-duplicate rule, and `duplicate: true` is reported on both resolutions so the sidebar can badge them.
- [0.11] 2026-09-06T10:19:31Z Task: packages/core/src/undo.ts — the snapshot undo stack of §6.5: `createUndoStack(root, sidecar, {cap, coalesceWindowMs, at})` seeding the document as opened, `push(stack, root, sidecar, {coalesceKey, at})`, `undo`, `redo`, `current`, `canUndo`, `canRedo`, `endCoalescing`, with coalescing of same-key pushes inside 1,000 ms, a 500-snapshot cap and structural sharing. Status: Complete. Files: packages/core/src/undo.ts (new), packages/core/test/undo.test.ts (new, 31 tests), packages/core/src/index.ts (re-exports appended). No dependency change; no edit to any passed task's file. Tests: `pnpm lint` clean; `pnpm test` 431/431 passed across 16 files (31 new in undo.test.ts); `cargo test` 0/0/0 (workspace compiles, no Rust tests yet) — all green in-container on this branch. Acceptance mapped one-to-one: the file holds 31 unit tests (acceptance asks 20); "collapses three 'typing' pushes inside one second into a single undo step" pushes at 100/400/900 ms and asserts two entries, the newest snapshot present and one undo landing back on the seed; "does not coalesce a 'rewrite' push between two 'typing' pushes" asserts four entries and walks all three undos back to the seed in order; "retains the last 500 of 600 pushes" asserts `entries.length === UNDO_CAP`, that the retained roots are exactly `roots.slice(100)` by identity, and that undoing to the bottom lands on push 101 (the seed and pushes 1–100 dropped); "restores a deep-equal root and sidecar, and the same objects that were pushed" asserts `toEqual` and `toBe` on both halves for undo and for redo, and a sibling test does the §6.5 "Use this" case end to end (Markdown reverts to `A **bold** start.` and `rewrites[0].chosen` reverts to null, then both come back on redo). Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: (1) The task text spells `push(root, sidecar, {coalesceKey, at})`; CLAUDE.md requires `packages/core` to be pure functions with injected state, so the stack is a value and the implemented spelling is `push(stack, root, sidecar, {coalesceKey, at})` — the store owns the stack and every function returns a new one. No factory/holder object was added; a Zustand store in Phase 1 keeps the stack in state and calls these reducers. (2) The window slides: §6.5 merges transactions "within 1 s of each other", so each push is compared with the group's latest push, not with its first; an unbroken burst at 900 ms intervals stays one step and one second of silence closes the group (both asserted). A push with no `coalesceKey` (the default) neither merges nor can be merged into, so two mode mutations a moment apart are always two steps. (3) `endCoalescing(stack)` is §6.5's source/rendered toggle — it ends the group without pushing, and returns the identical stack when no group is open. `undo` and `redo` close the group too, so a push after either cannot reach back into a pre-undo burst. (4) Structural sharing is implemented as "the stack never clones": snapshots are stored by reference, so consecutive snapshots share every subtree the mutation did not rebuild. Asserted directly (two roots sharing a `children[0]` object keep that identity inside the stack; 20 snapshots capped at 5 all hold the same shared paragraph object) rather than via `blocks.ts`, whose `structuredClone` deep-copies. The contract this rests on — a pushed root/sidecar is never mutated in place — is stated in the module doc. (5) `at` defaults to `Date.now()` so a caller with no better time works, but every behavioural test injects `at`; the clock is a parameter, not module state, and two default-clock tests cover the default path. (6) Cap trimming drops from the front and is asserted at cap 500, cap 3 and cap 1 (at cap 1 there is nothing to undo); a cap below 1, a non-integer cap and a negative window throw `RangeError`. (7) Negative-checked: disabling the coalesce branch turns 4 of the 31 red, ignoring the cap 4, letting undo/redo leave the group open 1, and keeping the redo tail on a new push 1. (8) Pre-existing and untouched: `pnpm --filter @essaydown/core typecheck` reports one error, `test/sidecar.test.ts(979,42) TS2339: Property 'variants' does not exist`, from task 0.10's file. Verified by `git stash` that it is identical at HEAD without this task's changes; `tsc` is not in the green bar (`pnpm lint && pnpm test && cargo test`) and passed tasks are never edited, so it is reported here rather than fixed — a narrowing of the `SIDECAR_LISTS` discriminated union at that line is the fix if a later task is authorized to make it.

- [0.verify] 2026-09-06T10:23:25Z Task: Phase 0 verification — freeze dependencies (no lockfile change allowed) and confirm the full suite is green at the phase head ahead of the three-OS CI run. Status: Complete. Files: none changed (verification-only task; HEAD is unchanged from 0.11's integrated SHA 5f10e6e1e641d06139c2ef3c1a8e56249e78115b). Tests: `pnpm install --frozen-lockfile` succeeded with no lockfile drift; `pnpm lint` clean; `pnpm test` 431/431 passed across 16 files; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests (workspace compiles, no Rust tests written yet, consistent with every Phase 0 task through 0.11). Iterations used: 1. First attempt passed: y. Tool calls: 9. Notes: Acceptance mapped one-to-one: (1) pnpm lint/test and cargo test are green in-container, verified fresh this iteration rather than assumed from 0.11's journal; (2) `git diff --stat` of this branch's HEAD against 0.11's integrated SHA (5f10e6e) over `package.json`, every workspace `**/package.json`, and `pnpm-lock.yaml` is empty, and `git status --short` is clean, so no dependency-manifest or lockfile change occurred. No dependency added or removed. The task's `ci`/`ciAcceptance` fields (three-OS run via ci/0.verify/a{n}) are the runner's CI-gate task to execute, not this loop task's acceptance; nothing here should be read as having run or claimed that. Next iteration/gate: none needed from this task; the phase head (5f10e6e) is ready for the CI gate.
- [plan.0.verifyh.r0] 2026-09-06T14:03:33Z Task: planning commit for the 0.verifyh GATE-FAILED (ci.yml run 34037147355: windows-latest 99/419 red, every failure CRLF vs LF; no eol policy in .gitattributes). Status: Complete. Files: docs/PRD.md (§8 Phase 0: +0.13 line-ending policy with the exact .gitattributes content, +0.verify.g1 needsCI repair; 0.12 → 0.verify.g1), ralph/tasks.json + ralph/EXPECTED_COUNT (122 → 125, regenerated), docs/DECISIONS.md (#018: cause, scope choice, graph change, CI evidence path convention = gate id, reversal), docker/entrypoints/essaydown-common.sh + grok-review (reviewer cold-build path /logs/ci/<verifier_id>h/accepted/), docs/lessons.md. Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (125 tasks); ralph conformance suite run before commit. Commit: wip(plan.0.verifyh.r0). Iterations used: 1. First attempt passed: y. Tool calls: ~12. Reviewer overturned: pending. Notes: the runner integrates this as plan(plan.0.verifyh.r0), supersedes 0.verifyh, then runs 0.13 and 0.verify.g1; next human gate is scripts/gate.sh 0.verify.g1h.
- [0.13] 2026-09-06T14:08:56Z Task: `.gitattributes` line-ending policy repair after the 0.verifyh GATE-FAILED (ci.yml run 34037147355: windows-latest 99/419 red, every failure `\r\n` vs `\n`; DECISIONS #018). Replaced the whole of `.gitattributes` with exactly four lines: `* text=auto eol=lf`, `fixtures/** -text`, `packages/core/test/fixtures/** -text`, `docs/lessons.md merge=union`. Status: Complete. Files: `.gitattributes` (replaced), `docs/progress/journal-main.md` (this entry). No test edits, no workflow edits, no source edits, no dependency change. Tests: Reproduction first — `rm -r docs/MARKDOWN-STYLE.md fixtures/markdown packages/core/test/fixtures && git -c core.autocrlf=true checkout -- docs/MARKDOWN-STYLE.md fixtures packages/core/test/fixtures` (Windows-style checkout) before the change: `git ls-files --eol ... | grep -c 'w/crlf'` reported 98 paths, and `pnpm test` was red (99/419 failed across 6 files, all `expect(format(next)).toBe(canonical)` byte mismatches) — matches the CI failure shape. Restored (`git checkout -- ...`), wrote the new `.gitattributes`, then repeated the same Windows-style checkout: `grep -c 'w/crlf'` now reports exactly 1 (`fixtures/markdown/crlf-line-endings.md`, with `i/crlf`, `attr/-text`), `git ls-files --eol | grep 'i/crlf'` lists only that same path (no renormalisation of tracked content), and on that checkout `pnpm lint` clean, `pnpm test` 431/431 passed across 16 files, `cargo test` 0/0/0 (workspace compiles, no Rust tests yet, consistent with every Phase 0 task through 0.11/0.verify). `git status --short` after the Windows-style checkout shows only `.gitattributes` modified (the new `eol=lf` normalises the rest back to the index on comparison), matching the acceptance's `git diff --stat` scope of `.gitattributes` plus the journal/lessons files. Iterations used: 1. First attempt passed: y. Tool calls: 9. Notes: this container is Linux, so the in-container Windows-style-checkout proxy above is not a Windows pass; per the task's own honest-statement clause, the only proof is the `0.verify.g1h` CI gate green on ubuntu-latest, macos-latest and windows-latest, which this task cannot observe and does not claim. No passed task's file was touched.
- [0.verify.g1] 2026-09-06T14:15:00Z Task: Phase 0 verification, repair 1 of 0.verify after the 0.verifyh GATE-FAILED (DECISIONS #018): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head (0.13, 13588ee, which includes the `.gitattributes` line-ending policy) is ready for the three-OS CI gate. Status: Complete. Files: none changed (verification-only task; HEAD is unchanged from 0.13's integrated SHA 13588ee). Tests: `pnpm install --frozen-lockfile` succeeded with no lockfile drift; `pnpm lint` clean; `pnpm test` 431/431 passed across 16 files; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests (workspace compiles, no Rust tests written yet, consistent with every Phase 0 task through 0.13). Iterations used: 1. First attempt passed: y. Tool calls: 8. Notes: Acceptance mapped one-to-one: (1) pnpm lint/test and cargo test are green in-container, verified fresh this iteration; (2) `git diff --stat` of this branch's HEAD against 0.13's integrated SHA (13588ee) over `package.json`, every workspace `**/package.json`, `pnpm-lock.yaml` and both `Cargo.toml`s is empty, and `git status --short` is clean, so no dependency-manifest or lockfile change occurred. No dependency added or removed. The task's `ci`/`ciAcceptance` fields (three-OS run via ci/0.verify.g1/a{n}, logs expected under /logs/ci/0.verify.g1h/accepted/ per DECISIONS #018) are the runner's CI-gate task to execute, not this loop task's acceptance; nothing here should be read as having run or claimed that, and this container is Linux so no macOS/Windows pass is claimed. No passed task's file was touched. Next: the phase head (13588ee) is ready for the 0.verify.g1h CI gate.
- [0.12.r0d] 2026-09-06T15:57:41Z Task: reconciliation of Phase 0 review attempt r0 at 81273f5 (verifier 0.verify.g1). Claude FAIL (2 blockers/5 should-fix/5 nits), Sol FAIL (3/8/0), Grok returned PASS (0/8/2). Verdict FAIL: silent deletion/duplication of zero-width inline nodes by replaceSentence/reorderSentences (reproduced by two reviewers) and an over-permissive front-matter writer. Status: Complete. Files: docs/reviews/phase-0-r0-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#review-0-r0: verdict, 21 findings with severity and disposition, deviations 1–24, #017 provenance corrected, manual procedures for single runner process and image rebuild, reversal), docs/PRD.md (§8 Phase 0: +0.14–0.22 fix tasks, +0.verify.r1 needsCI with promotions, +0.12.r1a/b/c/d, 0.close → 0.12.r1d; §4 zod row), ralph/tasks.json + ralph/EXPECTED_COUNT (125 → 140), docs/RUNNER-SPEC.md (§3/§5 evidence path = gate id; §5.5 promote-lessons manual), docs/V1.1-BACKLOG.md (new, 8 lines), docs/lessons.md (+10 class-level), docs/progress.md (regenerated). Tests: node ralph/generate-tasks.mjs --check, node ralph/validate-tasks.mjs (140), bash ralph/test/run.sh green. Commit: wip(0.12.r0d). Iterations used: 1. First attempt passed: y. Tool calls: ~14. Reviewer overturned: n/a. Notes: after integration the runner runs 0.14 (opus) first; the image must be rebuilt (`docker compose build`) before it starts (F17); next human gate is scripts/gate.sh 0.verify.r1h.
- [0.14] 2026-09-06T16:10:58Z Task: Phase 0 review fix, DECISIONS #review-0-r0 F1 (Claude findings 1-2, Sol finding 2, blocker): `packages/core/src/sentences.ts` `sliceInline` gave a width-0 atomic inline node (an image with empty alt, alone or inside a link) no owner at the end of a paragraph and two owners at offset 0, so `replaceSentence`/`reorderSentences` silently deleted or duplicated it, and the mark branch's `hi <= lo` overlap test discarded width-0 descendants (a linked empty-alt image lost its url). Status: Complete. Files: `packages/core/src/sentences.ts` (ownership rule + doc comment; `sliceInline` takes `includeAtTo`, the `zeroWidth && start === from` special case is gone, the mark branch is descended into unconditionally, `gapNodes` takes `includeAtTo` and now unwraps only a blank mark; new exported `sentenceMarkdown`), `packages/core/test/sentences-zero-width.test.ts` (new, 62 tests), `docs/progress/journal-main.md`, `docs/lessons.md`. No file of a passed task was edited (the probes and the invariant are a new test file, not an edit of 0.8's `sentences.test.ts`); nothing outside `packages/core/src/sentences.ts` and `packages/core/test/`; no dependency added. Tests: reproduction first - with the pre-fix ownership rule restored in place (and `sentenceMarkdown` kept so the file still imports), 10 of the 14 probe tests fail, including both reviewers' exact probes (`One. Two. ![](i.png)` replace and identity reorder, `Hi. ![](x.png)` identity reorder, the linked empty-alt image at both ends, adjacent images at the end); after the fix all 62 pass. `pnpm lint` clean; `pnpm test` 493/493 passed across 17 files (was 431/16; `sentences.test.ts` still 91 tests and `sentences.cases.json` still its 30 cases, both untouched); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. Iterations used: 1. First attempt passed: y. Tool calls: 12. Notes: The ownership rule, written into `sliceInline`'s doc comment: a node contributing no plain text sits at one offset `p` and belongs to the single slice with `from <= p < to` - a caller's slices are contiguous and half-open, so exactly one qualifies - except `p === plainText.length`, which no half-open slice contains and which the one slice ending the paragraph claims via `includeAtTo` (`replaceSentence`'s final `sliceInline`, `reorderSentences`' final `gapNodes`). A mark is now descended into even when it contributes no text, and `gapNodes` unwraps a mark only when everything it contributes is whitespace or a break, so a link around a width-0 image keeps its url instead of being unwrapped to a bare image. Reading recorded for the reviewer: the acceptance's `replaceSentence(root, id, i, <the sentence's own text>)` is read as the sentence's own inline *Markdown*, not `Sentence.text`; `Sentence.text` is plain text and drops every mark inside the sentence, so the literal reading would fail on any emphasised sentence and would assert a property of the formatter rather than of the slicing. `sentenceMarkdown(paragraph, index)` produces it; it is exported from `sentences.ts` only (not added to `packages/core/src/index.ts`, which is outside this task's file scope). The corpus-wide invariant runs over every fixture in `fixtures/markdown/index.json` (45 today, read from the index keys, never a literal): for every top-level paragraph, `format(reorderSentences(root, id, identity)) === format(root)` and `format(replaceSentence(root, id, i, sentenceMarkdown(...))) === format(root)`, plus a non-degeneracy assertion that the corpus has more sentences than paragraphs and more than zero of each. Honest limit: that invariant passes on the *pre-fix* code too - no corpus fixture has a width-0 atomic node at a paragraph boundary - so it is the class-level guard the review asked for, while the 14 probe tests are what reproduce F1. A fixture carrying such a node would strengthen it, but `fixtures/markdown/` is outside this task's file scope. `pnpm --filter @essaydown/core typecheck` is still red at exactly one pre-existing location, `test/sidecar.test.ts(979,42)` (0.11's journal, Claude finding 3 -> its own fix task); this task's two files add no typecheck error. Next: nothing outstanding from 0.14; the remaining r0 blocker is the front-matter writer (F2).
- [0.15] 2026-09-06T16:24:00Z Task: Phase 0 review fix, DECISIONS #review-0-r0 F2 (Sol findings 3 and 5, blocker): `packages/core/src/sidecar.ts`'s hand-written front-matter scalar scanner accepted what it could not represent - a malformed plain scalar (`title: bad: mapping`, a value opening on a YAML indicator) was writable, an unknown double-quoted escape (`"\q"`) was read as its own letter, `"\u0041"` was misdecoded as `u0041`, and a replacement value carrying a newline was written inside single quotes as two physical lines and reported as success. Status: Complete. Files: `packages/core/src/sidecar.ts` (the §6.1 grammar written into a doc comment; `YAML_INDICATORS`, `SCALAR_TAIL`, `LINE_BREAK`; readers return a reason instead of `null`; `readDoubleQuoted` decodes exactly `\\ \" \n \t \r \/ \b \f \0 \uXXXX \xXX` and reports `malformed` for anything else; new `encodeDoubleQuoted` replaces `JSON.stringify` in `serializeScalar`; `classify` refuses a plain scalar the writer could not emit plain; `writeFrontMatter` refuses a value carrying `\n`/`\r` before it reads the block), `packages/core/test/front-matter-grammar.test.ts` (new, 73 tests), `packages/core/test/sidecar.test.ts` (exactly two tests rewritten, see the deviation below), `docs/progress/journal-main.md`, `docs/lessons.md`. No dependency added (no YAML library; `pnpm-lock.yaml` and every manifest untouched); nothing outside `packages/core/src/sidecar.ts` and `packages/core/test/`. Tests: reproduction first - with `git checkout packages/core/src/sidecar.ts` (the pre-fix implementation) and the new tests in place, 39 of the 73 new tests fail, including the acceptance's own probes (`"\u0041"` read as `u0041`, `"\q"` read as `q`, `title: bad: mapping` writable, and every multiline-replacement case for plain, single- and double-quoted titles), plus the two rewritten sidecar tests; after the fix all 73 pass. `pnpm lint` clean; `pnpm test` 566/566 passed across 18 files (was 493/17 at 0.14; `sidecar.test.ts` is still exactly 73 tests, the count the acceptance names); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. Iterations used: 1. First attempt passed: y. Tool calls: 17. Notes: DEVIATION from the acceptance sentence "the existing sidecar tests (73 at r0) still pass": two of those 73 assert the exact behaviour F2 says is wrong - "carries an unknown double-quote escape through unchanged" (`"a \z b"` -> `a z b`) and "quotes a value that carries a newline" (`writeFrontMatter(root, {question: "one\ntwo"})` -> `question: "one\\ntwo"`, which the old code emitted from a *plain* line but wrote literally inside single quotes when the line was single-quoted). They cannot both keep their expectations and satisfy the task text, which is why the task authorises edits in `packages/core/test/`; each was rewritten in place (same `it` block, new name, a comment naming this task and the finding) to assert the new outcome plus the untouched bytes, so the file is still 73 tests and the other 71 are unchanged. Grammar decisions a reviewer should check: (1) the plain-scalar read test is `!needsQuoting(value)`, the same predicate the writer uses to decide whether to emit plain, so a plain scalar is readable exactly when it is re-emittable - `title: 11:00 sharp` and `C#minor` stay writable, `bad: mapping`, `trailing:` and a leading indicator do not; (2) a leading indicator gets reason `indicator`, an in-grammar-shape but out-of-grammar-content scalar gets `malformed`, an unterminated quote keeps `multi-line`; (3) a replacement value carrying `\n` or `\r` is refused with reason `multi-line` for every quoting style, including a key the block does not carry yet and a document with no front matter at all (the value is checked before the block is read, so it wins over `no-front-matter`); (4) `\uXXXX` decodes per UTF-16 code unit, so a surrogate pair reads as one astral character, and `encodeDoubleQuoted` writes a lone surrogate back as `\uXXXX` and C0/DEL/C1 as `\xXX` rather than raw. `readDoubleQuoted` and `encodeDoubleQuoted` are asserted as an inverse pair over 15 values, not just per-escape. Honest limits: `pnpm --filter @essaydown/core typecheck` is still red at exactly the one pre-existing location, `test/sidecar.test.ts(979,42)` (0.11's journal, Claude finding 3 -> its own fix task); neither file this task touched adds a typecheck error, and line 979 is before both rewritten tests. No corpus fixture changes classification under the new grammar (the front-matter fixtures were re-run as part of the 566). No passed task's source file was edited. Next: nothing outstanding from 0.15; F2 was the last r0 blocker.
- [0.16] 2026-09-06T16:35:00Z Task: Phase 0 review fix, DECISIONS #review-0-r0 F3 (Claude findings 3 and 10, Sol finding 10, Grok findings 5-6, should-fix): the green bar `pnpm lint && pnpm test && cargo test` covered neither `tsc` nor coverage thresholds, so packages/core shipped with `pnpm --filter @essaydown/core typecheck` red (`packages/core/test/sidecar.test.ts:979`, `variants` on the un-narrowed `orphans[0].entry` union) and 0.7's 100% blocks.ts branch threshold never ran. Status: Complete. Files: `package.json` (`lint` -> `eslint . && pnpm -r --if-present typecheck`; `test` -> `vitest run --coverage`, `coverage` unchanged), `apps/desktop/package.json` (+`typecheck: tsc --noEmit`, verified it actually type-checks by introducing then reverting a deliberate `const x: string = 123` in `src/main.tsx`), `vitest.config.ts` (global per-file thresholds raised from 0 to statements 97 / branches 94 / functions 100 / lines 99, just below this run's worst-per-file numbers; `blocks.ts` override unchanged at 100/100/100/100), `packages/core/test/sidecar.test.ts` (line 979: `orphans[0].entry.variants` replaced with a `if (orphan.list !== "rewrites") throw ...` discriminant check before reading `.entry.variants`, per the task's required fix), `docs/progress/journal-main.md`. No dependency added: `git diff --stat -- package.json '**/package.json' pnpm-lock.yaml` shows only the two script edits above, `pnpm-lock.yaml` untouched. ci.yml unchanged (already runs `pnpm lint`/`pnpm test`, confirmed by grep). Tests: (1) `pnpm lint` failure demonstration - added `packages/core/src/scratch-0.16-typecheck-probe.ts` with `export const scratchTypeError: string = 123;`; `pnpm lint` exited 2 at the `packages/core typecheck` step with `TS2322: Type 'number' is not assignable to type 'string'`; removed the file, `pnpm lint` exited 0 again (`git status --short` confirmed no trace left). (2) `pnpm test` failure demonstration - temporarily set the `blocks.ts` threshold override's `branches` to 101 (unmeetable, since 100% is the ceiling); `pnpm test` exited 1 with `ERROR: Coverage for branches (100%) does not meet "packages/core/src/blocks.ts" threshold (101%)`; reverted to the committed 100/100/100/100 and reran - exit 0, coverage summary unchanged (99% stmts / 96.3% branches / 100% funcs / 99.67% lines over all files). (3) Full bar: `pnpm lint` clean across all 6 packages that carry a `typecheck` script (apps/desktop, packages/core, coach, editor, export, modes); `pnpm test` 566/566 passed across 18 files (unchanged from 0.15) with coverage thresholds passing at the new non-zero values; `pnpm --filter @essaydown/core typecheck` exits 0 (the sidecar.ts:979 fix); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. Iterations used: 1. First attempt passed: y. Tool calls: ~28. Notes: DEVIATION/scope call - `e2e/shell` and `e2e/web` (workspace members under `pnpm-workspace.yaml`'s `e2e/*` glob) were left without a `typecheck` script. Both fail `tsc --noEmit` today (`node:assert/strict`, `describe`/`it`, and the `WebdriverIO` namespace all lack type declarations in that package's own `node_modules`, verified via `node -e "require.resolve('@types/node/package.json')"` failing there), and fixing that needs a new type-only devDependency (`@types/node` and/or `@types/mocha`/`@wdio/types`) in each of those two `package.json`s, which the task text explicitly forbids ("No new dependencies"). Read "add the script to packages that lack it" as scoped to packages the widened bar can typecheck for free with what's already installed (apps/desktop needed only the script, no new deps); flagging the e2e gap for a future task that is allowed to add the type packages. Threshold choice: read live from this run's `pnpm coverage` output (sentences.ts 97.76%/94.44%/100%/100%, sidecar.ts 98.96%/95.55%/100%/99.4%, every other file 100 across the board) rather than copied from the task text, and set "just below" the worst observed value per metric so the current suite passes with no slack for a future silent regression to hide in. Honest limit: the deliberate-failure/revert probes above changed `packages/core/src/` and `vitest.config.ts` transiently during this iteration and were fully reverted before the final green run and before this commit; `git status --short` at commit time shows only the four files listed above. No passed task's file was edited.
- [0.17] 2026-09-06T16:45:00Z Task: Phase 0 review fix, DECISIONS #review-0-r0 F4 and F5 (Claude findings 4-5, Sol finding 8, Grok finding 1, should-fix): (1) `merge-logs`'s `actions/upload-artifact/merge@v4` step used the default `separate-directories: false`, so the three per-OS `test-logs-<os>` artifacts collided on `lint.log`/`test.log`/`cargo-test.log` and the merged, digested artifact held only one OS's logs; (2) `ralph/test/run.sh` (RUNNER-SPEC §12), the runner's own conformance suite, was exercised only by `ralph/bootstrap.sh` and never by CI. Status: Complete. Files: `.github/workflows/ci.yml` only — `merge-logs` gained `separate-directories: true`; the `test` job's ubuntu leg (`if: runner.os == 'Linux'`) gained a step "Runner conformance suite (RUNNER-SPEC §12, ubuntu leg only)" running `bash ralph/test/run.sh 2>&1 | tee ralph-test.log` after the `Cargo test` step; `ralph-test.log` was added to that job's `Upload logs` artifact path list with `if-no-files-found: ignore` (the file exists only on the Linux leg, and `if: always()` on the upload step means it can fire with the log absent on macOS/Windows). No other file touched; no dependency added (`docker/versions.env` untouched, `ralph/test/run.sh` itself untouched). Tests: `actionlint .github/workflows/ci.yml` exits 0; `act --list` resolves both jobs (`test`, `merge-logs`) under workflow `CI` on the `push` trigger (the missing local Docker daemon only affects `act`'s ability to actually execute a job, not `--list`'s resolution, and the task's acceptance asks for resolution, not execution); `git diff --stat` against 0.16's HEAD shows exactly one file, `.github/workflows/ci.yml`, 8 insertions, 0 deletions. In-container bar: `pnpm lint` clean (eslint + the 6 `typecheck` scripts from 0.16); `pnpm test` 566/566 passed across 18 files with coverage unchanged from 0.16 (99%/96.3%/100%/99.67%); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. Iterations used: 1. First attempt passed: y. Tool calls: 12. Notes: per the task text and CLAUDE.md, this container is Linux and never runs the actual three-OS matrix or downloads the merged artifact — the three-OS proof (three `test-logs-<os>` uploads, one non-empty `ralph-test.log` only in the ubuntu one) and the merged artifact's layout (`separate-directories: true` producing `test-logs-{ubuntu,macos,windows}-latest/{lint,test,cargo-test}.log` plus the ubuntu subdirectory's `ralph-test.log`) are observed only by the `0.verify.r1h` CI gate, whose `ciAcceptance` names those three directories; nothing in this entry claims to have run or inspected that gate's actual output. The artifact name `test-logs` for the merged artifact and `test-logs-${{ matrix.os }}` per leg are unchanged, per the task's explicit instruction not to change the artifact name. Next: nothing outstanding from 0.17.
- [0.18] 2026-09-06T17:05:00Z Task: Phase 0 review fix, DECISIONS #review-0-r0 F6 and F7 (Sol finding 6 and Grok finding 2, should-fix required): (1) review start in `ralph/lib/run.mjs` `stepReview` trusted the runtime `integrated_sha` and never opened the verifier gate's evidence, so a review attempt started (and three reviewers ran) on a gate whose `accepted.json` was deleted or whose accepted artifact bytes no longer matched their recorded digest — RUNNER-SPEC §1's "consumers never read a bare `ci/<id>/`; they read through `accepted.json`" was unimplemented at exactly the consumer §5.1 describes; (2) `ralph/lib/gate.mjs` `isVerifyGate` was `/^\d+\.verifyh$/`, so `0.verify.g1h` and every `N.verify.r<k>[.g<n>]h` printed no `ROTATE-PRINCIPAL` from either the runner or `gate.sh` (DECISIONS #015). Status: Complete. Files: `ralph/lib/run.mjs` (import `readAcceptedCi` from `gate.mjs`; four lines in `stepReview` after the existing verification/implementation check: `accepted.json` must exist at `ctx.ciDir(gateId)`, `readAcceptedCi` re-digests every named artifact, and `acc.sha` must equal both `verification_sha` and `implementation_sha` — each failure a `RalphError` naming the gate and the mismatch, thrown before the `withLock` that writes the attempt metadata and spawns the reviewers), `ralph/lib/gate.mjs` (`isVerifyGate` → `/^\d+\.verify(?:\.r\d+)?(?:\.g\d+)?h$/`, the §5 verifier-id grammar, plus its doc comment; one definition serves both the runner's `emitHumanGate` and `runGate`'s post-record line, which is gate.sh's output path), `ralph/test/conformance.test.mjs` (+2 tests, +`rmSync` to the `node:fs` import), `docs/progress/journal-main.md`, `docs/lessons.md`. RUNNER-SPEC untouched (both items implement §1, §5.1 and §12 sentences that already exist); no dependency added; `git diff --stat` shows exactly `ralph/lib/gate.mjs`, `ralph/lib/run.mjs`, `ralph/test/conformance.test.mjs` plus the two docs. Tests: reproduction first — with the two new tests present and neither `lib/` change applied, `bash ralph/test/run.sh` reported `# tests 63 / # pass 61 / # fail 2`, and the failures are the findings themselves: the F6 test's case (a) (accepted.json deleted) ran the whole r0 review anyway and the run ended at `PRINCIPAL 0.9.r0d` instead of refusing, and the F7 test's runner assertion saw a bare `HUMAN_GATE 0.verify.g1h` with no `ROTATE-PRINCIPAL` line. After the fix `bash ralph/test/run.sh` is `# tests 63 / # pass 63 / # fail 0` — the 61 existing tests unchanged and unedited. Both new tests drive the real entry points only (`ralph.sh run`, `scripts/gate.sh` via the harness's `ralph`/`gate` helpers), never the library functions directly. In-container bar: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 566/566 across 18 files, coverage unchanged (99% / 96.3% / 100% / 99.67%); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. Iterations used: 1. First attempt passed: y. Tool calls: 20. Notes: ordering decision a reviewer should check — the pre-existing `verification_sha !== implementation_sha` check keeps its place *before* the new evidence check, so that case still emits the `REVIEW-SHA-MISMATCH` signal rather than becoming a thrown `RalphError`; the new checks then assert `acc.sha` against both shas explicitly (redundant once they are equal, but they are what the task text requires and they are what fails if the runtime record is the thing that is wrong). "Nothing recorded" is asserted per file (`phase_base_sha`, `verifier_id`, `verification_sha`, `implementation_sha` absent), per reviewer directory (`claude/`, `sol/`, `grok/` absent) and per state record (all three reviewers still `pending`); the attempt directory `.evidence/reviews/0/r0/` itself is created by the pre-existing `ensureDir` before the gate id is even resolved, so it exists but is empty — left as it was rather than moved, since moving it is not in this task's text. The F7 test builds both remaining shapes of the grammar through the real repair flows (a code-failed `0.verifyh` → planning commit → `0.verify.g1`, then a FAIL reconciliation of r0 → `0.verify.r1` + the r1 review set) and asserts the line in both output paths for each: `^HUMAN_GATE <gate>\nROTATE-PRINCIPAL$` from `ralph.sh run` and `^ACCEPT <gate> a1 run \S+\nROTATE-PRINCIPAL$` from `gate.sh`. The fourth shape, `N.verify.r<k>.g<n>h`, is covered by the regex and by the existing reviews test's fixture but is not separately asserted here. Next: nothing outstanding from 0.18.
- [0.19] 2026-09-06T16:51:10Z Task: Phase 0 review fix (DECISIONS #review-0-r0 F8, Grok finding 3 and Claude finding 11, the #017 doctor gap, should-fix required): `ralph.sh doctor` checked a passed task by ancestry only, so a principal commit made from a stale host checkout that deletes a passed task's files (#017: `c8b234a` deleted 91 files of 0.4 while `6a9acdc` stayed an ancestor of phase/0) was invisible; and a crash after `status: passed` but before the candidate tag, worktree and branch are removed left debris no check named. Status: Complete. Files: `ralph/lib/doctor.mjs` (new `RUNNER_COMMIT_SUBJECT` + `checkPrincipalDeletions` producing `principal-commit-deletes <sha> <n> paths`, called from `doctor()` after the plan-request loop; a new `passed-with-debris <id>` branch in the per-task loop; `adminMarkIntegrated` gained the idempotent debris-sweep path for an already-`passed` task), `ralph/lib/integrate.mjs` (the post-ref cleanup tail of `finishIntegration` extracted as the exported, idempotent `cleanupDebris(repo, label, {branch, wt, branchHead})`; `finishIntegration` calls it — no behaviour change on the integration path), `ralph/test/conformance.test.mjs` (+2 tests, +`revParse`/`refOid` already imported), `docs/progress/journal-main.md`, `docs/lessons.md`. No new dependency; `git diff --stat` is exactly those five files (140 insertions, 3 deletions), `docs/RUNNER-SPEC.md` untouched (§4.6 already says doctor "checks, for every task ... Each report line names the one `admin` command that fixes it"). Tests: reproduction first — with both new tests present and neither `lib/` change applied (`git stash push -- ralph/lib/doctor.mjs ralph/lib/integrate.mjs`), `bash ralph/test/run.sh` reported `# tests 65 / # pass 63 / # fail 2` and the two failures are exactly the two new tests; after the fix `# tests 65 / # pass 65 / # fail 0`, the 63 existing tests unchanged and unedited. `ralph/ralph.sh doctor` on the real repo at HEAD (run as `RALPH_ROOT=/work node ralph/lib/cli.mjs ralph doctor` with this worktree's code, read-only) prints `doctor: clean`, exit 0. In-container bar: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 566/566 across 18 files, coverage unchanged (99% / 96.3% / 100% / 99.67%); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. Iterations used: 1. First attempt passed: y. Tool calls: 26. Notes: THE COMPARISON THE ACCEPTANCE ASKS ME TO STATE — for every commit on a phase branch in `<base_main_sha>..<phase branch head>` whose subject does not match `/^(task\(|plan\(|fix\(0\.)/`, take the paths it deleted **relative to its parent** (`git diff-tree -r --diff-filter=D --name-only --no-commit-id -z`), then report only those paths that are (a) still absent from the **phase branch head tree** (`git ls-tree -r --name-only -z <head>`, one call per branch, memoised) and (b) untouched by any later commit on the branch (`git log <sha>..<head> -- <path>`, evaluated only for paths already failing (a), so the clean case costs nothing extra). (a) is what makes the real `c8b234a`/`59d2d3e` pair clean: `59d2d3e` restored all 91 paths, so the intersection is empty, where a parent-only comparison would report the pair forever. (b) was added after the third leg of the new test went red: without it, a later *task* commit that legitimately deletes the same path re-blames the earlier principal commit permanently, with no available repair. Positive proof that the check fires on the real event, not only on the fixture: a throwaway `git clone --shared /work /tmp/probe019` with `phase/0` forced back to `c8b234a` and `RALPH_EVIDENCE_DIR=/work/.evidence` printed `principal-commit-deletes c8b234a 91 paths (still absent from phase/0: fixtures/markdown/blockquote-nested.canonical.md, …)`; the clone was deleted and nothing under `/work` or `/logs` was written (doctor is read-only). `passed-with-debris` fires only for `commitTask` executions (loop / replan / interactive-principal / loop-external) and only when the record is `passed`, so it can never collide with the existing `unfinished-integration` line, which requires `status !== "passed"`. `admin mark-integrated` idempotency: when the record is already `passed` the transaction must NOT run again (the target ref moved long ago and `finishIntegration` would re-run `refreshCheckouts` against `candidate^`), so the new branch does the debris sweep only, audits the doctor line, leaves the state record byte-identical, and refuses with `!= recorded integrated_sha` if a leftover `candidate/<id>` points somewhere other than the recorded candidate — the runner never guesses. The existing crash-after-update-ref path is untouched: that crash happens before `ctx.set(passed)`, so the record is still `running` and the old `finishIntegration` path still runs. DEVIATION (declared): the acceptance's file list names "the journal" but not `docs/lessons.md`; CLAUDE.md step 3 requires a lessons line when something was learned, and CLAUDE.md outranks task text, so one line was appended. Next: nothing outstanding from 0.19.
- [0.20] 2026-09-06T17:20:00Z Task: Phase 0 review fix (DECISIONS #review-0-r0 F9 and F10, Claude finding 6 and Sol finding 11, should-fix required): (1) `ralph/lib/util.mjs` `withLock` broke a stale lock by path — read pid, `kill(pid, 0)`, `unlinkSync` — a TOCTOU in which two processes recovering from the same crash both take the lock, and its `finally` unlinked whatever was at the path whether or not it was still ours; (2) `writeAtomic`'s doc comment said "fsync'd" and no `fsync` was ever issued, so RUNNER-SPEC §8.4's close intent was not durable before the ref transaction it authorises. Status: Complete. Files: `ralph/lib/util.mjs` (`writeAtomic(p, content, { fs = REAL_FS })` — the syscalls it issues are now injectable, and the order is write → `fsync(fd)` → close → rename → `fsync(dir)`, the directory sync wrapped in a try/catch for platforms that refuse to open a directory for reading; new `readLockFile` and `breakStaleLock` helpers; `withLock` writes `<pid> <iso> <16 random bytes as hex>` into the lock, breaks a stale lock by renaming it to `<lock>.stale.<pid>.<ms>` and counts the break only when the rename succeeded *and* the bytes it carried away equal the bytes judged stale, re-reads the lock after acquisition and throws if it no longer carries our token, and releases only while the file still carries our token; `hooks.beforeBreak` / `hooks.afterCreate` are the two interleaving points, unused by the runner), `ralph/test/util.test.mjs` (new, 3 top-level tests / 6 including subtests), `ralph/test/run.sh` (the new file added to the node --test argument list), `docs/progress/journal-main.md`, `docs/lessons.md`. No new dependency (`randomBytes`/`fsyncSync` are `node:crypto`/`node:fs`; `pnpm-lock.yaml` and every manifest untouched). `git diff --stat` is exactly `ralph/lib/util.mjs`, `ralph/test/run.sh` and the new `ralph/test/util.test.mjs` plus the two docs — `docs/RUNNER-SPEC.md` untouched (§1 and §8.4 already state both properties). Tests: reproduction first — with the three tests written and neither fix applied, `node --test ralph/test/util.test.mjs` reported `# tests 6 / # pass 1 / # fail 5` and `bash ralph/test/run.sh` reported `# tests 71 / # pass 66 / # fail 5`; the only passing case was "a stale lock is broken and taken", which the old path also satisfied. The five failures are the findings: the concurrent breaker's lock was broken and the critical section ran anyway (no abort), the post-acquisition token re-read did not exist, the release unlinked a lock carrying another holder's bytes, and `writeAtomic` recorded no calls at all on the injected fs (it ignored the third argument and issued no `fsync`). After the fix `bash ralph/test/run.sh` is `# tests 71 / # pass 71 / # fail 0`, the 66 existing tests unchanged and unedited. In-container bar: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 566/566 across 18 files, coverage unchanged (99% / 96.3% / 100% / 99.67%); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. `RALPH_ROOT=/work ralph doctor` with this worktree's code prints `doctor: clean`, exit 0. Iterations used: 1. First attempt passed: y. Tool calls: 16. Notes: the design decision a reviewer should check — "break and take as one operation" is implemented as a compare-and-swap, not merely as "the rename succeeded". A bare rename is not sufficient: two processes recovering from the same crash can both see the stale file, the first renames it away and re-creates the lock with its own token, and the second's rename then succeeds against the *live* lock. So after the rename the stashed bytes are compared with the bytes that were judged stale; on a mismatch the file is renamed back (only while the lock path is still free — otherwise the stash is removed and the message says so) and a `RalphError` aborts this process, leaving the concurrent breaker the sole holder. The post-acquisition token re-read is the second, independent guard for the residual window between `openSync(lock, "wx")` and the first read. Both guards abort rather than retry, per the task text ("abort if it is not yours"): a process that discovers it is racing another breaker over the one lock must not keep trying, and the runner's callers all treat a `RalphError` as a stop. The alive-holder branch keeps its timeout/`Atomics.wait` behaviour unchanged, and a lock that disappears under the staleness read (`holder === null`) simply retries the create. New test file rather than an addition to `conformance.test.mjs`: those are fixture-repository scenarios driven through `ralph.sh`/`gate.sh`, and neither property here is observable that way — who owns the lock at a given instant needs a second actor interleaved at an exact point, and the syscall order needs the syscalls themselves recorded. Honest limit: `fsync` durability is asserted as the call order against an injected fs, not as a power-loss experiment; the real-fs leg of that test only proves the bytes land and no temporary file is left behind. The directory `fsync` is best-effort by design (a `catch` that swallows an `openSync(dir, "r")` refusal on platforms that do not permit it); the container is Linux, so the swallowing branch is not exercised here and no macOS or Windows claim is made. DEVIATION (declared): the acceptance's file list is "ralph/lib/util.mjs, ralph/test/ and the journal" and does not name `docs/lessons.md`; CLAUDE.md step 3 requires a lessons line when something was learned and CLAUDE.md outranks task text, so two lines were appended. Next: nothing outstanding from 0.20.
- [0.21] 2026-09-06T17:10:00Z Task: Phase 0 review fix (DECISIONS #review-0-r0 F11, Claude findings 7 and 12, should-fix required): the 0.1 scaffold left `tauri-plugin-opener`/`@tauri-apps/plugin-opener` (dependencies outside PRD §4 with no DECISIONS note) and the scaffold's `greet` command/handler (an IPC surface item outside §6.4's exhaustive list). Status: Complete. Files: `apps/desktop/src-tauri/Cargo.toml` (removed `tauri-plugin-opener = "2"`; `description` → `Essay Down`, `authors` → `["Savage Systems"]`), `apps/desktop/src-tauri/src/lib.rs` (removed the `greet` command, its `invoke_handler`, and `.plugin(tauri_plugin_opener::init())`), `apps/desktop/package.json` (removed `@tauri-apps/plugin-opener`), `apps/desktop/src-tauri/capabilities/default.json` (removed the now-nonexistent `opener:default` permission — not named in the task text but required by the acceptance's `grep -rn opener apps/` being empty), `apps/desktop/index.html` (`<title>` → `Essay Down`), `Cargo.lock` and `pnpm-lock.yaml` (regenerated by `cargo build --workspace` and `pnpm install`; both diffs are deletion-only, 515 and 10 lines respectively, confirmed via `git diff --stat`), `docs/progress/journal-main.md`. `App.tsx` left untouched per the task text ("Leave App.tsx to Phase 1's shell task"): it still calls `invoke("greet", ...)` on its form submit, which will now reject at runtime since the handler is gone, but nothing invokes it on mount and the window still opens and renders. Tests: `git diff` confirms no added dependency in any manifest or lockfile and both opener packages gone from both lockfiles (`grep -n plugin-opener pnpm-lock.yaml` and `grep -n opener Cargo.lock` both empty); `grep -rn opener apps/ -i` empty; `cargo build --workspace` and `cargo test` compile the workspace clean (0/0/0 across desktop_lib, desktop and doc-tests, unchanged); `pnpm tauri dev` under a foreground `Xvfb :99`, checked with a throwaway ~40-line Xlib program (`XFetchName`/`_NET_WM_NAME` walk of the window tree, per the 0.1 lesson, no `wmctrl`/`xdotool` available) reported the top-level window's `_NET_WM_NAME` and `WM_NAME` both as `Essay Down`; all dev processes and Xvfb killed by PID afterward, `ps aux` confirmed clean. Full bar: `pnpm lint` clean (eslint + 6 typecheck scripts); `pnpm test` 566/566 across 18 files, coverage unchanged (99%/96.3%/100%/99.67%); `cargo test` 0/0/0. Iterations used: 1. First attempt passed: y. Tool calls: ~22. Notes: nothing outstanding from 0.21.
- [0.22] 2026-09-06T17:10:42Z Task: Phase 0 review fix (DECISIONS #review-0-r0 F12 and F13, Sol finding 4, Grok findings 9–10, should-fix required): `packages/core/src/parse.ts`'s doc comment claimed line endings were normalised to LF on the way in, but `parse` never rewrote `\r\n`; micromark only drops the CR between blocks, so it survived inside a multi-line paragraph and inside a fenced code block (confirmed by probe: a CRLF two-line paragraph parsed to a `text` node whose `value` still held `\r\n`, and a CRLF-fenced `code` node's `value` did too), and `format` then emitted mixed line endings from those nodes. Status: Complete. Files: `packages/core/src/parse.ts` (`parse` now calls `createParser().parse(markdown.replace(/\r\n?/g, "\n"))` before handing the document to the parser — whole document, opaque nodes included, per invariant C and the 0.6 acceptance; doc comment updated to say what the code now does and why the rewrite is needed ahead of the parser rather than left to micromark), `packages/core/test/parse-line-endings.test.ts` (new, 4 tests, one per shape named in the task text — a CRLF two-line paragraph, a CRLF fenced code block, a CRLF html block, a CRLF yaml block — each asserting the source string literal contains `\r\n`, then walking every node's `value` for the absence of `\r`, then asserting `format` output is also `\r`-free; these are string literals, not new fixtures, per the task text's explicit instruction that the corpus reserves CRLF for `crlf-line-endings.md`), `fixtures/markdown/task-list-as-text.md`+`.canonical.md` (new fixture: `- [ ] Buy ink` / `- [x] Refill pen`, no task-list extension enabled so both stay a plain `list`/`listItem`/`paragraph`/`text` tree; canonical escapes the leading bracket, `- \[ ] Buy ink` / `- \[x] Refill pen`, which remark-stringify does on its own to keep the brackets from being misread on a future parse), `fixtures/markdown/autolink-literal.md`+`.canonical.md` (new fixture: a bare `https://example.com/pens` URL becomes a `link` node via the gfm-autolink-literal extension already wired into `createParser`; canonical renders it back as `<https://example.com/pens>`), `fixtures/markdown/index.json` (two new entries, computed the same way the existing entries are: `nodeTypes` from walking the parsed canonical file, `blockCount` = root's direct children + each `listItem` + each `tableRow`, `sectionCount` = heading count, `paragraphStartLines` from the canonical file's own parse; diff is purely additive, no existing entry touched), `docs/progress/journal-main.md`, `docs/lessons.md`. No edits to any 0.4 fixture file; no new dependency (`git diff --stat` touches only the files named above plus the two docs). Tests: reproduction first — with `parse-line-endings.test.ts` written and `parse.ts` stashed back to its pre-fix state, `npx vitest run packages/core/test/parse-line-endings.test.ts` failed all 4 tests (the paragraph and fenced-code cases failing the node-value assertion — `\r\n` from the CR micromark drops only at block boundaries — and the html/yaml cases failing the same way since those opaque nodes capture raw bytes verbatim); after restoring the fix, the same command is 4/4 green. `npx vitest run packages/core/test/roundtrip.test.ts packages/core/test/fixtures-corpus.test.ts packages/core/test/parse-line-endings.test.ts` is 151/151 (roundtrip's 141 = 3 invariants × 47 fixtures, confirming both new fixtures are picked up from the directory; fixtures-corpus.test.ts's 6 tests are the fixed count-equality/schema/hand-computed assertions, all still green with the corpus at 47). Full bar: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 578/578 across 19 files (566 + 4 new CRLF unit tests + 6 new roundtrip.test.ts cases for the 2 new fixtures; coverage 99%/96.3%/100%/99.67%, parse.ts itself still 100%/100%); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. Iterations used: 1. First attempt passed: y. Tool calls: ~24. Notes: nothing outstanding from 0.22.
- [0.verify.r1] 2026-09-06T17:30:00Z Task: Phase 0 verification for review attempt r1 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change), run the full suite in the container, confirm the phase head (0.22) is ready for the three-OS CI gate, and carry out the `promote-lessons` manual step (DECISIONS #review-0-r0) — append the two named rules, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md. Status: Complete. Files: `CLAUDE.md` and `AGENTS.md` (each +2 lines at the end of the `## Code rules` list, identical text in both, byte-identical below line 1 confirmed by `ralph/check-agent-rules.sh` and `tests/agent-rules.test.ts`), `docs/progress/journal-main.md` (this entry). No source file, fixture, or dependency manifest touched. Tests: `git diff --stat HEAD` shows exactly `AGENTS.md | 2 ++` and `CLAUDE.md | 2 ++`, 4 insertions, 0 deletions, no other file; `git diff --stat HEAD -- '**/package.json' '**/Cargo.toml' pnpm-lock.yaml Cargo.lock pnpm-workspace.yaml` is empty, so no dependency-manifest or lockfile change. `bash ralph/check-agent-rules.sh` prints `check-agent-rules: OK`. Full bar: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 578/578 across 19 files including `tests/agent-rules.test.ts`, coverage unchanged (99%/96.3%/100%/99.67%); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests. The task's `ci`/`ciAcceptance` fields (three-OS run via `ci/0.verify.r1/a{n}`) are the runner's CI-gate task to execute, not this loop task's acceptance; nothing here claims that run happened, and this container is Linux so no macOS/Windows pass is claimed. Iterations used: 1. First attempt passed: y. Tool calls: 12. Notes: the two promoted lines are exactly the task text's wording, appended after the existing last Code-rules bullet (secrets) and before the `## Runner facts` heading, in both files identically; nothing outstanding from 0.verify.r1.
- [0.12.r1d] 2026-09-07T06:26:05Z Task: reconciliation of Phase 0 review attempt r1 at 5649a93 (verifier 0.verify.r1). Claude PASS (0 blockers/4 should-fix/4 nits, on its third container run after two max_turns exits at 50 and 80 turns), Sol FAIL (2/1/0), Grok PASS (0/1/2). Verdict FAIL: the front-matter writer accepts a quoted duplicate of an app-owned key and writes control characters and line terminators raw (G1, G2; the F2 class the r0 reconciliation treated as a blocker), plus a spanning-mark split under identity edits (G3), a lock path freed during stale-lock recovery (G4), an untested review-start guard (G6) and two identity-rule gaps (G7, G8). Status: Complete. Files: docs/reviews/phase-0-r1-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#review-0-r1: verdict, 13 findings with severity and disposition, r0 dispositions confirmed, zod ratification and deviations 27–32 recorded, manual procedures for the handoff branch and the reviewer budget, reversal), docs/PRD.md (§8 Phase 0: +0.23–0.26 fix tasks, +0.verify.r2 needsCI with one promotion, +0.12.r2a/b/c/d, 0.close → 0.12.r2d; §6.1 LF wording; §7 gate row; promote-lessons references), ralph/tasks.json + ralph/EXPECTED_COUNT (140 → 150), docs/RUNNER-SPEC.md (§1 stale lock manual; §5.1 handoff branch; §11 reviewer budget), docs/V1.1-BACKLOG.md (+4 lines), docs/lessons.md (+6 class-level), docs/progress.md (regenerated), docs/handoffs/006-* and next-prompt.md (cherry-picked from handoff/006, deviation 29). Tests: node ralph/generate-tasks.mjs --check, node ralph/validate-tasks.mjs (150), bash ralph/test/run.sh green. Commit: wip(0.12.r1d). Iterations used: 1. First attempt passed: y. Tool calls: ~30. Reviewer overturned: n/a. Notes: after integration the runner runs 0.23 (opus) first; no image rebuild until 0.26 integrates (compose.yml, F17); next human gate is scripts/gate.sh 0.verify.r2h; the rotate.md procedure change waits for a chore commit on phase/1 before 1.verify.
- [0.23] 2026-09-07T06:45:07Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G1, G2, G7, G8; Sol finding 2 and Claude finding 1, blockers; Grok finding 1 and Claude finding 3, should-fix required): `packages/core/src/sidecar.ts`'s front-matter writer disagreed with its reader about what is representable. Status: Complete. Files: `packages/core/src/sidecar.ts`, `packages/core/test/front-matter-boundary.test.ts` (new, 97 tests), `docs/progress/journal-main.md`, `docs/lessons.md`. No other file: `git diff --stat HEAD` is those two code files plus the two docs, and `git diff --stat HEAD` over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty (zero new dependencies, no YAML library). The four fixes. (a) Key identity (G1): `scanFrontMatter` now decodes a quoted key spelling through the same two scalar readers a value uses, so `"question"`, `'question'` and `"questio\u006e"` all carry the identity `question` and a new `quotedKey` flag; `readFrontMatter` reports `duplicate` when a quoted spelling stands beside the plain one and `malformed` when it stands alone (both `writable: false`, so `writeFrontMatter` refuses whole and returns the argument root), and a key that opens on a quote it never closes marks the block malformed rather than inventing a key name. The block's other keys stay readable in the quoted-spelling cases — only the block-malformed case takes them down. (b) One quoting predicate (G2): new `isUnwritableRaw(value, i)` / `hasUnwritableRaw(value)` own the whole boundary — C0, DEL, C1, U+2028, U+2029 and lone surrogates — and `needsQuoting`, `serializeScalar` and `encodeDoubleQuoted` all ask it, so the reader's boundary and the writer's cannot drift apart again; `encodeDoubleQuoted` gained `\u2028`/`\u2029` (it already had `\xXX` for the control ranges and `\uXXXX` for lone surrogates) and now decides raw-vs-escape from the predicate instead of repeating the ranges; the single-quoted branch of `serializeScalar` promotes to double-quoted rather than emitting raw. `KEY_LINE`, `BLANK_OR_COMMENT` and `SCALAR_TAIL` match to end of line with `[^\n]*` instead of `.` (the scan has already split on `\n`; a dot under `u` without `s` also excluded `\r`, U+2028 and U+2029, so a line carrying one read as "not a mapping line" and made the whole block malformed). (c) Order of the skip (G7): `writeFrontMatter` computes `pending` — the requested keys that are *not* an unchanged writable value — first, returns `{ok:true, changed:[]}` with the argument root when `pending` is empty, and only then applies the line-break refusal, the `no-front-matter` check and the writability check, to `pending` alone. The relative order of those three is unchanged, so 0.15's "refuses the line break before it looks at the block at all" still holds. (d) G8: the corpus-wide identity case for an unchanged front-matter value now exists. Tests: reproduction first — with the new file written and `packages/core/src/sidecar.ts` stashed back to HEAD, `npx vitest run packages/core/test/front-matter-boundary.test.ts` reported 31 of 57 failing (the six quoted-spelling cases, the unclosed-quote case, 17 of the 21 code-point round-trips — the four that passed are C0/DEL/C1 into an already-double-quoted seed, which the old `encodeDoubleQuoted` already escaped — three of the eleven inverse-property rows plus the non-vacuity case, both U+2028 line-classification cases, and two of the three no-op cases); after restoring the fix the file is 97/97. Honest note: the G8 corpus block (9 fixture cases) passes on the pre-fix code as well, because G8 is a missing test rather than a bug — no fixture in the corpus carries a shape the old writer damaged; it is recorded as the identity case CLAUDE.md's promoted rule names, and the absence leg (every fixture the index says has no yaml node returns `no-front-matter` with root identity) was added so the rule reads corpus-wide rather than yaml-only. Fixture names are read from `fixtures/markdown/index.json` (`nodeTypes` contains `yaml`), never a literal count, and the file asserts both that the yaml list is non-empty and that the corpus carries a writable app-owned key and a non-writable one. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 675/675 across 20 files (578 + 97), coverage 99.45%/96.97%/100%/100%, up from 99%/96.3%/100%/99.67%, every threshold met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged; `bash ralph/test/run.sh` 71/71, untouched by this task. The 146 existing tests in `sidecar.test.ts` and `front-matter-grammar.test.ts` pass unedited — no test of a passed task was rewritten this time. Iterations used: 1. First attempt passed: y. Tool calls: ~25. Notes: two design readings a reviewer should check. First, the reason code for a lone quoted spelling: the §6.1 reason list is closed and none of its members says "a key spelled a way this writer cannot address", so the lone case reuses `malformed` and the paired case `duplicate`; both are `writable: false` with root identity, which is what the acceptance pins, and the choice is stated in the `FrontMatterUnsupportedReason` doc comment. Second, a quoted key that does not close on its line sets `malformed` for the whole block (fail-closed) rather than being treated as an opaque key name: YAML would reject such a block outright, and inventing a key name for it is how G1 happened. Nothing outstanding from 0.23; 0.24 is next in the chain.

- [0.24] 2026-09-07T06:56:00Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G3, Sol finding 3, should-fix required): `packages/core/src/sentences.ts` rebuilt the paragraph from per-sentence slices even when the argument asked for no change, so a mark spanning a sentence boundary came back split. Status: Complete. Files: `packages/core/src/sentences.ts` (two short-circuits and the doc comments), `packages/core/test/sentences-spanning-marks.test.ts` (new, 24 tests), six new fixtures under `fixtures/markdown/` with canonical siblings (`spanning-mark-strong`, `-emphasis`, `-link`, `-strikethrough`, `-first-boundary`, `-last-boundary`), `fixtures/markdown/index.json` (six additive entries), `docs/progress/journal-main.md`, `docs/lessons.md`. No other file: `git diff --cached --stat` is those files only, and the same diff over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty (zero new dependencies), so the edits stay inside the three trees the task text names. The three things asked for. (1) The no-op is the identity: `reorderSentences` returns the argument root when `order` is the identity permutation, and `replaceSentence` when `text` equals `sentenceMarkdown(paragraph, index, options)` — both after the full validation a real edit gets (unknown block, non-top-level block, non-permutation, out-of-range index all still throw, tested), and both decided from the validated argument rather than from a comparison of the rebuilt output's bytes (a test replaces `*One.*` with `_One._`, which serialises to the same bytes, and asserts the call is *not* the identity and takes the slicing path). The identity permutation is the only permutation of the empty sentence list, so the old `sentences.length === 0` branch is now unreachable and was removed rather than left as dead code under the coverage thresholds. (2) The corpus grew by six LF fixtures, one paragraph each, covering `strong`, `emphasis`, `link` and `delete` across a boundary plus a mark spanning the first boundary and one spanning the last (both in four-sentence paragraphs, so first, middle and last are distinguishable); each `.canonical.md` is `format(parse(source))` and is byte-identical to its source, and the `index.json` entries were computed the way the existing ones are (`nodeTypes` from the parsed canonical, `blockCount` = root's direct children + each `listItem` + each `tableRow`, `sectionCount` = heading count, `paragraphStartLines` from the canonical's own parse); the diff is purely additive, no existing entry touched. The new test reads the fixtures out of `index.json` and asserts the shape is present (one test per mark type, one for the first/middle/last boundary positions) rather than naming files, so deleting a fixture fails the suite. (3) The rule for a real edit is stated in `sliceInline`'s doc comment and referenced from both operations: no sentence keeps a spanning mark — every sentence the mark covers keeps its own copy over its own share of the text, and the boundary whitespace is unwrapped from the mark and left in place — which is the rule under which the bytes of every untouched sentence are unchanged, since an untouched sentence's own Markdown is the same slice of the same mark before and after. That is tested at the first, middle and last position for both operations (`**One. Two. Three.**` replaced at 0/1/2; `**One. Two. Three. Four.**` swapped at the first, a middle and the last position) and corpus-wide over every spanning-mark fixture. The doc comment also says why the alternative rule (keep one whole mark around the edited span) is not available in general: a mark covering only part of the sentences at its ends has no span left once those sentences move apart. Tests: reproduction first — with the new test file and the six fixtures in place and `sentences.ts` restored to HEAD, `npx vitest run packages/core/test/sentences-spanning-marks.test.ts` was 5 failed / 19 passed (both Sol probes, the mark-stays-whole assertion, the empty-permutation identity and the corpus no-op test; the rule-for-real-edits tests pass either way, because the rule is the one the code already implemented and this task only documents and pins it), and `npx vitest run packages/core/test/sentences-zero-width.test.ts packages/core/test/roundtrip.test.ts` was 7 failed / 222 passed — all six new fixtures failing the 0.14 corpus-wide identity invariant, which is the gap Sol named. After restoring the fix both files are green. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729 across 21 files (675 + 24 new + 18 roundtrip A/B/C for the 6 fixtures + 6 corpus identity + 6 front-matter-boundary corpus cases, every one of those four growths read from the index or the directory, not a literal), coverage 99.45%/96.98%/100%/100%, thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. The 30 cases of `sentences.cases.json` and the existing `sentences.test.ts` (91) and `sentences-zero-width.test.ts` (70) tests pass unedited — no test of a passed task was rewritten. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: one design reading a reviewer should check — the short-circuits return the *argument root object*, not a structurally equal copy, which is what `expect(next).toBe(root)` pins; it follows 0.23's `writeFrontMatter` precedent and means an identity reorder no longer strips `position` from the paragraph it did not change. `applyReorderSentences` (sidecar.ts) passes that same root to `reanchor`, whose anchors are unmoved under the identity permutation; its 73 tests are green. Nothing outstanding from 0.24.
- [0.25] 2026-09-07T07:07:46Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G4 and G5, Sol finding 1 and Claude finding 6, should-fix required; runner code, conformance tests required): (1) `breakStaleLock` renamed a lock it judged stale before it compared bytes, so the lock path was free between the rename and the compare and a third entrant could create it and run beside a live holder (Sol's probe `/logs/reviews/0/r1/sol/lock-probe.{mjs,log}`: "third entrant inside critical section while live holder active: true"); (2) `writeAtomic` swallowed a failed directory fsync in a bare catch, so a close intent published without directory durability left no trace. Status: Complete. Files: `ralph/lib/util.mjs`, `ralph/lib/doctor.mjs`, `ralph/lib/state.mjs` (2 lines, see the deviation below), `ralph/test/util.test.mjs` (rewritten to the new contract, 13 tests including subtests, was 6), `ralph/test/conformance.test.mjs` (+1 scenario, 34 tests, was 33), `docs/progress/journal-main.md`, `docs/lessons.md`. `docs/RUNNER-SPEC.md` untouched — §1 already carries the amendment the 0.12.r1d reconciliation made. Zero new dependencies: `git diff --stat` over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty. What changed. (1) Automatic stale-lock breaking is cut to a manual procedure, as CLAUDE.md prefers over more machinery: `breakStaleLock` and the `hooks.beforeBreak` interleaving point are deleted (the identifier no longer appears anywhere under `ralph/lib/`), and `withLock` now never renames or unlinks a lock it did not create. A lock whose holder pid is not running makes `withLock` throw a `RalphError` naming the file, the dead pid and the recovery; `doctor` reports the same lock as `stale-lock <pid> (<path>)` with the identical repair sentence, because both quote one exported helper (`staleLockRepair`) and read the holder through one exported helper (`lockHolder`), so the refusal and the drift report cannot describe different procedures. The token re-read after acquisition and the token-scoped release are kept unchanged; `hooks.afterCreate` is the one interleaving point left. A lock file whose first field is not a positive integer now counts as held-by-something-unidentifiable (`stale-lock unknown`) rather than as breakable — the old code treated `Number("garbage")` as "no pid" and broke it. (2) `writeAtomic` takes `{ fs, audit }`; the directory-fsync catch now calls `audit("writeAtomic: directory fsync failed <path> <code>")` and continues. The sink defaults to `auditNote`, which appends to the state layer's own `state/audit.log`: the `Ctx` constructor hands util.mjs that path via `setAuditLog` (util.mjs sits below the state layer and must not re-derive an evidence path), and with no path named yet the line goes to stderr rather than being lost. `<path>` is the file the call published, not its parent directory — the parent is `dirname` of it, and the fact worth recording is which intent lacks durability; the reading is stated in the doc comment. Tests: reproduction first. With both new test files in place and `ralph/lib/` restored to HEAD, `node --test ralph/test/util.test.mjs` was `# tests 13 / # pass 5 / # fail 8` and `node --test ralph/test/conformance.test.mjs` was `# tests 34 / # pass 33 / # fail 1`. The 8 + 1 red cases are the findings: the stale-lock refusal and its message, the "every later entrant sees the same lock file (same inode)" case, Sol's three-actor sequence, the two `writeAtomic` audit-line cases (directory open throws, directory fsync throws), and the CLI-level scenario. Honest note on which of the new cases do *not* reproduce a finding: "a third entrant driven through `hooks.afterCreate`" and "a directory fsync that succeeds writes no line at all" pass on the old code too — they are the presence/absence partners of the cases that do reproduce, kept so the rule reads as a property rather than a single symptom. After the fix `bash ralph/test/run.sh` is `# tests 79 / # pass 79 / # fail 0` (was 71/71). The three-actor test represents the live holder A by a lock file carrying a running pid rather than by a second process, because a holder's identity is exactly what `withLock` reads from that file, and it asserts the outcome Sol's probe wanted by inode: the live holder's lock file is the same inode before and after the third entrant's attempt (so the path was never freed), the third entrant waits the full timeout and throws `held by pid <pid>`, and no critical section ran beside the holder. The conformance scenario drives the same contract through the real entry points: with a dead-pid lock in place, `ralph.sh sync-state` exits 1 with the recovery message and `digestDir(.evidence/state)` is byte-identical before and after; `ralph.sh doctor` exits 3 with `stale-lock <pid> (<path>) → manual repair: … pgrep … remove <path> by hand` and `DOCTOR 1 findings`; `ralph.sh run` refuses and selects nothing; the lock file's bytes are unchanged throughout; and after removing the lock by hand `doctor` is clean and `run` integrates 0.1 normally. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729 across 21 files, coverage 99.45%/96.98%/100%/100%, unchanged (this task touches no `packages/` source); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests; `RALPH_ROOT=/work ralph doctor` with this worktree's code prints `doctor: clean`, exit 0. Rewritten tests of a passed task, with the reason (0.15's precedent): `ralph/test/util.test.mjs`'s first two subtests from 0.20 — "a stale lock is broken and taken, and released on the way out" and "a concurrent breaker between the staleness check and the break … the other stays the sole holder" — asserted the exact behaviour this finding calls wrong (that a stale lock *is* broken, and that `hooks.beforeBreak` exists), so "the existing tests still pass" and the fix could not both hold. They are replaced in place by the refusal cases; the two tests that survive unchanged are the post-acquisition token re-read and the token-scoped release, and the `writeAtomic` call-order test is unchanged. DEVIATION (declared): the task text's file list is `ralph/lib/util.mjs`, `ralph/lib/doctor.mjs`, `ralph/test/` and the journal, and this diff also touches `ralph/lib/state.mjs` — two lines, an import and `setAuditLog(this.paths.audit)` in the `Ctx` constructor. The task requires the durability line to land in "the audit log the state layer already writes", and only the state layer knows that path; the alternative (util.mjs re-deriving `<root>/<RALPH_EVIDENCE_DIR>/state/audit.log` for itself) duplicates the layout in a second place, which is the class of drift G1/G5 are both about. Second declared deviation, same as 0.20's: `docs/lessons.md` is not in the task's file list, and CLAUDE.md step 3 requires a lessons line, so two were appended. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: two readings a reviewer should check. First, `setAuditLog` is module-level mutable state in `ralph/lib/util.mjs` (CLAUDE.md's "no module-level config" rule is scoped to `packages/core`); the CLI builds exactly one `Ctx` per process, and it follows the existing `registerRun`/`globalThis.__ralphRun` precedent in this tree. Second, the refusal is unconditional — there is no `--force` and no timeout after which the runner breaks a lock anyway; a crashed holder now stops the loop until a human removes the file, which is the trade the reconciliation chose (recovery is rarer than the race it caused). Nothing outstanding from 0.25.
- [0.26] 2026-09-07T07:15:34Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G6 and deviation 30, Claude finding 2 and Grok finding 2, should-fix required; runner tests and compose only): the review-start block of `ralph/test/conformance.test.mjs` covered a missing `accepted.json` and a tampered artifact (both refused inside `readAcceptedCi`) but never reached the two guards `stepReview` runs after it (`acc.sha !== verificationSha`, `acc.sha !== implementationSha`); and the three review services shared the task services' 50-turn default with no reviewer-scaled budget. Status: Complete. Files: `ralph/test/conformance.test.mjs` (one new leg, (c), inserted between the existing tampered-artifact and intact-evidence legs of the "review start consumes the verifier gate's evidence" test — same test, no new top-level test), `docker/compose.yml`, `docs/progress/journal-main.md`. No other file: `git diff --stat` is exactly those two files (plus this journal entry), and the same diff over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty (zero new dependencies). (1) The new leg copies the pristine `accepted.json` and artifact bytes captured at the top of the test, restores the artifact (undoing leg (b)'s tamper), flips `accepted.json`'s `sha` field to a different well-formed 40-hex value (first hex digit toggled 0↔1, so `readAcceptedCi`'s digest check still passes — the artifact's own `sha256` entries are untouched), runs `ralph.sh run --phase 0`, and asserts non-zero exit, a message naming `0.verifyh` and matching `accepted sha .* != (verification|implementation)_sha`, and `nothingRecorded()` (the existing closure: no `phase_base_sha`/`verifier_id`/`verification_sha`/`implementation_sha` files, no reviewer directories, the three `0.9.r0{a,b,c}` tasks left `pending`). It restores `accepted.json`'s original bytes afterward so the following intact-evidence leg (renamed (c)→(d), unchanged otherwise) still starts the review normally. Reproduction first: with the two guards (`ralph/lib/run.mjs`, the `acc.sha !== verificationSha` / `acc.sha !== implementationSha` lines) temporarily deleted and the new leg in place, `node --test ralph/test/conformance.test.mjs` reported the "review start consumes…" test `not ok` (an unrelated later signal, `PRINCIPAL 0.9.r0d`, since with both guards gone the review actually starts and the fixture runs out of scripted CI/task responses — this is a real behaviour change downstream of the missing guard, not a broken test); with the guards restored the same run is green (34/34). (2) `docker/compose.yml` gets a second env anchor `x-review-env: &review-env` that merges `*env` (`<<: *env`) and overrides `RALPH_MAX_TURNS: ${RALPH_REVIEW_MAX_TURNS:-160}`; `claude-review`, `codex-review` and `grok-review` use `environment: *review-env` in place of `*env`; `claude-task` and `boundary-check` are untouched (still `*env`, default 50). Honest note: this container has no `docker` binary (host-side `ralph/bootstrap.sh` is what runs `docker compose`), so the acceptance's literal `docker compose config` command could not be executed here; the merge was instead verified by hand-resolving the two anchors from the file text (a scratch, uncommitted Node script, deleted after use) against plain YAML merge-key semantics (explicit keys override merged-in ones) — `x-env`'s `RALPH_MAX_TURNS` resolves to `50` with neither variable set, `x-review-env`'s to `160`, and only the three review services reference `*review-env`. Tests: `node --test ralph/test/conformance.test.mjs` 34/34 (unchanged test count — the new leg is inside an existing test, not a new one); `bash ralph/test/run.sh` 79/79 (unchanged from 0.25, same reason); full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729, coverage 99.45%/96.98%/100%/100%, unchanged (this task touches no `packages/` source); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: nothing outstanding from 0.26; this was the last of the four r1 fix tasks named ahead of 0.verify.r2 (0.23, 0.24, 0.25, 0.26) per the 0.12.r1d reconciliation's PRD §8 additions.
- [0.verify.r2] 2026-09-07T07:25:00Z Task: Phase 0 verification for review attempt r2 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change), run the full suite in the container, confirm the phase head (0.26) is ready for the three-OS CI gate, and carry out the `promote-lessons` manual step (DECISIONS #review-0-r1) — append the one named rule, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md. Status: Complete. Files: `CLAUDE.md` and `AGENTS.md` (each +1 line at the end of the `## Code rules` list, identical text in both, appended after the zero-width-ownership bullet and before the `## Runner facts` heading), `docs/progress/journal-main.md` (this entry). No source file, fixture, or dependency manifest touched. Tests: `git diff --stat` shows exactly `AGENTS.md | 1 +` and `CLAUDE.md | 1 +`, 2 insertions, 0 deletions, no other file; `git status --porcelain` after `pnpm lint`, `pnpm test` and `cargo test` (all three trigger no install/build artefacts under version control) confirms no `pnpm-lock.yaml`, `Cargo.lock`, `package.json` or `Cargo.toml` diff anywhere in the tree. `bash ralph/check-agent-rules.sh` prints `check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`, exit 0. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729 across 21 files including `tests/agent-rules.test.ts`, coverage 99.45%/96.98%/100%/100%, unchanged from 0.26, every threshold met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. Also ran, though not named by this task's acceptance, as a check that the phase head is CI-ready after 0.26's runner/compose changes: `bash ralph/test/run.sh` 79/79, unchanged from 0.26. The task's `ci`/`ciAcceptance` fields (three-OS run via `ci/0.verify.r2/a{n}`) are the CI gate's job to execute, not this loop task's acceptance; nothing here claims that run happened, and this container is Linux so no macOS or Windows pass is claimed. Iterations used: 1. First attempt passed: y. Tool calls: 11. Notes: the promoted line is exactly the task text's wording, appended after the existing last Code-rules bullet (zero-width partitioning) and before the `## Runner facts` heading, in both files identically; the promoting task naming, per the newly-promoted rule itself, which test discharges the rule it promotes: the rule is a process rule about future fix tasks' guard-level tests, not a code behaviour, so it has no discharging test of its own in this task — `ralph/check-agent-rules.sh` and `tests/agent-rules.test.ts` discharge the identity requirement (CLAUDE.md ≡ AGENTS.md below line 1) that lets the promotion land in both files at once. Nothing outstanding from 0.verify.r2; next is gate `scripts/gate.sh 0.verify.r2h`, then review attempt r2 (0.12.r2a/b/c/d).
- [0.12.r2d] 2026-09-07T11:11:39Z Task: reconciliation of Phase 0 review attempt r2 at 98bee5a (verifier 0.verify.r2). Claude PASS (0 blockers/4 should-fix/4 nits, 110 of 160 turns, one container run), Sol PASS (0/0/0), Grok PASS (0/0/1). Verdict PASS: no blocker and no should-fix marked required (RUNNER-SPEC §5.4); every r1 disposition G1–G13 confirmed by all three reviewers against the tree, none reopened; findings H1–H9 reconciled — 1 should-fix taken here (RUNNER-SPEC §1 named the lock as `flock`; now the O_EXCL file, H4), 3 should-fix deferred with revisit points (unidentifiable lock holder folded into "dead", H1; YAML implicit-type quoting in the front-matter writer, H2; corpus-wide identity for the three block operations, H3), 5 nits (PRD corpus counts taken, H6; four recorded). Status: Complete. Files: docs/reviews/phase-0-r2-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#review-0-r2: verdict, 9 findings with severity and disposition, r1 dispositions confirmed, deviations 33–34 and two observations, reversal; #009's lock bullet carries a marked amendment), docs/RUNNER-SPEC.md (§1 lock primitive), docs/PRD.md (§7 gate row count; 1.1 and 1.2 acceptances drop the literal 45), ralph/tasks.json (regenerated; EXPECTED_COUNT 150 unchanged; graph unchanged), docs/V1.1-BACKLOG.md (+7 lines), docs/lessons.md (+7 class-level), docs/progress.md (regenerated), docs/handoffs/007-* and next-prompt.md (cherry-picked from handoff/007, deviation 29). Tests: node ralph/generate-tasks.mjs --check, node ralph/validate-tasks.mjs (150), bash ralph/test/run.sh green. Commit: wip(0.12.r2d). Iterations used: 1. First attempt passed: y. Tool calls: ~25. Reviewer overturned: n/a. Notes: after integration the runner runs 0.close (main → this commit, phase/1 created at it); the principal rotates (handoff 008) before the runner restarts with --phase 1; first Phase 1 idle window: the chore(0.0) rotate.md/PRINCIPAL.md commit (handoff 007 Next Steps 6) and, on Michael's OK, the H1 fix(runner) commit with its conformance leg; no image rebuild pending (no docker/ change since 0.26).
- [1.1] 2026-09-07T11:45:00Z Task: ProseMirror schema for the §6.1 node set in `packages/editor/src/schema.ts`, with `mdastToPM(root)` and `pmToMdast(doc)`; `html` becomes a non-editable `raw` node rendering a grey box; `yaml` is held outside the PM doc and re-attached on serialize. Status: Complete. Files: `packages/editor/src/schema.ts` (new, the whole task — the task text names exactly one file and the converters are the schema's inverse pair, so they live in it rather than in a second file this task would have had to invent), `packages/editor/src/index.ts` (re-exports; `placeholder()` and its 0.1 test are untouched), `packages/editor/test/schema-roundtrip.test.ts` (new, 58 tests — the acceptance), `packages/editor/test/schema.test.ts` (new, 50 tests), `packages/editor/package.json` and `pnpm-lock.yaml` (dependencies, below), `docs/progress/journal-main.md`, `docs/lessons.md`. No file outside `packages/editor/` except the lockfile, the journal and lessons: no fixture was added, no `fixtures/markdown/index.json` entry touched, no `packages/core` source edited, and `git diff --stat` over `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` is empty. Dependencies: `prosemirror-model@1.25.11` (+ its one transitive dependency `orderedmap@2.1.1`) as a dependency of `@essaydown/editor`, plus `@essaydown/core` as a `workspace:*` link and `@types/mdast` / `@types/node` as devDependencies — all four inside PRD §4, which locks "**ProseMirror** with a custom schema mapping 1:1 to the supported mdast node set" as the rendered editor, so no `docs/DECISIONS.md` note is required (this is not a `verify` task; the zero-new-dependencies rule does not apply to it). Only `prosemirror-model` is added, not `-state`/`-view`/`-keymap`/`-inputrules`: nothing in this task mounts an editor, and 1.3 adds those with the input rules that need them. The three things asked for. (1) The schema is 1:1 with §6.1: nineteen mdast types enter the doc (`root` becomes `doc`, `yaml` is held beside it), and `MDAST_TYPES` in the source is the list the test maps, in both directions, onto `Object.keys(nodes) − doc` ∪ `Object.keys(marks)` — so a node type added to the schema without an mdast type, or an mdast type without a node, fails the suite. Four mdast types become marks (`emphasis`, `strong`, `delete` → marks; `inlineCode` → a `inline_code` mark on text, because mdast's `inlineCode` holds literal text and ProseMirror has no inline-node-with-value that a cursor can enter), `list` is one node with an `ordered` attr rather than the conventional `bullet_list`/`ordered_list` pair (mdast has one `list`, and `wrapInList` takes attrs, so 1.3 is unaffected), and `table_cell` is one type with no header variant because mdast has no header cell — the first `tableRow` is the header, which is a view's decision, not the model's. `parseDOM` is deliberately absent, recorded here as a boundary rather than an oversight: this schema's round trip is mdast ↔ PM, no Phase 1 task parses a DOM into it, and asserting `getAttrs` functions would need a DOM in the vitest environment (a new dependency) for a clipboard path that does not exist yet. (2) `html` becomes an atom with `contenteditable="false"` whose `toDOM` shows the *source bytes* as text in a grey box (inline `style`, plus `RAW_CLASS` for a later theme to override — inline, so the box is grey wherever the schema is mounted with no stylesheet for this task to invent). There are two such node types, `raw` (block) and `raw_inline` (phrasing), because ProseMirror types a node as one or the other and mdast puts `html` in both positions; they share attrs, `toDOM` and the atom/contenteditable pair, so they are the one "raw node" of the task text, one per position class. No corpus fixture has inline html, so `raw_inline` is pinned on a hand-written string (`A <span class="x">y</span> B.` → `text, raw_inline, text, raw_inline, text`, byte-identical after the trip) rather than by adding a fixture this task's acceptance does not ask for. (3) `mdastToPM` returns `{ doc, frontMatter }` and `pmToMdast` takes that pair, so `pmToMdast(mdastToPM(root))` is the whole trip with nothing for a caller to carry by hand; the reading of "`pmToMdast(doc)`" is stated in the doc comment — the front matter is genuinely outside the ProseMirror doc (`schema.nodes.yaml` is undefined, asserted), which is what stops a cursor from ever entering it, and it is re-attached as the root's first child with its `value` bytes copied, not re-serialised. Tests: the acceptance, over the corpus — one `it` per fixture **listed in `index.json`** asserting `format(pmToMdast(mdastToPM(parse(x)))) === format(parse(x))`, the loop written over `Object.keys(index)` so the matrix is the index's length (53 today) and never a literal, plus a separate test that the index lists exactly the `*.md` sources on disk, so the index cannot both define and certify the coverage claim (docs/lessons.md [0.12.r2d]); all 53 pass, and they passed on the first run of the converters. The raw-html clause: `raw-html.md`'s `html` node values are collected before and after the trip and compared, with the presence case asserted first (`before.length > 0`) so the equality is not vacuous, the exact bytes pinned, and the absence partner (`paragraph-simple.md` has no `html` node before or after) beside it; every fixture whose parse contains a `yaml` node is checked the same way. `schema.test.ts` adds the shapes the corpus does not contain: the 1:1 mapping in both directions; every `toDOM` exercised (a table-driven case per node type plus a test asserting the table covers exactly the node types that declare a `toDOM`, so a new node type without a case fails); the grey box; front matter present and absent; the empty-paragraph placeholder at both ends (a front-matter-only file and an empty list item keep their bytes); mark runs at the first, middle and last position of a paragraph and two adjacent runs staying separate; marks on leaf inline nodes (`hard_break`, `image`); mdast fields the parser always sets but the type makes optional falling back to the mdast defaults; and four guards, one test each, enumerated from the diff rather than from the acceptance — an unsupported mdast block, an unsupported mdast inline, a block node from a foreign schema and an inline node from a foreign schema each throw an error naming the type and the admitted set, instead of being silently dropped. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts, now including `packages/editor`); `pnpm test` 837/837 across 23 files (was 729/21; +109 from `packages/editor`, of which 108 are new here and 1 is 0.1's untouched `index.test.ts`), coverage `packages/editor/src/schema.ts` 100%/100%/100%/100% and every existing file unchanged, all thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged; `bash ralph/test/run.sh` 79/79, unchanged; `prettier --check packages/editor` clean (the repo as a whole is not prettier-clean and was not before this task). Iterations used: 1. First attempt passed: y. Tool calls: ~25. Notes: one behaviour a reviewer should look at hardest, because it is a real loss and not a bug — **the nesting of two marks over the same span is normalised**. ProseMirror records which marks cover a character, not how the source nested them, so `**[a](b.md)**` comes back as `[**a**](b.md)`; mark declaration order in `marks` is the nesting order (`link` outermost because that is what CommonMark produces for the common `[**a**](url)`, `inline_code` innermost because `inlineCode` can contain nothing), and `roundTrip("**[a](b.md)**") === "[**a**](b.md)\n"` is asserted as the pinned behaviour, not worked around. `emphasis`/`strong` in either nesting serialise to the same bytes, so the visible cases are `link` and `delete` against the others. No corpus fixture nests two marks — a scan of all 53 found zero — so the acceptance is exact on the corpus and says nothing about this; it is stated in the schema's doc comment, tested on hand-written input, and left for the Phase 1 review to accept or send back. Second: an empty paragraph is inserted wherever `block+` has nothing to hold and dropped again on the way back, which is what keeps a front-matter-only file and `-` byte-identical; the two rules are inverses and are tested as a pair. Nothing outstanding from 1.1; 1.2 (position map) and 1.5 (CodeMirror) depend only on 0.close and 1.1 respectively and are unaffected by anything here.
- [1.2] 2026-09-07T12:05:00Z Task: formatter position map in `packages/core/src/positions.ts`: `formatWithMap(root)` returns the canonical string plus a map from every mdast node, by path, to its `{startLine, startCol, endLine, endCol}` in that string, and `nodeAt(map, line, col)` is the reverse lookup; built by instrumenting remark-stringify's per-node output rather than re-parsing. Status: Complete. Files: `packages/core/src/positions.ts` (new, the whole implementation), `packages/core/src/index.ts` (re-exports only), `packages/core/test/positions-corpus.test.ts` (new, 374 tests — the acceptance, over the corpus), `packages/core/test/positions.test.ts` (new, 26 tests — shapes the corpus does not contain, and one test per guard), `docs/progress/journal-main.md`, `docs/lessons.md`. No other file: `git diff --stat` touches nothing under `fixtures/`, no golden file was created or referenced, `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` are untouched, and the same diff over every manifest, `pnpm-lock.yaml`, `Cargo.lock`, `Cargo.toml` and `pnpm-workspace.yaml` is empty — zero new dependencies, nothing added to PRD §4. How the map is built, since "instrumenting rather than re-parsing" is the task's method and not just its outcome. `mdast-util-to-markdown` dispatches every node through `state.handle`, a zwitch over `state.handlers`; `formatWithMap` installs a last extension whose `handlers` wrap every configured handler, so each dispatched node's own output string is captured as it is produced. The wrapped set is read off a real `State` (a throwaway `stringify` of an empty root whose `root` handler copies `state.handlers` and returns `""`) rather than re-composed here, so a later change to `createFormatter` — a new GFM extension, a new opaque handler — cannot leave a handler unwrapped and silently unmapped. Each node's string is then located inside its parent's from a cursor that only moves forward, which is what makes a child's range always a sub-range of its parent's and siblings disjoint (asserted corpus-wide). Two transformations happen between a child's string and the output, and each is handled rather than searched around: `blockquote` and `listItem` prefix their children's lines through `state.indentLines`, so that function is wrapped too and the exact prefixes it inserted are inverted by the exported `indentOffsetMap(source, result)`; and `containerPhrasing` calls a handler that has no `peek` a second time to look ahead at the next sibling, so the same node is emitted twice and only the later emission is kept (`lastPerNode`). Three readings a reviewer should check, all stated in the source's doc comments. (1) Ranges are half-open with an exclusive end, in the 1-based line/column convention mdast itself uses, so a paragraph `abc` on line 1 is `1:1-1:4` and column 4 belongs to no node — the line terminator, like a blank line, is deliberately unowned. The seeded sample therefore draws columns `1..max(1, line.length)`, which are the character positions; this is stated in the test beside the draw. (2) Zero-width ownership (the CLAUDE.md rule): a node whose output is the empty string owns no character, its range is empty and sits at the cursor — immediately after the previous sibling that produced output — so `nodeAt` never returns it. Tested at the first, middle and last child position of a paragraph, plus the `nodeAt` case. (3) Two node kinds get their range from their children rather than from their own output: the root, which spans the whole string including the trailing newline, and `tableRow`/`tableCell`, which `mdast-util-gfm-table` never dispatches (it serialises a table's cells into a matrix and lets `markdown-table` align them) and which therefore take the hull of the descendants that were dispatched. `nodeAt` never returns the root: it contains every position by construction, so it would answer every query — which is also what gives the acceptance's "null only on blank lines" clause its teeth (a blank line between two blocks returns null, asserted, and the sample's null case is asserted to actually occur). Tests: the acceptance, over the corpus. The matrix is `Object.keys(index)`'s length (53 today) and never a literal, and a separate test asserts the index lists exactly the `*.md` sources on disk, so the index cannot both define and certify the coverage claim (docs/lessons.md [0.12.r2d]). Per fixture: `formatWithMap(root).text === format(root)` (the instrumentation is transparent); the mapped `startLine` of every paragraph, in document order, equals `index.json`'s `paragraphStartLines` — with a non-vacuity partner asserting both that the corpus records paragraph lines at all and that some fixture records none; `map.unresolved` is empty and `Object.keys(map.ranges).length` equals the tree's node count, so "every mdast node" is checked rather than claimed; every node's range inside its parent's and after its previous sibling's; `nodeAt` over 200 seeded random `(l, c)` (mulberry32, fixed seed, 200 per fixture = 10,600 samples) returning a node whose range contains the point, or null only on a blank line; an exhaustive partner sweeping *every* character position of every non-blank line of every fixture (the seeded sample is the acceptance's wording, the sweep is the stronger claim); and purity — two calls deep-equal, and `JSON.stringify(root)` unchanged across the call, so the tree is not mutated. `positions.test.ts` adds the shapes the corpus does not contain and one test per guard enumerated from the diff, not from the acceptance: an empty root (empty string, root range `1:1-1:1`, `nodeAt` null); exact expected layouts for a marked paragraph, the opaque `yaml`/`html`/`code` bytes, a blockquote's `> ` prefixes, a nested list's bullet and indent, and a table's row/cell hulls; the empty-table-cell exception reported in `unresolved` with its filled neighbour resolved beside it (presence and absence); the look-ahead dedupe (three adjacent text nodes, one range each, in order); the `at < 0` guard driven for real — a `break` followed by an `html` node makes `containerPhrasing` replace the line ending the break emitted with a space, so the bytes that handler returned are not in the output at all and the node is reported unresolved with its siblings still placed; `indentOffsetMap`'s two guards (a different number of lines, a line that is not the source line with something in front) each refused, with the mapping asserted offset-by-offset including the end offset and a blank line that lost its trailing prefix; and `rangeContains` at the first, middle and last column of a single-line range, on an intermediate line of a multi-line range, and empty (covers nothing), plus `pathDepth`/`childPath`. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 1237/1237 across 25 files (was 837/23 at 1.1; +400, all new here), coverage `packages/core/src/positions.ts` 100%/98.27%/100%/100% statements/branches/functions/lines and every existing file unchanged, all thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged; `bash ralph/test/run.sh` 79/79, unchanged; `prettier --check` clean on the three files this task writes (`packages/core` as a whole is not prettier-clean and was not before this task — `src/index.ts` already failed at HEAD, so it was left as it was rather than reformatted for one added export block). Iterations used: 1. First attempt passed: y. Tool calls: ~30. Notes: the one branch not covered in `positions.ts` (line 139) is the `??=` fallback where the plugin creates `data.toMarkdownExtensions` — `createFormatter`'s own plugin always creates it first, and the fallback is kept rather than asserted away because it is the documented contract of that field. Two boundaries a later task should know: `nodeAt` is a linear scan over `map.entries` (the corpus's largest map is a few hundred entries; if the editor calls it per keystroke on a long essay, sort by start offset and binary-search, which the returned shape already allows), and `unresolved` is the honest exception list — it is empty for all 53 fixtures, but a node the serializer rewrites after emitting it (the `break`-then-`html` case) lands there rather than being given a wrong range. Nothing outstanding from 1.2.
- [1.3] 2026-09-07T12:26:45Z Task: Typora-style input rules and keymap for the 1.1 schema — `# ` heading, `- `/`1. ` lists, `> ` blockquote, ``` code block, `**x**`/`*x*`/`` `x` `` converting on close, `---` rule, `|a|b|`+Enter making a 2-col table, Tab/Shift-Tab list indent, Backspace on an empty list item exiting the list, and no history plugin. Status: Complete. Files: `packages/editor/src/input.ts` (new, the whole implementation — input rules, the two commands, `editorPlugins()`), `packages/editor/src/index.ts` (re-exports only), `packages/editor/test/typing.ts` (new, a headless keyboard), `packages/editor/test/input.test.ts` (new, 68 tests), `fixtures/editor/input-scenarios.json` (new, the 22 scenarios), `e2e/web/editor-input.spec.ts` (new, the acceptance), `e2e/web/playwright.config.ts` (`testDir` widened from `./tests` to `.` so the acceptance's named path is collected; `webServer` runs Vite), `apps/desktop/src/dev/DevEditor.tsx` and `dev-editor.css` (new, the `/dev/editor` route), `apps/desktop/src/main.tsx` (path check for the dev route), `apps/desktop/tsconfig.json`, `apps/desktop/package.json` and `packages/editor/package.json` and `pnpm-lock.yaml` (dependencies, below), `.gitignore` (Playwright's `test-results/` and `playwright-report/` run output), `docs/progress/journal-main.md`, `docs/lessons.md`. No file under `packages/core/`, no `fixtures/markdown/` entry, no golden file, and `git diff --stat` over `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` is empty. Dependencies: `prosemirror-commands`, `-inputrules`, `-keymap`, `-schema-list`, `-state`, `-view` (plus transitives `prosemirror-transform`, `w3c-keyname`) — every added package is a `prosemirror-*` one, inside PRD §4's "**ProseMirror** with a custom schema mapping 1:1 to the supported mdast node set", which 1.1 already established as the covering entry when it added `prosemirror-model` alone and left these to 1.3; no `docs/DECISIONS.md` note is required and this is not a `verify` task. `prosemirror-history` is deliberately absent — asserted by a test reading the package manifest, and a second test asserts the keymaps bind neither `Mod-z` nor `Mod-y`; undo is the store's, in 1.6 (PRD §4). For the same reason `undoInputRule` is not bound, which is stated in the module comment rather than left as an omission. Two readings a reviewer should check. (1) The mark rules measure their three boundaries *forward* from `start` and the length of group 1, never backwards from `end`: `handleTextInput` runs before the triggering character is inserted, so at the moment `**x**` closes, the document holds `**x*` and the closing delimiter is only partly there — a boundary derived from `end`, or found by searching for the delimiter, would move if the content itself contained one (tested: `` `a`b` `` keeps the inner delimiter). `removeStoredMark` is what stops the mark leaking into the next character, since every mark in this schema is inclusive. (2) mdast has one `list` node with an `ordered` attr rather than the conventional two node types (PRD §6.1, 1.1's schema), so `wrappingInputRule`'s default join would fuse a new bullet list into the ordered list above it; both list rules therefore carry an explicit join predicate (same flavour, and for an ordered list the number typed must continue the numbering), with all four cases tested. Tests: the acceptance ran for real. `e2e/web/editor-input.spec.ts` drives Chromium through `page.keyboard.type`/`press` against Vite on `/dev/editor` and reads the rendered Markdown pane back: **24 passed** — the 22 scenarios, a guard asserting the table has exactly 22 entries, and task 0.1's untouched placeholder spec, which the widened `testDir` still collects. The first scenario is the acceptance's own example verbatim (`# Title`, Enter, `Hello **world**` → `# Title\n\nHello **world**\n`). The scenario table lives in `fixtures/editor/input-scenarios.json`, not in the spec, so `packages/editor/test/input.test.ts` replays the same keystroke scripts headlessly through `typing.ts` and asserts the same strings — the browser half proves the rules survive a real `prosemirror-view`, the Vitest half is where the plugin code is covered and where the guards live, one test per guard enumerated from the diff rather than from the acceptance: `tableFromRow` refuses a non-cursor selection, a non-paragraph block and a paragraph that is not a row; `exitEmptyListItem` refuses a non-cursor selection, an item that still has text, an empty paragraph outside a list and an empty *second* paragraph of an item; `tableRowCells` refuses one clause at a time; the thematic-break rule refuses a heading; Tab and Shift-Tab return false outside a list so the key keeps its focus-moving meaning. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 1296/1296 across 26 files (was 1237/25 at 1.2; +59, all new here), coverage `packages/editor/src/input.ts` 100%/100%/100%/100% and every existing file unchanged, all thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged; `bash ralph/test/run.sh` 79/79, unchanged; `prettier --check` clean on the files this task writes. Iterations used: 2. First attempt passed: n. Tool calls: 18. Notes: attempt 1 wrote all of the code above and left it uncommitted with no journal entry (the runner's recovery commit `wip(1.3): recovery of uncommitted changes` is what preserved it); this attempt verified it rather than rewriting it, and the only code change it made was `prettier --write` on two files. What attempt 1 had not done, and what made the difference: **the Playwright browser is not in the container image**, so the acceptance had never been executed — `npx playwright install chromium` (without `--with-deps`, which tries to `su` to root and fails) downloads it to `~/.cache/ms-playwright`, outside the repo, and the spec then runs. Any later task with a pure-web dev-route acceptance (1.5's `/dev/source`, 1.8's `/dev/outline`) needs that step, and a CI job running these specs needs an install step of its own; 1.3 has no `ci` object, so nothing here claims a CI run happened. One behaviour left deliberately: Backspace immediately after a rule fired does not put the typed delimiters back, because binding `undoInputRule` would be a second, invisible undo model beside the store's — stated in the module comment for the Phase 1 review to accept or send back. Nothing outstanding from 1.3.
- [1.4] 2026-09-07T12:45:00Z Task: reveal-on-active-line decorations — the Markdown delimiters for emphasis/strong/code/links/headings are drawn only on the block containing the cursor and are absent everywhere else. Status: Complete. Files: `packages/editor/src/reveal.ts` (new, the whole implementation — `revealDelimiters`, `revealDecorations`, `revealPlugin`, `markDelimiters`, `activeBlock`, `delimiterDOM`), `packages/editor/src/input.ts` (`editorPlugins()` installs the reveal plugin last, +1 import, +1 line, +3 comment lines), `packages/editor/src/index.ts` (re-exports only), `packages/editor/test/reveal.test.ts` (new, 88 tests), `packages/editor/test/input.test.ts` (one existing test's plugin count 3 → 4, see the rewritten-test note below), `e2e/web/editor-reveal.spec.ts` (new, the acceptance), `docs/progress/journal-main.md`, `docs/lessons.md`. No other file: `git status --porcelain` is those seven paths, nothing under `packages/core/`, `fixtures/`, `apps/desktop/` or `e2e/web/playwright.config.ts` was touched (1.3 already widened `testDir` to `.`, so the new spec is collected without a config change), and `git diff --stat` over `pnpm-lock.yaml`, every `package.json`, `Cargo.lock`, `Cargo.toml`, `pnpm-workspace.yaml`, `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` is empty — **zero new dependencies**; `prosemirror-view`, which owns `Decoration`, has been a dependency of `@essaydown/editor` since 1.3. How it works, since "decoration" is the method and not just the outcome. Nothing here changes the document: a delimiter is a `Decoration.widget`, DOM that ProseMirror draws at a position and that is not part of the doc, so `pmToMdast` returns the same tree whether a delimiter shows or not (the dev route's Markdown pane is unchanged by every assertion below) and a block *hides* its delimiters by simply not being decorated any more — there is no second code path for hiding. `revealDecorations` is a pure function of the state and is installed as the plugin's `decorations` prop, which prosemirror-view re-reads on every state update including a selection-only one; that, and nothing else, is what makes the reveal follow the cursor, and it is why the plugin holds no state of its own for task 1.6's undo stack to have to restore. Three readings a reviewer should check, all stated in the source's doc comments. (1) **What is decorated.** The task text names five things — emphasis, strong, code, links, headings — so `markDelimiters` produces exactly `*`, `**`, `` ` ``, `[` … `](url)` (with ` "title"` when the link has one) and `#` … `######` followed by a space. The schema's fifth mark, `delete`, is deliberately **not** revealed because the task text does not name it; that is scope discipline, not an oversight, and it is asserted as an absence case (`markDelimiters(delete)` is null *and* a struck span in the active block contributes no delimiter) beside the four presence cases rather than left silent. Block-level delimiters — a code block's fences, a blockquote's `>`, a list's bullets — are not revealed for the same reason, with the code-block case tested. (2) **Ownership of the ends of a run**, the CLAUDE.md half-open-slice rule: a mark run over a block's inline children is the half-open range `[from, to)` its children cover, the opening delimiter is drawn at `from` and the closing one at `to`, and a mark covering no child cannot exist in ProseMirror (a mark is a property of a node), so no zero-width run is ever drawn. Tested at the first, middle and last position of a paragraph, plus the run that reaches the end of the block. Two adjacent runs therefore meet at exactly one position, where the close of the first must precede the open of the second — which fixes the sign of the `side` numbers: closes negative, opens positive, and within each group the mark's rank in the schema's declaration order (its nesting order, `link` outermost) so `[**a**](u.md)` reveals as `[`, `**`, `a`, `**`, `](u.md)`. The sign has a second, matching meaning in prosemirror-view (a negative side draws the widget before a cursor at the position and puts inserted content after it, which is what keeps a closing delimiter attached to the text it closes), so the two reasons agree rather than one being a coincidence. (3) **The active block is the textblock containing the selection's head** — `activeBlock` walks up from `$head` to the innermost textblock — so a table cell, a paragraph inside a blockquote and a list item's paragraph are each their own unit (all three tested), and a range selection dragged across two blocks reveals the one the cursor ended in. That is the task text's "the block containing the cursor" read literally; revealing every block a selection touches would be a different feature, and the reading is stated in the doc comment and tested in both directions (head in the paragraph, head in the heading). A node selection on a block atom (a `raw` grey box) resolves to no textblock and reveals nothing. Also deliberate: the widgets carry `marks: []`, which is what puts a delimiter *beside* the text rather than inside it — a widget otherwise inherits its neighbour's marks, so a `**` would render inside the `<strong>` it opens (bold) and a `[` inside the `<a>` (clickable). Tests: the acceptance ran for real. `e2e/web/editor-reveal.spec.ts` drives Chromium against Vite on `/dev/editor`: **3 tests, 8 `expect` calls, numbered A1–A8 in the test titles**, which is the acceptance's "8 assertions"; the helpers wait with `page.waitForFunction`, not with `expect`, so the count is exactly eight and can be checked by eye. The document is *typed*, not loaded, so 1.3's input rules build it and the `**` asserted can only be a decoration — the characters are not in the document after the rule fires. A1: with the cursor in the paragraph, its rendered text is `Hello **world**`. A2: the two `**` are the immediate element siblings of the `<strong>`, outside it (`{before, after, strong}` deep-equals `{"**", "**", "world"}`), which is the acceptance's "adjacent to the text". A3–A4: while the cursor is there the heading reads `Title` and contains zero `.essaydown-delimiter` elements. A5–A7: clicking into the heading makes it read `# Title`, its first child element carries `data-delimiter="# "`, and the paragraph is back to `Hello world`. A8: clicking back swaps them again (`{heading: "Title", paragraph: "Hello **world**"}`). The full web suite is 27 passed — the 3 new plus 1.3's 24, so the decorations do not disturb the input-rule scenarios, which read the Markdown pane. `packages/editor/test/reveal.test.ts` is the headless half, where the plugin code is covered and where the shapes the browser does not type are pinned: the five delimiter texts including a link with and without a title; the heading marker at each depth 1–6; a run spanning several children with the same mark as one run; a second run of the same mark later in the block as a second run; two adjacent links with different urls as two runs and two with the same url as one (`Mark.eq`, not the type); adjacent runs meeting at one position with the close ordered first; nesting order for `link`+`strong`; a run carrying an inline atom (an image); an empty block; scoping to a table cell and to a blockquote's paragraphs; purity (same selection twice deep-equal, and `JSON.stringify(doc)` unchanged across the call); the decorations themselves (one widget per delimiter at the delimiter's position, `marks` empty, keys); and one test per guard enumerated from the diff rather than from the acceptance — `markDelimiters` returning null for a mark the task does not name, `activeBlock` returning null with no textblock ancestor, `revealDelimiters` returning `[]` for that selection, the heading branch vs a non-heading block, a link's title present vs absent, a run closing at a child that lacks the mark vs one left open to the end of the block, and the "this mark's run is already open" skip. The widget's DOM factory is the one DOM call in the module and is reached through the view's own `ownerDocument`, so a recording double covers it headlessly (a span with the class, `contenteditable="false"`, the inline style, `data-delimiter` and the literal text) while the browser half exercises it for real — A6 reads `data-delimiter`, which nothing but that factory sets. A corpus sweep puts the cursor at the start of every textblock of all 53 fixtures in `fixtures/markdown/index.json` (the count read from the index, never a literal) and requires every delimiter to lie inside its own block, with a presence partner asserting the sweep really finds all five revealed kinds and no sixth, and a partner asserting the index lists exactly the non-canonical `*.md` sources on disk so the index cannot certify its own coverage (docs/lessons.md [0.12.r2d]); each `it` runs its own sweep, so no assertion depends on the order the others ran in. Rewritten test of a passed task, with the reason (0.15's precedent): `input.test.ts`'s "installs three plugins" asserted a count this task changes; it is rewritten in place — same `it` block, same file, a comment naming 1.4 — to four, and the claim it exists to make (that no plugin here keeps a history) is unchanged and still asserted, with `reveal.test.ts` asserting which of the four contributes the decorations. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 1384/1384 across 27 files (was 1296/26 at 1.3; +88, all new here), coverage `packages/editor/src/reveal.ts` 100%/100%/100%/100% statements/branches/functions/lines and every existing file unchanged, all thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged; `bash ralph/test/run.sh` 79/79, unchanged; `prettier --check` clean on every file this task writes. Iterations used: 1. First attempt passed: y. Tool calls: ~30. Notes: two boundaries a later task should know. First, the Playwright browser binary is still not in the image and the `playwright` CLI is **only** in `e2e/web/node_modules/.bin` — `npx playwright install chromium` from the repo root prints `playwright: not found`; `e2e/web/node_modules/.bin/playwright install chromium` is the command that works, and this worktree also needed `pnpm install --frozen-lockfile` first (a fresh worktree has no `node_modules`). 1.5's `/dev/source` and 1.8's `/dev/outline` need both steps. Second, `revealDelimiters` walks the active block's children on every state update; the corpus's largest block is a few hundred nodes and the walk is linear in that block only (never the document), but if a very long paragraph ever shows up in profiling, the plugin state can cache by `(block node identity, block start)` — nothing in the returned shape would change. Nothing outstanding from 1.4.
- [1.4] 2026-09-07T12:50:00Z Task: reveal-on-active-line decorations (attempt 2) — verification of the implementation attempt 1 committed as `wip(1.4)`, and execution of the Playwright acceptance in this attempt's container. Status: Complete. Files: `docs/progress/journal-main.md` (this entry), `docs/lessons.md` (+2 lines). **No code file was changed by this attempt**: `git status --porcelain` was empty when it started and `git diff` against the 1.3 head is exactly the eight paths the previous entry lists, so `packages/editor/src/reveal.ts`, `input.ts`, `index.ts`, the two test files and `e2e/web/editor-reveal.spec.ts` are attempt 1's bytes, unmodified — this attempt verified them rather than rewriting them, which is 1.3's precedent for a second attempt over a complete-but-unpromised tree. Why a second attempt existed at all, and what it changed: attempt 1 committed the code, the journal entry above and its lessons but never printed the DONE promise, so its claims were unverified evidence rather than a pass; the reason its acceptance claim needed re-executing is that **`~/.cache/ms-playwright` was absent at the start of this attempt** while the worktree's `node_modules` was present (the home-directory browser cache lives in the container, which is recreated per attempt; `node_modules` lives in the worktree, which persists), so whatever attempt 1 ran, no browser binary existed to run the acceptance with when this attempt began. Every number below is from this attempt's own runs, not copied from the entry above. Zero new dependencies re-checked independently: `git diff --stat` against the 1.3 head over `pnpm-lock.yaml`, every `package.json`, `Cargo.lock`, `Cargo.toml`, `pnpm-workspace.yaml`, `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` is empty. Tests: the acceptance, run for real here. `e2e/web/node_modules/.bin/playwright install chromium` (the CLI is a devDependency of `e2e/web` only, so the repo-root `npx playwright` spelling does not resolve; `--with-deps` is not used, it `su`s to root and fails) downloaded Chrome Headless Shell 153.0.8010.12 to `~/.cache/ms-playwright`, and `npx playwright test` from `e2e/web` against Vite on `/dev/editor` reported **27 passed** — the 3 `editor-reveal.spec.ts` tests plus 1.3's 24, so the decorations disturb none of the input-rule scenarios, which read the Markdown pane. Each acceptance sentence checked against the spec's own text, not against the entry above: the `expect` calls are on lines 67, 70, 84, 86, 95, 97, 103 and 106 — **exactly 8 assertions**, the helpers waiting with `page.waitForFunction` rather than `expect` so the count is countable by eye; cursor inside a strong span → the paragraph's rendered text is `Hello **world**` and the two `**` are the `previousElementSibling`/`nextElementSibling` of the `<strong>`, outside it, which is "adjacent to the text" asserted structurally rather than by substring (A1–A2); cursor moved to another block → the paragraph reads `Hello world` and the heading holds zero `.essaydown-delimiter` elements (A3–A4, A7); the heading `#` follows the same rule in both directions — `# Title` with `data-delimiter="# "` on its first child element while the cursor is in it (A5–A6), and back to `Title` when the cursor returns to the paragraph, which reveals its `**` again (A8). The document is typed, so 1.3's input rules build it and the `**` asserted cannot be leftover characters. Full bar in this container, all four runs mine: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` **1384/1384 across 27 files**, coverage 99.65%/97.75%/100%/100% overall with `packages/editor/src/reveal.ts` at 100%/100%/100%/100% and every pre-existing file's numbers unchanged from 1.3, all thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged; `bash ralph/test/run.sh` `# pass 79 / # fail 0`, unchanged. The tree is clean after all of it — Playwright's `test-results/` and `playwright-report/` are gitignored (1.3), so the acceptance run leaves nothing to commit. This container is Linux; no macOS or Windows pass is claimed, and 1.4 has no `ci` object so nothing here claims a CI run happened. Iterations used: 2. First attempt passed: n. Tool calls: 12. Notes: the correction to the record is the attempt bookkeeping only — the entry above says "Iterations used: 1. First attempt passed: y", which was written before that attempt ended without the promise; it is left as attempt 1 wrote it (journal entries are appended, never edited) and this entry supersedes those two fields. Nothing about its technical content is contradicted: every claim it makes that this attempt could check independently — the file list, the zero-dependency claim, the test counts, the per-file coverage, the 8-assertion count and the acceptance's behaviour in a real browser — held. Two boundaries for later tasks, both refinements of what the entry above and 1.3 recorded: the browser install is **per attempt, not per worktree**, so 1.5's `/dev/source` and 1.8's `/dev/outline` must re-run `e2e/web/node_modules/.bin/playwright install chromium` even though a previous attempt's journal says it was installed; and a prior attempt's `Status: Complete` is evidence to re-check, never a substitute for running the acceptance, since the three-command bar does not execute `e2e/web` at all. Nothing outstanding from 1.4.
- [1.5] 2026-09-07T13:04:58Z Task: CodeMirror 6 source view in `packages/editor/src/source.ts` — `lang-markdown` plus a private `styleTags` override layering 7 token tags (heading incl. setext, emphasis/strong, link/image/autolink, inline+fenced code, blockquote marker, list marker, table delimiter) over the stock `markdownHighlighting`'s shared `processingInstruction` tag; `essaydown-tok-*` classes colored in `source.css` via `prefers-color-scheme`, not `EditorView.theme`; `@codemirror/commands` (history) never imported. Status: Complete. Attempt 2: attempt 1's `wip(1.5): recovery of uncommitted changes` (04b2b90, already on this branch before this iteration started) had `source.ts`/`source.css`/`source.test.ts`/`DevSource.tsx`/`dev-source.css` fully written and the `@essaydown/editor` dependency added, but `/dev/source` was never wired into `apps/desktop/src/main.tsx`'s path switch, no `e2e/web/editor-source.spec.ts` existed, and `apps/desktop`'s own `package.json` lacked direct `@codemirror/state`/`@codemirror/view` deps that `DevSource.tsx` imports (pnpm hoists them transitively but `tsc --noEmit` in `apps/desktop` doesn't resolve un-declared deps) — `pnpm lint` failed on that alone. This iteration: added the route branch to `main.tsx` (mirroring the existing `isDevEditor` ternary), added the two `@codemirror/*` deps to `apps/desktop/package.json` (already resolved in the lockfile as transitives of `@essaydown/editor`, so `pnpm install` needed no new resolution), and wrote `e2e/web/editor-source.spec.ts`. Files: `apps/desktop/src/main.tsx`, `apps/desktop/package.json`, `e2e/web/editor-source.spec.ts` (new, this iteration); `packages/editor/src/source.ts`, `packages/editor/src/source.css`, `packages/editor/src/index.ts`, `packages/editor/package.json`, `packages/editor/test/source.test.ts`, `apps/desktop/src/dev/DevSource.tsx`, `apps/desktop/src/dev/dev-source.css`, `pnpm-lock.yaml` (attempt 1, unmodified this iteration). The essay fixture (`fixtures/markdown/essay-fixture.md`) contains a heading, a blockquote, a fenced code block, inline code, a list, a table and two images/links, but **no emphasis run at all** (checked: zero `*x*`/`_x_` spans in the whole file) — so a DOM query scoped to the rendered fixture cannot exercise the emphasis class, matching 1.1's lesson about corpus gaps. `editor-source.spec.ts` instead reads each class's computed `color` from a bare probe `<span>` appended to the already-loaded `/dev/source` page (the 7 CSS rules in `source.css` are unscoped class selectors, so a probe element sees the same cascade a real highlighted span would) and asserts, once per class per theme (`page.emulateMedia({colorScheme})`), that the class's color occurs exactly once among the 7 — 7 × 2 = 14 `expect` calls, matching the acceptance's count exactly. Tests: `pnpm lint && pnpm test && cargo test` green (1394 vitest tests incl. 10 in `source.test.ts`, 100% coverage on `source.ts`; `cargo test` finds 0 tests, unchanged from prior phases — no Rust crate has tests yet); separately, `e2e/web/node_modules/.bin/playwright install chromium` then `playwright test` — all 29 specs pass including the new 2 (`editor-source.spec.ts`'s light and dark tests, 14 assertions total as required). Iterations used: 2. First attempt passed: n (attempt 1 committed working source/CSS/test files but never wired the route, never wrote the e2e acceptance spec, and left a typecheck error — no DONE promise was printed, consistent with the 1.4 lesson that a journal-less wip commit is a lead, not evidence). Tool calls: ~20. Notes: none outstanding for 1.5; 1.8 is the next task noted in `main.tsx`'s route-switch comment to add `/dev/outline` the same way.

- [1.6] 2026-09-07T13:25:00Z Task: document store (Zustand) with task 0.11's undo stack wired to both views — ProseMirror transactions push coalesced `typing` snapshots, and Cmd/Ctrl+Z / Cmd/Ctrl+Shift+Z are bound at store level in the ProseMirror and CodeMirror keymaps. Status: Complete. Files: `packages/editor/src/store.ts` (new, the whole implementation), `packages/editor/src/index.ts` (re-exports only), `packages/editor/test/store.test.ts` (new, 26 tests — the headless half), `e2e/web/editor-undo.spec.ts` (new, 2 tests / 6 assertions — the acceptance), `apps/desktop/src/dev/DevEditor.tsx` (the dev route now renders the store's snapshot and binds the view to it), `packages/editor/package.json` + `pnpm-lock.yaml` (one dependency: `zustand@5.0.15`), `docs/progress/journal-main.md`, `docs/lessons.md`. No other file: `git status --short` lists exactly those, nothing under `fixtures/` is touched, no golden file was created or referenced, and `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` are untouched. The one new dependency is `zustand`, which PRD §4 names in the "State & undo" row ("Zustand document store owning a single snapshot-based undo stack (§6.5)"), so §9 rule 4 does not block it and no `DECISIONS.md` note is due; `Cargo.toml`, `Cargo.lock` and `pnpm-workspace.yaml` are unchanged. What the store is. `createDocumentStore(root, sidecar, options)` returns a `zustand/vanilla` store holding `{stack, document}` plus the four actions `commit`/`undo`/`redo`/`endCoalescing`, each of which is `core/undo.ts`'s function of the same name applied to this store's stack. Nothing about undo is decided in `packages/editor`: the 1 s window, the 500 cap, the coalescing rule and the structural sharing are all 0.11's and are still tested there. `document` is kept beside `stack` as `current(stack)` so a subscriber can select it directly, and it is the stack entry's own object, so its identity changes exactly when the snapshot does. Direction of travel, and how a loop is avoided. `bindProseMirror(store, view, {coalesceKey, now})` wires the two directions and returns `{dispatch, destroy}`. View → store: `dispatch` applies the transaction, and if `docChanged` it serialises the new doc with `pmToMdast`, re-attaching the front matter the store's current root holds (PRD §6.1 keeps `yaml` opaque and the editable document never carries it), and commits it with `coalesceKey: "typing"` and `at: now()`. Store → view: a subscription pulls any snapshot whose `root` is not the object the view is currently showing. The two are told apart by *reference*, not by bytes — `push` stores a root by reference, so the binding remembers the root it just committed and the notification that follows its own commit is a no-op. The pull replaces the whole document in one transaction rather than rebuilding the `EditorState`, so plugins and plugin state survive an undo (test: `keeps the view's plugins across a pull`). Why `BoundView` is two members. The binding names only `{state, updateState}`, which `EditorView` satisfies, so the whole wiring runs headlessly in Vitest against a `FakeView` — the same argument `test/typing.ts` (task 1.3) makes for the input rules, and the reason `store.ts` reaches 100 % statements/branches/functions/lines with no jsdom. Three key names, not two. Cmd/Ctrl+Z is one name (`Mod-z`). Cmd/Ctrl+Shift+Z is two, and this is the thing that would have shipped broken if it had been written from the chord rather than from the libraries: `prosemirror-keymap`'s `keydownHandler` and `@codemirror/view`'s `runHandlers` both look a shifted letter up first as `KeyboardEvent.key` with the shift modifier *dropped* (`Ctrl-Z`, i.e. the binding `Mod-Z`) and only then fall back to `base[event.keyCode]` with the modifier *kept* (`Shift-Ctrl-z`, i.e. `Shift-Mod-z`). Which route fires depends on the browser and the layout, so both names are bound to the same command, and there is one test per route: `redoes on the shifted-letter route` sends `key: "Z"`, and `redoes on the keyCode route` sends a `key` no binding names with `keyCode: 90`, which only the fallback can resolve. Guards enumerated from the diff, one test each. (1) `if (!transaction.docChanged) return` — `pushes nothing for a transaction that leaves the document alone` (a selection move, which is what the reveal plugin of 1.4 produces on every cursor move). (2) `if (doc.eq(view.state.doc)) return` inside the pull — `does not touch a view that already shows the store's document` (the initial pull) and, for the other side of the branch, `pulls the store's document into a view built from a different one`. (3) `if (state.document.root !== shown)` in the subscription — `ignores the store change its own commit caused` and `ignores a store change that lands on the same root object`. (4) `frontMatterOf`'s `first.type === "yaml"` — `re-attaches the store's front matter to every commit, byte for byte` and `commits a document with no front matter as one with none`. (5) `options.now ?? Date.now` — `times a commit with Date.now when no clock is injected`. (6) `destroy` — `stops pulling after destroy`. Acceptance, run for real. `e2e/web/editor-undo.spec.ts` in Chromium: `pnpm --filter @essaydown/e2e-web exec playwright test editor-undo.spec.ts` → 2 passed, and the full `e2e/web` suite → 31 passed, so 1.3's, 1.4's and 1.5's specs still pass against the rewritten `/dev/editor`. The 6 assertions are the acceptance's three clauses: type `one two three` at 10 ms/char then Cmd/Ctrl+Z → the Markdown pane goes from `one two three\n` to `` (2); type `first`, wait 1,500 ms, type ` second`, Cmd/Ctrl+Z → `first\n` (3); Cmd/Ctrl+Shift+Z → `first second\n` (1). The pane reads the *store*, not the view, so each of those is an assertion about which snapshot is current. Per the lesson from 1.4, the browser binary is per-attempt: `pnpm install --frozen-lockfile` then `e2e/web/node_modules/.bin/playwright install chromium` was run in this attempt before the spec. What is deliberately not wired. `sourceUndoKeymap(store)` / `undoKeyBindings(store)` exist, are unit-tested (keys, `preventDefault`, both commands run at the store, and the extension is accepted by a real `CodeMirror EditorState`) and are exported, but they are not installed in any running view: `/dev/source` renders the essay fixture with no store behind it, and giving it one before task 1.7 wires the CM↔store edit path would put a Ctrl+Z on that route that visibly does nothing. Task 1.7 ("Cmd/Ctrl+/ swaps PM ↔ CM over the store") is the task that creates a CodeMirror view over the store, and it installs this extension there. Likewise the store's `endCoalescing()` action exists and is tested but has no caller yet — 1.7's toggle is its caller. Tests: `pnpm lint` (eslint + `tsc --noEmit` in all 6 workspace projects) clean; `pnpm test` 1,420 tests in 29 files pass, coverage `store.ts` 100/100/100/100 and the run's own table shows every per-file threshold met; `cargo test` 0 tests, ok (no Rust changed); Playwright `e2e/web` 31 passed. Linux container only — no macOS or Windows claim, and the `Mod-` prefix's mac leg (Cmd rather than Ctrl) is resolved by the keymap libraries at runtime from `navigator.platform` and is therefore not exercised here. Iterations used: 1. First attempt passed: y. Tool calls: 27. Notes: 1.7 installs `sourceUndoKeymap` on the CodeMirror view it creates, calls `store.getState().endCoalescing()` on the toggle, and pushes CM bursts with `coalesceKey: "source"` — `bindProseMirror` already takes `coalesceKey` as an option for exactly that shape. If 1.7 needs the reverse binding for CodeMirror it should follow `bindProseMirror`'s reference-identity rule rather than comparing serialised bytes.
- [1.7] 2026-09-07T17:15:00Z Task: source toggle — Cmd/Ctrl+/ swaps the ProseMirror and CodeMirror views over the one document store of 1.6, with the cursor carried across by (line, ch) <-> PM position through the position map of 1.2, a toggle that closes the open coalescing group without pushing, lenient re-parsing of unparseable intermediate source, and the dev-only top bar ('Load fixture...' and 'Copy Markdown') on /dev/editor for the Phase 1 human gate. Status: Complete. Files: `packages/editor/src/toggle.ts` (attempt 1 wrote it; this attempt rewrote `afterLastLine`), `packages/editor/src/index.ts` (re-exports only, from attempt 1), `packages/editor/test/toggle.test.ts` (83 tests; attempt 1 wrote it, this attempt fixed three broken cases and added one), `apps/desktop/src/dev/DevEditor.tsx` (rewritten: two views over one store, the chord, the cursor carry, the top bar), `apps/desktop/src/dev/dev-editor.css` (the bar, the hidden file input, `.cm-editor` sizing), `e2e/web/editor-toggle.spec.ts` (new, the acceptance, 4 tests), `docs/progress/journal-main.md`, `docs/lessons.md`. No file under `packages/core/`, no fixture, no golden file, and `git diff --stat` over `docs/PRD.md`, `ralph/tasks.json` and `docs/progress.md` is empty. Zero new dependencies: the diff over every `package.json`, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty — the spec reaches `@essaydown/core` by a relative import of `packages/core/src/index.js` rather than by adding a workspace dependency to `e2e/web` (`@essaydown/core` does not resolve from a Playwright spec; the relative path does, because Playwright transpiles TS outside `node_modules`). What attempt 1 left and what this attempt did. Attempt 1 committed `toggle.ts` and `toggle.test.ts` as `wip(1.7): recovery of uncommitted changes` with no journal entry, no dev-route wiring, no acceptance spec and a red suite (3 failing tests). This attempt re-ran everything itself: one of the three failures was a production bug, two were bad test inputs. (1) Production: `afterLastLine` (the source -> rendered direction for a position no node owns) picked among ties — a paragraph and its last text node end at the same place in the canonical string — by whichever came last in pre-order, i.e. always the innermost, so a cursor on a blank line landed at the end of the paragraph above instead of after it. It now states one ownership rule in its doc comment and implements it: same line as the node's end -> the innermost end (the cursor is in the trailing columns of the block's own last line); a later line -> the outermost end (the block is over). Both branches are tested at the first, middle and last block of a three-paragraph document (`owns an unowned position by the tie rule, at the first, middle and last block`), plus the two single-branch cases attempt 1 already had (`clamps a column past the end of a text node to the end of that node`, `sends a blank line to the end of the block above it`) and the no-candidate case (empty document, front-matter-only document). (2) Test input: `answers a node that is not text with its own start` named ProseMirror position 22 in a document of size 14, so it passed through the unowned-position fallback rather than the code block it claimed to test; the position is now read from the document (`doc.descendants` for the `code_block`) and asserted at two offsets inside it. (3) Test input: the chord test built a real `KeyboardEvent`, which does not exist in the Node unit environment; it now uses the same plain-object shape `store.test.ts` already uses for `keydownHandler`. The dev route. `/dev/editor` now mounts one of the two views into one host: ProseMirror with `storePlugins` + `togglePlugins` + `editorPlugins`, or CodeMirror with `sourceExtensions()` + `sourceToggleKeymap` + `sourceUndoKeymap` and an update listener that hands every document change to `bindCodeMirror`. Both are bound to the store, so neither view is told what the other did; the toggle reads the outgoing view's cursor into `carried` (canonical (line, ch)) and the incoming view converts it back — `carried` is deliberately never cleared, because React StrictMode mounts the effect twice and clearing it would lose the cursor on the second mount. `toggleMode` is called outside the React state updater (StrictMode may call an updater twice, and closing the coalescing group is a side effect). The top bar is dev-only: 'Load fixture...' opens a real file chooser and commits whatever Markdown it is handed, 'Copy Markdown' writes the store's canonical serialisation to the clipboard and reports success or the browser's refusal in a status span, and a mode span shows which view is up. Acceptance, run for real. `e2e/web/editor-toggle.spec.ts` in Chromium (per 1.4's lesson the browser binary is per-attempt: `e2e/web/node_modules/.bin/playwright install chromium` was run in this attempt): 4 passed, and the full `e2e/web` suite 35 passed, so 1.3's, 1.4's, 1.5's and 1.6's specs still pass against the rewritten `/dev/editor`. Sentence 1 of the acceptance: the essay is loaded through the top bar's chooser, the 7th `<p>` is asserted to carry paragraph 7's text (so an indexing mismatch fails there, not inside the cursor assertion), a collapsed DOM range is placed at `sentencesOf(paragraph7)[1].start`, Cmd/Ctrl+/ is pressed, and the CodeMirror cursor's line — identified by its text, because CodeMirror virtualises the lines it does not show — is found in the canonical string, asserted unique in it, and its 1-based index asserted equal to `index.json`'s `paragraphStartLines[6]` (13); the cursor's `ch` is asserted equal to the sentence offset. Every input is read from its own file (`fixtures/markdown/essay-fixture.md`, `index.json`, `sentencesOf`); no line number, offset or prose is copied into the spec. Sentence 2: type in the rendered view, toggle, type in the source view, toggle back, and both the rendered view's text and the Markdown pane show the merged text. Sentence 3: 200 seeded (mulberry32, fixed seed) operations over toggle / arrow / Backspace / an alphabet that includes an unclosed fence, a lone table row and a bare heading marker, with `pageerror` and console errors collected throughout; the run asserts no failures, that the number of toggles is non-zero and that the final mode matches the toggles' parity (so a chord swallowed by an unfocused view cannot pass as a quiet no-op), and invariant A on what the run left behind (`format(parse(md)) === md` for the store's canonical text). Sentence 4: type a word, toggle to source and back, one Cmd/Ctrl+Z empties the Markdown pane (no no-op step to press through) and Cmd/Ctrl+Shift+Z restores it. Tests: `pnpm lint` (eslint + `tsc --noEmit` in all 6 workspace projects) clean; `pnpm test` 1,503 tests in 30 files pass, coverage 99.63%/97.68%/100%/100% with every per-file threshold met (`toggle.ts` 99.35/96.7/100/100; the three uncovered branches are defensive `undefined` guards on map lookups); `cargo test` 0 tests, ok (no Rust changed); Playwright `e2e/web` 35 passed. Linux container only — no macOS or Windows claim, and `Mod-` resolves to Cmd on macOS inside the two keymap libraries, which is not exercised here. Iterations used: 2. First attempt passed: n. Tool calls: 22. Notes: 1.8's `/dev/outline` route inherits a `/dev/editor` that owns the store and swaps two views over it; if it needs a third view, follow the same shape (bind to the store, never to the other view). The cursor carried across a toggle is a *canonical* (line, ch), so while the source view holds intermediate bytes that differ from `format(root)` the position is approximate by exactly the difference between those two strings — deliberate, and stated in `toggle.ts`'s module comment. `sourceExtensions()` still carries no editing keymap, so the source view's Enter and Backspace come from CodeMirror's native `beforeinput` handling; if Phase 2's real source pane needs Mod-Enter, list indentation or bracket matching, that is a keymap task, not a binding one.
- [1.8] 2026-09-07T17:35:00Z Task: outline PROTOTYPE (throwaway, dev route `/dev/outline`, attempt 2) — verification, formatting fix and journal/lessons close-out of the implementation attempt 1 left uncommitted-then-recovered as `wip(1.8): recovery of uncommitted changes` (56c1518), with no journal entry, no lessons entry and never run against Playwright. Status: Complete. Files: `apps/desktop/src/dev/DevOutline.tsx`, `apps/desktop/src/dev/outline-document.ts`, `apps/desktop/src/dev/DevEditor.tsx` (this attempt's only change to each: `npx prettier --write`, formatting only, no behaviour touched — see below), `docs/progress/journal-main.md` (this entry), `docs/lessons.md` (+1 line). Everything else is attempt 1's bytes, unmodified: `apps/desktop/src/dev/outline-handoff.ts`, `apps/desktop/src/dev/outline-hints.ts`, `apps/desktop/src/dev/dev-outline.css`, `apps/desktop/src/dev/dev-editor.css`, `apps/desktop/src/main.tsx`, `e2e/web/outline-produce.spec.ts`, `apps/desktop/package.json`, `pnpm-lock.yaml`. `git diff --stat` against the 1.7 head (a7db180) over those files is 11 files, 543 insertions, 9 deletions; no file under `packages/core/`, no fixture, no golden file, and `docs/PRD.md` / `ralph/tasks.json` / `docs/progress.md` are untouched. Zero new dependencies beyond PRD §4's own line: the only manifest/lockfile diff against 1.7 is `apps/desktop/package.json` (+3: `@dnd-kit/core`, `@dnd-kit/sortable` — the exact package PRD §4's "Drag and drop" row names — and `@types/mdast`, a devDependency-only type package `outline-document.ts` needs for its `Heading`/`Root`/`Yaml` imports, added to `apps/desktop` for the same reason 1.5's lesson records: a dev route's own `tsc --noEmit` only resolves types for packages listed in its own manifest) and `pnpm-lock.yaml` (+56, the resolution of those three). What the implementation is (attempt 1's, read and checked by this attempt rather than rewritten). `DevOutline.tsx`: a topic-question input, an add/remove list of questions (`OutlineQuestion { id, text, depth: 0 | 1 }`, order in the array is document order per `outline-document.ts`'s doc comment), Nest/Outdent buttons at the one level the task text names, dnd-kit `DndContext`/`SortableContext` reordering siblings within a depth (a drop across depths is a no-op — stated in the source comment, since the task's own nest/outdent buttons own the depth boundary), and a "Produce" button gated on a non-empty topic and at least one question. `outline-document.ts`'s `buildOutlineDocument` turns the topic and questions into an mdast root (one `heading` per question, `depth 2` for `depth 0` questions and `depth 3` for `depth 1`), writes `question: <topic>` into an initially-empty front-matter block with `writeFrontMatter` (task 0.23), and formats it — the muted hint line is deliberately not Markdown (§6.1 owns exactly `title`/`question` in front matter) and instead travels as `OutlineHandoff.hints` (question text, in document order, one per heading by construction). `outline-handoff.ts` is a one-shot `sessionStorage` channel between the two separate page loads (`main.tsx` has no client router, so React state cannot cross the navigation); `read` is non-destructive so a StrictMode double-mount cannot lose it, and the caller (`DevEditor`) clears it itself from a `useEffect`, which commits once. `outline-hints.ts`'s `questionHintPlugin` draws each hint as a ProseMirror widget decoration at its heading's own position, `side: -1` (before the heading, a sibling of its block element — "above the heading") and `marks: []` (the same reason `reveal.ts`, task 1.4, sets it: a widget otherwise inherits a neighbour's marks). `DevEditor.tsx` reads the handoff once via `useMemo` before the store is created, seeds the store from `parse(handoff.markdown)` when present, and installs the hint plugin only when `hints.length > 0` — a direct visit or "Load fixture…" leaves the array empty, which the plugin already treats as "draw nothing", so no route/flag distinguishes the two paths. `main.tsx` adds the third dev-route branch (`isDevOutline`) beside 1.5's and 1.7's. This attempt's own work: three checks and one fix. (1) Read every file end to end and traced the acceptance's Markdown string by hand against `buildOutlineDocument` and `writeFrontMatter`'s contract (task 0.23) — the empty-front-matter-block-plus-one-key path has no failure branch reachable from a single-line `<input>` value, which `outline-document.ts`'s doc comment states and asserts (`if (!written.ok) throw`, unreachable, documented as a compiler-forced narrowing per CLAUDE.md rather than silenced). (2) Ran `pnpm lint && pnpm test && cargo test` myself: lint clean (eslint + the 6 typecheck scripts, including `apps/desktop`'s, which is what the `@types/mdast` addition keeps green); `pnpm test` 1503/1503 across 30 files, unchanged from 1.7 (`apps/desktop` is outside `vitest.config.ts`'s `include`/`coverage.include` globs — packages/* only — so the new dev-route logic has no vitest coverage, the same as `DevEditor.tsx`/`DevSource.tsx` since 1.5/1.6/1.7; its only test coverage is the Playwright acceptance below); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged (no Rust touched). (3) Per the 1.4/1.4(attempt 2) lesson the browser binary is per-attempt, not per-worktree: `e2e/web/node_modules/.bin/playwright install chromium` (repo-root `npx playwright` still prints `not found`, per 1.4's lesson), then `npx playwright test` from `e2e/web` — full suite **36 passed** (the 1 new `outline-produce.spec.ts` test plus 1.3's/1.4's/1.5's/1.6's/1.7's 35), so the outline route disturbs none of the other dev routes. The fix: `npx prettier --check .` (the repo's `format` script, not part of `pnpm lint`, so it does not gate the three-command bar and was not run by attempt 1) flagged `DevOutline.tsx`, `outline-document.ts` and `DevEditor.tsx` — the three files attempt 1 wrote or touched — for formatting only (no logic difference either side of `--write`, confirmed by re-running the full `pnpm test`, `pnpm lint` and the affected Playwright specs after the reformat, all unchanged). `npx prettier --check .` still reports 81 files outside this diff (mostly `ralph/`) that were already non-conformant before 1.8 and are out of this task's scope; only the three files this task's own diff touches were reformatted. Acceptance, checked against the spec's own text and the browser run above, not copied from the acceptance sentence. `e2e/web/outline-produce.spec.ts`: fills the topic field, adds three questions ("Q1", "Q2", "Q2a") through the draft input and Add button, asserts 3 `[data-testid="outline-question"]` rows, clicks Nest on the row containing "Q2a" and asserts its `data-depth` is `"1"`, clicks Produce, waits for the `/dev/editor` URL, and reads `[data-testid="markdown"]`'s text — exactly `"---\nquestion: What is essay down?\n---\n\n## Q1\n\n## Q2\n\n### Q2a\n"`, the acceptance's canonical Markdown with `<topic>` substituted — plus three `.question-hint` elements, all `toBeVisible`, in order `"Q1"`, `"Q2"`, `"Q2a"`. This is a real cross-navigation, cross-route test: the topic and questions are typed in `/dev/outline`, the document is built and handed off via `sessionStorage`, and `/dev/editor` is a fresh page load that reads it back — nothing here is asserted against component state held in memory across the click. Iterations used: 2. First attempt passed: n (attempt 1 committed a complete, working implementation — `DevOutline.tsx`, the three outline-*.ts modules, the dev-route wiring, the CSS and the acceptance spec — as an uncommitted-changes recovery commit with no journal entry and the acceptance never run, consistent with the 1.4/1.5/1.7 pattern of a prior attempt's `wip` commit being a lead rather than evidence). Tool calls: 14. Notes: nothing outstanding from 1.8. `/dev/outline` is explicitly throwaway (task text) and PRD §8 does not point to a task that revisits it; the real outline UI, if any, is a Phase 2+ concern and would need its own task text before this route's shape is a commitment. `outline-hints.ts`'s `hintDecorations` walks `state.doc.descendants` and returns `false` (does not recurse) only at a `heading` node, `true` everywhere else — fine at prototype scale (no schema node here can contain a heading), but a future non-prototype version of "muted hint above a heading" should state that non-nesting assumption in its own doc comment rather than inherit it silently.
- [1.verify] 2026-09-09T09:00:39Z Task: Phase 1 verification — freeze dependencies (no lockfile change), run the full suite in the container, confirm the phase head is ready for the three-OS CI run the CI gate performs. Status: Complete. Files: `docs/progress/journal-main.md` (this entry) only; no source, fixture, manifest or lockfile touched. Zero new dependencies: `git diff --stat` against the 1.8 head (`d707403`, the last code-bearing commit — 1.9 is a human gate with no commit of its own) is 9 files (`docs/DECISIONS.md`, `docs/RUNNER-SPEC.md`, `docs/V1.1-BACKLOG.md`, two handoff docs, `ralph/lib/doctor.mjs`, `ralph/lib/util.mjs`, two `ralph/test/*.test.mjs`), all outside `packages/`, `apps/`, `e2e/` and every manifest/lockfile; the same diff scoped to `**/package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml`, `Cargo.lock` is empty. `pnpm install --frozen-lockfile` succeeded (763 entries, no resolution changes), independently confirming no lockfile drift. Toolchain matches `docker/versions.env` exactly: `node -v` 22.22.1, `pnpm -v` 11.25.0, `cargo --version` 1.98.1. Full bar in this container: `pnpm lint` clean (eslint + `tsc --noEmit` across all 6 workspace projects with a `typecheck` script); `pnpm test` 1503/1503 across 30 files, coverage 99.63%/97.68%/100%/100%, unchanged from 1.7/1.8 (no `packages/` source touched); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, unchanged (workspace still has the one member, `apps/desktop/src-tauri`; no Rust command exists yet to test). Secrets scan: `git status --porcelain --ignored` shows only build-artifact directories (`node_modules`, `target`, `.evidence`, `apps/desktop/src-tauri/gen`, `coverage`) as ignored and nothing untracked; a grep for API-key-shaped strings across the tracked source trees found none. The task's `ci`/`ciAcceptance` fields (three-OS Playwright run via `ci/1.verify/a{n}`, `ci.yml`) are the CI gate's job, not this loop task's acceptance, per 0.verify.r2's precedent — nothing here claims that run happened, and per DECISIONS #019/#repair-1.9(d) that gate is expected to fail its first round (`ci.yml` has no Playwright step yet) and is designed to take the GATE-FAILED path deliberately, not to be pre-empted by this task adding CI machinery (out of scope for a `verify` task in any case: CLAUDE.md forbids new dependencies and unrequested changes here). Iterations used: 1. First attempt passed: y. Tool calls: 14. Notes: nothing outstanding from 1.verify; next is `scripts/gate.sh 1.verifyh` (the CI gate), expected to fail its first round per DECISIONS #019, with `1.verify.g1` (the fix task, written by the `plan.1.verifyh.r0` planning commit per #repair-1.9(d)) adding the missing Playwright step to `ci.yml`.
- [plan.1.verifyh.r0] 2026-09-09T09:37:36Z Task: planning commit for the 1.verifyh GATE-FAILED (ci.yml run 34333160626 at 3e98707: macos-latest 5 keymap tests red in store.test.ts and toggle.test.ts, every one an event with ctrlKey only where Mod- is Meta on a Mac; ubuntu and windows green; artifacts [] because the Playwright check was never reached). Status: Complete. Files: docs/PRD.md (§8 Phase 1: +1.11 macOS keymap tests with the platform injected, both routes, presence and absence, one test per guard; +1.12 Playwright in ci.yml on all three runners, per-OS browser install, every spec under e2e/web/, failure in the job, merged playwright-report; +1.verify.g1 needsCI repair with refTemplate ci/1.verify.g1/a{n}; 1.10 → 1.verify.g1), ralph/tasks.json + ralph/EXPECTED_COUNT (150 → 154, regenerated; 1.10.r0a/b/c rewired to 1.verify.g1h), docs/DECISIONS.md (#020: what happened, cause, graph change, model choice, reviewer notes, reversal), docs/lessons.md. Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (154 tasks); bash ralph/test/run.sh run before the commit. Commit: wip(plan.1.verifyh.r0). Iterations used: 1. First attempt passed: y. Tool calls: ~14. Reviewer overturned: pending. Notes: the runner integrates this as plan(plan.1.verifyh.r0), supersedes 1.verifyh, then runs 1.11, 1.12 and 1.verify.g1; next human gate is scripts/gate.sh 1.verify.g1h; the Windows Playwright log is the first unknown of that round.

- [1.11] 2026-09-09T09:52:00Z Task: macOS keymap tests exercise the platform route — the five tests that were green on ubuntu-latest and windows-latest and red on macos-latest (1.verifyh, ci.yml run 34333160626 at 3e98707; DECISIONS #020) now name the platform they are asserting about, and each of the four `Mod-` chords is asserted handled *and* unhandled on each of the two platforms. Status: Complete. Files: `packages/editor/test/store.test.ts`, `packages/editor/test/toggle.test.ts`, `docs/progress/journal-main.md` (this entry), `docs/lessons.md` (+2 lines). No source file, no workflow, no config, no fixture, no golden file: `git diff --stat` against the branch base is exactly those four files (2 test files, 289 insertions, 61 deletions, plus the two docs). Zero new dependencies — the diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock` is empty; the only new imports in either test file are `vitest`'s own `vi` helpers, which both files already imported. **Reproduction, before the change.** `pnpm test` in this Linux container: 30 files, 1503/1503 passed, coverage 99.63%/97.68%/100%/100% — green, i.e. the container agrees with the two runners that passed and says nothing about the one that failed. The same suite with `navigator.platform` forced to `MacIntel` (a temporary `mac-platform.setup.ts` doing `Object.defineProperty(globalThis, "navigator", { value: { platform: "MacIntel" }, configurable: true, writable: true })`, wired in by a temporary `vitest.mac.config.ts` that `mergeConfig`s `setupFiles` onto `vitest.config.ts`, run as `npx vitest run --config vitest.mac.config.ts`): **2 files failed, 5 tests failed, 1498 passed** — `store.test.ts › undoKeymap (rendered view) ›` "undoes on Ctrl+z", "redoes on the shifted-letter route, where the event's key is 'Z'", "redoes on the keyCode route, where the event's key is not a letter", "is installed by storePlugins, ahead of the editing keymaps", and `toggle.test.ts › the chord ›` "Cmd/Ctrl+/ reaches a ProseMirror keymap". Exactly the five the task description names, and no other test in the 1503 moved. Both temporary files were deleted before the commit; `git status --short` after the deletion lists only the two test files. **Cause, read from the library rather than assumed.** `node_modules/.pnpm/prosemirror-keymap@1.2.3/…/dist/index.js` line 4: `const mac = typeof navigator != "undefined" && /Mac|iP(hone|[oa]d)/.test(navigator.platform)`, a module-level constant evaluated once at import, and `normalizeKeyName` turns `Mod-` into `Meta-` when it is true and `Ctrl-` otherwise. Node 22 fills `navigator.platform` from the host OS. The five tests fired events with `ctrlKey: true` only, so they encoded the non-Mac route and passed on Linux and Windows by accident of platform. PRD §6.5 says Cmd on macOS and Ctrl elsewhere, so the bindings (`Mod-z`, `Mod-Z`, `Shift-Mod-z` in `KEY_NAMES`, `Mod-/` in `TOGGLE_KEY`) are right and the tests were wrong; no source was touched. **How the platform is injected, and why the obvious form does not work.** The description's suggested shape (`vi.stubGlobal` + `vi.resetModules()` + a dynamic `import("prosemirror-keymap")`) was tried first and *silently does not isolate*: Vitest hands `node_modules` to Node's own ESM loader, which `vi.resetModules()` does not reach, so the second import returns the first evaluation's copy and both platforms answer alike — a probe asserting `MacIntel` handled meta and `Linux x86_64` did not returned "MacIntel=true, Linux x86_64=true". The equivalent that does work, and is what shipped (`keymapOn(platform)`, one copy per test file, documented in a doc comment in each): stub `navigator`, then import the library's **resolved URL with a query string** (`` `${import.meta.resolve("prosemirror-keymap")}?platform=…` ``) — a new URL is a new Node module, so the constant is re-evaluated — then `vi.resetModules()` and `vi.doMock("prosemirror-keymap", () => library)` before a dynamic import of `../src/store.js` / `../src/toggle.js`, because those are inlined source whose own `import { keymap } from "prosemirror-keymap"` would otherwise resolve back to the cached copy. No platform list is copied from a run: `MacIntel` and `Linux x86_64` are the two sides of the library's own regex, and the table states which modifier each means. **Negative control.** With the Mac row's modifier flipped to `ctrlKey` (a scratch edit, reverted immediately), all 8 Mac-platform tests in `store.test.ts` fail — the absence assertions are not passing vacuously. **Guards, enumerated from the diff, one test each.** Four chords × (Mac+meta handled, Mac+ctrl unhandled, non-Mac+ctrl handled, non-Mac+meta unhandled), plus the two plugin-installation routes under the same rule; the platform is the `describe.each` title, so each guard is `<describe> › <it>`. `Mod-z` (store.test.ts, `undoKeymap (rendered view) on a Mac platform` / `… on a non-Mac platform`): Mac+meta = "undoes on Mod-z, the platform's own modifier"; Mac+ctrl = "leaves Mod-z alone when the event carries the other platform's modifier"; non-Mac+ctrl = "undoes on Mod-z, the platform's own modifier"; non-Mac+meta = "leaves Mod-z alone when the event carries the other platform's modifier". `Mod-Z` (the shifted-letter route, same two describes): Mac+meta = "redoes on the shifted-letter route, where the event's key is 'Z'"; Mac+ctrl = "leaves the shifted-letter route alone under the other platform's modifier"; non-Mac+ctrl = "redoes on the shifted-letter route, where the event's key is 'Z'"; non-Mac+meta = "leaves the shifted-letter route alone under the other platform's modifier". `Shift-Mod-z` (the keyCode route, same two describes): Mac+meta = "redoes on the keyCode route, where the event's key is not a letter"; Mac+ctrl = "leaves the keyCode route alone under the other platform's modifier"; non-Mac+ctrl = "redoes on the keyCode route, where the event's key is not a letter"; non-Mac+meta = "leaves the keyCode route alone under the other platform's modifier". `storePlugins` (the same `Mod-z` through the plugin, which is the ordering test the description names, same two describes): Mac+meta = "is installed by storePlugins, ahead of the editing keymaps"; Mac+ctrl = "leaves storePlugins' chord alone under the other platform's modifier"; non-Mac+ctrl = "is installed by storePlugins, ahead of the editing keymaps"; non-Mac+meta = "leaves storePlugins' chord alone under the other platform's modifier". `Mod-/` (toggle.test.ts, `the chord on a Mac platform` / `the chord on a non-Mac platform`): Mac+meta = "Cmd/Ctrl+/ reaches a ProseMirror keymap on this platform's own modifier"; Mac+ctrl = "leaves the chord alone when the event carries the other platform's modifier"; non-Mac+ctrl = "Cmd/Ctrl+/ reaches a ProseMirror keymap on this platform's own modifier"; non-Mac+meta = "leaves the chord alone when the event carries the other platform's modifier". `togglePlugins` (same two describes): Mac+meta = "is installed by togglePlugins, which answers this platform's own modifier"; Mac+ctrl = "leaves togglePlugins' chord alone under the other platform's modifier"; non-Mac+ctrl = "is installed by togglePlugins, which answers this platform's own modifier"; non-Mac+meta = "leaves togglePlugins' chord alone under the other platform's modifier". Also kept, unchanged in meaning: "leaves an unmodified z alone" (now run on both platforms) and one platform-independent `togglePlugins is one plugin a ProseMirror state accepts`, which is what the old chord test's `state.plugins` assertion was really about. **The CodeMirror binding tests are untouched**, as the description requires: `undoKeyBindings (source view)` and the two `toggleKeyBindings` tests assert binding *names* (`["Mod-z", "Mod-Z", "Shift-Mod-z"]`, `TOGGLE_KEY`) and `preventDefault`, which are platform-independent strings — `@codemirror/view` resolves `Mod-` at match time, not at binding time. **One thing this proxy does not reproduce, stated because it changes one input.** `w3c-keyname` (which `prosemirror-keymap` imports for `keyName`) has its own host-derived `mac` constant, and a bare specifier inside an externalised package resolves to the same URL however this file asks for it, so it stays Linux here. It reads that constant only for a Meta+Shift event (`ignoreKey`), where a real Mac ignores `event.key` and re-derives the name from `keyCode`: so the `key: "Ω"` event reaches `Mod-Z` on a real Mac and `Shift-Mod-z` in this container. Both names are bound to redo, so the assertion (`handled === true`, document back at "b\n") is the same on either route; the divergence is written into the doc comment above `keymapOn` rather than left for the next reader to find. Tests: **with the host platform** — `pnpm lint` clean (eslint + `tsc --noEmit` in all 6 workspace projects), `pnpm test` **1524/1524 in 30 files** (1503 + 21: store.test.ts 26 → 39, toggle.test.ts 83 → 91) with coverage 99.63%/97.68%/100%/100%, every per-file threshold met and `store.ts` still 100/100/100/100, `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests (no Rust touched). **With the platform forced to `MacIntel`** — the same temporary setup file and config as the reproduction: **1524/1524 in 30 files**, zero failures; `pnpm lint` and `cargo test` do not read `navigator` and were run once (the stub lives only in a vitest setup file), and both are green. `npx prettier --check` clean on both files this diff touches. Honest statement, per the acceptance: this container is Linux (`node -v` 22.22.1, `navigator.platform` "Linux x86_64"); a forced `navigator.platform` is a proxy for macOS, not a macOS pass, and nothing here claims one — the only proof is the `1.verify.g1h` gate green on ubuntu, macos and windows, which this task cannot observe. Iterations used: 1. First attempt passed: y. Tool calls: 22. Notes: `keymapOn` is duplicated verbatim in the two test files rather than shared, because this task's diff is confined to those two files; if a third file ever needs it, that task should lift it into a `packages/editor/test/` helper beside `typing.ts` (1.3) and say so. The next `Mod-` chord anyone adds — Phase 2's source-pane keymap is the likely one — should be written into a `PLATFORMS`-shaped test from the start; a single-modifier keymap test is a platform assertion in disguise. 1.12 (Playwright in `ci.yml` on all three runners) is the other half of the 1.verifyh repair and is untouched here.
- [1.12] 2026-09-09T10:05:00Z Task: Playwright in CI (repair after the 1.verifyh GATE-FAILED; handoff 009 Finding 4, DECISIONS #019 and #020) — `.github/workflows/ci.yml` runs `pnpm lint`, `pnpm test` and `cargo test` on all three runners but never ran `e2e/web/`. Status: Complete. Files: `.github/workflows/ci.yml` only (plus this entry and `docs/lessons.md`). `git diff --stat` against the branch base (009895f) is exactly `.github/workflows/ci.yml | 35 +++++++++++++++++++++++++++++++++++`, no other file; `e2e/web/playwright.config.ts` needed no change (the HTML reporter and its `never`-open behaviour are set from the workflow's own `--reporter` flag and a `PLAYWRIGHT_HTML_OPEN` env var, not from the config). Zero new dependencies: the diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock` is empty; `@playwright/test` is already `e2e/web`'s own devDependency (task 1.3) and the browser binary is a runtime download, not a manifest entry. What changed, after the `Cargo test` step and before `Runner conformance suite`, on all three runners: two mutually-exclusive browser-install steps (`if: runner.os == 'Linux'` runs `pnpm --filter @essaydown/e2e-web exec playwright install --with-deps chromium`; `if: runner.os != 'Linux'` runs the same without `--with-deps`, since the description states the flag only belongs on Linux and every other new step is unconditional), a `Playwright (e2e/web)` step (`pnpm --filter @essaydown/e2e-web exec playwright test --reporter=list,html` with `PLAYWRIGHT_HTML_OPEN: never`, which starts the config's own Vite `webServer` and never opens a browser), and an `if: always()` upload step whose own `name:` and `with.name` are both `playwright-report-${{ matrix.os }}` (`path: e2e/web/playwright-report`, `if-no-files-found: ignore`, matching the existing `Upload logs` step's shape). `merge-logs` gets a second merge step, `name: playwright-report`, `uses: actions/upload-artifact/merge@v4`, `with.name: playwright-report`, `pattern: playwright-report-*`, `separate-directories: true`, `delete-merged: true` — copied from the existing `test-logs` merge step. No other step touched; the only new `runner.os`-conditioned steps are the two browser-install variants (checked with `node -e` against the parsed YAML, listed below). Verification, in this container. `node -e` requiring `./node_modules/.pnpm/yaml@2.9.0/node_modules/yaml` (already present under `node_modules/.pnpm` from `pnpm install --frozen-lockfile`; no new dependency) parsed `.github/workflows/ci.yml` and printed the `test` job's 25 step names in order: `…, Test, Cargo test, Install Playwright browser (Linux), Install Playwright browser (macOS, Windows), Playwright (e2e/web), playwright-report-${{ matrix.os }}, Runner conformance suite (RUNNER-SPEC §12, ubuntu leg only), Upload logs` — `Cargo test` before the browser-install step before `Playwright (e2e/web)` before the `playwright-report-${{ matrix.os }}` upload, as required; `merge-logs`'s two steps print `with.name`/`with.pattern` of `test-logs`/`test-logs-*` and `playwright-report`/`playwright-report-*`. `actionlint .github/workflows/ci.yml` reported nothing. After `pnpm install --frozen-lockfile`, the exact command lines from the workflow's Linux steps, run from the repo root: `pnpm --filter @essaydown/e2e-web exec playwright install --with-deps chromium` failed in this container (`su: Authentication failure` — no `sudo` binary and no root, `id` is uid 1000; this is the same class of container limitation lesson 1.3/1.4 already recorded for `--with-deps`, not a workflow defect, since a real Linux GitHub runner has passwordless sudo) — so the browser and the two remaining acceptance checks were verified through the macOS/Windows variant's command line instead, which is the same Chromium binary the Linux step would have produced and is honestly the closest this container can get: `pnpm --filter @essaydown/e2e-web exec playwright install chromium` downloaded Chromium 1243 + ffmpeg + chrome-headless-shell into `~/.cache/ms-playwright` (per-attempt, not per-worktree, per lesson 1.4). `pnpm --filter @essaydown/e2e-web exec playwright test --list` named all 36 tests across the same 7 files `find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*'` lists (`editor-input`, `editor-reveal`, `editor-source`, `editor-toggle`, `editor-undo`, `outline-produce`, `tests/placeholder`), compared as sets. `PLAYWRIGHT_HTML_OPEN=never pnpm --filter @essaydown/e2e-web exec playwright test --reporter=list,html` ran all 36, 36 passed, exit 0, `e2e/web/playwright-report/index.html` written. Then, to show the failure lives in the job: a temporary `e2e/web/temp-failing.spec.ts` with one failing `expect(1).toBe(2)` made the same command line report `1 failed`, exit non-zero (`ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL`, exit code 1), while `e2e/web/playwright-report/index.html` was still written (549343 bytes, re-generated); the temp spec, `e2e/web/playwright-report/` and `e2e/web/test-results/` were then deleted and the suite re-run clean (36 passed) before committing — `git status --short` shows only `.github/workflows/ci.yml` modified. `pnpm lint` clean (eslint + `tsc --noEmit` across all 6 workspace projects with a `typecheck` script); `pnpm test` 1524/1524 across 30 files, coverage 99.63%/97.68%/100%/100%, unchanged from 1.11 (no `packages/` source touched); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, unchanged. `npx prettier --check .github/workflows/ci.yml` still warns (pre-existing single-quote-vs-double-quote style on two lines this diff does not touch, confirmed unchanged by stashing this diff and re-running the check against 009895f); per lesson 1.8 only the files a task's own diff touches are brought into formatting scope, and this task's added lines are the ones prettier leaves alone, so the pre-existing warning is left as-is rather than widening the diff. Honest statement, per the acceptance: this container is Linux and cannot run GitHub Actions; the in-container run proves the command lines (with the `--with-deps` substitution noted above), not the workflow; a green `1.verify.g1h` gate proves the e2e suite and the unit suite ran on all three runners at the integrated SHA, which this task cannot observe and must not claim — the Windows Playwright log is still the first unknown of that round. Iterations used: 1. First attempt passed: y. Tool calls: ~24. Notes: nothing outstanding from 1.12; the Linux `--with-deps` command line itself remains unverified inside this container (no sudo/root) and can only be proven by the real `ubuntu-latest` leg of `1.verify.g1h`. 1.11 (the other half of the 1.verifyh repair) is untouched here.
- [1.verify.g1] 2026-09-09T10:05:24Z Task: Phase 1 verification, repair 1 of 1.verify after the 1.verifyh GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a); DECISIONS #020): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head (1.12, `009895f`) is ready for the three-OS CI run the CI gate performs. Status: Complete. Files: `docs/progress/journal-main.md` (this entry) only; no source, fixture, manifest or lockfile touched — the two repair tasks named in the 1.verifyh GATE-FAILED (1.11 macOS keymap tests, 1.12 Playwright in `ci.yml`) already integrated ahead of this task, so this task's own job is confirmation, not further code. `git diff --stat` against the branch base (1.12, `009895f`) over every dependency manifest and lockfile (`**/package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml`, `Cargo.lock`) is empty. `pnpm install --frozen-lockfile` succeeded (702 packages, "Lockfile passes supply-chain policies", no resolution changes, `git status --short` clean afterward), independently confirming no lockfile drift. Toolchain matches `docker/versions.env` exactly: `node -v` 22.22.1, `pnpm -v` 11.25.0, `cargo --version` 1.98.1. Full bar in this container: `pnpm lint` clean (eslint + `tsc --noEmit` across all 6 workspace projects with a `typecheck` script); `pnpm test` 1524/1524 across 30 files, coverage 99.63%/97.68%/100%/100%, unchanged from 1.12 (no `packages/` source touched since); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, unchanged (no Rust command exists yet to test). Secrets scan: `git status --porcelain --ignored` shows only build-artifact directories (`node_modules`, `target`, `coverage`, `apps/desktop/src-tauri/gen`, `e2e/*/node_modules`) as ignored and nothing untracked; a grep for API-key-shaped strings across the tracked source trees found none. The task's `ci`/`ciAcceptance` fields (full suite + `e2e/web` Playwright on all three runners via `ci/1.verify.g1/a{n}`, `ci.yml`) are the CI gate's job, not this loop task's acceptance, per 0.verify.r2's and 1.verify's precedent — nothing here claims that run happened, and this container is Linux so no macOS or Windows pass is claimed; the Windows Playwright log remains the first unknown of this round, and 1.11's macOS keymap fix is a proxy (`navigator.platform` forced in Vitest), not a macOS pass, per its own journal entry. Iterations used: 1. First attempt passed: y. Tool calls: 15. Notes: nothing outstanding from 1.verify.g1; next is `scripts/gate.sh 1.verify.g1h` (the CI gate) — a full second round after the 1.11/1.12 fixes, expected honestly rather than assumed given the still-unverified Linux `--with-deps` Playwright install line 1.12 recorded.
- [1.10.r0d] 2026-09-09T17:53:47Z Task: reconciliation of Phase 1 review attempt r0 at e9ed88b (verifier 1.verify.g1, gate 1.verify.g1h ACCEPT a1, run 34338761650). Claude FAIL (1 blocker/6 should-fix/5 nits), Sol FAIL (0/6/1, all Required), Grok PASS (0/2/4). Verdict FAIL: a trailing space typed before Enter is serialised as `&#x20;` because the editor's tree keeps whitespace micromark strips (Michael's 1.9 record, PRD §6 portability), the editor's own copy/paste discards structure (no parseDOM), cursor mapping and empty-table ranges wrong, both editing paths O(document) per keystroke with the phase check's follow-up now mandatory, three packages outside PRD §4, two backlog triggers (H2, H3/H5) fired unnoticed. Status: Complete. Files: docs/reviews/phase-1-r0-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#002-outline-feel: the 1.9 payload and record body verbatim with provenance; #review-1-r0: verdict, 16 findings with severity and disposition, Michael's dependency decision (a) and his "third time" note, the latency follow-up decided as a Phase 1 task, the ca1cd3d reading, the reviewer record with the Phase 0 counts and the shared-finding severity comparison, reversal), docs/PRD.md (§8 Phase 1: +1.13–1.22 fix tasks, +1.verify.r1 needsCI carrying one promoted rule, +1.10.r1a/b/c/d, 1.close → 1.10.r1d; §4 Source-editor sentence ratifying @lezer/highlight and @lezer/markdown and excluding the codemirror meta-package; post.1 reviewer table = unique findings and shared-finding severity, never verdicts), ralph/tasks.json + ralph/EXPECTED_COUNT (154 → 170), docs/RUNNER-SPEC.md (§2: hand-written human records carry a provenance line), docs/V1.1-BACKLOG.md (+8 lines), docs/lessons.md (+13 class-level), docs/handoffs/010-*.md + 011-*.md + next-prompt.md (cherry-picked from handoff/010 and handoff/011; the next-prompt conflict resolved to the 011 version), docs/progress.md (regenerated). Tests: node ralph/generate-tasks.mjs --check, node ralph/validate-tasks.mjs (170), bash ralph/test/run.sh green (counts in the commit body). Commit: wip(1.10.r0d). Not done here, by design: no code change (RUNNER-SPEC §8.1 allowlist); the ten fix tasks carry the code.
- [1.13] 2026-09-09T18:10:00Z Task: Phase 1 review fix F1 (DECISIONS #review-1-r0 F1 — Claude blocker 1, Sol required 6, Grok 1; Michael's 1.9 record in #002-outline-feel, escalated to a PRD §6 portability defect): a paragraph, heading or table cell edited in the rendered view kept ProseMirror's text verbatim, so a trailing space typed before Enter reached `format`, whose `unsafe` table encodes a space before a line ending as a numeric character reference, and the editor's Markdown was not a fixed point of `parse`∘`format`. Status: Complete. Files: `packages/editor/src/schema.ts` (+63/-3), `packages/editor/test/editor-fixed-point.test.ts` (new), `e2e/web/editor-trailing-space.spec.ts` (new), `docs/progress/journal-main.md` (this entry), `docs/lessons.md` (+2 lines). `git diff --stat` against the branch base (`b269627`) is exactly those five files — no fixture, no golden file, no workflow, no config; the diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock` is empty (zero new dependencies, PRD §4), and the only imports the new test file adds are ones the sibling tests already use (`prosemirror-state`'s `EditorState`, `prosemirror-model`'s `Node` type, `node:fs`/`node:url`, the two `packages/core` entry points). **Reproduction, before the change.** A scratch Vitest file driving `packages/editor/test/typing.ts` (the headless keyboard 1.3 built) printed exactly the description's three strings: `hello ` + Enter + `world` -> `"hello&#x20;\n\nworld\n"`, `- one ` + Enter -> `"- one&#x20;\n-\n"` (Enter opens a second item; the entity is the defect), `# head ` -> `"# head&#x20;\n"`. Scratch file deleted before the commit. **The fix, and where it is not.** One new function in `schema.ts`, `trimBlockEnds(nodes)`, called from `blockToMdast` for the three blocks whose content is inline — `paragraph`, `heading`, `table_cell` — so every consumer of the editor's tree (the serializer, the DevEditor copy button, the store) sees the same document; not in the serializer's `unsafe` table, which Grok's finding says must not be weakened and which is right about the trees it is given, and not at the copy button. It strips the block's own two ends and nothing else: the leading whitespace of the block's **first** inline node and the trailing whitespace of its **last**, each only when that node is a text node outside `inline_code`. The whitespace set is CommonMark's ASCII whitespace (`/[\t\n\v\f\r ]+/`), written as a character class rather than `\s` on purpose — `\s` also matches U+00A0 and the U+2000 block, which a Markdown file keeps and `parse` preserves. The ownership rule for zero-width items is stated once, in the doc comment above the function: an atom (`image`, `hard_break`, `raw_inline`) or an inline-code run at an end owns that end and stops the strip there; a node that is both first and last is trimmed at both ends; a run trimmed to nothing is dropped rather than kept as a zero-length text node (which ProseMirror rejects), and dropping it does not promote its neighbour to the boundary because both ends are chosen before either is trimmed; `code_block`, `raw` and the opaque `html`/`yaml` bytes never reach the function. **Guards, enumerated from the diff, one test each, all in `packages/editor/test/editor-fixed-point.test.ts` under `block-end whitespace, one test per guard`.** (1) leading strip -> "guard 1 (leading strip): the leading whitespace of a block's first text run is dropped"; (2) trailing strip -> "guard 2 (trailing strip): the trailing whitespace of a block's last text run is dropped"; (3) break-adjacent untouched -> "guard 3 (break-adjacent untouched): a text run beside a hard break keeps its spaces" (`one ` + `break` + ` two ` becomes `one `, `break`, ` two`: the run before the break keeps its trailing space and the run after keeps its leading one, while the block's own last end is still trimmed); (4) inline code untouched -> "guard 4 (inline code untouched): an inline-code run at a block end keeps its literal bytes"; (5) raw untouched -> "guard 5 (raw untouched): a raw_inline at a block end keeps its bytes and stops the strip" (also asserts the block-level `raw` node, which never reaches the strip); (6) empty run dropped -> "guard 6 (empty run dropped): a run trimmed to nothing is dropped, not kept as an empty node" (a `strong` run of two spaces at the block start disappears, wrapper and all, leaving one plain text child); (7) table cell -> "guard 7 (table cell): a table cell's own two ends are stripped like a paragraph's" (first and last cell of two rows, so first/middle/last positions of the row are covered). One more, not a guard but the third block type the call site adds: "a heading's ends are stripped too (the third block the strip runs on)". **Each guard is asserted able to fail, by mutation** (scratch edits to `schema.ts`, each reverted immediately; the file was restored from a copy and re-run green after every one). Mutation A, the strip removed from all three call sites: 54 of the 117 tests in the new file red — guards 1, 2, 3, 6, 7 and the heading test, plus 48 corpus legs. Mutation B, the `inline_code` exclusion deleted: exactly 1 red, guard 4. Mutation C, the boundaries scanning past atoms to the first/last *text* node instead of the first/last node: exactly 1 red, guard 5. Mutation D, every run trimmed at both ends instead of the block's two ends: 28 red — guards 3 and 5 plus 26 corpus legs (the fixtures with marks or inline code, both legs each). Guards 4 and 5 are absence guards and stay green under A by design, which is why B and C were run. **The corpus leg.** `editor fixed point over the corpus`, seeded from the editor's own output rather than from `parse(fixture)` — the blind spot Claude's risk 1 names. For every fixture *listed in* `fixtures/markdown/index.json` (the loop is over the index's own keys and a separate test asserts `names.length === Object.keys(index).length`, so no count is written here): build `mdastToPM(parse(x))`, apply a typing-shaped transaction (`EditorState.create({doc}).tr.insertText(" ", pos)` at the end of every paragraph and heading, applied back-to-front so the positions still to come stay valid), then assert `format(pmToMdast(doc))` equals `format(parse(x))`, is a fixed point of `parse`∘`format`, and contains no entity. The transaction is asserted to have done something (`typed.doc.eq(doc)` is true exactly when the fixture has no paragraph or heading), and a separate test asserts at least one fixture in the index has a block the transaction reaches. The same leg on the unchanged doc is the absence case (`… is an editor fixed point unchanged (the absence case)`). A committed sensitivity test, "the leg can fail: the same tree without the strip serialises to a character reference", hand-builds the mdast `pmToMdast` used to hand the serializer for a paragraph typed as `hello ` and asserts it serialises to a numeric character reference and not to `hello\n` — matched as `/^hello&#x[0-9a-f]+;\n$/i` rather than spelled, so that `grep -rn '&#x20;' e2e/web packages/editor/test` names only the seven absence assertions (three in the spec, four in the unit file) and nothing that expects the defect's bytes. **The existing suites are unchanged**: `packages/editor/test/schema-roundtrip.test.ts` and the `packages/core` roundtrip/invariant suites are byte-identical to the base (they are not in `git diff --name-only b269627`) and green — the new leg is a new file, deliberately, so that "unchanged" is checkable by the diff. **Playwright, `e2e/web/editor-trailing-space.spec.ts`, on `/dev/editor`** (browser installed per attempt, lesson 1.4: `pnpm --filter @essaydown/e2e-web exec playwright install chromium`, never `--with-deps` in this container, lesson 1.12): presence `X`, space, Enter, `Y` -> the Markdown pane is exactly `X\n\nY\n`; absence `X`, Enter, `Y` -> the same string; `- one ` -> `- one\n`; `## Title ` -> `## Title\n`; the three trailing-space cases also assert the pane does not contain the entity. All 4 pass. Because presence and absence are the same string by construction, the pair only means something if the presence case could fail, so it was run against the pre-fix code (the strip removed from the three call sites, restored immediately): **3 of the 4 red** — presence, list and heading, each `Expected - 1 / Received + 1` — and the absence case green, which is the honest proof that a real Chromium contenteditable really does put the typed space into the document (`prosemirror-view` normalises the browser's nbsp back to a space) and that the entity reached the pane before this fix. `e2e/web/playwright-report/` and `e2e/web/test-results/` deleted afterwards; `git status --short` lists only the three code files. Tests: `pnpm lint` clean (eslint + `tsc --noEmit` in all 6 workspace projects); `pnpm test` **1641/1641 in 31 files** (1524 + 117: the new file's 8 guard tests, 1 index-count test, 106 corpus tests = 53 fixtures × 2 legs, 1 reach test, 1 sensitivity test), coverage 99.64%/97.72%/100%/100% with `editor/src/schema.ts` at 100/100/100/100; `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests (no Rust touched). Whole `e2e/web` suite `PLAYWRIGHT_HTML_OPEN=never … playwright test`: **40 passed** (36 + the 4 new). `npx prettier --check` clean on all three code files this diff touches (lesson 1.8). Honest statement: this container is Linux, so nothing here claims a macOS or Windows pass; the Playwright evidence is one Chromium in this container, and the three-OS proof is the CI gate's, not this task's. Iterations used: 1. First attempt passed: y. Tool calls: 27. Notes: nothing outstanding from 1.13. Two things a later task should know rather than rediscover. (a) The corpus leg only types at the end of paragraphs and headings, because that is the acceptance's shape; table cells are covered by guard 7 alone, and the leg would extend naturally to a cell-typing transaction if a later fix touches `table_cell`. (b) A whitespace-only paragraph (every run trimmed to nothing) serialises to an mdast paragraph with no children, which is a shape `parse` never produces; no fixture and no typing route reaches it here (`blocksToMdast` already drops the *empty* ProseMirror paragraph, and a paragraph holding one space is not that), so it is left alone deliberately rather than fixed blind — if a later task can type a blank-but-not-empty block, that block should be dropped at the same place. F2 (no `parseDOM`), F3 (empty-cell ranges), F4/F5 (cursor coordinates, per-keystroke cost) are other tasks and are untouched here.
- [1.14] 2026-09-09T19:05:00Z Task: Phase 1 review fix F2 (DECISIONS #review-1-r0 F2 — Sol required 1, Claude's clipboard finding): the ProseMirror schema defined `toDOM` for every node type and mark and no `parseDOM`, and the doc comment near line 94 asserted the native clipboard path was unused — it is not, `prosemirror-view` serialises a copied slice with this schema's `DOMSerializer` and reads it back with its `DOMParser`, so the editor's own copy → paste rebuilt the pasted HTML as plain text (`# Heading\n\n**bold**\n` selected, copied and pasted over itself became `Heading\n\nbold\n`, Sol's `/report/paste.cjs`). Status: Complete. Files: `packages/editor/src/schema.ts` (+204/-15), `packages/editor/test/parse-dom.test.ts` (new, 297 lines), `e2e/web/editor-clipboard.spec.ts` (new, 199 lines), plus `docs/lessons.md` and this entry. `git diff --stat` against the branch base (`4386477`, task 1.13) is exactly those three code files, 685 insertions, 15 deletions; the diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock` is empty (zero new dependencies, PRD §4), and no fixture, golden file, workflow or config is touched. **The fix.** Each `toDOM` gained the rule that inverts exactly the element it writes, and every attribute the node carries is in that element: `heading.depth` in the tag (one rule per depth, 1–6), `list.start` in `start`, `image`/`link` in `src`/`href`/`alt`/`title`, `code_block` in `data-lang`/`data-meta`, and the three the rendering has no HTML spelling for — `list.spread`, `list_item.spread`, `table.align` — in `data-spread` and `data-align`, each written only when it differs from the schema default so the elements a reader sees are byte-unchanged (`["ul", 0]` is still `["ul", 0]`, `["table", ["tbody", 0]]` still that). The two opaque atoms carry their bytes twice: as the element's text, which is the grey box the reader sees, and as `data-essaydown-raw`, which is what the rule reads — the text is never the source of truth on the way in, because `raw`/`raw_inline` are leaf types, so the parser creates the node from the attribute and never descends, and a pasted `<script>x</script>` comes back as those characters. `paragraph`, `heading` and `table_cell` set `preserveWhitespace: "full"`, without which the parser normalises the literal `\n` mdast writes for a soft line break to a space and a wrapped blockquote comes back joined onto one line. **The DOM clause of the acceptance, answered: there is no DOM in vitest here.** `vitest.config.ts` sets no `environment`, and `jsdom` appears in `pnpm-lock.yaml` only as vitest's own optional peer range (`jsdom: '*'`, never installed); `happy-dom` and `linkedom` are absent. Adding one would be a new dependency, which PRD §4 forbids, so the headless leg asserts the schema (enumeration, and each `getAttrs` against the attributes its own `toDOM` wrote, through a two-method element stand-in) and the `DOMSerializer` → `DOMParser` → `eq` corpus leg is carried by the Playwright spec, as the task text allows. **Evidence.** `pnpm lint` green (eslint + six `tsc --noEmit`); `pnpm test` 32 files, 1666/1666 passed, coverage 99.65 % statements / 97.82 % branches / 100 % functions / 100 % lines with `schema.ts` at 100/100/100/100; `cargo test` green (0 tests, unchanged — no Rust in this task). `e2e/web`: the whole suite 50/50 passed against Chromium installed this attempt (`e2e/web/node_modules/.bin/playwright install chromium`, per lessons 1.3/1.4 — the browser cache does not survive between attempts), of which `editor-clipboard.spec.ts` is 10. The corpus leg loads all **53** fixtures — `Object.keys(index).length`, read from `fixtures/markdown/index.json`, asserted against the loop's own counter, never a literal — through the dev bar, selects all, copies and pastes over the selection through the editor's own `copy`/`paste` handlers, and asserts the Markdown pane is byte-identical to `<name>.canonical.md` before and after; it also asserts the copy really put a `data-pm-slice` on the clipboard, so a no-op paste cannot pass it. Six explicit structure cases (heading, strong+emphasis, nested list, table, fenced code block, raw HTML block) assert both the bytes and that the structure is still structure (`h1`/`h2`, `strong`/`em`, `ul li ul li`, `table tbody tr td`, `pre code`, `div.essaydown-raw` all still present after the paste); the raw case asserts the pasted document holds the same value (`data-essaydown-raw` equals the source bytes, the box's text equals them) and that no element was created from it (`.ProseMirror strong` is 0, the box has 0 element children); the plain-text case sets the real clipboard with `navigator.clipboard.writeText("**still plain**")` and presses Ctrl/Cmd+V into a paragraph, which comes back as the 15 characters, `strong` and `em` both 0. The 1.1 schema round-trip suite (`schema.test.ts`, `schema-roundtrip.test.ts`) is untouched by the diff and green. **Mutation check (lesson 1.13), run before calling this done.** With `packages/editor/src/schema.ts` reverted to `4386477` and both new test files kept: `parse-dom.test.ts` 22 failed / 3 passed, and `editor-clipboard.spec.ts` **9 of 10 red** — every corpus and structure case fails, the plain-text case stays green, which is correct and is what makes it the absence case of the pair (a plain-text paste needs no `parseDOM`). The schema was restored from the copy taken before the mutation and `git status` verified clean against HEAD. **One test per guard the diff adds**, enumerated from the diff rather than from the acceptance: `emptyToNull` (a `data-` attribute is always a string, so `""` is how the schema's `null` default is spelled) — "code_block: the info string and its absence"; `startOf`'s `Number.isNaN` refusal — "list: a `start` that is not a number is refused rather than passed on as NaN"; `alignOf`'s three validations (not JSON, JSON but not an array, an array of strings outside the four admitted values) — "table: an alignment attribute from foreign HTML is validated, never trusted"; `spreadOf`'s absent-means-default reading — "list: ordered with and without a start, bullet, and both spreads" and "list_item: spread, present and absent"; the `code_block` rule's `contentElement` and its `?? dom` fallback for a foreign `<pre>` with no inner `<code>` — "code_block: the content element is the inner `code`, and a bare `pre` falls back to itself"; `rawAttrs`' `?? ""` — "`raw`/`raw_inline`: an element with the attribute but no value parses to the empty default" (both types); the `img[src]`/`a[href]` `?? ""` fallbacks — the last assertion of "image: url, alt and title, present and absent" and of "link: url and title, present and absent"; `preserveWhitespace: "full"` and its deliberate absence on the block-content rules — "the three inline-content rules preserve whitespace fully, so a soft break stays a break", which asserts both the presence and the absence set; and the two `toDOM` shape guards that keep the rendered elements unchanged — "list: a bullet list with nothing to say still renders as the bare element it always did" and the `["table", ["tbody", 0]]` assertion in the table case. Tests: `pnpm lint && pnpm test && cargo test` green in this container; `e2e/web` 50/50 in Chromium (Linux only — this container cannot speak for macOS or Windows, and the three-OS CI leg of task 1.12 is the only proof there). Iterations used: 2. First attempt passed: n. Tool calls: 20. Notes: attempt 1 wrote all three files and never printed the promise; its work arrived as the `wip(1.14): recovery of uncommitted changes` commit and carried no journal entry, so this attempt re-ran every acceptance sentence and every bar itself and reports only its own numbers. Nothing is left open for a later task: the enumeration test is the guard that a future node type cannot be added with a `toDOM` and no rule, and it fails on the node-name lists rather than on a count copied from today's schema.
- [1.15] 2026-09-09T19:05:00Z Task: Phase 1 review fix F3 (DECISIONS #review-1-r0 F3 — Sol finding 4, Required): `formatWithMap` in `packages/core/src/positions.ts` derived the ranges of the two node types `mdast-util-gfm-table` never dispatches (`tableRow`, `tableCell`) from the hull of their dispatched descendants, so a cell with no descendants had no range at all and a wholly empty body row had none either — the paths `0.1`, `0.1.0` and `0.1.1` of `| a | b |\n| - | - |\n| | |\n` were unresolvable, which is exactly the shape the 1.3 table input rule creates (`packages/editor/src/input.ts:179`), so a cursor toggle out of the new empty body cell had nothing to map. Status: Complete. Files: `packages/core/src/positions.ts` (+156/-14), `packages/core/test/positions.test.ts` (+216/-1), `packages/core/test/positions-corpus.test.ts` (+68), `fixtures/markdown/table-empty-cells.md` (new), `fixtures/markdown/table-empty-cells.canonical.md` (new), `fixtures/markdown/index.json` (+12, the new entry only), `e2e/web/editor-table-empty-cell.spec.ts` (new, 98 lines), plus `docs/lessons.md` and this entry. `git diff --stat` against the branch base (`14137c9`, task 1.14) is exactly those seven code/fixture files and touches nothing outside the task's scope list; the diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock` is empty (zero new dependencies, PRD §4), and no workflow, golden file or config is touched. **The fix.** One new step in `placeChildren`: after a `table`'s own children have been placed, `placeTableGrid` places its rows and cells from the grid `markdown-table` wrote rather than from their descendants — one line per row, a delimiter line under the header (so body row `i` is on line `i + 1`), and the row's line partitioned by its unescaped `|` into one half-open slice per column. `setPlacedSpan` records the span and unions it with the hull of any descendant already placed outside it; `cellSlices`/`delimiterOffsets`/`contentSlice` are the three helpers. **Ownership rule, stated once in `placeTableGrid`'s doc comment** (CLAUDE.md's half-open-slice rule): a row owns its whole line including both outer delimiters; the delimiters themselves belong to the row, not to either cell beside them; a cell owns its slice with `markdown-table`'s padding removed from both ends; a cell whose whole slice is padding is empty, owns no character, and sits where its first character would be written — one column past the single padding space after its opening delimiter, so two columns past the delimiter itself. Being zero width it is never returned by `nodeAt` (a cursor on the padding is in the row), and it is what a caret inside that cell resolves to when the source view opens, because 1.7's cursor map reads `ranges` by path, not by position. **The six guards, enumerated from the diff, one named test each, all in `packages/core/test/positions.test.ts` under `formatWithMap: the table grid, guard by guard (task 1.15)`.** Guard 1, the line-count check (`lines.length !== rows.length + 1` falls back to the hull) → "guard 1: a table whose output is not one line per row plus the delimiter line keeps the hull" (a cell holding `<br>\n<br>`). Guard 2, the delimiter line belongs to no row → "guard 2: the delimiter line belongs to no row, so body row i is on line i + 1" (a four-row table; the rows' start lines are 1, 3, 4, 5). Guard 3, a row whose delimiters do not give one slice per column falls back to the hull → "guard 3: a row line whose delimiters do not divide it into one slice per column keeps the hull" (a `|` inside opaque HTML makes a fourth delimiter on a two-column line, and the header row above it still takes its whole line). Guard 4, an escaped `\|` is not a boundary → "guard 4: an escaped delimiter inside a cell is not a cell boundary". Guard 5, the all-padding slice is a point → "guard 5: a cell whose slice is all padding is the point where its first character would go". Guard 6, the hull widening in `setPlacedSpan` → "guard 6: a cell's range is widened to cover a descendant the padding put outside it" (a cell whose content itself starts with a space is written `|  c |` and its `text` node sits one column left of the trimmed slice). **Each guard asserted able to fail, by mutation** (scratch edits, each reverted immediately; the file was restored from a copy and re-run green after every one). With the whole fix reverted to `14137c9`: 13 red across the two files, including guards 3, 4 and 5 — guards 1, 2 and 6 stay green under that mutation by design (1 and 3 assert what the fix must *not* touch, 2 and 6 need the grid to exist at all), so each was given its own mutation, per lesson [1.13]. Mutation A, the line-count guard deleted: exactly 1 red, guard 1. Mutation B, `lines[index]` instead of `lines[index === 0 ? 0 : index + 1]`: 12 red including guard 2. Mutation C, `setPlacedSpan` recording `placed` without the union: exactly 1 red, guard 6. **Empty-cell placement, first/middle/last** (`formatWithMap: empty table cells (task 1.15)`): "places the first, the middle and the last empty cell of a row inside its own delimiters" (the three empty cells at columns 0, 1 and 2 of three different rows), "gives a wholly empty body row its whole line and each of its cells a point in it", "places the cells of a table whose header cells are empty", "resolves every path of the reproduction table, the root and its subtree apart". **`nodeAt` presence and absence** (`nodeAt over a table with empty cells (task 1.15)`): every column of every line of the reproduction table is answered, never with the root and always with a range that contains the point; the padding of the empty body row is answered with the row and never with the zero-width cell; past the end of a line, past the last line and before the first line are null. The same pair on the new fixture is `packages/core/test/positions-corpus.test.ts` → "resolves every path of table-empty-cells.md, and rejects the root and out-of-range ones", whose presence half enumerates the paths from the tree (a forgotten cell is a missing key, not a shorter loop) and whose absence half asserts the root is never answered on any character of any line, plus the three out-of-range positions. Note for a later reader: the line terminator is null only on the table's **last** line — on an interior line that column is still inside the multi-line `table` range — and the test says so. **The new fixture.** `fixtures/markdown/table-empty-cells.md`, LF, six lines, a three-column table with an empty cell first, middle and last in three successive rows and a wholly empty sixth row; `index.json` gains its entry (`nodeTypes` sorted, `blockCount` 6, `sectionCount` 0, `paragraphStartLines` [] — the shape 0.22 wrote, and `fixtures-corpus.test.ts` validates it against the schema and against the canonical file). The corpus test "places every childless table cell of the corpus at a point inside its own delimiters" is what keeps the older "maps every node of the tree, and nothing is unresolved" clause from being vacuous for cells: it counts the fixture's childless cells from its own tree and requires every one of them to have been placed. **Purity**: no DOM anywhere in the change; the existing corpus test "the map is a pure function of root, which is not mutated" covers the new fixture too. **Playwright, `e2e/web/editor-table-empty-cell.spec.ts`, on `/dev/editor`** (Chromium installed this attempt — `e2e/web/node_modules/.bin/playwright install chromium`, never `--with-deps` in this container, lessons 1.3/1.4/1.12): type `|a|b|`, press Enter (the 1.3 input rule leaves the caret in the new empty body cell; nothing is clicked afterwards), assert the store's Markdown is exactly `| a | b |\n| - | - |\n|   |   |\n`, press Ctrl/Cmd+/ and assert the mode is `source`, then read the CodeMirror cursor from the DOM selection: its line is the body row's, and its `ch` is strictly between that row's first two delimiters and exactly `delimiters[0] + 2`. Every asserted line and `ch` is derived in the test from the one canonical string written at the top of the file, never hand-counted. **The spec was run against the pre-fix code and is red**: the cursor landed on `| a | b |`, the header row, because the empty cell had no range and the mapping fell back. Tests: `pnpm lint` green (eslint + `tsc --noEmit` in all 6 workspace projects); `pnpm test` **32 files, 1698/1698 passed**, coverage 99.66 % statements / 97.90 % branches / 100 % functions / 100 % lines, `positions.ts` at 100 statements / 98.88 branches / 100 functions / 100 lines; `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests (no Rust in this task). Whole `e2e/web` suite: **51/51 passed** (50 + the 1 new). `npx prettier --check` clean on the four code files this diff touches; `index.json` was deliberately *not* reformatted (prettier would rewrite all 638 lines of a file this task only adds 12 to — out of scope, lesson 1.8). `e2e/web/playwright-report/` and `test-results/` deleted before the commit; `git status --short` lists only this task's own files. Honest statement: this container is Linux, so nothing here claims a macOS or Windows pass; the browser evidence is one Chromium here, and the three-OS proof is the CI gate's. Iterations used: 2. First attempt passed: n. Tool calls: 26. Notes: attempt 1 wrote `positions.ts`, the two unit test files, the fixture pair, the index entry and the Playwright spec and never printed the promise; its work arrived as the `wip(1.15): recovery of uncommitted changes` commit and carried no journal entry, so this attempt re-ran every acceptance sentence and every bar itself and reports only its own numbers. What attempt 2 added on top of it: the explicit presence/absence test for the new fixture that the acceptance names (the corpus tests covered it only generically), the three targeted guard mutations for guards 1, 2 and 6, and a tightening of the zero-width ownership sentence in `placeTableGrid`'s doc comment, which said "one column after its opening delimiter" where the code places the point two columns after it. Nothing is left open for a later task; F4 (cursor coordinates) and F5 (per-keystroke cost) are other tasks and are untouched here.
