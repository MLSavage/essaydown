# Handoff 004 — Phase 0 at the verify gate (0.0–0.11 and 0.verify passed; `HUMAN_GATE 0.verifyh` open)

Written 2026-09-06 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at `HUMAN_GATE 0.verifyh` + `ROTATE-PRINCIPAL` (DECISIONS #015: the session that ran the phase's tasks never runs the gate or the reconciliation). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `003-phase-0-0.3-running.md`; `002-phase-0-running.md` is still the reference for the gate evidence pattern, the deviations list and the reconciliation checklist (its Next Steps 4). Next: `005-*.md`.

## Current State

- `phase/0` tip `f8c91c5` (`task(0.verify)`) on `5f10e6e` (`task(0.11)`) on `efa1cb2` (`task(0.10)`) on `30bf7a8`/`0978154`/`9d5d04b`/`fc9390a` (0.9/0.8/0.7/0.6) on `task(0.5)` on `6297093` (`chore(0.0): DECISIONS #016 + #017 + two lessons`) on `59d2d3e` (`fix(0.4): restore the 91 files … c8b234a deleted`, Michael) on `c8b234a` on `6a9acdc` (`task(0.4)`). Passed: 0.0–0.11, gate 0.2h (ACCEPT a1), 0.verify (Sonnet, attempt 1, 431/431 tests, no lockfile change). Attempt counts worth knowing: 0.5 passed at attempt 1 of a **re-cut** branch (the first branch is kept as `abandoned/0.5-r1`, three attempts, see Decisions); 0.10 passed at attempt 3 (a1 died to the five-hour subscription limit, a2 to the 50-turn cap); everything else first attempt. Open: `0.verifyh` (`human-pending`, CI gate). `doctor` clean. `main` untouched at the seed `660f3aa`. Nothing pushed except the `ci/*` refs `gate.sh` pushes and deletes.
- **Runner**: tmux session `essaydown`, window 1 `runner`, pane pid 8940 (`zsh`, idle: `run` exited at the gate). Restart: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter`. Attach from a phone: `tmux attach -t essaydown`, `Ctrl-b 1`.
- **Watcher** (background Bash, `run_in_background`, timeout 3600000; one per stop signal; none running now). Replace the marker with the task/attempt text of the newest `grep '^\[ralph\]' .evidence/runner.log | tail -1` line (after ACCEPT it will be the review set: `0.12.r0a`/`b`/`c` lines, whichever the runner prints first). When a marker text already occurs earlier in the log (a retried attempt 1), wait for the occurrence count to grow instead of the first match:
  ```
  cd /Users/mlsavage/Developer/essaydown; until grep -q "<MARKER> (loop" .evidence/runner.log; do sleep 10; done; start=$(grep -n "<MARKER> (loop" .evidence/runner.log | tail -1 | cut -d: -f1); until tail -n +"$start" .evidence/runner.log | grep -qE "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT) |<promise>COMPLETE"; do sleep 60; done; tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|ROTATE-PRINCIPAL) |^ROTATE-PRINCIPAL$|^\[ralph\] " | tail -12
  ```
  Never add `stop-check` to the final grep: it matches transcript lines and floods the output. Read an attempt's outcome from the transcript's final `result` event (`subtype`, `num_turns`, `is_error`, `result`); when `result` is the rate-limit banner, read the last `rate_limit_event` in `runner.log` (`resetsAt`, `utilization`).
- **Expected next stop**: Michael runs `scripts/gate.sh 0.verifyh` → `ACCEPT 0.verifyh a1` (then the advisory `ROTATE-PRINCIPAL`, already satisfied by this rotation) → restart → review set 0.12 (three reviewer containers in parallel) → `PRINCIPAL 0.12.r0d`; or `GATE-FAILED` + `PLAN-GATE` → plan request → planning commit with `0.verify.g1` + its gate.

## Corrections

- `003-phase-0-0.3-running.md` (Next Steps 2) and the previous `next-prompt.md` (step 3): "put the findings into the task's worktree as lesson lines (`git -C .wt/<id> cherry-pick` of a phase/0 lessons commit)" does not work once the task branch has its own commits touching `docs/lessons.md` (the cherry-pick and the integration rebase both conflict), and `startTask` (`ralph/lib/integrate.mjs`) reuses an existing branch and worktree, so a retried agent never sees commits made to `phase/0` after its branch was cut. Two procedures replace it: (a) a blocked task whose branch must see new `phase/0` commits is **re-cut** (DECISIONS #016, last bullet: `git worktree remove .wt/<id> --force`, `git branch -m task/<id> abandoned/<id>-r1`, `ralph/ralph.sh retry <id>`; reversal before the runner starts: Ctrl-C the runner, remove the worktree, `git branch -D task/<id>`, rename the abandoned branch back; state stays `pending`); (b) a task that is not blocked (`NO-JOURNAL`, attempts < 3) gets its lesson line committed **directly on its task branch** (`chore(<id>): principal lesson …`, done for 0.10 as `49eb5e5`, squashed into `efa1cb2`); never a DECISIONS edit on a task branch.
- `002-phase-0-running.md` deviation 13 / `#repair-phase0-checkout`: the `76774f2` runner fix refreshes only a **clean** checkout. The handoff-003 commit `c8b234a`, made on the host checkout 57 s after `task(0.4)` integrated, deleted the 91 files 0.4 added; `doctor`'s ancestry check could not see it. Repaired by Michael in `59d2d3e`; procedure and doctor gap in DECISIONS #017.
- DECISIONS #017 says "Michael verified it independently on the Mini"; the finding and the repair commit came from Michael's **independent guide session**. Correct the attribution in the next entry that touches #017 (the review-0-r0 reconciliation is the natural place); never amend #017.
- The previous `next-prompt.md` step 4/5 wording stands; this file's Next Steps are the current version.
- `.evidence/tasks/0.5/1.log` is the transcript of the re-cut 0.5 (DONE); `2.log` and `3.log` there belong to the abandoned first branch (`abandoned/0.5-r1`) and are not attempts of the passed task.

## Decisions

- **DECISIONS #016-passed-task-files** (`6297093`): "passed tasks are never edited" covers task records, ids, status and integrated commits (CLAUDE.md, AGENTS.md, PRINCIPAL.md, PRD §10); RUNNER-SPEC is silent on files. 0.4's literal `44` was right at 0.4 and wrong at 0.5; the green bar (CLAUDE.md step 2, RUNNER-SPEC §4.3) compelled 0.5's edit of `fixtures-corpus.test.ts`, so it was in scope. The authorization also stands as the imperative `[0.4]` lesson line (the agent's orientation channel), which is what unblocked the re-cut 0.5 at attempt 1. RUNNER-SPEC is silent on renaming an unintegrated task branch; #016 sanctions it, not the spec.
- **DECISIONS #017-host-checkout-commit** (`6297093`): the c8b234a repair; manual procedure — the principal never commits on the host checkout while the runner loop is live, and before staging runs `doctor` (clean), `git reset --hard phase/0` (or stash / reset / pop), confirms `git log -1 phase/0`; doctor gap recorded as a **finding for the 0.12 reviewers** (a passed task's files silently reverted by a later commit is invisible to the ancestry check), no machinery added; **no principal commits on `phase/0` after `0.12.r0d` integrates** (RUNNER-SPEC §8.1: `phase/0` head must equal `reconciliation_sha` at `0.close`).
- Runner deviations noticed since 003 (none adds machinery): (19) a rate-limit death of the container's Claude subscription (`rate_limit_event` `status: rejected`, five-hour window) ends the attempt as `NO-JOURNAL` and counts toward STUCK (0.10 a1); (20) the runner itself commits a dirty worktree as `wip(<id>): recovery of uncommitted changes` (`ralph/lib/run.mjs` lines 107/119) — this is what saved 0.10 a2's full implementation after the max-turns death; (21) a principal commit on a task branch is squashed into the task commit (0.10, `49eb5e5` → `efa1cb2`); (22) the auto-mode classifier of this Claude Code session refused every `git commit` on the **host checkout** (worktree commits were allowed): Michael pasted the host-checkout commit himself from a message file the principal wrote in the scratchpad.
- Reversal shape rule (Michael, 2026-09-06): a stated reversal for a runner command must return the task to the status it came from. `abandon` sets `abandoned`, which `retry` refuses (`ralph/lib/doctor.mjs` `retry()` accepts only `blocked`/`integration-failed`), so `abandon` is never a reversal of `retry`; prefer git-only reversals that leave runtime state untouched, and read the state transition of every ralph command named.

## Gotchas

- The five-hour subscription window is shared by every container attempt. Before restarting after any `NO-JOURNAL`, read the last `rate_limit_event` in `runner.log`; if `status` is `rejected`, wait for `resetsAt` (+30 s) before `send-keys`, otherwise the restart burns another attempt (three → STUCK). Utilization at this rotation: five-hour 0.11, seven-day 0.20.
- Opus tasks spend ~25 of their 50 turns orienting (reading PRD, sources, lock file) and can die at the cap with everything green but no journal (0.10 a2). The steer that worked at a3 (20 turns): "do not re-read or rewrite; suite from HEAD; fix only red; journal and commit by turn 20; then DONE". If a Phase 1 Opus task shows the same shape twice, raise the turn budget in a `fix(runner)` commit + conformance test only if RUNNER-SPEC allows it; otherwise cut per CLAUDE.md and record in DECISIONS.
- If a retried agent ends without DONE citing a conflict the lessons channel was supposed to resolve, do not send another loop attempt; write the planning commit appending the fix task the agent asked for (Michael's standing instruction).
- Host-checkout commits: only with the runner loop stopped and `doctor` clean, after `git reset --hard phase/0` and a `git log -1 phase/0` check (#017). Message files in the scratchpad are fine for Michael to `git commit -F`; he reads them before committing.
- All of 002's and 003's gotchas stand: never start the runner with a cherry-pick unresolved; `RALPH_CI_ADAPTER=fake` is the test gate; `docker compose run` needs `ESSAYDOWN_ROOT`; the `ROTATE-PRINCIPAL` line only appears from runs started after `3a86b3d`.
- `scripts/gate.sh 0.verifyh` prints `ROTATE-PRINCIPAL` after recording the attempt; that reminder is already satisfied by this rotation (DECISIONS #015 rotates at the `HUMAN_GATE`, before the gate runs). The fresh session runs the gate and continues to the reconciliation.

## Next Steps

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, the signal lines of `runner.log`; one-line state to Michael. Then the gate: `cd ~/Developer/essaydown && scripts/gate.sh 0.verifyh` (pushes `ci/0.verify/a1` at `f8c91c5`, waits for the three-OS `ci.yml` run, fetches the named artifacts into `.evidence/ci/0.verifyh/a1/`, writes `accepted.json` on ACCEPT; pattern in 002 Current State). On `GATE-FAILED` + `PLAN-GATE`: plan request `plan.0.verifyh.r1` → `ralph/ralph.sh plan plan.0.verifyh.r1` → planning commit adding `0.verify.g1` + gate per RUNNER-SPEC §3 (generator + validator green), `<promise>DONE plan.0.verifyh.r1</promise>` in the commit, restart.
2. On ACCEPT: restart the runner; the review set 0.12 starts (`.evidence/reviews/0/r0/{phase_base_sha, verifier_id (0.verify), verification_sha, implementation_sha}` recorded, both SHAs must equal `f8c91c5`; worktrees `.wt/review-{claude,sol,grok}`; watch `.evidence/reviews/0/r0/<who>/transcript.log` and `exit-code`). A failed reviewer → `STUCK 0.12.r0<a|b|c>` → read its transcript, `ralph/ralph.sh retry 0.12.r0<x>`, restart (Grok headless is the open question; RUNNER-SPEC §2 has the host fallback).
3. `PRINCIPAL 0.12.r0d`: reconcile in `.wt/0.12.r0d` per 002 Next Steps 4. Hand the reviewers' findings the deviations list (002 items 1–18 + 19–22 above) and dispose of the #017 doctor gap explicitly; correct #017's provenance in the `review-0-r0` entry; `verdict: PASS|FAIL` line; on FAIL the fix tasks + `0.verify.r1` + `0.12.r1a/b/c/d` written physically into PRD §8 (`node ralph/generate-tasks.mjs`, `node ralph/validate-tasks.mjs`), close rewired to `0.12.r1d`; journal line; commit `wip(0.12.r0d): …` with `<promise>DONE 0.12.r0d</promise>`; restart. After that commit integrates, **no principal commits on `phase/0`**: the runner syncs state and runs `0.close` (moves `main`, creates `phase/1`).
4. `/rotate` → handoff 005 for Phase 1 (the runner is then started with `--phase 1`; next-prompt must say so).

## Open Questions

- Grok headless in the container (learned at 0.12.r0c); Ollama for 5.3b (deferred) — unchanged from 002.
- Whether 50 turns is enough for Phase 1 Opus tasks (0.10 needed three attempts; two were budget/limit deaths, not defects). Decide from evidence, in DECISIONS, not by adding machinery.
