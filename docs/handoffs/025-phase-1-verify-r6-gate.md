# Handoff 025 — Phase 1 at `HUMAN_GATE 1.verify.r6h`: the r6 chain (1.45–1.48) integrated, the verifier passed, the CI gate open for Michael

Written 2026-09-12 by the principal (the fresh session that ran the `1.10.r5d` reconciliation and watched the r6 chain; Claude Code, Remote Control session on the Mac Mini), at the `HUMAN_GATE 1.verify.r6h` stop signal, per DECISIONS #015 (the runner printed `ROTATE-PRINCIPAL`). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `024-phase-1-review-r5-reconciliation-pending.md` (now in the tree at `phase/1`, cherry-picked by `1.10.r5d`). This commit is on `handoff/025`, cut from `phase/1` — not stacked on `handoff/024`, because 023 and 024 are already in `phase/1` and a stacked cut would make the next cherry-pick a conflicting range; `git log --oneline phase/1..handoff/025` shows one commit. The `1.10.r6d` reconciliation cherry-picks `phase/1..handoff/025` (plus any handoff stacked on it later). Next: `026-*.md`.

## Current State

- **`phase/1` tip `c2f6526`** (`task(1.verify.r6)`); 73 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean. `CLAUDE.md`/`AGENTS.md` gained the two promoted bullets (K1, K2) at `c2f6526`.
- **The r6 chain, all passed** (`.evidence/state/tasks.json`, turn counts from the transcripts' result lines): `1.45` (opus) passed at a2 in 37 turns, integrated `5780e9e` — a1 died at 2 turns on a session-limit 429 before its stub (`NO-JOURNAL 1.45`, the second instance of #027's case; Michael reset it, the plain restart followed); `1.46` (opus) passed at a2 in 44, integrated `5163835` — a1 `error_max_turns` at 51 with all four deliverables committed on the branch; `1.47` (sonnet) a1 51 without a stub (`NO-JOURNAL`), a2 51 (stubbed, capped widening the text assertion to every corpus caller), a3 51 (everything green: lint, 3,254 unit tests with no expected fail, cargo, e2e 88/88, capped before completing its stub), `STUCK 1.47`, two `chore(1.47): principal lesson` commits on its branch (`4bb029c`, `e921e2f`), logs moved aside (`1.a1-capped-nostub.log`, `2.a2-capped.log`, `3.a3-capped.log`), `retry`, passed at 42, integrated `4f7fc35`; `1.48` (sonnet) passed at a1 (54 per its result line), integrated `749a51e`; `1.verify.r6` (sonnet) passed at a1 in 38, integrated `c2f6526`. The suite's expected-fail count is 0 since `1.46`; `e2e/web/` holds 22 spec files at `c2f6526` (`git ls-tree -r --name-only c2f6526 e2e/web | grep -c '\.spec\.ts$'`).
- **Gate `1.verify.r6h`: human-pending** (CI gate, `ci/1.verify.r6/a{n}`, artifacts `test-logs` and `playwright-report`). Not run yet. `1.10.r6a/b/c` pending on it; `1.10.r6d` pending on them; `1.close` on `1.10.r6d`.
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.verify.r6h human-pending; last passed 1.verify.r6; gates open: 1.verify.r6h; plan requests: none; blocked: none." Graph 232 tasks (`EXPECTED_COUNT` 232).
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r6h` / `ROTATE-PRINCIPAL` (runner.log lines 96,740–96,741; the log is 96,741 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the gate's ACCEPT and the idle checks: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter` — the runner starts `1.10.r6a/b/c` together.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r6h` (line 96,740).
- **Expected next stop**: `scripts/gate.sh 1.verify.r6h` → `ACCEPT` (then the restart and `[ralph] 1.10.r6a attempt 1` with b and c within a second) or `GATE-FAILED 1.verify.r6h` (the `0251db3` planning pattern: `ralph/ralph.sh plan plan.1.verify.r6h.r0`, a `.g1` repair scoped to what failed); after the review set, `STUCK 1.10.r6` only if a reviewer fails (a Grok 402 is Michael's call, #025/#027), otherwise `PRINCIPAL 1.10.r6d` — rotate there, the reconciliation is a fresh session (#015).

## Corrections

- `024` Gotchas, "the r6 chain is at least two implementation tasks": four fix tasks — `1.45` (the serializer's whole-scalar encoding), `1.46` (the position map), `1.47` (the fixture, the legs' code-point rule, the text assertion), `1.48` (the three uncovered cells). `024`'s `next-prompt.md` named `1.45` as "app-owned handlers with `codePointAt`" and gave it the fixture: the probe decided otherwise (#review-1-r5, "the cause … and the shape 1.45 takes") — the encode decision is right in the parser's code-unit terms, only its width was wrong, so 1.45 wraps `state.containerPhrasing` and the fixture went to 1.47 after the map fix (the scratch suite showed the deletion leg puts an encoded neighbour last in a block).
- `024` Open Questions, "whether 1.47 precedes or follows 1.46": follows, with 1.48 last.
- `023`/`024` constraint text, "a principal commit goes on `handoff/NNN` … cut from the newest unmerged `handoff/NNN`": the stacking applies only while the previous handoff is not yet in the phase branch; once a reconciliation has cherry-picked it, the next handoff is cut from `phase/<N>` again (this commit).

## Decisions

- **`#review-1-r5`** (in the tree at `159c4c0` → integrated as `task(1.10.r5d)`): verdict FAIL; K1–K9; the fix shape decided from a prototype (96 of 96 trees fixed points); the 1.40 pin dispositioned into 1.46; reviewer record (three independent reports; unique Claude 5 of 8, Sol 0 of 2, Grok 1 of 3; three shared findings all agreed; cumulative twenty-two shared, eight disagreements, low side Grok 6 / Claude 2 / Sol 2); the turn-budget paragraph; promote candidates (a) and (b) now in CLAUDE.md via `1.verify.r6`, (c) recorded for `docs/PRINCIPAL.md` at the boundary.
- **No DECISIONS entry this block after `#review-1-r5`.** Two principal-lesson commits on `task/1.47` (`4bb029c` before attempt 2: stub first with `printf`; `e921e2f` before the retry: the work complete and green in attempt 3, one new journal line, the suite once). The clause of 1.47's *description* "and in `expectNoForbiddenEntity`'s corpus callers" over-reached (the space-typing legs' editor text holds whitespace the strip drops); the *acceptance* names the two neighbour legs only, so the acceptance bound and no entry was needed — 1.47's journal records the reading. Candidate lesson for `docs/PRINCIPAL.md` at the boundary: a task description's scope sentence and its acceptance sentence are written from the same list, so an agent never meets a description clause the acceptance cannot discharge.
- **Runner deviations this block: none.** The 429 on `1.45` a1 consumed an attempt exactly as #027 describes (second instance; evidence for the boundary `fix(runner)` on `api_error_status`). `1.47` a1 capped without a stub and was read as `NO-JOURNAL` (a cap with a stub is not a stop; without one it is) — per RUNNER-SPEC §4.3. `retry 1.47` after `STUCK`: `blocked → pending`, attempts 0, as `ralph/lib/doctor.mjs` states. No `admin` command, no `docker/` change, no `fix(runner)`.
- **Turn budget, evidence only** (transcripts' result lines): 1.45 a1 2 (429) / a2 37; 1.46 a1 51 (capped, work done) / a2 44; 1.47 51 / 51 / 51 then 42 on the retry; 1.48 54; 1.verify.r6 38. Two of four fix tasks capped with the work complete on the branch; both recovered by the retry-with-a-lesson shape. Phase 1 to date: 26 of 58 `loop` tasks needed more than one attempt (23 of 52 at #review-1-r5 plus 1.45, 1.46, 1.47); `NO-JOURNAL` stops 14 (12 plus 1.45's 429 and 1.47's cap-without-stub). Michael's call at the boundary.

## Gotchas

- **`scripts/gate.sh 1.verify.r6h` runs longer than Claude Code's 120 s foreground limit** and gets backgrounded with a notification; read its output file, not the pane; it prints `ROTATE-PRINCIPAL` after ACCEPT — that reminder is for the reconciliation session, not this gate; the restart follows ACCEPT.
- **After ACCEPT, the idle checks before the restart**: `.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux pane at the prompt, `doctor` clean. The three reviewers start together; check `started_at` within a second in `.evidence/state/tasks.json` — that is #025's independence condition for the reconciliation's count.
- **A Grok 402** during the set: the runner writes `STUCK 1.10.r6`; #025's manual procedure and Michael's decision (#027) before any single-reviewer retry — never retried on the principal's initiative.
- **A 429 in a task or reviewer transcript**: do not restart into the limit; wait for the reset Michael names, then the plain restart command; no `retry` (the attempt is consumed, the task stays `running`).
- **Spec-file count is 22 now** (1.45 and 1.46 each added a spec); the reconciliation counts from the directory, never from a journal.
- **In this zsh** a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. **Hand Michael the gate**: one-line state, then `scripts/gate.sh 1.verify.r6h` (backgrounded; read its output file; `.evidence/ci/1.verify.r6h/a1/` and `…/accepted/` on ACCEPT). On ACCEPT: the idle checks, the restart command above, arm the watcher; the review set `1.10.r6a/b/c` runs (Claude and Sol build cold; Grok reads the tree and the accepted CI). On `GATE-FAILED`: the `0251db3` planning pattern — `ralph/ralph.sh plan plan.1.verify.r6h.r0`, one `.g1` repair task scoped to what failed with the rest filed on the backlog with a revisit trigger (#021, #022, #024), `1.verify.r6.g1` `needsCI` with `ci/1.verify.r6.g1/a{n}`, the reviewers re-pointed at `1.verify.r6.g1`.
2. **At `PRINCIPAL 1.10.r6d`**: `/rotate`; the reconciliation is a fresh session (#015). It cherry-picks `phase/1..handoff/025` (one commit, plus any later handoff stacked on `handoff/025`), copies the three reports to `docs/reviews/phase-1-r6-{claude,sol,grok}.md`, verifies K1–K9 against the tree, counts the reviewer record (Grok independent only if started with its siblings), writes `#review-1-r6` with the verdict first. On PASS: `1.close` runs (main → `reconciliation_sha`, `phase/2` created); on FAIL: the r7 chain on the `159c4c0` pattern.
3. **Phase boundary idle window** (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)`; H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5 (the stacked-handoff rule with its "already in the tree" condition, the third case), the stale `description` line, PRINCIPAL.md's doctor sentence, PRINCIPAL.md gaining Sol's r4 lesson, #028's, Claude's and Sol's r5 lessons and the description-vs-acceptance lesson above; `git branch -D handoff/010 … handoff/025`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 and the Decisions above), the 429 signal (#027, two instances), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option.

## Open Questions

- **The r6 verdict**: K1 and K2 are fixed and covered on both halves (serializer and map) with the astral fixture in the corpus; the risk lines the reconciliation filed (astral members at a link's edge, a cell, a heading, a soft break — `[review-1-r5, Claude risk 1]`, hard stop `2.verify`) are the class a probing reviewer will seed next. A Required finding there makes r6 FAIL and the r7 chain one test-only task plus whatever the map shows.
- **Whether Grok starts with its siblings** (independence) and whether its usage holds — Michael's question if a 402 appears.
- The turn budget and the 429 signal: Michael's calls at the boundary, evidence unchanged in kind, two more instances each.
