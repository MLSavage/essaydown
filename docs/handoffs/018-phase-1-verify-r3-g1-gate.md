# Handoff 018 — Phase 1 at `HUMAN_GATE 1.verify.r3.g1h`: gate r3h failed on macOS, planning commit #022 integrated, repair `1.33` + `1.verify.r3.g1` passed, gate open

Written 2026-09-11 by the principal (the session that watched gate `1.verify.r3h` and wrote the `plan.1.verify.r3h.r0` planning commit; Claude Code, Remote Control session on the Mac Mini), at the `HUMAN_GATE 1.verify.r3.g1h` stop signal (rotate regardless of context at a verifier gate; DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `017-phase-1-verify-r3-gate.md` (on branch `handoff/017`, not in the tree). This commit is on `handoff/018` (rotate.md step 5: a verifier gate is open, so never on `phase/1`); the `1.10.r3d` reconciliation cherry-picks `handoff/017` and `handoff/018` (and any later `handoff/NNN`). Next: `019-*.md`.

## Current State

- **`phase/1` tip `f90ed50`** (`task(1.verify.r3.g1)`, attempt 1); 50 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`), 41 of them `task(1.*)`. No principal commit on `phase/1` since `b149f0c` and none until `1.close`. `main` unchanged at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). `origin` holds `ci/1.verify/a1` and `ci/1.verify.r2/a1` (stale, GATE-FAILED rounds) and presumably now `ci/1.verify.r3/a1` (this round's GATE-FAILED ref; not listed this session) — `gate.sh gc` at the boundary.
- **Gate `1.verify.r3h`: GATE-FAILED at a1** (ci.yml run 34563964087 at `32552e0`, `.evidence/ci/1.verify.r3h/a1/`): ubuntu and windows green on everything (35 unit files, cargo, 79 Playwright cases each); macos 78 passed, 1 failed — task 1.30's H8 case in `e2e/web/editor-mark-edge.spec.ts`, the pane polling for `a *b*  c` and receiving the seed's `a *b* c`. Cause (principal reading, confirmed by 1.33 from the log and the spec): `ControlOrMeta+Home` is not a document-start motion on macOS (the OS key-binding layer decides Home/End/chords in a contenteditable), so the space was typed at the paragraph's end and stripped; the `<em>` assertion used `toHaveText`, which normalises whitespace, so it could not catch the miss. Test defects only; the app is not implicated. `1.verify.r3h` is `superseded`.
- **Planning commit `plan.1.verify.r3h.r0`**: integrated at `de5dcf2`, DECISIONS `#022` (what happened, cause, scope decision, graph change, model choice, reviewer notes, reversal), `EXPECTED_COUNT` 194 → 197, one `docs/V1.1-BACKLOG.md` line (the four other rendered-view `ControlOrMeta+Home/End` uses in `editor-soft-line-breaks.spec.ts:85,107` and `editor-trailing-break.spec.ts:125,138`, which pass on all three runners only because a click already leaves the caret at the end; hard stop `2.verify`), a `[plan.1.verify.r3h.r0]` lesson. **`1.33`** (opus; `editor-mark-edge.spec.ts` only) passed at attempt 1 at 22 turns; integrated `440472e`: caret placed with `ArrowUp`/`ArrowDown` (Blink's one-line block edges) plus counted arrows, every `<em>` assertion byte-exact (`textContent` + `toBe`, `/^word ?$/` for the input-rule case), all four grep guards zero; its mutation (caret at the paragraph's end) reproduced the macOS diff exactly and went red on both the `<em>` and the pane assertion. **`1.verify.r3.g1`** (sonnet) passed at attempt 1 at 24 turns; integrated `f90ed50`; journal only. No cap, no stop signal, no principal action in this chain; both attempts appended their own journal line.
- **Gate `1.verify.r3.g1h`**: `human-pending`, opened at `f90ed50`; `.evidence/ci/1.verify.r3.g1h/` does not exist yet. The runner printed `HUMAN_GATE 1.verify.r3.g1h` then `ROTATE-PRINCIPAL` (runner.log lines 69,722–69,723). Michael runs `scripts/gate.sh 1.verify.r3.g1h` (a push notification went out at rotation); `accepted.json` or `rejected.json` under `.evidence/ci/1.verify.r3.g1h/` is the outcome.
- **Tasks** (`ralph/ralph.sh status`): phase 1 — 63 tasks, passed 54, superseded 3, human-pending 1, pending 5 (`1.10.r3a/b/c/d`, `1.close`). `doctor` clean, host checkout on `phase/1` at `f90ed50`, tree clean, `.wt/` holds the runner's task worktrees only (plus this rotation's temporary one, removed by it), branches `handoff/010` … `handoff/018`. Lessons tagged `1.*`/`plan.1.*`: 106.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `ROTATE-PRINCIPAL`. `.locks/` empty, no `ralph run` process, no containers. Restart after the gate is accepted: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's two watchers are stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r3.g1h` (log line 69,722; the log is 69,723 lines). A second watcher on the gate directory: `setopt nullglob; d=/Users/mlsavage/Developer/essaydown/.evidence/ci/1.verify.r3.g1h; until [ -f $d/accepted.json ] || [ -f $d/rejected.json ]; do sleep 30; done; ls $d` (one notification). Note from this session: `gate.sh` writes neither file on GATE-FAILED (only `a1/{run.json,result.json,workflow.log}`), so that watcher never fired; the runner.log watcher's `GATE-FAILED` line and the `gate.sh` foreground output are the signal — watch `$d/a1/result.json` instead if a file watcher is wanted.
- **Expected next stop**: on `ACCEPT`, restart → `1.10.r3a/b/c` (one container run each, ~18 min at r2; `.evidence/reviews/1/r3/` records `verifier_id 1.verify.r3.g1`, `verification_sha == implementation_sha == f90ed50`) → `PRINCIPAL 1.10.r3d` → rotate; the reconciliation is a fresh session (#015). On `GATE-FAILED`, the runner marks `1.verify.r3.g1h` blocked and opens `plan.1.verify.r3.g1h.r0`: `ralph/ralph.sh plan plan.1.verify.r3.g1h.r0`, planning commit per the #022 pattern (`de5dcf2`: a repair task `1.34` scoped to what failed, `1.verify.r3.g2` with `refTemplate ci/1.verify.r3.g2/a{n}`, `1.10.r3a/b/c` rewired to `1.verify.r3.g2` in PRD §8 (the generator makes it `.g2h`) and their `/logs/ci/1.verify.r3.g1h/accepted/` references (Claude 1, Sol 1, Grok 2) moved, `EXPECTED_COUNT` 197 → 200, `verdict`-free DECISIONS `#023`; the scratchpad script shape is `docs/`-free and lives only in that session — rebuild it from the `#022` graph-change bullet).

## Corrections

- `017` Expected next stop and Gotchas assumed the risk at `1.verify.r3h` was the real-clipboard case on windows-latest. It passed on all three runners; the failure was macOS, in 1.30's mark-edge case, on a caret chord — a member of the #020/#021 platform-accident class in the e2e layer, not the clipboard one.
- `017` Phase boundary items listed `gate.sh gc` for `ci/1.verify/a1` and `ci/1.verify.r2/a1` "and whatever `ci/1.verify.r3/a1` turns out to be"; it is a GATE-FAILED ref like the other two.
- `017` Current State counted 52 passed / 5 pending / 60 tasks; at rotation it is 54 passed / 5 pending / 63 tasks (the planning commit added `1.33`, `1.verify.r3.g1`, `1.verify.r3.g1h`; superseded is now 3).
- `017` Watcher: the gate-directory watcher waits for `accepted.json`/`rejected.json`, but `gate.sh` writes no `rejected.json` on GATE-FAILED (see Current State); it only fires on ACCEPT.

## Decisions

- **`#022` (DECISIONS.md, in the tree at `de5dcf2`)**: scope (3) of four — replace the chord with Blink-only motions and make the `<em>` assertions byte-exact, in the failing spec file only (the absence case in the same file included: same construct, same normalised assertion); the four other rendered-view uses to the backlog with a revisit trigger (the r3 reconciliation if a reviewer raises it, else Phase 2 planning as one test-only task plus a guard over `e2e/web/` in the `no-codemirror-history` shape); the source-view `Home`/`End` are CodeMirror's keymap, handled in JS, not members. Model: opus for `1.33` (the container cannot observe macOS), sonnet for the verifier. Reversal recorded (git-only before integration; none after). No reviewer text changed beyond the rewire and the accepted-artifact path (#review-1-r0).
- **Promote-lessons candidate for the r3 reconciliation** (not decided; the reconciliation decides, into `1.verify.r4`'s text on FAIL or `2.verify`'s on PASS): the platform-accident class now has three members — #020 (a `Mod-` chord resolved from the host in a unit test), #021 (one OS's clipboard convention), #022 (a Home chord the OS layer resolves in an e2e) — and CLAUDE.md's Code rules carry none of them. Candidate wording: `An e2e places the caret only through motions Blink alone decides (arrow keys, clicks at computed points), never a Home/End key or a modifier chord in the rendered view; a mark's text is asserted byte-exact, never through a whitespace-normalising matcher; a green Linux container proves nothing about a key the OS layer resolves.` Alongside the still-pending CLAUDE.md clarification from 017 ("each attempt appends its own journal line", steps 2 and 4).
- **Runner: no deviation, no `fix(runner)`.** `gate.sh` and the runner behaved per RUNNER-SPEC §2 (GATE-FAILED → blocked → `plan.<gate>.r0`; `plan` → worktree on `plan/1.verify.r3h/r0`; DONE → `plan(...)` candidate → suite → `sync-state` → `1.verify.r3h` superseded → `1.33` started). Conformance suite 83 pass / 0 fail in the plan worktree before the commit.
- **Turn budget, evidence only (the decision stays at the Phase 2 boundary on Michael's OK, #review-1-r1)**: `1.33` a1 22, `1.verify.r3.g1` a1 24; no cap. Phase 1 to date: 16 of 38 loop tasks needed more than one attempt; `NO-JOURNAL` stops 11 (unchanged since 017). Under the stub rule the last four loop attempts (1.31, 1.32, 1.verify.r3, 1.33, 1.verify.r3.g1 — five) all passed at a1; the number stays 80, conditional.

## Gotchas

- **`gate.sh` at this gate prints `ROTATE-PRINCIPAL`** as a reminder; the next session is fresh, so no second rotation is needed before the gate outcome. It rotates at `PRINCIPAL 1.10.r3d` (ACCEPT path) or at `HUMAN_GATE 1.verify.r3.g2h` (GATE-FAILED path).
- **The macOS Playwright log joins the Windows one as the first unknown of every gate.** `run.json` records `artifacts: []` on failure (#020: fetched only on success) even though the `merge-logs` job ran and merged three per-OS `playwright-report` uploads this round; the gate's own `workflow.log` is the evidence, `grep -nE '##\[error\]|[0-9]+ (failed|passed) \(' .evidence/ci/<gate>/a1/workflow.log` finds the failing step and the per-OS counts.
- **Each attempt appends its own journal line** (the `[1.29]` lesson; both new tasks complied). If a task in the r3 review set or later trips it: `chore(<id>): principal lesson` on its task branch plus a restart (`retry` only when `blocked`).
- **`1.10.r3a/b/c` texts** now cite `/logs/ci/1.verify.r3.g1h/accepted/`; everything else in them is as 017 described (the browser probes per fix task; Grok asked whether a read-only review could have found H1/H2 and why H7's severity differed). The reviewers will find `1.33`'s spec change in the tree; #022's "for the reviewers" bullet lists what the reconciliation should read for.
- **Phase boundary items** (only after `1.close`): H7's `fix(runner)` on `ralph/PROMPT.md` + `tests/agent-rules.test.ts` widened (backlog line, hard stop `2.1`); `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `gate.sh gc` for `ci/1.verify/a1`, `ci/1.verify.r2/a1`, `ci/1.verify.r3/a1` (list first: `git ls-remote origin 'refs/heads/ci/*'`); the `phase/1` push (#010); `git branch -D handoff/010 … handoff/018`; the conditional turn-budget `fix(runner)` only on Michael's OK; the two CLAUDE.md promote candidates above if not yet promoted.
- **Reports and Sol's evidence**: at `PRINCIPAL 1.10.r3d`, `.evidence/reviews/1/r3/{claude,sol,grok}/report.md` → `docs/reviews/phase-1-r3-*.md` verbatim; Sol's probe scripts and logs sit directly under `.evidence/reviews/1/r2/sol/` (`probe.mjs`, `probe.log`, `control.mjs`), not under a `report/` directory as 017 said — evidence, not for the tree. `status.json` verdicts are not the signal; the bodies are.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers. `grep -F -- '- [id]'` needs the `--`. A backlog line's bracket carries a suffix (`[<id>, found outside scope]`), so an anchored `\[<id>\]` grep misses it.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm the runner.log watcher (and a file watcher on `.evidence/ci/1.verify.r3.g1h/a1/result.json` if wanted); one-line state to Michael; the ask is `scripts/gate.sh 1.verify.r3.g1h` (no placeholder) if `.evidence/ci/1.verify.r3.g1h/` does not exist yet.
2. On `ACCEPT` (`.evidence/ci/1.verify.r3.g1h/accepted.json`, three OS directories in both artifacts): idle checks, restart, `1.10.r3a/b/c` → `PRINCIPAL 1.10.r3d` → `/rotate`; the reconciliation is a fresh session that cherry-picks `handoff/017` and `handoff/018` into `.wt/1.10.r3d`, copies the three reports, writes `DECISIONS.md#review-1-r3` with `verdict: PASS|FAIL` first (H1–H9 confirmed or reopened, the per-reviewer unique-findings table re-derived from the bodies, the `chore(1.29)` principal-lesson commit audited as a deviation of the #016 shape, #022's scope decision and the four backlogged chord uses read against what the reviewers raise, both CLAUDE.md promote candidates carried into the next verify task's promote-lessons text, the turn-budget note with counts read from `.evidence/state/tasks.json` and the transcripts), regenerates `docs/progress.md`, and on FAIL appends `1.34…` + `1.verify.r4` + `1.10.r4a/b/c/d` the way `e6d7493` did.
3. On `GATE-FAILED`: read `.evidence/ci/1.verify.r3.g1h/a1/workflow.log` before anything changes; `ralph/ralph.sh plan plan.1.verify.r3.g1h.r0`; planning commit per `de5dcf2` (#022) in `.wt/plan.1.verify.r3.g1h.r0` with DECISIONS `#023`; conformance suite green there; restart; `HUMAN_GATE 1.verify.r3.g2h` → rotate there.
4. After `PRINCIPAL 1.10.r3d` (fresh session): on PASS `1.close` → `CLOSED` → rotate at the phase boundary on `phase/2` from the host checkout with the runner idle, then the boundary items above; on FAIL the r4 chain.

## Open Questions

- Whether `ArrowUp`/`ArrowDown` reach a one-line block's edges on macOS Chromium as they do on Linux (Blink's line-motion fallback; the gate is the only proof). If not, the `#022` fallback is a click at a computed point of the `<em>`'s box, and `#023`'s repair scopes to that.
- Whether the r3 reviewers raise the four backlogged chord uses (Claude's r2 risk-1 habit suggests yes) — the reconciliation then either promotes the CLAUDE.md rule and files them for Phase 2 or scopes a `1.34` test-only sweep.
- The turn-budget number: 80 proposed, conditional; five consecutive a1 passes under the stub rule — Michael's call at the boundary.
