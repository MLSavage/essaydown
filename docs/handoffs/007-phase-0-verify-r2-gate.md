# Handoff 007 — Phase 0 at gate 0.verify.r2h (review attempt r1 reconciled FAIL; fix chain 0.23–0.26 and 0.verify.r2 passed; review attempt r2 next)

Written 2026-09-07 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `HUMAN_GATE 0.verify.r2h` rotation required by DECISIONS #015 (the reconciliation `0.12.r2d` is never run by the session that ran the phase's tasks or the previous reconciliation). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `006-phase-0-verify-r1-gate.md`; `002-phase-0-running.md` stays the reference for the gate evidence pattern and the reconciliation checklist (its Next Steps 4). Next: `008-*.md`.

## Current State

- `phase/0` tip `98bee5a` (`task(0.verify.r2)`) on `3af7fe8` (0.26) on `dd82ae9` (0.25) on `4440fa8` (0.24) on `87f0c93` (0.23) on `6cb8ed2` (0.12.r1d, verdict FAIL) on `5649a93` (0.verify.r1). Passed: 0.0–0.11, gate 0.2h, 0.verify, 0.13, 0.verify.g1, gate 0.verify.g1h, 0.12.r0a/b/c/d, 0.14–0.22, 0.verify.r1, gate 0.verify.r1h (ACCEPT a1, run 34076975712), 0.12.r1a/b/c, 0.12.r1d, 0.23–0.26, 0.verify.r2 — every task from 0.13 onward at attempt 1 (0.23 14 min, 0.24/0.25 11 min, 0.26 8 min, 0.verify.r2 4 min; 0.12.r1a needed three container runs, see Decisions 30). Superseded: 0.verifyh. **Open gate: `0.verify.r2h` (human-pending, waiting for `scripts/gate.sh`).** Pending: 0.12.r2a/b/c → 0.12.r2d → 0.close. Graph 150 tasks, `doctor` clean, `main` untouched at the seed `660f3aa`, `.evidence/reviews/0/r1/verdict` = `FAIL`, no remote `ci/*` refs expected (gc after the gate if any remain).
- **This handoff is on branch `handoff/007`, not on `phase/0`** (DECISIONS #review-0-r1 deviation 29: a commit on the phase branch after the verifier breaks RUNNER-SPEC §5.1's `verification_sha == implementation_sha` at review start). The host checkout stays on `phase/0` at `98bee5a`, clean. The r2 reconciliation cherry-picks `handoff/007` into `task/0.12.r2d` (docs/**, inside the §8.1 allowlist) exactly as 006 was cherry-picked from `handoff/006` into 0.12.r1d. Delete both `handoff/*` branches after 0.close. The SessionStart hook names the newest handoff in the working tree, so it will say 006 until the cherry-pick; read 007 with `git show handoff/007:docs/handoffs/007-phase-0-verify-r2-gate.md` if the tree does not have it.
- **Runner**: tmux session `essaydown`, window 1 `runner`, pane pid 8940, currently `zsh` (exited at `HUMAN_GATE 0.verify.r2h`, followed by `ROTATE-PRINCIPAL` — the first time the runner itself printed it, 0.18's code being live). Restart: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter` after `tmux capture-pane -p -t essaydown:runner | tail -3` shows the shell prompt (exactly one runner process, F14). **The next start is the first to load 0.25's lock code** (a dead-pid lock is refused, never broken: if `.locks/ralph` exists with no live process, remove it by hand first; nothing hand-written must ever be left there) and 0.26's compose budget (`RALPH_REVIEW_MAX_TURNS`, default 160, for the three review services — no env var needed on the restart). Image rebuilt after 0.26 (`docker compose -f docker/compose.yml build`, cached, at 07:19Z; `ESSAYDOWN_ROOT` must be set).
- **Watcher**: the background-Bash `until` loop of 002–006 was killed three times this session by the harness's memory-pressure guard (Mac Mini at ~50 % free), each time silently. Use the `Monitor` tool instead (persistent, 30 s poll, one event per line; no sleep-less loop):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 0.verify.r2h` / `ROTATE-PRINCIPAL` (log at 25,484 lines). After the gate ACCEPTs and the runner restarts, the review start prints nothing until the stop signal (r0 and r1 behaved the same); the audit log shows `reviewer started` ×3.
- **Expected next stop**: `PRINCIPAL 0.12.r2d` after the three reviewers finish (Sol and Grok took 13 min at r1; Claude 13 min once it had 160 turns), or `STUCK 0.12.r2<a|b|c>` for a failed reviewer.

## Corrections

- `006-phase-0-verify-r1-gate.md` Current State "Restart … this start is the first to load the 0.18–0.20 runner code": true, but the restart stopped at once with `REVIEW-SHA-MISMATCH 0.12.r1: verification 5649a93 != implementation 5c7f14f` because 006 itself sat on `phase/0` (deviation 29). Its Next Steps 3 restart instruction is therefore wrong for any handoff committed at a verifier gate; this file's Current State replaces it.
- `006` Next Steps 3 "`ralph/ralph.sh retry 0.12.r1<x>` (reversal: none needed …; the attempt directory stays)": the retry overwrites `transcript.log` in the reviewer's directory (the entrypoint `tee`s over it); copy it to `transcript.attempt<n>-<reason>.log` first (done for r1's two failed Claude runs).
- `006` Gotchas "watcher … `until` loop" and its Current State watcher command: superseded by the Monitor form above.
- `006` Decisions "(28) … nothing to do unless a same-run task depends on the new behaviour": still true; additionally the runner now prints `ROTATE-PRINCIPAL` at verifier gates, so the advisory from `gate.sh` is the second copy.
- `.claude/commands/rotate.md` step 5 ("commit … on the current phase branch while holding the runner lock"): wrong at a verifier gate since deviation 29; this rotation committed on `handoff/007` from a temporary worktree (`git worktree add -b handoff/007 .wt/handoff-007 phase/0`, commit, `git worktree remove`), without the lock — the runner was not running, the commit touches nothing the runner reads, and 0.25's incoming code refuses a leftover hand-written lock. The file itself is outside the §8.1 allowlist and is fixed by a `chore(0.0)` commit on `phase/1` before 1.verify (Next Steps 6).

## Decisions

- **DECISIONS #review-0-r1** (in `6cb8ed2`): verdict **FAIL**; 13 findings G1–G13 (G1/G2 blockers in the front-matter writer → 0.23; G3 spanning marks → 0.24; G4 stale-lock path freed → 0.25, automatic breaking cut to a manual procedure; G5 → 0.25; G6 untested review-start guard → 0.26; G7/G8 → 0.23; G9–G12 docs taken in the reconciliation; G13 recorded); r0 dispositions F1–F21 confirmed by all three reviewers, none reopened; Michael's `zod` ratification recorded (relayed via handoff 005, he can strike it); the install-time lesson, deviation 27 and the container PATH went to `docs/V1.1-BACKLOG.md`; RUNNER-SPEC §1 (stale lock manual), §5.1 (handoff branch), §11 (reviewer budget) amended; PRD §6.1 LF wording, §7 gate row, promote-lessons references fixed; `EXPECTED_COUNT` 140 → 150; 0.verify.r2 promoted one rule (one test per guard) into CLAUDE.md/AGENTS.md line 41.
- Runner deviations recorded there (none adds machinery): (29) rotate-commit vs review-start contradiction → manual procedure `handoff/NNN` (this file follows it); (30) reviewer turn budget: r1 Claude ended on `max_turns` at 50 and 80 turns (89 tool calls in the second run, still mid-review), completed in 96 turns at 160; 0.26 made 160 the review services' default; (31) one runner start without `tee` (a pasted command split at a newline) — the log lacks stdout for 02:59–03:13Z, the audit log is complete; (32) cargo absent from the reviewer container's login-shell PATH (Sol worked around it).
- Since the reconciliation (not yet in DECISIONS; for the r2 reconciliation entry): (33) **0.25's diff touches `ralph/lib/state.mjs`** (two lines: `setAuditLog(this.paths.audit)` so `writeAtomic` can record a failed directory fsync in the audit log, G5), which its task text did not list — compelled by the audit line the task asked for, since the write helper sits below the state layer; hand it to the r2 reviewers, dispose as recorded. (34) `docker compose build` after 0.26 was run **during** 0.verify.r2 rather than before it: the runner goes from 0.26 straight into 0.verify.r2 with no stop signal, and compose.yml is not baked into the image, so the first container that could observe the change is 0.12.r2a; the F17 wording ("before the next container task starts") is unsatisfiable when the docker/ change and the next task are adjacent in the chain — record the reading: before the next container task that can observe it.

## Gotchas

- **Order at this gate is fixed**: `scripts/gate.sh 0.verify.r2h` → `ACCEPT` → restart the runner (0.18's review start refuses without `accepted.json`). The gate pushes `ci/0.verify.r2/a1` at `98bee5a`, expects the single artifact `test-logs` with the three OS directories, writes `accepted.json`, deletes the ref, prints `ROTATE-PRINCIPAL` (advisory; done).
- **Never commit on `phase/0` from here to 0.close.** Not the handoff, not DECISIONS, not a lesson. After `0.12.r2d` integrates (on PASS), RUNNER-SPEC §8.1 requires `phase/0` head == `reconciliation_sha`; before it, §5.1 requires the head == the verifier. Every principal commit goes on `handoff/NNN` (docs) or into the reconciliation's task branch.
- Reviewer retry: `ralph/ralph.sh retry 0.12.r2<x>` resets `blocked → pending` and `attempts` to 0 (reversal: none; state returns to what `run` left). Copy `transcript.log` aside first. A reviewer's `result` event with `terminal_reason: max_turns` is a budget problem (now 160); `rate_limit_event` with `status: rejected` → wait for `resetsAt` + 30 s. Only the failed reviewer re-runs; the review-start SHA check re-runs too, so the phase tip must still equal `98bee5a`.
- 0.19's doctor reports principal commits that delete paths and passed tasks with debris; the reconciliation commit adds and appends only. 0.25's doctor reports `stale-lock <pid>` with the manual removal as the repair.
- Opus steer that works (0.14–0.25 at attempt 1 with F/G-numbered scoping): task texts that name the file list, the probes to turn into tests, and the exact rule to decide. Keep steers on the task branch (`chore(<id>): principal lesson …`) or re-cut per #016.
- The harness kills background shells under memory pressure without a stop signal in the log; `Monitor` survived. Check `ralph/ralph.sh status` on every wake before trusting silence.
- Reviewer reports for r1 are `docs/reviews/phase-0-r1-*.md`; r2 reviewers verify G1–G13 against the tree and read DECISIONS #review-0-r1 (not the handoffs). Give Michael verdicts and counts, never the reports.
- `RALPH_CI_ADAPTER=fake` is the test gate; `docker compose -f docker/compose.yml` needs `ESSAYDOWN_ROOT`; `.claude/settings.json` denies `git push --delete*` for the principal's shell, but `gate.sh gc`/`cleanupRef` delete refs through node.

## Next Steps

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, the last `^\[ralph\]` line and stop signal; one-line state to Michael; arm the Monitor (command above).
2. Give Michael: `cd ~/Developer/essaydown && scripts/gate.sh 0.verify.r2h`. Expect `ACCEPT 0.verify.r2h a1 run <id>` then `ROTATE-PRINCIPAL`; evidence under `.evidence/ci/0.verify.r2h/a1/` and `accepted.json` (pattern: 002 Current State "Gate evidence pattern"); confirm `test-logs/` holds the three OS directories and `sha` = `98bee5a…`. GATE-FAILED → `ralph/ralph.sh plan plan.0.verify.r2h.r0` → planning commit on the request branch adding `0.verify.r2.g1` + gate `0.verify.r2.g1h` physically in PRD §8 (RUNNER-SPEC §2 rule (a); `0.12.r2a/b/c` rewired to the new gate; generator and validator green; `EXPECTED_COUNT` bumped) with `<promise>DONE plan.0.verify.r2h.r0</promise>`, then restart. Transient CI failure → `scripts/gate.sh rerun 0.verify.r2h`.
3. On ACCEPT: check `.locks/` is empty, restart the runner → `0.12.r2a/b/c` (three containers; `.evidence/reviews/0/r2/{phase_base_sha, verifier_id, verification_sha, implementation_sha}` — `verifier_id` must read `0.verify.r2`, both SHAs `98bee5a…`; `docker top` of the claude-review container shows `--max-turns 160`). A failed reviewer → `STUCK 0.12.r2<x>` → read the transcript's last `result`, copy the transcript aside, `ralph/ralph.sh retry 0.12.r2<x>`, restart.
4. `PRINCIPAL 0.12.r2d`: reconcile in `.wt/0.12.r2d` on `task/0.12.r2d` per 002 Next Steps 4 and the r1 pattern in `6cb8ed2` (`git show --stat 6cb8ed2`): first `git cherry-pick handoff/007`; reports to `docs/reviews/phase-0-r2-{claude,sol,grok}.md`; `DECISIONS.md#review-0-r2` with `verdict: PASS|FAIL` as the **first** `verdict:` text; each r1 disposition G1–G13 confirmed or reopened; deviations 33–34 recorded; `docs/progress.md` regenerated (current state ≤ 6 lines, last 5 journal entries, metrics table extended with 0.23–0.26, 0.verify.r2, the r2 reviewers); nits to `docs/V1.1-BACKLOG.md`; on FAIL the fix tasks + `0.verify.r3` + `0.12.r3a/b/c/d` physically in PRD §8 (generator and validator green), close rewired to `0.12.r3d`; on PASS nothing else changes and the diff since `implementation_sha` stays within `docs/**`, `DECISIONS.md`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`; run `bash ralph/test/run.sh` (about 2 min; set `TMPDIR` to the scratchpad). Journal line; `wip(0.12.r2d): …` with `<promise>DONE 0.12.r2d</promise>`; restart → on PASS `0.close` runs (moves `main`, creates `phase/1`). **No principal commits on `phase/0` after `0.12.r2d` integrates.**
5. After `0.close`: `/rotate` (handoff 008 for Phase 1; the runner is then started with `--phase 1`); `git branch -D handoff/006 handoff/007` once their content is on `main` via the reconciliation commits.
6. First idle window in Phase 1 (before any task starts, or between tasks — never after 1.verify): one `chore(0.0)` commit on `phase/1` updating `.claude/commands/rotate.md` step 5 (handoff on `handoff/NNN` at a verifier gate, via a temporary worktree, no lock) and `docs/PRINCIPAL.md`'s Handoff cadence line, message says `Reverse: git revert this commit.`

## Open Questions

- Whether r2's Claude reviewer fits 160 turns with the r2 task text (G1–G13 plus three independent probes); the text tells it to write the report by turn 130.
- Whether the memory-pressure kills are the reviewer containers (three at once at review start) or the host's other load; `memory_pressure` read 42–52 % free each time. Nothing to change unless a container is killed.
- Grok headless in the container worked at r0 and r1; the RUNNER-SPEC §2 host fallback stays untested. Ollama for 5.3b remains deferred.
