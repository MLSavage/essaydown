# Handoff 015 — Phase 1 at `1.28` running: gate `1.verify.r2h` failed on Windows (clipboard CRLF), planning commit `plan.1.verify.r2h.r0` integrated, repair chain `1.28` → `1.verify.r2.g1` → `1.verify.r2.g1h`

Written 2026-09-10 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the end of the `GATE-FAILED 1.verify.r2h a1` working block (next-prompt 014 step 4: planning commit, then rotate). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `014-phase-1-verify-r2-gate.md` (on `handoff/014`, not in the `phase/1` tree; 010–013 are in the tree since `b149f0c`). This commit is on `handoff/015` (rotate.md step 5: a task is in flight and a reconciliation `1.10.r2d` is pending, so never on `phase/1`); the `1.10.r2d` reconciliation cherry-picks `handoff/014` then `handoff/015`. Next: `016-*.md`.

## Current State

- **`phase/1` tip `6a39a92`** (`plan(plan.1.verify.r2h.r0)`, integrated by the runner from `f247114` on `plan/1.verify.r2h/r0`); 39 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`), 31 of them `task(1.*)`. No commit on `phase/1` by the principal's hand since `b149f0c` and none until `1.close`. `main` unchanged at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010); `git ls-remote origin 'ci/*'` shows `ci/1.verify/a1` at `3e98707` (stale) and `ci/1.verify.r2/a1` at `6d9345c` (the failed attempt's ref) — both for `gate.sh gc` at the phase boundary.
- **Gate `1.verify.r2h`**: `GATE-FAILED` a1 (ci.yml run 34499653269 at `6d9345c`, 16:03–16:11 UTC), now `superseded`. Evidence `.evidence/ci/1.verify.r2h/a1/{run.json,result.json,workflow.log}`, `artifacts: []`. Red leg: `test (windows-latest)` › `Playwright (e2e/web)`, 3 failed / 65 passed, all three the Copy Markdown cases task 1.23 added in `e2e/web/editor-clipboard.spec.ts`, each receiving a carriage return before the newline from `navigator.clipboard.readText()`. Ubuntu and macOS green on every step. Plan request `plan.1.verify.r2h.r0` `resolved`, `planning_sha 6a39a92`.
- **Repair chain** (DECISIONS #021; tasks 181 → 184): **`1.28`** (opus, test-only, `e2e/web/editor-clipboard.spec.ts`: the three copy cases assert the string the app handed `writeText` through a `page.addInitScript` spy, byte-exact; no `readText`, no `process.platform`, no carriage-return escape in code; 1.23's flush-removal mutation re-run against the spy) — **running, attempt 1** (`[ralph] 1.28 attempt 1 (loop, opus, max-turns 50)`, log line 60,443 region; container `essaydown-claude-task-run-*`, worktree `.wt/1.28`); then **`1.verify.r2.g1`** (sonnet, `needsCI`, `refTemplate ci/1.verify.r2.g1/a{n}`, `artifactNames` unchanged; the CLAUDE.md/AGENTS.md steps of `1.verify.r2` are already in the tree and not repeated); then gate **`1.verify.r2.g1h`** (`scripts/gate.sh 1.verify.r2.g1h`); then `1.10.r2a/b/c` (rewired to `1.verify.r2.g1h`, their accepted-artifact path now `/logs/ci/1.verify.r2.g1h/accepted/`), `1.10.r2d`, `1.close`.
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `node` (the loop is live: `ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log`, restarted 16:3x UTC after the planning commit). `.locks/` holds the runner's lock while a task runs; `doctor` clean at rotation; host checkout on `phase/1` at `6a39a92`, tree clean. Restart after any stop signal, only after `.locks/` is empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` is empty, `docker ps` is empty and the pane shows the prompt: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's two watchers are stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `[ralph] 1.28 attempt 1`. At `HUMAN_GATE 1.verify.r2.g1h` arm the second `Monitor` on `.evidence/ci/1.verify.r2.g1h/{accepted,rejected}.json` (exits on the first hit; `GATE-FAILED` writes neither — it shows in the gate's own stdout and in `result.json`).
- **Expected next stop**: `HUMAN_GATE 1.verify.r2.g1h` after `1.28` and `1.verify.r2.g1` pass (two container runs, 10–20 min each). Before it: `NO-JOURNAL`/`STUCK` on `1.28` or `1.verify.r2.g1` are possible (three attempts each; the journal-stub rule is in force from `1.verify.r2`, so a capped attempt with its stub leaves the loop running).

## Corrections

- `014` Gotchas "After `ACCEPT`" and next-prompt 014 step 3 said the r2 review set's `verifier_id` is `1.verify.r2` and both SHAs `6d9345c`. Stale: the gate failed, so `verifier_id` will be `1.verify.r2.g1` and both SHAs the `integrated_sha` of `1.verify.r2.g1` (RUNNER-SPEC §2 rule (a): recorded from the actual accepted dependency at review start).
- `014` Expected next stop assumed `ACCEPT`. The Windows Playwright leg, named as the first unknown since #020, was the failure — the first time the e2e suite ran the 1.23 cases on Windows.
- `014` Gotchas "only 014 this time" for the reconciliation's cherry-pick. Now two: `handoff/014` then `handoff/015`, in that order, before anything else in `.wt/1.10.r2d`.

## Decisions

- **`#021-gate-1.verify.r2h-windows-clipboard-crlf`** (in `6a39a92`): cause (the OS clipboard read back on Windows carries CRLF; the app wrote LF; the assertion encoded one platform's convention, the class of #018 and #020); scope decision, explicit — spy `writeText` and assert byte-exact, chosen as strictly better over normalising the carriage return or branching on the platform (with the reasons); graph change `1.28` + `1.verify.r2.g1`, `1.10.r2a/b/c` rewired in the PRD rows themselves (explicit physical tasks since #review-1-r1) with the four accepted-path references moved; model choice (opus for `1.28` because the container cannot observe the failing platform and a miss costs a CI round plus a planning commit); reviewer notes; reversal (git-only before integration; the `plan` command's `pending → running` has no ralph reversal — `plan-retry` needs `blocked`/`integration-failed`, `plan-abandon` goes to `abandoned`).
- **No runner deviation this block.** `ralph.sh plan plan.1.verify.r2h.r0` as designed (worktree `.wt/plan.1.verify.r2h.r0` on `plan/1.verify.r2h/r0` from `6d9345c`), the planning commit `f247114` with the DONE promise, one restart after the four idle checks, integration by the runner (`6a39a92`, `sync-state` marked `1.verify.r2h` `superseded`), `1.28` started by itself. No `retry`, no admin command, no `docker/` change, no `fix(runner)`. Checks before the commit: `generate-tasks --check`, `validate-tasks`, `bash ralph/test/run.sh` (83 pass, 0 fail).
- **Edit of pending reviewer rows.** The four `/logs/ci/1.verify.r2h/accepted/` references in `1.10.r2a/b/c`'s descriptions were changed to `/logs/ci/1.verify.r2.g1h/accepted/` in the same planning commit: pending tasks, not passed records; recorded in #021 so the reconciliation does not read it as drift. The two references left in the tree are in `1.verify.r2`'s `ciAcceptance` and `1.verify.r2h`'s acceptance (passed / superseded, untouched).

## Gotchas

- **Rotation rule**: this file is on `handoff/015`; nothing else is on that branch. `docs/handoffs/` in the `phase/1` tree still ends at 013; the SessionStart hook names 013 as newest, which is wrong by two — read 014 and 015 with `git show handoff/01N:docs/handoffs/…`.
- **`1.28`'s scratch mutation**: the acceptance re-runs 1.23's flush removal in `DevEditor.tsx` and requires `git checkout --` before the commit; a capped attempt can freeze it into the checkpoint (1.25 a1 did this with `// SCRATCH MUTATION B`, 014 Gotchas). If `1.28` a1 caps, a2 reverts scratch edits before any other step; if `1.28` integrates, `git show --stat` the candidate for `apps/desktop/src/dev/DevEditor.tsx` — it must not be in the diff (scope is the spec file, lessons, journal).
- **Review set after the gate**: no `[ralph] <id> attempt` line; the evidence of the start is three `essaydown-{claude,codex,grok}-review-run-*` containers and `.evidence/reviews/1/r2/{verifier_id,verification_sha,implementation_sha}` (`verifier_id` `1.verify.r2.g1`, both SHAs its `integrated_sha`); ~18 min; `REVIEW-SHA-MISMATCH`/`STUCK` per `#review-0-r1` (retry once with the same budget, then record). `PRINCIPAL 1.10.r2d` is a fresh session (#015).
- **A second gate failure** → `PLAN-GATE plan.1.verify.r2.g1h.r0` → planning commit with `1.29…` + `1.verify.r2.g2` (`refTemplate ci/1.verify.r2.g2/a{n}`) and `1.10.r2a/b/c` rewired to `1.verify.r2.g2h` (again in the PRD rows, again moving the accepted path); the pattern is `git show 6a39a92` with #021, and the shape check is `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` in the plan worktree. The Windows Playwright log stays the first thing to read.
- **Phase boundary items** (carried from 013/014): `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `gate.sh gc` for `ci/1.verify/a1` and `ci/1.verify.r2/a1` (plus whatever `1.verify.r2.g1h` pushes); the `phase/1` push (#010); `git branch -D handoff/010 … handoff/015`; the conditional turn-budget `fix(runner)` on Michael's OK (#review-1-r1; r2 data in 014 Decisions); `1.25`'s multi-line journal entry and the stated-invariant sweep are Phase 2 planning items.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm the runner.log watcher; one-line state to Michael (nothing to ask until the gate opens).
2. At `HUMAN_GATE 1.verify.r2.g1h`: arm the gate-outcome `Monitor`; hand Michael `cd ~/Developer/essaydown && scripts/gate.sh 1.verify.r2.g1h` (the id is the whole argument; pushes `ci/1.verify.r2.g1/a1` at `1.verify.r2.g1`'s `integrated_sha`; prints `ROTATE-PRINCIPAL`, no second rotation).
3. On `ACCEPT`: the four idle checks → the restart command above → the three reviewer containers and `.evidence/reviews/1/r2/` → at `PRINCIPAL 1.10.r2d` give Michael the one line he tracks (each reviewer's verdict and blocker/should-fix/nit counts from `.evidence/reviews/1/r2/*/status.json`, unique findings per reviewer from the report headings), then `/rotate` regardless of context (handoff 016 on `handoff/016`); the reconciliation is the session after that and cherry-picks `handoff/014`, `handoff/015`, `handoff/016`.
4. On `GATE-FAILED`: Gotchas, fourth bullet; then `/rotate`.
5. On `NO-JOURNAL`/`STUCK` for `1.28` or `1.verify.r2.g1`: read `/logs/tasks/<id>/<n>.log` tail; a capped attempt with its stub is restarted with the same run command after the idle checks (1.25 precedent); at `STUCK` re-cut per #016 with `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart.

## Open Questions

- Whether the r2 reviewers, with the severity paragraph in TEMPLATE.md, still spread three ways on any one defect (014).
- The turn-budget number: 80 proposed, conditional; `1.28`'s attempt count adds to the r2 data before Michael decides at the Phase 2 boundary.
- Whether any other spec reads the OS clipboard for a copy assertion after `1.28` (the tree's `readText` count is a reviewer item in #021).
