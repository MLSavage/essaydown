# Handoff 010 — Phase 1 at `GATE-FAILED 1.verifyh a1`: macOS unit tests failed the workflow before the Playwright artifact was checked; plan request `plan.1.verifyh.r0` pending, runner idle

Written 2026-09-09 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `HUMAN_GATE 1.verifyh` rotation (DECISIONS #015; the runner and `gate.sh` both printed `ROTATE-PRINCIPAL`). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `009-phase-1-1.9-idle-window.md`. This commit is on `handoff/010` (rotate.md step 5: a verifier gate is open, so never on `phase/1`); the `1.10.r0d` reconciliation cherry-picks it. Next: `011-*.md`.

## Current State

- **`phase/1` tip `3e98707`** (`task(1.verify)`, integrated 2026-09-09T09:0xZ, attempt 1). Since handoff 009: `5345198` (handoff 009), `4590b8d` (DECISIONS #repair-1.9), `0a24bfa` (DECISIONS #019), `3e98707`. `main` unchanged at `052e206`. `origin/phase/1` is at `4590b8d` (Michael pushes by hand, #010); `origin` also holds `ci/1.verify/a1` at `3e98707` (`cleanupRef` removes it at the next gate).
- **Tasks**: 1.1–1.9 passed (attempts in handoff 009; 1.9 repaired, `a2`), **`1.verify` passed a1 (`integrated_sha 3e98707`)**, **`1.verifyh` blocked `GATE-FAILED` a1**, plan request **`plan.1.verifyh.r0` pending** (`kind gate-repair`, `trigger_sha 3e98707`, `evidence_path .evidence/ci/1.verifyh/a1`, reason `workflow ci.yml concluded failure`). Pending: `1.10.r0a/b/c/d`, `1.close`. `doctor` clean; host checkout on `phase/1` at `3e98707`; `.wt/` holds only this rotation's temporary worktree (removed by this rotation); no `task/*` branches.
- **CI run `34333160626`** (`ci/1.verify/a1`, 09:10–09:16Z): `test (ubuntu-latest)` success, `test (windows-latest)` success, **`test (macos-latest)` failure at step `Test`**, `merge-logs` success. macOS `pnpm test`: 2 files failed, 28 passed of 30; 5 tests failed, 1498 passed of 1503 — all five are keymap tests that fire a synthetic event with `ctrlKey: true`: `packages/editor/test/store.test.ts` › `undoKeymap (rendered view)` › "undoes on Ctrl+z", "redoes on the shifted-letter route, where the event's key is 'Z'", "redoes on the keyCode route, where the event's key is not a letter", "is installed by storePlugins, ahead of the editing keymaps"; `packages/editor/test/toggle.test.ts` › `the chord` › "Cmd/Ctrl+/ reaches a ProseMirror keymap". Each is `expected false to be true` on the "handled" boolean. Lint, cargo test and the runner conformance suite were green on all three. `workflow.log` (705 KB) is at `.evidence/ci/1.verifyh/a1/workflow.log`; `run.json` shows `artifacts: []` — **the `playwright-report` check was never reached** (artifacts are fetched only on `conclusion === success`).
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verifyh` / `ROTATE-PRINCIPAL`. `.locks/` empty, no `ralph run` process. **Do not `run` before the planning commit** — the runner has nothing eligible and would stop at `PLAN-GATE` again. Order: `ralph/ralph.sh plan plan.1.verifyh.r0` (prints `[ralph] plan … worktree .wt/plan.1.verifyh.r0 on plan/1.verifyh/r0` and `PRINCIPAL plan.1.verifyh.r0`), planning commit there with `<promise>DONE plan.1.verifyh.r0</promise>` in the message, then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`; the runner integrates the planning commit, marks `1.verifyh` superseded, and starts the first fix task.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `ROTATE-PRINCIPAL` after `HUMAN_GATE 1.verifyh` (log at 36,976 lines).
- **Expected next stop**: after the planning commit and restart, `[ralph] 1.<n> attempt 1` for the first fix task, then `HUMAN_GATE 1.verify.g1h` → `scripts/gate.sh 1.verify.g1h` → `ACCEPT … run <id>` with `test-logs` and `playwright-report`, or a second `GATE-FAILED` (→ `plan.1.verify.g1h.r0`, a `.g2` repair).

## Corrections

- `009` Gotchas, first bullet, and `009` Current State "Expected next stop": the gate did fail, but **not** on the missing artifact — the macOS `Test` step failed first and the artifact check never ran. The Playwright gap is still real (Finding 4, #019) and is repaired in the same planning commit; it is no longer the trigger.
- `009` Decisions "Finding 4": "never on macOS or Windows" is now half-corrected — the unit suite ran on all three; the e2e suite still has not run in CI anywhere.
- Commit message of `0a24bfa` says "origin/phase/1 at ca1cd3d"; at that moment `git ls-remote` already showed `4590b8d` (Michael had pushed the amended commit). The amend itself (`4590b8d`, described in #019) happened before that push; #019's sentence is correct, the commit message is not. Record only; nothing to revert.
- `009` Open Questions: findings 1 and 2 — resolved (#019: (b) presence-not-sense acceptance; the no-example-text rule). BBEdit file — resolved (#019: no list syntax in the file; observation stays not reproduced). Playwright pre-emptive vs designed round — resolved (#019: designed round).
- `next-prompt.md` at `0a24bfa` said step 4 rotates then runs the plan request: rotation done here; the plan request is this handoff's Next Steps 1.

## Decisions

- **DECISIONS #repair-1.9** (`4590b8d`, reworded in `0a24bfa`): 1.9's placeholder record re-recorded as `a2` from Michael's verbatim texts; the three ACCEPT fields set under the lock; reversal stated; findings (a)–(d) with backlog lines (a)–(c).
- **DECISIONS #019** (`0a24bfa`): `1.verifyh` takes the `GATE-FAILED` round (provenance through the plan request; first live exercise of RUNNER-SPEC §2 rule (a) outside fixtures — now happening); the fix task puts the failure in the job (a failing spec fails the workflow, `ciGate` refuses on conclusion; the report is evidence, never the pass condition); acceptance constraints (a) no literal spec count, (b) a green `1.verify.g1h` proves the suite ran on all three runners at the integrated SHA; never amend a pushed `phase/N` commit; no example text in a command handed to Michael; the BBEdit file has no list syntax.
- **Runner and gate behaviour this block (no deviation).** `1.verify` a1: one container run, integrated `3e98707` (lockfile unchanged, journal present). `gate.sh 1.verifyh`: ref push, `run.json`, wait, `result.json`, `workflow.log` fetched, `GATE-FAILED` → `blocked`, plan request raised, `PLAN-GATE` and `ROTATE-PRINCIPAL` printed — the path the 0.verifyh precedent (`1b1ddc2`, DECISIONS #018) took. Three principal commits landed on `phase/1` in the 1.9 idle window before `1.verify` (`5345198`, `4590b8d`, `0a24bfa`); `1.verify` was cut on top of them, so RUNNER-SPEC §5.1 holds (`implementation_sha` will be the `.g1` verifier's head).
- **Diagnosis of the macOS failures (principal reading, for the fix task to confirm, not assume).** The bindings are `Mod-z`, `Mod-Z`, `Mod-/` (`packages/editor/src/store.ts:189–190`, `toggle.ts:55`); `packages/editor/src` contains no platform detection of its own. `prosemirror-keymap` and `@codemirror/view` resolve `Mod-` from `navigator.platform`: Meta on a Mac, Ctrl elsewhere; jsdom reports `MacIntel` on darwin. The five tests build events with `ctrlKey: true` only, so on a macOS runner the keymap expects `metaKey` and reports unhandled. Linux containers and Windows agree with the tests by accident of platform. This is the CLAUDE.md rule "never claim a macOS or Windows pass from inside the container" made concrete: the tests encode one platform's chord.

## Gotchas

- **The planning commit needs two fix tasks, both physically in PRD §8 and `ralph/tasks.json`, before `1.verify.g1`** (RUNNER-SPEC §2 rule (a); copy the shape of `1b1ddc2`: `docs/PRD.md` §8 rows, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`, a DECISIONS entry, a journal line, a lessons line; `1.10.r0a/b/c` rewired from `1.verifyh` to `1.verify.g1h`; generator and validator green — `bash ralph/test/run.sh`):
  1. **macOS keymap tests** (product test code, `packages/editor/test/store.test.ts`, `toggle.test.ts`): the task decides, with the log as its reproduction, whether the tests or the chord are wrong. The chord is right (§6.5 says Cmd on macOS, Ctrl elsewhere), so the tests must exercise the platform route the keymap library actually takes — both routes, presence and absence (a `ctrlKey` event on a Mac platform is **not** handled; a `metaKey` event on a non-Mac platform is **not** handled), by injecting the platform rather than reading the host's. No `.skip`, no literal platform list copied from one run. One test per guard, enumerated from the diff (CLAUDE.md).
  2. **Playwright in CI** (`.github/workflows/ci.yml`, no manifest change): per #019 — install the browser per OS (`--with-deps` on Linux only), run every spec file under `e2e/web/` against the Vite `webServer` on all three runners, fail the job on any failing spec, upload a merged `playwright-report`; acceptance (a) no literal spec count, (b) states that a green `1.verify.g1h` proves the suite ran on all three runners at the integrated SHA. Windows: the Vite dev server and `pnpm --filter` spellings in `playwright.config.ts` (`cwd: repoRoot`) have never run there; the task text should name the Windows log as the first thing to read on a failure.
  Then **`1.verify.g1`** (`needsCI`, same `ci` object, `refTemplate ci/1.verify.g1/a{n}`, `artifactNames` unchanged, `ciAcceptance` unchanged), depending on the second fix task; generated `1.verify.g1h` replaces `1.verifyh` in `1.10.r0a/b/c`.
- Runner commits are forbidden on `phase/1` from now until `1.close` (rotate.md step 5, deviation 29): the planning commit goes on `plan/1.verifyh/r0` via `ralph/ralph.sh plan`, integrated by the runner; anything else the principal writes goes on `handoff/NNN`.
- `e2e/web` in CI: the CLI is only in `e2e/web/node_modules/.bin` (`pnpm --filter @essaydown/e2e-web exec playwright …` is the workspace spelling); the browser cache is per runner, so install it in the job. The 1.4/1.7/1.8 lessons hold the container spellings.
- Michael's `bbedit-no-bullet-points-example-file.md` is untracked in the repo root; it must not be staged by anything (a planning commit stages named paths only). Ask him to move it out.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael. Then `ralph/ralph.sh plan plan.1.verifyh.r0` and, in `.wt/plan.1.verifyh.r0` on `plan/1.verifyh/r0`, the planning commit of Gotchas, first bullet, starting with `git cherry-pick handoff/010` is **not** done here (the reconciliation does that); the planning commit touches only the files `1b1ddc2` touched. Commit `wip(plan.1.verifyh.r0): …` with `<promise>DONE plan.1.verifyh.r0</promise>`; restart the runner; confirm the planning commit integrates and `1.verifyh` reads `superseded`.
2. Fix tasks run (three attempts each before `STUCK`; re-cut per #016). Before `1.verify.g1` starts, nothing to do; at `HUMAN_GATE 1.verify.g1h`: `cd ~/Developer/essaydown && scripts/gate.sh 1.verify.g1h`; on a second failure read the failing job's step from `.evidence/ci/1.verify.g1h/a1/workflow.log` first (`gh run view <id> --json jobs` names the step).
3. On `ACCEPT`: rotate again if context is past half (handoff 011 on `handoff/011`); otherwise continue to `1.10.r0a/b/c` (`.evidence/reviews/1/r0/verifier_id` reads `1.verify.g1`; `docker top` of the claude-review container shows `--max-turns 160`) → `PRINCIPAL 1.10.r0d` run by a fresh session: reconcile in `.wt/1.10.r0d` per the r2 pattern (`052e206`), `git cherry-pick handoff/010` (and `handoff/011` if any) first; copy the 1.9 `payload` and the `a2.md` record body to `DECISIONS.md#002-outline-feel`; dispose of the `&#x20;` trailing-space defect (PRD §6) with a fix task; re-point #review-0-r2 H2; `verdict` first in `#review-1-r0`; `docs/progress.md` regenerated; `bash ralph/test/run.sh` green; `<promise>DONE 1.10.r0d</promise>`; restart → `1.close` → `/rotate` → `git branch -D handoff/010` (and `handoff/011`).
4. Next `chore(0.0)` idle-window commit (phase boundary): rotate.md step 5 third case + stale `description` line (009 Corrections).

## Open Questions

- Whether the macOS failure is only the tests (expected) or also the chord on a real Mac (the `pnpm dev` observation at 1.9 was on Michael's Mac and `Cmd+/` worked, so the chord is right; the fix task confirms from the log, not from this note).
- What the Windows runner says about the Vite `webServer` and Playwright, unknown until the `.g1` round.
- Which Phase 2/3 task first calls `writeFrontMatter` (H2 re-pointing), decided at `1.10.r0d`.
