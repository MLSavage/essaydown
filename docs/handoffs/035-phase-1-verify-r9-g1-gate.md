# Handoff 035 — Phase 1 at the r9 repair gate: `1.66` passed at `61fc197` (attempt 3), `1.verify.r9.g1` passed at `287c884` (attempt 1), `HUMAN_GATE 1.verify.r9.g1h` open for Michael; the handoff stack is seven commits on `handoff/035`

Written 2026-09-23 by the principal (the session that took `1.66` through its `NO-JOURNAL` to its pass and followed `1.verify.r9.g1` to the gate; Claude Code on the Mac Mini), at the rotation point a CI gate is (DECISIONS #015; the runner printed `ROTATE-PRINCIPAL`). Supersedes `034-phase-1-r9-gate-repair-1.66.md` (on `handoff/034`, not in the tree), and through it 033, 032, 031 and the in-tree `030-phase-1-review-r8-reconciliation.md`. This commit is on `handoff/035`, stacked on `handoff/034`; the `1.10.r9d` reconciliation cherry-picks `phase/1..handoff/035` as one range — seven commits, `docs/handoffs/**` and `docs/DECISIONS.md` (#038, #040) only. Next: `036-*.md`.

## Current State

- **`phase/1` tip `287c884`** (`task(1.verify.r9.g1)`); `main` at `052e206`; `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **`1.66` passed at `61fc197`, attempt 3** (sonnet). Attempts 1 and 2 capped at 51 turns with the whole deliverable built (a1: 54 tool calls — 10 orientation and stub, 12 config reads, 32 edits, swept to `d4dd484`; a2: 57 calls — 20 re-reading, 4 a first mutation edit undone, 4 finding `node_modules` absent and installing, 9 reporter timings and the shift-key fix, 6 the mutation and its revert, 11 the backlog line, 3 lint/test/cargo all green, swept to `e829ba3`); a2 ended `NO-JOURNAL`. One principal lesson (`chore(1.66)` `4b75c43`, `docs/lessons.md` only, the #028/#038 pattern, squashed into `61fc197`) named the remaining step; a3 passed in 31 turns. Its journal: Test Files 45, Tests 4572, cargo ok ×3, e2e/web 102 passed over 31 specs, the essay-fixture leg 159 ms in the container, the mutation 14 failed (13 fixtures and the summary test), `blockAlone(` 2 → 1 in position-map-inverse.test.ts (the removed definition line; the acceptance's `it` count 15 → 15 binds).
- **`1.verify.r9.g1` passed at `287c884`, attempt 1** (sonnet, 29 turns, `stop-check: GREEN`).
- **Gate `1.verify.r9.g1h` human-pending, attempts 0.** Michael: `scripts/gate.sh 1.verify.r9.g1h` (a CI gate: no `--outcome`, no note; his `gh` login; it pushes `287c884` to `ci/1.verify.r9.g1/a1`, waits for `ci.yml` on three OSes, fetches `test-logs` and `playwright-report`, writes `accepted.json` on ACCEPT; from a session it is backgrounded and its output file read).
- **Handoff stack**: `handoff/035` = `handoff/034` + this commit; `handoff/034` sits on `2507210`. `phase/1` moved two task commits past that base (`61fc197`, `287c884`) and neither touches `docs/DECISIONS.md` (`git diff --stat 2507210 phase/1 -- docs/DECISIONS.md` is empty), so no rebase is needed: `git log --oneline phase/1..handoff/035 | wc -l` prints 7. `git branch --list 'handoff/03*'` prints `handoff/030` (a stale ref — 030 is in the tree), `handoff/033`, `handoff/034`, `handoff/035`.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, at the prompt after `HUMAN_GATE 1.verify.r9.g1h` / `ROTATE-PRINCIPAL` (runner.log 141,041 lines at writing). `.locks/` empty, no `ralph run` process, no container. Restart after ACCEPT: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped; **a fresh one after every restart or `retry`**; `ralph/ralph.sh status` on every expiry notice — a line in the expiry's second is lost):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `[ralph] 1.verify.r9.g1 attempt 1` (the last `[ralph]` line); after the restart the next is `[ralph] 1.10.r9a attempt 1`.
- **Expected next stop**: the gate's outcome. ACCEPT → restart → the review set `1.10.r9a/b/c` (Claude, Sol, Grok in parallel) → `PRINCIPAL 1.10.r9d` (rotate; a fresh session reconciles). GATE-FAILED → `gh run view <run id> --log-failed` first, then a planning commit `plan.1.verify.r9.g1h.r0`.

## Corrections

- `034` Current State, "`1.66` running, attempt 1": passed at attempt 3 (`61fc197`).
- `034` Gotchas and `next-prompt.md` job 2, "on `STUCK 1.66` … `ralph/ralph.sh retry 1.66`": the stop was `NO-JOURNAL`, which leaves the task `running` with its attempt count kept (audit: `running -> running (no journal entry)`), and `retry` refuses a `running` task (`ralph/lib/doctor.mjs:203`) — the procedure was the lesson commit and a restart; no log was moved aside. The source-diff check was run and empty.
- `034` Current State and `next-prompt.md` job 1, "`git branch --list 'handoff/03*'` (033 and 034 only)": `handoff/030` also exists (stale ref; its file is in the tree); the boundary delete covers it.
- `034` Next Steps 3, "`git branch -D handoff/010 … handoff/030 handoff/033 handoff/034`": add `handoff/035`.
- `034` Current State, "the reconciliation cherry-picks `phase/1..handoff/034` (six commits)": `phase/1..handoff/035`, seven commits.

## Decisions

- **No DECISIONS entry this block.** No runner deviation: no `admin`, no `fix(runner)`, no `retry`, no `docker/` change, no 429 (the three `rate_limit_event`s in `1.66`'s a2 transcript read `allowed`, five-hour utilisation 15–17%).
- **One principal commit**: `chore(1.66): principal lesson — the remaining step after two capped attempts` (`4b75c43` on `task/1.66`, `docs/lessons.md` only, reversal in the message; squashed into `61fc197`). It handed a3 the numbers already read from the capped transcripts and named each remaining part as one foreground call; a3 used 31 turns.
- **Runner fact read, not changed**: `NO-JOURNAL` (run.mjs:127 `stuckOr`) counts the attempt and keeps the task `running`; only the third counted attempt flips it to `blocked` (`STUCK`), which is where `retry` and the #027 log move-aside apply.
- **Turn-budget evidence** (conditional on Michael's OK — unchanged as a decision, #review-1-r1): `1.66` adds two caps (a1 51, a2 51) and a 31-turn pass with the work carried over. The `[review-1-r9, turn budget]` evidence line for the reconciliation: `1.64`'s six caps, `1.63`'s and `1.65`'s a1 caps, `1.66`'s two.
- **Rules in force**: as 033/034 (the three r9 promotions included). Nothing new promoted.

## Gotchas

- **The gate `1.verify.r9.g1h`** is a CI gate like `1.verify.r9h`. On GATE-FAILED the gate fetches no artifacts (#review-1-r3 I7): `gh run view <id> --log-failed` first; a same-SHA rerun (`scripts/gate.sh rerun 1.verify.r9.g1h`) only for a transient failure (#023, #029); any tree change is a `1.verify.r9.g2` repair through `ralph/ralph.sh plan plan.1.verify.r9.g1h.r0` on its `plan/1.verify.r9.g1h/r0` branch (#037 and #039 are the pattern).
- **A fresh container has no `node_modules`**: every `1.64` and `1.66` attempt ran `pnpm install --frozen-lockfile` (10 s) before its first vitest; a lesson that enumerates remaining steps names it, or the agent spends four calls discovering it.
- **A description clause that counts a definition line** (`1.66`'s "`grep -c 'blockAlone('` unchanged") disagrees with its own "`function blockAlone` 0 after"; the acceptance binds and the journal records both numbers. Read the acceptance greps against the tree before writing a task's counting clauses.
- **Reading a capped transcript**: `node` over the stream-json (tool_use blocks counted per assistant message; the `result` line's `num_turns` and `subtype`) gives the per-part turn cost #038 asks for; the host `grep` is ugrep and the runner.log echoes every transcript line, so grep runner.log only on `^[A-Z-]+ <id>` and `^\[ralph\]`, never on a word.
- **Worktrees**: `.wt/handoff-035` is removed by this rotation; the prior session's scratch worktree at `/private/tmp/claude-501/-Users-mlsavage-Developer-essaydown/b6d390f4-…/scratchpad/970bb5c` (detached, `node_modules`) is still registered — throwaway; `git worktree remove --force` it at the boundary.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `ralph/ralph.sh status` says "running attempt 2" while the log shows attempt 3: read the log. A `tail -3` on a test runner's output loses its summary: capture to a file and grep.

## Next Steps

1. **The gate**: Michael runs `scripts/gate.sh 1.verify.r9.g1h`. On ACCEPT: idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the pane at the prompt, `doctor` clean), the restart line, a fresh watcher; the review set `1.10.r9a/b/c` (a Grok 402 is never retried without Michael, #027; a single-reviewer retry follows #025; never reconfigure a reviewer inside the set; count before raising a pattern) to `PRINCIPAL 1.10.r9d` — rotate there. On GATE-FAILED: the Gotchas procedure, then rotate with the planning commit written.
2. **The reconciliation `1.10.r9d`** in a fresh session (#015): the FAIL pattern `task(1.10.r8d)` at `3c96b9b` with #review-1-r8, the PASS pattern `task(0.12.r2d)` at `052e206` with #review-0-r2; the implementation SHA is `287c884`; it cherry-picks `phase/1..handoff/035` (seven commits, no DECISIONS tail conflict), regenerates `docs/progress.md`, appends `1.9.r1` (#040: at the r9 implementation SHA, `1.9`'s procedure plus the `[repair-1.9 (c)]` two readers, `1.close` depending on it beside `1.10.r9d`, `EXPECTED_COUNT` +1), the `[review-1-r9, turn budget]` evidence line, the reviewer record; then `1.close` waits on `1.9.r1` on a PASS, or the r10 chain on a FAIL.
3. **After `1.close`** (host checkout on `phase/2`, runner idle): 034's Next Steps 3 unchanged — the `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` with its conformance test, the recovery-commit guard, the gate fetch-on-failure I7 ×2), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r9 principal lessons, Claude r8 nit 3 (`ci.yml` `if: always()`), React 19's ratification (Michael's OK), the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/030 handoff/033 handoff/034 handoff/035`; the scratch worktree; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 … #review-1-r8, handoffs 031–035, #038), the 429 signal (#027), the reviewer-prompt option, Grok's read-only mode.

## Open Questions

- Whether the block-local leg holds on the three runners at `1.verify.r9.g1h` (159 ms in the container against the family's 30 s).
- The r9 review verdict: a PASS leads to `1.9.r1` then `1.close`; a FAIL opens the r10 chain.
- The cap raise, the 429 signal, Grok's mode, React 19: Michael's calls, evidence unchanged in kind.
