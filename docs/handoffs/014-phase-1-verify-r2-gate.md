# Handoff 014 — Phase 1 at `HUMAN_GATE 1.verify.r2h`: r2 fix chain 1.23–1.27 and `1.verify.r2` integrated, CI gate open

Written 2026-09-10 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `HUMAN_GATE 1.verify.r2h` stop signal (the verifier-gate rotation of DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `013-phase-1-review-r1-reconciliation-pending.md` (in the `phase/1` tree since `b149f0c`, the r1 reconciliation, which cherry-picked 012 and 013). This commit is on `handoff/014` (rotate.md step 5: a reconciliation `1.10.r2d` is pending, so never on `phase/1`); the `1.10.r2d` reconciliation cherry-picks `handoff/014`. Next: `015-*.md`.

## Current State

- **`phase/1` tip `6d9345c`** (`task(1.verify.r2)`, attempt 1); 38 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`), 31 of them `task(1.*)`. No commit on `phase/1` by the principal since `b149f0c` (the reconciliation, integrated by the runner) and none until `1.close`. `main` unchanged at `052e206`. `origin/phase/1` is at `4590b8d` (Michael pushes by hand, #010); `git ls-remote origin 'ci/*'` at rotation shows only `ci/1.verify/a1` at `3e98707` (stale; `gate.sh gc` at the phase boundary) — `ci/1.verify.r1/a1` is no longer listed; the gate will push `ci/1.verify.r2/a1`.
- **The r1 reconciliation** integrated as `task(1.10.r1d)` at `b149f0c`: verdict FAIL (`.evidence/reviews/1/r1/verdict`), DECISIONS `#review-1-r1` (G1 blocker + G2–G6 required + G7 deferred + G8–G12 nits; the reviewer table; Michael's four decisions with their reasoning), 170 → 181 tasks, `1.close` → `1.10.r2d`.
- **The r2 fix chain**, all passed (`.evidence/state/tasks.json`): `1.23` at attempt 2 (`7b0d052`; a1 capped at the turn limit with the journal written, recorded as "no wip commit", no stop signal, the loop continued by itself), `1.24` at 1 (`e038619`), `1.25` at 2 (`590e484`; a1 `NO-JOURNAL` at the cap, one manual restart, a1's checkpoint carried a live scratch mutation that a2 found and reverted), `1.26` at 1 (`1973174`), `1.27` at 1 (`88752d9`), `1.verify.r2` at 1 (`6d9345c`: the store-reader rule appended to `## Code rules` and the six-step `## Every iteration` order with the journal stub, in CLAUDE.md and AGENTS.md, `check-agent-rules` OK).
- **Gate `1.verify.r2h`**: `human-pending`, attempts 0, no `.evidence/ci/1.verify.r2h/` yet. Command for Michael: `scripts/gate.sh 1.verify.r2h` (pushes `ci/1.verify.r2/a1` at `6d9345c`, waits for `ci.yml`, fetches `test-logs` and `playwright-report`, asserts three OS directories in each). Then `1.10.r2a/b/c` run when the gate is `ACCEPT`.
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r2h` / `ROTATE-PRINCIPAL` (log line 59,963–59,964; log at 59,964 lines). `.locks/` empty, no `ralph run` process, no containers, `.wt/` empty, `doctor` clean, host checkout on `phase/1` at `6d9345c`, tree clean. Restart after the gate is accepted: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r2h`. The gate outcome is not in `runner.log`: a second `Monitor` polls `.evidence/ci/1.verify.r2h/{accepted,rejected}.json` and exits on the first hit (013 Decisions).
- **Expected next stop**: `ACCEPT` → restart → the three reviewer containers (no `[ralph] <id> attempt` line for a review set; evidence is `docker ps` and `.evidence/reviews/1/r2/{verifier_id,verification_sha,implementation_sha}`) → `PRINCIPAL 1.10.r2d` (a fresh session, #015). `GATE-FAILED` → `PLAN-GATE` → a planning commit per DECISIONS #019/#020's pattern.

## Corrections

- `013` Gotchas said the turn-budget knob is `docker/compose.yml` `x-env` and that a raise means an image rebuild. Wrong: `ralph/lib/run.mjs:108` sets `maxTurns` and always passes `RALPH_MAX_TURNS`, so the compose default is a dead fallback; a raise is a `fix(runner)` commit with its conformance leg and no rebuild. Recorded at `#review-1-r1`.
- `013` Corrections said the ten multi-attempt tasks each had their implementation committed at the cap. True, but by the runner, not the agent: every capped attempt made zero `git commit` calls; `stepLoop`'s recovery commit (`run.mjs:119`) checkpointed the dirty tree.
- `013` Decisions gave the r1 unique-finding count from the headings; the reconciliation re-derived it from the bodies and it held (Claude 6 of 9, Sol 2 of 4, Grok 1 of 4). The cumulative low-rater count the principal first gave Michael in the r1 session (Sol 1) was wrong: Sol's should-fix on G1 is below Claude's blocker, so it is Grok 4, Claude 2, Sol 2 over six disagreements; `#review-1-r1` carries the table.
- `013` Gotchas expected the fix chain's capped attempts to stop the runner (`NO-JOURNAL`). Only `1.25`'s did; `1.23`'s a1 had written its journal line before the cap, so the loop continued on its own (`run.mjs:147`, "no DONE promise"). That is the behaviour the journal-stub rule now makes the default.

## Decisions

- **`#review-1-r1`** (in `b149f0c`): verdict FAIL; findings G1–G12; each r0 disposition F1–F16 confirmed or reopened (F1 → G4 family, F9 → G5 sibling); the reviewer table; Michael's decisions — the r2 chain at 50 turns as five single-concern tasks; the raise **conditional, not approved**, proposed at 80 from three capped transcripts (`/logs/tasks/{1.14,1.17,1.21}/1.log`: 54–67 tool uses at `num_turns` 51, all in the verification tail, against 52–56 for the attempts that finished; the journal's self-reported tool-call counts undercount the transcript by about 2×); the journal stub before implementing, promoted via `1.verify.r2`; the 25× factor kept with the reasoning (2× environment spread against a 2.5× tightening); one severity-definition paragraph in `docs/reviews/TEMPLATE.md` (one defect drew blocker / should-fix / nit); Grok's inputs untouched; Michael's 1.19 note recorded there because `#review-1-r0` is a passed record; PRD §4 names `serde_json` and `tauri-build` (G12); RUNNER-SPEC §10 gains the stub sentence.
- **No runner deviation this block.** One `NO-JOURNAL` restart (1.25), same run command, idle conditions checked first. No re-cut, no `retry`, no admin command, no `docker/` change (the image is current for the r2 reviewers).
- **r2 data for the conditional raise, read at rotation:** two capped first attempts in five fix tasks (1.23 sonnet at 50 tool uses, 1.25 opus at 56), both in the verification tail, both checkpointed; the other three passed at attempt 1 (1.24 ~30 self-reported tool calls, 1.26, 1.27 ~20). The proposal and its condition stand as written in `#review-1-r1`; the next reconciliation adds these two transcripts to the evidence before Michael decides at the Phase 2 boundary.

## Gotchas

- **Rotation rule at this gate**: this file is on `handoff/014`; the `1.10.r2d` reconciliation starts with `git cherry-pick handoff/014` into `.wt/1.10.r2d` (only 014 this time; 012 and 013 are already in the tree at `b149f0c`), then reads `.evidence/reviews/1/r2/{claude,sol,grok}/report.md` in full, copies them to `docs/reviews/phase-1-r2-{claude,sol,grok}.md`, writes `DECISIONS.md#review-1-r2` with `verdict: PASS|FAIL` as its first line, confirms or reopens every r1 disposition G1–G12 and every r0 one F1–F16, and counts the reviewer table from the reports. On PASS it regenerates `docs/progress.md` and `1.close` runs on restart; on FAIL it appends `1.28…` + `1.verify.r3` + `1.10.r3a/b/c/d` the way `b149f0c` did (`git show --stat b149f0c`).
- **Two hygiene items for `#review-1-r2`, not defects:** `1.25`'s journal entry is 19 lines (indented paragraphs) where the template is one line, so `summary.md` and `progress.md`'s "last 5" show only its first line — a note, not an edit (passed task); and a capped attempt can freeze a scratch mutation into the checkpoint (1.25 a1 left `// SCRATCH MUTATION B` live in `schema.ts`, a2 found it) — a lesson for the next verifier's promote-lessons: revert scratch edits before any other step, or make the mutation on a copy.
- **Gate watch**: `scripts/gate.sh 1.verify.r2h` prints `ROTATE-PRINCIPAL` after recording (013 Corrections: no second rotation is needed). Arm the `accepted.json`/`rejected.json` monitor with `setopt nullglob` (013 Decisions).
- **After `ACCEPT`**: the three reviewer containers run ~18 min (r1: 04:40 → 04:58); Claude and Grok get `--max-turns 160`. `PRINCIPAL 1.10.r2d` is the next stop, run by a fresh session (#015): rotate there without opening `.wt/1.10.r2d`.
- **Phase boundary items** (unchanged from 013 Next Steps 4): `chore(0.0)` for rotate.md step 5's third case and the stale `description` line (009 Corrections); `gate.sh gc` for the stale `ci/1.verify/a1`; the `phase/1` push (#010); `git branch -D handoff/010 handoff/011 handoff/012 handoff/013 handoff/014`; the conditional turn-budget `fix(runner)` on Michael's OK; the stated-invariant sweep and the coverage include are Phase 2 planning items (backlog).
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers. A `sed -i` over the journal or lessons must target only this task's own line: a global substitution touched an old entry once this session and was reverted before the commit.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm both watchers; one-line state to Michael; hand him `scripts/gate.sh 1.verify.r2h` (no placeholder; the id is the whole argument).
2. On `ACCEPT`: `.locks/` empty, no `ralph run` process, no containers, pane at the prompt → the restart command above → watch for the three containers and `.evidence/reviews/1/r2/` → `PRINCIPAL 1.10.r2d` → `/rotate` there regardless of context (handoff 015 on `handoff/015`); the reconciliation is the session after that.
3. On `GATE-FAILED`: read `.evidence/ci/1.verify.r2h/a1/workflow.log` for the red leg, then a planning commit per #019/#020 (repair task + `1.verify.r2.g1`), on the request's branch, never on `phase/1`.
4. Phase boundary (after `1.close`, on `phase/2` from the host checkout, runner idle): the items under Gotchas.

## Open Questions

- Whether the r2 reviewers, with the severity paragraph in TEMPLATE.md, still spread three ways on any one defect — the backlog's reviewer-prompt trigger reads that.
- The turn-budget number: 80 proposed; the r2 chain added two capped attempts at 50 and 56 tool uses; Michael decides at the Phase 2 boundary with the transcript measure.
- Whether `1.25`'s multi-line journal entry needs a one-line rule in `docs/progress.md`'s template (a Phase 2 planning nit, not a fix task).
