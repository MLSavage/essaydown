# Handoff 005 — Phase 0 fix chain running (review r0 reconciled FAIL; 0.14 passed, 0.15 in flight; next gate 0.verify.r1h)

Written 2026-09-06 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at Michael's instruction to rotate before 0.16 (the previous late-window session produced `c8b234a`, DECISIONS #017). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `004-phase-0-verify-gate.md`; `002-phase-0-running.md` stays the reference for the gate evidence pattern, deviations 1–18 and the reconciliation checklist (its Next Steps 4). Next: `006-*.md`.

## Current State

- `phase/0` tip `b86edca` (`task(0.14)`) on `c0f3c98` (`task(0.12.r0d)`, the r0 reconciliation, `reconciliation_sha` of attempt r0) on `81273f5` (`task(0.verify.g1)`) on `13588ee` (`task(0.13)`) on `1b1ddc2` (`plan(plan.0.verifyh.r0)`) on `3ad8544` (handoff 004) on `f8c91c5` (`task(0.verify)`). Passed: 0.0–0.11, gate 0.2h (ACCEPT a1), 0.verify, 0.13, 0.verify.g1, gate 0.verify.g1h (ACCEPT a1, run 34042388223), 0.12.r0a/b/c, 0.12.r0d, 0.14 — every one of 0.13, 0.verify.g1, 0.14 at attempt 1. Superseded: 0.verifyh (GATE-FAILED a1, run 34037147355, Windows CRLF). Running: **0.15** (Opus, attempt 1, started 16:12:35Z). Pending chain: 0.16 → 0.17 → 0.18 → 0.19 → 0.20 → 0.21 → 0.22 → 0.verify.r1 → gate 0.verify.r1h → 0.12.r1a/b/c → 0.12.r1d → 0.close. Graph: 140 tasks (`EXPECTED_COUNT`), `doctor` clean, `main` untouched at the seed `660f3aa`. `.evidence/reviews/0/r0/verdict` = `FAIL`.
- **Runner**: tmux session `essaydown`, window 1 `runner`, pane pid 8940 (`node` while a run is live; `zsh` when it has exited at a stop signal). Restart: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter`. Attach from a phone: `tmux attach -t essaydown`, `Ctrl-b 1`. Exactly one runner process at any time (DECISIONS #review-0-r0 F14): `tmux capture-pane -p -t essaydown:runner | tail -3` must show the shell prompt before a restart.
- **Watcher** (background Bash, `run_in_background`, timeout 3600000; one per stop signal; none running after this rotation). Arm it from the **current end of the log** — never with a negative offset (the 004 variant with `start-3` tripped on an old `HUMAN_GATE` line this session):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); until tail -n +"$start" .evidence/runner.log | grep -qE "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH) |<promise>COMPLETE"; do sleep 60; done; tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|ROTATE-PRINCIPAL) |^ROTATE-PRINCIPAL$|^\[ralph\] " | tail -14; tail -6 .evidence/state/audit.log | cut -c1-200
  ```
  The current marker line is `[ralph] 0.15 attempt 1 (loop, opus, max-turns 50)`. Read an attempt's outcome from the transcript's final `result` event (`.evidence/tasks/<id>/<n>.log`: `subtype`, `num_turns`, `is_error`, `result`); a rate-limit banner → read the last `rate_limit_event` in `runner.log` (`resetsAt`, `utilization`).
- **Expected next stop**: `HUMAN_GATE 0.verify.r1h` after eight more integrations (0.15–0.22, 0.verify.r1; ~10 min per Sonnet task, 10–30 per Opus task at attempt 1); or `STUCK`/`NO-JOURNAL` on an Opus task (0.15, 0.18, 0.19, 0.20 are Opus). `ROTATE-PRINCIPAL` follows that gate from `gate.sh`, and from the runner too once 0.18 has landed (F7).

## Corrections

- `004-phase-0-verify-gate.md` Current State "Expected next stop" and Next Steps 3–4: the gate returned GATE-FAILED (Windows CRLF, run 34037147355), the repair chain 0.13 → 0.verify.g1 → 0.verify.g1h ACCEPT ran, the review set ran, and the reconciliation's verdict is **FAIL**, so `0.close` does not follow `0.12.r0d`; the path is the fix chain above, then attempt r1. 004's "no principal commits on `phase/0` after `0.12.r0d` integrates" now reads **after `0.12.r1d` integrates** (RUNNER-SPEC §8.1 compares `phase/0` head with the newest attempt's `reconciliation_sha`); this rotation's commit is before that point and is on the allowlist regardless (`docs/**`).
- The previous `next-prompt.md` (steps 2–6) is entirely stale; this file's Next Steps replace it.
- `004` Gotchas: "`scripts/gate.sh 0.verifyh` prints `ROTATE-PRINCIPAL`" — the runner itself did **not** print it for `0.verify.g1h` (`isVerifyGate` matches only `N.verifyh`; F7, fixed by 0.18); only `gate.sh`'s count showed it.
- `002` Next Steps 4 "hand the reviewers this list of deviations": the r0 reviewers were not handed the list explicitly (they read the handoffs from the tree); 24 deviations are now disposed in DECISIONS #review-0-r0, so r1 reviewers read that entry instead.
- DECISIONS #review-0-r0 F20 (the `zod` row) reads as if nothing was skipped. Michael's ruling (2026-09-06, relayed to this session, not yet in DECISIONS): 0.10 installed a runtime dependency not in PRD §4 while CLAUDE.md required a DECISIONS note and his OK; neither existed; he **ratifies it after the fact** because it is plainly the plan's intent (PRD §6.2, tasks 0.10 and 2.7 name it; `zod@^4.5.4` is a direct dependency of packages/core in the lockfile; `zod-validation-error` is transitive only). Record that sentence in the next entry that touches #review-0-r0 (the r1 reconciliation), plus the lesson below. Never edit the r0 entry.

## Decisions

- **DECISIONS #018-gate-0.verifyh-line-endings** (`1b1ddc2`): windows-latest converted LF to CRLF at checkout (no `.gitattributes` policy); scope choice repo-wide `* text=auto eol=lf` + `-text` on the two fixture trees (Michael's guide, 80%); graph rule (a) → 0.13 + 0.verify.g1; **CI evidence path convention = the gate id** (`/logs/ci/<producer>h/…`); reviewer prompt fixed (`essaydown-common.sh`, `grok-review`); generator wording and `0.verify`'s ciAcceptance left (passed-task fingerprints). Deviation 23.
- **DECISIONS #review-0-r0** (`c0f3c98`): verdict FAIL. F1 blocker (sentence slicing loses/duplicates zero-width inline nodes; Claude + Sol reproduced) → 0.14 (passed). F2 blocker (front-matter writer over-permissive; Sol) → 0.15. Required should-fixes: F3 typecheck + coverage into the bar's `lint`/`test` scripts (0.16); F4 CI merge `separate-directories` + F5 conformance suite in CI (0.17); F6 review start consumes accepted.json + F7 `isVerifyGate` grammar (0.18); F8 doctor checks for principal deletions (#017 gap) and passed-with-debris (0.19); F9 `withLock` identity-based break + F10 `writeAtomic` fsync (0.20); F11 scaffold opener plugin + `greet` removed (0.21); F12 CRLF normalisation in `parse` + F13 two fixtures (0.22). Deferred to `docs/V1.1-BACKLOG.md`: F14 second-runner exposure (**manual procedure: one runner process**), F15 reviewer peer-read isolation (deviation 24), F16 structural sharing (Phase 3), F17 image staleness (**manual procedure: `docker compose build` after any `docker/` commit integrates, before the next container task** — done this session at 15:58Z for `1b1ddc2`), F18 generator wording. F19 `promote-lessons` cut to a manual step carried by the next verifier's task text (RUNNER-SPEC §5.5 reworded); two rules promoted via 0.verify.r1. F20 `zod` row in PRD §4 (see Corrections). F21 `cargo test` vacuous in Phase 0. #017 provenance corrected (guide session). RUNNER-SPEC §3/§5 evidence path aligned to the gate id. `EXPECTED_COUNT` 125 → 140.
- **Michael's `zod` ruling** (above) and its lesson, to be appended at the next stop signal that allows a lessons line, or by the r1 reconciliation: nothing checks "is this package in PRD §4?" at install time, which is why a reviewer found it three tasks later instead of the task at install; if that check belongs anywhere it is the existing bar scripts 0.17 already touches (Michael), not new machinery. Do **not** widen 0.17's text for it now; it is a backlog line and a reconciliation decision.
- Runner deviations since 004 (none adds machinery): (25) the runner prints `ROTATE-PRINCIPAL` only for `N.verifyh`, not for `.g<n>h`/`.r<k>h` gates (F7; 0.18 fixes it in code because it contradicts §11); (26) the review image is whatever was last built — a committed `docker/` change is not picked up until the principal rebuilds (F17, manual procedure); (27) `sync-state` after a planning commit rewrites `0.13`'s raw dependency `0.verify` to `0.verifyh` (a superseded gate), which counts as satisfied only because `state.mjs` treats `superseded` as passed for everyone — the spec says "only for the fix tasks the planning commit appended after it"; harmless so far, note for the r1 reviewers.
- Rotation while the runner is live: this handoff commit is made on the host checkout under the runner lock (the `/rotate` procedure), with `doctor` clean and the checkout at `phase/0` head, while 0.15 runs in its own worktree. It touches only `docs/handoffs/`, so 0.15's integration rebase cannot conflict. This is the one sanctioned exception to #017's "never commit on the host checkout while the loop is live".

## Gotchas

- **After 0.20 integrates, the rotate lock handshake changes shape**: `withLock` will write a token into `.locks/ralph` and release only its own. The `/rotate` procedure's `noclobber` echo of `$$` still blocks the runner correctly (a foreign, live lock is simply "held"), but read the new `withLock` in `ralph/lib/util.mjs` before the next rotation and confirm that a lock file without a token is treated as held, not stale; if it is broken as stale, rotate only with the runner exited at a stop signal.
- **After 0.19 integrates, `doctor` reports principal commits that delete paths** (`principal-commit-deletes`). Handoff commits add files only; never let a `chore(0.0)` commit delete anything.
- **After 0.18 integrates, review start refuses when `/logs/ci/0.verify.r1h/accepted.json` is missing or a digest differs.** That is the intended behaviour; it means `gate.sh 0.verify.r1h` must have ACCEPTed before the restart, which is already the order.
- 0.18–0.20 change the runner that the host runs: the host checkout is refreshed to `phase/0` after each integration, so the new code is live for the next task. `bash ralph/test/run.sh` on the host (about 90 s) is the check if anything looks off; 0.17 also puts it in CI.
- Opus tasks spend ~25 of 50 turns orienting; the steer that worked for 0.10 a3: "do not re-read or rewrite; suite from HEAD; fix only red; journal and commit by turn 20; then DONE". 0.14 (Opus) passed at attempt 1 in 9 minutes with the F-numbered task text, so the reconciliation's explicit scoping helps; keep steers on the task branch (`chore(<id>): principal lesson …`) or re-cut per #016.
- Five-hour subscription window is shared by every container attempt: before restarting after a `NO-JOURNAL`, read the last `rate_limit_event`; if `status` is `rejected`, wait for `resetsAt` + 30 s.
- The reviewers' r0 reports are in `docs/reviews/phase-0-r0-*.md`; the r1 task text tells the reviewers to verify each F disposition against the tree. Do not restate the reports to Michael; give him verdicts and counts.
- `RALPH_CI_ADAPTER=fake` is the test gate; `docker compose run` needs `ESSAYDOWN_ROOT`; never start the runner with a cherry-pick unresolved; the host-checkout rule (#017) stands outside the rotate procedure.

## Next Steps

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, the last `^\[ralph\]` line and any stop signal; one-line state to Michael; arm the watcher (command above, from the log's current end).
2. Restart the runner after every stop signal (one process; check the pane first). `STUCK`/`NO-JOURNAL`/`NO-COMMIT`: read the transcript's `result`, name the cause in one line, steer via a lesson line on the task's own branch or re-cut per #016, `ralph/ralph.sh retry <id>` if blocked, restart. A retried agent ending without DONE and citing a conflict the lessons channel was meant to resolve → no third attempt: write the planning commit appending the fix task it asks for (Michael's standing instruction).
3. `HUMAN_GATE 0.verify.r1h` → `ROTATE-PRINCIPAL` (DECISIONS #015): `/rotate` first (handoff 006), then the fresh session gives Michael `cd ~/Developer/essaydown && scripts/gate.sh 0.verify.r1h` (pushes `ci/0.verify.r1/a1`; ACCEPT requires the merged `test-logs` artifact to hold the three OS directories, 0.verify.r1's ciAcceptance). GATE-FAILED → `ralph/ralph.sh plan plan.0.verify.r1h.r0` → planning commit with `0.verify.r1.g1` + gate (RUNNER-SPEC §2 rule (a); reviewers 0.12.r1a/b/c rewired), `<promise>DONE plan.0.verify.r1h.r0</promise>`, restart.
4. On ACCEPT: restart → 0.12.r1a/b/c (three containers; `.evidence/reviews/0/r1/`; `verifier_id` must read `0.verify.r1`, both SHAs equal) → `PRINCIPAL 0.12.r1d`: reconcile in `.wt/0.12.r1d` per 002 Next Steps 4, r1-specific: reports to `docs/reviews/phase-0-r1-*.md`; `DECISIONS.md#review-0-r1` with `verdict: PASS|FAIL` as the first `verdict:` text in the entry (the runner takes the first match); each r0 disposition confirmed or reopened; Michael's `zod` ratification sentence and the install-time lesson recorded there; deviation 27 disposed; `docs/progress.md` regenerated; on FAIL the fix tasks + `0.verify.r2` + `0.12.r2a/b/c/d` written physically, close rewired to `0.12.r2d`; on PASS nothing else changes and the diff since `implementation_sha` stays within `docs/**`, `DECISIONS.md`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`. Journal line; `wip(0.12.r1d): …` with `<promise>DONE 0.12.r1d</promise>`; restart → `0.close` (moves `main`, creates `phase/1`). **No principal commits on `phase/0` after `0.12.r1d` integrates.**
5. Housekeeping (successor, at any stop signal with the runner idle): the orphan ref `ci/0.verify/a1` is still on the remote at `f8c91c5` (`git ls-remote --refs origin 'refs/heads/ci/*'`). Run `scripts/gate.sh gc`. RUNNER-SPEC §2 says gc deletes only refs whose attempt is `accepted`, `rejected` or `abandoned`; this attempt is GATE-FAILED. If gc refuses it, that is a spec wording gap: record the reading in DECISIONS at the next allowed moment (GATE-FAILED ≡ rejected for gc purposes, or the ref stays until the phase closes) — do not patch gc.
6. After `0.close`: `/rotate` (handoff for Phase 1; the runner is then started with `--phase 1`; `docker compose build` is not needed unless a Phase 1 task touches `docker/`).

## Open Questions

- Whether `gate.sh gc` treats a GATE-FAILED attempt as `rejected` (Next Steps 5).
- Whether 50 turns is enough for the Opus runner tasks 0.18–0.20 (each adds conformance tests to a 90-second suite); decide from evidence in DECISIONS, not by adding machinery.
- Grok headless in the container: worked at r0 (report in 12 minutes, read-only, cited the gate-id path); the RUNNER-SPEC §2 host fallback was not needed. Ollama for 5.3b remains deferred.
