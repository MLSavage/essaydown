# Handoff 022 — Phase 1 at `PRINCIPAL 1.10.r4d`: gate `1.verify.r4.g1h` accepted at a1, r4 reviewers done (Claude FAIL, Sol FAIL, Grok FAIL but derived — #025), reconciliation pending

Written 2026-09-12 by the principal (the session that ran the `1.10.r3d` reconciliation, the r4 chain, the `#024` planning commit, and watched the r4 review set; Claude Code, Remote Control session on the Mac Mini), at the `PRINCIPAL 1.10.r4d` stop signal (rotate regardless of context; the reconciliation is a fresh session, DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `021-phase-1-verify-r4-g1-gate.md` (on `handoff/021`) and `020-phase-1-verify-r4-gate.md` (on `handoff/020`); 019 and earlier are in the tree since `ede7729`. This commit is on `handoff/022`, which already carries `add1a48` (`docs(0.0): DECISIONS #025`); the `1.10.r4d` reconciliation cherry-picks `handoff/020`, `handoff/021` and both commits of `handoff/022`. Next: `023-*.md`.

## Current State

- **`phase/1` tip `6c0e601`** (`task(1.verify.r4.g1)`), unchanged since handoff 021; 60 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean. `.wt/1.10.r4d` on `task/1.10.r4d` at `6c0e601`, clean, waiting for the reconciliation.
- **Gate `1.verify.r4.g1h`: ACCEPT at a1** (run 34687540740; 1.39's anchor-and-count route landed right on ubuntu, macos and windows; three OS directories in both artifacts).
- **Review attempt r4**: `1.10.r4a/b/c` started together 10:13:49Z from `6c0e601`; Claude and Sol passed 10:48:13Z; Grok's run died on `402 Payment Required: Grok Build usage balance exhausted` after 13 model calls → `STUCK 1.10.r4`. Michael reset the usage (his one reset; **he will not pay for Grok; on a recurrence he checks the usage and decides**). Retry procedure (Grok's a1 files moved aside as `report.a1-402.md` / `transcript.a1-402.log`, `ralph.sh retry 1.10.r4c`, restart): Grok passed at 11:14:56Z after 10 minutes — **having read Claude's and Sol's finished reports** (7 reads each, from its 16th tool call, before writing anything; 17 sentences identical to Claude's, findings 1–6 and all three risks are Claude's in order; not disclosed in its inputs line). **DECISIONS `#025`** (`handoff/022` `add1a48`): the single-reviewer retry path breaks §5's independence; manual procedure for any future single-reviewer retry (hold the sibling directories out of the attempt directory during the retry, restore before the reconciliation; never retry a Grok 402 without Michael); the boundary `fix(runner)` scope; and the counting rule for the reconciliation — Grok's r4 report is **derived, not independent**: unique findings 0 by construction, no entry in the low-rater table, its verdict outside the PASS/FAIL count; the copy still goes to `docs/reviews/phase-1-r4-grok.md` verbatim.
- **`status.json`** (not the signal; the bodies are): Claude `FAIL` 1 blocker / 3 should-fix / 5 nits (`num_turns` 110); Sol `FAIL` 1 / 1 / 0; Grok `FAIL` 1 / 3 / 2 (derived). Reports: `.evidence/reviews/1/r4/{claude,sol,grok}/report.md` (70, 53, 75 lines). Sol's probes and logs sit under `.evidence/reviews/1/r4/sol/` (`strike-probe.mjs`, `cursor-probe.mjs`, `extra-probe.mjs`, `fuzz.log`, `paste-probe.log`, `caret-mutation.log`, …) — evidence, not for the tree.
- **Findings headline (orientation only; the reconciliation dispositions them from the bodies):** Claude 1 **blocker** `packages/core/src/format.ts:54` + `schema.ts` `FLANKING_MARKS` — a `delete` (strikethrough) run whose edge character is punctuation next to unmarked alphanumeric text serialises to `~~` delimiters GFM cannot open or close (the serializer half of the flanking rule; the strip's mark-edge clause reads whitespace and breaks only); Sol 1 **blocker** `schema.ts` / `format.ts:68` — deleting the space after a strikethrough run ending in a link makes Copy Markdown emit non-round-tripping bytes and lose the strikethrough (the same family, from the deletion side); Claude 2 should-fix — 1.36's `needsBlankLine` misses the paragraph→setext-heading pair; Claude 3 should-fix / Sol 2 **should-fix Required** — `toggle.ts:351` `toSource` maps a caret past every node the correspondence knows to the block's *start* (1.37's `[1.37]` backlog line, now a finding); Claude 4 should-fix — the chord guard's regex misses `Shift+End`, `Alt+Home`, `ControlOrMeta+Shift+End`; Claude 5 nit — the guard allowlist's stated reason (`@codemirror/commands` `standardKeymap`) is false, that package is not installed; Claude 6 nit — the `[plan.1.verify.r4h.r0]` backlog line says the vertical-arrow construct "remains" at two sites, but `e2e/web/editor-break-in-mark.spec.ts` (1.34) holds it too; Claude 7 nit — the heading input rule on a hard-wrapped paragraph; Claude 8 nit — 1.37's attempt-1 journal stub has no timestamp; Claude 9 nit — turn budget, evidence only (four of five r4 opus fix tasks capped at a1). Both blockers are one family — the serializer is an unowned half of the fixed-point property (Claude's risk 1).
- **Tasks** (`ralph/ralph.sh status`): phase 1 — 77 tasks, passed 74, superseded 4, principal-pending 1 (`1.10.r4d`), pending 1 (`1.close`); `doctor` clean. Branches `handoff/010` … `handoff/022`. Lessons tagged `1.*`/`plan.1.*`: 124.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r4d` (runner.log line 79,578; the log is 79,579 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the reconciliation's DONE commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r4d` (line 79,578).
- **Expected next stop**: the reconciliation commits `wip(1.10.r4d): …` with `<promise>DONE 1.10.r4d</promise>` in `.wt/1.10.r4d` (no pnpm there — `[1.10.r3d]` lesson), restart → the runner squashes `task(1.10.r4d)` and runs `sync-state`; on FAIL the appended `1.40…` fix tasks run → `HUMAN_GATE 1.verify.r5h` (rotate there); on PASS `1.close` → `CLOSED`.

## Corrections

- `021` Expected next stop assumed the review set would run clean at attempt 1: Grok failed on a 402, was retried, and its report is derived (#025). `021`'s "the r4 reviewers' inputs" list is right for Claude and Sol only.
- `021` Gotchas: the reconciliation now cherry-picks three branches — `git cherry-pick phase/1..handoff/020 && git cherry-pick phase/1..handoff/021 && git cherry-pick phase/1..handoff/022` (one, one, two commits; verify with `git log --oneline phase/1..handoff/NNN`; the `next-prompt.md` conflict resolves to the incoming version each time). `DECISIONS.md` gets `#025` at its end before `#review-1-r4` is appended.
- `020`/`021` "the `[1.37]` cursor-map defect is dispositioned by the reconciliation beyond the reviewers' findings": it is now Claude 3 / Sol 2 (Required), a finding.

## Decisions

- **`#025`** (above; on `handoff/022`). **`#024`** in the tree at `0251db3`. **Michael's decision (2026-09-12, recorded in #025):** no paying for Grok; the one reset is spent; a further 402 is his call.
- **Turn budget, evidence only**: unchanged from 020/021 — six of six capped opus fix tasks on the strip family; the r4 repair round at a1 (18, 26 turns); Claude's r4 review at 110 turns. Decision at the boundary on Michael's OK.
- **Runner deviations this block**: `#025` (the retry path's independence gap; the runner did what §3 says). The `STUCK` after a reviewer's 402 is per RUNNER-SPEC.

## Gotchas

- **Grok's r4 report reads like Claude's because it is** — count it as derived (#025), do not raise a "Grok agrees with Claude" pattern from it, and do not compare its severities.
- **Before any single-reviewer retry**: #025's manual procedure (hold the sibling directories, clear the failed attempt's own `report.md`). A Grok 402 is Michael's call.
- **Never run pnpm, vitest or Playwright on the Mac inside `.wt/1.10.r4d`** — the runner runs the reconciliation's suite there in the container; reproduce blockers in `git worktree add --detach <scratchpad>/probe 6c0e601` (install there, remove after). The strikethrough family can be reproduced headlessly through `mdastToPM` → transaction → `pmToMdast` → `format` (the shape the r3 reconciliation used).
- **`handoff/022` holds two commits** (`add1a48` #025, and this rotation). `handoff/020` and `handoff/021` one each.
- **Evidence for the boundary `gc`**: `git ls-remote origin 'refs/heads/ci/*'` first; expect the GATE-FAILED rounds (`ci/1.verify/a1`, `ci/1.verify.r2/a1`, `ci/1.verify.r3/a1`, `ci/1.verify.r4/a1`), `ci/1.verify.r3.g1/a1` (abandoned), possibly `ci/1.verify.r3.g1/a2` and `ci/1.verify.r4.g1/a1`.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.

## Next Steps

1. Fresh session (the reconciliation session, #015): `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael (no ask yet — the reconciliation is the principal's own work in `.wt/1.10.r4d`).
2. In `.wt/1.10.r4d` only: the three cherry-picks; the three reports copied verbatim to `docs/reviews/phase-1-r4-{claude,sol,grok}.md`; both blockers reproduced in the detached scratch worktree (Claude 1: a `delete` run whose edge character is punctuation next to alphanumerics, through `format`; Sol 1: `strike-probe.mjs`'s shape — delete the space after a strikethrough run ending in a link); `DECISIONS.md#review-1-r4` after `#025` with `verdict: PASS|FAIL` first — findings numbered J1… with dispositions, I1–I10 confirmed or reopened, the reviewer record with Grok derived per #025 (two independent reports), the `[1.37]` line and the `[plan.1.verify.r4h.r0]` line dispositioned (Claude 3/Sol 2 and Claude 6), Claude 8's timestamp-less stub read against the runner's per-attempt count (it counted — the line exists), the turn-budget note with counts read from `.evidence/state/tasks.json` and the transcripts, Michael's Grok decision cited, the r5 reviewer rows written for three reviewers with the Grok question recorded for him; class-level lessons; backlog lines (the `#025` boundary `fix(runner)`, hard stop `2.1`); `docs/progress.md` regenerated; on FAIL `1.40…` + `1.verify.r5` + `1.10.r5a/b/c/d` in PRD §8 and `ralph/tasks.json` the way `ede7729` did (`node ralph/generate-tasks.mjs`, `EXPECTED_COUNT` from the printed count, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green — the conformance suite does not install node_modules); commit `wip(1.10.r4d): …` with `<promise>DONE 1.10.r4d</promise>`; only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (§8.1).
3. Idle checks, restart; rotate at the next stop signal (`HUMAN_GATE 1.verify.r5h` or `CLOSED`).
4. Phase boundary items (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)` (per-reviewer view on a partial start; `check_report` refuses sibling reads; conformance per guard); `#023`'s `fix(runner)`; H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5's third case, the stale `description` line and PRINCIPAL.md's doctor sentence; the conditional items on Michael's OK only — the turn-budget raise, the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option and Grok's future (budget + #025); `git branch -D handoff/010 … handoff/022`; ask Michael for `scripts/gate.sh gc` and the `phase/1` push (#010).

## Open Questions

- The r4 verdict: two independent FAILs on one family (the serializer's strikethrough flanking) plus a Required cursor-map finding; the reconciliation's reproduction decides, and a confirmed blocker is FAIL and the r5 chain (the first Phase 1 fix in `packages/core/src/format.ts`, a passed task's file — new task, never an edit of the record).
- Whether Grok reviews r5 at all (Michael: budget; #025: contamination on any retry) — the r5 rows are written for three reviewers and the question is his.
- The turn budget and the fetch-on-failure change: Michael's calls at the boundary.
