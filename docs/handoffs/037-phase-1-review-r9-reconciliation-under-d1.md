# Handoff 037 — Phase 1 at `PRINCIPAL 1.10.r9d` under DECISIONS #041: Michael's D1 (the review stop rule; a scoped r10 round) and D2 (the principal on Opus 5.5, Fable by escalation); the FAIL reconciliation `1.10.r9d` waits for a fresh Opus 5.5 session; the handoff stack is twelve commits on `handoff/037`

Written 2026-09-24 by the principal (the session that ran the gate `1.verify.r9.g1h`, followed the r9 review set, reviewed proposal 001 v2 and recorded Michael's D1 and D2; Claude Code on the Mac Mini, Fable 5.1 — the last Fable principal session under D2). Supersedes `036-phase-1-review-r9-reconciliation-pending.md` (on `handoff/036`, not in the tree) and, through it, 035–031 and the in-tree `030-phase-1-review-r8-reconciliation.md`. This commit is on `handoff/037`, stacked on `handoff/036`; the `1.10.r9d` reconciliation cherry-picks `phase/1..handoff/037` as one range — twelve commits: `docs/handoffs/**`, `docs/DECISIONS.md` (#038, #040, #041), `docs/proposals/001-token-efficiency.md`, `docs/PRINCIPAL.md` (the D2 section). Next: `038-*.md`.

## Current State

- **`phase/1` tip `287c884`** (`task(1.verify.r9.g1)`); `main` at `052e206`; `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean except the untracked `docs/proposals/` (the same bytes as the stack's commit `3c9b92f`; leave it or delete it, the stack carries it), `doctor` clean.
- **Gate `1.verify.r9.g1h` ACCEPT at a1** (run `35968837053` at `287c884`, from the principal session, exit 0). Per OS 45 files / 4572 tests, cargo ok ×3, 102 Playwright; the block-local leg 1,297 / 920 / 1,875 ms (ubuntu / macos / windows) against 30 s.
- **Review set r9 passed, attempt 1 each** (`07:28Z`–`07:48Z`; SHAs equal at `287c884`; exit 0 ×3; no 402). Claude (claude-opus) FAIL 1 blocker / 3 should-fix / 2 nits; Sol FAIL 0 / 3 Required / 0; Grok PASS 0/0/0. Unique findings 6 / 3 / 0, shared 0. The findings, with file:line and routes, are in 036's Current State and in `.evidence/reviews/1/r9/{claude,sol,grok}/report.md`; #041 names the disposition of every one.
- **`1.10.r9d` principal-pending, attempts 0**, worktree `.wt/1.10.r9d` on `task/1.10.r9d` at `287c884`, clean. `EXPECTED_COUNT` 276.
- **Decisions in force for the reconciliation: DECISIONS #041** (on this stack). D1: verdict FAIL; fix tasks `1.67` (Claude's blocker, the 75-pair sweep and the join/space-deletion legs as tests, the installed handlers read first) and `1.68` (Claude's finding 3, the `:181` caret race under #037); everything else backlogged with a `2.verify` trigger (Claude 2, 4, 5, 6; Sol 1, 2, 3); `1.verify.r10` + `1.verify.r10h`; `1.10.r10a/b/c` with Michael's scope verbatim in the task text ("confirming the two fixes and the backlog dispositions, blockers only, no new sweeps or browser drives"); `1.10.r10d`; `1.9.r1` per #040; `1.close` on `1.10.r10d` and `1.9.r1`. **A blocker reported in r10 goes to Michael before any fix chain is planned.** D2: the principal on Opus 5.5 from this rotation; Fable by escalation per PRINCIPAL.md's new section; one brief at most for `1.10.r9d` (what D1 leaves open: whether `1.67` splits, the exact r10 reviewer text).
- **Handoff stack**: `handoff/037` = `handoff/036` + four commits (`3c9b92f` proposal v3, `9920baf` #041, `12d5477` PRINCIPAL.md, this one). The stack sits on `2507210`; `phase/1` is two task commits ahead with no `docs/DECISIONS.md`, `docs/PRINCIPAL.md` or `docs/proposals/` change, so `git log --oneline phase/1..handoff/037 | wc -l` prints 12 and the cherry-pick is conflict-free. `git branch --list 'handoff/03*'`: `030` (stale ref, file in tree), `033`, `034`, `035`, `036`, `037`.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, at the prompt after `PRINCIPAL 1.10.r9d` (runner.log 141,043 lines). `.locks/` empty, no `ralph run` process, no container. Restart after the reconciliation's DONE commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped; a fresh one after every restart or `retry`; `ralph/ralph.sh status` on every expiry notice):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r9d`. A review set prints no `[ralph] <id> attempt` lines; the watcher fires at `PRINCIPAL`.
- **Expected next stop**: after the reconciliation's DONE commit and the restart, `[ralph] 1.67 attempt 1`.

## Corrections

- `036` Current State "Expected next stop … the r10 chain's first fix task (ids from the planning)" and Gotchas "one fix task per finding on the r8 pattern": superseded by #041 — two fix tasks only, the rest backlogged.
- `036` Open Questions 1 (one task or two for the blocker; whether Claude's should-fix 2 rides with it): Claude 2 is backlogged under D1; the split of `1.67` is the reconciliation's call (a Fable brief if needed).
- `036` relaunch line `claude --remote-control essaydown-principal`: under D2 it is `claude --model claude-opus-5-5 --remote-control essaydown-principal`.
- `036` "the reconciliation cherry-picks `phase/1..handoff/036` (eight commits)": `phase/1..handoff/037`, twelve commits.
- `036` Next Steps 3 and `next-prompt.md` job 5, the boundary `git branch -D` list: add `handoff/037`.

## Decisions

- **DECISIONS #041** (this stack, `9920baf`): D1 and D2 in Michael's words, the effect on `1.10.r9d`, the r10 blocker rule, the D2 relaunch and escalation, the reversal. #040 (the `1.9.r1` yes) unchanged.
- **Proposal 001 v3 committed as received** (`3c9b92f`, `docs/proposals/001-token-efficiency.md`); D3–D5 are open until the Phase 2 boundary and are Michael's.
- **PRINCIPAL.md gains "Model and escalation"** (`12d5477`): the D2 trigger list and brief; not in `CLAUDE.md`.
- **No runner deviation**: no `admin`, no `fix(runner)`, no `retry`, no `docker/` change, no reviewer reconfiguration.
- **Turn-budget evidence** unchanged from 035/036 (conditional on Michael's OK): the `[review-1-r9, turn budget]` line names `1.64`'s six caps, `1.63`'s and `1.65`'s a1 caps, `1.66`'s two caps then its 31-turn pass. The recount for the proposal: 187 task runs with a result line, 54 capped, $457 list; opus 104 runs $356, sonnet 76 runs $94; all 23 verify runs $17.29; Phase 1 fix tasks 1.40–1.66 $7.21 per task; Claude reviewer r9 $13.07 at 113 turns.

## Gotchas

- **The r10 reviewers' task text is where D1 saves**: Michael's scope goes in verbatim, beside the r9 dispositions and the backlog lines with their triggers, so the reviewers confirm and do not re-derive. Never reconfigure a reviewer inside the set (#review-1-r0); a `docker/entrypoints` change is boundary only.
- **`1.67` follows CLAUDE.md's mark-edge rule**: read the installed `mdast-util-to-markdown` `emphasis`/`strong` handlers before touching the editor's tree; both halves owned (the tree the editor builds, the bytes the serializer writes); the sweep enumerates both neighbour classes with an astral member; the corpus is asserted as the invariant with the two broken pairs as named guards; the L11 backlog line is corrected on a new line, never edited.
- **`1.68` follows #037**: the caret placed by a bounded re-click that reads the dev-bar readout or by counted arrows from an asserted anchor; no magnitude from one run; the per-test budget is the family's.
- **The FAIL reconciliation's checklist** (`task(1.10.r8d)` at `3c96b9b`): cherry-pick `phase/1..handoff/037` first (twelve commits, `docs/**` only); `docs/DECISIONS.md` #review-1-r9 (the verdict line the runner's regex reads, `verdict: FAIL`; each finding with severity and disposition per #041; the reviewer record with the counts; the `[review-1-r9, turn budget]` line; the gate's numbers); `.evidence/reviews/1/r9/verdict` is written by the runner from that entry; the three reports copied to `docs/reviews/phase-1-r9-*.md`; the promote-lessons candidates named verbatim in `1.verify.r10`'s text; `docs/progress.md` regenerated; `ralph/tasks.json` and `ralph/EXPECTED_COUNT`; `generate-tasks --check`, `validate-tasks`, `bash ralph/test/run.sh` with its count captured to a file; touch only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (§8.1); the promise only in the `wip(1.10.r9d)` commit message, never in a file.
- **A fresh container has no `node_modules`**: every fix-task text names `pnpm install --frozen-lockfile` first.
- **Reading a transcript for a 402 or a rate limit**: grep `"status":402` and `rate_limit_event`, never a bare `402`.
- **Worktrees**: `.wt/handoff-037` is removed by this rotation; `.wt/1.10.r9d` is the runner's; the prior session's scratch worktree at `/private/tmp/claude-501/-Users-mlsavage-Developer-essaydown/b6d390f4-…/scratchpad/970bb5c` is still registered — `git worktree remove --force` it at the boundary.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit — background it and read its output file. A `tail -3` on a test runner's output loses its summary: capture to a file and grep.

## Next Steps

1. **The reconciliation `1.10.r9d`** in a fresh Opus 5.5 session (#015, #041 D2): the checklist above under D1; then the restart line and a fresh watcher for `1.67`'s first attempt.
2. **The r10 chain**: `1.67`, `1.68`, `1.verify.r10`, the CI gate `1.verify.r10h` (from the session, backgrounded, or Michael's shell), the scoped `1.10.r10a/b/c`, `PRINCIPAL 1.10.r10d` — rotate there. At `1.10.r10d`: a PASS reconciliation, then `HUMAN_GATE 1.9.r1` (Michael's observation on the r9 tree, `scripts/gate.sh 1.9.r1 --outcome ACCEPT|REJECT` with a note), then `1.close`; **a blocker in r10 is brought to Michael before any planning commit**. A capped attempt with the work built is the chain's pattern (`next-prompt.md` job 3).
3. **After `1.close`** (host checkout on `phase/2`, runner idle): D3–D5 of proposal 001 on Michael's word, each its own commit with its reversal, covered by `bash ralph/test/run.sh` and the suite; 036's boundary list unchanged — the `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` with its conformance test, the recovery-commit guard, the gate fetch-on-failure I7 ×2 — proposal item 6 merges here), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r9 principal lessons, Claude r8 nit 3 (`ci.yml` `if: always()`), React 19's ratification and the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/030 handoff/033 handoff/034 handoff/035 handoff/036 handoff/037`; the scratch worktree; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 … #review-1-r8, handoffs 031–037, #038), the 429 signal (#027), Grok's read-only mode (count first: Grok's unique findings at r7, r8, r9 from the three `status.json` files).

## Open Questions

- Whether `1.67` is one task or two (the handler fix with the 75-pair sweep; the join and space-deletion corpus legs) — the reconciliation's call, a Fable brief if it wants one.
- Whether `1.9.r1` runs before or after the r10 chain's CI gate: its observation SHA is `287c884` either way; running it early lets Michael's REJECT (a plan request) be planned with the r10 fixes.
- D3–D5: Michael's calls at the boundary; the usage meter (item 10) reads `rate_limit_event` utilisation, not list dollars.
