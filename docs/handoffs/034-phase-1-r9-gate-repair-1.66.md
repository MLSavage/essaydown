# Handoff 034 — Phase 1 in the r9 gate repair: `1.verify.r9h` GATE-FAILED on one whole-document test's budget (DECISIONS #039), the planning commit integrated at `2507210`, `1.66` running; the handoff stack rebased onto the new tip; Michael's yes to `1.9.r1` recorded (#040)

Written 2026-09-23 by the principal (the session that ran both `1.64` retries, followed the chain to the gate, and wrote the `1.verify.r9h` planning commit; Claude Code on the Mac Mini), at the rotation point a repaired GATE-FAILED is. Supersedes `033-phase-1-verify-r9-gate.md` (on `handoff/033`, not in the tree). This commit is on `handoff/034`, stacked on `handoff/033`, which now sits on `phase/1` at `2507210` (rebased this session, #039); the `1.10.r9d` reconciliation cherry-picks `phase/1..handoff/034` as one conflict-free range — six commits, `docs/handoffs/**` and `docs/DECISIONS.md` (#038, #040) only. Next: `035-*.md`.

## Current State

- **`phase/1` tip `2507210`** (`plan(plan.1.verify.r9h.r0)`); `main` at `052e206`; `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **Gate `1.verify.r9h` GATE-FAILED a1 → `superseded`** (ci.yml run 35894799995 at `29d2e08`): one test, 1.63's block-edge corpus leg on `essay-fixture.md`, timed out at 60 s on ubuntu (86.3 s) and windows (96.2 s) and passed on macOS at 59.9 s; 4571 tests, lint, cargo and every e2e spec green on all three. **DECISIONS #039** (in the tree): the block-local oracle, not a budget raise or a narrowing. Graph 276 tasks (220 raw + 21 review-set + 35 CI gates): **`1.66`** (sonnet: lift 1.64's `blockAlone` into `packages/editor/test/block-alone.ts`, apply it to 1.63's leg with the bridge and the family's 30 s, repeat 1.63's mutation, one `[review-1-r9, CI timing]` backlog line) → **`1.verify.r9.g1`** (sonnet, needsCI, `ci/1.verify.r9.g1/a{n}`) → gate **`1.verify.r9.g1h`** (Michael) → `1.10.r9a/b/c` (rewired) → `1.10.r9d`; `1.close` on `1.10.r9d` (and on `1.9.r1` once the reconciliation appends it, #040).
- **`1.66` running, attempt 1** (sonnet, 50 turns) in `.wt/1.66`, started 17:44Z at the restart that integrated the plan.
- **Handoff stack**: `handoff/033` rebased onto `2507210` (`git rebase --onto phase/1 5fb4163 handoff/033`; the one EOF conflict on DECISIONS.md resolved by keeping #039 then #038 — #038's block byte-identical to its original); `handoff/031` and `handoff/032` refs deleted (their commits live in `handoff/033`'s history). `handoff/034` = `handoff/033` + #040 + this rotation commit. `git log --oneline phase/1..handoff/034 | wc -l` prints 6.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, the loop live on `1.66` (runner.log 138,794 lines at writing). `.locks/` holds the runner's lock while it runs. Restart after any stop: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped; **a fresh one after every restart or `retry`**; `ralph/ralph.sh status` on every expiry notice — a line in the expiry's second is lost):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `[ralph] 1.66 attempt 1`.
- **Expected next stop**: `1.66`'s integration then `[ralph] 1.verify.r9.g1 attempt 1`; then `HUMAN_GATE 1.verify.r9.g1h` (Michael: `scripts/gate.sh 1.verify.r9.g1h`; **rotate there**, #015); on ACCEPT the review set; then `PRINCIPAL 1.10.r9d` (a fresh session).

## Corrections

- `033` Current State, "Expected next stop: the gate's own outcome (ACCEPT → restart; GATE-FAILED → a planning commit)": GATE-FAILED; the planning commit is `2507210`.
- `033` Gotchas, "`handoff/031` → `handoff/032` → `handoff/033`, stacked; the reconciliation cherry-picks `phase/1..handoff/033` (four commits)": the stack was rebased onto `2507210` and the `031`/`032` refs deleted; the range is now `phase/1..handoff/034` (six commits).
- `033` and `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/033`": `handoff/010 … handoff/030`, `handoff/033` and `handoff/034` (031 and 032 are already gone).
- `033` Open Questions, "Michael's answer on the `1.9.r1` approval retry": answered — yes (#040).
- `031` Current State, "Graph 273 tasks": 276 since `2507210`.

## Decisions

- **`#039`** (in the tree, `2507210`): the GATE-FAILED's cause (a whole-document oracle's per-test budget set at eight times the container's 7.8 s; the runners eight to twelve times slower), the scope decision (four candidates; the block-local oracle chosen), the graph change (`1.66`, `1.verify.r9.g1`, the reviewers rewired, `EXPECTED_COUNT` 273 → 276), the model choice (sonnet), the handoff-stack rebase the reconciliation needs (done), the boundary item (the gate's fetch-on-failure, I7's second instance — `run.json` with `artifacts: []` on a failed run), the reversal.
- **`#040`** (this branch, `59ecce4`): **Michael's yes to `1.9.r1`** ("Yes to 1.9.r1, append it in the reconciliation"): the reconciliation appends the approval retry at the r9 implementation SHA (`1.verify.r9.g1`'s integrated commit) per rule (b), with `1.9`'s procedure plus the `[repair-1.9 (c)]` two readers of different engines, `1.close` depending on it beside `1.10.r9d`, `EXPECTED_COUNT` +1; a REJECT is a plan request on `1.9.r1`.
- **Rules in force**: as 033 (the three r9 promotions included). Nothing new promoted.
- **Runner deviations this block**: none — the planning commit followed RUNNER-SPEC §2 rule (a) through `ralph/ralph.sh plan`; the evidence was read with `gh run view 35894799995 --log-failed` (I7). No 429, no `NO-JOURNAL`, no `admin`, no `docker/` change, no `fix(runner)`.
- **Turn-budget evidence** (conditional on Michael's OK — unchanged as a decision): as 033; `1.66`'s attempts add to it.

## Gotchas

- **`1.66`'s mutation is in source** (step (5): 1.63's typed-marks rule reverted in `packages/editor/src/toggle.ts`, not committed): a capped attempt can leave it in the working tree and the runner's recovery commit sweeps it onto `task/1.66` (memory `project-recovery-commit-can-carry-a-mutation`). Before any `retry`, `git -C .wt/1.66 diff --stat 2507210 HEAD -- packages/editor/src` must be empty; if not, the retry lesson tells the agent to `git checkout 2507210 -- packages/editor/src/toggle.ts` first and to journal that the sweep happened.
- **The block-local bridge in 1.63's leg**: an edge is one position, so the per-line constant shift is asserted across the two routes of one edge and across both edges of a single-line block; the count equation in the summary test is the guard that every compared edge was bridged. An agent that reports the bridge as vacuous for multi-line blocks is reading it right — 1.64's leg carries the per-line claim over every position; 1.63's leg carries it at edges.
- **The gate `1.verify.r9.g1h`** is a CI gate like `1.verify.r9h`: `scripts/gate.sh 1.verify.r9.g1h`, no `--outcome`, Michael's `gh` login; from a session backgrounded and its output file read; on ACCEPT restart the runner; on GATE-FAILED read `gh run view <id> --log-failed` (the gate fetches artifacts only on success) and write the next planning commit (`plan.1.verify.r9.g1h.r0`; #037 and #039 are the pattern).
- **Worktrees**: `.wt/handoff-rebase` is removed by this rotation; the prior session's scratch worktree at `<its scratchpad>/970bb5c` (detached, with `node_modules`) is still registered — throwaway, never used for a task; `git worktree remove --force` it at the boundary.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`; the host `grep` is ugrep and rejects a `.\{0,500\}` bound — use `node` for long-text extraction. `ralph/ralph.sh status` says "running attempt 1" while the log shows a later attempt: read the log. A `tail -3` on a test runner's output loses its summary lines: capture to a file and grep `# pass` / `# fail`.

## Next Steps

1. **Follow the repair**: `1.66` (a capped a1 with the work built is the chain's pattern; a `STUCK` gets the source-diff check above before any `retry`), `1.verify.r9.g1`, then `HUMAN_GATE 1.verify.r9.g1h` — Michael: `scripts/gate.sh 1.verify.r9.g1h`; rotate there. On ACCEPT: idle checks, restart, fresh watcher; the review set; at `PRINCIPAL 1.10.r9d` rotate.
2. **The reconciliation `1.10.r9d`** in a fresh session (#015): the FAIL pattern `task(1.10.r8d)` at `3c96b9b` with #review-1-r8, the PASS pattern `task(0.12.r2d)` at `052e206` with #review-0-r2; the implementation SHA is `1.verify.r9.g1`'s integrated commit; it cherry-picks `phase/1..handoff/034` (six commits; no DECISIONS tail conflict after the rebase), regenerates `docs/progress.md`, appends `1.9.r1` (#040) with `EXPECTED_COUNT` +1, the `[review-1-r9, turn budget]` evidence line (`1.64`'s six caps, `1.63`/`1.65`'s a1 caps, `1.66`'s count), the reviewer record; then `1.close` waits on `1.9.r1` (Michael runs it at the r9 SHA) on a PASS, or the r10 chain on a FAIL.
3. **After `1.close`** (host checkout on `phase/2`, runner idle): 033's Next Steps 3 unchanged plus #039's boundary item — the `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` with its conformance test, the recovery-commit guard, the gate fetch-on-failure I7 ×2), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r9 principal lessons (#038, #039), Claude r8 nit 3 (`ci.yml` `if: always()`), React 19's ratification (Michael's OK), the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/030 handoff/033 handoff/034`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 … #review-1-r8, handoffs 031–034, #038), the 429 signal (#027), the reviewer-prompt option, Grok's read-only mode.

## Open Questions

- Whether `1.66` lands in one attempt and the repaired leg holds on three runners at `1.verify.r9.g1h`.
- The r9 review verdict: a PASS leads to `1.9.r1` then `1.close`; a FAIL opens the r10 chain.
- The cap raise, the 429 signal, Grok's mode, React 19: Michael's calls, evidence unchanged in kind.
