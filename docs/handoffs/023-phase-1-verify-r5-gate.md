# Handoff 023 — Phase 1 at `HUMAN_GATE 1.verify.r5h`: the r4 reconciliation (FAIL) integrated, the r5 fix chain 1.40–1.44 and `1.verify.r5` passed, the CI gate open for Michael

Written 2026-09-12 by the principal (the session that ran the `1.10.r4d` reconciliation and watched the r5 chain; Claude Code, Remote Control session on the Mac Mini), at the `HUMAN_GATE 1.verify.r5h` stop signal (`ROTATE-PRINCIPAL` printed). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `022-phase-1-review-r4-reconciliation-pending.md` (in the tree since `cef59c2`, with 020 and 021 and DECISIONS #025). This commit is on `handoff/023`, which already carries `e03ee87` (DECISIONS #026), `eb59116` (#027) and `e305be6` (#028); the `1.10.r5d` reconciliation cherry-picks all four commits (`git log --oneline phase/1..handoff/023` must show four). Next: `024-*.md`.

## Current State

- **`phase/1` tip `3dd9c4b`** (`task(1.verify.r5)`); 67 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean. CLAUDE.md/AGENTS.md now carry the two rules `1.verify.r5` promoted (the two-halves ownership of a fixed-point family; a kind table derived from the rule's branches).
- **Passed this block, all integrated** (attempts and turns from `.evidence/state/tasks.json` and the transcripts): `1.10.r4d` (reconciliation, verdict FAIL, `cef59c2`); `1.40` (opus; three attempts — capped 51, then 23 and 13 stopped on a task-text conflict — then `retry` after DECISIONS #026, passed at 29 turns); `1.41` (sonnet; three attempts capped at 51 with the work complete; first `retry`: capped 51, a session-limit 429 at 16 turns, then DONE with `NO-JOURNAL` — the earlier stub completed in place; second `retry`: passed at 38); `1.42` (sonnet; three attempts capped at 51 with the work complete; `retry`: passed at 18); `1.43` (opus; 42 turns withholding DONE on the "no expected fail" clause, then 13 and 8 re-confirming it; `retry` after #028: passed at 15); `1.44` (sonnet; passed at attempt 1, 35 turns); `1.verify.r5` (sonnet; attempt 1, 43 turns). **Gate `1.verify.r5h`: human-pending**, the CI ref `ci/1.verify.r5/a1` at `3dd9c4b`; Michael runs `scripts/gate.sh 1.verify.r5h`.
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.verify.r5h human-pending; last passed 1.verify.r5; gates open: 1.verify.r5h; plan requests: none; blocked: none." Graph 222 tasks (`EXPECTED_COUNT` 222 since `cef59c2`).
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r5h` / `ROTATE-PRINCIPAL` (runner.log lines 89,897–89,898). `.locks/` empty, no `ralph run` process, no containers. Restart after the gate's ACCEPT: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter` — the runner then starts `1.10.r5a/b/c` together from `3dd9c4b`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r5h` (line 89,897).
- **Expected next stop**: `scripts/gate.sh 1.verify.r5h` → ACCEPT (restart; the review set runs ~35 min; `STUCK 1.10.r5` if a reviewer fails; then `PRINCIPAL 1.10.r5d` — rotate, a fresh session runs it) or `GATE-FAILED` (planning commit `#029` via `ralph/ralph.sh plan plan.1.verify.r5h.r0`, the `0251db3` pattern; a `.g1` repair task + `1.verify.r5.g1`; after any `gate.sh rerun` that accepts, `doctor` names `--resume` — never run it, #023's abandon).

## Corrections

- `022` Open Questions, "whether Grok reviews r5": **answered by Michael** ("session limit reset, grok also has usage limits reset, we're good to proceed as normal"), recorded as #027 — Grok reviews r5; `1.10.r5c` stands.
- `022` Next Steps 2 assumed the r5 fix tasks would run clean: every one of 1.40–1.43 needed a `retry` with a principal lesson (Decisions below); 1.44 and 1.verify.r5 passed at attempt 1.
- `#026` Decision 2 (1.43's scope widened to `positions.ts` by a lesson line) is **withdrawn by #028**: a lesson line is not a scope instrument; 1.43 printed DONE with 1.40's `it.fails` pin in `packages/core/test/positions-corpus.test.ts` remaining (the suite reports **1 expected fail**). The `formatWithMap` fix is a task for the next planning-capable commit.
- `022` "Turn budget: six of six capped opus fix tasks on the strip family": now, from the r5 chain, 1.40 (opus) capped once with the work done, 1.41 (sonnet) capped four times, 1.42 (sonnet) three times — every cap in the verification tail after the code was complete; 1.43 lost attempts to an undecided conflict, not the cap. The evidence is model-independent.

## Decisions

- **`#review-1-r4`** (in the tree at `cef59c2`): verdict FAIL; J1 blocker (the serializer half of the flanking family) → 1.40 + 1.41; J2 → 1.42; J3 Required → 1.43; J4/J5 → 1.44; J6–J9 taken, backlog or recorded; Grok's r4 report derived (#025); reviewer record Claude 7 of 9 unique, Sol 0 of 2, two shared findings agreed; r5 rows for three reviewers.
- **`#026`** (`handoff/023` `e03ee87`): 1.40's task-text conflict — the editor legs' entity assertion narrowed to the rule (no reference the parsed text keeps; the decoded output equals the editor's text); 1.40 may edit that one assertion (its agent accepted it — the conflict was inside its own acceptance). Decision 2 withdrawn by #028.
- **`#027`** (`eb59116`): Michael's Grok decision; the session-limit 429 that consumed a 1.41 attempt (a `fix(runner)` candidate — read `api_error_status` and stop with a named signal — conditional on Michael's OK, hard stop `2.1`); task logs moved aside before every `retry` (the runner writes `<attempt>.log` and `retry` resets the counter: 1.40's capped attempt-1 transcript was overwritten; 1.41's, 1.42's and 1.43's are kept as `.evidence/tasks/<id>/<n>.<suffix>.log`).
- **`#028`** (`e305be6`): #026 Decision 2 withdrawn; 1.43's "no expected fail" clause read as the pin it removes; the `placeChildren` fix for an outside-encoded neighbour goes to the r6 chain (r5 FAIL) or Phase 2 planning (r5 PASS), the pin in the tree at close as #024 allows.
- **Runner deviations this block**: none in the runner's own behaviour — every stop (`STUCK` ×3, `NO-JOURNAL` ×1, `HUMAN_GATE`) was per RUNNER-SPEC. Principal commits on task branches: `chore(1.40)`, `chore(1.41)` ×3, `chore(1.42)`, `chore(1.43)`, each `docs/lessons.md` only with its reversal, each followed by `ralph.sh retry <id>` (the task was `blocked`; `retry` keeps the branch and worktree, `start` is idempotent) or the plain restart (1.41 after the 429, `running`).
- **Turn budget, evidence only (Michael's call at the boundary)**: r5 chain — 1.40 a1 51 (work done), 1.41 a1/a2/a3 51 + retry a1 51 (work done from a3), 1.42 a1/a2/a3 51 (work done from a1), 1.43 42/13/8 (conflict), 1.44 35, 1.verify.r5 43. Phase 1 to date: the `[review-1-r4, turn budget]` backlog line's 19 of 46 plus the r5 chain's four of six `loop` tasks needing more than one attempt; the cap is spent on verification tails on both models, the reading #review-1-r1 made from the r1 chain.

## Gotchas

- **Before any `ralph.sh retry <id>`**: `mv .evidence/tasks/<id>/<n>.log <n>.<suffix>.log` for every attempt log first (#027) — the retry's attempt 1 writes `1.log`.
- **A `retry` lesson must say "append a NEW `- [<id>] ` line"** in so many words with the `printf` command; two agents completed an earlier stub in place (1.41's DONE became `NO-JOURNAL`), and one agent (1.41 attempt 3 of the first retry) refused a lesson that told it to skip the suite — CLAUDE.md step 3 outranks a lesson, so tell it to run the suite once, not to skip it.
- **A lesson line cannot widen a task's scope** (#028); a conflict inside a task's own acceptance is decided by naming the sentence that yields (#026 Decision 1's shape).
- **1.42 attempt 2 saw an e2e flake in the container** (`editor-soft-line-breaks.spec.ts` under the full parallel suite, green in isolation and on the rerun) — one occurrence, in-container, not a runner; if it appears on a runner at the gate, it is the `[review-1-r3 I7, flake signature]` class only if the signature matches; otherwise a `.g1` repair.
- **`pnpm test` reports 1 expected fail** (1.40's `positions-corpus.test.ts` pin); the reviewers will see it with its `[1.40, found outside scope]` backlog line — the r5 reconciliation dispositions it (J-class: the cursor map for a text node beside an encoded neighbour, on the toggle path).
- Never run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>`; reproduce in `git worktree add --detach <scratchpad>/probe <sha>` (the r4 reconciliation did; removed after). In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. Hand Michael `scripts/gate.sh 1.verify.r5h` (no restart until it prints ACCEPT). On ACCEPT: idle checks, restart; the three reviewers start together; rotate at `PRINCIPAL 1.10.r5d` (a fresh session runs the reconciliation, #015). On `GATE-FAILED`: `ralph/ralph.sh plan plan.1.verify.r5h.r0`, the planning commit `#029` in `.wt/plan.1.verify.r5h.r0` (the `0251db3` pattern: a `.g1` repair scoped to what failed, `1.verify.r5.g1`, the reviewer rows rewired by the generator), then restart.
2. If a reviewer fails (`STUCK 1.10.r5`): a Grok 402 → stop and report the usage to Michael (#025, #027); any single-reviewer retry → #025's manual procedure first (hold the sibling directories out of `.evidence/reviews/1/r5/`, move the failed attempt's own `report.md`/`transcript.log` aside), restore the siblings before the reconciliation.
3. The `1.10.r5d` reconciliation (fresh session): `git cherry-pick phase/1..handoff/023` (four commits; `next-prompt.md` conflicts resolve to the incoming version); reports copied verbatim; blockers reproduced in a scratch worktree; `#review-1-r5` after `#028` with `verdict:` first; the r4 dispositions J1–J9 confirmed or reopened; the 1.40 pin dispositioned; on FAIL the r6 chain with the `formatWithMap` `placeChildren` fix as its own task (`packages/core/src/positions.ts` + `positions-corpus.test.ts`, the pin folded), `1.verify.r6`, `1.10.r6a/b/c/d`; on PASS `1.close` with the pin carried to Phase 2 planning; the reviewer record with Grok counted (independent this time, if it started with its siblings); the turn-budget paragraph from the transcripts (the r5 chain's counts above) and the 429 candidate; `docs/progress.md` regenerated (the Phase 1 table rows for 1.40–1.44, 1.verify.r5, the r5 reviewers).
4. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)`; H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5's third case, the stale `description` line and PRINCIPAL.md's doctor sentence, plus PRINCIPAL.md gaining Sol's r4 lesson (a test-only task's scope is not a deferral reason) and #028's (a lesson line is not a scope instrument; logs aside before a retry); `git branch -D handoff/010 … handoff/023`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (the r5 chain's evidence above), the 429 signal, the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option.

## Open Questions

- The r5 verdict: J1's fix is in with 32 guards, the neighbour leg and two browser cases; the one known open defect is the position-map gap behind 1.40's pin (a should-fix on the toggle path by J3's class, not the copy criterion) — a reviewer rating it Required makes r5 FAIL and the r6 chain one task.
- The turn budget (80 turns) and the 429 signal: Michael's calls at the boundary, with this block's transcripts as the evidence.
