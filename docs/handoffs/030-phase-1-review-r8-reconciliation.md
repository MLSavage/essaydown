# Handoff 030 — Phase 1 at `PRINCIPAL 1.10.r8d`: the r8 gate repaired (#037, `1.62`, `1.verify.r8.g1`, gate ACCEPT a1 98/98 on three OSes), the r8 review set done (Claude PASS, Sol FAIL, Grok PASS; no blocker), the reconciliation next in a fresh session

Written 2026-09-22 by the principal (the session that ran the `1.verify.r8h` gate, the `plan.1.verify.r8h.r0` repair, the `1.verify.r8.g1h` gate and the r8 review set; Claude Code on the Mac Mini), at the `PRINCIPAL 1.10.r8d` stop signal, per PRINCIPAL.md's cadence (DECISIONS #015: the reconciliation is never run by the session that ran the phase's tasks). Supersedes `029-phase-1-verify-r8-gate.md` (on `handoff/029`, not yet in the tree). This commit is on `handoff/030`, **stacked on `handoff/029`** (`git log --oneline phase/1..handoff/030` shows two commits: 029's and this one); the `1.10.r8d` reconciliation cherry-picks `phase/1..handoff/030` as one range. Next: `031-*.md`.

## Current State

- **`phase/1` tip `970bb5c`** (`task(1.verify.r8.g1)`); 97 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **The r8 gate**: `1.verify.r8h` GATE-FAILED a1 (run 35755695990 at `b7f45a5`: ubuntu 97/98, windows 96/98, macos 98/98 — both failures at the click that seeds the caret in the two new toggle specs); DECISIONS **#037** (planning commit `862f291`); **`1.62`** passed (`b04f522`; a1 51 capped with the work built, a2 51 success — sonnet); **`1.verify.r8.g1`** passed (`970bb5c`; a1 25); gate **`1.verify.r8.g1h` ACCEPT a1** (run 35762164206, 98/98 on all three runners, `test-logs` and `playwright-report` fetched, three OS directories each). `1.verify.r8h` `superseded`. Graph 264 tasks (210 raw + 21 review-set + 33 CI gates).
- **The r8 review set, all three passed at attempt 1**, started within 198 ms (`started_at` 17:48:16.234/.330/.432Z — #025's condition met): `.evidence/reviews/1/r8/{claude,sol,grok}/` at implementation_sha `970bb5cd060e5cf226b5fa8fa51ce2113d4b62d6`. `status.json`: Claude **PASS** (0 blockers, 2 should-fix, 3 nits); Sol **FAIL** (0 blockers, 2 should-fix, both marked Required); Grok **PASS** (0/0/0). Findings by title: Claude 1 `format.ts:431-432` `locateChild`'s give-up path uncovered; Claude 2 the human gate `1.9` recorded at `ca1cd3d`, stale for two §7 clauses; Claude nits: React 19 vs PRD §4's React 18, M3 (a)'s promise rule not satisfiable as written, `ci.yml` merge-logs step without `if: always()`. Sol 1 `toggle.ts:567`/`:506` — after a freshly closed emphasis/strong input rule (stored marks cleared) a toggle at the block's end puts the source caret inside the closing delimiter; Sol 2 `toggle.ts:172` with `schema.ts:918` — inline correspondences derive live PM positions from normalised mdast widths, so leading whitespace removed in conversion shifts them. Grok: no findings; its gate table re-verifies M1–M11 and L1–L11, K1–K9 back to F1–F16.
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.10.r8d principal-pending; last passed 1.10.r8c; gates open: none; plan requests: none; blocked: none."
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r8d` (runner.log lines 129,156–129,157; the log is 129,157 lines). `.locks/` empty, no `ralph run` process, no containers. `.wt/1.10.r8d` exists on `task/1.10.r8d` (the runner created it). Restart after the reconciliation's DONE commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped at rotation; **arm a fresh one after every restart or `retry`**):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r8d`. After the reconciliation's restart the next own line is `CLOSED 1` on PASS (the runner runs `1.close`) or the r9 chain's first `[ralph] 1.63 attempt 1` on FAIL.
- **Expected next stop**: the reconciliation commit (a fresh session); then `1.close` or the r9 chain.

## Corrections

- `029` Current State and its Next Steps 1–3: superseded — the gate failed, was repaired and passed; the review set is done; the open item is the reconciliation.
- `029` Open Question 1 ("whether the gate passes on windows at a1"): answered — no, and not windows alone: ubuntu-latest failed the same case (#037).
- `029` "36 of 74" multi-attempt count: the r8 repair adds `1.62` (a2) and `1.verify.r8.g1` (a1), so the running count is 37 of 76 ids needing more than one attempt (by 029's method); `NO-JOURNAL` 16 and `STUCK` 10 unchanged.
- `029` and this file's `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/029`": through `handoff/030`.
- `.claude/commands/rotate.md` step 5 says `.wt/handoff-NNN` cut from `phase/<N>`; this rotation, like 027–029's, used the session scratchpad and stacked on `handoff/029` because 029 is not yet in the tree (next-prompt's stacking rule) — the boundary `chore(0.0)` item stands.

## Decisions

- **`#037`** (in the tree, `862f291`): the failing precondition asserted a DOM spelling of a caret at a reveal-widget boundary; verified in the installed prosemirror-view 1.42.3 — `NodeViewDesc.domFromPos` (dist/index.js line 922) spells that position on the parent beside the `contentEditable="false"` widget (`<p>`/4 after a closing widget, `<td>`/0 before an opening one — the exact received values), Blink's hit test spells it in the text node, `scanFor` calls the two not equivalent, and `handlers.focus` (line 3904) re-spells on a 20 ms timer. Chosen: a dev-bar selection readout (`data-testid="selection"`, a plugin view writing `{ before, empty, marks, stored }` imperatively, no React state) asserted by both specs, and an interior-text click for the pipe spec with its arrow removed. Not chosen: rerun (#023's criterion fails), accepting both spellings (a hedge), `pmViewDesc.posFromDOM` (undocumented). `1.62`'s container reproduction: both specs green in the container before the change (the race is runner-only evidence, recorded on the `[1.46]` backlog class as a second race of the family).
- **Rules in force**: as 029 (no promise literal in any file an agent reads; the boundary `fix(runner)` list). New from #037 and its lesson: an e2e that must know where the editor's caret is reads the editor's selection or clicks an interior text position; never a DOM anchor at a widget boundary, never an arrow after a boundary click.
- **Runner deviations this block**: none. No `retry` (the runner ran `1.62` a2 itself after the a1 cap), no 429, no `STUCK`, no `NO-JOURNAL`, no `admin`, no `docker/` change, no `fix(runner)`. Plan request `plan.1.verify.r8h.r0` integrated at the first `ralph.sh run` (conformance 83/83 in the plan worktree).
- **Turn-budget evidence** (conditional on Michael's OK — unchanged as a decision): `1.62` (sonnet) a1 51 capped with the work built, a2 51 with DONE on the last turn; `1.verify.r8.g1` a1 25. With 029's table: every fix task of the r8 chain and its repair (six: four opus, two sonnet) capped at a1 with the work built and passed at a2.
- **Reviewer record, counted from the reports (for `#review-1-r8`)**: Claude 5 findings (2 should-fix, 3 nits), Sol 2 (both should-fix Required), Grok 0 — no finding shared between two reporters. Grok's PASS-with-blockers running count stays 3 of 5 counted attempts (this r8 PASS has no blocker in any report, so it is not an instance); a Grok read-only-mode proposal remains Michael's call at the boundary.

## Gotchas

- **Sol's verdict is FAIL with no blocker**: the two Required should-fixes carry it. The reconciliation reproduces both in a detached scratch worktree at `970bb5c` before writing anything: Sol 1 (`see *foo*` typed, the input rule closes the run, toggle at the block's end, where does the source caret land — read `toggle.ts:506` and `:567` and the 1.52/1.60 pins first: the emphasis/strong counterpart of M5's two-route guard), Sol 2 (a paragraph whose mdast text starts with whitespace the converter drops — check `schema.ts:918` and whether any fixture in `fixtures/markdown/index.json` reaches it; the corpus inverse leg is the guard to run). Claude 2 (the stale human gate `1.9` at `ca1cd3d`) is a criterion question for the entry, not a fix task: state where the two §7 clauses are now evidenced (the three-OS gate, the reviewers' latency checks) and whether a `1.9.r1` approval retry is needed before `1.close` — Michael's call if so.
- **Claude nit 2 reopens M3 (a)'s wording** (the promise rule "not satisfiable as written"): CLAUDE.md's `## Every iteration` step 6 itself prints the promise. The entry should restate the rule as "no file an agent reads carries the promise *except* CLAUDE.md/AGENTS.md step 6, which is the instruction to print it" or move it to the boundary `fix(runner)` item (`transcriptHasDone` reading only assistant text blocks makes the file rule moot) — decide, do not widen.
- **Claude nit 1 (React 19 vs PRD §4 "React 18")**: `docs/dependencies.json` justifies both entries by the PRD row; a DECISIONS note ratifying React 19 (as #review-1-r0 (a) ratified `zod`) needs Michael's OK — conditional, boundary item.
- **The watcher dedups whole lines** (memory `project-watcher-dedup-after-retry`); arm a fresh one after every restart.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit; Michael runs it in his shell. Claude Code blocks a foreground `sleep`; wait with a backgrounded `until` loop.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. **`PRINCIPAL 1.10.r8d`** (a fresh session): in `.wt/1.10.r8d`, `git cherry-pick phase/1..handoff/030` (two commits, `docs/handoffs/` only — no DECISIONS.md conflict); copy the three reports byte-identically to `docs/reviews/phase-1-r8-{claude,sol,grok}.md`; reproduce Sol 1, Sol 2 and Claude 1 in a detached scratch worktree at `970bb5c` (`git worktree add --detach <scratchpad>/970bb5c 970bb5c`, `pnpm install --frozen-lockfile` there, never in `.wt/1.10.r8d`); write `#review-1-r8` with the verdict first, M1–M11 each confirmed or reopened (Grok's table is the most explicit), the backlog deferrals checked for a fired trigger (M11, `[#030 product]`, M7, L11/M10 — none of the three reports names one), the reviewer record above, the turn-budget paragraph; regenerate `docs/progress.md`; commit on `task/1.10.r8d` touching only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1) with the DONE promise for `1.10.r8d` in the commit message only; restart the runner; fresh watcher. On PASS the runner runs `1.close`; on FAIL append the r9 chain on the `task(1.10.r7d)` pattern at `9538cf1` (fix tasks, `1.verify.r9`, `1.10.r9a/b/c/d`), every pin a fix re-decides named by file and line, acceptance tails naming file-level greps and never the suite's total, no promise literal in any file, and the scope decided in the scratch worktree after running the corpus legs.
2. **After `1.close`** (host checkout on `phase/2`, runner idle): the phase boundary idle window — 029's Next Steps 4 unchanged, plus: the #037 lesson's rule as a CLAUDE.md/AGENTS.md candidate for `2.verify`'s promote step (not now); Claude nit 1 (React 19 ratification, Michael's OK); Claude nit 3 (`ci.yml` merge step `if: always()`, a `.g<n>`-free workflow change only at the boundary since a workflow change moves the SHA); `git branch -D handoff/010 … handoff/030`; `scripts/gate.sh gc` and the `phase/1` push (#010), Michael's shell.

## Open Questions

- Whether Sol 1 and Sol 2 reproduce at `970bb5c` (Sol's report gives the routes; neither was reproduced by this session — the reconciliation decides).
- Whether Claude 2 (the stale `1.9` human gate) needs a `1.9.r1` approval retry before `1.close` or is evidenced by the three-OS gate and the reviewers' latency checks — Michael's call if a retry is proposed.
- The cap raise, the 429 signal, Grok's mode and React 19: Michael's calls, evidence unchanged in kind.
