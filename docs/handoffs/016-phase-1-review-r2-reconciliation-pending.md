# Handoff 016 — Phase 1 at `PRINCIPAL 1.10.r2d`: repair chain `1.28` → `1.verify.r2.g1` integrated, gate `1.verify.r2.g1h` accepted, review attempt r2 complete (Claude FAIL, Sol FAIL, Grok PASS), reconciliation pending

Written 2026-09-10 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `PRINCIPAL 1.10.r2d` stop signal (a reconciliation is never run by the session that watched the reviews; DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `015-phase-1-verify-r2-g1-repair-running.md` (on `handoff/015`, not in the `phase/1` tree; 014 is on `handoff/014`; 010–013 are in the tree since `b149f0c`). This commit is on `handoff/016` (rotate.md step 5: a reconciliation is pending, so never on `phase/1`); the `1.10.r2d` reconciliation cherry-picks `handoff/014`, `handoff/015`, `handoff/016`, in that order. Next: `017-*.md`.

## Current State

- **`phase/1` tip `c9408ee`** (`task(1.verify.r2.g1)`, attempt 2); 41 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`), 33 of them `task(1.*)`. No commit on `phase/1` by the principal's hand since `b149f0c` and none until `1.close`. `main` unchanged at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). `git ls-remote origin 'ci/*'` at rotation listed `ci/1.verify/a1` (`3e98707`) and `ci/1.verify.r2/a1` (`6d9345c`) only; `ci/1.verify.r2.g1/a1` (the accepted ref) was not listed — check `git ls-remote origin ci/1.verify.r2.g1/a1` before `gate.sh gc` at the phase boundary rather than assuming either way.
- **Repair chain** (DECISIONS #021): **`1.28`** passed at attempt 1 (opus; integrated `c794f27`; diff `e2e/web/editor-clipboard.spec.ts`, `docs/lessons.md`, `docs/progress/journal-main.md` only — `apps/desktop/src/dev/DevEditor.tsx` is not in it, the scratch-mutation scope rule held). **`1.verify.r2.g1`** passed at attempt 2 (sonnet; integrated `c9408ee`; lessons and journal only). Attempt 1 stopped at orientation without implementing: the agent read `ralph/PROMPT.md`'s "Every iteration" list (runner-owned, unchanged since task 0.0: implement, then one journal entry, one commit) as task text conflicting with CLAUDE.md step 2 (journal stub committed before implementing), invoked the top-of-file conflict rule, and committed `wip(1.verify.r2.g1): clean break — …` with a lessons line and a `Status: Blocked` journal entry (both now in the tree at `c9408ee`, alongside attempt 2's second lessons line and `Status: Complete` entry). Attempt 2 stubbed the journal at `8457b7a` and finished. No principal intervention: the runner started attempt 2 by itself (`[ralph] 1.verify.r2.g1 attempt 2`).
- **Gate `1.verify.r2.g1h`**: `ACCEPT` at attempt 1 — `.evidence/ci/1.verify.r2.g1h/accepted.json`: run `34509206442` on `ci/1.verify.r2.g1/a1` at `c9408ee…`, workflow `ci.yml`, artifacts `test-logs` (108,838 bytes) and `playwright-report` (1,915,157 bytes), recorded `2026-09-10T17:43:31Z`; `accepted/` holds both plus `result.json`, `run.json`, `workflow.log`. Grok's gate table reads `68 passed` on all three runners for `e2e/web`. `gate.sh` printed `ROTATE-PRINCIPAL`; no second rotation was done there.
- **Review attempt r2**: `1.10.r2a/b/c` passed at attempt 1, one container run each, started `17:44:10Z`, finished `18:01:56Z` (~17.8 min), every `exit-code` 0. `.evidence/reviews/1/r2/`: `verifier_id` = `1.verify.r2.g1`, `verification_sha` = `implementation_sha` = `c9408ee`; no `REVIEW-SHA-MISMATCH`, no `STUCK`. `status.json` verdicts: **Claude FAIL** (blockers 1, should-fix 3, nits 2; 6 findings), **Sol FAIL** (blockers 1, should-fix 0, nits 1; 2 findings), **Grok PASS** (blockers 0, should-fix 0, nits 2; 2 findings). Verdicts are not the signal (#review-1-r0): the reconciliation reads the bodies.
- **Tasks** (`.evidence/state/tasks.json`): everything through `1.10.r2c` passed; `1.verify.r2h` superseded; **`1.10.r2d` principal-pending, attempts 0**, `task_branch task/1.10.r2d`, worktree `.wt/1.10.r2d` cut by the runner at `c9408ee`; `1.close` pending. `status`: phase 1 — 50 tasks, passed 44, superseded 2, principal-pending 1, pending 3 (re-read `status` before quoting). `doctor` clean, host checkout on `phase/1` at `c9408ee`, tree clean, `.wt/` holds `1.10.r2d` only (plus this rotation's temporary worktree, removed by it), branches `handoff/010` … `handoff/016`.
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r2d` / `[ralph] 1.10.r2d: work in …/.wt/1.10.r2d on task/1.10.r2d; commit wip(1.10.r2d) with <promise>DONE 1.10.r2d</promise> …, then ralph.sh run again`. `.locks/` empty, no `ralph run` process, no containers. Restart after the reconciliation commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watchers are stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r2d` (log line 62,070).
- **Expected next stop**: after `wip(1.10.r2d): …` with the DONE promise on `task/1.10.r2d` and the restart, the runner integrates `task(1.10.r2d)` and `sync-state` reads the verdict from `DECISIONS.md#review-1-r2`. On PASS: `1.close` → `CLOSED` / phase boundary. On FAIL: the planning commit's fix chain (`1.29…`), then `1.verify.r3` → `HUMAN_GATE 1.verify.r3h` → `1.10.r3a/b/c/d`.

## Corrections

- `015` Current State said `.locks/` holds the runner's lock while a task runs. It does not: `withLock` holds `.locks/ralph` only around a critical section (013 Corrections); `.locks/` was empty at every check this session while a container ran. Idle is the four-part check (lock, process, containers, pane), not the lock alone.
- `015` Expected next stop named `HUMAN_GATE 1.verify.r2.g1h` after "two container runs". There were three: `1.verify.r2.g1` needed attempt 2 (the orientation stop above). Not a `NO-JOURNAL` or `STUCK`: the runner's own next attempt, no principal action.
- `015` Gotchas said the reconciliation cherry-picks `handoff/014` then `handoff/015`. Now three: `handoff/014`, `handoff/015`, `handoff/016`, in that order.

## Decisions

- **No DECISIONS entry and no runner deviation this block.** The gate ran clean at attempt 1; the review set ran clean at attempt 1; nothing was retried, re-cut or repaired; no `admin` command, no `docker/` change, no `fix(runner)`. The `1.verify.r2.g1` attempt-1 stop was left to the runner's own retry, which was the right call: attempt 2 passed inside the same three-attempt budget and the only cost was ~9 minutes.
- **`ralph/PROMPT.md` drift is real but not a conflict.** `ralph/PROMPT.md` lines 25–34 (the per-task wrapper's "Every iteration": implement at step 2, one journal entry at step 4, one commit at step 6, clean break after 5 debugging tool calls) predate `1.verify.r2`'s CLAUDE.md step 2 (stub first, `wip(<id>): journal stub`, complete in place). CLAUDE.md's list is a superset and outranks the wrapper by the wrapper's own line 7; `1.28` (opus) and `1.verify.r2.g1` attempt 2 (sonnet) both followed CLAUDE.md under the unchanged wrapper and passed. Two r2 reviewers name it (Claude finding 4 should-fix, Grok finding 2 nit). Aligning the wrapper is a runner-owned `chore(0.0)`/`fix(runner)` on `ralph/PROMPT.md` — one file, no machinery — that the reconciliation dispositions (as an r2 finding) and that lands either inside a fix task's chain if the reconciliation says FAIL and scopes it, or at the phase boundary from the host checkout with the runner idle (rotate.md step 5, first case). It never goes on `phase/1` mid-flight and never inside a reviewer task.
- **Findings, as read from the report bodies' numbered items (counts to be re-derived by the reconciliation, not copied from here)**:
  - Claude (6): #1 **blocker** `packages/editor/src/schema.ts:674` `stripUnparsableWhitespace` leaves a `hard_break` at a block's end, `format` writes a bare trailing `\`, and the copied Markdown does not round-trip (gate row "The copied Markdown round-trips" = fail; browser probe with `hard-break.md`'s shape); #2 should-fix `apps/desktop/src/dev/DevEditor.tsx:102` the `toggle` flush has no test that fails without it (68/68 green with it removed); #3 should-fix `e2e/web/editor-clipboard.spec.ts:248` after 1.28 no test exercises Copy Markdown's real clipboard write (`installWriteTextSpy` is unconditional in `openEditor`); #4 should-fix `ralph/PROMPT.md:25` the wrapper drift above; #5 nit `schema.ts:682` a heading ending in `hard_break` serialises to `"H\\\n\n"` and reparses as a paragraph (same root cause as #1); #6 nit `schema.ts:686` a `table_cell` soft break serialises as `&#xA;` (a fixed point, but a numeric reference in the user's file).
  - Sol (2): #1 **blocker** `packages/editor/src/toggle.ts:628` a flush inside a continuous source typing burst splits undo history when the continuation outlasts one coalescing window (reproduction in `/report/probe.mjs` + `probe.log`; gate row "store-owned undo preserves the continuous typing group (§6.5, 1.6/1.24)" = fail); #2 nit `docs/V1.1-BACKLOG.md:44` the stated reason for deferring the emphasis-edge case (unreachable without a mark-toggle command) is false — `a *b* c`, caret after `b`, type a space reproduces it.
  - Grok (2): #1 nit `packages/editor/test/position-map-editor-leg.test.ts:180` `expect(names.length).toBe(Object.keys(index).length)` is `x === x`; #2 nit the wrapper drift above. Grok's gate table also records, for post.1: "Rated lower: G1 (Grok nit vs Claude blocker / Sol should-fix Required)" as its own r1 miss.
  - Shared: only the wrapper drift (Claude #4 should-fix, Grok #2 nit, absent from Sol). The two blockers are distinct defects (trailing hard break vs. undo split past one window) and each is unique to its reviewer. Unique-finding count by reviewer, from the numbered items: Claude 5 of 6, Sol 2 of 2, Grok 1 of 2. No reviewer change is proposed; none is allowed inside a review set (#review-1-r0).
- **Rotation decision**: `PRINCIPAL 1.10.r2d` ends the working block; the reconciliation is a fresh session (#015), so this session rotates without opening `.wt/1.10.r2d`.

## Gotchas

- **The reconciliation's first moves** in `.wt/1.10.r2d` (branch `task/1.10.r2d` at `c9408ee`): `git cherry-pick handoff/014`, then `handoff/015`, then `handoff/016`. All three touch `docs/handoffs/next-prompt.md` from the same base (`b149f0c`'s version): expect a conflict on `next-prompt.md` at the second and third pick — resolve to the incoming (later) version each time — and none on the three `NNN-*.md` files. Record the resolution in #review-1-r2 the way #review-1-r1 recorded the 012/013 one.
- **Two of three reviewers say FAIL on two different blockers, each backed by a browser reproduction** (Claude's probe with `hard-break.md`'s shape; Sol's `/report/probe.mjs`). A FAIL reconciliation follows `b149f0c` (`task(1.10.r1d)`, #review-1-r1): fix tasks `1.29…` + `1.verify.r3` (needsCI, `refTemplate ci/1.verify.r3/a{n}`) + `1.10.r3a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `EXPECTED_COUNT` bumped, `1.close` rewired to depend on `1.10.r3d`, `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green (83 runner tests at `6a39a92`), the DECISIONS section with `verdict: FAIL` as its first line (`run.mjs` `verdictFromDecisions`). The reconciliation commit touches only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1). Claude #1 is 1.13/1.25 territory (`stripUnparsableWhitespace`'s ownership rule for zero-width items at a block's end — the CLAUDE.md rule on half-open slices names first, middle and last positions); Sol #1 is 1.24/1.26 territory; both are passed tasks, never edited; each fix is a new task.
- **Read the three bodies before sizing**: Claude #1/#5/#6 are one family (end-of-block `hard_break` / soft-break serialisation) and probably one task with the three positions tested; Sol #1's remedy interacts with 1.24's coalescing window and 1.26's test — the task text should name the corpus leg and the probe shape that must pass. Claude #2 and #3 are test-only gaps (the 1.27 pattern); Claude #3 is a direct consequence of #021's scope decision (spy over readText) and needs a disposition even if it is "accepted, recorded", because the reconciliation chose that scope.
- **Turn budget** (open since 012): `1.28` a1 passed on opus; `1.verify.r2.g1` a1 stopped at 11 turns by choice, not the cap. No new cap data this block; the number stays conditional on Michael's OK (#review-1-r1) and is decided at the Phase 2 boundary unless the r3 chain is planned first.
- **Reports**: copy `.evidence/reviews/1/r2/{claude,sol,grok}/report.md` to `docs/reviews/phase-1-r2-{claude,sol,grok}.md` verbatim (the r1 pattern; `docs/reviews/` holds `phase-1-r1-*` and `TEMPLATE.md`); Sol's `/report/` also holds `probe.mjs`, `probe.log`, `control.mjs`, `phase.diff`, `positions.diff`, `artifact-digests.log` and per-command logs — evidence, not for the tree.
- **Phase boundary items** (carried from 013/014/015): `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `ralph/PROMPT.md` alignment (Decisions above) if not taken in a fix chain; `gate.sh gc` for `ci/1.verify/a1`, `ci/1.verify.r2/a1` and whatever `ci/1.verify.r2.g1/a1` turns out to be; the `phase/1` push (#010); `git branch -D handoff/010 … handoff/016`; the conditional turn-budget `fix(runner)`; `1.25`'s multi-line journal entry and the stated-invariant sweep as Phase 2 planning items.
- **`1.verify.r2.g1`'s attempt-1 entries are in the tree**: a `Status: Blocked` journal line and a lessons line that recommends stopping on wrapper drift. The second lessons line (attempt 2) corrects the first. The reconciliation's #review-1-r2 should say which reading stands (the second), so the next sonnet verify task does not repeat the stop; do not edit either line (never edit an old line).
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers. A `Monitor` on `accepted.json`/`rejected.json` exits non-zero when its final `[ -f rejected.json ]` test fails; harmless, the event line is what matters.

## Next Steps

1. Fresh session (the reconciliation): `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael; `cd .wt/1.10.r2d && git cherry-pick handoff/014 && git cherry-pick handoff/015 && git cherry-pick handoff/016` (resolve `next-prompt.md` to the incoming version each time); read the three reports in full; the three reports to `docs/reviews/phase-1-r2-{claude,sol,grok}.md`; `DECISIONS.md#review-1-r2` with `verdict: PASS|FAIL` first, each finding numbered with its disposition, each r1 disposition (G1–G12) confirmed or reopened, the per-reviewer unique-findings table (post.1's input), the `ralph/PROMPT.md` drift disposition, the turn-budget note with counts read from `.evidence/state` at the time of writing; journal entry; `wip(1.10.r2d): …` with `<promise>DONE 1.10.r2d</promise>`; restart.
2. On FAIL (the expected path): fix tasks `1.29…` + `1.verify.r3` + `1.10.r3a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `1.close` rewired, the way `b149f0c` did it; the fix chain runs (three attempts each before `STUCK`; a `NO-JOURNAL` at a1 is restarted with the same run command; a re-cut per #016 with `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart) → `1.verify.r3` → `HUMAN_GATE 1.verify.r3h` → rotate there regardless of context.
3. On PASS: `docs/progress.md` regenerated in the same commit, restart → `1.close` → `/rotate` at the phase boundary (on `phase/2` from the host checkout, runner idle) → `git branch -D handoff/010 … handoff/016`; ask Michael for `scripts/gate.sh gc` and the `phase/1` push (#010).
4. Phase boundary idle window: the `chore(0.0)` commits above (rotate.md step 5 third case + stale `description`; `ralph/PROMPT.md` "Every iteration" aligned to CLAUDE.md step 2 if not already taken); post.1's inputs include the per-phase unique-findings table (PRD §8 post.1).

## Open Questions

- Whether Claude #1/#5/#6 (end-of-block `hard_break`, heading, table cell) is one fix task or two, and whether the table-cell `&#xA;` nit is in scope at all — after reading the three bodies.
- Whether Sol #1's remedy (a flush that does not close the undo group while a burst continues) is a `toggle.ts` change, a `store.ts` change, or both — and which of 1.24/1.26's tests must stay green.
- Whether Claude #3 (no real clipboard write exercised after 1.28) gets a Windows-safe test (assert `writeText` was called with the spy *and* one non-Windows real round trip is impossible without `process.platform`, which the rules forbid) or an "accepted, recorded" disposition.
- The turn-budget number: still 80 proposed, conditional; no new cap data from this block.
