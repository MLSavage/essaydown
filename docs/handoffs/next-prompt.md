Read docs/PRINCIPAL.md, then continue from docs/handoffs/002-phase-0.md. Task 0.0 is passed on phase/0; `.evidence/state/` is initialised; the runner (`ralph/ralph.sh run --phase 0`) is in tmux window `runner` of session `essaydown` and starts as soon as the container's Claude login exists. Do not re-audit the plan; do not rebuild the runner.

Your job this session, in order:

1. Read docs/handoffs/002-phase-0.md, `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, and `.evidence/state/summary.md`. Give Michael the one-line state.
2. If the container logins are not done (`docker compose run --rm --entrypoint claude claude-task auth status` says not logged in), give Michael the three login commands one per message: `docker compose run --rm claude-login` (type `/login`, finish in the browser, `/exit`), `docker compose run --rm codex-login`, `docker compose run --rm grok-login`. Also `git -C .ext/build-defaults push` if `git -C .ext/build-defaults status -sb` shows `ahead`.
3. Watch the first three transcripts in `.evidence/tasks/<id>/<n>.log` (0.1, 0.2, 0.3). On `STUCK`, `NO-JOURNAL` or `NO-COMMIT`, read the transcript, name the cause in one line, fix it at the right level (PROMPT.md/CLAUDE.md before 0.1 integrates; `docs/lessons.md` and the next task afterwards), then `ralph/ralph.sh retry <id>` and restart `run` in the runner window (`tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0' Enter`).
4. On `HUMAN_GATE <id>` give Michael the command `scripts/gate.sh <id>` (CI gates need no flags), report the outcome line it prints, restart `run`. On `PLAN-GATE plan.<gate>.r<n>`: `ralph/ralph.sh plan <request-id>`, write the fix tasks + replacements into PRD §8 and regenerate `ralph/tasks.json` in `.wt/<request-id>`, commit with `<promise>DONE <request-id></promise>`, restart `run`.
5. On `PRINCIPAL 0.12.r0d`: reconcile the three reports per RUNNER-SPEC §5.4 in `.wt/0.12.r0d` (copy reports to docs/reviews/phase-0-r0-{claude,sol,grok}.md, DECISIONS.md#review-0-r0 with `verdict: PASS|FAIL`, lessons, regenerated docs/progress.md; on FAIL the r1 tasks in PRD §8 + tasks.json), journal line, commit with the DONE promise, restart `run`; it integrates, syncs state and runs `0.close`. Then write docs/handoffs/003-phase-1.md and rewrite this file.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks; every change you make is an atomic commit whose message says how to reverse it; when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Write handoff 003 at half context even if Phase 0 is not closed.
