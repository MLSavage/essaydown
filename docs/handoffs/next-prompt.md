Read docs/PRINCIPAL.md, then continue from docs/handoffs/008-phase-1-start.md (in the tree on `phase/1`; it supersedes 007-phase-0-verify-r2-gate.md; the reconciliation pattern to copy is DECISIONS #review-0-r2 in commit 052e206, `git show --stat 052e206`). Phase 0 is closed: review attempt r2 was reconciled PASS, `0.close` moved `main` to 052e206 and created `phase/1`; the runner is idle at the phase boundary, exited at `<promise>COMPLETE</promise>`, and has not yet been started for Phase 1. Next: start the runner with `--phase 1`, tasks 1.1–1.8, human gate 1.9, 1.verify, gate 1.verifyh, review set 1.10, reconciliation 1.10.r0d, 1.close. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC. The reconciliation `PRINCIPAL 1.10.r0d` is run by a fresh session: rotate at `HUMAN_GATE 1.verifyh` regardless of context (DECISIONS #015), with the handoff on `handoff/009` (rotate.md step 5).

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|CLOSED)|^<promise>' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the watcher from handoff 008 with the `Monitor` tool (persistent, 30 s poll), not a background shell.
2. Ask Michael in one line whether the deferred H1 runner fix (DECISIONS #review-0-r2; `lockHolder` reports an unreadable lock file as a dead holder) is done now as a `fix(runner)` commit on `phase/1` with a conformance leg, or left in the backlog (recommended). Then start the runner: `.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`. Confirm `[ralph] 1.1 attempt 1`.
3. Tasks 1.1–1.8 (opus/sonnet, three attempts each before `STUCK`; re-cut per DECISIONS #016 with a `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart). Tell Michael before `1.verify` that `1.verifyh` is expected to fail once on the missing `playwright-report` artifact unless a Phase 1 task extended `.github/workflows/ci.yml` (handoff 008 Gotchas); the designed route is `GATE-FAILED` → `ralph/ralph.sh plan plan.1.verifyh.r0` → planning commit adding `1.verify.g1` + its gate physically in PRD §8 (RUNNER-SPEC §2 rule (a), reviewers rewired, generator and validator green, `EXPECTED_COUNT` bumped) with `<promise>DONE plan.1.verifyh.r0</promise>`, restart.
4. `HUMAN_GATE 1.9` (observation gate, `recordTarget 002-outline-feel`): give Michael the `pnpm dev` procedure from the task text and `cd ~/Developer/essaydown && scripts/gate.sh 1.9`; read the `payload` of `.evidence/human/1.9/accepted.json`, never the runner outcome; restart → `1.verify` → `HUMAN_GATE 1.verifyh` → `scripts/gate.sh 1.verifyh` → expect `ACCEPT 1.verifyh a1 run <id>` with `test-logs` (three OS directories) and `playwright-report`, then `ROTATE-PRINCIPAL` → `/rotate` (handoff 009 on `handoff/009` from a temporary worktree) and hand Michael the relaunch line.
5. The fresh session then: restart → `1.10.r0a/b/c` (`.evidence/reviews/1/r0/verifier_id` must read `1.verify` or `1.verify.g<n>`, both SHAs equal the verifier's integrated SHA; `docker top` of the claude-review container shows `--max-turns 160`) → `PRINCIPAL 1.10.r0d`: reconcile in `.wt/1.10.r0d` per the r2 pattern, starting with `git cherry-pick handoff/009`; copy the 1.9 record payload to `DECISIONS.md#002-outline-feel`; `verdict: PASS|FAIL` first in `DECISIONS.md#review-1-r0`; H1–H3 and H2's re-pointing disposed; `docs/progress.md` regenerated; on FAIL fix tasks + `1.verify.r1` + `1.10.r1a/b/c/d` physically in PRD §8, close rewired; `bash ralph/test/run.sh` green; journal line; `wip(1.10.r0d): …` with `<promise>DONE 1.10.r0d</promise>`; restart → `1.close` → `/rotate` → `git branch -D handoff/009`.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live, and before staging there run `doctor` (clean), `git reset --hard phase/1` **before** writing your edits (a reset after writing discards them), confirm `git log -1 phase/1` (DECISIONS #017); never commit on `phase/1` between `1.verify` and `1.close` — a principal commit then goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note and Michael's OK; after any `docker/` commit integrates, `docker compose -f docker/compose.yml build` (with `ESSAYDOWN_ROOT`) before the next container task that can observe it (#review-0-r2 deviation 34); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at half context even if no gate is open.
