Read docs/PRINCIPAL.md, then continue from handoff 026: `git show handoff/026:docs/handoffs/026-phase-1-review-r6-reconciliation-pending.md` (it is on branch `handoff/026`, stacked on `handoff/025` which is not yet in the tree — `git log --oneline phase/1..handoff/026` shows three commits: 025's handoff, DECISIONS #029 at `d2e5e3b`, 026's handoff; it supersedes 025-phase-1-verify-r6-gate.md; the FAIL reconciliation pattern is `task(1.10.r5d)` at `159c4c0` with #review-1-r5, the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2). Phase 1 stands at `PRINCIPAL 1.10.r6d`: `phase/1` tip `c2f6526` (`task(1.verify.r6)`), gate `1.verify.r6h` passed at a2 (run 34737435949, 88/88/88) after a transient windows-only failure at a1 (#029), review set `1.10.r6a/b/c` all passed at attempt 1 and started within 200 ms, verdicts FAIL / FAIL / FAIL (Claude 1 blocker + 7 should-fix + 2 nits, Sol 1 blocker + 2 should-fix, Grok 1 blocker), reports in `.evidence/reviews/1/r6/{claude,sol,grok}/report.md`. The runner is stopped at the prompt in tmux `essaydown:runner` (pane pid 8940) after `PRINCIPAL 1.10.r6d`; `.locks/` empty, no containers; `.wt/1.10.r6d` exists on `task/1.10.r6d`. You are the fresh session that runs the reconciliation (DECISIONS #015). Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`, `git log --oneline phase/1..handoff/026 | wc -l` (must print 3). Give Michael the one-line state and say the reconciliation is starting; nothing is needed from him until the verdict.
2. The reconciliation `1.10.r6d` in `.wt/1.10.r6d` on `task/1.10.r6d`: `git cherry-pick phase/1..handoff/026`; copy the three reports byte-identically to `docs/reviews/phase-1-r6-{claude,sol,grok}.md` (`cmp` clean); reproduce every blocker and Required finding in a detached scratch worktree at `c2f6526` (Sol's and Grok's shared blocker at `packages/core/src/format.ts:193`, the two-reference astral case `format(parse('*a.*&#x1F600;*(b)*'))`; Claude's blocker at `format.ts:262–267`, the autolink-literal backslash doubling under `parse ∘ format`; the shared Required at `positions.ts:540/562`, the `html` rewrite missing from the enumeration; Claude's Required at `toggle.ts:392`, the inline-code caret); write `#review-1-r6` in `docs/DECISIONS.md` with the verdict first (PRD §7 criteria), K1–K9 each confirmed or reopened against the tree, every finding dispositioned with its reproduction, the reviewer record counted from the reports (two blocker families: one shared by Sol and Grok, one unique to Claude; unique and shared counts per reviewer; Grok independent because it started with its siblings), the turn-budget paragraph from the transcripts, the r6 gate's a1/a2 record quoted from #029; on FAIL append the r7 chain to `ralph/tasks.json` on the `159c4c0` pattern (fix tasks from `1.49`, each with acceptance sentences the tests discharge and the CLAUDE.md rules named; `1.verify.r7` `needsCI` with `ci/1.verify.r7/a{n}` and the artifact names; `1.verify.r7h`; `1.10.r7a/b/c/d`; `1.close` re-pointed at `1.10.r7d`), `ralph/EXPECTED_COUNT` updated, `ralph/ralph.sh generate-tasks`, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green; one journal line; a commit touching only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` with `<promise>DONE 1.10.r6d</promise>` in its message. On PASS the runner runs `1.close`.
3. Idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, `doctor` clean), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`; arm the runner.log watcher from handoff 026 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first). Watch the r7 chain: a capped attempt with the work complete on the branch → `chore(<id>): principal lesson` on its task branch, logs moved aside, `retry`; a 429 in a transcript → wait for the reset Michael names, then the plain restart; the verifier's gate → hand Michael `scripts/gate.sh 1.verify.r7h` (he runs it in his shell; read his background task's output file), a windows-only single-case failure with the spec unchanged and no editor runtime change → the #023/#029 rerun, then the abandon procedure.
4. `/rotate` at `HUMAN_GATE 1.verify.r7h` (the next reconciliation is a fresh session, DECISIONS #015) and at half context.
5. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)` (two instances, #029); H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5 (the stacked-handoff rule with its "already in the tree" condition, the third case), the stale `description` line, PRINCIPAL.md's doctor sentence and PRINCIPAL.md gaining Sol's r4 lesson, #028's, the r5 principal lessons and handoff 025's description-vs-acceptance lesson; `git branch -D handoff/010 … handoff/026`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 and handoff 025 Decisions), the 429 signal (#027, two instances), the gate fetch-on-failure (#review-1-r3 I7; a1 of r6h fetched nothing), the reviewer-prompt option.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree (cut from `phase/1`, or stacked on the newest `handoff/NNN` only while that one is not yet in the phase branch, so the reconciliation cherry-picks one range) and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (`git clean -fdX` there if it happened) — reproduce in a detached scratch worktree; a principal lesson for a task that lost an attempt goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message) — a lesson line orients, it never widens a task's scope (#028), and when a description clause and the acceptance disagree the acceptance binds; before `ralph/ralph.sh retry <id>` move every `.evidence/tasks/<id>/<n>.log` aside with a suffix (#027), and the lesson tells the agent to append a NEW `- [<id>] ` journal line with a `printf` that prints the timestamp and to run the suite once (an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed, and a capped attempt without a stub is `NO-JOURNAL` too; a lesson telling it to skip the suite is refused); a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023, #025, #026–#029 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor (#022, #024); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024); a same-SHA rerun is only for a transient failure and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023, #029); when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern; `scripts/gate.sh` outruns Claude Code's 120 s foreground limit and is backgrounded — read its output file. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
