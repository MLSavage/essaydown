Read docs/PRINCIPAL.md, then continue from docs/handoffs/006-phase-0-verify-r1-gate.md (it supersedes 005-phase-0-fix-chain-running.md; read 002-phase-0-running.md once for the gate evidence pattern and the reconciliation checklist in its Next Steps 4). Phase 0 review attempt r0 was reconciled FAIL (DECISIONS #review-0-r0); the fix chain 0.14–0.22 and 0.verify.r1 have all passed at attempt 1; the runner is idle at `HUMAN_GATE 0.verify.r1h`. Next: the gate, review attempt r1 (0.12.r1a/b/c/d), then 0.close. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC. The reconciliation `PRINCIPAL 0.12.r1d` is run by a fresh session — this one (the session that ran the fix chain rotated at the gate, DECISIONS #015).

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|ROTATE-PRINCIPAL)' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the watcher from handoff 006 (from the log's current end, never a negative offset; the `COMPLETE` alternative is anchored).
2. Give Michael `cd ~/Developer/essaydown && scripts/gate.sh 0.verify.r1h`. Expect `ACCEPT 0.verify.r1h a1 run <id>` and the advisory `ROTATE-PRINCIPAL` (already done; ignore). Confirm `.evidence/ci/0.verify.r1h/accepted.json` and that `test-logs/` holds the three OS directories. On GATE-FAILED: `ralph/ralph.sh plan plan.0.verify.r1h.r0`, planning commit adding `0.verify.r1.g1` + its gate physically in PRD §8 (RUNNER-SPEC §2 rule (a), reviewers rewired, generator and validator green), `<promise>DONE plan.0.verify.r1h.r0</promise>`, restart. Transient CI failure → `scripts/gate.sh rerun 0.verify.r1h`.
3. On ACCEPT restart the runner with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter` once `tmux capture-pane -p -t essaydown:runner | tail -3` shows the shell prompt (exactly one runner process; this start is the first to load the 0.18–0.20 runner code, deviation 28). → 0.12.r1a/b/c: watch `.evidence/reviews/0/r1/*/transcript.log` and `exit-code`; `verifier_id` must read `0.verify.r1` and both SHAs equal `5649a93`. A failed reviewer → `STUCK 0.12.r1<x>` → read the transcript's final `result` event (rate-limit banner → last `rate_limit_event`, wait for `resetsAt`), `ralph/ralph.sh retry 0.12.r1<x>`, restart.
4. `PRINCIPAL 0.12.r1d`: reconcile in `.wt/0.12.r1d` per 002 Next Steps 4 and handoff 006 Next Steps 4: `verdict: PASS|FAIL` as the first `verdict:` text of `DECISIONS.md#review-0-r1`; each r0 disposition F1–F21 confirmed or reopened; Michael's `zod` ratification sentence, the install-time lesson and runner deviations 27–28 recorded there (handoff 006 Decisions); `docs/progress.md` regenerated; on FAIL fix tasks + `0.verify.r2` + `0.12.r2a/b/c/d` physically in PRD §8 with `node ralph/generate-tasks.mjs` and `node ralph/validate-tasks.mjs` green, close rewired to `0.12.r2d`; on PASS the diff since `implementation_sha` stays within `docs/**`, `DECISIONS.md`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`. Journal line; `wip(0.12.r1d): …` with `<promise>DONE 0.12.r1d</promise>`; restart → `0.close`. After that commit integrates, no principal commits on `phase/0` (RUNNER-SPEC §8.1).
5. After `0.close`: `/rotate` (handoff 007 for Phase 1; the runner is then started with `--phase 1`).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the state transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live except the `/rotate` procedure under the runner lock, and before staging there run `doctor` (clean), `git reset --hard phase/0`, confirm `git log -1 phase/0` (DECISIONS #017); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` outside `/rotate` (0.20's token lock); never append to DECISIONS.md or the journal on `phase/0` while a task is in flight; after any `docker/` commit integrates, `docker compose build` before the next container task (#review-0-r0 F17); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at half context even if no gate is open.
