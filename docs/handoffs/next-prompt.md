Read docs/PRINCIPAL.md, then continue from docs/handoffs/002-phase-0-running.md (it supersedes 002-phase-0.md). Phase 0 is running: 0.0–0.2 and gate 0.2h passed; task 0.3 (or a later one) is in flight or the runner has stopped on a signal. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `tail -c 2000 .evidence/runner.log | grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE)'`. Give Michael the one-line state.
2. If a task is running: re-arm the watcher with the exact command in the handoff (replace the task/attempt marker) and wait. If the runner has stopped: act on the signal, then restart it in tmux window `essaydown:runner` with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter`.
3. On `STUCK`/`NO-JOURNAL`/`NO-COMMIT`: read the transcript's final `result` event, name the cause in one line, put the findings into the task's worktree as lesson lines (`git -C .wt/<id> cherry-pick` of a phase/0 lessons commit), `ralph/ralph.sh retry <id>` if blocked, restart. Never commit on the host checkout unless `doctor` is clean, and never append to DECISIONS.md or the journal on phase/0 while a task is in flight.
4. On `HUMAN_GATE 0.verifyh`: give Michael `cd ~/Developer/essaydown && scripts/gate.sh 0.verifyh` (it waits for the three-OS run and prints ACCEPT or GATE-FAILED + PLAN-GATE). On ACCEPT restart the runner: the review set 0.12 starts (three reviewer containers in parallel; watch `.evidence/reviews/0/r0/*/transcript.log`).
5. On `PRINCIPAL 0.12.r0d`: reconcile per the handoff's step 4 in `.wt/0.12.r0d`, commit with `<promise>DONE 0.12.r0d</promise>`, restart; the runner integrates, syncs state and runs `0.close`. Then write docs/handoffs/003-phase-1.md and rewrite this file.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks; every change you make is an atomic commit whose message says how to reverse it; when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Write handoff 003 at half context even if Phase 0 is not closed.
