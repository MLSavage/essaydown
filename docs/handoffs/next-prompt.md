Read docs/PRINCIPAL.md, then continue from handoff 013: `git show handoff/013:docs/handoffs/013-phase-1-review-r1-reconciliation-pending.md` (it is on branch `handoff/013`, not on `phase/1`; it supersedes 012-phase-1-verify-r1-gate.md, on `handoff/012`; 010 and 011 are in the tree at `b269627`; the FAIL reconciliation pattern is `task(1.10.r0d)` at `b269627` with DECISIONS #review-1-r0, `git show --stat b269627`; the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2). Phase 1 stands at `PRINCIPAL 1.10.r1d`: `phase/1` tip `91a3de5` (`1.verify.r1`), gate `1.verify.r1h` accepted at attempt 1 (run 34437589101), review attempt r1 complete at attempt 1 for all three reviewers — Claude FAIL (1 blocker, 3 should-fix, 5 nits), Sol FAIL (4 should-fix, all required), Grok PASS (2 should-fix, 2 nits) — reports in `.evidence/reviews/1/r1/{claude,sol,grok}/report.md`, the runner is idle at the prompt, `.locks/` empty, no `ralph run` process, no containers, worktree `.wt/1.10.r1d` on `task/1.10.r1d` at `91a3de5` cut by the runner. You are the fresh session that runs the reconciliation `PRINCIPAL 1.10.r1d` (a reconciliation is never run by the session that watched the reviews, DECISIONS #015; the previous session ran the gate and watched r1 and rotated here). Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the watcher from handoff 013 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first), not a background shell.
2. The reconciliation, in `.wt/1.10.r1d` only (RUNNER-SPEC §8.1: `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`): `git cherry-pick handoff/012` then `git cherry-pick handoff/013` first (expect a conflict on `docs/handoffs/next-prompt.md` only; resolve to the 013 version; record it in #review-1-r1); read all three reports in full; copy them verbatim to `docs/reviews/phase-1-r1-{claude,sol,grok}.md`; `DECISIONS.md#review-1-r1` with `verdict: PASS|FAIL` as its first line (`run.mjs` `verdictFromDecisions` reads it), every finding numbered with a disposition, each r0 disposition F1–F16 confirmed or reopened, the per-reviewer unique-findings and shared-severity table (post.1's input; count from the reports, never from the handoff), the turn-budget decision (read the attempt counts from `.evidence/state/tasks.json` at the time of writing; handoff 013 Corrections lists the ids to check against) and the 1.17 25× decision, both with Michael; journal entry (`docs/progress/journal-main.md`); on PASS `docs/progress.md` regenerated in the same commit; `wip(1.10.r1d): …` with `<promise>DONE 1.10.r1d</promise>`; then `.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
3. On FAIL (the expected path — two of three reviewers say FAIL and two findings are shared by all three): fix tasks + `1.verify.r2` + `1.10.r2a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `EXPECTED_COUNT` bumped, `1.close` rewired to `1.10.r2d`, `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green, the way `b269627` did it; a fix for a passed task is a new task, never an edit of the passed one (1.17 owns the Copy-Markdown regression; the `INLINE_WHITESPACE` decision owns the soft-break collapse). After the restart the fix chain runs (three attempts each before `STUCK`; a `NO-JOURNAL` at a1 is restarted with the same run command after `.locks/` empty, no `ralph run` process, `doctor` clean, pane at the prompt; a re-cut per DECISIONS #016 with a `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart; after any `docker/` commit integrates — the turn-budget change if taken — `docker compose -f docker/compose.yml build` with `ESSAYDOWN_ROOT` before the next container task) → `1.verify.r2` → `HUMAN_GATE 1.verify.r2h` → `/rotate` there regardless of context (handoff 014 on `handoff/014`).
4. On PASS: restart → `1.close` → `CLOSED` → `/rotate` at the phase boundary (on `phase/2` from the host checkout, runner idle) → `git branch -D handoff/010 handoff/011 handoff/012 handoff/013`; ask Michael for `scripts/gate.sh gc` (or `git push origin --delete ci/1.verify/a1 ci/1.verify.r1/a1`) and the `phase/1` push (#010).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md; a command you hand Michael never contains a placeholder or example text to edit in place — ask for the text first and hand the finished command, or leave a blank so obviously unfilled that running it as written fails (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; tests never encode one platform's chord or a magnitude from one run; when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
