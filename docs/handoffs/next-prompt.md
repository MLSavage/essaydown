Read docs/PRINCIPAL.md, then continue from handoff 019: `git show handoff/019:docs/handoffs/019-phase-1-review-r3-reconciliation-pending.md` (it is on branch `handoff/019`, not on `phase/1`, together with `9bcd691` = DECISIONS #023; it supersedes 018-phase-1-verify-r3-g1-gate.md on `handoff/018` and 017-phase-1-verify-r3-gate.md on `handoff/017`; 014–016 are in the tree since `e6d7493`, 010–013 since `b149f0c`; the FAIL reconciliation patterns are `task(1.10.r2d)` at `e6d7493` with #review-1-r2 and `task(1.10.r1d)` at `b149f0c` with #review-1-r1; the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2). Phase 1 stands at `PRINCIPAL 1.10.r3d`: `phase/1` tip `f90ed50` (`task(1.verify.r3.g1)`), gate `1.verify.r3.g1h` passed at a2 after a transient rerun (a1's windows timeout; DECISIONS #023 on `handoff/019`), `1.10.r3a/b/c` passed at attempt 1 (Claude FAIL 1 blocker, Sol FAIL 1 blocker, Grok PASS per `status.json`; the bodies are the signal), `1.10.r3d` principal-pending with its worktree `.wt/1.10.r3d` on `task/1.10.r3d` at `f90ed50`. The runner is stopped at the prompt in tmux `essaydown:runner` (pane pid 8940) after `PRINCIPAL 1.10.r3d`; `.locks/` empty, no containers. You are the fresh session that runs the reconciliation `1.10.r3d` (DECISIONS #015: never the session that ran the phase's tasks or watched its review set). Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the runner.log watcher from handoff 019 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first).
2. The reconciliation, in `.wt/1.10.r3d` only: `git cherry-pick phase/1..handoff/017 && git cherry-pick phase/1..handoff/018 && git cherry-pick phase/1..handoff/019` (check each range with `git log --oneline phase/1..handoff/NNN` first: one, one, two commits); copy `.evidence/reviews/1/r3/{claude,sol,grok}/report.md` verbatim to `docs/reviews/phase-1-r3-{claude,sol,grok}.md`; reproduce each blocker yourself before dispositioning it (Sol 1: `fixtures/markdown/nested-lists.md`, paste `X\nY` into the first paragraph, Copy Markdown, compare with `format(parse(...))`; Claude 1: a `hard_break` as the last node of an emphasis/strong/delete run with unmarked text after it, through `format`); write `DECISIONS.md#review-1-r3` after `#023` with `verdict: PASS|FAIL` first — each finding numbered with its disposition, H1–H9 confirmed or reopened, the per-reviewer unique-findings table re-derived from the bodies, the `chore(1.29)` principal-lesson commit audited as a deviation of the #016 shape, #022's scope decision and the four backlogged chord uses read against Claude 5 / Sol 3 / Grok 2 (the revisit trigger fired), `#023` cited against Claude 4, the two CLAUDE.md promote candidates from handoff 018 ("each attempt appends its own journal line"; the Blink-only caret rule) plus the `[0.0]` doctor lesson on `handoff/019` carried into the next verify task's promote-lessons text, the turn-budget note with counts read from `.evidence/state/tasks.json` and the transcripts at the time of writing; backlog lines (the `#023` boundary `fix(runner)`, hard stop `2.1`; Claude 6 and 8 name backlog triggers that have fired — disposition them, do not re-defer); append class-level lessons; regenerate `docs/progress.md`; on FAIL append `1.34…` fix tasks + `1.verify.r4` + `1.10.r4a/b/c/d` in PRD §8 and `ralph/tasks.json` the way `e6d7493` did (`node ralph/generate-tasks.mjs`, `ralph/EXPECTED_COUNT` from the generator's printed count, `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green); commit `wip(1.10.r3d): …` with `<promise>DONE 1.10.r3d</promise>` in the message, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1).
3. Idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, `doctor` clean), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`. The runner squashes `task(1.10.r3d)`, runs `sync-state`, then the first `1.34…` task (FAIL) or `1.close` (PASS). `/rotate` at the next stop signal: `HUMAN_GATE 1.verify.r4h` (hand Michael `scripts/gate.sh 1.verify.r4h`; on `GATE-FAILED` the planning commit is `#024` per `de5dcf2`; after any `gate.sh rerun` that accepts, `doctor` flags the rejected earlier attempt — never run its `--resume`, use `#023`'s abandon) or `CLOSED`.
4. Phase boundary idle window (only after `1.close`, from the host checkout on `phase/2` with the runner idle): `#023`'s `fix(runner)` (doctor's recorded clause for blocked → rerun → passed; `--resume` refused on a superseded attempt; one conformance test per guard plus `doctor` clean at the end of the transient-rerun test); H7's `fix(runner)` — `ralph/PROMPT.md` "Iteration" steps 2–6 replaced by CLAUDE.md's steps 2–5 verbatim, `tests/agent-rules.test.ts` widened; `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `git branch -D handoff/010 handoff/011 handoff/012 handoff/013 handoff/014 handoff/015 handoff/016 handoff/017 handoff/018 handoff/019`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); the conditional turn-budget `fix(runner)` only on Michael's OK.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); a principal lesson for a task that lost an attempt on procedure goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message), followed by `ralph/ralph.sh retry <id>` only if the task is `blocked` and by the same restart command otherwise — the runner counts one new `- [<id>] ` journal line per attempt, so an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023 is the pattern); a command you hand Michael never contains a placeholder or example text to edit in place — ask for the text first and hand the finished command, or leave a blank so obviously unfilled that running it as written fails (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e places the caret only through motions Blink alone decides (#022); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022); a same-SHA rerun is only for a transient failure — a file unchanged since its last green run on that OS, a hang before any assertion — and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023); when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
