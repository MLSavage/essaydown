Read docs/PRINCIPAL.md, then continue from docs/handoffs/007-phase-0-verify-r2-gate.md (on branch `handoff/007`, not in the phase/0 tree: `git show handoff/007:docs/handoffs/007-phase-0-verify-r2-gate.md`; it supersedes 006-phase-0-verify-r1-gate.md; read 002-phase-0-running.md once for the gate evidence pattern and the reconciliation checklist in its Next Steps 4). Phase 0 review attempt r1 was reconciled FAIL (DECISIONS #review-0-r1); the fix chain 0.23–0.26 and 0.verify.r2 have all passed at attempt 1; the runner is idle at `HUMAN_GATE 0.verify.r2h`. Next: the gate, review attempt r2 (0.12.r2a/b/c/d), then 0.close. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC. The reconciliation `PRINCIPAL 0.12.r2d` is run by a fresh session — this one (the session that ran the r1 reconciliation and directed the r2 chain rotated at the gate, DECISIONS #015).

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|ROTATE-PRINCIPAL)' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the watcher from handoff 007 with the `Monitor` tool (persistent, 30 s poll), not a background shell — the harness killed three background watchers this run under memory pressure.
2. Give Michael `cd ~/Developer/essaydown && scripts/gate.sh 0.verify.r2h`. Expect `ACCEPT 0.verify.r2h a1 run <id>` and the advisory `ROTATE-PRINCIPAL` (already done; ignore). Confirm `.evidence/ci/0.verify.r2h/accepted.json` (`sha` 98bee5a…) and that `test-logs/` holds the three OS directories. On GATE-FAILED: `ralph/ralph.sh plan plan.0.verify.r2h.r0`, planning commit adding `0.verify.r2.g1` + its gate physically in PRD §8 (RUNNER-SPEC §2 rule (a), reviewers rewired, generator and validator green), `<promise>DONE plan.0.verify.r2h.r0</promise>`, restart. Transient CI failure → `scripts/gate.sh rerun 0.verify.r2h`.
3. On ACCEPT: `.locks/` must be empty (0.25's code, live from this start, refuses a dead-pid lock instead of breaking it); restart the runner with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter` once `tmux capture-pane -p -t essaydown:runner | tail -3` shows the shell prompt (exactly one runner process). → 0.12.r2a/b/c: `.evidence/reviews/0/r2/verifier_id` must read `0.verify.r2` and both SHAs equal `98bee5a`; `docker top` of the claude-review container shows `--max-turns 160`. A failed reviewer → `STUCK 0.12.r2<x>` → read the transcript's final `result` event (`max_turns` → budget; rate-limit banner → wait for `resetsAt`), copy `transcript.log` aside, `ralph/ralph.sh retry 0.12.r2<x>`, restart.
4. `PRINCIPAL 0.12.r2d`: reconcile in `.wt/0.12.r2d` per 002 Next Steps 4 and handoff 007 Next Steps 4, starting with `git cherry-pick handoff/007`: `verdict: PASS|FAIL` as the first `verdict:` text of `DECISIONS.md#review-0-r2`; each r1 disposition G1–G13 confirmed or reopened; deviations 33–34 recorded there; `docs/progress.md` regenerated; on FAIL fix tasks + `0.verify.r3` + `0.12.r3a/b/c/d` physically in PRD §8 with `node ralph/generate-tasks.mjs` and `node ralph/validate-tasks.mjs` green, close rewired to `0.12.r3d`; on PASS the diff since `implementation_sha` stays within `docs/**`, `DECISIONS.md`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`. `bash ralph/test/run.sh` green. Journal line; `wip(0.12.r2d): …` with `<promise>DONE 0.12.r2d</promise>`; restart → `0.close`. After that commit integrates, no principal commits on `phase/0` (RUNNER-SPEC §8.1).
5. After `0.close`: `/rotate` (handoff 008 for Phase 1; the runner is then started with `--phase 1`); delete `handoff/006` and `handoff/007` once the reconciliation commits carry their content; then, in Phase 1's first idle window, the `chore(0.0)` commit on `phase/1` that updates `.claude/commands/rotate.md` step 5 and PRINCIPAL.md for the handoff branch (handoff 007 Next Steps 6).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the state transition in `ralph/lib/doctor.mjs` first); never commit on `phase/0` between the verifier and 0.close — not a handoff, not DECISIONS, not a lesson (DECISIONS #review-0-r1 deviation 29: a principal commit on the phase branch goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it); never commit on the host checkout while the runner loop is live, and before staging there run `doctor` (clean), `git reset --hard phase/0`, confirm `git log -1 phase/0` (DECISIONS #017); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.20's token lock; 0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/0` while a task is in flight; after any `docker/` commit integrates, `docker compose -f docker/compose.yml build` (with `ESSAYDOWN_ROOT`) before the next container task that can observe it (#review-0-r0 F17, reading in handoff 007 Decisions 34); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at half context even if no gate is open.
