Read docs/PRINCIPAL.md, then continue from handoff 028: `git show handoff/028:docs/handoffs/028-phase-1-verify-r7-gate.md` (it is on branch `handoff/028`, stacked on `handoff/027` which is not yet in the tree — `git log --oneline phase/1..handoff/028` shows nineteen commits: DECISIONS #030–#034, 027's handoff and its eight corrections, 028's handoff and four corrections; it supersedes 027-phase-1-r7-chain-1.51-stuck.md; the FAIL reconciliation pattern is `task(1.10.r6d)` at `695fb0f` with #review-1-r6, the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2). Phase 1 stands at `PRINCIPAL 1.10.r7d`: the r7 review set passed at attempt 1 from `5618078` (verifier `1.verify.r7.g2`, gate `1.verify.r7.g2h` ACCEPT a1, run 35688068381, 93/93/93), verdicts **Claude FAIL (2 blockers, 3 should-fix with 1 Required, 4 nits), Sol FAIL (1 blocker, 1 Required), Grok PASS (0/0/0)**; reports in `.evidence/reviews/1/r7/{claude,sol,grok}/report.md`; the runner is stopped at the prompt in tmux `essaydown:runner` after `PRINCIPAL 1.10.r7d`; `.locks/` empty, no containers; `.wt/1.10.r7d` exists on `task/1.10.r7d`. **You are the fresh session that runs the reconciliation (DECISIONS #015).** Read handoff 028's Corrections first: the headlines (Claude 1 the third `SPLIT_PAIR` form rewriting verbatim bytes inside code spans and inline html; Claude 2 = Sol 1 a hard break before inline html written as backslash-space; Claude 3 Required — task 1.54 integrated from a capped attempt because `transcriptHasDone` greps the whole transcript and the principal's lesson lines quoted the promise literal, verified; Sol 2 Required — `inlineCodeSpelling` misses the GFM table pipe escape) and the rule they add: never write the literal DONE promise into any file an agent reads. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`, `git log --oneline phase/1..handoff/028 | wc -l` (must print 19). Give Michael the one-line state and say the reconciliation is starting; nothing is needed from him until the verdict.
2. The reconciliation `1.10.r7d` in `.wt/1.10.r7d` on `task/1.10.r7d`: `git cherry-pick phase/1..handoff/028` (the `docs/DECISIONS.md` tail conflicts — #035 and #036 are in the tree, #030–#034 on the branch: keep both, #030–#034 after #036, `git cherry-pick --continue`, and record the resolution in the entry); copy the three reports byte-identically to `docs/reviews/phase-1-r7-{claude,sol,grok}.md` (`cmp` clean); reproduce every blocker and Required finding in a detached scratch worktree at `5618078` (`git worktree add --detach <scratchpad>/5618078 5618078`, `pnpm install --frozen-lockfile` there; never in `.wt/1.10.r7d`); write `#review-1-r7` with the verdict first (PRD §7 criteria), K1–K9 and L1–L11 each confirmed or reopened, #030–#036 recorded under their tasks (1.51's fixture and exclusion, 1.52's code-block exclusion, 1.53's pin, 1.54's integration from a capped attempt — the record defect, the tree green by the candidate suite and the gate —, the G4 route's four gate rounds), the #030 product backlog line written to V1.1-BACKLOG.md now, every finding dispositioned with its reproduction, the reviewer record counted from the reports (Grok PASS with blockers in the tree: 3 of 5 counted attempts; the `[review-1-r1, reviewer record]` condition — count before raising a pattern), the turn-budget paragraph (handoff 028's Decisions holds the r7 chain's turn table; add the review turns Claude 128 / Grok 26), the gates' record (r7h GATE-FAILED #035, r7.g1h GATE-FAILED #036, r7.g2h ACCEPT a1); on FAIL append the r8 chain from `1.57` on the `task(1.10.r6d)` pattern at `695fb0f` — fix tasks whose scope sentences name every file that pins the behaviour they change (grep the tests for the positions and bytes each fix re-decides), whose fixtures and scopes were run through the corpus legs in the scratch worktree first, whose acceptance tails name file-level greps and never the suite's total, and whose texts never quote the DONE promise; `1.verify.r8` `needsCI` with `ci/1.verify.r8/a{n}`; `1.verify.r8h`; `1.10.r8a/b/c/d`; `1.close` re-pointed at `1.10.r8d`; `ralph/EXPECTED_COUNT` updated, `generate-tasks`, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green; one journal line; a commit touching only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` with the DONE promise for 1.10.r7d in its message. On PASS the runner runs `1.close`.
3. Idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux pane at the prompt, `doctor` clean), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`; arm the runner.log watcher from handoff 028 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first; a FRESH one after every restart or `retry`). Watch the r8 chain: a capped attempt with the work complete → `chore(<id>): principal lesson` on its task branch (no promise literal), logs moved aside, `retry` — and before any `retry`, `git -C .wt/<id> diff <branch-base> --stat -- 'packages/*/src' fixtures` for a swept mutation; a withheld DONE on a task-text conflict → a DECISIONS entry stacked on `handoff/028` first, deciding which sentence yields under #028's rule; a 429 → wait for the reset Michael names, then the plain restart; the verifier's gate → hand Michael `scripts/gate.sh 1.verify.r8h`; a windows-only single-case failure → read the case before choosing between the #023/#029 rerun and a `.g<n>` repair.
4. `/rotate` at `HUMAN_GATE 1.verify.r8h` (the next reconciliation is a fresh session, DECISIONS #015 reproduces every blocker and Required finding in a detached scratch worktree at `fd03e40`, writes `#review-1-r7` with K1–K9 and L1–L11 each confirmed or reopened and #030–#034 recorded under their tasks, and on FAIL appends the r8 chain only after running the corpus legs over every fixture and scope it decides in that scratch worktree, with acceptance tails naming file-level greps and never the suite's total) and at half context.
5. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)` (two instances, #029); H7's `fix(runner)`; the recovery-commit guard (handoff 028 Decisions: a capped attempt's recovery commit swept a mid-mutation `packages/core/src/format.ts` onto `task/1.54`); `chore(0.0)` for rotate.md step 5 (the stacked-handoff rule with its "already in the tree" condition, the scratchpad worktree, the fresh-watcher rule), the stale `description` line, PRINCIPAL.md's doctor sentence and PRINCIPAL.md gaining Sol's r4 lesson, #028's, #030–#034's, the r5 and r6 principal lessons and handoff 025's description-vs-acceptance lesson; `git branch -D handoff/010 … handoff/028`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5, #review-1-r6 and the r7 chain's turn table in handoff 028), the 429 signal (#027, four instances), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree (cut from `phase/1`, or stacked on the newest `handoff/NNN` only while that one is not yet in the phase branch, so the reconciliation cherry-picks one range) and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (`git clean -fdX` there if it happened) — reproduce in a detached scratch worktree; a principal lesson for a task that lost an attempt goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message) — a lesson line orients, it never widens a task's scope (#028), and when a description clause and the acceptance disagree the acceptance binds; before `ralph/ralph.sh retry <id>` move every `.evidence/tasks/<id>/<n>.log` aside with a suffix (#027), and the lesson tells the agent to append a NEW `- [<id>] ` journal line with a `printf` that prints the timestamp and to run the suite once (an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed, and a capped attempt without a stub is `NO-JOURNAL` too; a lesson telling it to skip the suite is refused); a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023, #025, #026–#029 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor (#022, #024); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024); a same-SHA rerun is only for a transient failure and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023, #029); when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern; `scripts/gate.sh` outruns Claude Code's 120 s foreground limit and is backgrounded — read its output file. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
