Read docs/PRINCIPAL.md, then continue from docs/handoffs/005-phase-0-fix-chain-running.md (it supersedes 004-phase-0-verify-gate.md; read 002-phase-0-running.md once for the gate evidence pattern and the reconciliation checklist in its Next Steps 4). Phase 0 review attempt r0 was reconciled FAIL (DECISIONS #review-0-r0): the fix chain 0.14 → 0.22 is running (0.14 passed; 0.15 was in flight at the rotation), then 0.verify.r1, gate 0.verify.r1h, review attempt r1 (0.12.r1a/b/c/d), then 0.close. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC. The reconciliation `PRINCIPAL 0.12.r1d` is run by a fresh session: rotate at `HUMAN_GATE 0.verify.r1h` (DECISIONS #015).

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|ROTATE-PRINCIPAL)' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the watcher from handoff 005 (from the log's current end, never a negative offset).
2. Keep the runner going through 0.15–0.22 and 0.verify.r1: exactly one runner process; restart after every stop signal with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter` once `tmux capture-pane -p -t essaydown:runner | tail -3` shows the shell prompt. On `STUCK`/`NO-JOURNAL`/`NO-COMMIT`: read the transcript's final `result` event (rate-limit banner → read the last `rate_limit_event` and wait for `resetsAt` before restarting), name the cause in one line, steer via a lesson line committed on the task's own branch (`chore(<id>): principal lesson …`) or re-cut the branch per DECISIONS #016, `ralph/ralph.sh retry <id>` if blocked, restart. A retried agent ending without DONE and citing a conflict the lessons channel was meant to resolve → no third attempt: write the planning commit appending the fix task it asks for.
3. At any stop signal with the runner idle: `scripts/gate.sh gc` for the orphan remote ref `ci/0.verify/a1` (GATE-FAILED attempt); if gc refuses it, record the reading in DECISIONS at the next allowed moment (handoff 005 Next Steps 5), do not patch gc. Also carry Michael's `zod` ratification sentence and the install-time lesson (handoff 005 Corrections and Decisions) into the r1 reconciliation entry.
4. `HUMAN_GATE 0.verify.r1h` → `/rotate` (handoff 006), then tell Michael "ready to rotate" with the relaunch command. The fresh session gives Michael `cd ~/Developer/essaydown && scripts/gate.sh 0.verify.r1h`; on GATE-FAILED: `ralph/ralph.sh plan plan.0.verify.r1h.r0`, planning commit adding `0.verify.r1.g1` + its gate (RUNNER-SPEC §2 rule (a)), `<promise>DONE plan.0.verify.r1h.r0</promise>`, restart.
5. On ACCEPT the fresh session restarts the runner → 0.12.r1a/b/c (watch `.evidence/reviews/0/r1/*/transcript.log` and `exit-code`; a failed reviewer → `STUCK 0.12.r1<x>` → transcript, `ralph/ralph.sh retry 0.12.r1<x>`, restart) → `PRINCIPAL 0.12.r1d`: reconcile in `.wt/0.12.r1d` per 002 Next Steps 4 and handoff 005 Next Steps 4 (`verdict: PASS|FAIL` as the first `verdict:` text of `DECISIONS.md#review-0-r1`; each r0 disposition confirmed; on FAIL fix tasks + `0.verify.r2` + `0.12.r2a/b/c/d` physically in PRD §8 with `node ralph/generate-tasks.mjs` and `node ralph/validate-tasks.mjs` green, close rewired to `0.12.r2d`); journal line; `wip(0.12.r1d): …` with `<promise>DONE 0.12.r1d</promise>`; restart → `0.close`. After that commit integrates, no principal commits on `phase/0` (RUNNER-SPEC §8.1).
6. After `0.close`: `/rotate` (handoff for Phase 1; the runner is then started with `--phase 1`).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the state transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live except the `/rotate` procedure under the runner lock, and before staging there run `doctor` (clean), `git reset --hard phase/0`, confirm `git log -1 phase/0` (DECISIONS #017); a principal commit never deletes a path (0.19's doctor check); never append to DECISIONS.md or the journal on `phase/0` while a task is in flight; after any `docker/` commit integrates, `docker compose build` before the next container task (#review-0-r0 F17); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at half context even if no gate is open.
