Read docs/PRINCIPAL.md, then continue from docs/handoffs/009-phase-1-1.9-idle-window.md (in the tree on `phase/1`; it supersedes 008-phase-1-start.md; the reconciliation pattern to copy is DECISIONS #review-0-r2 in commit 052e206, `git show --stat 052e206`). Phase 1 stands at the `HUMAN_GATE 1.9` idle window: 1.1–1.8 passed, 1.9 is passed/ACCEPT but its record was written with unedited placeholder text; the audited repair is in the handoff's Decisions and is done only once `DECISIONS.md#repair-1.9` exists on `phase/1` — check that before anything else. The runner is idle (tmux `essaydown:runner`, `.locks/` empty) and has not been restarted for `1.verify`. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC. The reconciliation `PRINCIPAL 1.10.r0d` is run by a fresh session: rotate at `HUMAN_GATE 1.verifyh` regardless of context (DECISIONS #015), with handoff 010 on `handoff/010` (rotate.md step 5).

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`, `grep -c '#repair-1.9' docs/DECISIONS.md`. Give Michael the one-line state. Arm the watcher from handoff 009 with the `Monitor` tool (persistent, 30 s poll), not a background shell.
2. If `#repair-1.9` is absent: do the repair exactly as handoff 009 Decisions states (Michael's verbatim texts are there in summary; ask him to paste them again only if `.evidence/human/1.9/a2.md` does not exist), `doctor` clean, DECISIONS entry + backlog lines, one commit on `phase/1` from the host checkout with the #017 procedure (`doctor` clean, `git reset --hard phase/1` before writing, `git log -1 phase/1` confirmed). Ask Michael for the Markdown file that lost its bullet lists in BBEdit and the preview engine BBEdit is set to; it stays outside the tree.
3. Michael decided (DECISIONS #019): the designed `GATE-FAILED` round, for provenance; do not pre-empt it. Start the runner if it is not running: `.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`. Confirm `[ralph] 1.verify attempt 1`.
4. `HUMAN_GATE 1.verifyh`: `cd ~/Developer/essaydown && scripts/gate.sh 1.verifyh`; expect `GATE-FAILED 1.verifyh a1` on the missing `playwright-report` (no Phase 1 task touched `ci.yml`). Then **rotate first** (`/rotate`, handoff 010 on `handoff/010` from a temporary worktree, DECISIONS #015) and hand Michael the relaunch line. The fresh session runs `ralph/ralph.sh plan plan.1.verifyh.r0` → planning commit adding `1.verify.g1` + its gate physically in PRD §8 (RUNNER-SPEC §2 rule (a), `1.10.r0a/b/c` rewired, generator and validator green, `EXPECTED_COUNT` bumped) with `<promise>DONE plan.1.verifyh.r0</promise>`; the task text puts the failure in the job, not in the artifact check (DECISIONS #019): the workflow installs the Playwright browser, runs every spec file under `e2e/web/` against the Vite webServer on all three runners, fails the job on any failing spec (so `conclusion !== success` and `ciGate` refuses), and uploads the merged `playwright-report` as evidence for the reviewers, never as the pass condition; acceptance (a) no literal spec count — the count comes from the directory; (b) states plainly that a green `1.verify.g1h` proves the suite ran on all three runners at the integrated SHA; restart → `1.verify.g1` → `scripts/gate.sh 1.verify.g1h` → `ACCEPT` with `test-logs` and `playwright-report`.
5. Then `1.10.r0a/b/c` (`.evidence/reviews/1/r0/verifier_id` must read `1.verify.g1`, both SHAs equal the verifier's integrated SHA; `docker top` of the claude-review container shows `--max-turns 160`) → `PRINCIPAL 1.10.r0d`: reconcile in `.wt/1.10.r0d` per the r2 pattern, starting with `git cherry-pick handoff/010`; copy the 1.9 record `payload` and the `a2.md` record body to `DECISIONS.md#002-outline-feel`; dispose of the `&#x20;` trailing-space defect as a PRD §6 portability finding with a fix task (mechanism and the BBEdit engine experiment are in handoff 009 Decisions); re-point #review-0-r2 H2; `verdict: PASS|FAIL` first in `DECISIONS.md#review-1-r0`; `docs/progress.md` regenerated; on FAIL fix tasks + `1.verify.r1` + `1.10.r1a/b/c/d` physically in PRD §8, close rewired; `bash ralph/test/run.sh` green; journal line; `wip(1.10.r0d): …` with `<promise>DONE 1.10.r0d</promise>`; restart → `1.close` → `/rotate` → `git branch -D handoff/010`.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live, and before staging there run `doctor` (clean), `git reset --hard phase/1` **before** writing your edits (a reset after writing discards them), confirm `git log -1 phase/1` (DECISIONS #017); never commit on `phase/1` between `1.verify` and `1.close` — a principal commit then goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note and Michael's OK; after any `docker/` commit integrates, `docker compose -f docker/compose.yml build` (with `ESSAYDOWN_ROOT`) before the next container task that can observe it (#review-0-r2 deviation 34); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md; a command you hand Michael never contains a placeholder or example text to edit in place — ask for the text first and hand the finished command, or leave a blank so obviously unfilled that running it as written fails (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`origin/phase/1` is checked with `git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git log`/`status` output at the moment of writing, never carried from memory. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
