Read docs/PRINCIPAL.md (including its new "Model and escalation" section), then continue from handoff 037: `git show handoff/037:docs/handoffs/037-phase-1-review-r9-reconciliation-under-d1.md` (it is on branch `handoff/037`, stacked on `handoff/036` → `035` → `034` → `033`, which sits on `phase/1`'s `2507210`; `phase/1` has since moved two task commits to `287c884` with no change to DECISIONS.md, PRINCIPAL.md or docs/proposals/, so `git log --oneline phase/1..handoff/037` shows twelve commits, none in the tree: handoff 031, DECISIONS #038, handoff 032, handoff 033, DECISIONS #040, handoff 034, handoff 035, handoff 036, proposal 001 v3, DECISIONS #041, PRINCIPAL.md D2 section, handoff 037; it supersedes 036–031 and 030-phase-1-review-r8-reconciliation.md; the FAIL reconciliation pattern is `task(1.10.r8d)` at `3c96b9b` with #review-1-r8). Read DECISIONS #041 on the stack: `git show handoff/037:docs/DECISIONS.md | tail -n 12`. Phase 1 is at `PRINCIPAL 1.10.r9d`: gate `1.verify.r9.g1h` ACCEPT at a1 (run 35968837053 at `287c884`), the r9 review set done at attempt 1 each — Claude FAIL (1 blocker, 3 should-fix, 2 nits), Sol FAIL (0 blockers, 3 should-fix), Grok PASS (0/0/0), 0 shared findings. **You are the fresh session that runs the reconciliation `1.10.r9d` (DECISIONS #015), on Opus 5.5 under #041 D2; Fable is a subagent only on PRINCIPAL.md's trigger list, with a written brief, one at most here.** Michael decided D1 (#041): verdict FAIL; two fix tasks only; the rest backlogged; a scoped r10 round; a blocker in r10 comes to him before any fix chain. Michael answered N4: yes to `1.9.r1` (#040) — append it; nothing to ask. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`, `git log --oneline phase/1..handoff/037 | wc -l` (must print 12), `git branch --list 'handoff/03*'` (030 — a stale ref whose file is in the tree — 033, 034, 035, 036, 037). Give Michael the one-line state. Runner liveness: `.locks/`, `pgrep -f 'ralph/lib/cli.mjs ralph run'`, `docker ps`, the tmux pane `essaydown:runner` (expected: at the prompt after `PRINCIPAL 1.10.r9d`). The watcher is armed only after the restart (handoff 037's `Monitor` command: persistent, 30 s poll, 30 min cap, `setopt nullglob` first; a FRESH one after every restart or `retry` and on every expiry; `status` on every expiry notice). The host checkout may hold an untracked `docs/proposals/` identical to the stack's commit; leave it.
2. **The reconciliation `1.10.r9d`** in `.wt/1.10.r9d` on `task/1.10.r9d` (at `287c884`, clean), under #041 D1: read the three reports under `.evidence/reviews/1/r9/{claude,sol,grok}/report.md` and `status.json`; `git cherry-pick phase/1..handoff/037` first (twelve commits, `docs/**` only, conflict-free); write `docs/DECISIONS.md` #review-1-r9 on the #review-1-r8 pattern with `verdict: FAIL` (the runner's regex reads it) — each finding with file:line, severity and its #041 disposition (fix: Claude 1 → `1.67`, Claude 3 → `1.68`; backlog with a `2.verify` trigger, each with its native route from the report: Claude 2, 4, 5, 6, Sol 1, 2, 3), the counts (unique 6/3/0, shared 0), the reviewer record (Grok's third zero-finding PASS beside two FAILs recorded as a fact, no reconfiguration), the `[review-1-r9, turn budget]` evidence line (`1.64`'s six caps, `1.63`'s and `1.65`'s a1 caps, `1.66`'s two caps then its 31-turn pass), the gate's three-OS numbers and the block-local leg's 1,297/920/1,875 ms; copy the reports to `docs/reviews/phase-1-r9-{claude,sol,grok}.md`; the backlog lines in `docs/V1.1-BACKLOG.md` (the L11 line corrected on a new line); the promote-lessons candidates verbatim in `1.verify.r10`'s text; append to `ralph/tasks.json` in PRD §8 form: `1.67` (opus; Claude's blocker: read the installed `mdast-util-to-markdown` `emphasis`/`strong` handlers first, app handlers in `handleDelete`'s shape at `format.ts:131`, Claude's sweep D as the test — 5 × 5 mark kinds × {punctuation, letter, astral} first-run endings, 75 pairs, the corpus asserted as the invariant and the two broken pairs as named guards — plus the join of every adjacent paragraph pair and the deletion of the single separating space as the destructive writing-surface legs; `pnpm install --frozen-lockfile` named first), `1.68` (Claude 3: `e2e/web/editor-soft-line-breaks.spec.ts:181` under #037 — a bounded re-click reading the dev-bar readout or counted arrows from an asserted anchor, never a magnitude), `1.verify.r10` (`needsCI`, depends on `1.68`), `1.10.r10a/b/c` (Michael's scope verbatim: "confirming the two fixes and the backlog dispositions, blockers only, no new sweeps or browser drives"; the r9 dispositions and the backlog lines named so nothing is re-derived) and `1.10.r10d` (whose text says: a blocker reported in r10 goes to Michael before any planning commit), and `1.9.r1` per #040 (at `287c884`, `1.9`'s procedure plus the `[repair-1.9 (c)]` two readers, `1.close` depending on it beside `1.10.r10d`, the observation SHA named as the tree Michael drives); `ralph/EXPECTED_COUNT` from 276 by the number appended; regenerate `docs/progress.md`; `node ralph/lib/cli.mjs generate-tasks --check`, `validate-tasks`, `bash ralph/test/run.sh` with its count captured to a file and grepped; the journal line and the lessons lines; touch only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); the promise only in the `wip(1.10.r9d)` commit message, never in any file. A Fable brief (PRINCIPAL.md's section) only for what D1 leaves open — whether `1.67` splits, the exact r10 reviewer text — one at most. Then the idle checks (`.locks/` empty, `pgrep` empty, `docker ps` empty, the pane at the prompt, `doctor` clean), `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`, a fresh watcher, and the r10 chain: `1.67`, `1.68`, `1.verify.r10`, the CI gate `1.verify.r10h` (`scripts/gate.sh 1.verify.r10h`, backgrounded from the session with its output file read, or Michael's shell; on GATE-FAILED `gh run view <run id> --log-failed` first, a same-SHA rerun only for a transient failure (#023, #029), any tree change a `.g<n>` repair through `ralph/ralph.sh plan` on its `plan/<gate>/r<n>` branch — #037 and #039 are the pattern), the review set `1.10.r10a/b/c` (the only parallelism; a Grok 402 is never retried without Michael (#027); a single-reviewer retry follows #025; never reconfigure a reviewer inside the set; count first before raising a pattern) to `PRINCIPAL 1.10.r10d` — `/rotate` there; that reconciliation is a fresh session's, and a blocker in it is Michael's before any planning commit.
3. A capped attempt with the work built is the chain's pattern (the runner retries itself up to three). On `NO-JOURNAL <id>` the task stays `running` with its attempts kept and `retry` refuses it: the source-diff check first (`git -C .wt/<id> diff --stat <base> HEAD -- packages/editor/src packages/core/src` empty; a recovery commit can sweep a mutation), then one `[<id>]` lesson line committed as `chore(<id>): principal lesson …` on `task/<id>` in `.wt/<id>` (`docs/lessons.md` only, `Reverse: git revert this commit.`) that names the remaining step and the turn cost of each part read from the capped transcripts, hands the numbers already read, tells the agent to append a NEW `- [<id>] ` journal line with the `printf` that prints the timestamp, to `pnpm install --frozen-lockfile` first and to run the suite once — never a scope change, never the promise literal; then the idle checks, the restart, a fresh watcher. On `STUCK <id>` (the third cap, status `blocked`): the same lesson, the three logs moved aside with a suffix (#027), `ralph/ralph.sh retry <id>`, restart, fresh watcher; `STUCK` is a Fable trigger. A second `retry` of the same task is a DECISIONS entry on `handoff/NNN` first (#038), and a third is Michael's.
4. `/rotate` at the next stop signal that ends the working block (`PRINCIPAL 1.10.r10d`, a `GATE-FAILED` with its planning commit written, or an unrepaired `STUCK`) and before context passes about 150k. The relaunch line under D2 is `git show handoff/NNN:docs/handoffs/next-prompt.md | pbcopy && claude --model claude-opus-5-5 --remote-control essaydown-principal`.
5. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): handoff 037's Next Steps 3 — D3–D5 of `docs/proposals/001-token-efficiency.md` on Michael's word, then the boundary `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` with its conformance test, the recovery-commit guard, the gate fetch-on-failure I7 ×2), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r9 principal lessons, Claude r8 nit 3 (`ci.yml` merge step `if: always()`), React 19's ratification and the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/030 handoff/033 handoff/034 handoff/035 handoff/036 handoff/037`; `git worktree remove --force` the prior session's scratch worktree at `970bb5c`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 through #review-1-r8, handoffs 031–037 and #038), the 429 signal (#027), Grok's read-only mode (count first: Grok's unique findings at r7, r8, r9 from the three `status.json` files).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` and `retry` have none, and `abandon` is destructive); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree (stacked on `handoff/037` while that one is not yet in the phase branch, so the reconciliation cherry-picks one range; if a planning commit lands on `phase/1` first and the stack carries a DECISIONS.md tail, rebase the stack onto the new tip as #039 records, from a temporary worktree, and delete the superseded refs) and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (`git clean -fdX` there if it happened) — reproduce in a detached scratch worktree; a principal lesson for a task that lost an attempt goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message) — a lesson line orients, it never widens a task's scope (#028), and when a description clause and the acceptance disagree the acceptance binds; before `ralph/ralph.sh retry <id>` move every `.evidence/tasks/<id>/<n>.log` aside with a suffix (#027), and the lesson tells the agent to append a NEW `- [<id>] ` journal line with a `printf` that prints the timestamp and to run the suite once (an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed, and a capped attempt without a stub is `NO-JOURNAL` too; a lesson telling it to skip the suite is refused); when a task's every deliverable is committed and green and the attempts still cap, the lesson names the remaining step and the turn cost of each part in tool calls read from the capped transcript (#038); a second `retry` of the same task is a DECISIONS entry on `handoff/NNN` first (#038), and a third is Michael's; a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023, #025, #026–#029, #038, #039 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution, and the journal's last byte is checked before any append (it can lack a trailing newline; an anchored `grep -c '^- \[<id>\]'` verifies); a test's per-test budget is the family's 30 s with a block-local oracle, never a multiple of a container timing (#039); tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor or a click at a measured point, reads the editor's caret from the dev-bar selection readout and never asserts a DOM anchor at a widget boundary (#022, #024, #037); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024, #037, #039); a same-SHA rerun is only for a transient failure and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023, #029); a fix for a mark-edge or delimiter defect reads the serializer's handler for that mark in the installed package before touching the editor's tree (CLAUDE.md); the review stop rule (#041 D1): a finding becomes a fix task only if it is blocking — wrong bytes or lost text reaching the user silently, a crash, a security issue — the rest is backlog with a trigger, and a blocker found in r10 is Michael's before any fix chain; Fable only on PRINCIPAL.md's triggers with a written brief, never edits; when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern, and the host `grep` is ugrep (a long `.\{0,500\}` bound is rejected — use `node` for long-text extraction; a bare `402` matches numbers — grep `"status":402`); `scripts/gate.sh` outruns Claude Code's 120 s foreground limit and is backgrounded — read its output file; a foreground `sleep` is blocked — wait with a backgrounded `until` loop; a test runner's summary is captured to a file and grepped, never read through `tail -3`. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and before context passes about 150k — write the handoff before designing the next action; never write the literal DONE promise into any file an agent reads — a lessons line, a task text, a DECISIONS entry, a backlog line, a handoff — because the runner greps the whole transcript, which echoes every file the agent reads (#review-1-r7 M3, restated at #review-1-r8 N6; the boundary fix reads only assistant text blocks); the plan protocol's promise goes only in the `wip(plan.…)` or `wip(<id>)` commit message.
