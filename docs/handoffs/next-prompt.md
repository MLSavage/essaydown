Read docs/PRINCIPAL.md, then continue from handoff 030: `git show handoff/030:docs/handoffs/030-phase-1-review-r8-reconciliation.md` (it is on branch `handoff/030`, stacked on `handoff/029` — `git log --oneline phase/1..handoff/030` shows two commits, neither in the tree; it supersedes 029-phase-1-verify-r8-gate.md on `handoff/029` and 028-phase-1-verify-r7-gate.md in the tree; the FAIL reconciliation pattern is `task(1.10.r7d)` at `9538cf1` with #review-1-r7, the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2). Phase 1 stands at `PRINCIPAL 1.10.r8d`: the r8 gate failed at a1, was repaired by `plan.1.verify.r8h.r0` (DECISIONS #037: the dev-bar selection readout, `1.62`, `1.verify.r8.g1`) and passed at `1.verify.r8.g1h` a1 (run 35762164206 at `970bb5c`, 98/98 on all three runners); the r8 review set passed at attempt 1, three reviewers started within 198 ms: Claude PASS (2 should-fix, 3 nits), Sol FAIL (2 should-fix Required, no blocker), Grok PASS (nothing); the runner is stopped at the prompt in tmux `essaydown:runner` after `PRINCIPAL 1.10.r8d`; `.locks/` empty, no containers; `.wt/1.10.r8d` exists on `task/1.10.r8d`. **This session is the fresh session that runs the reconciliation `1.10.r8d` (DECISIONS #015).** Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`, `git log --oneline phase/1..handoff/030 | wc -l` (must print 2). Give Michael the one-line state and say the reconciliation is starting; nothing is needed from him until the verdict.
2. The reconciliation, in `.wt/1.10.r8d` on `task/1.10.r8d`: `git cherry-pick phase/1..handoff/030` first (docs/handoffs only); copy `.evidence/reviews/1/r8/{claude,sol,grok}/report.md` byte-identically to `docs/reviews/phase-1-r8-{claude,sol,grok}.md`; reproduce every Required and should-fix finding — Sol 1 (`toggle.ts:567`/`:506`, the emphasis/strong input-rule route at a block's end), Sol 2 (`toggle.ts:172` with `schema.ts:918`, leading whitespace dropped in conversion), Claude 1 (`format.ts:431-432`, `locateChild`'s give-up path) — in a detached scratch worktree at `970bb5c` (`git worktree add --detach <scratchpad>/970bb5c 970bb5c`, `pnpm install --frozen-lockfile` there, never in `.wt/1.10.r8d`), running the corpus legs over every fixture there before deciding scope; write `#review-1-r8` with the verdict first, M1–M11 each confirmed or reopened, the backlog deferrals checked for a fired trigger, the reviewer record counted from the reports (handoff 030's Decisions has the counts; Grok's PASS-with-blockers count stays 3 of 5, this attempt is not an instance), the turn-budget paragraph (030's Decisions holds the r8 repair's turns), Claude 2 (the stale `1.9` human gate) as a criterion paragraph stating where the two §7 clauses are evidenced now, and Claude nit 2 (M3 (a)'s promise rule) restated so it is satisfiable; regenerate `docs/progress.md`; commit touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1) with the DONE promise for `1.10.r8d` in the commit message only, never in a file. On FAIL append the r9 chain on the `task(1.10.r7d)` pattern at `9538cf1` (fix tasks, `1.verify.r9` carrying the promote-lessons step, `1.10.r9a/b/c/d`), every pin a fix re-decides named by file and line, acceptance tails naming file-level greps and never the suite's total, `generate-tasks --check`, `validate-tasks`, `bash ralph/test/run.sh` green.
3. Restart: idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux pane at the prompt, `doctor` clean), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`; arm the runner.log watcher from handoff 030 with the `Monitor` tool (persistent, 30 s poll, 30 min cap, `setopt nullglob` first; a FRESH one after every restart or `retry` and on every expiry). On PASS the runner integrates the reconciliation and runs `1.close` (`CLOSED 1`); on FAIL it runs the r9 chain — a capped first attempt with the work built is the chain's pattern (the runner retries itself), a `STUCK` gets the swept-mutation check before any `retry`.
4. `/rotate` at the next stop signal that ends the working block (`CLOSED 1`, or the r9 chain's `HUMAN_GATE 1.verify.r9h`) and at half context.
5. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): handoff 030's Next Steps 2 — `#025`'s, `#023`'s (two instances, #029) and H7's `fix(runner)`; `#review-1-r7` M3 (b)'s `transcriptHasDone` fix with its conformance test; the recovery-commit guard (a source change on a test-only task; a swept `zz-*` probe file); `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and PRINCIPAL.md gaining the r4–r8 principal lessons and handoff 025's description-vs-acceptance lesson; Claude r8 nit 3 (`ci.yml` merge step `if: always()`); `git branch -D handoff/010 … handoff/030`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5, #review-1-r6, #review-1-r7, the r8 chain and its repair — six of six r8 fix tasks capped at a1 with the work built), the 429 signal (#027, four instances), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option, Grok's read-only mode, React 19's ratification (Claude r8 nit 1).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree (cut from `phase/1`, or stacked on the newest `handoff/NNN` only while that one is not yet in the phase branch, so the reconciliation cherry-picks one range) and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (`git clean -fdX` there if it happened) — reproduce in a detached scratch worktree; a principal lesson for a task that lost an attempt goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message) — a lesson line orients, it never widens a task's scope (#028), and when a description clause and the acceptance disagree the acceptance binds; before `ralph/ralph.sh retry <id>` move every `.evidence/tasks/<id>/<n>.log` aside with a suffix (#027), and the lesson tells the agent to append a NEW `- [<id>] ` journal line with a `printf` that prints the timestamp and to run the suite once (an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed, and a capped attempt without a stub is `NO-JOURNAL` too; a lesson telling it to skip the suite is refused); a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023, #025, #026–#029 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor or a click at a measured point, reads the editor's caret from the dev-bar selection readout and never asserts a DOM anchor at a widget boundary (#022, #024, #037); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024, #037); a same-SHA rerun is only for a transient failure and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023, #029); when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern; `scripts/gate.sh` outruns Claude Code's 120 s foreground limit and is backgrounded — read its output file; a foreground `sleep` is blocked — wait with a backgrounded `until` loop. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action; never write the literal DONE promise into any file an agent reads — a lessons line, a task text, a DECISIONS entry, a handoff — because the runner greps the whole transcript, which echoes every file the agent reads (#review-1-r7 M3; the boundary fix reads only assistant text blocks).
