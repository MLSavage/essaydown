Read docs/PRINCIPAL.md, then continue from docs/handoffs/004-phase-0-verify-gate.md (it supersedes 003-phase-0-0.3-running.md; read 002-phase-0-running.md once for the gate evidence pattern, the deviations list and the reconciliation checklist in its Next Steps 4). Phase 0 is at the verify gate: 0.0–0.11 and 0.verify passed, `HUMAN_GATE 0.verifyh` is open, the runner has exited at the gate. You are the fresh session after the #015 rotation: you run the gate and the reconciliation. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|ROTATE-PRINCIPAL)' .evidence/runner.log | tail -5`. Give Michael the one-line state.
2. Give Michael `cd ~/Developer/essaydown && scripts/gate.sh 0.verifyh` (it waits for the three-OS run and prints `ACCEPT 0.verifyh a1` or `GATE-FAILED` + `PLAN-GATE`, then the advisory `ROTATE-PRINCIPAL`, already satisfied by the 004 rotation). On GATE-FAILED: `ralph/ralph.sh plan plan.0.verifyh.r1`, planning commit adding `0.verify.g1` + its gate (RUNNER-SPEC §3; generator and validator green), `<promise>DONE plan.0.verifyh.r1</promise>` in the commit message, restart.
3. On ACCEPT restart the runner in tmux window `essaydown:runner` with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter`; the review set 0.12 starts (three reviewer containers in parallel; watch `.evidence/reviews/0/r0/*/transcript.log` and `exit-code`). Arm the watcher from the handoff with the newest `^\[ralph\]` marker. A failed reviewer → `STUCK 0.12.r0<a|b|c>`: read its transcript, `ralph/ralph.sh retry 0.12.r0<x>`, restart.
4. On `PRINCIPAL 0.12.r0d`: reconcile in `.wt/0.12.r0d` per handoff 002's Next Steps 4 (three reports copied, `DECISIONS.md#review-0-r0` with the `verdict: PASS|FAIL` line, findings with severity and disposition including the #017 doctor gap and the deviations list 1–22, #017's provenance corrected there, class-level lessons, `docs/progress.md` regenerated, `docs/V1.1-BACKLOG.md` for nits; on FAIL the fix tasks + `0.verify.r1` + `0.12.r1a/b/c/d` written physically into PRD §8 with `node ralph/generate-tasks.mjs` and `node ralph/validate-tasks.mjs` green, close rewired to `0.12.r1d`). Journal line; commit `wip(0.12.r0d): …` with `<promise>DONE 0.12.r0d</promise>`; restart. The runner integrates, syncs state and runs `0.close` (moves `main`, creates `phase/1`). After the reconciliation commit integrates, no principal commits on `phase/0` (RUNNER-SPEC §8.1).
5. On `STUCK`/`NO-JOURNAL`/`NO-COMMIT` of any task: read the transcript's final `result` event (if it is the rate-limit banner, read the last `rate_limit_event` and wait for `resetsAt` before restarting), name the cause in one line, put the steer as a lesson line committed on the task's own branch (`chore(<id>): principal lesson …`), or re-cut the branch per DECISIONS #016 when it must see new `phase/0` commits; `ralph/ralph.sh retry <id>` if blocked; restart. If a retried agent ends without DONE citing a conflict the lessons channel was meant to resolve, do not retry again: write the planning commit appending the fix task it asked for.
6. After `0.close`: `/rotate` (handoff 005 for Phase 1; the runner is then started with `--phase 1`).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the state transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live, and before staging there run `doctor` (clean), `git reset --hard phase/0`, confirm `git log -1 phase/0` (DECISIONS #017); never append to DECISIONS.md or the journal on `phase/0` while a task is in flight; when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at half context even if no gate is open.
