Read docs/PRINCIPAL.md (its "Model and escalation" section applies: you are the Opus 5.5 principal; Fable only on its trigger list, with a written brief), then continue from handoff 038: `git show handoff/038:docs/handoffs/038-phase-1-review-r10-reconciliation-pending.md` (on branch `handoff/038`, two commits on `phase/1`'s `3e4a3f6`: DECISIONS #042 and handoff 038; `git log --oneline phase/1..handoff/038 | wc -l` prints 2; it supersedes 037, which is in the tree). Phase 1 is at `PRINCIPAL 1.10.r10d`: the r10 chain passed (`1.67`, `1.68`, `1.verify.r10`), gate `1.verify.r10h` ACCEPT a2 after a transient macOS rerun (#042), `1.9.r1` ACCEPT a1, and the scoped r10 review set is done — Claude PASS 0/0/0, Grok PASS 0/0/0, Sol PASS 0/0/0 on a #025 single-reviewer retry after a Codex usage limit (independent by construction; the siblings were held out and are back). **You are the fresh session that runs the PASS reconciliation `1.10.r10d` (DECISIONS #015), then watches `1.close`.** Under #041 D1 a blocker in r10 would go to Michael before any planning commit; none was reported — read the three reports for one hidden in a risk or a lesson before you write PASS.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED|CLOSE-DRIFT)|^<promise>' .evidence/runner.log | tail -5`, `git log --oneline phase/1..handoff/038 | wc -l` (must print 2), `ls .evidence/reviews/1/r10/{claude,sol,grok}/report.md` (three). Runner liveness: `.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux pane `essaydown:runner` at the prompt after `PRINCIPAL 1.10.r10d`. Give Michael the one-line state.
2. **The PASS reconciliation `1.10.r10d`** in `.wt/1.10.r10d` on `task/1.10.r10d` (at `3e4a3f6`, clean): `git cherry-pick phase/1..handoff/038` first (two commits, `docs/**`); read the three reports and `status.json`; copy them to `docs/reviews/phase-1-r10-{claude,sol,grok}.md` (`cmp`); append `1.9.r1`'s accepted record (`.evidence/human/1.9.r1/a1.md`, payload `{}`) to DECISIONS `#002-outline-feel` (RUNNER-SPEC §2, §5.4) with its deviations named (VS Code for Typora, ~250 words, the `pnpm --filter @essaydown/desktop dev` command); write `docs/DECISIONS.md` `#review-1-r10` with `verdict: PASS` — per-criterion verdict, the O1/O3 fixes confirmed, the backlog dispositions confirmed, the Sol retry under #025 (times and files in handoff 038), the reviewer record counted from the three `status.json` files, the `[review-1-r10, turn budget]` evidence line, the gate numbers from `.evidence/ci/1.verify.r10h/accepted/`; backlog lines only for what the reports or the note add (the `[1.46]` line's sixth instance, the `pnpm dev` text fix for `2.9`); the promote-lessons candidates named verbatim in `2.verify`'s PRD §8 text if any are promoted (a pending task's text, allowed), else recorded; no tasks appended, `EXPECTED_COUNT` stays 285; regenerate `docs/progress.md`; `node ralph/generate-tasks.mjs --check`, `node ralph/validate-tasks.mjs`, `bash ralph/test/run.sh` backgrounded with its count captured to a file and grepped; the journal line and lessons lines; touch only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (§8.1; `close.mjs:66` refuses any product-file change since `3e4a3f6`); the promise only in the `wip(1.10.r10d)` commit message. Then the idle checks, the restart (`tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`), a fresh watcher (handoff 038's command; take the line count before the restart).
3. **`1.close`**: the runner runs it after the reconciliation integrates; expect `CLOSED 1`. On a refusal or `CLOSE-DRIFT`, read `ralph/lib/close.mjs`'s message against its preconditions first; never change a product file to satisfy it; a runner contradiction is cut to a manual procedure and recorded (#023 is the pattern). `/rotate` at `CLOSED 1` or at an unresolved close stop, and before context passes about 150k. The relaunch line under D2 is `git show handoff/NNN:docs/handoffs/next-prompt.md | pbcopy && claude --model claude-opus-5-5 --remote-control essaydown-principal` (or, from the host checkout at the boundary, `pbcopy < docs/handoffs/next-prompt.md && claude --model claude-opus-5-5 --remote-control essaydown-principal`).
4. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): handoff 038's Next Steps 3 — D3–D5 of `docs/proposals/001-token-efficiency.md` on Michael's word, the boundary `fix(runner)` list, `chore(0.0)` for rotate.md (the existing-branch case and `--model` in its relaunch lines), the branch and worktree cleanup, and Michael's asks (`gate.sh gc`, the `phase/1` push, the turn-budget raise, the 429 and reviewer usage-limit signal, Grok's read-only mode — Grok's unique findings r7–r10: 0, 0, 0, 0).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` and `retry` have none, and `abandon` is destructive); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree (stacked on `handoff/038` while that one is not yet in the phase branch, so the reconciliation cherry-picks one range; if a planning commit lands on `phase/1` first and the stack carries a DECISIONS.md tail, rebase the stack onto the new tip as #039 records, from a temporary worktree, and delete the superseded refs) and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (`git clean -fdX` there if it happened) — reproduce in a detached scratch worktree; a principal lesson for a task that lost an attempt goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message) — a lesson line orients, it never widens a task's scope (#028), and when a description clause and the acceptance disagree the acceptance binds; before `ralph/ralph.sh retry <id>` move every `.evidence/tasks/<id>/<n>.log` aside with a suffix (#027), and the lesson tells the agent to append a NEW `- [<id>] ` journal line with a `printf` that prints the timestamp and to run the suite once (an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed, and a capped attempt without a stub is `NO-JOURNAL` too; a lesson telling it to skip the suite is refused); when a task's every deliverable is committed and green and the attempts still cap, the lesson names the remaining step and the turn cost of each part in tool calls read from the capped transcript (#038); a second `retry` of the same task is a DECISIONS entry on `handoff/NNN` first (#038), and a third is Michael's; a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023, #025, #026–#029, #038, #039 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution, and the journal's last byte is checked before any append (it can lack a trailing newline; an anchored `grep -c '^- \[<id>\]'` verifies); a test's per-test budget is the family's 30 s with a block-local oracle, never a multiple of a container timing (#039); tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor or a click at a measured point, reads the editor's caret from the dev-bar selection readout and never asserts a DOM anchor at a widget boundary (#022, #024, #037); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024, #037, #039); a same-SHA rerun is only for a transient failure and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023, #029); a fix for a mark-edge or delimiter defect reads the serializer's handler for that mark in the installed package before touching the editor's tree (CLAUDE.md); the review stop rule (#041 D1): a finding becomes a fix task only if it is blocking — wrong bytes or lost text reaching the user silently, a crash, a security issue — the rest is backlog with a trigger, and a blocker found in r10 is Michael's before any fix chain; Fable only on PRINCIPAL.md's triggers with a written brief, never edits; when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern, and the host `grep` is ugrep (a long `.\{0,500\}` bound is rejected — use `node` for long-text extraction; a bare `402` matches numbers — grep `"status":402`); `scripts/gate.sh` outruns Claude Code's 120 s foreground limit and is backgrounded — read its output file; a foreground `sleep` is blocked — wait with a backgrounded `until` loop; a test runner's summary is captured to a file and grepped, never read through `tail -3`. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and before context passes about 150k — write the handoff before designing the next action; never write the literal DONE promise into any file an agent reads — a lessons line, a task text, a DECISIONS entry, a backlog line, a handoff — because the runner greps the whole transcript, which echoes every file the agent reads (#review-1-r7 M3, restated at #review-1-r8 N6; the boundary fix reads only assistant text blocks); the plan protocol's promise goes only in the `wip(plan.…)` or `wip(<id>)` commit message.
