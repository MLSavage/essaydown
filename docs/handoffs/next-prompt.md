Read docs/PRINCIPAL.md, then continue from handoff 022: `git show handoff/022:docs/handoffs/022-phase-1-review-r4-reconciliation-pending.md` (it is on branch `handoff/022`, not on `phase/1`, together with `add1a48` = DECISIONS #025; it supersedes 021-phase-1-verify-r4-g1-gate.md on `handoff/021` and 020-phase-1-verify-r4-gate.md on `handoff/020`; 019 and earlier are in the tree since `ede7729`; the FAIL reconciliation patterns are `task(1.10.r3d)` at `ede7729` with #review-1-r3 and `task(1.10.r2d)` at `e6d7493` with #review-1-r2; the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2; the GATE-FAILED planning patterns are `0251db3` with #024 and `de5dcf2` with #022). Phase 1 stands at `PRINCIPAL 1.10.r4d`: `phase/1` tip `6c0e601` (`task(1.verify.r4.g1)`), gate `1.verify.r4.g1h` ACCEPT at a1, `1.10.r4a/b/c` passed (Claude FAIL 1 blocker, Sol FAIL 1 blocker, Grok FAIL per `status.json` — but Grok's report is derived, not independent: it was retried alone after a 402 and read Claude's and Sol's finished reports, DECISIONS #025 on `handoff/022`), `1.10.r4d` principal-pending with its worktree `.wt/1.10.r4d` on `task/1.10.r4d` at `6c0e601`. The runner is stopped at the prompt in tmux `essaydown:runner` (pane pid 8940) after `PRINCIPAL 1.10.r4d`; `.locks/` empty, no containers. You are the fresh session that runs the reconciliation `1.10.r4d` (DECISIONS #015: never the session that ran the phase's tasks or watched its review set). Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the runner.log watcher from handoff 022 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first).
2. The reconciliation, in `.wt/1.10.r4d` only — never install or run pnpm, vitest or Playwright there on the Mac (the runner runs the reconciliation's suite in that worktree inside the container; a host `node_modules` fails it, lesson [1.10.r3d]); reproduce in a detached scratch worktree instead: `git worktree add --detach <scratchpad>/probe 6c0e601`, install and probe there, remove it after. `git cherry-pick phase/1..handoff/020 && git cherry-pick phase/1..handoff/021 && git cherry-pick phase/1..handoff/022` (check each range with `git log --oneline phase/1..handoff/NNN` first: one, one, two commits; the `next-prompt.md` conflict resolves to the incoming version each time); copy `.evidence/reviews/1/r4/{claude,sol,grok}/report.md` verbatim to `docs/reviews/phase-1-r4-{claude,sol,grok}.md`; reproduce each blocker yourself before dispositioning it (Claude 1: a `delete` run whose edge character is punctuation directly next to unmarked alphanumeric text, through `mdastToPM` → `pmToMdast` → `format`, then `parse` — the `~~` delimiters GFM cannot open or close; Sol 1: `.evidence/reviews/1/r4/sol/strike-probe.mjs`'s shape, deleting the space after a strikethrough run ending in a link, Copy Markdown against `format(parse(...))`); write `DECISIONS.md#review-1-r4` after `#025` with `verdict: PASS|FAIL` first — each finding numbered (J after I) with its disposition, I1–I10 confirmed or reopened, the per-reviewer unique-findings table re-derived from the bodies with Grok's r4 report counted as derived per #025 (two independent reports; Grok's unique findings 0 by construction, no low-rater entry, its verdict outside the count), the `[1.37]` cursor-map backlog line dispositioned as Claude 3 / Sol 2 (Required), the `[plan.1.verify.r4h.r0]` vertical-arrow line read against Claude 6 (the construct is also in `e2e/web/editor-break-in-mark.spec.ts`), Claude 4/5 (the chord guard's regex and its allowlist reason) dispositioned, Claude 8 (1.37's timestamp-less attempt-1 stub) read against the runner's per-attempt count, the turn-budget note with counts read from `.evidence/state/tasks.json` and the transcripts at the time of writing, Michael's Grok decision cited from #025, the r5 reviewer rows written for three reviewers with the Grok question recorded for him; backlog lines (the `#025` boundary `fix(runner)`, hard stop `2.1`); append class-level lessons; regenerate `docs/progress.md`; on FAIL append `1.40…` fix tasks + `1.verify.r5` + `1.10.r5a/b/c/d` in PRD §8 and `ralph/tasks.json` the way `ede7729` did (`node ralph/generate-tasks.mjs`, `ralph/EXPECTED_COUNT` from the generator's printed count, `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green — the conformance suite installs nothing); commit `wip(1.10.r4d): …` with `<promise>DONE 1.10.r4d</promise>` in the message, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1).
3. Idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, `doctor` clean), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`. The runner squashes `task(1.10.r4d)`, runs `sync-state`, then the first `1.40…` task (FAIL) or `1.close` (PASS). `/rotate` at the next stop signal: `HUMAN_GATE 1.verify.r5h` (hand Michael `scripts/gate.sh 1.verify.r5h`; on `GATE-FAILED` the planning commit is `#026` per `0251db3`; after any `gate.sh rerun` that accepts, `doctor` flags the rejected earlier attempt — never run its `--resume`, use `#023`'s abandon) or `CLOSED`. If a reviewer fails: `STUCK` names `retry` — for Grok on a 402, stop and report the usage to Michael (his decision, #025); for any single-reviewer retry, #025's manual procedure first (hold the sibling directories out of the attempt directory, clear the failed attempt's own `report.md`), restore the siblings before the reconciliation.
4. Phase boundary idle window (only after `1.close`, from the host checkout on `phase/2` with the runner idle): `#025`'s `fix(runner)` (a partial review-set start gives the reviewer a view of its own directory only; `check_report` refuses a sibling read; one conformance test per guard); `#023`'s `fix(runner)` (doctor's recorded clause for blocked → rerun → passed; `--resume` refused on a superseded attempt; one conformance test per guard plus `doctor` clean at the end of the transient-rerun test); H7's `fix(runner)` — `ralph/PROMPT.md` "Iteration" steps replaced by CLAUDE.md's verbatim, `tests/agent-rules.test.ts` widened; `chore(0.0)` for rotate.md step 5's third case, the stale `description` line and PRINCIPAL.md's doctor sentence; `git branch -D handoff/010 handoff/011 handoff/012 handoff/013 handoff/014 handoff/015 handoff/016 handoff/017 handoff/018 handoff/019 handoff/020 handoff/021 handoff/022`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget `fix(runner)` (six of six capped opus fix tasks on the strip family), the gate fetch-on-failure change (#review-1-r3 I7), the reviewer-prompt option and Grok's future (budget, #025).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (the runner runs a principal task's suite there in the container; a host `node_modules` fails it — `git clean -fdX` in that worktree if it happened); a principal lesson for a task that lost an attempt on procedure goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message), followed by `ralph/ralph.sh retry <id>` only if the task is `blocked` and by the same restart command otherwise — the runner counts one new `- [<id>] ` journal line per attempt, so an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed; a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023 and #025 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place — ask for the text first and hand the finished command, or leave a blank so obviously unfilled that running it as written fails (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor (#022, #024); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024); a same-SHA rerun is only for a transient failure — a file unchanged since its last green run on that OS, a hang before any assertion — and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023); when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
