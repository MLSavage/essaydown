Read docs/PRINCIPAL.md, then continue from handoff 011: `git show handoff/011:docs/handoffs/011-phase-1-review-r0-reconciliation-pending.md` (it is on branch `handoff/011`, not on `phase/1`; it supersedes 010-phase-1-verifyh-gate-failed.md on `handoff/010`, which is also not in the tree; the FAIL reconciliation pattern to copy is `task(0.12.r0d)` at `c0f3c98` with DECISIONS #review-0-r0, `git show --stat c0f3c98`; the DECISIONS layout and `verdict:` placement are #review-0-r2 in `052e206`). Phase 1 stands at `PRINCIPAL 1.10.r0d`: `phase/1` tip `e9ed88b` (`1.verify.g1`), gate `1.verify.g1h` ACCEPT a1 run 34338761650 with `test-logs` and `playwright-report`, review attempt r0 in — Claude FAIL (1 blocker / 6 should-fix / 5 nits), Sol FAIL (0 / 6 / 1), Grok PASS (0 / 2 / 4) — the runner is idle, and `.wt/1.10.r0d` on `task/1.10.r0d` is the reconciliation worktree, untouched. You are the fresh session that runs the reconciliation (DECISIONS #015). Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`. Give Michael the one-line state with the three verdict lines. Arm the watcher from handoff 011 with the `Monitor` tool (persistent, 30 s poll), not a background shell. Ask Michael the dependency question from handoff 011 Open Questions (`@lezer/highlight`, `@lezer/markdown`, unused `codemirror`; recommend (a) at 70%) so the disposition can be written without a second round-trip.
2. Reconcile in `.wt/1.10.r0d` per handoff 011 Next Steps 2: `git cherry-pick handoff/010 handoff/011` first; the three reports to `docs/reviews/phase-1-r0-{claude,sol,grok}.md`; the 1.9 record `payload` and the `a2.md` body appended to `DECISIONS.md#002-outline-feel`; `DECISIONS.md#review-1-r0` with `verdict: FAIL` first, per-finding dispositions, the `&#x20;` trailing-space defect disposed of as a PRD §6 portability finding with a fix task in the r1 chain (mechanism and the BBEdit experiment in handoff 009 Decisions, the file's examination in #019), the latency follow-up decided (PRD §8 1.10: "< 16 ms or a follow-up task is inserted at reconciliation"), the dependency note after Michael's answer, the `ca1cd3d` reading; re-point #review-0-r2 H2; fix tasks + `1.verify.r1` + `1.10.r1a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `1.close` rewired (check `git show c0f3c98 -- docs/PRD.md | grep close` first), `EXPECTED_COUNT` bumped; class-level lessons; `docs/progress.md` regenerated; `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green; journal line; `wip(1.10.r0d): …` with `<promise>DONE 1.10.r0d</promise>`. Then start the runner: `.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`. Confirm `task(1.10.r0d)` integrates and the first fix task starts.
3. Fix chain (three attempts each before `STUCK`; re-cut per DECISIONS #016 with a `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart; after any `docker/` commit integrates, `docker compose -f docker/compose.yml build` with `ESSAYDOWN_ROOT` before the next container task) → `1.verify.r1` → `HUMAN_GATE 1.verify.r1h`: `cd ~/Developer/essaydown && scripts/gate.sh 1.verify.r1h`; on failure read the failing job's step in `.evidence/ci/1.verify.r1h/a1/workflow.log` first (`gh run view <id> --json jobs`), then `ralph/ralph.sh plan plan.1.verify.r1h.r0` and a `.g1` repair the way `2418bc7` did it. Rotate at that gate regardless of context (handoff 012 on `handoff/012`).
4. On `ACCEPT`: `1.10.r1a/b/c` (`.evidence/reviews/1/r1/verifier_id` must read `1.verify.r1`, both SHAs equal the verifier's integrated SHA; `docker top` of the claude-review container shows `--max-turns 160`) → `PRINCIPAL 1.10.r1d` **by a fresh session**: reconcile per the same pattern, cherry-picking `handoff/010`, `handoff/011` and `handoff/012` if they are not yet in `phase/1`; `verdict: PASS|FAIL` first in `DECISIONS.md#review-1-r1`; on PASS restart → `1.close` → `/rotate` at the phase boundary (on `phase/2` from the host checkout) → `git branch -D handoff/010 handoff/011 handoff/012`; ask Michael to delete the stale `ci/1.verify/a1` ref on origin.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), and the reconciliation commit goes only on `task/1.10.r0d` in `.wt/1.10.r0d`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note and Michael's OK; when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md; a command you hand Michael never contains a placeholder or example text to edit in place — ask for the text first and hand the finished command, or leave a blank so obviously unfilled that running it as written fails (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; tests never encode one platform's chord or a magnitude from one run; in this zsh a bare `echo ===` aborts a compound command, quote separators. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
