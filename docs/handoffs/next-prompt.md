Read docs/PRINCIPAL.md, then continue from handoff 024: `git show handoff/024:docs/handoffs/024-phase-1-review-r5-reconciliation-pending.md` (it is on branch `handoff/024`, stacked on `handoff/023`, not on `phase/1`; `git log --oneline phase/1..handoff/024` shows five commits — `e03ee87` = DECISIONS #026, `eb59116` = #027, `e305be6` = #028, `0424d44` = handoff 023, and handoff 024; it supersedes 023-phase-1-verify-r5-gate.md; the FAIL reconciliation patterns are `task(1.10.r4d)` at `cef59c2` with #review-1-r4 and `task(1.10.r3d)` at `ede7729` with #review-1-r3; the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2). Phase 1 stands at `PRINCIPAL 1.10.r5d`: `phase/1` tip `3dd9c4b` (`task(1.verify.r5)`), gate `1.verify.r5h` accepted (run 34705334786), review set `1.10.r5a/b/c` passed at attempt 1, started together (Grok independent), all three verdicts FAIL — Claude 1 blocker / 3 should-fix (1 Required) / 4 nits, Sol 1 blocker / 1 should-fix Required, Grok 0 blockers / 1 should-fix Required / 2 nits; the shared blocker is the astral (non-BMP) neighbour of a flanking mark encoded as a lone-surrogate reference in `packages/core/src/format.ts` and the built-in handlers, the shared Required is `placeChildren` in `packages/core/src/positions.ts` (1.40's `it.fails` pin). The runner is stopped at the prompt in tmux `essaydown:runner` (pane pid 8940) after `PRINCIPAL 1.10.r5d`; `.locks/` empty, no containers; the worktree `.wt/1.10.r5d` on `task/1.10.r5d` at `3dd9c4b` exists. You are the fresh session DECISIONS #015 requires for the reconciliation — the session that watched the review set has rotated out. Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC, and never on `phase/1` before `1.close`.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`, `git log --oneline phase/1..handoff/024 | wc -l` (must print 5). Give Michael the one-line state; nothing is needed from him until the restart.
2. The `1.10.r5d` reconciliation in `.wt/1.10.r5d` on `task/1.10.r5d`: `git cherry-pick phase/1..handoff/024` (a `next-prompt.md` conflict, if any, resolves to the incoming version); copy the three reports verbatim to `docs/reviews/phase-1-r5-{claude,sol,grok}.md` from `.evidence/reviews/1/r5/<reviewer>/report.md`; reproduce findings 1 (astral encoding) and 2 (`placeChildren`) in `git worktree add --detach <scratchpad>/probe 3dd9c4b` (Sol's `serializer-probe.mjs` and `map-probe.mjs` under `.evidence/reviews/1/r5/sol/` are seeds; remove the probe after); write `DECISIONS.md#review-1-r5` after `#028` with `verdict: FAIL` first, J1–J9 of `#review-1-r4` confirmed or reopened (J1 reopened for the astral case), the 1.40 pin dispositioned into the `placeChildren` task, Claude's finding 3 discharged by the cherry-pick, finding 4 and the nits to a test-only task or the backlog with a revisit trigger, the reviewer record (Claude 8 / Sol 2 / Grok 3; shared: the blocker Claude–Sol, the Required all three, the whitespace cell Claude–Grok; severity agrees on every shared finding), the turn-budget paragraph (1.40 a1 51; 1.41 51 ×4 then a 429 and a `NO-JOURNAL`; 1.42 51 ×3; 1.43 42/13/8 on an undecided conflict; 1.44 35; 1.verify.r5 43); append the r6 chain to `ralph/tasks.json` on the `cef59c2` pattern — `1.45` (astral encoding: `format.ts` app-owned handlers with `codePointAt`, an invariant-A fixture `astral-neighbour.md` with canonical sibling and index entry, guards astral-outside × {punctuation, whitespace, inline code, link} inside × both sides × three marks asserting bytes, one mark, parsed text equal to the tree's text and the fixed point, a typed Playwright route), `1.46` (`placeChildren` retry with the code-point reference, `SpellingTable` entry, the pin and `OUTSIDE_ENCODED_NEIGHBOUR` deleted, a `position-map-editor-leg` clause asserting `toSource` monotone and `toRendered` its inverse over the encoded neighbour, a toggle-then-type Playwright case by counted arrows; depends on 1.45), `1.47` test-only (1.41's decoded-text assertion, the whitespace×whitespace `encodeInfo` guard, the chord-guard regex and title, the journal spec count), `1.verify.r6` (`needsCI`, `ci/1.verify.r6/a{n}`), `1.10.r6a/b/c/d` (`review-set`); every fix task's text names "append a NEW `- [<id>] ` journal line with `printf`" and "run the suite once", and a "no expected fail" clause only on tasks sequenced after 1.46; `node ralph/generate-tasks.mjs`, `EXPECTED_COUNT` from the printed count, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green; `docs/progress.md` regenerated; commit `wip(1.10.r5d): …` with `<promise>DONE 1.10.r5d</promise>`, touching only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`.
3. Idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt, `doctor` clean), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`; arm the runner.log watcher from handoff 024 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first). The runner integrates `1.10.r5d` and starts `1.45`.
4. During the r6 chain: on `STUCK <id>` read `.evidence/tasks/<id>/*.log`, write a `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message), move every attempt log aside with a suffix, `ralph/ralph.sh retry <id>`, restart; on a 429 in a transcript wait for the reset Michael names, then the plain restart. `/rotate` at `HUMAN_GATE 1.verify.r6h` (the next session hands Michael `scripts/gate.sh 1.verify.r6h` and rotates at `PRINCIPAL 1.10.r6d`), and at half context.
5. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)`; H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5's third case and the stacked-handoff rule (handoff 024 Decisions), the stale `description` line, PRINCIPAL.md's doctor sentence and PRINCIPAL.md gaining Sol's r4 lesson and #028's; `git branch -D handoff/010 … handoff/024`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: handoff 023 Decisions), the 429 signal (#027), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree (cut from the newest unmerged `handoff/NNN` so the reconciliation cherry-picks one range) and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); never install or run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>` (`git clean -fdX` there if it happened) — reproduce in a detached scratch worktree; a principal lesson for a task that lost an attempt goes as `chore(<id>): principal lesson …` on its task branch in `.wt/<id>` (`docs/lessons.md` only, reversal in the message) — a lesson line orients, it never widens a task's scope (#028); before `ralph/ralph.sh retry <id>` move every `.evidence/tasks/<id>/<n>.log` aside with a suffix (#027), and the lesson tells the agent to append a NEW `- [<id>] ` journal line with a `printf` that prints the timestamp and to run the suite once (an attempt that completes an earlier stub and appends nothing is `NO-JOURNAL` even with DONE printed; a lesson telling it to skip the suite is refused); a single-reviewer retry follows #025's manual procedure and a Grok 402 is never retried without Michael; a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise) — the astral fix needs none; when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md (#023, #025, #026–#028 are the pattern); a command you hand Michael never contains a placeholder or example text to edit in place (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run, and an e2e reaches a rendered position by counted horizontal arrows from an asserted anchor (#022, #024); a gate repair scopes to what failed and files the rest with a revisit trigger (#021, #022, #024); a same-SHA rerun is only for a transient failure and any tree change is a `.g<n>` repair (RUNNER-SPEC §2, #023); when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings; a derived report is not counted), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers, `--` before a `- [id]` grep pattern; `scripts/gate.sh` outruns Claude Code's 120 s foreground limit and is backgrounded — read its output file. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
