Read docs/PRINCIPAL.md, then continue from docs/handoffs/001-bootstrap.md. Planning is locked at PRD v14; nothing is built; PREFLIGHT §0 has been run so the repos and clones exist.

Your job this session, in order:

1. Read docs/progress.md, docs/PRD.md §10 and §8 Phase 0, docs/RUNNER-SPEC.md, docs/lessons.md. Do not re-audit the plan. Confirm `git log --oneline -1` on main is the seed commit and `ls .ext/build-defaults` shows BUILD-DEFAULTS.md.
2. Walk docs/PREFLIGHT.md's checklist: run every check you can yourself (`docker compose version`, `claude --version`, `codex --version`, `grok --version`, `gh auth status`), write the versions into the file, and for anything you cannot do give Michael one command per message. Ask him the two open questions in the handoff (fixture essay, Grok login-state path) and record the answers in the file.
3. Write `scripts/validate-defaults.mjs` in `.ext/build-defaults` (spec in the handoff's Gotchas), run it, commit it there, and give Michael the `git -C .ext/build-defaults push` command.
4. Create `phase/0` from main and do task 0.0 exactly as written in PRD §8 Phase 0 and RUNNER-SPEC §11–§12, in this order, one atomic `wip(0.0): <part>` commit per part: Dockerfile + compose.yml + versions.env → entrypoints → generate-tasks.mjs + validate-tasks.mjs (EXPECTED_COUNT must be 122) → gate.sh → ralph.sh with every §11 command → conformance suite → bootstrap.sh → CLAUDE.md/AGENTS.md (include docs/PRINCIPAL.md's rules) → .gitattributes/.gitignore. Record the commit convention as DECISIONS.md#009-bootstrap-commits. 0.0's acceptance list is the definition of done; run each check and quote its output in the journal entry.
5. Run `ralph/bootstrap.sh`. When it passes: write docs/handoffs/002-phase-0.md, rewrite this file for the next session, commit both on phase/0, then open a second tmux window (`tmux new-window -n runner`), start `ralph.sh run --phase 0` there, and tell Michael in one line which STOP signal to expect first and how to check on it from his phone (`tmux attach -t essaydown` after ssh, or ask you).

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks; if 0.0 reveals a runner contradiction, cut the feature to a manual procedure and record it in DECISIONS.md — do not add machinery. Give Michael one-line state, then the ask, then the command; never a diff. Write handoff 002 at half context even if 0.0 is not finished.
