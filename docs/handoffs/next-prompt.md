Read docs/PRINCIPAL.md, then continue from handoff 016: `git show handoff/016:docs/handoffs/016-phase-1-review-r2-reconciliation-pending.md` (it is on branch `handoff/016`, not on `phase/1`; it supersedes 015-phase-1-verify-r2-g1-repair-running.md on `handoff/015` and 014-phase-1-verify-r2-gate.md on `handoff/014`; 010–013 are in the tree at `b149f0c`; the FAIL reconciliation patterns are `task(1.10.r1d)` at `b149f0c` with #review-1-r1 and `task(1.10.r0d)` at `b269627` with #review-1-r0; the PASS pattern is `task(0.12.r2d)` at `052e206` with #review-0-r2; the gate-failure planning pattern is `plan(plan.1.verify.r2h.r0)` at `6a39a92` with #021). Phase 1 stands at `PRINCIPAL 1.10.r2d`: `phase/1` tip `c9408ee` (`task(1.verify.r2.g1)`), gate `1.verify.r2.g1h` accepted at attempt 1, review attempt r2 complete at `c9408ee` (Claude FAIL 1/3/2, Sol FAIL 1/0/1, Grok PASS 0/0/2 as blockers/should-fix/nits; two distinct blockers), `1.10.r2d` principal-pending with worktree `.wt/1.10.r2d` on `task/1.10.r2d`. The runner is stopped at the prompt in tmux `essaydown:runner` (pane pid 8940); `.locks/` empty, no containers. You are the fresh session that runs the reconciliation `PRINCIPAL 1.10.r2d` (DECISIONS #015). Do not re-audit the plan; do not rebuild the runner; fix it only with an atomic `fix(runner): …` commit plus a conformance test when it contradicts RUNNER-SPEC.

Your job this session, in order:

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, `grep -E '^\[ralph\]|^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|PRINCIPAL|PLAN-GATE|GATE-FAILED|CLOSED)|^<promise>' .evidence/runner.log | tail -5`. Give Michael the one-line state. Arm the runner.log watcher from handoff 016 with the `Monitor` tool (persistent, 30 s poll, `setopt nullglob` first).
2. In `.wt/1.10.r2d`: `git cherry-pick handoff/014 && git cherry-pick handoff/015 && git cherry-pick handoff/016` (a conflict on `docs/handoffs/next-prompt.md` at the second and third pick resolves to the incoming version; none on the `NNN-*.md` files; record the resolution in #review-1-r2 the way #review-1-r1 did). Read `.evidence/reviews/1/r2/{claude,sol,grok}/report.md` in full and copy them verbatim to `docs/reviews/phase-1-r2-{claude,sol,grok}.md`.
3. Write `DECISIONS.md#review-1-r2` with `verdict: PASS|FAIL` as its first line (`run.mjs` `verdictFromDecisions` reads it): each finding numbered with its disposition (Claude 6, Sol 2, Grok 2; the two blockers are distinct — Claude #1 trailing `hard_break` at a block's end does not round-trip, Sol #1 a flush inside a continuous source burst splits undo past one coalescing window — each with a browser reproduction), each r1 disposition G1–G12 confirmed or reopened, the per-reviewer unique-findings table (post.1's input; re-derived from the bodies, not copied from 016), the `ralph/PROMPT.md` "Every iteration" drift disposition (Claude #4 / Grok #2: a runner-owned one-file chore, never on `phase/1` mid-flight), the turn-budget note with counts read from `.evidence/state/tasks.json` at the time of writing. On FAIL: fix tasks `1.29…` + `1.verify.r3` (needsCI, `refTemplate ci/1.verify.r3/a{n}`, its gate `1.verify.r3h`) + `1.10.r3a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `ralph/EXPECTED_COUNT` bumped, `1.close` rewired to depend on `1.10.r3d`, `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green, the way `b149f0c` did it; fixes to passed tasks' files are new tasks (1.13/1.25 for Claude #1's family, 1.24/1.26 for Sol #1), never edits of passed records. On PASS: regenerate `docs/progress.md` in the same commit. Journal entry per the template; commit `wip(1.10.r2d): …` with `<promise>DONE 1.10.r2d</promise>` in the message, touching only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1).
4. Idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, `tmux capture-pane -p -t essaydown:runner | tail -3` shows the prompt), then `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`. The runner integrates `task(1.10.r2d)` and `sync-state` reads the verdict. On FAIL the fix chain runs (three attempts each; `NO-JOURNAL` at a1 restarts with the same run command; `STUCK` re-cuts per #016 with `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart) → `1.verify.r3` → `HUMAN_GATE 1.verify.r3h` → `/rotate` there regardless of context. On PASS → `1.close` → `CLOSED` → `/rotate` at the phase boundary on `phase/2` from the host checkout with the runner idle; then `git branch -D handoff/010 handoff/011 handoff/012 handoff/013 handoff/014 handoff/015 handoff/016`, and ask Michael for `scripts/gate.sh gc` and the `phase/1` push (#010).
5. Phase boundary idle window (only after `1.close`): `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `chore(0.0)` aligning `ralph/PROMPT.md`'s "Every iteration" list with CLAUDE.md step 2 if the reconciliation did not scope it into a fix task; the conditional turn-budget `fix(runner)` only on Michael's OK.

Constraints: subscription logins only, no API keys anywhere; containers never push; gates never commit; never edit passed tasks (records and integrated commits — files are not frozen, DECISIONS #016); every change you make is an atomic commit whose message says how to reverse it, and a reversal you state for a runner command must actually return the task to the status it came from (read the transition in `ralph/lib/doctor.mjs` first; `plan` has none); never commit on the host checkout while the runner loop is live (DECISIONS #017); never commit on `phase/1` from now until `1.close` — a principal commit goes on `handoff/NNN` from a temporary worktree and the reconciliation cherry-picks it (DECISIONS #review-0-r1 deviation 29, rotate.md step 5), a planning commit goes only on its `plan/<gate>/r<n>` branch in `.wt/plan.<gate>.r<n>` via `ralph/ralph.sh plan`, and a reconciliation commit goes only on its `task/<id>` branch in `.wt/<id>`, touching only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1); a principal commit never deletes a path (0.19's doctor check); never hand-write `.locks/ralph` (0.25 refuses stale locks and names the manual removal); never append to DECISIONS.md or the journal on `phase/1` while a task is in flight; no dependency outside PRD §4 without a DECISIONS note, Michael's OK and its `docs/dependencies.json` entry (1.20: `pnpm lint` fails otherwise); when the runner contradicts itself, cut to a manual procedure and record it in DECISIONS.md; a command you hand Michael never contains a placeholder or example text to edit in place — ask for the text first and hand the finished command, or leave a blank so obviously unfilled that running it as written fails (DECISIONS #019); never amend a commit on `phase/N` once it has been pushed (`git ls-remote origin phase/1` before any amend; a pushed commit is corrected by a new commit, #019); counts in a state line are read from `git rev-list --count` / `status` output at the moment of writing, never carried from memory; a `sed -i` over the journal or lessons targets only this task's own line, never a global substitution; tests never encode one platform's chord, clipboard convention or a magnitude from one run; when the facts make one option strictly better, say so and do not frame it as a compromise with a percentage; before raising a pattern about a reviewer, count first (unique findings, severity on shared findings), and never reconfigure a reviewer inside a review set (DECISIONS #review-1-r0); a runner parameter change is proposed only with transcript evidence and stays conditional on Michael's OK (#review-1-r1); in this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers. Give Michael one-line state, then the ask, then the command; never a diff. Rotate (`/rotate`) at every stop signal that ends a working block and at half context even if no gate is open — write the handoff before designing the next action.
