# Handoff 026 — Phase 1 at `PRINCIPAL 1.10.r6d`: gate r6h ACCEPT at a2 after a transient rerun (#029), review set r6 done, all three FAIL, the reconciliation pending for a fresh session

Written 2026-09-13 by the principal (the session that ran the `1.verify.r6h` gate with Michael and watched the r6 review set; Claude Code, Remote Control session on the Mac Mini), at the `PRINCIPAL 1.10.r6d` stop signal, per DECISIONS #015 (the reconciliation is a fresh session). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `025-phase-1-verify-r6-gate.md` (still on `handoff/025`, not yet in the tree). This commit is on `handoff/026`, stacked on `handoff/025` because 025 is not in `phase/1` yet; `git log --oneline phase/1..handoff/026` shows three commits (025's handoff, DECISIONS #029 at `d2e5e3b`, this one) and the `1.10.r6d` reconciliation cherry-picks that one range. Next: `027-*.md`.

## Current State

- **`phase/1` tip `c2f6526`** (`task(1.verify.r6)`); 73 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **Gate `1.verify.r6h`: passed at a2** (`.evidence/ci/1.verify.r6h/accepted.json` → attempt 2, run 34737435949, SHA `c2f6526`, 88/88/88 Playwright on the three runners, both artifacts under `a2/`). a1 (run 34735711543) failed on windows-latest only, one case in `editor-soft-line-breaks.spec.ts` one arrow press short; transient by the #023 criterion (spec unchanged since `af8a62c`, passed on windows at r5h, no editor runtime code changed since `3dd9c4b`); `scripts/gate.sh rerun` withdrew `plan.1.verify.r6h.r0` (`abandoned`); a1 then marked `abandoned` by #023's manual procedure so `doctor` is clean. All of it in DECISIONS #029 (on `handoff/025`, `d2e5e3b`).
- **Review set `1.10.r6a/b/c`: all passed at attempt 1**, started together at `04:24:53.556Z` / `.654Z` / `.754Z` (Grok independent, #025's condition), all recorded finished at `04:51:18Z`. Reports in `.evidence/reviews/1/r6/{claude,sol,grok}/report.md` (61 / 55 / 55 lines), `status.json` all `implementation_sha c2f6526`, exit codes 0. **Verdicts: FAIL, FAIL, FAIL.** Claude: 1 blocker, 7 should-fix (one Required), 2 nits. Sol: 1 blocker, 2 should-fix (one Required). Grok: 1 blocker, 0 should-fix. Turns (`num_turns` in the transcripts): Claude 123, Grok 25; Sol's not read this session.
- **`1.10.r6d`: `principal-pending`** (`attempts 0`, `task_branch task/1.10.r6d`, worktree `.wt/1.10.r6d` exists, cut from `phase/1`). `1.close` depends on it.
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.10.r6d principal-pending; last passed 1.10.r6c; gates open: none; plan requests: none; blocked: none." Graph 232 tasks (`EXPECTED_COUNT` 232).
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r6d` (runner.log lines 96,742–96,743; the log is 96,743 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the reconciliation commit carries the DONE promise: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r6d` (line 96,742). The runner prints nothing at a review set's start; its next own lines are `[ralph] <first r7 task> attempt 1` after the restart.
- **Expected next stop**: after the reconciliation (FAIL expected on the reports: the r7 chain planned in `task(1.10.r6d)` on the `159c4c0` pattern) the runner runs the first r7 fix task; then `HUMAN_GATE 1.verify.r7h`.

## Corrections

- `025` Current State, "the r6 chain … `1.10.r6a/b/c` pending on it": done; the gate took two attempts, not one, and the accepted attempt is a2 — the reconciliation reads `accepted.json`, never `a1/`.
- `025` Next Steps 2, "cherry-picks `phase/1..handoff/025` (one commit …)": the range is now `phase/1..handoff/026`, three commits; `handoff/025` is an ancestor of `handoff/026`, so cherry-picking `phase/1..handoff/026` takes 025's handoff, #029 and this handoff in order.
- `025`'s `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/025`": through `handoff/026`.

## Decisions

- **`#029`** (on `handoff/025` at `d2e5e3b`, not yet in the tree): the `1.verify.r6h` transient rerun, the second instance of #023's doctor contradiction (a `rejected` a1 on a `passed` gate read as incomplete, the `--resume a1` remedy wrong), the same abandon-a1 procedure; the boundary `fix(runner)` #023 scopes now has two instances. Reversals in the entry.
- **Runner deviations this block: none.** `scripts/gate.sh rerun` and `scripts/gate.sh abandon` are the runner's own commands; no `admin`, no `docker/` change, no `fix(runner)`, no `retry`.
- **What the reviewers found (headlines; the reconciliation reproduces, never trusts):** Sol's and Grok's blocker is one finding at `packages/core/src/format.ts:193` — `widenSplitSurrogateReferences` (1.45) only repairs a lone-surrogate reference beside its *raw* mate (`SPLIT_PAIR`), so an astral character between two flanking runs whose *both* halves `container-phrasing.js` encodes (`encodeAfter` then `encodingInfo.before`) is written as two lone-surrogate references; Sol's reproduction: `format(parse('*a.*&#x1F600;*(b)*'))` → `*a.*&#xD83D;&#xDE00;*(b)*\n`, reparsing to U+FFFD U+FFFD. Claude's blocker is different: `format.ts:262–267`, an autolink literal whose URL holds a backslash is written in `<…>` form with the backslash doubled, and every `parse ∘ format` doubles it again (idempotence broken). Required should-fixes: Claude 2 — `toggle.ts:392` `cursorMap` maps a caret inside/at the end of an inline code span to the span's start; Sol 2 and Claude 3 are the same finding — `positions.ts:540/562` `rewrittenEmissions` claims three rewrites are "the whole enumeration" but `container-phrasing.js` lines 60–72 has a third (a trailing line ending replaced by a space before an `html` child), so K2's enumeration guard is incomplete. Claude's others: table-cell end maps to the row start (`toggle.ts:413`), a run ending in punctuation maps after its closing delimiter (`toggle.ts:260`), the blind counted `press` in the soft-line-breaks spec (the a1 windows failure, the anchor-and-count rule's weakness), 1.47's lesson-vs-description reading, URL-shaped text serialised without escaping, and two nits (1.47's journal says 21 spec files, the tree has 22 — K7's class again).
- **Turn budget, evidence only** (no new implementation attempts this block): unchanged from 025 — Phase 1 to date 26 of 58 `loop` tasks needed more than one attempt; `NO-JOURNAL` stops 14. Michael's call at the boundary.

## Gotchas

- **The reconciliation cherry-picks a three-commit range**: `git cherry-pick phase/1..handoff/026` in `.wt/1.10.r6d` — all three touch only `docs/**`. `handoff/025` alone is a proper prefix; never cherry-pick both branches separately (duplicate commits).
- **`.evidence/ci/1.verify.r6h/a1/abandoned.json` is the #023 misrecord**: a1 was rejected and superseded; `accepted.json` names a2; `plan.1.verify.r6h.r0` reads `abandoned` by the rerun. The reconciliation quotes #029, not the word.
- **Sol and Grok share the blocker line (`format.ts:193`); Claude's blocker is a different defect.** Count them as two blocker families, one shared by two reviewers and one unique to Claude; Sol 2 / Claude 3 is one shared Required. Count from the reports before any pattern is raised (#review-1-r0).
- **Reproduce in a detached scratch worktree**, never in `.wt/1.10.r6d` (no pnpm on the Mac in a task worktree). Sol's directory holds runnable probes (`serializer-probe.mjs`, `map-probe.mjs`, `unicode-browser.mjs` …) with their logs; read them as evidence, re-run them only from the scratch worktree.
- **The r7 chain's first task must fix `format.ts:193` on both halves** (the encoder's whole-scalar rule now for the two-reference case as well as the reference-beside-raw case, and the map's instrumentation of it); the K2 enumeration gains the `html` rewrite with a corpus case that fails without it (the CLAUDE.md rule "a corpus case asserting that nothing is unresolved is the guard that names which rewrites exist").
- **In this zsh** a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.
- `scripts/gate.sh` outruns Claude Code's 120 s foreground limit; when Michael runs it in his own shell (`! scripts/gate.sh …`) the output lands in his background task file, read it there.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. **Fresh session runs `1.10.r6d`** (#015): in `.wt/1.10.r6d` on `task/1.10.r6d`, `git cherry-pick phase/1..handoff/026`; copy the three reports byte-identically to `docs/reviews/phase-1-r6-{claude,sol,grok}.md` (`cmp` clean); reproduce every blocker and Required finding in a detached scratch worktree at `c2f6526`; write `#review-1-r6` with the verdict first (PRD §7 criteria), K1–K9 each confirmed or reopened, the reviewer record counted from the reports (Grok independent: started with its siblings), the turn-budget paragraph; on FAIL append the r7 chain to `ralph/tasks.json` on the `159c4c0` pattern (fix tasks from 1.49, `1.verify.r7` `needsCI` with `ci/1.verify.r7/a{n}`, `1.verify.r7h`, `1.10.r7a/b/c/d`, `1.close` re-pointed), `EXPECTED_COUNT` updated, `generate-tasks`, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green; journal line; commit with `<promise>DONE 1.10.r6d</promise>` in the message; idle checks; restart; watcher.
2. **Watch the r7 chain**; a capped attempt with the work complete → the retry-with-a-lesson shape (`chore(<id>): principal lesson` on the task branch, logs moved aside, `retry`); a 429 → wait for the reset Michael names; `/rotate` at `HUMAN_GATE 1.verify.r7h` and at half context.
3. **Phase boundary idle window** (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)` (two instances now, #029); H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5 (the stacked-handoff rule with its "already in the tree" condition, the third case), the stale `description` line, PRINCIPAL.md's doctor sentence, PRINCIPAL.md gaining Sol's r4 lesson, #028's, the r5 principal lessons, 025's description-vs-acceptance lesson; `git branch -D handoff/010 … handoff/026`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`; `ci/1.verify.r6/a1` and `a2` are both gc candidates) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise, the 429 signal (#027, two instances), the gate fetch-on-failure (#review-1-r3 I7 — a1 fetched no artifacts, the failure was read from `workflow.log`), the reviewer-prompt option.

## Open Questions

- **Whether Claude's autolink blocker reproduces at `c2f6526`** and whether it is Phase 1 scope (the editor has no autolink; the fixture corpus decides — if no fixture reaches the `<…>` form the reconciliation may disposition it as a Required with a corpus fixture, not a blocker; the verdict is still FAIL on the shared blocker).
- **Whether the two-reference astral case is one guard or a family** (astral between two marks, at a cell edge, at a heading end, before a soft break — #review-1-r5's filed risk lines); the reconciliation's probe decides the r7 fix shape, as #review-1-r5 did for 1.45.
- The turn budget and the 429 signal: Michael's calls at the boundary, evidence unchanged.
