# Handoff 019 — Phase 1 at `PRINCIPAL 1.10.r3d`: gate `1.verify.r3.g1h` accepted at a2 (transient rerun), r3 reviewers done (Claude FAIL, Sol FAIL, Grok PASS), reconciliation pending

Written 2026-09-11 by the principal (the session that watched gate `1.verify.r3.g1h` and the r3 review set; Claude Code, Remote Control session on the Mac Mini), at the `PRINCIPAL 1.10.r3d` stop signal (rotate regardless of context; the reconciliation is a fresh session, DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `018-phase-1-verify-r3-g1-gate.md` (on `handoff/018`, not in the tree). This commit is on `handoff/019`, which already carries `9bcd691` (`docs(0.0): DECISIONS #023`); the `1.10.r3d` reconciliation cherry-picks `handoff/017`, `handoff/018` and both commits of `handoff/019`. Next: `020-*.md`.

## Current State

- **`phase/1` tip `f90ed50`** (`task(1.verify.r3.g1)`), unchanged since handoff 018; 50 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`). No principal commit on `phase/1` since `b149f0c` and none until `1.close`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010).
- **Gate `1.verify.r3.g1h`: passed at a2.** a1 (ci.yml run 34572776730 at `f90ed50`, 07:05–07:13Z): macos-latest 79/79 (1.33's repair held), ubuntu-latest 79/79, windows-latest 78 passed / 1 failed — `editor-clipboard.spec.ts:126` "strong and emphasis round-trips through the clipboard", a 30 s `setInputFiles` timeout before any assertion, in a file unchanged since `task(1.32)` and green on windows at the four prior gates. Transient → `scripts/gate.sh rerun 1.verify.r3.g1h` (the build's first rerun): a2 (run 34573685490, ref `ci/1.verify.r3.g1/a2`, 07:17–07:31Z) 79/79/79, `ACCEPT`, `accepted.json` → a2, three OS directories in both artifacts; the plan request `plan.1.verify.r3.g1h.r0` was withdrawn by the rerun (`abandoned`, "withdrawn: transient rerun"). Then `doctor` reported a1 `incomplete … → --resume a1` (a doctor defect, and a destructive remedy); cut to a manual procedure: `scripts/gate.sh abandon 1.verify.r3.g1h a1 --reason "… (DECISIONS #023)"` wrote `a1/abandoned.json`, no state change, doctor clean. All of it in **DECISIONS `#023`** (`handoff/019` `9bcd691`).
- **Review attempt r3**: `1.10.r3a/b/c` passed at a1, 17:09:00Z → 17:26:34Z (17.5 min, three containers in parallel); `.evidence/reviews/1/r3/`: `verifier_id 1.verify.r3.g1`, `verification_sha == implementation_sha == f90ed50`. `status.json` (not the signal; the bodies are): Claude `FAIL` 1 blocker / 4 should-fix / 3 nits; Sol `FAIL` 1 blocker / 2 should-fix / 1 nit; Grok `PASS` 0 / 0 / 2 nits. Reports: `.evidence/reviews/1/r3/{claude,sol,grok}/report.md` (69, 58, 62 lines). Sol's probes and logs sit directly under `.evidence/reviews/1/r3/sol/` (`probe.mjs`, `paste-probe.mjs`, `cursor-probe.mjs`, `history-probe.mjs`, `fuzz.mjs` and their logs, `editor-diff.txt`) — evidence, not for the tree.
- **Findings headline** (numbered as in the bodies; the reconciliation dispositions them, this is orientation only): Claude 1 **blocker** `schema.ts:826` `giveUpEdges` + `:752–766` — a `hard_break` that is the last node of an emphasis/strong/delete run with unmarked text after it reaches `format`; Claude 2 **should-fix Required** `schema.ts:787` — the mark-edge clause splits a link-marked run and the split-off whitespace keeps the `link` mark; Claude 3 should-fix — a link's edge whitespace at a block boundary is dropped; Claude 4 should-fix — the accepted evidence is the a2 rerun (answered by #023; cite it); Claude 5 should-fix — the four backlogged `ControlOrMeta+Home/End` uses; Claude 6 nit — the fourth `x === x` (`editor-fixed-point.test.ts:763`, backlog line 54's trigger fires now); Claude 7 nit — `docs/reviews/phase-0-r0-claude.md:26` quotes the owner's e-mail address (public repo, #011); Claude 8 nit — backlog line 53 (Unicode whitespace at a mark edge) trigger fires now. Sol 1 **blocker** `schema.ts:594` — a list item keeps its parsed `spread` after a paste adds a paragraph (`nested-lists.md`, paste `X\nY`, Copy Markdown is not a fixed point); Sol 2 **should-fix Required** `position-map-editor-leg.test.ts:186` — the position-map editor leg is insertion-only (CLAUDE.md's destructive-transaction rule); Sol 3 **should-fix Required** `editor-soft-line-breaks.spec.ts:85` — G4 chord (the same construct as H8); Sol 4 nit — `x === x`. Grok 1 nit — `x === x`; Grok 2 nit — the four chord uses. Shared: `x === x` (3 of 3), the chord uses (3 of 3: should-fix / Required / nit) — #022's revisit trigger ("if a reviewer raises it at r3") has fired.
- **Tasks** (`ralph/ralph.sh status`): phase 1 — 63 tasks, passed 58, superseded 3, principal-pending 1 (`1.10.r3d`), pending 1 (`1.close`). `doctor` clean. Host checkout on `phase/1` at `f90ed50`, tree clean. `.wt/1.10.r3d` on `task/1.10.r3d` at `f90ed50`, clean, waiting for the reconciliation. Branches `handoff/010` … `handoff/019`. Lessons tagged `1.*`/`plan.1.*`: 106 (plus one `[0.0]` line on `handoff/019`).
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r3d` (runner.log line 69,724; the log is 69,725 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the reconciliation's DONE commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r3d` (line 69,724).
- **Expected next stop**: the reconciliation commits `wip(1.10.r3d): …` with `<promise>DONE 1.10.r3d</promise>` in `.wt/1.10.r3d`, restart → the runner squashes `task(1.10.r3d)` and runs `sync-state`; on FAIL the appended `1.34…` fix tasks run → `HUMAN_GATE 1.verify.r4h` (rotate there); on PASS `1.close` → `CLOSED` → rotate at the phase boundary on `phase/2` from the host checkout.

## Corrections

- `018` Expected next stop assumed `ACCEPT` at a1 or a `GATE-FAILED` planning commit numbered `#023`. Neither: a1 failed on a transient, a2 accepted after a rerun, and `#023` is the rerun-and-doctor entry (`handoff/019` `9bcd691`); a future planning commit is `#024`.
- `018` Next Steps 3 and `next-prompt.md` step 3 (restart on ACCEPT) omitted that `doctor` flags the rejected a1 after a rerun and that `ralph run` refuses on it; the manual procedure is `#023`'s abandon, once per rerun, until the boundary `fix(runner)`.
- `018` Current State counted 54 passed / 5 pending; at rotation it is 58 passed / 1 principal-pending / 1 pending (the gate and the three reviewers passed).
- `018` "the reconciliation cherry-picks `handoff/017` and `handoff/018`": it also cherry-picks the two commits of `handoff/019` (`git cherry-pick phase/1..handoff/019`); `DECISIONS.md` gets `#023` at its end before `#review-1-r3` is appended.

## Decisions

- **`#023` (DECISIONS.md, on `handoff/019` `9bcd691`, not yet in the tree)**: the transient rerun (why not a `.g2` repair: the failing file unchanged since 1.32, green on windows at four prior gates, a 30 s hang before the first assertion), the doctor false positive (`doctor.mjs` `recorded` has no clause for blocked → rerun → passed; the rerun conformance test never calls `doctor`), why the named remedy `--resume a1` is destructive (re-enters `ciGate`, writes `blocked`/`GATE-FAILED` over the a2 `passed` record, raises `plan….r1`), why no `fix(runner)` now (`stepReview` requires `phase/1` head == `f90ed50`; a commit before the reviewers started would have been `REVIEW-SHA-MISMATCH`), the manual procedure (`gate.sh abandon … a1`), the boundary fix scope (doctor clause; resume refused on a superseded attempt; one conformance test per guard plus `doctor` clean at the end of the transient-rerun test), and reversals. The reconciliation files the boundary fix in `docs/V1.1-BACKLOG.md` with hard stop `2.1` and cites `#023` against Claude finding 4.
- **Runner deviations this block**: the doctor false positive above is the one deviation (spec §2 defines incomplete as "no matching state transition"; a1 has one). Everything else per RUNNER-SPEC: GATE-FAILED → blocked → plan request; rerun from `blocked` with the request withdrawn; a2 ACCEPT → passed; `stepReview` recorded the r3 metadata and started three reviewers from one snapshot; `PRINCIPAL 1.10.r3d`.
- **Promote-lessons candidates for the r3 reconciliation** (unchanged from 018, both still pending): "each attempt appends its own journal line" (steps 2 and 4) and the Blink-only caret rule (`#022` wording in 018 Decisions). New from today, for the reconciliation to weigh: "a doctor remedy is read against `gate.mjs` before anyone runs it; every conformance scenario that ends in a new state ends with a doctor-clean assertion" (the `[0.0]` lesson on `handoff/019`).
- **Turn budget, evidence only (decision at the Phase 2 boundary on Michael's OK, #review-1-r1)**: `1.10.r3a/b/c` a1 each, no cap (reviewers run at `RALPH_REVIEW_MAX_TURNS` 160). Phase 1 loop tasks: 16 of 38 needed more than one attempt; `NO-JOURNAL` stops 11 — both unchanged since 017. The number stays 80, conditional.

## Gotchas

- **After any `gate.sh rerun` that accepts, `doctor` flags the rejected earlier attempt** and names `--resume a<n>` — never run it (it overwrites the accepted record). Procedure: `#023`'s abandon with a reason naming `#023`, then `doctor` clean, then restart. Until the boundary `fix(runner)` lands.
- **`handoff/019` holds two commits** (`9bcd691` DECISIONS `#023` + lessons, and this rotation). `git worktree add -b handoff/019` would fail; the next rotation is `handoff/020`, fresh. The reconciliation cherry-picks in order: `git cherry-pick phase/1..handoff/017`, `…018`, `…019` (each is only its own commits; verify with `git log --oneline phase/1..handoff/NNN` first: 017 and 018 one commit each, 019 two).
- **Claude finding 7 names the owner's e-mail address in `docs/reviews/phase-0-r0-claude.md:26`** — a review copy is a record (#016: files are not frozen, records are); the reconciliation decides whether a redaction commit is a record edit. Do not paste the address anywhere new.
- **Claude finding 4 is about the rerun itself.** `#023` is the answer; the reconciliation cites the four prior green windows runs and the unchanged file, and records that the abandon is a misrecord by name only.
- **The chord trigger fired** (3 of 3 reviewers; Sol rates `:85` Required). Per `#022` the revisit is "the r3 reconciliation if a reviewer raises it": on FAIL it is a test-only task in the `1.34…` chain plus a grep guard over `e2e/web/` in the `no-codemirror-history` shape; the source-view `Home`/`End` (CodeMirror keymap) stay out.
- **Evidence for the boundary `gc`**: `git ls-remote origin 'refs/heads/ci/*'` first; expect `ci/1.verify/a1`, `ci/1.verify.r2/a1`, `ci/1.verify.r3/a1` (GATE-FAILED rounds), `ci/1.verify.r3.g1/a1` (abandoned) and possibly `a2` (accepted); `gc` deletes accepted/rejected/abandoned only.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `ls .locks/*` with an empty directory prints "no matches found", which is not a lock. `grep -F -- '- [id]'` needs the `--`.

## Next Steps

1. Fresh session (this is the reconciliation session, #015): `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael (no ask yet — the reconciliation is the principal's own work in `.wt/1.10.r3d`).
2. In `.wt/1.10.r3d` (branch `task/1.10.r3d`, cut at `f90ed50`): `git cherry-pick phase/1..handoff/017 && git cherry-pick phase/1..handoff/018 && git cherry-pick phase/1..handoff/019`; copy the three reports verbatim to `docs/reviews/phase-1-r3-{claude,sol,grok}.md`; read the bodies (not `status.json`) and verify each blocker yourself before dispositioning (Sol 1: load `fixtures/markdown/nested-lists.md`, paste `X\nY`, Copy Markdown; Claude 1: a `hard_break` as the last node of a mark run with unmarked text after it); write `DECISIONS.md#review-1-r3` after `#023` with `verdict: PASS|FAIL` first (each finding numbered with its disposition; H1–H9 confirmed or reopened; the per-reviewer unique-findings table re-derived from the bodies; the `chore(1.29)` principal-lesson commit audited as a deviation of the #016 shape; #022's scope decision and the four chord uses against Claude 5 / Sol 3 / Grok 2; `#023` against Claude 4; the two 018 promote candidates plus today's doctor lesson carried into the next verify task's promote-lessons text; the turn-budget note with counts read from `.evidence/state/tasks.json` and the transcripts at the time of writing); backlog lines (the `#023` boundary fix, hard stop `2.1`; Claude 6/8's fired triggers dispositioned, not re-deferred); regenerate `docs/progress.md`; on FAIL append `1.34…` + `1.verify.r4` + `1.10.r4a/b/c/d` in PRD §8 and `ralph/tasks.json` the way `e6d7493` did (`node ralph/generate-tasks.mjs`, `EXPECTED_COUNT` 197 → new count read from the generator's output, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green); commit `wip(1.10.r3d): …` with `<promise>DONE 1.10.r3d</promise>` in the message; touch only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (§8.1).
3. Idle checks, restart; the runner integrates `task(1.10.r3d)`, runs `sync-state`, then the first `1.34…` task (FAIL) or `1.close` (PASS). Rotate at the next stop signal (`HUMAN_GATE 1.verify.r4h` or `CLOSED`).
4. Phase boundary items (only after `1.close`, host checkout on `phase/2`, runner idle): `#023`'s `fix(runner)` (doctor clause + resume refusal + conformance); H7's `fix(runner)` on `ralph/PROMPT.md` + `tests/agent-rules.test.ts`; `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `git branch -D handoff/010 … handoff/019`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); the conditional turn-budget `fix(runner)` only on Michael's OK; the CLAUDE.md promote candidates if not yet promoted.

## Open Questions

- The r3 verdict: two reviewers FAIL on different blockers (Claude: `hard_break` at a mark run's end; Sol: a list item's `spread` after a paste); Grok PASS. The reconciliation's own reproduction decides each; a confirmed blocker is FAIL and the r4 chain.
- Whether the four chord uses become one test-only task in the r4 chain (three reviewers raised them; Sol Required) or, on PASS, Phase 2 planning per `#022` — on PASS with a Required finding the severity rule says FAIL, so the question only matters if Sol 3 is downgraded on reading.
- The turn-budget number: 80 proposed, conditional; the r3 chain's a1 record continues — Michael's call at the boundary.
