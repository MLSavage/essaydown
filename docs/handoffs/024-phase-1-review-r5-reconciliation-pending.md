# Handoff 024 — Phase 1 at `PRINCIPAL 1.10.r5d`: gate `1.verify.r5h` accepted, the r5 review set passed with three FAIL verdicts, the reconciliation pending for a fresh session

Written 2026-09-12 by the principal (the session that handed Michael the `1.verify.r5h` gate and watched the r5 review set; Claude Code, Remote Control session on the Mac Mini), at the `PRINCIPAL 1.10.r5d` stop signal. Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `023-phase-1-verify-r5-gate.md` (on `handoff/023`, not in the tree). This commit is on `handoff/024`, cut from `handoff/023` (not from `phase/1`) so the chain is linear: `git log --oneline phase/1..handoff/024` must show five commits — `e03ee87` (DECISIONS #026), `eb59116` (#027), `e305be6` (#028), `0424d44` (handoff 023), and this one. The `1.10.r5d` reconciliation cherry-picks all five with one command. Next: `025-*.md`.

## Current State

- **`phase/1` tip `3dd9c4b`** (`task(1.verify.r5)`); 67 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean. No commit on `phase/1` this block (a gate accept and a review set write only evidence).
- **Gate `1.verify.r5h`: ACCEPT** at attempt a1, GitHub run `34705334786`, CI ref `ci/1.verify.r5/a1` at `3dd9c4b`; the run's evidence is under `.evidence/ci/1.verify.r5h/accepted/` (`workflow.log`, `result.json`, `run.json`, `playwright-report`, `test-logs`). `scripts/gate.sh` printed `ROTATE-PRINCIPAL` after ACCEPT.
- **Review set `1.10.r5a/b/c`: passed, attempt 1 each**, started together at `16:36:40Z` (three `started_at` values within 200 ms in `.evidence/state/tasks.json`) and finished at `16:57:45Z`; Grok started with its siblings, so its r5 report counts as independent (#025's condition). Reports and transcripts under `.evidence/reviews/1/r5/{claude,sol,grok}/` (`implementation_sha` = `verification_sha` = `3dd9c4b`, `verifier_id` `1.verify.r5`). Verdicts from the three `status.json` files: **claude FAIL** (1 blocker, 3 should-fix of which 1 Required, 4 nits); **sol FAIL** (1 blocker, 1 should-fix Required, 0 nits); **grok FAIL** (0 blockers, 1 should-fix Required, 2 nits). Sol left its probes in its directory (`unicode-browser.{mjs,log}`, `serializer-probe.{mjs,log}`, `map-probe.{mjs,log}`, `fuzz.log`, the full test/lint/cargo/e2e logs).
- **`1.10.r5d`: principal-pending**, worktree `.wt/1.10.r5d` on `task/1.10.r5d` at `3dd9c4b` (the runner created it). Graph 222 tasks (`EXPECTED_COUNT` 222).
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.10.r5d principal-pending; last passed 1.10.r5c; gates open: none; plan requests: none; blocked: none."
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r5d` (runner.log lines 89,899–89,900; the log is 89,900 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the reconciliation's DONE commit and its restart-time idle checks: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter` — the runner integrates `1.10.r5d` (squash onto `phase/1`, suite on the candidate) and starts the first task the reconciliation appended.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r5d` (line 89,899).
- **Expected next stop**: none until the reconciliation commits. After its restart: `[ralph] 1.45 attempt 1` (the r6 chain's first task), then `STUCK 1.4x` on a capped or conflicted fix task, `HUMAN_GATE 1.verify.r6h` at the chain's end (rotate there), `INTEGRATION-FAILED 1.10.r5d` only if the squash's suite differs from `3dd9c4b`'s (it should not: the reconciliation touches `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` only).

## Corrections

- `023` "the `1.10.r5d` reconciliation cherry-picks all four commits (`git log --oneline phase/1..handoff/023` must show four)": now **five**, `git cherry-pick phase/1..handoff/024`; `handoff/024` is stacked on `handoff/023`. No `next-prompt.md` conflict is expected — `phase/1` has no commit under `docs/handoffs/` since `cef59c2` (the merge base of both handoff branches) — so 023's "conflicts resolve to the incoming version" applies only if one appears.
- `023` Expected next stop, "`STUCK 1.10.r5` if a reviewer fails": did not happen; all three reviewers exited 0 with a report and `status.json`; #025's manual procedure and the Grok-402 branch were not needed.
- `023` Open Questions, "a reviewer rating [the position-map gap] Required makes r5 FAIL and the r6 chain one task": the gap is Required by all three, **and** Claude and Sol each found a second, independent blocker (the astral-character encoding, below) that 023 did not anticipate — the r6 chain is at least two implementation tasks, not one.
- `023` Current State claimed the CI ref name `ci/1.verify.r5/a1`; the gate evidence directory is `.evidence/ci/1.verify.r5h/a1/` and `…/accepted/` (gate id, not verifier id). Both existed; the ref name is right, the directory is keyed by the gate.

## Decisions

- **No DECISIONS entry this block.** `#026`–`#028` stand on `handoff/023` (`e03ee87`, `eb59116`, `e305be6`); the next entry is `#review-1-r5` (the reconciliation's) and the next numbered one `#029`.
- **Rotation deviation (this commit)**: `.claude/commands/rotate.md` step 5 cuts `handoff/NNN` from `phase/<N>`; this one is cut from `handoff/023` because a second branch from `phase/1` that also edits `next-prompt.md` would make the reconciliation's cherry-pick a two-range, conflicting sequence. Reverse: `git revert` this commit; the branch base is a fact of the graph, not a file. Candidate `chore(0.0)` for the boundary window: rotate.md step 5 gains "when an unmerged `handoff/NNN` exists, cut the next from it and cherry-pick the whole range".
- **Runner deviations this block: none.** The gate's ACCEPT, the three-reviewer start from `3dd9c4b`, the per-reviewer `status.json`, and the `PRINCIPAL 1.10.r5d` stop were per RUNNER-SPEC §3, §5.5. The gate command ran past the harness's 120 s foreground limit and was moved to the background by Claude Code, not by the runner (exit 0, `outcome: ACCEPT`); no action.
- **The r5 findings, as the three reports state them** (the reconciliation confirms each in a scratch worktree; nothing here is a disposition):
  1. **Blocker, shared by Claude and Sol, absent from Grok** — `packages/core/src/format.ts` (`classifyCharacter`, `encodeCharacterReference`, `handleDelete`'s `open`/`close`) and the built-in `emphasis`/`strong` handlers classify and encode one UTF-16 unit (`charCodeAt(0)`; `mdast-util-to-markdown/lib/util/container-phrasing.js:90–116` encodes `charCodeAt(0)` and slices one unit). An astral (non-BMP) character directly outside a flanking mark whose inner edge is punctuation or whitespace is written as a lone-surrogate reference (`~~a.~~&#xD83D;\uDE00`), which reparses to U+FFFD: the character is destroyed on Copy Markdown (status "Copied Markdown") and reaches the document through the 1.7 toggle. Claude: 2,208-tree headless sweep, 72 fail, exactly {astral outside} × {punctuation or whitespace inside}, all three marks, both sides; invariant A fails on `parse("~~a.~~&#x1F600;\n")` and `*a.*&#x1F600;`; a Phase 0 gap for `*`/`**` extended to `~~` by 1.40, unreachable by every corpus leg because no fixture holds a non-BMP character. Sol: the same on `~~a.~~ 😀` with the r4 caret route (one Backspace), supplementary letters `𐐀`, `𝕒` too, both sides. Both name the fix class: own the astral case in app handlers with `codePointAt` (the J1 rule, both halves), a `text` handler or boundary-encoding step that emits a whole scalar as `&#x1F600;`, guards astral-outside × {punctuation, whitespace, inline code, link} inside × both sides × three marks asserting bytes, one mark, parsed text equal to the tree's text, and the fixed point; an invariant-A fixture (`astral-neighbour.md` with canonical sibling and index entry) so every corpus leg reaches it; a Playwright typed route (`keyboard.type` of `*wow!*` then an emoji, Copy, `toBe`). No new dependency; the upstream fix is not in PRD §4's gift.
  2. **Should-fix Required, shared by all three** — `packages/core/src/positions.ts:455` (`placeChildren`, task 1.2): a text child whose edge character `containerPhrasing` encoded is looked up by `indexOf(child.value)` and missed, so it is `unresolved`, `cursorMap` has no range or spelling for it, and `toSource` of a caret inside the neighbour falls to `afterLastNode`, which answers the end of the preceding run. Reproduced in Chromium by all three on the J1 seed: toggle then type `X` lands inside the struck run (`Alpha ~~beta.X~~gamma delta`); Sol's `x *a.*&#x62;` → `x *a.X*b`. This is 1.40's `it.fails` pin (`positions-corpus.test.ts:114`, `OUTSIDE_ENCODED_NEIGHBOUR`) and the `[1.40, found outside scope]` backlog line whose revisit is this review, hard stop `1.close`; DECISIONS #028 already names it the r6 chain's task. Fix shape (all three agree): retry the missed text child with its first and/or last code point replaced by its `&#x…;` reference (the only two rewrites `container-phrasing.js` makes; after finding 1, the code-point form), record it in the node's `SpellingTable`, delete the pin and the exemption, a clause in `position-map-editor-leg.test.ts`'s deletion leg asserting `toSource` monotone over the encoded neighbour and `toRendered` its inverse, a Playwright toggle-then-type case with counted arrows.
  3. **Should-fix (Claude 3)** — the tree at `3dd9c4b` cites DECISIONS #026, #027, #028 (`docs/lessons.md:402,413`, the journal at `:292,314`) that are not in the tree: they are on `handoff/023`. Discharged by the reconciliation's cherry-pick; record it as such in `#review-1-r5`.
  4. **Should-fix (Claude 4)** — `packages/editor/test/editor-fixed-point.test.ts:1466–1505`, the two neighbour legs: the assertion 1.41's text (as narrowed by #026 Decision 1) names — the decoded text equals the editor's text — is absent; the legs assert the fixed point, mark counts and `expectNoForbiddenEntity` only. Grok's report notes the same legs never call `formatWithMap`/`cursorMap`.
  5. **Nits** — the whitespace-outside × whitespace-inside `encodeInfo` cell (`format.ts:94`) has no guard (Claude 5, Grok 2, shared); `tests/no-caret-chords-in-e2e.test.ts` matches only the double-quoted key-name form (Claude 6) and its per-file `it` title still names the narrower class (Grok 3); `1.verify.r5`'s journal says 23 spec files under `e2e/web/` where the directory holds 20 (Claude 7); `schema.ts:711` `isSetextHeading` has no `KINDS` feeder (Claude 5); turn-budget evidence only (Claude 8).
- **Reviewer record for `#review-1-r5`** (count before any pattern): Claude 8 findings, Sol 2, Grok 3; unique to one reviewer: Claude 3, 4, 6, 7, 8 and the `isSetextHeading` half of 5, Grok 3; shared: the blocker (Claude 1 = Sol 1, both by independent routes — Claude a typed emoji, Sol the r4 Backspace route with letters too), the Required (all three), the whitespace cell (Claude 5 = Grok 2). Severity agrees on every shared finding. Grok's independence section says it could have found r4's J1 and J3 from the tree; that is its own claim to weigh, not a count.
- **Turn budget, evidence only**: unchanged from 023 (1.40 a1 51; 1.41 51 ×4 then a 429 and a `NO-JOURNAL`; 1.42 51 ×3; 1.43 42/13/8 on an undecided conflict; 1.44 35; 1.verify.r5 43). Claude's finding 8 reads the same numbers from `/logs/tasks/`. Michael's call at the boundary.

## Gotchas

- **The reconciliation cherry-picks five commits, not four**: `git cherry-pick phase/1..handoff/024` from `.wt/1.10.r5d`; verify `git log --oneline phase/1..handoff/024 | wc -l` prints 5 first.
- **The r6 chain is at least two implementation tasks**: the astral encoding fix (finding 1, `format.ts`, an invariant-A fixture, corpus legs, a typed Playwright route) and the `placeChildren` fix (finding 2, `positions.ts`, the pin folded). Order matters: finding 2's retry must match the code-point reference form finding 1 introduces, so the encoding fix runs first; the planner names the dependency. A test-only task can carry Claude 4 (the decoded-text assertion) and the nits, on the `1.44` pattern; do not fold it into the fix tasks (Sol's r4 lesson: a test-only task's scope is not a deferral reason, and a fix task's tail is where caps happen).
- **Every task text in the r6 chain names its journal line and suite run explicitly** (the 1.40–1.43 retries' lessons): "append a NEW `- [<id>] ` line with `printf`", "run the suite once", and no "no expected fail" clause that a pin elsewhere contradicts (#028) — after the `placeChildren` task the suite has zero expected fails, so that clause is safe only on tasks sequenced after it.
- **Reproduce before dispositioning**: `git worktree add --detach <scratchpad>/probe 3dd9c4b`, `pnpm install --frozen-lockfile` there (never in a `.wt/<id>`); Sol's `serializer-probe.mjs` and `map-probe.mjs` in `.evidence/reviews/1/r5/sol/` are runnable seeds; remove the probe worktree after.
- **Before any `ralph.sh retry <id>`**: `mv .evidence/tasks/<id>/<n>.log <n>.<suffix>.log` for every attempt log first (#027).
- **In this zsh** a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.
- **`scripts/gate.sh` runs longer than Claude Code's 120 s foreground limit** and gets backgrounded with a notification; read its output file, not the pane. Harmless.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. **The `1.10.r5d` reconciliation (this next session — fresh, #015)** in `.wt/1.10.r5d` on `task/1.10.r5d`: `git cherry-pick phase/1..handoff/024` (five commits); copy the three reports verbatim to `docs/reviews/phase-1-r5-{claude,sol,grok}.md`; reproduce findings 1 and 2 in a scratch probe worktree at `3dd9c4b`; write `DECISIONS.md#review-1-r5` after `#028` with `verdict: FAIL` first (three FAIL verdicts, two blockers by independent routes), J1–J9 of `#review-1-r4` confirmed or reopened (J1 reopened for the astral case: the flanking family was closed for BMP neighbours only), the 1.40 pin dispositioned into the `placeChildren` task, findings 3–5 dispositioned (3 by the cherry-pick; 4 and the nits to a test-only task or the backlog with a revisit trigger), the reviewer record above, the turn-budget paragraph; append the r6 chain to `ralph/tasks.json` on the `cef59c2` pattern — `1.45` (astral encoding, `format.ts` + fixture + corpus legs + typed e2e), `1.46` (`placeChildren`, depends on 1.45, pin folded, editor-leg clause, toggle-then-type e2e), `1.47` test-only (Claude 4, the whitespace cell, the chord-guard regex and title, the journal count), `1.verify.r6` (`needsCI`, `ci/1.verify.r6/a{n}`), `1.10.r6a/b/c/d` (`review-set`); `node ralph/generate-tasks.mjs`, `EXPECTED_COUNT` from the printed count, `--check`, `validate-tasks`, `bash ralph/test/run.sh` green; `docs/progress.md` regenerated; commit `wip(1.10.r5d): …` with `<promise>DONE 1.10.r5d</promise>`, touching only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`; idle checks; restart; arm the watcher; rotate at `HUMAN_GATE 1.verify.r6h` or at half context.
2. **During the r6 chain**: a `STUCK` on a fix task → read the attempt logs, a `chore(<id>): principal lesson` on its task branch (`docs/lessons.md` only, reversal in the message, the journal-line and run-the-suite-once sentences), logs moved aside, `ralph/ralph.sh retry <id>`, restart. A conflict inside a task's own acceptance is decided by naming the sentence that yields (#026 Decision 1's shape), never by widening scope (#028).
3. **At `HUMAN_GATE 1.verify.r6h`**: rotate; the next session hands Michael `scripts/gate.sh 1.verify.r6h`, restarts on ACCEPT, rotates at `PRINCIPAL 1.10.r6d`. On `GATE-FAILED` the `0251db3` planning pattern (`ralph/ralph.sh plan plan.1.verify.r6h.r0`, a `.g1` repair scoped to what failed).
4. **Phase boundary idle window** (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s `fix(runner)`; H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5's third case and the stacked-handoff rule above, the stale `description` line, PRINCIPAL.md's doctor sentence, PRINCIPAL.md gaining Sol's r4 lesson and #028's; `git branch -D handoff/010 … handoff/024`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise, the 429 signal (#027), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option.

## Open Questions

- **Where the astral fix lives**: Claude proposes app-registered `emphasis`/`strong` handlers mirroring the built-ins plus an app-owned `text` handler; Sol proposes the app's boundary-encoding step consuming and emitting whole scalars on both sides. Both stay inside PRD §4; the reconciliation picks after the probe shows which one `containerPhrasing`'s own rewrite (`encodeAfter === value.slice(0, 1)`) can be kept from firing on. The chosen shape becomes 1.45's acceptance, not a lesson line.
- **Whether 1.47 (test-only) precedes or follows 1.46**: the decoded-text assertion (Claude 4) is independent of both fixes; the whitespace cell guard is independent; sequencing it last keeps the fix tasks' tails short. The planner's call.
- The turn budget and the 429 signal: Michael's calls at the boundary, evidence unchanged.
