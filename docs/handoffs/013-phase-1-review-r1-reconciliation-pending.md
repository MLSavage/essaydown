# Handoff 013 — Phase 1 at `PRINCIPAL 1.10.r1d`: gate `1.verify.r1h` accepted, review attempt r1 complete (Claude FAIL, Sol FAIL, Grok PASS), reconciliation pending

Written 2026-09-10 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `PRINCIPAL 1.10.r1d` stop signal (a reconciliation is never run by the session that watched the reviews; DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `012-phase-1-verify-r1-gate.md` (on `handoff/012`, not in the `phase/1` tree). This commit is on `handoff/013` (rotate.md step 5: a reconciliation is pending, so never on `phase/1`); the `1.10.r1d` reconciliation cherry-picks `handoff/012` then `handoff/013` (010 and 011 are already in `phase/1` since `b269627`). Next: `014-*.md`.

## Current State

- **`phase/1` tip `91a3de5`** (`task(1.verify.r1)`, attempt 1); 31 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`). No commit on `phase/1` since handoff 012 and none until `1.close`. `main` unchanged at `052e206`. `origin/phase/1` is at `4590b8d` (Michael pushes by hand, #010); `origin` still holds the stale `ci/1.verify/a1` at `3e98707` and now `ci/1.verify.r1/a1` at `91a3de5` (the accepted gate ref; `gate.sh gc` at the phase boundary).
- **Gate `1.verify.r1h`**: `ACCEPT` at attempt 1 — `.evidence/ci/1.verify.r1h/accepted.json`: run `34437589101` on `ci/1.verify.r1/a1` at `91a3de53…`, workflow `ci.yml`, artifacts `test-logs` (108,982 bytes) and `playwright-report` (1,881,901 bytes), recorded `2026-09-10T04:39:27Z`. The 1.17 latency spec ran on all three OS runners and passed (Claude's report: 59 Playwright tests, 12 spec files, green on all three; Vitest 1845/0 identical on all three).
- **Review attempt r1**: `1.10.r1a/b/c` passed at attempt 1, one container run each, started `04:40:12Z`, finished `04:58:45Z` (~18.5 min), every `exit-code` 0. `.evidence/reviews/1/r1/`: `verifier_id` = `1.verify.r1`, `verification_sha` = `implementation_sha` = `91a3de5`; Claude and Grok ran with `--max-turns 160`; no `REVIEW-SHA-MISMATCH`, no `STUCK`. `status.json` verdicts: **Claude FAIL** (blockers 1, should-fix 3, nits 5; 9 findings), **Sol FAIL** (blockers 0, should-fix 4, nits 0; "all four should-fix findings are required"; cold build in `/scratch/sol`, `pnpm install --frozen-lockfile`, lint, test, cargo, full Playwright, isolated `latency.spec.ts --workers=1`, two independent browser drivers `probes.cjs`/`cursor-undo.cjs`, credential scan, no code edited), **Grok PASS** (blockers 0, should-fix 2, nits 2). Verdicts are not the signal (#review-1-r0): the reconciliation reads the bodies.
- **Tasks** (`.evidence/state/tasks.json`): everything through `1.10.r1c` passed; **`1.10.r1d` principal-pending, attempts 0**, `task_branch task/1.10.r1d`, worktree `.wt/1.10.r1d` cut by the runner at `91a3de5`; `1.close` pending. Graph 170 tasks, `doctor` clean, host checkout on `phase/1` at `91a3de5`, tree clean, `.wt/` holds `1.10.r1d` only (plus this rotation's temporary worktree, removed by it), branches `handoff/010`, `handoff/011`, `handoff/012`, `handoff/013`.
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r1d` / `[ralph] 1.10.r1d: work in …/.wt/1.10.r1d on task/1.10.r1d; commit wip(1.10.r1d) with <promise>DONE 1.10.r1d</promise> …, then ralph.sh run again`. `.locks/` empty, no `ralph run` process, no containers. Restart after the reconciliation commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r1d` (log line 53,123; log at 53,124 lines).
- **Expected next stop**: after `wip(1.10.r1d): …` with the DONE promise on `task/1.10.r1d` and the restart, the runner integrates `task(1.10.r1d)` and `sync-state` reads the verdict from `DECISIONS.md#review-1-r1`. On PASS: `1.close` runs → `CLOSED` / phase boundary. On FAIL: the planning commit's fix chain (`1.23…`), then `1.verify.r2` → `HUMAN_GATE 1.verify.r2h` → `1.10.r2a/b/c/d`.

## Corrections

- `012` Current State said `.locks/` empty is part of the idle condition — true, but it is also empty while reviewers run: `withLock` (`ralph/lib/util.mjs`) holds `.locks/ralph` only around a critical section, never for the length of a container run. Idle is `.locks/` empty **and** `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty **and** `docker ps` empty **and** the pane at the prompt.
- `012` Expected next stop said the runner prints `[ralph] 1.10.r1a attempt 1 …` when the reviewers start. A review set prints no `[ralph] <id> attempt` line (none for r0 either); the evidence of the start is the three containers (`essaydown-{claude,codex,grok}-review-run-*`) and `.evidence/reviews/1/r1/{verifier_id,verification_sha,implementation_sha}`. The first log line after the restart was `PRINCIPAL 1.10.r1d`.
- `012` Gotchas said "9 of the 14 loop tasks" needed a second attempt. Read from `.evidence/state/tasks.json` now: of the 23 Phase 1 loop tasks (1.1–1.8, 1.11–1.22, 1.verify, 1.verify.g1, 1.verify.r1), **10 needed more than one attempt** — 1.3, 1.4, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17 at 2 and 1.21 at 3 (1.9 is the human record; its `attempts 2` is the superseded r0 record, not a container run). The reconciliation re-reads the file before writing the number.
- `012` Gotchas, gate bullet: `gate.sh` printed `ROTATE-PRINCIPAL` at ACCEPT as designed; no second rotation was done there (the rotation had already happened). Correct as written; recorded so the next verifier gate is not read as needing two rotations.

## Decisions

- **No DECISIONS entry and no runner deviation this block.** The gate ran clean at attempt 1; the review set ran clean at attempt 1; nothing was retried, re-cut or repaired. `.evidence/ci/1.verify.r1h/` holds `accepted.json`, `run.json` and the two artifacts; the accepted `.g1h` inputs were available to Sol (its report lists `/logs/ci/1.verify.r1h/accepted/` and `1.verify.g1h/accepted/`).
- **Watcher shape**: the gate outcome is not in `runner.log` (`gate.sh` writes `.evidence/ci/<id>/accepted.json`), so this session ran a second `Monitor` that polled for `accepted.json`/`rejected.json` and exited on the first hit. In zsh a glob with no match aborts the script (`no matches found`): a watcher that globs `a*/workflow.log` needs `setopt nullglob` first. Worth keeping for every future CI gate.
- **Findings, as read from the report headings (bodies to be read by the reconciliation; counts to be re-derived there, not copied from here)**:
  - Shared by all three: (i) **Copy Markdown during the source-view deferred commit** — `DevEditor.tsx:206-209` formats the store's last committed root without `sourceBinding.current?.flush()`, so a copy during a source burst (1.17's deferred parse) copies stale text and reports success: Claude **blocker** #1, Sol should-fix #1, Grok nit #3. (ii) **Rendered typing collapses soft line breaks** in the edited paragraph to spaces (`schema.ts` `INLINE_WHITESPACE`, ~151–193): Claude should-fix #3, Sol should-fix #3, Grok should-fix #2 (Grok's e2e reference `editor-cursor.spec.ts:89`, `> alpha\n> beta`).
  - Shared by two: (iii) **`undoKeymap` on the rendered side has no test reading the binding table's key list** (the F9 class survives at `store.ts:230-243`): Grok should-fix #1, Claude nit #5.
  - Unique — Sol #2 source Undo/Redo runs on committed history without settling the pending burst (`store.ts:258`); Sol #4 the position-map family has no editor-output-seeded corpus leg (`positions-corpus.test.ts:79`; the rule promoted by `1.verify.r1`); Claude #2 leading whitespace of the text run *after* a node is the `&#x20;` family, not one defect (`schema.ts:605-612`, `editor-fixed-point.test.ts:59`); Claude #4 coverage `include` excludes `apps/desktop/src/**` (~700 lines, 8 files, the directory where the phase's defects cluster); Claude nits #6–#9 (latency `pane` metric reports `n=0 of 120` at both sizes; the V1.1-BACKLOG revisit trigger wording; one line of Michael's 1.9 record carried nowhere in #002-outline-feel; `serde_json`/`tauri-build` citations in `docs/dependencies.json`); Grok #4 `expect(checked).toBe(Object.keys(index).length)` is `x === x` (`editor-clipboard.spec.ts:99`).
  - Unique-finding count by reviewer, from the headings: Claude 6 of 9, Sol 2 of 4, Grok 1 of 4. No reviewer is consistently low on the three shared findings (Grok rates (i) a nit where Claude rates it a blocker; Claude rates (iii) a nit where Grok rates it should-fix). No reviewer change is proposed; none is allowed inside a review set (#review-1-r0).
- **Rotation decision**: `PRINCIPAL 1.10.r1d` ends the working block; the reconciliation is a fresh session (#015), so this session rotates without opening `.wt/1.10.r1d`.

## Gotchas

- **The reconciliation's first moves** in `.wt/1.10.r1d` (branch `task/1.10.r1d` at `91a3de5`): `git cherry-pick handoff/012` then `git cherry-pick handoff/013`. Both touch `docs/handoffs/next-prompt.md` from the same base (013 was written on top of 012's content but not on its branch): expect a conflict on `next-prompt.md` only — resolve to the 013 version — and none on the two `NNN-*.md` files. Record the resolution in #review-1-r1 the way #review-1-r0 recorded the 010/011 one.
- **Two of three reviewers say FAIL, and findings (i) and (ii) are shared by all three with should-fix or worse from two reviewers each.** A FAIL reconciliation follows `b269627` (`task(1.10.r0d)`, #review-1-r0): fix tasks `1.23…` + `1.verify.r2` + `1.10.r2a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `EXPECTED_COUNT` bumped, `1.close` rewired to depend on `1.10.r2d`, `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` green (83 runner tests at `91a3de5`), the DECISIONS section with `verdict: FAIL` as its first line (`run.mjs` `verdictFromDecisions` reads it). The reconciliation commit touches only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1). Finding (i) is a 1.17 regression: 1.17 is passed and is never edited; the fix is a new task.
- **Finding (ii) is the one to size carefully**: the `INLINE_WHITESPACE` decision in `schema.ts` was made in the r0 fix chain (1.13/1.14 territory) and the three reports disagree on remedy scope (preserve soft breaks in the edited paragraph vs. the whole document). Read the three bodies before writing the task text; the task text should name the corpus leg (`editor fixed point`) that must stay green and add a fixture with soft breaks if none exists.
- **Turn budget** (012 Open Question): the count is 10 of 23 loop tasks over one attempt (Corrections above), every one with its implementation committed at the cap. Decide with Michael at the reconciliation: raise `RALPH_MAX_TURNS` in `docker/compose.yml` `x-env` (a `docker/` commit → `docker compose -f docker/compose.yml build` with `ESSAYDOWN_ROOT` before the next container task) or split large opus tasks at planning time. If the r2 fix chain is planned, the decision applies to it, so it is decided first.
- **1.17's 25× factor**: no reviewer required tightening it; Claude nit #6 says the pane metric is `n=0 of 120` at both sizes (the measurement, not the factor, is the weak part) and riskiest-thing #3 says the deferred parse moved cost off the keystroke without bounding where it went. Record or task it in #review-1-r1; a code change belongs in the r2 chain only if FAIL stands for other reasons.
- **`docs/dependencies.json` is live** (1.20): any fix task that adds a package adds its entry in the same diff or `pnpm lint` fails.
- **Reports**: copy `.evidence/reviews/1/r1/{claude,sol,grok}/report.md` to `docs/reviews/phase-1-r1-{claude,sol,grok}.md` verbatim (the r0 pattern); Sol's `/report/` directory also holds `probes.cjs`, `cursor-undo.cjs`, `reviewed-diff.txt` and per-command logs — evidence, not for the tree.
- `origin` holds `ci/1.verify/a1` (stale) and `ci/1.verify.r1/a1` (accepted): `gate.sh gc` by Michael at the phase boundary, low priority.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers.

## Next Steps

1. Fresh session (the reconciliation): `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael; `cd .wt/1.10.r1d && git cherry-pick handoff/012 && git cherry-pick handoff/013` (resolve `next-prompt.md` to 013); read the three reports in full; the three reports to `docs/reviews/phase-1-r1-{claude,sol,grok}.md`; `DECISIONS.md#review-1-r1` with `verdict: PASS|FAIL` first, each finding numbered with its disposition, each r0 disposition (F1–F16) confirmed or reopened, the per-reviewer unique-findings table (post.1's input), the turn-budget decision and the 25× decision with counts read from `.evidence/state` at the time of writing; journal entry; `wip(1.10.r1d): …` with `<promise>DONE 1.10.r1d</promise>`; restart.
2. On FAIL (the expected path): fix tasks + `1.verify.r2` + `1.10.r2a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `1.close` rewired, the way `b269627` did it; the fix chain runs (three attempts each before `STUCK`; a `NO-JOURNAL` at a1 is restarted with the same run command; a re-cut per #016 with `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart) → `1.verify.r2` → `HUMAN_GATE 1.verify.r2h` → rotate there regardless of context.
3. On PASS: `docs/progress.md` regenerated in the same commit, restart → `1.close` → `/rotate` at the phase boundary (on `phase/2` from the host checkout, runner idle) → `git branch -D handoff/010 handoff/011 handoff/012 handoff/013`; ask Michael for `scripts/gate.sh gc` (or `git push origin --delete ci/1.verify/a1 ci/1.verify.r1/a1`) and the `phase/1` push (#010).
4. Phase boundary idle window (unchanged from 012): the `chore(0.0)` commit for rotate.md step 5 third case + the stale `description` line (009 Corrections); post.1's inputs include the per-phase unique-findings table (PRD §8 post.1); add `setopt nullglob` and the `accepted.json` monitor to the gate-watch recipe in `.claude/commands/rotate.md` or the runbook if one exists by then.

## Open Questions

- Turn budget: raise `RALPH_MAX_TURNS` (to what) or split large opus tasks — decided at the reconciliation with Michael, before the r2 chain is planned if there is one.
- Whether finding (ii)'s remedy is scoped to the edited paragraph or the whole document, and whether it is one task or two with (Claude #2) the after-node whitespace family — after reading the three bodies.
- Whether Claude #4 (coverage `include` for `apps/desktop/src/**`) is a Phase 1 fix task or a Phase 2 planning note — it is test infrastructure, not product code, and PRD §4's per-file threshold config already exists.
