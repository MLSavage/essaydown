# Handoff 008 — Phase 0 closed (review r2 PASS); Phase 1 not yet started (runner idle at the phase boundary)

Written 2026-09-07 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the phase-close rotation (PRINCIPAL.md Handoff cadence). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `007-phase-0-verify-r2-gate.md`; the reconciliation checklist for Phase 1 is the r2 pattern in `052e206` (`git show --stat 052e206`, DECISIONS #review-0-r2) rather than 002's Next Steps 4. Next: `009-*.md`.

## Current State

- **Phase 0 is closed.** `0.close` ran at 11:19Z: `main` moved from the seed `660f3aa` to `052e206` (`task(0.12.r2d)`, reconciliation, verdict PASS), `phase/1` created at `052e206`; `.evidence/closes/0.json` names review attempt r2, verifier `0.verify.r2`, `main_sha 052e206`; `.evidence/reviews/0/r2/verdict` = `PASS`. Phase 0 final counts: 48 tasks, 47 passed, 1 superseded (0.verifyh). `handoff/006` and `handoff/007` deleted (their content is on `main` via `6cb8ed2` and `052e206`).
- **`phase/1` tip `ea1d618`** (`chore(0.0): rotate.md step 5/7 + PRINCIPAL.md`, the idle-window commit planned by handoff 007 Next Steps 6) on `052e206`. This rotation's commit lands on top. Phase 1: 16 tasks, all `pending`: `1.1` (opus, ProseMirror schema; depends only on `0.close`), `1.2` (opus, position map; also only `0.close` — the runner takes them one at a time, PRD order, expect `1.1` first), `1.3` → `1.4`, `1.5`, `1.6`, `1.7`, `1.8` (sonnet, Outline prototype), **`1.9` human observation gate** (`recordTarget 002-outline-feel`; Michael runs `pnpm dev`, writes ~1,000 words on `/dev/editor`, toggles source, checks the copied Markdown in Typora, tries `/dev/outline`, records the verdict with `scripts/gate.sh 1.9` — the first gate with a `payload`, copied to `DECISIONS.md#002-outline-feel` by the reconciliation), `1.verify` (needsCI, `ci/1.verify/a{n}`, artifacts `test-logs` **and `playwright-report`**), gate `1.verifyh`, review set `1.10.r0a/b/c/d`, `1.close`. Graph 150 tasks, `doctor` clean, host checkout on `phase/1` (switched at the boundary with the runner idle, #017 procedure).
- **Remote** (`git ls-remote origin`): `main` still at the seed `660f3aa`, `phase/0` stale at `b86edca`, no `ci/*` refs. `main` is pushed only by Michael by hand (DECISIONS #010); nothing in Phase 1 needs the remote branches until `scripts/gate.sh 1.verifyh` pushes its own `ci/` ref. Optional for Michael, from the host: `git push origin main phase/0 phase/1`.
- **Runner**: tmux session `essaydown`, window 1 `runner`, pane pid 8940, `zsh` (exited at `<promise>COMPLETE</promise>` after `CLOSED 0 main 052e206 next phase/1`). **Start Phase 1** with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter` after `tmux capture-pane -p -t essaydown:runner | tail -3` shows the shell prompt and `pgrep -f 'ralph/lib/cli.mjs ralph run'` is empty (exactly one runner process, F14). `.locks/` is empty. No runner code changed since 0.26, so this start loads what the r2 review and the close ran on. Image: no `docker/` change since 0.26's build (07:19Z); no rebuild needed.
- **Watcher** (`Monitor` tool, persistent; background shells get killed under memory pressure without a trace):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `<promise>COMPLETE</promise>` (log at 26,711 lines). The `[ralph] 1.1 attempt 1 (loop, opus, max-turns 50) in …/.wt/1.1` line is the first thing Phase 1 prints.
- **Expected next stop**: `HUMAN_GATE 1.9` after 1.1–1.8 pass (Phase 0 fix tasks took 8–14 min each at attempt 1; these are larger, budget 15–30 min each, so roughly 2–4 hours), or `STUCK 1.<n>` earlier. The gate is an observation gate on the Mini's or the MacBook's dev server (`pnpm dev`), not CI.

## Corrections

- `007-phase-0-verify-r2-gate.md` Next Steps 5 and 6 are done: `handoff/006`/`handoff/007` deleted; the `chore(0.0)` rotate-procedure commit is `ea1d618`. Its Open Question 1 (Claude fitting 160 turns) is answered: 110 turns, one container run, PASS.
- `007` Current State "Delete both `handoff/*` branches after 0.close": done. The SessionStart hook now names this file, which is in the tree.
- `002-phase-0-running.md` Next Steps 4 (reconciliation checklist): for Phase 1 use DECISIONS #review-0-r2's layout (verdict line first; per-criterion verdict; findings with reconciled severity and disposition, deferred items with revisit points; prior dispositions confirmed/reopened; deviations; graph change; promote-lessons named for the next verifier or "none"; backlog; reversal) plus the human record copy (`/logs/human/1.9/accepted.json` payload → `DECISIONS.md#002-outline-feel`, RUNNER-SPEC §5.4).
- `.claude/commands/rotate.md` front-matter `description` still says "commit both under the runner lock"; step 5 (authoritative) no longer takes a lock. One-line cosmetic fix for the next `chore(0.0)` idle-window commit; do not make a commit for it alone.
- `DECISIONS.md#review-0-r2` H2 revisit says "the Phase 1 task that first calls `writeFrontMatter` from the shell". Phase 1 is the editor; 1.1 holds `yaml` outside the PM doc and re-attaches it, and no Phase 1 text names `writeFrontMatter`. If no Phase 1 task calls it, the 1.10.r0d reconciliation re-points H2 to the Phase 2/3 task that does (title/question editing), hard stop before `4.verify` unchanged.

## Decisions

- **DECISIONS #review-0-r2** (in `052e206`): verdict **PASS**. Claude PASS 0/4/4 (110 of 160 turns), Sol PASS 0/0/0, Grok PASS 0/0/1; G1–G13 confirmed by all three, none reopened; findings H1–H9: H4 taken (RUNNER-SPEC §1 names the O_EXCL lock file; #009's "same semantics" marked wrong), H6 taken (PRD §7 corpus count "53 at r2"; 1.1/1.2 acceptances drop the literal 45 — the only tasks.json change), H1/H2/H3 should-fix **deferred** with revisit points, H5/H7/H8/H9 nits recorded; all deferred items in `docs/V1.1-BACKLOG.md`. Deviations 33 (0.25's `state.mjs` line) and 34 (F17 read as "before the next container task that can observe the change") recorded; the runner printing `ROTATE-PRINCIPAL` itself and the Monitor watcher recorded as observations. Promote-lessons for `1.verify`: **none named**. 7 class-level lessons appended (`[0.12.r2d]`).
- **H1 (`lockHolder` folds an unreadable lock file into "dead")** is the one deferred item with a proposed principal action: a `fix(runner)` commit on `phase/1` with a `util.test.mjs` leg, **on Michael's OK only**, in an idle window (now, before the Phase 1 start, or at `HUMAN_GATE 1.9`; never after `1.verify`). Recommendation: leave it in the backlog until the Phase 1 reconciliation unless Michael wants it now (60 %: it is fail-closed and the repair text's `pgrep` step protects a live holder; a runner commit before 1.1 costs nothing but principal time and adds runner surface the 1.10 reviewers must cover).
- **`ea1d618` (chore(0.0))**: `rotate.md` step 5 — handoff commits go on the phase branch from the host checkout only when the runner is idle at a phase boundary; otherwise on `handoff/NNN` from a temporary worktree, cherry-picked by the next reconciliation; `.locks/ralph` is never hand-written; step 7 gains the `git show handoff/NNN:… | pbcopy` relaunch line; `PRINCIPAL.md` Handoff cadence carries the same rule. Reverse: `git revert ea1d618`.
- No runner deviations since #review-0-r2 (the close ran first try: intent, one ref transaction, `closes/0.json`; `refreshCheckouts` moved nothing because no checkout was on `main`).

## Gotchas

- **Phase 1 is the first phase with new runtime dependencies** (ProseMirror, CodeMirror 6, Zustand, Playwright per PRD §4). A task that installs anything outside §4 needs a DECISIONS note and Michael's OK (the `zod` lesson, #review-0-r1 (a)/(b)); the 1.10 reviewers diff the lockfile against §4. `1.verify` must not change a manifest.
- **`1.verifyh` expects two artifacts, `test-logs` and `playwright-report`**; `ci.yml` at `ea1d618` uploads exactly `test-logs` and has no Playwright step (r2 Claude gate table, workflow lines 139/155), and **no Phase 1 task text names `ci.yml` or the report upload** (1.3–1.8 add specs under `e2e/web`). Unless a Phase 1 agent extends the workflow within its scope (unlikely; scope is the task text), the gate fails on the missing artifact → `GATE-FAILED` → `plan.1.verifyh.r0` → repair task `1.verify.g1` adds the Playwright run and the upload (the 0.verifyh/#018 path: RUNNER-SPEC §2 rule (a), physically in PRD §8, generator and validator green). That is the designed route and costs one CI round; do not pre-empt it with a principal edit of `ci.yml` (product code, outside a task) — but say it to Michael before `1.verify` so the failure is expected, not alarming. If Michael would rather avoid the round: the only sanctioned alternative is a DECISIONS-noted planning commit before `1.verify` starts, which the runner has no trigger for — record the cut, do not add machinery.
- **Human gate 1.9** is `gateKind: observation` with a `recordTarget`: `scripts/gate.sh 1.9` writes `/logs/human/1.9/accepted.json` with a `payload` (Michael's verdict and any Outline re-scoping); read the `payload` fields, never the runner outcome, when directing later tasks; the reconciliation copies it to `DECISIONS.md#002-outline-feel`. `REJECT` there is a real outcome (RUNNER-SPEC §2), not a gate repair.
- Container tasks resume the Phase 0 pattern: `STUCK` after three attempts → read `/logs/tasks/<id>/` transcripts, re-cut per #016 (`chore(<id>): principal lesson …` on the task branch), `ralph/ralph.sh retry <id>` resets `blocked → pending` and `attempts` to 0 (reversal: none; the state returns to what `run` left). Steers that worked at attempt 1 in Phase 0: name the file list, the probes to turn into tests, and the exact rule to decide.
- Never commit on the host checkout while the runner loop is live (#017); the loop runs 1.1 → 1.8 without a stop signal, so the next idle windows are `HUMAN_GATE 1.9` and `HUMAN_GATE 1.verifyh`. A handoff written mid-run goes on `handoff/009` per rotate.md step 5.
- After any `docker/` commit integrates, `docker compose -f docker/compose.yml build` (with `ESSAYDOWN_ROOT`) before the next container task that can observe it (#review-0-r2 deviation 34). Backlog line (32): cargo absent from the reviewer container's login-shell PATH — check the image at the next `docker/` change.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence; the Monitor survived a full run.

## Next Steps

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, last `^\[ralph\]` line and stop signal; one-line state to Michael; arm the Monitor (command above).
2. Ask Michael the H1 question (Decisions, second bullet) in one line; default is "backlog". Then start the runner with `--phase 1` (Current State "Runner"). Confirm the first `[ralph] 1.1 attempt 1` line and `.wt/1.1` on `task/1.1` cut from `phase/1` at this handoff's commit.
3. Tell Michael, before `1.verify` starts, that `1.verifyh` is expected to fail once on the missing `playwright-report` artifact unless a task extended `ci.yml` (Gotchas, second bullet); check `git log --oneline main..phase/1 -- .github/workflows/ci.yml` at `HUMAN_GATE 1.9`.
4. `HUMAN_GATE 1.9`: give Michael the `pnpm dev` procedure from the task text and `scripts/gate.sh 1.9`; evidence under `.evidence/human/1.9/`; restart. Then `1.verify` → `HUMAN_GATE 1.verifyh` → `scripts/gate.sh 1.verifyh` (on GATE-FAILED: `ralph/ralph.sh plan plan.1.verifyh.r0`, planning commit with `1.verify.g1` + gate, reviewers rewired, generator/validator green, `<promise>DONE plan.1.verifyh.r0</promise>`) → **rotate at that gate regardless of context** (DECISIONS #015; the handoff goes on `handoff/009`, deviation 29) → `1.10.r0a/b/c` → `PRINCIPAL 1.10.r0d` run by the fresh session, per the r2 pattern in `052e206`.
5. At the first `chore(0.0)` idle-window commit of the phase (if any is needed): fix the stale `description` line of `.claude/commands/rotate.md` in the same commit.

## Open Questions

- Whether a Phase 1 agent extends `ci.yml` for `playwright-report` on its own (in scope only if its task text can be read to require it; 1.3 is the first Playwright task). Resolved at `HUMAN_GATE 1.9` by the log check in Next Steps 3.
- Whether Michael wants H1 fixed before Phase 1 starts or left for the Phase 1 reconciliation (Decisions).
- Which Phase 2/3 task first calls `writeFrontMatter` (for the H2 revisit re-pointing); decided at `1.10.r0d`.
- The r2 Claude risk that the lock's pid liveness check crosses a bind mount: PRD §9.10 confines lock-taking to the host; nothing to do unless a container ever takes the lock.
