# Handoff 032 — Phase 1 in the r9 chain at the second `STUCK 1.64`: the retry's three attempts capped at 51 with the whole deliverable committed and verified green; the cost is the journal step, not the leg (DECISIONS #038); a second retry with a lesson naming the one remaining step

Written 2026-09-23 by the principal (the session that ran the first `1.64` retry; Claude Code on the Mac Mini), at the second `STUCK 1.64`, per the handoff-first rule — written before the second retry and kept by the same session, which continues the block (Michael is not at the keyboard; a full rotation would stall the chain on the relaunch). Supersedes `031-phase-1-r9-chain-1.64-stuck.md` (on `handoff/031`, not in the tree). This commit is on `handoff/032`, stacked on `handoff/031` (cut from `phase/1` at `5fb4163`); the `1.10.r9d` reconciliation cherry-picks `phase/1..handoff/032` as one range (`docs/handoffs/**` and `docs/DECISIONS.md` only). Next: `033-*.md`.

## Current State

- **`phase/1` tip `5fb4163`** (`task(1.63)`); `main` at `052e206`; `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **`1.64` `blocked` — the second `STUCK 1.64`**, six capped attempts in all (`.evidence/tasks/1.64/`: `{1,2,3}.a<n>-capped.log` from the first three, `{1,2,3}.log` from the first retry, to be moved to `{1,2,3}.r1a<n>-capped.log` before the second). `task/1.64` at `037cfa5` in `.wt/1.64`, clean, eleven commits on `phase/1`: six journal stubs (`- [1.64]` lines 371–376 of the journal; the seventh attempt appends the seventh), two recovery commits (`87aecca`, `3856950`), the principal lesson `35e8054`, and the work — `packages/editor/src/schema.ts` +190 (`keptCharacters`, the exported kept-character correspondence; `stripUnparsableWhitespace` its projection), `packages/editor/src/toggle.ts` +294/−69, `packages/editor/test/toggle.test.ts` +488 (the whitespace × node × container guards and a2's empty-link guard), `packages/editor/test/position-map-inverse.test.ts` +364 (the `deleteAtEveryBlockStart` leg: the block-local oracle, one test per swept block, every position, no shard and no bound, the three exclusions positively bounded), `e2e/web/editor-toggle-stripped-lead.spec.ts` (both cases), two backlog lines. **Verified in the retry's a3 transcript** (`3.log`): `pnpm lint && pnpm test && cargo test` green (`Test Files 44 passed`, `Tests 4494 passed`, cargo `ok`), the e2e suite green, the mutation red (`124 failed | 815 passed` over the two test files), the leg's counts `fixtures=65 changed=60 unchanged=5 blocksSwept=262 lost=54 kept=208 agreeing=32123 insideDropped=54 excluded=31` (`[1.53]` 5, L9 23, the `[1.64]` block-final-link class 3). What no attempt reached: the completed journal line and the commit that prints the promise.
- **Runner**: tmux `essaydown`, window `runner`, `zsh` at the prompt after `STUCK 1.64` (runner.log 134,894 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the retry: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; **a fresh one after every restart or `retry`**; note it can miss a line that lands in the same second as its expiry — the second `STUCK` did — so `ralph/ralph.sh status` on every wake):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: the second `STUCK 1.64`. After the second retry and restart the next own line is `[ralph] 1.64 attempt 1` again.
- **Expected next stop**: `1.64`'s integration then `[ralph] 1.65 attempt 1`; the block ends at `HUMAN_GATE 1.verify.r9h` (rotate there).

## Corrections

- `031` Next Steps 1, "On a second `STUCK` with the leg still the cost: a `#038`-shaped manual decision (cut the leg's oracle …)": the leg is not the cost — a1 of the retry built the block-local oracle and a3's whole verification ran in under five minutes. #038 records what the cost is (the turn count of the verification-and-journal pass under the 50-turn constant) and the decision (a second retry, the lesson naming the one remaining step).
- `031` Current State, "the essay fixture's 101 blocks and 27,878 positions in 9.7 s": the number held on an idle container; beside the suite's other vitest workers the per-fixture test still crossed 30 s, and a1 partitioned it as one test per swept block (`fb526ce`) — a partition, not a bound; every position is swept.
- `031` Gotchas, "one `it` per fixture": superseded by the partition above; the acceptance fixes no test count.
- `031` and `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/031`": through `handoff/032`.

## Decisions

- **`#038`** (this branch, `d6481ad`): the reading (the journal step, not the leg; six caps with the work built, twice verified green), the decision (second `retry` on the #028 pattern; the lesson names the remaining step and the turn cost of each part read from the capped transcript; never a scope change; never the promise literal), the stop rule (a third capped cycle with the work still complete is the turn budget's, Michael's call under #review-1-r1 — no fourth `retry` without him), the principal lesson, the reversal (the entry `git revert`; `retry` has no reversal command; `abandon` is destructive and not one).
- **Rules in force**: as 031. Nothing new promoted.
- **Runner deviations this block**: the r9 chain's first `retry` (`1.64`, after `35e8054`), now the second (#038). No 429, no `NO-JOURNAL`, no `admin`, no `docker/` change, no `fix(runner)`.
- **Turn-budget evidence** (conditional on Michael's OK — unchanged as a decision): `1.64` six capped attempts at 51, every one with the work built; the retry's a2 and a3 capped after a green suite, cargo and e2e — the first caps in the chain where nothing was left to implement. With #review-1-r8's table and 031's line: every opus fix task of the r8 and r9 chains capped at a1.
- **Open ask to Michael, unchanged** (#review-1-r8 N4): whether a `1.9.r1` approval retry at the r9 implementation SHA precedes `1.close`.

## Gotchas

- **The second retry's lesson is #038's instrument and its content is fixed there**: the implementation is complete; the attempt's work is a NEW `- [1.64] ` journal line, the suite once, the e2e suite once; each step one tool call in the foreground with the tool's maximum timeout (a background poll is a turn each); the mutation in one call (apply, run the two files, keep the failing titles, `git checkout -- packages/editor/src/toggle.ts`); the counts from one temporary probe reverted in the same call; no re-reading of code the commit messages name; the template read once. A lesson never skips the suite and never widens scope.
- Before `ralph/ralph.sh retry 1.64`: `git -C .wt/1.64 status --short` empty; the three logs moved aside with `r1a<n>-capped`; the lesson committed as `chore(1.64): principal lesson — the one remaining step` on `task/1.64` (`docs/lessons.md` only, `Reverse: git revert this commit.`). Never install or run pnpm in `.wt/1.64`.
- The journal on `task/1.64` ends with a newline now; check the last byte before any append anyway (memory `project-journal-trailing-newline`). Six `- [1.64]` lines exist; `grep -c -- '^- \[1.64\]'` after the attempt must be 7.
- The recovery-commit check before any `retry` (memory `project-recovery-commit-can-carry-a-mutation`): `3856950` swept a within-scope guard that was in the green run — consistent. A third `STUCK` gets the same check.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit; Michael runs it in his shell. Claude Code blocks a foreground `sleep`; wait with a backgrounded `until` loop.
- `ralph/ralph.sh status` says "running attempt 1" while the log shows a later attempt: the summary's attempt field lags; read the log.

## Next Steps

1. **The second `1.64` retry** (this session, right after this commit): logs aside, the lesson commit, `ralph/ralph.sh retry 1.64`, idle checks, restart, fresh watcher. On a third `STUCK` with the work still complete: stop, no `retry`; the state line to Michael names #038's stop rule and the raise as his call, with the six-attempt evidence; rotate there.
2. **The rest of the r9 chain**: `1.65` (opus), `1.verify.r9` (sonnet; promote (a)–(c) of #review-1-r8), gate `1.verify.r9h` (Michael: `scripts/gate.sh 1.verify.r9h`; **rotate there**), the r9 review set, `1.10.r9d` in a fresh session (#015) — the FAIL pattern `task(1.10.r8d)` at `3c96b9b` with #review-1-r8, the PASS pattern `task(0.12.r2d)` at `052e206` with #review-0-r2; it cherry-picks `phase/1..handoff/032` (two handoff commits plus #038; `docs/**` only), appends the `[review-1-r9, turn budget]` evidence line to `docs/V1.1-BACKLOG.md` with `1.64`'s six caps, and on Michael's yes to N4 appends `1.9.r1`.
3. **After `1.close`** (host checkout on `phase/2`, runner idle): 031's Next Steps 3 unchanged — the boundary `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` over assistant text blocks with its conformance test, the recovery-commit guard), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r9 principal lessons (now #038's), Claude r8 nit 3 (`ci.yml` `if: always()`), React 19's ratification (Michael's OK), the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/032`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 … #review-1-r8, 031, #038), the 429 signal (#027), the gate fetch-on-failure (I7), the reviewer-prompt option, Grok's read-only mode.

## Open Questions

- Whether the second retry lands `1.64` in one attempt with the remaining-step lesson, or the third cycle caps and the turn budget goes to Michael.
- Michael's answer on the `1.9.r1` approval retry (N4).
- The cap raise, the 429 signal, Grok's mode, React 19: Michael's calls, evidence unchanged in kind.
