# Handoff 017 — Phase 1 at `HUMAN_GATE 1.verify.r3h`: reconciliation r2 FAIL integrated, fix chain `1.29`–`1.32` + `1.verify.r3` passed, gate open

Written 2026-09-11 by the principal (the fresh session that ran the `1.10.r2d` reconciliation; Claude Code, Remote Control session on the Mac Mini), at the `HUMAN_GATE 1.verify.r3h` stop signal (rotate regardless of context at a verifier gate; DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `016-phase-1-review-r2-reconciliation-pending.md` (in the tree since `e6d7493`, with 014 and 015). This commit is on `handoff/017` (rotate.md step 5: a verifier gate is open, so never on `phase/1`); the `1.10.r3d` reconciliation cherry-picks `handoff/017` (and any later `handoff/NNN`). Next: `018-*.md`.

## Current State

- **`phase/1` tip `32552e0`** (`task(1.verify.r3)`, attempt 1); 47 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`), 39 of them `task(1.*)`. No principal commit on `phase/1` since `b149f0c` and none until `1.close`. `main` unchanged at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). `origin` holds `ci/1.verify/a1` (stale) and `ci/1.verify.r2/a1` (the GATE-FAILED round's ref); `ci/1.verify.r2.g1/a1` is not on origin — `gate.sh gc` at the boundary for the two that are.
- **Reconciliation `1.10.r2d`**: integrated at `e6d7493`, verdict **FAIL** (`.evidence/reviews/1/r2/verdict`), DECISIONS `#review-1-r2` (H1–H9). Fix chain appended and run: **`1.29`** (opus; H1/H5/H6, the strip at atoms and the deletion leg) passed at attempt 3 — a1 `error_max_turns` at `num_turns` 51 with the work committed, a2 finished and printed DONE but completed a1's journal stub in place, so the stop-check reported `NO-JOURNAL` (see Gotchas), a3 at 12 turns after a principal lesson on the branch; integrated `a55b5ec`. **`1.30`** (opus; H8, mark edges) passed at attempt 2 — a1 capped at 51 with the work committed, no stop signal, a2 at 27; integrated `23b1698`. **`1.31`** (opus; H2, the segment's first and last keystroke times; G9's commit metric) passed at attempt 1 at 49 turns; integrated `9307afd`. **`1.32`** (opus; H3/H4/H9) passed at attempt 1 at 38 turns; integrated `2273cdb` — the toggle flush at `DevEditor.tsx:102` is **live** (the new ordering case goes red without it; comment unchanged, file untouched), the real-clipboard copy case is in, the tautology is gone, and a fourth `x === x` instance was found outside scope (`editor-fixed-point.test.ts:763`, backlog line). **`1.verify.r3`** (sonnet) passed at attempt 1 at 35 turns; integrated `32552e0`; it appended the promoted rule to `CLAUDE.md`/`AGENTS.md` (line 45: the invariant-over-the-corpus rule), journal only otherwise.
- **Gate `1.verify.r3h`**: `human-pending`, opened at `32552e0`; `.evidence/ci/1.verify.r3h/` does not exist yet. The runner printed `HUMAN_GATE 1.verify.r3h` then `ROTATE-PRINCIPAL` (runner.log lines 67,827–67,828). Michael runs `scripts/gate.sh 1.verify.r3h`; `accepted.json` or `rejected.json` under `.evidence/ci/1.verify.r3h/` is the outcome.
- **Tasks** (`ralph/ralph.sh status`): phase 1 — 60 tasks, passed 52, superseded 2, human-pending 1, pending 5 (`1.10.r3a/b/c/d`, `1.close`). `doctor` clean, host checkout on `phase/1` at `32552e0`, tree clean, `.wt/` holds the runner's task worktrees only (plus this rotation's temporary one, removed by it), branches `handoff/010` … `handoff/017`. Lessons tagged `1.*`/`plan.1.*`: 104.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `ROTATE-PRINCIPAL`. `.locks/` empty, no `ralph run` process, no containers. Restart after the gate is accepted: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r3h` (log line 67,827). A second watcher on the gate directory: `setopt nullglob; d=/Users/mlsavage/Developer/essaydown/.evidence/ci/1.verify.r3h; until [ -f $d/accepted.json ] || [ -f $d/rejected.json ]; do sleep 30; done; ls $d` (one notification; exits non-zero when its last test fails — harmless).
- **Expected next stop**: on `ACCEPT`, restart → `1.10.r3a/b/c` (one container run each, ~18 min at r2) → `PRINCIPAL 1.10.r3d` → rotate; the reconciliation is a fresh session (#015). On `GATE-FAILED`, the runner marks `1.verify.r3h` blocked and opens plan request `plan.1.verify.r3h.r0`: `ralph/ralph.sh plan plan.1.verify.r3h.r0`, planning commit per the #021 pattern (`6a39a92`: a repair task `1.33` + `1.verify.r3.g1` with `refTemplate ci/1.verify.r3.g1/a{n}`, `1.10.r3a/b/c` rewired to it and their `/logs/ci/…/accepted/` references updated, `EXPECTED_COUNT` bumped, `verdict`-free DECISIONS `#022`).

## Corrections

- `016` Decisions said the `ralph/PROMPT.md` alignment "lands either inside a fix task's chain if the reconciliation says FAIL and scopes it, or at the phase boundary". The reconciliation chose the boundary (`#review-1-r2` H7): a runner file rendered from the host checkout (`docker/entrypoints/claude-task:20`) is a `fix(runner)` with a conformance test, never a container task's edit; each r3 task text names CLAUDE.md's list as the one that stands, and none of the five r3-chain attempts stopped on it.
- `016` Gotchas (turn budget) said "no new cap data this block". The r2 and r3 chains now hold four more capped first attempts, all with the work committed: 1.23, 1.25 (before the stub rule), 1.29, 1.30 (under it — both continued without a stop signal). See Decisions.
- `016` Current State counted 44 passed / 3 pending; at rotation it is 52 passed / 5 pending (the r3 chain added ten tasks and five passed).

## Decisions

- **`#review-1-r2` (DECISIONS.md, in the tree at `e6d7493`)**: verdict FAIL; H1 blocker (trailing `hard_break`, Claude), H2 blocker (a flush inside a long burst splits undo, Sol), H3/H4 should-fix required (toggle flush untested; no real clipboard write after 1.28), H5/H6 nits in 1.29, H7 the wrapper drift as a boundary `fix(runner)`, H8 the r1 emphasis-edge deferral reopened (its reachability premise false; Sol) as a required should-fix, H9 nit in 1.32. G1–G12 confirmed or reopened; the reviewer record re-derived (unique findings Claude 5 of 6, Sol 2 of 2, Grok 1 of 2; cumulative low side Grok 5, Claude 2, Sol 2); promote-lessons for `1.verify.r3` (one rule, now CLAUDE.md line 45); four backlog lines. No decision in it is Michael's (autonomous session); every conditional item stays conditional.
- **Runner observation, no code change (to be audited in `#review-1-r3`)**: the stop-check's journal test is `journalCount(wt, id) > before.journal` (`ralph/lib/run.mjs`, the `journalOk` line) — the count of `- [<id>] ` lines must **grow during the attempt** — and it runs before the DONE check. That is RUNNER-SPEC §4.3 ("journal entry present for this attempt") and §10 ("exactly one line per attempt"), so the runner does not contradict the spec. What it exposed is CLAUDE.md's wording ("complete your own stub in place"), which a second attempt reads as licence to complete the first attempt's stub and append nothing; `1.29` a2 did exactly that with DONE printed and the suite green, and lost the attempt. **Principal action taken**: one commit `chore(1.29): principal lesson — each attempt appends its own journal line` (`1bee69e`, on `task/1.29` in `.wt/1.29`, `docs/lessons.md` only, reversal in its message; squashed into `task(1.29)` at `a55b5ec`) — the #016 re-cut shape without the `retry` (the task was `running` with attempts 2, not blocked), then the same restart command. Attempts 1.30 a2 and every later task appended their own line. **Not a `fix(runner)`**: nothing contradicts the spec. **For the r3 reconciliation**: the clarification belongs in CLAUDE.md steps 2 and 4 (every attempt appends its own stub; an earlier attempt's line stays as it is) via the next verify task's promote-lessons text (`1.verify.r4` on a FAIL, or `2.verify` on a PASS), and `#review-1-r3` records the `chore(1.29)` commit as a principal deviation of the #016 shape.
- **Turn budget, evidence only (the decision stays at the Phase 2 boundary on Michael's OK, #review-1-r1)**: r3 chain by the transcript measure — 1.29 a1 `error_max_turns` 51 / a2 21 / a3 12; 1.30 a1 `error_max_turns` 51 / a2 27; 1.31 49; 1.32 38; 1.verify.r3 35. Under the stub rule a cap is now a continuation, not a stop: both r3 caps (1.29, 1.30) ran straight into a2 with no `NO-JOURNAL` and no restart; the only stop in the chain was the journal-count miss above, which is a wording problem, not a cap. Phase 1 to date: 16 of 36 loop tasks needed more than one attempt; `NO-JOURNAL` stops 11 (the ten counted at #review-1-r2 plus 1.29 a2). The cost of a cap under the stub rule is one extra container run (~10 min) and no principal action, which weakens the case for raising the cap; the number stays 80, conditional, and Michael decides.

## Gotchas

- **Each attempt appends its own journal line.** The runner counts `- [<id>] ` lines per attempt. An agent that completes an earlier attempt's stub and appends nothing is `NO-JOURNAL` even with DONE printed and the suite green. The `[1.29]` principal lesson in `docs/lessons.md` (2026-09-11T04:02:00Z) says so and is visible to every later task; if a task in the r3 review set or later still trips it, the same `chore(<id>): principal lesson` on its task branch plus a restart is the procedure (a `retry` only when the task is `blocked`).
- **The gate is the first proof of macOS and Windows** for four source changes (`schema.ts` twice, `undo.ts`/`toggle.ts`, the clipboard spec's real-write case). The new real-clipboard case (1.32) grants `clipboard-read`/`clipboard-write` and asserts the status plus `readText()` *contains* the word; if windows-latest fails there, the planning commit's repair scopes only that case (the #021 shape) — the spy cases are untouched.
- **`1.10.r3a/b/c` texts** name the browser probes per fix task and ask Grok, as `1.10.r2c` did, to state whether a read-only review could have found H1/H2 and why H7's severity differed. No reviewer change inside the set (#review-1-r0).
- **Phase boundary items** (only after `1.close`): H7's `fix(runner)` on `ralph/PROMPT.md` + `tests/agent-rules.test.ts` widened (backlog line, hard stop `2.1`); `chore(0.0)` for rotate.md step 5's third case and the stale `description` line; `gate.sh gc` for `ci/1.verify/a1` and `ci/1.verify.r2/a1` (and whatever `ci/1.verify.r3/a1` turns out to be); the `phase/1` push (#010); `git branch -D handoff/010 … handoff/017`; the conditional turn-budget `fix(runner)` only on Michael's OK; the CLAUDE.md wording clarification if not yet promoted.
- **Reports and Sol's evidence**: at `PRINCIPAL 1.10.r3d`, `.evidence/reviews/1/r3/{claude,sol,grok}/report.md` → `docs/reviews/phase-1-r3-*.md` verbatim; Sol's `/report/` probe scripts and logs are evidence, not for the tree. `status.json` verdicts are not the signal; the bodies are.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers. `grep -F -- '- [id]'` needs the `--`, or ugrep reads the pattern as an option.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm both watchers; one-line state to Michael; hand him `scripts/gate.sh 1.verify.r3h` (no placeholder).
2. On `ACCEPT` (`.evidence/ci/1.verify.r3h/accepted.json`, three OS directories in both artifacts): idle checks, restart, `1.10.r3a/b/c` → `PRINCIPAL 1.10.r3d` → `/rotate`; the reconciliation is a fresh session that cherry-picks `handoff/017` and any later handoff into `.wt/1.10.r3d`.
3. On `GATE-FAILED`: `ralph/ralph.sh plan plan.1.verify.r3h.r0`, read the run's log, planning commit per `6a39a92` (#021) in `.wt/plan.1.verify.r3h.r0` with a DECISIONS `#022`, restart; `1.33` + `1.verify.r3.g1` → `HUMAN_GATE 1.verify.r3.g1h` → rotate there.
4. After `PRINCIPAL 1.10.r3d` (fresh session): the reconciliation — on PASS `docs/progress.md` regenerated, `1.close` → `CLOSED` → rotate at the phase boundary on `phase/2` from the host checkout with the runner idle, then the boundary items above; on FAIL the r4 chain per the `e6d7493` pattern, with the CLAUDE.md wording clarification in `1.verify.r4`'s promote-lessons text.

## Open Questions

- Whether the r3 reviewers find a further member of the "tree the editor can build that `parse` cannot produce" family beyond the typed, deleted and marked shapes now guarded (the backlog's image-of-`parse` assertion is a Phase 2 planning item).
- Whether the real-clipboard case survives windows-latest with the permission grant (it did for 1.23's cases before #021; the assertion is now "contains").
- The turn-budget number: 80 proposed, conditional; the stub rule has changed what a cap costs (a continuation, not a stop) — Michael's call at the boundary.
