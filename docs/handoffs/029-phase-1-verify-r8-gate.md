# Handoff 029 — Phase 1 at `HUMAN_GATE 1.verify.r8h`: the r7 reconciliation done (FAIL, #review-1-r7), the r8 chain 1.57–1.61 integrated, the verifier passed, the gate open for Michael, the r8 review set next

Written 2026-09-22 by the principal (the fresh session that ran the `1.10.r7d` reconciliation and watched the r8 chain; Claude Code on the Mac Mini), at the `HUMAN_GATE 1.verify.r8h` / `ROTATE-PRINCIPAL` stop signal, per PRINCIPAL.md's cadence. Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `028-phase-1-verify-r7-gate.md` (now in the tree — `task(1.10.r7d)` cherry-picked `phase/1..handoff/028`). This commit is on `handoff/029`, cut fresh from `phase/1` at `b7f45a5` (nothing is stacked: `git log --oneline phase/1..handoff/029` shows this one commit); the `1.10.r8d` reconciliation cherry-picks `phase/1..handoff/029` (plus anything stacked on it later). Next: `030-*.md`.

## Current State

- **`phase/1` tip `b7f45a5`** (`task(1.verify.r8)`); 94 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **The r7 reconciliation**: `task(1.10.r7d)` `9538cf1`, DECISIONS `#review-1-r7`, verdict **FAIL** (`.evidence/reviews/1/r7/verdict`); the r8 chain appended (261 tasks). Reports in `docs/reviews/phase-1-r7-{claude,sol,grok}.md`.
- **The r8 chain, all passed, every fix task at attempt 2 after a first attempt capped at 51 with the work built** (turns from the transcripts' result lines): 1.57 (`d435fab`; a1 51 capped, a2 32); 1.58 (`05a4fe0`; 51, 31); 1.59 (`33320c6`; 51, 44 — the recovery commit had swept two of the agent's throwaway probe files onto the branch, `e2e/web/zz-diag3.spec.ts` and `packages/editor/test/zz-d.test.ts`; attempt 2 removed them, the integrated commit holds neither); 1.60 (`b4ca3de`; 51, 34; the recovery commit swept only the new spec, no mid-mutation source); 1.61 (`f5be76c`, sonnet; 51, 61); `1.verify.r8` (`b7f45a5`; a1 43; the three promote bullets M1, M2, M5 in CLAUDE.md and AGENTS.md; 30 spec files under `e2e/web/`, counted from the directory). No `retry`, no 429, no `STUCK`, no `NO-JOURNAL` in the chain — the first chain of the phase without a stop signal.
- **Gate `1.verify.r8h`: `human-pending`**, nothing under `.evidence/ci/1.verify.r8h/` yet. Michael has the command: `scripts/gate.sh 1.verify.r8h` (his shell; it outruns the 120 s foreground limit — read his background task's output file, or `.evidence/ci/1.verify.r8h/` afterwards).
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.verify.r8h human-pending; last passed 1.verify.r8; gates open: 1.verify.r8h; plan requests: none; blocked: none." Graph 261 tasks.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r8h` / `ROTATE-PRINCIPAL` (runner.log lines 126,828–126,829; the log is 126,829 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after ACCEPT: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped at rotation; **arm a fresh one after every restart or `retry`** — the `awk` dedup swallows a repeated `attempt 1` line):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r8h`. The runner prints nothing at a review set's start; after the restart its next own line is `PRINCIPAL 1.10.r8d` (or `STUCK 1.10.r8` on a reviewer failure).
- **Expected next stops**: `ACCEPT` (or `GATE-FAILED`) from `scripts/gate.sh`; then the review set `1.10.r8a/b/c`; then `PRINCIPAL 1.10.r8d` (rotate there; the reconciliation is a fresh session, #015).

## Corrections

- `028` Current State, every one of its four corrections, and its Next Steps 1–3: superseded — the reconciliation is done (`9538cf1`), the r8 chain is integrated, the gate above is the open one. `028`'s cherry-pick instructions (the `phase/1..handoff/028` range, the DECISIONS.md tail conflict): consumed; `handoff/028` is in the tree and `handoff/029` is not stacked on it.
- `028` Corrections, "31 of 63 `loop` tasks": counted at `#review-1-r7` as **31 of 68** task ids with a `(loop` attempt line in `.evidence/runner.log` (unique ids, verify and gate-repair tasks included; the command is in the entry) — the r8 chain adds six ids, all passed (five at a2), so the running count is 36 of 74 needing more than one attempt; `NO-JOURNAL` 16 and `STUCK` 10 unchanged.
- `028` Decisions, "the boundary `fix(runner)` list gains `transcriptHasDone`": now recorded as `#review-1-r7` M3 (b) with the conformance-test shape (a transcript whose tool result echoes the promise and whose assistant text does not must be `false`), beside the recovery-commit guard.
- `028`'s `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/028`": through `handoff/029`.
- `.claude/commands/rotate.md` step 5 says the temporary worktree is `.wt/handoff-NNN`; this rotation, like 027's and 028's, used the session scratchpad (`.wt/` is the runner's directory) — the boundary `chore(0.0)` item stands.

## Decisions

- **`#review-1-r7`** (in the tree, `9538cf1`): verdict FAIL; M1–M11 (M1 the join widening rewriting verbatim leaves, M2 the hard break before html, M3 task 1.54 integrated from a capped attempt, M4 the table extension's pipe escape, M5 the block-final code-span end, M6 the destructive leg's timeout, M7–M10 nits, M11 the hard break inside a container — found by the reconciliation, backlog with a trigger); L1–L11 and K1–K9 each confirmed or reopened (K1, K2, L4, L5 reopened; J3 a third time); the r8 chain; the reviewer record (Claude 8 of 9 unique, Sol 1 of 2, Grok 0 of 0; Grok PASS with two blocker families in the tree, 3 of 5 counted attempts — evidence for Michael between review sets, no change inside one); the promote candidates (a)–(c) that `1.verify.r8` carried into CLAUDE.md/AGENTS.md lines 53–55. Every blocker, Required finding and fix shape was reproduced and prototyped in a detached scratch worktree at `5618078` before any task text was written — and the r8 chain is the first with no stop signal.
- **Rules in force from `#review-1-r7`**: (a) no lessons line, task text, DECISIONS entry, handoff or any file an agent reads carries the literal DONE promise — say "print the DONE promise for <id>"; (b) the boundary `fix(runner)` list: `transcriptHasDone` reads only the assistant's own text blocks (conformance test as above) and the recovery-commit guard (a `wip(<id>): recovery` that changes `packages/*/src` on a test-only task flags it; 1.59's swept probe files are a second shape of the same hazard — a recovery that adds a file under `e2e/` or `packages/*/test` named `zz-*`).
- **Runner deviations this block**: none in the r8 chain (no `retry`, no 429, no `STUCK`, no `NO-JOURNAL`); the reconciliation's cherry-pick had one conflict (the DECISIONS.md tail, resolved by keeping both). No `admin`, no `docker/` change, no `fix(runner)`.
- **Turn-budget evidence** (for the boundary, conditional on Michael's OK — unchanged as a decision): the r8 chain's five fix tasks, four opus and one sonnet, all capped at 51 on the first attempt with the work built and all passed on the second at 31–61 turns; the verifier 43 at a1. With the r7 table (`#review-1-r7`), that is nine of ten opus fix tasks over two chains capped at a1 with the work done — the 80-turn raise's evidence is now two chains wide.

## Gotchas

- **A windows-only single-case failure at this gate is not automatically the known transient** — 1.60 changed `packages/editor/src/toggle.ts` and `apps/desktop/src/dev/DevEditor.tsx` (editor runtime) and the chain added four new specs (`editor-copy-code-span-references`, `editor-copy-hard-break-before-html`, `editor-toggle-cell-code-pipe`, `editor-toggle-code-span-end`), so a failure in any toggle spec or any new spec is a change under #023's criterion ("spec unchanged, no editor runtime change") — read the case; a repair is `plan.1.verify.r8h.r0` → `1.verify.r8.g1`, scoped to what failed (#019, #021, #022, #024, #035, #036). Only a failure in an untouched, non-toggle spec is the #023/#029 rerun-and-abandon. The `[1.46]` class is now recorded as reproducing on Linux too (`#review-1-r7` M3 (d)); the click route is the answer for a caret that must sit at one place.
- **The watcher dedups whole lines** (memory `project-watcher-dedup-after-retry`); arm a fresh one after every restart, and re-arm on the 30-minute expiry.
- **Before any `retry`**: `git -C .wt/<id> diff <branch-base> --stat -- 'packages/*/src' fixtures` for a swept mutation, and `git -C .wt/<id> diff <branch-base> --stat --name-only | grep zz-` for swept probe files (1.59's shape).
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit; Michael runs it in his shell. `git worktree list | grep <sha>` also matches task worktrees left at that SHA (`.wt/1.verify.r7.g2` is at `5618078`).
- Python heredocs on this Mac need `# -*- coding: utf-8 -*-` as their first line when the script holds non-ASCII text.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. **The gate**: Michael runs `scripts/gate.sh 1.verify.r8h`; read `.evidence/ci/1.verify.r8h/` (`accepted.json` on ACCEPT; on GATE-FAILED `a1/result.json`, `run.json`, `workflow.log`) and give him the one-line result. GATE-FAILED → read the failing case before choosing between the rerun (#023/#029, untouched non-toggle spec, transient) and the repair (`ralph/ralph.sh plan plan.1.verify.r8h.r0`, a `1.verify.r8.g1` chain scoped to what failed; the r8 review rows then read `/logs/ci/1.verify.r8.g1h/accepted/`, rewired by the planning commit as #035 did).
2. **On ACCEPT**: idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux pane at the prompt, `doctor` clean), the restart, a fresh watcher; the review set starts the three reviewers together (`started_at` within 200 ms in `.evidence/state/tasks.json` — #025's independence condition); `STUCK 1.10.r8` on a 402 → #025's manual procedure, Michael's call.
3. **`PRINCIPAL 1.10.r8d`**: `/rotate`; the reconciliation is a fresh session (#015): it cherry-picks `phase/1..handoff/029` (and whatever is stacked on it), copies the reports, reproduces every blocker and Required finding in a detached scratch worktree at the implementation SHA (`b7f45a5` if the gate passes at a1; the `.g<n>` verifier's SHA otherwise), writes `#review-1-r8` (M1–M11 each confirmed or reopened; the backlog deferrals named in the r8 rows checked for a fired trigger), and on PASS the runner runs `1.close`; on FAIL it appends the r9 chain on the `task(1.10.r7d)` pattern at `9538cf1` — after running the corpus legs over every fixture and scope it decides, in the scratch worktree, before writing the task texts, with every pin a fix re-decides named by file and line, acceptance tails naming file-level greps and never a suite total, and no promise literal anywhere.
4. **Phase boundary idle window** (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s (two instances, #029); H7's; `#review-1-r7` M3 (b) — `transcriptHasDone` reading only assistant text blocks, with its conformance test; the recovery-commit guard (a source change on a test-only task; a swept `zz-*` probe file); `chore(0.0)` for rotate.md step 5 (the stacked-handoff rule's "already in the tree" condition, the scratchpad worktree, the fresh-watcher rule), the stale `description` line, PRINCIPAL.md's doctor sentence and PRINCIPAL.md gaining Sol's r4 lesson, #028's, #030–#034's, the r5, r6 and r7 principal lessons (`[1.10.r7d]` in lessons.md: the prototype-and-probe hour, the reviewer's guard list checked against the parser) and handoff 025's description-vs-acceptance lesson; `git branch -D handoff/010 … handoff/029`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: `#review-1-r5`, `#review-1-r6`, `#review-1-r7` and the r8 chain above — nine of ten opus fix tasks capped at a1 over two chains), the 429 signal (#027, four instances), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option, and Grok's read-only mode (3 of 5 PASS-with-blockers).

## Open Questions

- Whether the gate passes on windows at a1 with 1.60's runtime change and four new specs in the tree.
- Whether the r8 reviewers accept the M11, `[#030 product]`, M7 and L11/M10 deferrals and the two-view instrument's two-route form.
- The cap raise, the 429 signal and Grok's mode: Michael's calls, evidence unchanged in kind and larger.
