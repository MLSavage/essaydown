# Handoff 006 — Phase 0 at gate 0.verify.r1h (fix chain 0.15–0.22 and 0.verify.r1 passed; review attempt r1 next)

Written 2026-09-06 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `HUMAN_GATE 0.verify.r1h` rotation required by DECISIONS #015 (the reconciliation `0.12.r1d` is never run by the session that ran the phase's tasks). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `005-phase-0-fix-chain-running.md`; `002-phase-0-running.md` stays the reference for the gate evidence pattern, deviations 1–18 and the reconciliation checklist (its Next Steps 4). Next: `007-*.md`.

## Current State

- `phase/0` tip `5649a93` (`task(0.verify.r1)`) on `1e12b1c` (0.22) on `f5cacc3` (0.21) on `2526729` (0.20) on `11ab7e3` (0.19) on `136cd33` (0.18) on `dfd1a93` (0.17) on `42896f3` (0.16) on `6f8a7ae` (0.15) on `f07beac` (handoff 005) on `b86edca` (0.14). Passed: 0.0–0.11, gate 0.2h (ACCEPT a1), 0.verify, 0.13, 0.verify.g1, gate 0.verify.g1h (ACCEPT a1), 0.12.r0a/b/c, 0.12.r0d, 0.14–0.22, 0.verify.r1 — every task from 0.13 onward at attempt 1 (0.15 8 min, 0.16/0.17 4 min each, 0.18–0.20 about 10 min each, 0.21/0.22/0.verify.r1 under 5 min). Superseded: 0.verifyh (GATE-FAILED a1). **Open gate: `0.verify.r1h` (human-pending, waiting for `scripts/gate.sh`).** Pending: 0.12.r1a/b/c → 0.12.r1d → 0.close. Graph 140 tasks, `doctor` clean, `main` untouched at the seed `660f3aa`, `.evidence/reviews/0/r0/verdict` = `FAIL`, no remote `ci/*` refs (gc ran, see Decisions).
- **Runner**: tmux session `essaydown`, window 1 `runner`, pane pid 8940, currently `zsh` (exited at `HUMAN_GATE 0.verify.r1h`, the last two log lines are `[ralph] 0.verify.r1 attempt 1 …` and `HUMAN_GATE 0.verify.r1h`). Restart: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter`. Exactly one runner process at any time (F14 manual procedure): `tmux capture-pane -p -t essaydown:runner | tail -3` must show the shell prompt first. The next start loads the 0.18–0.20 runner code for the first time (see Decisions, deviation 28).
- **Watcher** (background Bash, `run_in_background`, timeout 3600000; one per stop signal; none running after this rotation). Arm it from the current end of the log; the `COMPLETE` alternative is now anchored (the unanchored 005 form tripped on a transcript line quoting `run.mjs`):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); until tail -n +"$start" .evidence/runner.log | grep -qE "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH) |^<promise>COMPLETE"; do sleep 60; done; tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|ROTATE-PRINCIPAL) |^ROTATE-PRINCIPAL$|^\[ralph\] " | tail -14; tail -6 .evidence/state/audit.log | cut -c1-200
  ```
  Current marker line: `HUMAN_GATE 0.verify.r1h` (log at 20,667 lines). After the gate ACCEPTs and the runner restarts, the marker becomes `[ralph] 0.12.r1a …` (three reviewer containers in parallel, evidence in `.evidence/reviews/0/r1/`).
- **Expected next stop**: `PRINCIPAL 0.12.r1d` after the three reviewers finish (r0 took about 12 minutes per reviewer); or `STUCK 0.12.r1<a|b|c>` for a failed reviewer.

## Corrections

- `005-phase-0-fix-chain-running.md` Current State "Expected next stop … or `STUCK`/`NO-JOURNAL` on an Opus task" and Open Questions "whether 50 turns is enough for 0.18–0.20": every task passed at attempt 1; the question is closed by evidence, no DECISIONS line needed.
- `005` Open Questions / Next Steps 5 "whether `gate.sh gc` treats a GATE-FAILED attempt as `rejected`": it does (`attemptStatus` in `ralph/lib/gate.mjs` reads `result.json` with `conclusion != success` as `rejected`); `scripts/gate.sh gc` deleted `ci/0.verify/a1 (rejected)` at 17:17Z. No spec wording gap; nothing to record in DECISIONS beyond a one-clause mention in the r1 reconciliation if the reviewers raise it.
- `005` Gotchas "After 0.18 integrates … the runner prints `ROTATE-PRINCIPAL`": the code is in the tree (`gate.mjs` line 52 and `run.mjs` line 58) but the runner did **not** print it after `HUMAN_GATE 0.verify.r1h`, because the live `node` process was started before 0.18 integrated and ES modules are loaded once per process (deviation 28 below). `scripts/gate.sh 0.verify.r1h` runs fresh and will print it.
- `005` Gotchas "After 0.20 integrates, the rotate lock handshake changes shape": read `withLock` in `ralph/lib/util.mjs` (lines 119–141): a lock file whose first token is a live pid is held; the `/rotate` echo of `$$` therefore blocks the runner correctly. A lock file with no pid token (empty first field) would be broken as stale — never create one by hand. This rotation ran with the runner exited, so the question was moot here.
- The previous `next-prompt.md` steps 1–3 are done; this file's Next Steps replace it.

## Decisions

- No DECISIONS entries were added this session (none allowed while tasks were in flight; none needed at the gate). Still owed to the r1 reconciliation entry `DECISIONS.md#review-0-r1` (carried from 005 Corrections/Decisions, verbatim intent): (a) Michael's ruling that 0.10's `zod` install (a runtime dependency not in PRD §4, no DECISIONS note, no OK) is **ratified after the fact** because it is plainly the plan's intent (PRD §6.2, tasks 0.10 and 2.7 name it; `zod@^4.5.4` is a direct dependency of packages/core; `zod-validation-error` is transitive only); (b) the lesson that nothing checks "is this package in PRD §4?" at install time, so a reviewer found it three tasks later — if that check belongs anywhere it is the existing bar scripts, not new machinery; a `docs/V1.1-BACKLOG.md` line, not a task; (c) deviation 27 (`sync-state` rewrites a raw dependency to a superseded gate that `state.mjs` treats as satisfied for everyone) disposed one way or the other.
- Integrated this session (all `task(<id>)` squashes of DECISIONS #review-0-r0 fix tasks, none touching `docker/`): 0.15 F2 (front-matter writer), 0.16 F3 (typecheck + coverage in the bar scripts), 0.17 F4/F5 (CI merge `separate-directories`, conformance suite in CI), 0.18 F6/F7 (review start consumes `accepted.json`; `isVerifyGate` grammar), 0.19 F8 (doctor: principal deletions, passed-with-debris), 0.20 F9/F10 (`withLock` identity break, `writeAtomic` fsync), 0.21 F11 (scaffold opener, `greet` removed), 0.22 F12/F13 (CRLF in `parse`, two fixtures), 0.verify.r1 (dependency freeze, three-OS suite, promoted two lessons into CLAUDE.md's Code rules — the F19 manual `promote-lessons` step). `docker compose build` not needed (no `docker/` diff since `f07beac`).
- Runner deviations since 005 (none adds machinery): (28) **runner code integrated by a task is not live until the next `ralph.sh run` start** — the process imports `ralph/lib/*.mjs` once; the host checkout is refreshed after each integration but the running process is not. Consequence this session: `ROTATE-PRINCIPAL` not printed by the runner at `0.verify.r1h`. Procedure: after any task that changes `ralph/` integrates, the next stop signal's restart picks it up; nothing to do unless a same-run task depends on the new behaviour (none did: 0.18's `accepted.json` check is exercised at review start, which happens after this restart). Hand this to the r1 reviewers with 27.
- Watcher pattern correction (this file's Current State): `^<promise>COMPLETE` anchored.

## Gotchas

- **Review start after 0.18 refuses when `.evidence/ci/0.verify.r1h/accepted.json` is missing or an artifact digest differs.** Order is therefore fixed: `scripts/gate.sh 0.verify.r1h` → `ACCEPT` → restart the runner. The gate pushes `ci/0.verify.r1/a1` at `5649a93`, waits for `ci.yml`, expects the single artifact `test-logs` (merged with `separate-directories`, so it must hold the three OS directories — that is what 0.verify.r1's acceptance and the r1 reviewers check), writes `accepted.json`, deletes the ref (`cleanupRef: true`), and prints `ROTATE-PRINCIPAL` (advisory; the rotation already happened, ignore it).
- 0.19's doctor now reports principal commits that delete paths and passed tasks with debris. Handoff commits add files only. The reconciliation commit on `task/0.12.r1d` adds `docs/reviews/phase-0-r1-*.md`, appends to DECISIONS/journal/backlog, regenerates `docs/progress.md`; it deletes nothing.
- 0.20's `withLock` writes `pid time nonce` and releases only its own token. `ralph.sh` commands taken while the runner is idle are unaffected. Do not hand-write `.locks/ralph` outside the `/rotate` procedure.
- Opus steer that works (0.10 a3, 0.14–0.20 at attempt 1 with F-numbered scoping): "do not re-read or rewrite; suite from HEAD; fix only red; journal and commit by turn 20; then DONE". Keep steers on the task branch (`chore(<id>): principal lesson …`) or re-cut per #016.
- Five-hour subscription window is shared by every container attempt (three reviewers run at once): before restarting after a `STUCK`/`NO-JOURNAL`, read the last `rate_limit_event` in `runner.log`; if `status` is `rejected`, wait for `resetsAt` + 30 s.
- Reviewer reports for r0 are `docs/reviews/phase-0-r0-*.md`; r1 reviewers verify each F disposition against the tree and read DECISIONS #review-0-r0 for the 24 disposed deviations (not the handoffs). Give Michael verdicts and counts, never the reports.
- `RALPH_CI_ADAPTER=fake` is the test gate; `docker compose run` needs `ESSAYDOWN_ROOT`; the host-checkout rule (#017) stands outside the rotate procedure; `.claude/settings.json` denies `git push --delete*` for the principal's shell, but `gate.sh gc`/`cleanupRef` delete refs through node and are unaffected.

## Next Steps

1. `ralph/ralph.sh status`, `ralph/ralph.sh doctor`, the last `^\[ralph\]` line and stop signal; one-line state to Michael; arm the watcher (command above).
2. Give Michael: `cd ~/Developer/essaydown && scripts/gate.sh 0.verify.r1h`. Expect `ACCEPT 0.verify.r1h a1 run <id>` then `ROTATE-PRINCIPAL`; evidence under `.evidence/ci/0.verify.r1h/a1/` and `accepted.json` (pattern: 002 Current State "Gate evidence pattern"). Confirm `test-logs/` holds three OS directories. GATE-FAILED → `ralph/ralph.sh plan plan.0.verify.r1h.r0` → planning commit on the request branch adding `0.verify.r1.g1` + gate `0.verify.r1.g1h` physically in PRD §8 (RUNNER-SPEC §2 rule (a); `0.12.r1a/b/c` rewired to the new gate; `node ralph/generate-tasks.mjs` and `node ralph/validate-tasks.mjs` green; `EXPECTED_COUNT` bumped) with `<promise>DONE plan.0.verify.r1h.r0</promise>`, then restart. Transient CI failure (runner outage, not code) → `scripts/gate.sh rerun 0.verify.r1h` instead.
3. On ACCEPT: restart the runner → `0.12.r1a/b/c` (three containers, `.evidence/reviews/0/r1/{phase_base_sha, verifier_id, verification_sha, implementation_sha}` — `verifier_id` must read `0.verify.r1`, both SHAs `5649a93…`; watch `.evidence/reviews/0/r1/<who>/transcript.log` and `exit-code`). A failed reviewer → `STUCK 0.12.r1<x>` → read the transcript's last `result`, `ralph/ralph.sh retry 0.12.r1<x>` (reversal: none needed, `retry` resets `blocked → pending`; the attempt directory stays), restart.
4. `PRINCIPAL 0.12.r1d`: reconcile in `.wt/0.12.r1d` on `task/0.12.r1d` per 002 Next Steps 4, r1-specific: reports copied to `docs/reviews/phase-0-r1-{claude,sol,grok}.md`; `DECISIONS.md#review-0-r1` with `verdict: PASS|FAIL` as the **first** `verdict:` text in the entry; each r0 disposition (F1–F21) confirmed or reopened; the `zod` ratification sentence, the install-time lesson and deviations 27–28 recorded (Decisions above); `docs/progress.md` regenerated; nits to `docs/V1.1-BACKLOG.md`; on FAIL the fix tasks + `0.verify.r2` + `0.12.r2a/b/c/d` written physically into PRD §8 (generator and validator green, `EXPECTED_COUNT` updated), close rewired to `0.12.r2d`; on PASS nothing else changes and the diff since `implementation_sha` stays within `docs/**`, `DECISIONS.md`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT`. Journal line; `wip(0.12.r1d): …` with `<promise>DONE 0.12.r1d</promise>`; restart → `0.close` (moves `main`, creates `phase/1`). **No principal commits on `phase/0` after `0.12.r1d` integrates** (RUNNER-SPEC §8.1).
5. After `0.close`: `/rotate` (handoff 007 for Phase 1; the runner is then started with `--phase 1`; `docker compose build` only if a Phase 1 task touches `docker/`).

## Open Questions

- Whether the three reviewers at once fit the subscription window at this hour (r0 did; rate-limit evidence is in `runner.log`).
- Grok headless in the container worked at r0; the RUNNER-SPEC §2 host fallback stays untested. Ollama for 5.3b remains deferred.
