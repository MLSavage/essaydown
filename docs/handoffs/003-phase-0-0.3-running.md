# Handoff 003 — Phase 0, task 0.3 running (rotation after codifying principal rotation)

Written 2026-09-06 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at Michael's request after landing DECISIONS #015. Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `002-phase-0-running.md` (still correct for the gate evidence pattern and the deviations list; read it once). Next: `004-*.md`.

## Current State

- `phase/0` tip `3a86b3d` (`chore(0.0): principal rotation`, on top of `595b060`). Passed: 0.0, 0.1, 0.2, gate 0.2h (ACCEPT a1). Running: 0.3 (Opus, attempt 1, started 2026-09-06 07:00Z; transcript `.evidence/tasks/0.3/1.log`, ~270 events at 07:20Z, no `result` event yet). `doctor` clean. `main` untouched at the seed `660f3aa`. Nothing pushed except the `ci/*` refs `gate.sh` pushes and deletes.
- **Runner**: tmux session `essaydown`, window 1 `runner`, pane 1.0 (shell pid 8940, `node` child while a task runs). It runs `ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log`, exits at every stop signal; restart it with `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 0 2>&1 | tee -a .evidence/runner.log' Enter`. Attach from a phone: `tmux attach -t essaydown`, `Ctrl-b 1`.
- **Watcher** (background Bash, `run_in_background`, timeout 3600000; one per stop signal; stopped at this rotation, re-arm first thing). Replace the `0.3 attempt 1` marker with the task/attempt just started:
  ```
  cd /Users/mlsavage/Developer/essaydown; until grep -q "0.3 attempt 1 (loop" .evidence/runner.log; do sleep 10; done; start=$(grep -n "0.3 attempt 1 (loop" .evidence/runner.log | tail -1 | cut -d: -f1); until tail -n +"$start" .evidence/runner.log | grep -qE "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT) |<promise>COMPLETE"; do sleep 60; done; tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|ROTATE-PRINCIPAL) |^ROTATE-PRINCIPAL$|^\[ralph\] |stop-check" | tail -12
  ```
  Read an attempt's outcome from the transcript's final `result` event (`subtype`, `num_turns`, `result`).
- **Expected next stop**: 0.3 ends (DONE → integration → 0.4 starts in the same run, or `NO-JOURNAL`/`STUCK`). Then 0.4–0.11, then `HUMAN_GATE 0.verifyh` followed by the line `ROTATE-PRINCIPAL`.

## Corrections

- `002-phase-0-running.md`, Next Steps 4–5: the reconciliation `PRINCIPAL 0.12.r0d` is not done by the session that ran 0.3–0.11. At `HUMAN_GATE 0.verifyh` that session runs `/rotate` and stops; the fresh session gives Michael the gate command and does the reconciliation (DECISIONS #015).
- `next-prompt.md` (previous version) named `#012` for this rule; the entry is `#015` (`#012` is container credentials).

## Decisions since 002

- **DECISIONS #015-principal-rotation** (`3a86b3d`): rotate at `HUMAN_GATE N.verifyh` regardless of context; `ralph.sh run` prints `ROTATE-PRINCIPAL` after `HUMAN_GATE N.verifyh` and `scripts/gate.sh` prints it after recording any `N.verifyh` attempt (advisory line, not a stop signal, RUNNER-SPEC §11); conformance test `ROTATE-PRINCIPAL: …` (suite: 61 tests, all green with the fixed regex); `.claude/commands/rotate.md` is the procedure; `.claude/settings.json` has a `SessionStart` hook printing the newest handoff path and `ralph.sh status`. Reverse: `git revert 3a86b3d`.
- Committed on `phase/0` with 0.3 in flight, holding `.locks/ralph`. Residual risk accepted: if 0.3's agent also appends to `docs/DECISIONS.md` (only for a missing-tool finding, PRD §9.4), integration reports `CONFLICT 0.3` → `ralph/ralph.sh resolve-conflict 0.3` after fixing the merge in `.wt/0.3`.

## Gotchas

- The running `ralph.sh run` process loaded `ralph/lib/*.mjs` before `3a86b3d`; the new `ROTATE-PRINCIPAL` line appears only from the next restart (every stop signal restarts it, so long before 0.verifyh).
- `/rotate` is a project slash command; it is visible to sessions started after `3a86b3d`. Its lock hold is a `noclobber` write to `.locks/ralph` (pid + timestamp); keep it under a few seconds, the runner's `withLock` gives up after 30 s.
- All of 002's gotchas stand (lessons for a retried task go into its worktree via cherry-pick; never start the runner with a cherry-pick unresolved; `RALPH_CI_ADAPTER=fake` is the test gate; `docker compose run` needs `ESSAYDOWN_ROOT`).
- The 0.3 transcript has not been read by any principal yet; if it ends without DONE, read its final `result` event before `retry`.

## Next Steps

1. Re-arm the watcher (marker `0.3 attempt 1`, or whatever `grep '^\[ralph\]' .evidence/runner.log | tail -1` shows). Restart the runner after every stop signal.
2. Tasks 0.3–0.11 (Opus/Sonnet); act on `STUCK`/`NO-JOURNAL`/`NO-COMMIT` per `next-prompt.md` step 3.
3. At `HUMAN_GATE 0.verifyh` + `ROTATE-PRINCIPAL`: run `/rotate`, tell Michael "ready to rotate" with the relaunch command. The fresh session gives Michael `scripts/gate.sh 0.verifyh`, restarts the runner on ACCEPT (review set 0.12, three reviewer containers), and reconciles `PRINCIPAL 0.12.r0d` per 002 Next Steps 4, then `0.close`.
4. Handoff 004 at the next rotation point.

## Open Questions

- Unchanged from 002: Grok headless in the container (learned at 0.12.r0c); Ollama for 5.3b (deferred).
