# Handoff 031 — Phase 1 in the r9 chain at `STUCK 1.64`: the r8 reconciliation integrated (verdict FAIL, #review-1-r8), `1.63` passed, `1.64` three attempts capped at 51 with the work built, the retry's lesson decided (the block-local two-view oracle) and not yet written

Written 2026-09-23 by the principal (the fresh session that ran the `1.10.r8d` reconciliation; Claude Code on the Mac Mini), at the `STUCK 1.64` stop signal, per the handoff-first rule (a stop signal is a rotation trigger even when the next action is small). Supersedes `030-phase-1-review-r8-reconciliation.md` (in the tree since `3c96b9b`). This commit is on `handoff/031`, cut from `phase/1` at `5fb4163`; the `1.10.r9d` reconciliation cherry-picks `phase/1..handoff/031`. Next: `032-*.md`.

## Current State

- **`phase/1` tip `5fb4163`** (`task(1.63)`); `main` at `052e206`; `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **The r8 reconciliation** `1.10.r8d` integrated at `3c96b9b`: verdict **FAIL** (DECISIONS **#review-1-r8**) on Sol's two should-fix marked Required, both reproduced headlessly at `970bb5c` — N1 the cursor map's block-edge branch ignoring the typed marks for every mark but inline code, with the link members at both edges the reconciliation found; N2 the inline correspondences built from normalised widths while the conversion drops characters the live document keeps (54 of 248 corpus blocks after one deleted lead). N3 (Claude 1, the walk's give-up path) taken; N4 (Claude 2, the stale `1.9` record) a criterion paragraph with an ask to Michael; N5–N7 nits (backlog / restated / backlog); N8 the `[1.53]` class's after-side member. Graph 273 tasks (218 raw + 21 review-set + 34 CI gates): `1.63` → `1.64` → `1.65` → `1.verify.r9` (+ `1.verify.r9h`) → `1.10.r9a/b/c/d`; `1.close` on `1.10.r9d`. `.evidence/reviews/1/r8/verdict` is `FAIL`.
- **`1.63` passed** at `5fb4163` (opus; a1 51 capped with the work built, a2 passed): the block-edge invariant over every mark kind, the guard family, the corpus leg with N8's bounded exclusion, the `byteEndOf` link branch (its autolink form has no corpus instance, covered by rationale, journaled), `e2e/web/editor-toggle-mark-end.spec.ts`.
- **`1.64` `blocked` — `STUCK 1.64`** after three attempts, every one `error_max_turns` at 51 (`.evidence/tasks/1.64/{1,2,3}.log`), the work built on `task/1.64` in `.wt/1.64` (eight commits: three journal stubs, two recovery commits, a1's probe files dropped by a2, the corpus leg and the spec): `packages/editor/src/schema.ts` +190 (the exported kept-character correspondence), `packages/editor/src/toggle.ts` +294/−66 (the live correspondence in both directions), `packages/editor/test/toggle.test.ts` +467 (the whitespace × node × container guards), `packages/editor/test/position-map-inverse.test.ts` +304 (the `deleteAtEveryBlockStart` leg), `e2e/web/editor-toggle-stripped-lead.spec.ts` (Sol's route, both cases), one backlog line. a3's own suite run was green (`Test Files 44 passed`, `Tests 4307 passed`) and it capped re-running the whole suite "again, plus cargo test" — the second-verification-pass cap. **What cost the turns**: the leg's two-view oracle formats the whole document twice per caret position, 53 s on `essay-fixture.md` against the 30 s timeout the acceptance fixes (`grep -c '30_000\|30000' … at least 3`); a2 ran out of turns fighting it and a3 shipped a leg **sharded twelve ways with the byte sweep on one fixture bounded by time** ("bounded the byte sweep on one fixture only", `position-map-inverse.test.ts:497–511, :623–637` on the branch) — a narrowing the task text forbids. **The recovery commit `87aecca` swept a 161-line mid-edit of that test file** (a re-shard "position N of every 12" over the earlier "block N of every 12"); whether it is a consistent state is unknown — the retry's agent reads that diff first.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `STUCK 1.64` (runner.log 133,736 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the retry: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped at rotation; **arm a fresh one after every restart or `retry`**):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `STUCK 1.64`. After the retry and restart the next own line is `[ralph] 1.64 attempt 1` (the retry resets attempts).
- **Expected next stop**: `1.64`'s integration then `[ralph] 1.65 attempt 1`; the block ends at `HUMAN_GATE 1.verify.r9h` (rotate there).

## Corrections

- `030` Current State, "Expected next stop: the reconciliation commit; then `1.close` or the r9 chain": the reconciliation is done (FAIL); the r9 chain is running.
- `030` Decisions, "Grok's PASS-with-blockers count stays 3 of 5": confirmed at #review-1-r8; the new fact recorded there is Grok's zero-finding PASS beside two Required should-fix (not a blocker instance).
- `030` and `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/030`": through `handoff/031`.
- `030` Gotchas, "Claude nit 2 reopens M3 (a)'s wording … decide, do not widen": decided at #review-1-r8 N6 — restated as satisfiable (the files whose job is to say what to print carry the literal by design; nothing written from now on quotes it); M3 (b) unchanged at the boundary.
- The reconciliation's journal line was first appended glued to the previous entry: `journal-main.md` ended without a trailing newline after `task(1.verify.r8.g1)`; fixed before the commit (`grep -c '^- \[1.10.r8d\]'` is 1). Check the last byte before any append (memory `project-journal-trailing-newline`).

## Decisions

- **`#review-1-r8`** (in the tree, `3c96b9b`): verdict FAIL; findings N1–N8 with dispositions; M1–M11 each confirmed or reopened (M5 reopened as N1; J3 a fourth time as N2); backlog deferrals checked, none fired; the reviewer record (unique findings Claude 5 of 5, Sol 2 of 2, Grok 0 of 0; no shared finding; cumulative unchanged; Grok's PASS-with-blockers 3 of 5); the turn-budget counts (76 loop ids, 37 multi-attempt, `NO-JOURNAL` 16, `STUCK` 10, before this chain); promote candidates (a)–(c) for `1.verify.r9`; the N1 prototype (one loop over the enclosing mark entries; one pin yields, `position-map-editor-leg.test.ts:832` via `byteEndOf`); the reversal. Five `[1.10.r8d]` lessons. Backlog: the N8 member on the `[1.53]` line, N4's ask on `[repair-1.9 (c)]`, `[review-1-r8 N5, React 19]`, `[review-1-r8 N7, ci.yml merge step]`, the turn-budget and reviewer-record evidence lines.
- **Rules in force**: as 030, plus N6's restatement of M3 (a). New from this block, for the `1.64` retry (a lesson line, not a scope change): a corpus oracle that serialises per caret position runs on the block alone — `doc.cut($block.before(), $block.after())` keeps the containers as open ancestors (verified in the scratch worktree at `970bb5c`: a list item's paragraph cuts to `list > list_item > paragraph`, a cell's to `table > table_row > table_cell`), positions offset by `before` — so the whole position set is kept and no bound, shard or time cap is needed (the essay fixture: 101 blocks, 27,878 positions, 9.7 s, against 53 s whole-document).
- **Runner deviations this block**: none. No `retry` yet (the `STUCK` is fresh), no 429, no `NO-JOURNAL`, no `admin`, no `docker/` change, no `fix(runner)`. The reconciliation ran the full procedure (cherry-pick of `phase/1..handoff/030` clean, `generate-tasks --check` byte-identical, `validate-tasks` OK, `bash ralph/test/run.sh` 83/83); the reproductions and the N1 prototype ran in a detached scratch worktree at `970bb5c` (`<scratchpad>/970bb5c`, still present on disk with `node_modules`, throwaway — `git worktree remove --force` it or leave it; never used for a task).
- **Turn-budget evidence** (conditional on Michael's OK — unchanged as a decision): `1.63` (opus) a1 51 capped with the work built, a2 passed; `1.64` (opus) a1 51 / a2 51 / a3 51, `STUCK`, every cap with the work built — the a2 and a3 caps spent on the leg's cost and a second verification pass. With #review-1-r8's table: every opus fix task of the r8 and r9 chains so far capped at a1.
- **Open ask to Michael, unchanged** (#review-1-r8 N4): whether a `1.9.r1` approval retry at the r9 implementation SHA precedes `1.close` — on yes, the r9 reconciliation appends it; nothing to run now.

## Gotchas

- **The `1.64` retry is the next action and it is a principal decision the task text already answers**: "at every text position of that block" and "never a narrowing" bind; the 30 s timeout is fixed by the acceptance grep. The lesson line tells the agent the block-local oracle (Decisions above), tells it to read `87aecca`'s diff before touching the leg (a swept mid-edit — memory `project-recovery-commit-can-carry-a-mutation`), to remove the twelve-way shard and the time bound (one `it` per fixture, every position, the exclusions positively bounded as the text says), to append a NEW `- [1.64] ` journal line with the `printf` that prints the timestamp, and to run the suite once (a second verification pass is what a3's cap was spent on). It never widens scope (#028) and never quotes the promise (N6).
- Before `ralph/ralph.sh retry 1.64`: move `.evidence/tasks/1.64/1.log`, `2.log`, `3.log` aside with a suffix (#027: `mv 1.log 1.a1-capped.log` etc.); the lesson goes as `chore(1.64): principal lesson …` on `task/1.64` in `.wt/1.64` (`docs/lessons.md` only, reversal in the message); `git -C .wt/1.64 status` must be clean first (it is: the recovery commit holds everything). Never install or run pnpm in `.wt/1.64`.
- The watcher dedups whole lines (memory `project-watcher-dedup-after-retry`): after the retry the runner prints `[ralph] 1.64 attempt 1` again — arm a fresh watcher after the restart, never reuse one.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit; Michael runs it in his shell. Claude Code blocks a foreground `sleep`; wait with a backgrounded `until` loop.
- `ralph/ralph.sh status` says "running attempt 1" while the log shows attempt 2 or 3 of a task: the summary's attempt field lags the log; read the log.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence; a 30-minute silent watcher window during an opus attempt is normal (`docker ps` and the transcript's growth are the liveness check).

## Next Steps

1. **The `1.64` retry** (this block, next session): `git -C .wt/1.64 status --short` clean and `git -C .wt/1.64 log --oneline phase/1..HEAD` showing the eight commits; move the three logs aside; write the lesson line (Gotchas above; one `[1.64] <ISO> LESSON:` line, the block-local oracle with its numbers, the recovery-commit read, the shard and bound removed, the journal `printf`, the suite once) and commit it as `chore(1.64): principal lesson — the block-local two-view oracle for deleteAtEveryBlockStart` with `Reverse: git revert this commit.`; `ralph/ralph.sh retry 1.64`; idle checks; restart the runner; fresh watcher. On a second `STUCK` with the leg still the cost: a `#038`-shaped manual decision (cut the leg's oracle to the block-local form by task text — a planning commit is not available for a running task; the lesson is the instrument; #026–#028 are the pattern).
2. **The rest of the r9 chain**: `1.65` (opus), `1.verify.r9` (sonnet; promote (a)–(c)), gate `1.verify.r9h` (Michael: `scripts/gate.sh 1.verify.r9h`; **rotate there**), the r9 review set, `1.10.r9d` in a fresh session (#015) — the FAIL pattern is `task(1.10.r8d)` at `3c96b9b` with #review-1-r8, the PASS pattern `task(0.12.r2d)` at `052e206` with #review-0-r2; it cherry-picks `phase/1..handoff/031` (one commit, `docs/handoffs/` only) and, on Michael's yes to N4, appends `1.9.r1`.
3. **After `1.close`** (host checkout on `phase/2`, runner idle): 030's Next Steps 2 unchanged — the boundary `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` over assistant text blocks with its conformance test, the recovery-commit guard), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r8 principal lessons (now including #review-1-r8 N4's human-gate lesson and the `[1.10.r8d]` prototype lesson), Claude r8 nit 3 (`ci.yml` `if: always()`, `[review-1-r8 N7]`), React 19's ratification (`[review-1-r8 N5]`, Michael's OK), the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/031`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 … #review-1-r8 and this chain), the 429 signal (#027), the gate fetch-on-failure (I7), the reviewer-prompt option, Grok's read-only mode.

## Open Questions

- Whether `1.64`'s retry lands at one attempt with the block-local oracle (the r9 chain's first `retry`), or the leg's cost needs a manual decision.
- Michael's answer on the `1.9.r1` approval retry (N4).
- The cap raise, the 429 signal, Grok's mode, React 19: Michael's calls, evidence unchanged in kind.
