# Handoff 036 — Phase 1 at `PRINCIPAL 1.10.r9d`: gate `1.verify.r9.g1h` ACCEPT at a1, review set r9 done (Claude FAIL 1/3/2, Sol FAIL 0/3/0, Grok PASS 0/0/0), the FAIL reconciliation `1.10.r9d` waits for a fresh session; the handoff stack is eight commits on `handoff/036`

Written 2026-09-24 by the principal (the session that ran the CI gate `1.verify.r9.g1h` from the session, restarted the runner and followed the r9 review set to `PRINCIPAL 1.10.r9d`; Claude Code on the Mac Mini), at the rotation point a `PRINCIPAL` signal is (DECISIONS #015: the reconciliation is never run by the session that followed the phase's tasks). Supersedes `035-phase-1-verify-r9-g1-gate.md` (on `handoff/035`, not in the tree), and through it 034, 033, 032, 031 and the in-tree `030-phase-1-review-r8-reconciliation.md`. This commit is on `handoff/036`, stacked on `handoff/035`; the `1.10.r9d` reconciliation cherry-picks `phase/1..handoff/036` as one range — eight commits, `docs/handoffs/**` and `docs/DECISIONS.md` (#038, #040) only. Next: `037-*.md`.

## Current State

- **`phase/1` tip `287c884`** (`task(1.verify.r9.g1)`); `main` at `052e206`; `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **Gate `1.verify.r9.g1h` ACCEPT at a1** (ci.yml run `35968837053` at `287c884`, ref `ci/1.verify.r9.g1/a1`, `07:17Z`–`07:27Z`, run from the principal session with Michael's `gh` login, backgrounded, exit 0 — the #review-1-r5 and r9h precedent). Every runner green; `test-logs` and `playwright-report` each hold three OS directories under `.evidence/ci/1.verify.r9.g1h/a1/`. Per OS: `Test Files 45 passed (45)`, `Tests 4572 passed (4572)`, cargo `ok` ×3, `102 passed` Playwright. The block-local leg (`essay-fixture.md: at every textblock edge, on both routes, the two views write the same bytes`) against the family's 30 s: ubuntu 1,297 ms, macos 920 ms, windows 1,875 ms — handoff 035's first open question closes.
- **Review set r9 passed, attempt 1 each** (`07:28:21Z`–`07:48:39Z`, started together from `287c884`; `verification_sha == implementation_sha == 287c884`, `verifier_id 1.verify.r9.g1`; all three `exit-code 0`; no 402, no retry, no reconfiguration). `.evidence/reviews/1/r9/{claude,sol,grok}/`:
  - **Claude (claude-opus): FAIL — 1 blocker, 3 should-fix, 2 nits.** Blocker: `packages/core/src/format.ts:786` — when two flanking runs of *different* marks become adjacent and the first ends in punctuation, the built-in `emphasis`/`strong` handlers write the delimiters as one run (`emphasis("(b)") + strong("z")` → `*(b)***z**`, which parses as `text("*(b)*") + strong("z")`); reached by Backspace at the start of the second of two paragraphs (`joinBackward`, driven in Chromium with the 1.62 readout) and by Delete of the single space between two runs in one paragraph; `[review-1-r6 L11]`'s "unreachable from the editor" premise is true of same-mark runs and false of different-mark runs. Should-fix 2: `position-map-inverse.test.ts:583` and `toggle.test.ts:2009` — the block-local legs bridge to the document with a shift computed from both sides and asserted only against itself. Should-fix 3: `e2e/web/editor-soft-line-breaks.spec.ts:181` — the caret precondition after the computed-point click raced under the full suite once in three container runs (`offset 16` received, `5` expected; 3/3 green alone). Should-fix 4: `positions.ts:1060` — the `overlapping-edit` give-up arm is uncovered through `buildMap`. Nits: `toggle.ts:414/:420` `inline === undefined` arms uncovered; `schema.ts:1042/:1055` defensive arms uncovered. Riskiest: every corpus leg edits one position inside one block — none makes two nodes adjacent, joins, splits or moves a block.
  - **Sol (GPT-6 Codex): FAIL — 0 blockers, 3 should-fix (all Required), 0 nits**, each reproduced with native input and real Copy Markdown. 1: `toggle.ts:508` — an interior boundary whose typed marks match neither neighbouring run is assigned to the following run (`q [ab](u)*cd* r`, caret at the link's end). 2: `toggle.ts:675` — the dropped-whitespace shortcut returns outside every enclosing mark while the live caret still carries one (`*x ` + code span + `*`, Delete x, ArrowRight). 3: `toggle.ts:692` — the leaf-start counterpart of the code-span end rule is missing and ordinary Delete reaches it (`a` + code span, Delete a). Coverage 99.45 → 99.52 % statements; quiet 10 k p95 rendered pane 12.3 ms, source commit 42.2 ms; independent sweeps 0 fixed-point failures across 2,592 and 9,720 documents.
  - **Grok (Grok 4.6): PASS — 0/0/0.** Static read; gate table all pass; riskiest: the `[1.46]` caret-motion class (four gates by four routes), the fenced-code inverse exclusion, the three named cursor-map members hard-stopped at `2.verify`.
  - **Counts before any pattern claim**: unique findings Claude 6, Sol 3, Grok 0; **0 shared findings** (no file:line overlap — Claude's are the serializer, the oracle bridge, the caret race and coverage; Sol's are three ownership branches of `toggle.ts`); Grok's zero against two FAILs is the r8 grok-row question again and belongs in the reviewer record, not in a reconfiguration.
- **`1.10.r9d` principal-pending, attempts 0**, worktree `.wt/1.10.r9d` on `task/1.10.r9d` at `287c884`, clean. Model `opus`, `interactive-principal`. `EXPECTED_COUNT` 276.
- **Handoff stack**: `handoff/036` = `handoff/035` + this commit; the stack sits on `2507210`, `phase/1` is two task commits ahead with no `docs/DECISIONS.md` change, so `git log --oneline phase/1..handoff/036 | wc -l` prints 8 and the cherry-pick is conflict-free. `git branch --list 'handoff/03*'`: `handoff/030` (stale ref, file in tree), `033`, `034`, `035`, `036`.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, at the prompt after `PRINCIPAL 1.10.r9d` (runner.log 141,043 lines at writing). `.locks/` empty, no `ralph run` process, no container. Restart after the reconciliation's DONE commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll, 30 min cap — re-arm on expiry; this session's is stopped; **a fresh one after every restart or `retry`**; `ralph/ralph.sh status` on every expiry notice):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r9d` (the last stop signal); a review set prints no `[ralph] <id> attempt` lines — the watcher is silent for its whole run (20 min here) and fires at `PRINCIPAL`.
- **Expected next stop**: after the reconciliation's DONE commit and the restart, the r10 chain's first fix task `[ralph] 1.67 attempt 1` (ids from the planning), or `HUMAN_GATE 1.9.r1` if the reconciliation orders it first.

## Corrections

- `035` Current State, "Gate `1.verify.r9.g1h` human-pending" and Next Steps 1 "Michael runs `scripts/gate.sh 1.verify.r9.g1h`": run from the principal session (backgrounded, output file read), the r5/r9h precedent; ACCEPT at a1.
- `035` Open Questions 1 (the block-local leg on the three runners): closed — 920–1,875 ms against 30 s.
- `035` Open Questions 2 and Next Steps 2 "on a PASS … `1.9.r1` then `1.close`": the verdict is FAIL (two of three); the reconciliation is the FAIL pattern (`task(1.10.r8d)` at `3c96b9b`, #review-1-r8) and opens the r10 chain; `1.9.r1` is still appended (#040) and `1.close` depends on it beside the chain.
- `035` Current State, "the reconciliation cherry-picks `phase/1..handoff/035` (seven commits)": `phase/1..handoff/036`, eight commits.
- `035` Next Steps 3 and `next-prompt.md` job 5, the boundary `git branch -D` list: add `handoff/036`.

## Decisions

- **No DECISIONS entry this block.** No runner deviation: no `admin`, no `fix(runner)`, no `retry`, no `docker/` change, no reviewer reconfiguration, no 402, no same-SHA rerun. The gate ran once and accepted.
- **One principal commit**: this rotation (`chore(0.0)`, `docs/handoffs/**` only, on `handoff/036`).
- **Runner fact read, not changed**: a review set writes per-reviewer `status.json`, `exit-code`, `report.md` and `transcript.log` under `.evidence/reviews/<phase>/<rN>/<reviewer>/` and no `.evidence/tasks/<id>/` directory; `ralph.sh status` reports the three as `passed` with `attempts 1` regardless of verdict (the verdict is the reconciliation's to record in `.evidence/reviews/1/r9/verdict`).
- **Turn-budget evidence** (conditional on Michael's OK, #review-1-r1): unchanged from 035 — no attempt capped this block (the gate and the reviewers are not turn-capped tasks). The `[review-1-r9, turn budget]` line the reconciliation writes: `1.64`'s six caps, `1.63`'s and `1.65`'s a1 caps, `1.66`'s two caps then its 31-turn pass.
- **Rules in force**: as 033–035 (the three r9 promotions included). Nothing new promoted; the r10 promotions are the reconciliation's call from the three reviews' lesson sections.

## Gotchas

- **The reconciliation's inputs**: Claude's blocker names a *serializer* class (mdast-util-to-markdown's built-in `emphasis`/`strong` handlers, `format.ts:786`) reached by two one-keystroke editor routes — the fix task reads the installed handler and the `[review-1-r6 L11]` backlog line before touching the editor (CLAUDE.md's mark-edge rule); Sol's three are `toggle.ts` ownership branches (`:508`, `:675`, `:692`) with native routes — one fix task per finding on the r8 pattern, each with its enumerated guard set, the corpus leg that Claude's risk 1 asks for (a transaction that makes two nodes adjacent — a join, a whole-separator deletion — over ordered pairs of mark kinds) belongs to the blocker's task. Claude's should-fix 3 (the `:181` caret race) is `[1.46]`'s class (#037): scope it to a per-step poll or a backlog line with the trigger, never a magnitude.
- **The FAIL reconciliation's own checklist** (`task(1.10.r8d)` at `3c96b9b` is the model): cherry-pick `phase/1..handoff/036` first (eight commits, `docs/**` only); `docs/DECISIONS.md` #review-1-r9 with the reviewer record and the counts above; `docs/progress.md` regenerated; `ralph/tasks.json` appends the r10 chain (`1.67…`, `1.verify.r10`, `1.verify.r10h`, `1.10.r10a/b/c/d`) and `1.9.r1` (#040: at the r9 implementation SHA `287c884`, `1.9`'s procedure plus the `[repair-1.9 (c)]` two readers, `1.close` depending on it beside `1.10.r10d`); `ralph/EXPECTED_COUNT` from 276 by the number of appended tasks; `generate-tasks --check`, `validate-tasks`, `bash ralph/test/run.sh` with its count captured to a file; the promise only in the `wip(1.10.r9d)` commit message, never in a file. Touch only `docs/**`, `ralph/tasks.json`, `ralph/EXPECTED_COUNT` (RUNNER-SPEC §8.1).
- **`1.9.r1` model choice**: #040 records Michael's yes; the reconciliation appends it, nothing to ask. It is a human gate (`--outcome`, a note) on the r9 tree, so the r10 fixes move `phase/1` past it — record in the task text that the observation SHA is the tree Michael drives, not the phase tip.
- **A fresh container has no `node_modules`**: every fix-task text that enumerates steps names `pnpm install --frozen-lockfile` first (10 s), or the agent spends four calls discovering it.
- **Reading a review transcript for a 402 or a rate limit**: grep `"status":402` and `rate_limit_event`, never a bare `402` (the host `grep` is ugrep; a bare number matches line numbers and byte counts — 30 false hits in Claude's r9 transcript).
- **Worktrees**: `.wt/handoff-036` is removed by this rotation; `.wt/1.10.r9d` is the runner's (the reconciliation's working copy); the prior session's scratch worktree at `/private/tmp/claude-501/-Users-mlsavage-Developer-essaydown/b6d390f4-…/scratchpad/970bb5c` (detached, `node_modules`) is still registered — throwaway; `git worktree remove --force` it at the boundary.
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit — background it and read its output file. A `tail -3` on a test runner's output loses its summary: capture to a file and grep.

## Next Steps

1. **The reconciliation `1.10.r9d`** in a fresh session (#015): the FAIL pattern `task(1.10.r8d)` at `3c96b9b` with #review-1-r8; the implementation SHA is `287c884`; the checklist in Gotchas; then the restart line and a fresh watcher for `1.67`'s first attempt.
2. **The r10 chain**: the fix tasks sequentially, `1.verify.r10`, the CI gate `1.verify.r10h` (run from the session as here, or Michael's shell), the review set `1.10.r10a/b/c`, `PRINCIPAL 1.10.r10d` — rotate there. A capped attempt with the work built is the chain's pattern (035 Next Steps and `next-prompt.md` job 3: `NO-JOURNAL` → lesson commit and restart; `STUCK` → lesson, logs aside, `retry`, restart; a second `retry` is a DECISIONS entry first, a third is Michael's).
3. **After `1.close`** (host checkout on `phase/2`, runner idle): 035's Next Steps 3 unchanged — the `fix(runner)` list (#025, #023 ×2, H7, #review-1-r7 M3 (b) `transcriptHasDone` with its conformance test, the recovery-commit guard, the gate fetch-on-failure I7 ×2), `chore(0.0)` for rotate.md step 5, the stale `description` line, PRINCIPAL.md's doctor sentence and the r4–r9 principal lessons, Claude r8 nit 3 (`ci.yml` `if: always()`), React 19's ratification (Michael's OK), the `check-deps` major-version comparison (Michael's OK); `git branch -D handoff/010 … handoff/030 handoff/033 handoff/034 handoff/035 handoff/036`; the scratch worktree; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5 … #review-1-r8, handoffs 031–036, #038), the 429 signal (#027), the reviewer-prompt option, Grok's read-only mode.

## Open Questions

- Whether the reconciliation scopes Claude's blocker as one fix task (serializer handler override plus the adjacency corpus leg) or two; whether Claude's should-fix 2 (the oracle bridge) rides with it or stands alone.
- Grok's third consecutive zero-finding PASS against two FAILs (r7: 0, r8: 0, r9: 0 unique findings — count it from the three `status.json` files before writing the pattern): a reviewer-record line at r9, a Michael decision (the read-only mode, the reviewer-prompt option) at the boundary, never inside a set.
- The cap raise, the 429 signal, React 19: Michael's calls, evidence unchanged in kind.
