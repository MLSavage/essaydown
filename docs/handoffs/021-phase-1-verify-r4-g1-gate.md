# Handoff 021 — Phase 1 at `HUMAN_GATE 1.verify.r4.g1h`: gate `1.verify.r4h` GATE-FAILED at a1 (#024), repair 1.39 and `1.verify.r4.g1` passed, the second three-OS gate of the r4 round waits for Michael

Written 2026-09-12 by the principal (the session that ran the `1.10.r3d` reconciliation, watched the r4 chain, and wrote the `#024` planning commit; Claude Code, Remote Control session on the Mac Mini), at the `HUMAN_GATE 1.verify.r4.g1h` stop signal (rotate regardless of context, DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `020-phase-1-verify-r4-gate.md` (on `handoff/020`, not in the tree). This commit is on `handoff/021` (a verifier gate is open, so never on `phase/1`; rotate.md step 5, second case); the `1.10.r4d` reconciliation cherry-picks `handoff/020` then `handoff/021`. Next: `022-*.md`.

## Current State

- **`phase/1` tip `6c0e601`** (`task(1.verify.r4.g1)`); 60 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`). `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean; no `.wt/` worktrees.
- **Gate `1.verify.r4h`: GATE-FAILED at a1, superseded** (ci.yml run 34685866034 at `4c133e0`): ubuntu-latest and windows-latest each red on one Playwright case, the same — 1.38's G4 first-line case in `e2e/web/editor-soft-line-breaks.spec.ts`, its caret precondition receiving offset 8 / 16 instead of 5 (`ArrowUp` in a two-line paragraph is a visual-line motion landing by x-position and font metrics); macos-latest 83/83; every unit, cargo and lint step green on all three. Code-caused → planning commit `plan(plan.1.verify.r4h.r0)` `0251db3` = **DECISIONS `#024`** (cause, scope decision — counted horizontal arrows from the asserted block-end anchor, in the failing file only; the trailing-break file's two `toDocumentEnd` uses on the backlog with a trigger — graph change, model choice, reviewer notes, reversal). `EXPECTED_COUNT` 208 → 211.
- **Repair round passed at attempt 1**: `1.39` (opus, test-only; 18 turns; `grep -c 'toDocumentStart\|toDocumentEnd\|ArrowUp\|ArrowDown' e2e/web/editor-soft-line-breaks.spec.ts` is 0 at the tip) and `1.verify.r4.g1` (sonnet; 26 turns; no promote step — 1.verify.r4's two promotions are in the tree at `4c133e0`).
- **Gate `1.verify.r4.g1h`: `human-pending`, attempts 0.** Michael runs `scripts/gate.sh 1.verify.r4.g1h` (ci.yml on `ci/1.verify.r4.g1/a1` at `6c0e601`; artifacts `test-logs` and `playwright-report`; three OS directories expected in each). First unknown: the ubuntu-latest and windows-latest Playwright legs of `editor-soft-line-breaks.spec.ts` (1.39's anchor-and-count route; the container and macOS landed right on the old route too, so only those two runners can prove the repair).
- **Tasks** (`ralph/ralph.sh status`): phase 1 — 77 tasks, passed 71, superseded 4 (`1.verifyh`, `1.verify.r2h`, `1.verify.r3h`, `1.verify.r4h`), human-pending 1, pending 5 (`1.10.r4a/b/c/d`, `1.close`); `doctor` clean. Branches `handoff/010` … `handoff/021`. Lessons tagged `1.*`/`plan.1.*`: 124.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r4.g1h` / `ROTATE-PRINCIPAL` (runner.log line 79,575; the log is 79,576 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the gate's ACCEPT (and after `doctor` is clean): `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r4.g1h` (line 79,575).
- **Expected next stop**: after `ACCEPT` at a1 and the restart, `stepReview` starts `1.10.r4a/b/c` from one snapshot (`phase/1` must stay at `6c0e601`), then `PRINCIPAL 1.10.r4d` (a fresh session; rotate there). On `GATE-FAILED`: `ralph/ralph.sh plan plan.1.verify.r4.g1h.r0` → planning commit `#025` in the `#024`/`de5dcf2` shape (`1.40` repair + `1.verify.r4.g2`, `1.10.r4a/b/c` rewired again). On a transient failure (a file unchanged since its last green run on that OS, a hang before any assertion): `scripts/gate.sh rerun 1.verify.r4.g1h`, then `#023`'s abandon for the rejected a1 once `doctor` flags it.

## Corrections

- `020` Current State and Expected next stop assumed `ACCEPT` at a1 or a planning commit numbered `#024` on failure: the gate failed at a1 and `#024` is that planning commit (in the tree at `0251db3`); a further planning commit this round is `#025`.
- `020` Gotchas "`handoff/020` holds one commit … the reconciliation cherry-picks `git cherry-pick phase/1..handoff/020`": still one commit, and now `handoff/021` (one commit) after it — `git cherry-pick phase/1..handoff/020 && git cherry-pick phase/1..handoff/021`, each range checked first (the `next-prompt.md` conflict resolves to the later version, as at every reconciliation).
- `020` Decisions said the r4 chain's e2e repairs were proven "in the container": the `#024` lesson — a route the container and macOS both pass can still land wrong on ubuntu and windows when it depends on rendered geometry; "Blink-only" means decided from the document's characters.

## Decisions

- **`#024`** (in the tree at `0251db3`): what happened, cause (a vertical arrow in a multi-line block is a visual-line motion; 1.33's one-line rule was over-extended by 1.38; the location assertion did its job), scope (counted `ArrowLeft` from the asserted block-end anchor, the failing file only; the trailing-break file's two `toDocumentEnd` uses on the backlog with a trigger — they passed on all three runners with their landing assertions), graph change (1.39, `1.verify.r4.g1`, reviewers rewired to `1.verify.r4.g1h`), reviewer notes (vertical arrows elsewhere; the `it.fails` known-defect case of 1.37 is the opposite of a skip, the reconciliation dispositions the defect), reversal (git-only).
- **Turn budget, evidence only**: the r4 repair round passed at a1 in 18 and 26 turns (test-only, sonnet verifier). The chain evidence stands as in 020 (six of six capped opus fix tasks on the strip family); the decision is Michael's at the boundary.
- **Runner deviations this block**: none. GATE-FAILED → blocked → plan request → `ralph.sh plan` → `plan(...)` integration → `superseded`, all per RUNNER-SPEC §2.

## Gotchas

- **After `ACCEPT`: `doctor`, then idle checks, then restart.** After any `gate.sh rerun` that accepts, `doctor` names `--resume a<n>` — never run it; `scripts/gate.sh abandon 1.verify.r4.g1h a1 --reason "rejected a1 superseded by the transient rerun a2 (ACCEPT); marked abandoned only so doctor stops reading it as incomplete (DECISIONS #023)"`, then `doctor` clean, then restart.
- **Never run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>`** (the `[1.10.r3d]` lesson; the runner runs a principal task's suite there in the container). A headless probe goes in `git worktree add --detach <scratchpad>/probe <sha>` and is removed after.
- **The r4 reviewers' inputs**: `/logs/ci/1.verify.r4.g1h/accepted/`; `#023` and `#024` are in the tree at their SHA; `handoff/020` and `handoff/021` are not (they enter at the reconciliation). The `[1.37]` cursor-map backlog line and its `it.fails` case, and the `[plan.1.verify.r4h.r0]` vertical-arrow line, are the two things the reconciliation dispositions beyond the reviewers' findings.
- **Evidence for the boundary `gc`**: `git ls-remote origin 'refs/heads/ci/*'` first; expect `ci/1.verify/a1`, `ci/1.verify.r2/a1`, `ci/1.verify.r3/a1`, `ci/1.verify.r4/a1` (GATE-FAILED rounds), `ci/1.verify.r3.g1/a1` (abandoned), possibly `ci/1.verify.r3.g1/a2`, and `ci/1.verify.r4.g1/a<n>` after this gate.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael and the ask: `scripts/gate.sh 1.verify.r4.g1h`.
2. On `ACCEPT`: `doctor` clean (if a rerun accepted, `#023`'s abandon first), idle checks, restart; the runner starts `1.10.r4a/b/c` (~20 min at r3) → `PRINCIPAL 1.10.r4d`. Rotate there; the reconciliation is a fresh session (#015). Its checklist: cherry-pick `handoff/020` and `handoff/021` (each range one commit; the `next-prompt.md` conflict resolves to the later version); copy the three reports verbatim; reproduce any blocker in a detached scratch worktree, never in `.wt/1.10.r4d`; `#review-1-r4` after `#024` with `verdict:` first, I1–I10 confirmed or reopened, the reviewer table re-derived from the bodies, the `[1.37]` cursor-map line and the `[plan.1.verify.r4h.r0]` vertical-arrow line dispositioned, the turn-budget note with counts from the state file and the transcripts; on PASS `1.close` follows; on FAIL `1.40…` + `1.verify.r5` + `1.10.r5a/b/c/d` the way `ede7729` did.
3. On `GATE-FAILED`: read `.evidence/ci/1.verify.r4.g1h/a1/workflow.log`, classify (code-caused → `ralph/ralph.sh plan plan.1.verify.r4.g1h.r0`, planning commit `#025` per `#024`, scoped to what failed; transient → `gate.sh rerun`, #023).
4. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): `#023`'s `fix(runner)`; H7's `fix(runner)`; `chore(0.0)` for rotate.md step 5's third case, the stale `description` line and PRINCIPAL.md's doctor sentence; the conditional items on Michael's OK only — the turn-budget raise, the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option; `git branch -D handoff/010 … handoff/021`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010).

## Open Questions

- Whether 1.39's anchor-and-count route lands at offset 5 on ubuntu-latest and windows-latest — the only proof is this gate.
- The r4 verdict (as in 020): whether a reviewer rates the `[1.37]` cursor-map defect a blocker decides between `1.close` and an r5 chain.
- The turn budget and the fetch-on-failure change: Michael's calls at the boundary.
