# Handoff 020 — Phase 1 at `HUMAN_GATE 1.verify.r4h`: the r3 reconciliation integrated (verdict FAIL, DECISIONS #review-1-r3), fix chain 1.34–1.38 and `1.verify.r4` passed, the three-OS gate waits for Michael

Written 2026-09-12 by the principal (the fresh session that ran the `1.10.r3d` reconciliation and watched the r4 fix chain; Claude Code, Remote Control session on the Mac Mini), at the `HUMAN_GATE 1.verify.r4h` stop signal (rotate regardless of context, DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `019-phase-1-review-r3-reconciliation-pending.md` (in the tree since `ede7729`, with 017 and 018). This commit is on `handoff/020` (a verifier gate is open, so never on `phase/1`; rotate.md step 5, second case); the `1.10.r4d` reconciliation cherry-picks it. Next: `021-*.md`.

## Current State

- **`phase/1` tip `4c133e0`** (`task(1.verify.r4)`); 57 commits since the phase base `052e206` (`git rev-list --count 052e206..phase/1`). `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean. No principal commit on `phase/1` until `1.close`.
- **The r3 reconciliation** integrated as `task(1.10.r3d)` `ede7729` (verdict FAIL; `.evidence/reviews/1/r3/verdict` reads `FAIL`); DECISIONS `#review-1-r3` with findings I1–I10; reports byte-identical in `docs/reviews/phase-1-r3-*.md`; handoffs 017, 018, 019 and `#023` in the tree; the owner's e-mail address in `docs/reviews/phase-0-r0-claude.md:26` redacted to `<owner address>` (I9, recorded). `EXPECTED_COUNT` 197 → 208 (162 raw + 21 review-set + 25 CI gates). The first restart after the DONE commit stopped `PRINCIPAL 1.10.r3d suite failed` (attempts 0 → 1): this session's headless reproductions had run `pnpm exec vitest` in `.wt/1.10.r3d` on the Mac, and the runner runs a principal task's suite in that same worktree inside the container, where pnpm refused to purge the host-built modules directory without a TTY; `git clean -fdX` removed the six ignored `node_modules` directories, a `[1.10.r3d]` lesson was committed on the task branch, the second restart integrated. Lesson in `docs/lessons.md` (the last `[1.10.r3d]` line) and in this session's memory: a principal never installs or runs the JS toolchain in a task worktree on the host; probe in a detached scratch worktree or in the container.
- **The r4 fix chain, all passed** (`ralph/ralph.sh status`; `.evidence/state/tasks.json`): `1.34` (I1, the mark-edge clause at an atom + the tautology; a1 capped at `num_turns` 51 / 50 tool uses with the work committed, a2 passed at 26), `1.35` (I3 + I4, the link predicate; a1 capped 51 / 50, a2 passed at 20), `1.36` (I2, `spread` re-derived + the list-split leg; a1 capped 51 / 50, a2 passed at 25), `1.37` (I5, the position-map deletion leg; a1 capped 51 / 50, a2 passed at 36; it found a cursor-map defect and filed it as a `[1.37]` backlog line — a cursor past every node the correspondence knows, after a dropped break or inside a split item's empty paragraph, maps to the item's start; outside its test-only scope, for the r4 reviewers), `1.38` (I6, the four chords replaced, `tests/no-caret-chords-in-e2e.test.ts` added; passed at a1, 35 turns), `1.verify.r4` (sonnet; passed at a1, 40 turns; promoted (a) the journal-line-per-attempt sentence into `## Every iteration` steps 2 and 4 and (b) the Blink-only caret rule as the last `## Code rules` bullet of both `CLAUDE.md` and `AGENTS.md`; (c) the doctor lesson recorded as not an agent rule). Every capped a1 continued without a stop signal under the journal-stub rule; no `NO-JOURNAL`, `STUCK` or `NO-COMMIT` this chain.
- **Gate `1.verify.r4h`: `human-pending`, attempts 0.** Michael runs `scripts/gate.sh 1.verify.r4h` (ci.yml on `ci/1.verify.r4/a1` at `4c133e0`; artifacts `test-logs` and `playwright-report`; three OS directories expected in each). First unknowns: the macOS Playwright leg of `e2e/web/editor-soft-line-breaks.spec.ts` and `editor-trailing-break.spec.ts` (1.38's repaired chords; the container cannot observe macOS) and the windows leg of the three new specs (`editor-break-in-mark`, `editor-link-edge`, 1.36's list paste case).
- **Tasks**: phase 1 — 74 tasks, passed 69, superseded 3, human-pending 1 (`1.verify.r4h`), pending 4 (`1.10.r4a/b/c/d`) + `1.close`; `doctor` clean. Branches `handoff/010` … `handoff/020`. Lessons tagged `1.*`/`plan.1.*`: 122.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r4h` / `ROTATE-PRINCIPAL` (runner.log line 77,686; the log is 77,687 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after the gate's ACCEPT (and after `doctor` is clean): `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r4h` (line 77,686).
- **Expected next stop**: after `ACCEPT` at a1 and the restart, `stepReview` starts `1.10.r4a/b/c` from one snapshot (`REVIEW-SHA-MISMATCH` if `phase/1` moved — it must stay at `4c133e0`), then `PRINCIPAL 1.10.r4d` (a fresh session; rotate there). On `GATE-FAILED`: `ralph/ralph.sh plan plan.1.verify.r4h.r0` → planning commit `#024` in the `de5dcf2` shape (`1.39` repair + `1.verify.r4.g1`, `1.10.r4a/b/c` rewired). On a transient failure (a file unchanged since its last green run on that OS, a hang before any assertion): `scripts/gate.sh rerun 1.verify.r4h`, then `#023`'s abandon for the rejected a1 once `doctor` flags it.

## Corrections

- `019` Current State "16 of 38" Phase 1 loop tasks at more than one attempt: 15 of 38 at the reconciliation, counted from `tasks.json` execution joined with the state file (the list is in `#review-1-r3`'s turn-budget item); with the r4 chain it is now 19 of 44 (1.34, 1.35, 1.36, 1.37 added; 1.38 and 1.verify.r4 at a1).
- `019` Next Steps 3 said the runner "squashes `task(1.10.r3d)`" on the first restart: it did on the second; the first stopped on the host `node_modules` (Current State above). Not a runner defect — the runner's own path (`run.mjs` `stepPrincipal` → `runSuite(ctx, wt)`).
- `019` Open Questions asked whether the chord uses become one r4 task or Phase 2 planning: one r4 task (1.38), because Sol rated the `:85` case Required on its own merits (I6 in `#review-1-r3`).

## Decisions

- **`#review-1-r3`** (in the tree at `ede7729`): verdict FAIL; I1 blocker → 1.34, I2 blocker → 1.36, I3 Required → 1.35 (with I4), I5 Required → 1.37, I6 Required → 1.38, I7 recorded (#023 quoted; fetch-on-failure a conditional boundary candidate), I8 → 1.34, I9 taken (the redaction), I10 decided (ASCII). H1–H9 confirmed, H1's family and H8 reopened. Reviewer record: unique Claude 6 of 8, Sol 2 of 4, Grok 0 of 2; cumulative low-rater Grok 6, Claude 2, Sol 2; Grok PASS with blockers at r2 and r3; the three-way-spread condition has not fired; no reviewer change inside a set. `chore(1.29)` audited as a #016 deviation and sanctioned as the shape. Promote candidates (a) and (b) promoted by `1.verify.r4`, (c) recorded.
- **Turn budget, evidence only (decision at the Phase 2 boundary on Michael's OK, #review-1-r1)**: every opus fix task that touched `packages/editor/src/schema.ts` or its test family in the r2, r3 and r4 chains capped at a1 with the work committed and passed at a2 in 20–36 turns (1.29, 1.30, 1.34, 1.35, 1.36, 1.37 — six of six; 1.31 finished one turn under the cap); the two test-only or non-strip tasks passed at a1 (1.32, 1.38). Reviewers at 160: Claude 85 turns at r3. The number stays 80, conditional; the transcript measure (`num_turns`, tool-use blocks) is the input.
- **Runner deviations this block**: none new. The `suite failed` stop was the runner behaving as specified against a polluted worktree.

## Gotchas

- **The gate first, then `doctor`, then restart.** After `ACCEPT`, `doctor` must be clean before `ralph run`; after any `gate.sh rerun` that accepts, `doctor` names `--resume a<n>` — never run it (it overwrites the accepted record); `scripts/gate.sh abandon 1.verify.r4h a1 --reason "rejected a1 superseded by the transient rerun a2 (ACCEPT); marked abandoned only so doctor stops reading it as incomplete (DECISIONS #023)"`, then `doctor` clean, then restart.
- **Never run pnpm, vitest or Playwright on the Mac inside any `.wt/<id>`** (this session's lesson). A headless probe goes in `git worktree add --detach <scratchpad>/probe <sha>` and is removed after, or runs in the container.
- **`handoff/020` holds one commit** (this rotation). The `1.10.r4d` reconciliation cherry-picks `git cherry-pick phase/1..handoff/020` (verify one commit first) plus any later `handoff/021…` written before it; `#review-1-r4` is appended after `#review-1-r3`. Delete `handoff/010` … `handoff/020` after `1.close`.
- **The `[1.37]` cursor-map backlog line** is a defect found by a fix task outside its scope, in the tree at `773e1cb`; the r4 reviewers will read it, and the `1.10.r4d` reconciliation dispositions it (a fix task in an r5 chain if a reviewer rates it a blocker or Required; otherwise Phase 2 planning with its trigger).
- **Evidence for the boundary `gc`**: `git ls-remote origin 'refs/heads/ci/*'` first; expect `ci/1.verify/a1`, `ci/1.verify.r2/a1`, `ci/1.verify.r3/a1` (GATE-FAILED rounds), `ci/1.verify.r3.g1/a1` (abandoned), possibly `ci/1.verify.r3.g1/a2`, and `ci/1.verify.r4/a<n>` after this gate.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -F -- '- [id]'` needs the `--`.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael and the ask: `scripts/gate.sh 1.verify.r4h`.
2. On `ACCEPT`: `doctor` clean (if a rerun accepted, `#023`'s abandon first), idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux prompt), restart; the runner starts `1.10.r4a/b/c` (three containers, ~20 min at r3) → `PRINCIPAL 1.10.r4d`. Rotate there; the reconciliation is a fresh session (#015). Its checklist: cherry-pick `handoff/020` (+ later ones); copy the three reports verbatim; reproduce any blocker in a detached scratch worktree, never in `.wt/1.10.r4d`; `#review-1-r4` after `#review-1-r3` with `verdict:` first, I1–I10 confirmed or reopened, the reviewer table re-derived from the bodies, the `[1.37]` cursor-map line dispositioned, the turn-budget note with counts from the state file and the transcripts; on PASS `1.close` follows; on FAIL `1.39…` + `1.verify.r5` + `1.10.r5a/b/c/d` the way `ede7729` did.
3. On `GATE-FAILED`: read the log (`.evidence/ci/1.verify.r4h/a1/workflow.log`), classify (code-caused → `ralph/ralph.sh plan plan.1.verify.r4h.r0`, planning commit `#024` per `de5dcf2`, scoped to what failed with the rest on the backlog, #021/#022; transient → `gate.sh rerun`, #023).
4. Phase boundary idle window (only after `1.close`, host checkout on `phase/2`, runner idle): `#023`'s `fix(runner)` (doctor clause; `--resume` refused on a superseded attempt; one conformance test per guard plus `doctor` clean at the end of the transient-rerun test); H7's `fix(runner)` (`ralph/PROMPT.md` steps aligned to CLAUDE.md, `tests/agent-rules.test.ts` widened); `chore(0.0)` for rotate.md step 5's third case, the stale `description` line, and PRINCIPAL.md's doctor sentence (candidate (c)); the conditional items on Michael's OK only — the turn-budget raise (six of six capped opus fix tasks as the evidence), the gate fetch-on-failure (I7), the reviewer-prompt option (the counts above); `git branch -D handoff/010 … handoff/020`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010).

## Open Questions

- The r4 verdict: the five fixes are integrated with their legs and guards; the r3 reviewers' class — trees the editor builds that `parse` cannot produce — now has inline nestings and one block attribute covered, and the `[1.37]` cursor-map defect is the first named member of the position-map family found by a destructive leg. Whether a reviewer rates it a blocker decides between `1.close` and an r5 chain.
- The turn budget: six of six capped opus fix tasks on the strip family is the evidence; the decision is Michael's at the boundary, as a `fix(runner)` with its conformance leg.
- Whether Michael wants the fetch-on-failure change to `gate.sh` (#020's rule) at the boundary; one lost trace so far.
