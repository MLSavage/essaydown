# Handoff 011 — Phase 1 at `PRINCIPAL 1.10.r0d`: gate `1.verify.g1h` accepted, review attempt r0 in (Claude FAIL, Sol FAIL, Grok PASS), reconciliation pending for a fresh session

Written 2026-09-09 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `PRINCIPAL 1.10.r0d` rotation (a reconciliation is never run by the session that ran the phase's tasks; DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `010-phase-1-verifyh-gate-failed.md` (on `handoff/010`, not in the tree). This commit is on `handoff/011` (rotate.md step 5: a reconciliation is pending, so never on `phase/1`); the `1.10.r0d` reconciliation cherry-picks `handoff/010` and then `handoff/011`. Next: `012-*.md`.

## Current State

- **`phase/1` tip `e9ed88b`** (`task(1.verify.g1)`, attempt 1). Since handoff 010: `2418bc7` (`plan(plan.1.verifyh.r0)`, integrated from `plan/1.verifyh/r0`), `009895f` (`task(1.11)`), `d0d2452` (`task(1.12)`), `e9ed88b`. `main` unchanged at `052e206`. `origin/phase/1` is at `4590b8d` (Michael pushes by hand, #010); `origin` still holds `ci/1.verify/a1` at `3e98707` (the failed gate's ref; `ci/1.verify.g1/a1` was cleaned by its gate).
- **Tasks**: 1.1–1.9 passed (handoff 009), `1.verify` passed a1, `1.verifyh` **superseded** by `plan.1.verifyh.r0` (`2418bc7`), plan request resolved, **`1.11` passed a1, `1.12` passed a1, `1.verify.g1` passed a1 (`integrated_sha e9ed88b`)**, **`1.verify.g1h` ACCEPT a1, run 34338761650** (`accepted.json` sha `e9ed88b`; artifacts `test-logs` and `playwright-report`, each holding three OS directories; Playwright printed `36 passed` on ubuntu, macos and windows), **`1.10.r0a/b/c` passed** (one container run each; `.evidence/reviews/1/r0/`: `verifier_id 1.verify.g1`, `implementation_sha` = `verification_sha` = `e9ed88b`, `phase_base_sha 052e206`; claude-review ran with `--max-turns 160 --model opus`), **`1.10.r0d` principal-pending**, `1.close` pending. Reviewer verdicts from `status.json`: **Claude FAIL (1 blocker / 6 should-fix / 5 nits), Sol FAIL (0 / 6 / 1), Grok PASS (0 / 2 / 4)**. `doctor` clean; host checkout on `phase/1` at `e9ed88b`, tree clean (Michael moved his BBEdit file out); `.wt/` holds `1.10.r0d` (branch `task/1.10.r0d` at `e9ed88b`, the reconciliation's worktree) and this rotation's temporary worktree (removed by this rotation).
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `zsh` at the prompt after `PRINCIPAL 1.10.r0d`. `.locks/` empty, no `ralph run` process. Restart after the reconciliation commit: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `PRINCIPAL 1.10.r0d` (log at 39,900 lines).
- **Expected next stop**: after the reconciliation commit (`<promise>DONE 1.10.r0d</promise>`) and restart, the runner integrates it as `task(1.10.r0d)`; with verdict FAIL the first fix task of the r1 chain starts (`[ralph] 1.<n> attempt 1`), then `1.verify.r1` → `HUMAN_GATE 1.verify.r1h` (rotate there regardless of context) → `1.10.r1a/b/c` → `PRINCIPAL 1.10.r1d` (fresh session) → `1.close`.

## Corrections

- `010` Open Questions, first item — resolved: the macOS failure was only the tests. 1.11's journal records the reproduction: the Linux suite with `navigator.platform` forced to `MacIntel` failed exactly the five tests and no other; after the change both platforms are green.
- `010` Open Questions, second item — resolved: the Windows runner started the Vite `webServer` and ran the Playwright suite green (`36 passed` in the windows-latest job of run 34338761650). The "first unknown" is gone.
- `010` Current State said `origin` holds `ci/1.verify/a1` and that `cleanupRef` removes it at the next gate — it did not: the next gate cleaned its own ref (`ci/1.verify.g1/a1`) only. The failed attempt's ref stays until someone deletes it by hand (Gotchas).
- `010` Gotchas, first bullet, said the planning commit's DECISIONS entry would be written by the principal in the plan worktree — done as **#020**, on `plan/1.verifyh/r0`, now in `phase/1` at `2418bc7`; `next-prompt.md` on `handoff/010` step 5 still says "starting with `git cherry-pick handoff/010`"; now it is `handoff/010` then `handoff/011`.

## Decisions

- **DECISIONS #020** (`2418bc7`): what happened at `1.verifyh` a1, the cause (the `Mod-` platform constant read once at import from `navigator.platform`; five tests encoding the non-Mac route), the graph change (1.11 opus, 1.12 sonnet, `1.verify.g1`, `1.10` → `1.verify.g1`, `EXPECTED_COUNT` 150 → 154), model choice, notes for the reviewers, reversal (git-only before integration; none after).
- **Runner and gate behaviour this block (no deviation).** First live exercise of the plan-request path outside fixtures: `ralph.sh plan` (request `pending` → `running`, worktree on `plan/1.verifyh/r0`), planning commit with the DONE promise, `ralph.sh run` integrated it as `plan(plan.1.verifyh.r0)` (`2418bc7`), marked `1.verifyh` `superseded`, and started 1.11 — the 0.verifyh precedent (`1b1ddc2`, #018) repeated exactly. All three fix-chain tasks passed at attempt 1. `gate.sh 1.verify.g1h`: ref push, wait, artifacts fetched (both merged artifacts, three OS directories each), `ACCEPT`, `ROTATE-PRINCIPAL` printed (0.18's full verifier-gate grammar, working). Review start read the gate's `accepted.json` (0.18 F6) and recorded `verifier_id 1.verify.g1`.
- **Rotation decision.** Context is well under half, but `PRINCIPAL 1.10.r0d` ends the working block and the reconciliation must be a fresh session (#015): rotated here, no reconciliation work started, `.wt/1.10.r0d` untouched.

## Gotchas

- **The verdict is FAIL before the reconciliation reads a line** (RUNNER-SPEC §5.4: any blocker, or any should-fix marked required): Claude's finding 1 is a blocker, Sol marks findings 1–6 "Required". The reconciliation copies the r0 FAIL pattern (`c0f3c98`, `task(0.12.r0d)`, DECISIONS #review-0-r0: dispositions per finding, fix tasks with one finding class each, `0.verify.r1`, `0.12.r1a/b/c/d` physically in PRD §8, `0.close` rewired to the r1 reconciliation), not the r2 PASS pattern (`052e206`) — read both; the r2 one still holds the DECISIONS layout and the `verdict:` placement.
- **Findings the three agree on (headlines; the reports are the source, `.evidence/reviews/1/r0/<reviewer>/report.md`):**
  - `&#x20;` trailing space from rendered typing reaching the serializer — Claude 1 (blocker), Sol 6, Grok 1. This is the defect handoff 009 reserved for this reconciliation (mechanism in 009 Decisions; the BBEdit file's examination in #019). It is now a blocker, so the fix task is in the r1 chain, not the backlog; the PRD §6 portability reading (a trailing space at a paragraph end is not content) is the disposition to write.
  - Typing latency at 10k words versus the phase's 16 ms p95 check — Claude 7 (rendered view ~1 ms of margin, linear cost), Sol 5 (measured over budget), Claude 2 (source view re-parses the whole document per keystroke, 2.5× the budget). PRD §8 1.10 says "< 16 ms or a follow-up task is inserted at reconciliation": the follow-up task is required, in the r1 chain or as a Phase 2 task with a DECISIONS note saying which — decide, do not defer silently.
  - Dependencies not in PRD §4 — Claude 4 (`@lezer/highlight`, `@lezer/markdown` direct in `packages/editor`), Grok 2 (`codemirror` declared, never imported). CLAUDE.md: no dependency outside §4 without a DECISIONS note and Michael's OK — this needs Michael's answer (Open Questions) before the disposition is written.
  - Source-view undo/redo chords never exercised by a key event — Claude 5 (the 1.11 blind spot one layer over); `keymapOn` duplicated across the two test files — Grok 5; Playwright's `ControlOrMeta+Shift+z` spelling — Claude 6.
  - Sol 1–4: clipboard round-trip loses structure (no DOM parse rules in the schema), raw CodeMirror coordinates passed to the canonical map (`DevEditor.tsx:92`), character offsets ignoring escapes and continuation prefixes (`toggle.ts:233`), empty table cells unmapped (`positions.ts:353`) — each "Required" with a reproduction script under the reviewer's `/report/`.
  - Records and runner: Claude 8 (`ca1cd3d` `fix(runner)` on `phase/1` without a task record — it is the H1 fix, #review-0-r2; record the reading), Claude 9 and Grok 6 (the hand-written 1.9 record and its missing word count — #repair-1.9 already audits it; copying `payload` and the `a2.md` body into `#002-outline-feel` is this reconciliation's job), Claude 10–12 / Sol 7 / Grok 3–4 (nits: tautological count assertion, probe-span colour test, `App.tsx` `greet`, heading depth colours).
- The reconciliation worktree `.wt/1.10.r0d` is cut at `e9ed88b`; RUNNER-SPEC §8.1's close allowlist means only `docs/**`, `ralph/tasks.json` and `ralph/EXPECTED_COUNT` may change in it (plus the cherry-picked handoffs, which are `docs/**`). Fix tasks change code; the reconciliation only names them.
- `origin` holds the stale `ci/1.verify/a1` ref (Corrections). Containers never push and gates never commit; deleting it is a hand command for Michael, low priority: `git push origin --delete ci/1.verify/a1`.
- `1.close` rewiring on FAIL: in PRD §8 `1.close` depends on the review-set macro `1.10`; check how `0.close` was rewired at `c0f3c98` (`git show c0f3c98 -- docs/PRD.md | grep close`) before editing, and run `node ralph/generate-tasks.mjs --check && node ralph/validate-tasks.mjs && bash ralph/test/run.sh` (the last takes ~2 min, 83 tests at this tip).
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this shell (zsh) a bare `echo ===` aborts a compound command — quote separators.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael with the three verdict lines; ask Michael the dependency question (Open Questions) so the disposition of Claude 4 / Grok 2 can be written without a second round-trip.
2. Reconcile in `.wt/1.10.r0d` on `task/1.10.r0d`: `git cherry-pick handoff/010 handoff/011` first; copy the three reports to `docs/reviews/phase-1-r0-{claude,sol,grok}.md`; append the 1.9 record (`.evidence/human/1.9/accepted.json` `payload` fields and the `a2.md` body) to `DECISIONS.md#002-outline-feel`; write `DECISIONS.md#review-1-r0` with `verdict: FAIL` first, then per-finding dispositions (taken / deferred with revisit point / rejected with reason), the `&#x20;` disposition as a PRD §6 portability finding with its fix task, the latency follow-up decision, the dependency note (after Michael's answer), the `ca1cd3d` reading; re-point `#review-0-r2` H2 to the Phase 2/3 task that first calls `writeFrontMatter` (Claude 3 names `apps/desktop/src/dev/outline-document.ts:41` as a caller already — decide whether H2 is now a Phase 1 fix); fix tasks + `1.verify.r1` + `1.10.r1a/b/c/d` physically in PRD §8 and `ralph/tasks.json`, `1.close` rewired, `EXPECTED_COUNT` bumped; class-level lessons appended; `docs/progress.md` regenerated; `bash ralph/test/run.sh` green; journal line; commit `wip(1.10.r0d): …` with `<promise>DONE 1.10.r0d</promise>`; restart the runner; confirm `task(1.10.r0d)` integrates and the first fix task starts.
3. Fix chain runs (three attempts each before `STUCK`; re-cut per #016 with `chore(<id>): principal lesson …` on the task branch, `ralph/ralph.sh retry <id>`, restart). After any `docker/` commit integrates, rebuild the image before the next container task (#review-0-r2 deviation 34). `1.verify.r1` → `HUMAN_GATE 1.verify.r1h`: `cd ~/Developer/essaydown && scripts/gate.sh 1.verify.r1h`; rotate there regardless of context (handoff 012 on `handoff/012`).
4. `1.10.r1a/b/c` → `PRINCIPAL 1.10.r1d` by a fresh session → `1.close` → `/rotate` at the phase boundary (on `phase/2` from the host checkout) → `git branch -D handoff/010 handoff/011` (and `handoff/012`).
5. Next `chore(0.0)` idle-window commit (phase boundary): rotate.md step 5 third case + stale `description` line (009 Corrections); ask Michael to delete `ci/1.verify/a1` on origin.

## Open Questions

- **For Michael, before the disposition is written:** `@lezer/highlight`, `@lezer/markdown` (imported by `packages/editor/src/source.ts`) and `codemirror` (declared, unused) are outside PRD §4 as written. Options: (a) a DECISIONS note admitting the two `@lezer` packages as the lang-markdown highlighter's own surface and a fix task removing the unused `codemirror` kitchen-sink; (b) a fix task that reaches the highlighter through `@codemirror/lang-markdown`'s re-exports only, if they exist. The principal recommends (a), 70%.
- Whether the latency follow-up is a Phase 1 fix task (blocks `1.close`) or a Phase 2 task with a DECISIONS note; the PRD sentence allows either, the r1 chain is already long.
- Which task first calls `writeFrontMatter` for the H2 re-pointing (Claude 3 says `/dev/outline`'s Produce already does — that may make H2 a Phase 1 fix).
