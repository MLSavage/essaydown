# Handoff 028 — Phase 1 at `HUMAN_GATE 1.verify.r7h`: the r7 chain (1.49–1.54) integrated, the verifier passed, the gate open for Michael, the r7 review set next

Written 2026-09-21 by the principal (the session that ran the `1.10.r6d` reconciliation and drove the r7 chain through four principal decisions; Claude Code on the Mac Mini), at the `HUMAN_GATE 1.verify.r7h` / `ROTATE-PRINCIPAL` stop signal, per PRINCIPAL.md's cadence. Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `027-phase-1-r7-chain-1.51-stuck.md` (on `handoff/027`, not yet in the tree, with its eight corrections). This commit is on `handoff/028`, **stacked on `handoff/027`** because 027 is not in `phase/1`; `git log --oneline phase/1..handoff/028` shows fifteen commits (#030, 027's handoff, #031, #032, #033, #034, eight corrections of 027, this one) and the `1.10.r7d` reconciliation cherry-picks that one range. Next: `029-*.md`.

## Current State

- **`phase/1` tip `fd03e40`** (`task(1.verify.r7)`); 81 commits since the phase base `052e206`. `main` at `052e206`. `origin/phase/1` at `4590b8d` (Michael pushes by hand, #010). Host checkout on `phase/1`, tree clean, `doctor` clean.
- **The r7 chain, all passed**: 1.49 (a1, 38 turns, `6d78458`); 1.50 (a1 capped at 51 with the work done, a2 25, `faf6103`); 1.51 (a1 capped, a2/a3 withheld DONE on #030's conflict, retry a1 withheld on #030's bytes, a2 re-confirmed, a3 applied #031 — 19 turns, `7626696`); 1.52 (a1 capped, a2 withheld on the expected-fail contradiction, a3 applied #032 — 23 turns, `c902965`); 1.53 (a1 and a2 lost to the 429 session limit, a3 withheld on the 1.46 pin, retry a1 13 turns under #033, `b01c7fe`); 1.54 (three sonnet attempts capped at 51, the third mid-mutation, retry a1 under #034 with the mutated `format.ts` restored, `e28c273`); `1.verify.r7` (a1, 41 turns, `fd03e40`; the two promote bullets L1 and L3–L5 in CLAUDE.md/AGENTS.md).
- **Gate `1.verify.r7h`: `human-pending`**, nothing under `.evidence/ci/1.verify.r7h/` yet. Michael has the command: `scripts/gate.sh 1.verify.r7h` (his shell; it outruns the 120 s foreground limit — read his background task's output file, or `.evidence/ci/1.verify.r7h/` afterwards).
- **Tasks** (`ralph/ralph.sh status`): "Phase 1, 1.verify.r7h human-pending; last passed 1.verify.r7; gates open: 1.verify.r7h; plan requests: none; blocked: none." Graph 244 tasks.
- **Runner**: tmux `essaydown`, window `runner`, pane pid 8940, `zsh` at the prompt after `HUMAN_GATE 1.verify.r7h` / `ROTATE-PRINCIPAL` (runner.log lines 114,596–114,597; the log is 114,597 lines). `.locks/` empty, no `ralph run` process, no containers. Restart after ACCEPT: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's is stopped at rotation; **arm a fresh one after every restart or `retry`** — the `awk` dedup swallows a repeated `attempt 1` line):
  ```
  setopt nullglob; cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r7h`. The runner prints nothing at a review set's start; after the restart its next own line is `PRINCIPAL 1.10.r7d` (or `STUCK 1.10.r7` on a reviewer failure).
- **Expected next stops**: `ACCEPT` (or `GATE-FAILED`) from `scripts/gate.sh`; then the review set `1.10.r7a/b/c`; then `PRINCIPAL 1.10.r7d` (a fresh session, #015).

## Corrections

- **`PRINCIPAL 1.10.r7d` reached** (2026-09-22 05:21Z): the r7 review set passed at attempt 1 (started `04:54:40.171Z` / `.274Z` / `.378Z`, within 207 ms — Grok independent, #025), reports in `.evidence/reviews/1/r7/{claude,sol,grok}/report.md` (58 / 49 / 57 lines), all three `implementation_sha 5618078`. **Verdicts: Claude FAIL (2 blockers / 3 should-fix, 1 Required / 4 nits), Sol FAIL (1 blocker / 1 should-fix Required / 0), Grok PASS (0 / 0 / 0)** — Grok's PASS-with-blockers count becomes 3 of 5 counted attempts. Turns: Claude 128, Grok 26, Sol none. `1.10.r7d` is `principal-pending`, `.wt/1.10.r7d` on `task/1.10.r7d` at `5618078`; the runner is at the prompt; `.locks/` empty; this session's watcher is stopped. **The reconciliation is the next session's first job (#015).**
- **Headlines the reconciliation reproduces, never trusts** (from the reports' numbered items): Claude 1 **blocker** — 1.49's third `SPLIT_PAIR` form runs over the *joined* phrasing output, so the verbatim ASCII bytes `&#xD83D;&#xDE00;` inside an `inlineCode` or inline `html` child are rewritten to `&#x1F600;` (``format(parse("`&#xD83D;&#xDE00;`\n"))``); Claude 2 = Sol 1 **blocker** — a hard `break` before inline html is written as backslash-space (`container-phrasing.js:66–75` replaces the previous result's trailing line ending whatever node wrote it; Sol's route: load `alpha\\` + LF + `<i>beta</i>`, Copy → `alpha\ <i>beta</i>`); Claude 3 **should-fix Required** — task 1.54 was integrated from an attempt that never finished: `ralph/lib/integrate.mjs:189–192` `transcriptHasDone` is a substring search over the whole JSONL transcript and the principal's two `[1.54]` lesson lines carry the literal promise, echoed into the transcript when the agent read lessons.md — **verified by this session**: `.evidence/tasks/1.54/1.log` (the retry's a1) ends `error_max_turns` at 51 with zero assistant texts holding the promise, and `task(1.54)`'s three journal lines all read `In progress` (the suite on the candidate and the r7.g2h gate were the only guards; the tree is green, the record is the defect); Sol 2 **should-fix Required** — 1.51's `inlineCodeSpelling` assumes one UTF-16 unit per unit and misses the GFM table handler's pipe escaping inside a cell (`mdast-util-gfm-table/lib/index.js:291–298`; value `a|b` written `a\|b`, the length check fails); Claude 4 — the block-final `inline_code` end, the L6 tuple 1.53's rule does not reach (`inline_code` is not `inclusive: false`); Claude 5 — the essay-fixture destructive leg runs near vitest's 5 s default (5,051 ms once in the cold build); Claude 6–9 nits (the `link.peek` triple serialisation; a false comment in positions.ts about `text` having `peek`; **the #030 product backlog line was never written to V1.1-BACKLOG.md** — the principal's omission, to be written by the reconciliation; an upstream adjacent-emphasis case with a GFM literal).
- **Rules from this block for every principal**: never write the literal `<promise>DONE <id></promise>` into a lessons line, task text or any file an agent reads (say "print the DONE promise for <id>"); the boundary `fix(runner)` list gains `transcriptHasDone` reading only the assistant's own text blocks. The ten existing lessons lines that quote it belong to tasks already passed (1.47, 1.51–1.54); no live risk, and they are not edited (append-only).

- **Gate `1.verify.r7.g2h` ACCEPT at a1** (2026-09-22, run 35688068381, SHA `5618078`, 93/93 Playwright on all three runners; `1.56` `218b2a9` and `1.verify.r7.g2` `5618078` passed at attempt 1): the runner was restarted and the r7 review set `1.10.r7a/b/c` runs from `5618078` (the verifier is `1.verify.r7.g2`, gate `1.verify.r7.g2h`). **Expected next stop**: `PRINCIPAL 1.10.r7d` (or `STUCK 1.10.r7` on a reviewer failure). The reconciliation is a fresh session: it cherry-picks `phase/1..handoff/028` (the `docs/DECISIONS.md` tail conflicts — #035 and #036 in the tree, #030–#034 on the branch: keep both, #030–#034 after #036), copies the reports, reproduces every blocker and Required finding in a detached scratch worktree at `5618078`, and on FAIL appends the r8 chain after running the corpus legs over every fixture and scope it decides.

- **Second repair round** (2026-09-22): `1.55` passed (`c5f82af`), `1.verify.r7.g1` passed (`5a39abd`), its gate `1.verify.r7.g1h` GATE-FAILED at a1 (run 35685069545) on windows-latest alone — G4 again, offset 11 received after five rights from an asserted 0: the native `ArrowUp` reverted by ProseMirror before the next key (the `[1.46]` class), the third arrow route to fail at that one position. Planning commit `plan.1.verify.r7.g1h.r0` (`2ad9a88`, DECISIONS **#036 in the tree**): `1.56` (sonnet; G4 placed by a click at the measured end of the first line, 1.55's text-node discriminator kept as the proof) → `1.verify.r7.g2` (needsCI, `ci/1.verify.r7.g2/a{n}`) → gate `1.verify.r7.g2h`; `1.10.r7a/b/c` rewired to it; `EXPECTED_COUNT` 250; the runner restarted to integrate it and run 1.56. **Expected next stops**: `[ralph] 1.56 attempt 1`, `1.verify.r7.g2`, `HUMAN_GATE 1.verify.r7.g2h` (the gate command becomes `scripts/gate.sh 1.verify.r7.g2h`). The `1.10.r7d` cherry-pick conflict note now covers #035 **and** #036 in the tree before #030–#034. This is the fourth CI round on one case; if the click route fails too, the next planning commit reads the received offset and the discriminator's value first — a received 6 with `alpha \nbeta gamma` typed correctly means the offset assertion is the only wrong instrument and goes.

- **The gate ran and failed** (after this file was first written): `1.verify.r7h` GATE-FAILED at a1 (ci.yml run 35629907462), windows-latest 91/93 on e2e/web — 1.49's spec importing core in the page through a Vite `/@fs` URL built from a Windows path, and G4's `ArrowLeft` across the soft line break never moving the caret (1.54's poll named the step); ubuntu and macos 93/93. The principal wrote the planning commit `plan.1.verify.r7h.r0` (`5d6ef0b` on `plan/1.verify.r7h/r0`, DECISIONS **#035 in the tree**): `1.55` (opus; the two spec repairs) → `1.verify.r7.g1` (needsCI, `ci/1.verify.r7.g1/a{n}`) → its gate `1.verify.r7.g1h`; `1.10.r7a/b/c` rewired to it; `EXPECTED_COUNT` 247; the runner restarted to integrate it and run 1.55. **Expected next stops**: `[ralph] 1.55 attempt 1`, `1.verify.r7.g1`, then `HUMAN_GATE 1.verify.r7.g1h` (rotate there; the gate command becomes `scripts/gate.sh 1.verify.r7.g1h`). **For `1.10.r7d`**: #035 is in the tree and #030–#034 are on this branch, so `git cherry-pick phase/1..handoff/028` conflicts at the tail of `docs/DECISIONS.md` — keep both, #030–#034 after #035 (the entries carry their dates). Current State's gate line and Next Steps 1–2 are superseded by this correction; the watcher marker is now the last `[ralph]` line.

- `027` Current State and every one of its eight corrections: superseded — the chain is done; the per-task record is above. `027`'s "eleven/thirteen/fourteen commits" counts: fifteen with this one.
- `026` Decisions, "26 of 58 `loop` tasks": 26 of 57 (counted at #review-1-r6); with the r7 chain, Phase 1's `loop` tasks needing more than one attempt are 31 of 63 (1.50, 1.51, 1.52, 1.53, 1.54 added), `NO-JOURNAL` stops 16 (1.53 a2 and 1.54 a2 added; four of the sixteen are 429s: 1.41, 1.45, 1.53 twice), `STUCK` 10 (1.51, 1.53, 1.54 added).
- `026`'s `next-prompt.md` job 5, "`git branch -D handoff/010 … handoff/026`": through `handoff/028`.

## Decisions

- **`#review-1-r6`** (in the tree, `695fb0f`): verdict FAIL; L1–L11; the r7 chain; the reviewer record.
- **`#030`–`#034`** (on `handoff/027`, not in the tree until `1.10.r7d` cherry-picks): four task-text conflicts the reconciliation wrote and one orientation — #030/#031 the `inline-html.md` fixture (a soft break before inline html fires the one serializer rewrite that changes a text value; the fixture holds inline html without it, bytes `alpha <i>beta</i> gamma`, `*ab* <b>x</b> yz`, `<span>x</span> gamma`) and the trailing-atom block end as 1.52's member; #032 the code-block interior as a positive named exclusion of the corpus inverse leg (no `it.fails`; the chain's "no expected fail" tails hold); #033 the 1.46 pin at a run's end yields to the `$pos.marks()` rule (one line); #034 the press helper's two forms (differs-per-step for counted horizontal arrows; settle-then-anchor for the four vertical sites). Each carries its reversal.
- **Runner deviations this block**: three `retry` (1.51, 1.53, 1.54) with the lesson-and-logs-aside shape; two 429 stops (1.53 a1 at 39 turns, a2 at 1 — #027's third and fourth instances; Michael named the reset, the plain restart followed); one `NO-JOURNAL` from a sonnet cap without a stub (1.54 a2). No `admin`, no `docker/` change, no `fix(runner)`. **New hazard, recorded for the boundary**: the runner's `wip(<id>): recovery of uncommitted changes` after a capped attempt swept a mid-mutation `packages/core/src/format.ts` onto `task/1.54` (`b175e87`); the retry restored it from the branch base and `git diff b01c7fe phase/1 --stat -- 'packages/*/src' fixtures` is empty at `e28c273` — a `fix(runner)` candidate (the recovery commit flags a diff under `packages/*/src` for a test-only task).
- **Michael's usage question (2026-09-21), answered with the turn table**: of 16 opus attempts in the chain, ~6 were work, 6 re-confirmed conflicts the reconciliation wrote, 3 capped in the verification tail after the work, 2 were the limit. The model split (opus for source fixes, sonnet for test-only) stands on the r5–r7 evidence; the two levers are the reconciliation prototyping every fixture and scope in a scratch worktree before writing task text (#033's lesson) and the 50 → 80 cap raise for opus fix tasks (four of five capped at 51 with the work done this chain) — **conditional on Michael's OK, not decided**; the principal also cut its own interim status checks to watcher events only.
- **Reviewer facts for r7**: the r7 rows carry the r6 dispositions, the two deferrals (L9, L11) and the r6 class to search beyond; they do **not** name `handoff/028`, so the reviewers will find #030–#034 absent from the tree at `fd03e40` (K3's class) — expected, not a defect; a reviewer reads the named ref (lesson [1.43] 15:48) and the reconciliation cherry-picks. Grok reviews r7 unless a 402 stops it (`STUCK 1.10.r7`, #025's manual procedure, Michael decides; never retried on the principal's initiative).

## Gotchas

- **`handoff/028` is stacked on `handoff/027`**: the reconciliation cherry-picks `phase/1..handoff/028` once; never cherry-pick `handoff/027` separately. The temporary worktrees for both live in the session scratchpad, not `.wt/handoff-NNN`.
- **A windows-only single-case failure at this gate is not automatically the known transient**: 1.54 rewrote every counted-press helper, so a failure in any spec 1.54 touched is a change under #023's criterion ("spec unchanged") — read the case; a repair is `plan.1.verify.r7h.r0` → `1.verify.r7.g1`, scoped to what failed (#019, #021, #022, #024). Only a failure in an untouched spec with no editor runtime change is the #023/#029 rerun-and-abandon.
- **The watcher dedups whole lines** (memory `project-watcher-dedup-after-retry`); arm a fresh one after every restart.
- **Before any `retry`**: `git -C .wt/<id> diff <branch-base> --stat -- 'packages/*/src' fixtures` — a non-empty result on a test-only task is a swept mutation (memory `project-recovery-commit-can-carry-a-mutation`).
- In this zsh a bare `echo ===` aborts a compound command and an unmatched glob aborts a script — quote separators, `setopt nullglob` in watchers; `grep -- '- [id]'` needs the `--`. `scripts/gate.sh` outruns the 120 s foreground limit; Michael runs it in his shell.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence.

## Next Steps

1. **The gate**: Michael runs `scripts/gate.sh 1.verify.r7h`; read `.evidence/ci/1.verify.r7h/` (`accepted.json` on ACCEPT; on GATE-FAILED `a1/result.json`, `run.json`, `workflow.log`) and give him the one-line result. GATE-FAILED → read the failing case before choosing between the rerun (#023/#029, untouched spec, transient) and the repair (`ralph/ralph.sh plan plan.1.verify.r7h.r0`, a `1.verify.r7.g1` chain scoped to what failed).
2. **On ACCEPT**: idle checks (`.locks/` empty, `pgrep -f 'ralph/lib/cli.mjs ralph run'` empty, `docker ps` empty, the tmux pane at the prompt, `doctor` clean), the restart, a fresh watcher; the review set starts the three reviewers together (`started_at` within 200 ms in `.evidence/state/tasks.json` — #025's independence condition); `STUCK 1.10.r7` on a 402 → #025's manual procedure, Michael's call.
3. **`PRINCIPAL 1.10.r7d`**: `/rotate`; the reconciliation is a fresh session (#015); it cherry-picks `phase/1..handoff/028`, copies the reports, reproduces every blocker and Required finding in a detached scratch worktree at `fd03e40`, writes `#review-1-r7` (K1–K9 and L1–L11 each confirmed or reopened; #030–#034 named under their tasks), and on FAIL appends the r8 chain — **after running the corpus legs over every fixture and scope it decides, in the scratch worktree, before writing the task texts** (#030–#033's lesson), with acceptance tails that name file-level greps and never the suite's total (#032's lesson). The FAIL pattern is `task(1.10.r6d)` at `695fb0f`; the PASS pattern `task(0.12.r2d)` at `052e206`.
4. **Phase boundary idle window** (only after `1.close`, host checkout on `phase/2`, runner idle): `#025`'s `fix(runner)`; `#023`'s (two instances, #029); H7's; the recovery-commit guard above; `chore(0.0)` for rotate.md step 5 (the stacked-handoff rule's "already in the tree" condition, the scratchpad worktree, the fresh-watcher rule), the stale `description` line, PRINCIPAL.md's doctor sentence and PRINCIPAL.md gaining Sol's r4 lesson, #028's, #030–#034's, the r5 and r6 principal lessons; `git branch -D handoff/010 … handoff/028`; ask Michael for `scripts/gate.sh gc` (after `git ls-remote origin 'refs/heads/ci/*'`) and the `phase/1` push (#010); on Michael's OK only: the turn-budget raise (evidence: #review-1-r5, #review-1-r6, the r7 chain's turn table in the usage answer above), the 429 signal (#027, four instances), the gate fetch-on-failure (#review-1-r3 I7), the reviewer-prompt option.

## Open Questions

- Whether the gate passes on windows at a1 now that every counted press is synchronised (1.54), and whether the r7 reviewers accept the deferrals L9 and L11 and the #032 exclusion.
- The cap raise and the 429 signal: Michael's calls, evidence unchanged and now larger.
- Whether Grok reviews r7 (the usage budget; a 402 is Michael's decision).
