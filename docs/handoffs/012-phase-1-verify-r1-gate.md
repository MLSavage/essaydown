# Handoff 012 — Phase 1 at `HUMAN_GATE 1.verify.r1h`: review r0 reconciled FAIL, fix chain 1.13–1.22 passed, verifier `1.verify.r1` integrated, gate open

Written 2026-09-09 by the principal (Claude Code, Remote Control session in tmux `essaydown`, window 0 `principal`, on the Mac Mini), at the `HUMAN_GATE 1.verify.r1h` rotation (rotate at every verifier gate regardless of context; DECISIONS #015). Role, reading order and rules: `docs/PRINCIPAL.md`. Supersedes `011-phase-1-review-r0-reconciliation-pending.md` (now in the tree at `b269627`). This commit is on `handoff/012` (rotate.md step 5: a verifier gate is open and a review attempt is pending, so never on `phase/1`); the `1.10.r1d` reconciliation cherry-picks `handoff/012` (010 and 011 are already in `phase/1`). Next: `013-*.md`.

## Current State

- **`phase/1` tip `91a3de5`** (`task(1.verify.r1)`, attempt 1); 31 commits since the phase base `052e206`. Since handoff 011: `b269627` (`task(1.10.r0d)`, the reconciliation, verdict FAIL, `.evidence/reviews/1/r0/verdict` = `FAIL`, `reconciliation_sha b269627`), then the fix chain — `4386477` 1.13 a1, `14137c9` 1.14 a2, `31ad497` 1.15 a2, `9fc8f52` 1.16 a2, `9fd6a81` 1.17 a2, `41d1e7c` 1.18 a1, `f7a83d4` 1.19 a1, `2b1c29b` 1.20 a1, `0fb6313` 1.21 a3, `d007631` 1.22 a1 — then `91a3de5`. `main` unchanged at `052e206`. `origin/phase/1` is at `4590b8d` (Michael pushes by hand, #010); `origin` still holds the stale `ci/1.verify/a1` at `3e98707`.
- **Tasks** (`.evidence/state`): 1.1–1.9, 1.verify, 1.11, 1.12, 1.verify.g1, 1.verify.g1h, 1.10.r0a/b/c/d passed (1.verifyh superseded); 1.13–1.22 passed with the attempt numbers above; `1.verify.r1` passed a1 (`integrated_sha 91a3de5`); **`1.verify.r1h` human-pending, attempts 0** — the open gate; `1.10.r1a/b/c/d`, `1.close` pending. Graph 170 tasks, `doctor` clean, host checkout on `phase/1` at `91a3de5`, tree clean, `.wt/` empty apart from this rotation's temporary worktree (removed by it), branches `handoff/010`, `handoff/011`, `handoff/012`.
- **Runner**: tmux `essaydown`, window 1 `runner`, pane pid 8940, `zsh` at the prompt after `stop-check: GREEN` / `HUMAN_GATE 1.verify.r1h` / `ROTATE-PRINCIPAL`. `.locks/` empty, no `ralph run` process. Restart after the gate's ACCEPT: `tmux send-keys -t essaydown:runner 'ralph/ralph.sh run --phase 1 2>&1 | tee -a .evidence/runner.log' Enter`.
- **Watcher** (`Monitor` tool, persistent, 30 s poll; this session's watcher is stopped at rotation):
  ```
  cd /Users/mlsavage/Developer/essaydown; start=$(($(wc -l < .evidence/runner.log)+1)); while true; do tail -n +"$start" .evidence/runner.log | grep -E "^(HUMAN_GATE|STUCK|NO-JOURNAL|NO-COMMIT|INTEGRATION-FAILED|CONFLICT|DOCTOR|ITERATIONS|PRINCIPAL|REPLAN|PLAN-GATE|STALE-CHECKOUT|REVIEW-SHA-MISMATCH|CLOSED|GATE-FAILED|ROTATE-PRINCIPAL) |^<promise>COMPLETE|^\[ralph\] "; sleep 30; done 2>/dev/null | awk '!seen[$0]++ { print; fflush() }'
  ```
  Current marker: `HUMAN_GATE 1.verify.r1h` (log at 53,122 lines).
- **Expected next stop**: Michael runs `scripts/gate.sh 1.verify.r1h` (ci.yml on `ci/1.verify.r1/a1` at `91a3de5`; artifacts `test-logs` and `playwright-report`, three OS directories each; the run now includes the 1.17 latency spec). On `ACCEPT` and restart the runner starts `1.10.r1a/b/c` in one container run each (`[ralph] 1.10.r1a attempt 1 …`) and stops at `PRINCIPAL 1.10.r1d` (a fresh session; the session that watches the reviews rotates there). On `GATE-FAILED`: `PLAN-GATE plan.1.verify.r1h.r0` → the plan-request path of #020.

## Corrections

- `011` Open Questions, first item (the dependencies) — answered by Michael, option (a) with two additions: declaring the two `@lezer` packages is the correct declaration, not a compromise; `codemirror` had to go because it put `@codemirror/commands` (history) into the lockfile and falsified the two safety comments (1.19 re-verified them after the removal and gave the property a test); and the rule needed an enforcement point (1.20: `scripts/check-deps.mjs` in the root `lint` script, allowlist `docs/dependencies.json`). All recorded in DECISIONS #review-1-r0 F7/F8.
- `011` Open Questions, second item (latency follow-up) — decided: a Phase 1 task (1.17), not Phase 2. Third item (H2 re-pointing) — decided: a Phase 1 task (1.18); the trigger had fired at 1.8.
- `011` Next Steps 4 and the task prompt said the `1.10.r1d` reconciliation cherry-picks `handoff/010`, `handoff/011` and `handoff/012`; 010 and 011 are in `phase/1` since `b269627`, so **only `handoff/012` is cherry-picked** (a second cherry-pick of 010/011 would be empty or conflict).
- `011` Gotchas said the runner suite is 83 tests — still 83 at `b269627` and at every fix task's integration (1.20 added vitest tests, not runner tests).

## Decisions

- **DECISIONS #review-1-r0** (`b269627`): verdict FAIL; 16 findings F1–F16 with dispositions; F1 the `&#x20;` blocker disposed as a PRD §6 portability defect fixed at the editor (1.13) with the `editor fixed point` corpus leg; F2–F4 Sol's required findings (1.14 parseDOM, 1.15 empty table cells, 1.16 cursor coordinates); F5 the PRD-mandated latency follow-up as a Phase 1 task (1.17); F6 H2/H9 (1.18); F7 Michael's dependency decision (1.19 + PRD §4 sentence); F8 the §4 enforcement task (1.20); F9 assertions that cannot fail (1.21); F10 H3/H5 test-only (1.22); F11–F16 nits recorded/deferred/taken. The reviewer record: no vote, verdicts are not the signal; Phase 0 counts Claude 28 / Sol 14 / Grok 14; eleven shared findings compared for severity, no reviewer consistently low, no Grok change (Michael declined the cold-build experiment: r1 is not an A/B, and the read-only vantage is a feature); post.1 now tabulates unique findings per reviewer and shared-finding severity, never verdicts; `1.10.r1c`'s text asks Grok to explain, per Claude/Sol r0 finding, whether a read-only review could have found it. **#002-outline-feel**: the 1.9 payload and record body verbatim with provenance. RUNNER-SPEC §2: hand-written human records carry a `provenance:` line. One rule promoted via `1.verify.r1` (round-trip families seeded from the writing surface's own output; now in CLAUDE.md/AGENTS.md at `91a3de5`).
- **Runner behaviour this block (no deviation, nothing added).** The reconciliation integrated as `task(1.10.r0d)` and `sync-state` recorded the verdict (`run.mjs` `verdictFromDecisions` read `verdict: FAIL` from the section). Five of the eleven loop tasks hit the 50-turn cap on attempt 1 with their implementation committed and no journal entry (`NO-JOURNAL 1.14`, `1.15`, `1.16`, `1.17`, `1.21`; each `num_turns 51`, `max_turns` in the transcript); each restart (`ralph.sh run` again) ran attempt 2, which wrote the journal and passed — the same shape as 1.3/1.5/1.7/1.8 earlier in the phase. 1.21 attempt 2 hit the cap again *with* the journal committed (it was re-running the e2e suite before committing the DONE promise), so the runner retried to attempt 3 without a stop signal (a `NO-JOURNAL` is emitted only when no journal entry exists; a cap-hit with a journal is a plain failed attempt) and attempt 3 passed. No `STUCK`, no re-cut, no `INTEGRATION-FAILED`. Cherry-picking `handoff/010` then `handoff/011` into the reconciliation worktree conflicted on `docs/handoffs/next-prompt.md` only (both rotations rewrote it from different bases); resolved to the 011 version, recorded in #review-1-r0.
- **Rotation decision.** `HUMAN_GATE 1.verify.r1h` is a verifier gate: rotate regardless of context (#015), before the gate is run, so the session that runs the gate and watches `1.10.r1a/b/c` is fresh and rotates again at `PRINCIPAL 1.10.r1d`.

## Gotchas

- **The gate command** is `cd ~/Developer/essaydown && scripts/gate.sh 1.verify.r1h` (Michael, on the host; it pushes `ci/1.verify.r1/a1` at `91a3de5`, waits, fetches both artifacts, prints `ACCEPT` or `GATE-FAILED`, and `ROTATE-PRINCIPAL`; the rotation is already done, so no second rotation at ACCEPT). The run is the first to execute `e2e/web/latency.spec.ts` on three runners: its assertion is a shape (p95 at 10k words within 25× of p95 at 1k words, per view), so it should not flake, but a failure of that spec on one OS is a code-caused failure like any other → `.g1` repair, never a rerun. On `GATE-FAILED` read the failing job's step first (`gh run view <run_id> --json jobs`; the log is `.evidence/ci/1.verify.r1h/a1/workflow.log`), then `ralph/ralph.sh plan plan.1.verify.r1h.r0`, a planning commit with the fix task(s) and `1.verify.r1.g1` the way `2418bc7` did (#020), DONE promise, restart.
- **On ACCEPT**, after the restart: `.evidence/reviews/1/r1/verifier_id` must read `1.verify.r1` and both SHAs equal `91a3de5`; `docker top` of the claude-review container shows `--max-turns 160`. No `docker/` commit is in the chain, so **no image rebuild is due** before r1. Reviewers cannot see one another's directories (F15 backlog still stands as recorded).
- **Turn budget, for the `1.10.r1d` reconciliation to decide, not mid-chain**: in Phase 1, 9 of the 14 loop tasks needed a second attempt because attempt 1 hit `RALPH_MAX_TURNS` 50 (compose `x-env` default; reviewers have their own 160). Every one of those first attempts had committed a complete implementation; the cost is one restart each and one wasted container run of ~10–15 min. Options at the reconciliation: raise the loop default in `docker/compose.yml` (a `docker/` commit → rebuild before the next container task), or split large opus tasks. Record the count from `.evidence/state` at the time of writing, not from here.
- **1.17's factor**: the committed shape assertion allows p95 at 10k words up to 25× p95 at 1k words; the container numbers were rendered 2 → 14 ms and source pane 3 → 27 ms, so it holds by a wide margin, and 25× is loose enough to be nearly unfalsifiable. The r1 reviewers read the spec; the reconciliation decides whether the factor is tightened (a code change → r2 chain, only if FAIL for other reasons) or recorded.
- **`docs/dependencies.json` is live**: `pnpm lint` fails on any direct dependency without an entry naming its PRD §4 row or DECISIONS id, and on a stale entry. A fix or plan task that adds a package must add the entry in the same diff (the check's error names the sentence to add).
- **1.21's test rewrite**: the redo presses are now `ControlOrMeta+Shift+Z` with one lowercase case kept; the source-view chord e2e is green in the container only — the gate is the first macOS/Windows evidence for it.
- `origin` still holds `ci/1.verify/a1`: `gate.sh gc` (RUNNER-SPEC §2) or `git push origin --delete ci/1.verify/a1` by Michael at the phase boundary, low priority.
- Harness memory pressure: `ralph/ralph.sh status` on every wake before trusting silence. In this shell (zsh) a bare `echo ===` aborts a compound command — quote separators. A `NO-JOURNAL` stop is restarted with the same run command after `.locks/` empty, no `ralph run` process, `doctor` clean and the tmux pane at the prompt.

## Next Steps

1. Fresh session: `ralph/ralph.sh status` / `doctor`; arm the watcher; one-line state to Michael; hand him `cd ~/Developer/essaydown && scripts/gate.sh 1.verify.r1h`.
2. On `ACCEPT`: restart the runner; confirm `1.10.r1a/b/c` start (`verifier_id 1.verify.r1`, SHAs `91a3de5`, `--max-turns 160`); wait for `PRINCIPAL 1.10.r1d`; rotate there (`/rotate`, handoff 013 on `handoff/013`) — the reconciliation is a fresh session.
3. `1.10.r1d` reconciliation (fresh session): `git cherry-pick handoff/012` (and `handoff/013`) into `.wt/1.10.r1d` first; reports to `docs/reviews/phase-1-r1-{claude,sol,grok}.md`; `DECISIONS.md#review-1-r1` with `verdict: PASS|FAIL` first; on PASS `docs/progress.md` regenerated, `wip(1.10.r1d): …` with the DONE promise, restart → `1.close` → `/rotate` at the phase boundary (on `phase/2` from the host checkout) → `git branch -D handoff/010 handoff/011 handoff/012 handoff/013`; ask Michael to delete `ci/1.verify/a1` on origin. On FAIL: the r2 chain, the way `b269627` did it.
4. On `GATE-FAILED`: Gotchas, first bullet.
5. Phase boundary idle window: the `chore(0.0)` commit for rotate.md step 5 third case + the stale `description` line (009 Corrections); post.1's inputs now include the per-phase unique-findings table (PRD §8 post.1).

## Open Questions

- Whether the loop-task turn budget (`RALPH_MAX_TURNS` 50) rises, and to what, or whether large opus tasks are split at planning time — for the `1.10.r1d` reconciliation with Michael, with the attempt counts read from `.evidence/state` then.
- Whether 1.17's 25× factor is tightened or recorded — after the r1 reports.
