# V1.1 backlog — nits and deferred findings (PRD §8 severity rule; RUNNER-SPEC §5.4)

Append-only. Each line: origin (review attempt, finding), what, why deferred, when to revisit.

- [review-0-r0 F5, Claude 4] `eslint.config.js` ignores `ralph/**`; the runner is unlinted. Deferred: not a gate criterion, unbounded runner edit. Revisit: first Phase 1 stop signal with the runner idle (a `fix(runner)` commit + conformance suite green).
- [review-0-r0 F14, Sol 1] A second `ralph.sh run` can launch the same task in the same worktree. Manual procedure in DECISIONS #review-0-r0; a `run` pidfile guard is v1.1 runner work. Revisit: v1.1 runner.
- [review-0-r0 F15, Sol 7] Reviewers can read peer report directories via the read-only `/logs` and host-path mounts (RUNNER-SPEC §5.2). Deferred: needs compose surgery on the bind worktrees depend on. Revisit: Phase 1 review set start.
- [review-0-r0 F16, Sol 9] `blocks.ts`/`sentences.ts` deep-clone the root per edit; no structural sharing reaches undo; `undo.ts:19` comment overstates. Deferred: correctness holds; memory matters with Phase 3 modes. Revisit: Phase 3 reconciliation (measure 500 snapshots of the essay fixture).
- [review-0-r0 F17, Claude 8] Nothing detects a reviewer/task image older than the committed `docker/` tree. Manual procedure (rebuild after any `docker/` change). Revisit: v1.1 runner (baked digest check).
- [review-0-r0 F18, Grok 7] `ralph/generate-tasks.mjs` gate-description string and `0.verify`'s ciAcceptance say `/logs/ci/<producer>/…`; the convention is the gate id (#018). Deferred: changing them mutates passed-task fingerprints. Revisit: v1.1 runner (fingerprint rule that ignores description text of gates).
- [review-0-r0 F11, Claude 12] `apps/desktop/src/App.tsx` is the scaffold's welcome page with vendor logos. Deferred to Phase 1's shell task, which replaces it.
- [review-0-r0, Claude 10] Global coverage floors are set by 0.16 at the measured values; watch for decay at each reconciliation (record the numbers in progress.md metrics).
- [review-0-r1 G10, Claude 5] `apps/desktop/src/App.tsx` still invokes the `greet` IPC command that 0.21 removed, so the dev route's one control rejects at runtime; no Phase 0 acceptance clicks anything. Phase 1's shell task replaces the page (not a restyle). Revisit: the Phase 1 shell task.
- [review-0-r1 (b), handoff 005] Nothing checks "is this package in PRD §4?" at install time (0.10's `zod`, found at r0 three tasks later, ratified in #review-0-r1). If a check belongs anywhere it is the existing bar scripts (a lockfile-vs-§4 diff), not new machinery. Revisit: v1.1 bar scripts.
- [review-0-r1 (c), deviation 27] `sync-state` rewrites a raw dependency to a superseded gate that `state.mjs` treats as satisfied for everyone; RUNNER-SPEC says only for the fix tasks appended after it. Harmless while every superseded gate's dependents are exactly those fix tasks. Revisit: the first planning commit whose superseded gate has another dependent.
- [review-0-r1 (32), Sol] cargo is absent from the reviewer container's login-shell PATH (Sol used the pinned toolchain directory); Claude's cold build did not report it. Revisit: the Phase 1 image check after the next `docker/` change.
- [review-0-r2 H1, Claude 1] `ralph/lib/util.mjs` `lockHolder` reports an unreadable or non-integer lock file as a dead holder, so `withLock` and `doctor` attach the manual-removal instruction to a lock that may be live (reachable in the zero-byte window inside `withLock` itself). Deferred: fail-closed, the repair's `pgrep` step guards the operator, runner code is outside the PASS allowlist. Revisit: first Phase 1 idle window, one `fix(runner)` commit on phase/1 (unidentifiable holder → wait/timeout path; `doctor` `unreadable-lock`; one `util.test.mjs` leg) on Michael's OK; else the Phase 1 reconciliation.
- [review-0-r2 H2, Claude 2 + Grok risk 2] `needsQuoting` (`sidecar.ts`) ignores YAML implicit type resolution: the strings `true`/`no`/`null`/`~`/`123`/`1.5`/`.inf`/`2026-01-01` are written bare and a third-party YAML reader takes them back typed; the app's reader and invariant A hold. Also: a Phase 1 save path must not treat `ok: false` on a quoted-key spelling (G1) as a whole-save abort — skip keys whose `readFrontMatter` entry is not writable. Deferred: not a §7 criterion; first third-party reader is Phase 4 export. Revisit: the Phase 1 task that first calls `writeFrontMatter` from the shell (core-schema resolvers as one clause of `needsQuoting`; value table at `front-matter-boundary.test.ts:231`); hard stop before 4.verify.
- [review-0-r2 H3, Claude 3] The promoted "every f(root, …) → root has a corpus-wide identity test" rule is discharged only for `reorderSentences`, `replaceSentence`, `writeFrontMatter`; `moveBlock(root,i,i)`, `moveSection(root,i,i)`, `replaceBlock(root,id,ownNode)` are byte-identical over all 53 fixtures today but have synthetic tests only. Deferred: missing guard, no bug. Revisit: the first Phase 1 task editing `packages/core/src/blocks.ts`, else the Phase 1 reconciliation appends a test-only task.
- [review-0-r2 H5, Claude 5] `fixtures-corpus.test.ts:116` builds the node-type union from `index.json` metadata, so the corpus-coverage claim certifies itself; true today (all 53 parsed). Revisit: with H3 (build the union from parsed trees; assert each index entry's `nodeTypes` against its tree).
- [review-0-r2 H7, Claude 7] `setAuditLog` in `ralph/lib/util.mjs` is module-level state shared by every `Ctx`; `writeAtomic` tests pass by construction order. Revisit: v1.1 runner (pass the sink explicitly or key by root).
- [review-0-r2 H8, Claude 8] `ralph/lib/close.mjs` allowlist entry `/^DECISIONS\.md$/` can never match (file is `docs/DECISIONS.md`); RUNNER-SPEC §8.1 spells the same list. Harmless. Revisit: v1.1 runner, strike both together.
- [review-0-r2 H9, Grok 1] `front-matter-boundary.test.ts:203` returns early on `ok: false`, so a regression from "escaped" to "refused" for the 21 UNWRITABLE rows stays green. Revisit: with H2 (assert `ok: true` per row before the invariant-A check).
- [review-0-r2 H1] fixed at `ca1cd3d` (`fix(runner): an unreadable lock file is an unidentifiable holder …`), Michael's OK, 1.9 idle window; the line above stays as history.
- [repair-1.9 (a)] `doctor.mjs:102` repairs a non-CI human gate with `scripts/gate.sh <id> --resume a<n>`, which only `ciGate` implements; `humanGate` ignores `--resume` and `requireEligible` refuses a passed gate. Revisit: v1.1 runner — either implement the human-gate resume (record on disk → transition only) or print the manual procedure of DECISIONS #repair-1.9; one conformance leg for the human case.
- [repair-1.9 (b)] `gate.sh` accepted a record whose payload and body were the prepared command's own `<…>` placeholders. Revisit: v1.1 runner — refuse a `--payload` value or `--note`/`--file` body that is a single angle-bracketed placeholder; one conformance leg. Until then: the principal never hands Michael a gate command with placeholder text (ask for the text first).
- [repair-1.9 (c)] Observation gates that check copied Markdown name one reader (1.9: Typora). Revisit: the next observation gate's procedure names two readers with different engines (BBEdit ships both cmark and Markdown.pl); PRD §8 text change at planning time, not runner machinery. **Asked of Michael at #review-1-r8 (N4, Claude's r8 finding 2):** the `1.9` record was taken at `ca1cd3d` on a tree whose round-trip it reports broken, and no human has driven the fixed tree; the two §7 clauses are evidenced by machine (the three-OS gate, the reviewers' browser routes and corpus families). On Michael's yes, the r9 reconciliation appends a `1.9.r1` approval retry at the r9 implementation SHA with this line's two-reader procedure; on no, the line's trigger stands as written.
- [#019] `ciGate` records `{name, sha256, bytes}` per artifact in `run.json`/`accepted.json`; unused. Revisit: v1.1 runner — an empty `playwright-report` is visible from `bytes` today; only if a real case ever needs it, never as the pass condition.
- [review-0-r1 (b)] discharged by 1.20 (`scripts/check-deps.mjs` in the root `lint` script, allowlist `docs/dependencies.json`; DECISIONS #review-1-r0 F8); the line above stays as history.
- [review-0-r2 H2, H9] discharged by 1.18 (core-schema implicit resolvers in `needsQuoting`; `ok: true` per row); the revisit trigger fired at 1.8 unnoticed — see the lesson in #review-1-r0.
- [review-0-r2 H3, H5] discharged by 1.22 (test-only: corpus identity for `moveBlock`/`moveSection`/`replaceBlock`; node-type union from parsed trees).
- [review-1-r0 F11, Claude 12 + Grok 4] `apps/desktop/src/App.tsx` (scaffold page, `invoke("greet")` that 0.21 removed): the two lines above say "Phase 1's shell task", which does not exist — Phase 1 has no shell task. Re-pointed: the first Phase 2 task that gives `/` a real page (2.4, the file-tree sidebar shell, or whichever Phase 2 task first edits `App.tsx`/the `main.tsx` route table) replaces the page; "replaces" means the route table's fall-through to `App` renders the shell, not a restyle. Hard stop: `2.verify`.
- [review-1-r0, Grok 5] `keymapOn` is duplicated verbatim in `packages/editor/test/store.test.ts` and `toggle.test.ts` (the 1.11 journal already names the lift). Revisit: lift it to one helper beside `typing.ts` when a third file needs it; not before (a helper with two callers is not a class).
- [review-1-r0, Sol 7] Task 1.5's description says headings are colour-coded "by depth"; `source.ts` gives every ATX/Setext depth one `headingTag` and one colour, and `source.test.ts` asserts them alike. Deferred: the 1.5 acceptance names seven distinct classes, not depths, and Michael's 1.9 record raises no heading-colour complaint. Revisit: the Phase 2 shell's first source-view visual pass decides depth colours or strikes "by depth" from the wording (planning-time PRD text change).
- [review-1-r0, Sol gate table + Grok 6] Human observation gates that name a size ("~1,000 words") validate presence, not the count. Revisit: the next observation gate's procedure (2.9) puts the count, or "not counted", in the payload the reconciliation copies; PRD §8 text change at Phase 2 planning, no runner machinery.
- [review-1-r0, Michael] Reviewer prompt severity sentence (`docker/entrypoints/essaydown-common.sh` `review_prompt`): only if the shared-finding severity comparison (DECISIONS #review-1-r0, repeated by post.1 per phase) shows one reviewer consistently rating shared findings lower; a docker/ change at a phase boundary with no review in flight, never inside a review set. Grok's read-only vantage stays (its unique catches come from reading manifests and logs).
- [review-1-r0 F5, 1.17] Incremental `pmToMdast`: the rendered keystroke path converts the whole ProseMirror document to mdast on every doc-changing transaction (`packages/editor/src/store.ts`), so it stays O(document) after 1.17 — 1.17 removed the repeated serialisation around it (snapshot cache) and moved the source view's whole-buffer `parse` off the keystroke (burst-boundary commit), but not this. The r0 numbers that opened the finding: rendered p95 15.0 / 13.9 / 14.1 ms at 10 k words and 26.3 ms at 20 k (Claude, keydown → first mutation of the Markdown pane), 38.6 / 39.5 ms (Sol, beforeinput → the next timer task), source p95 40.6 ms at 10 k words (Claude); `parse` 37 ms against `format` 0.9 ms at 10 k words. 1.17 in this container under `e2e/web/latency.spec.ts`: rendered p95 14.7 ms → 9.5 ms at 10 k words, source p95 27.4 ms → 0.5 ms. Deferred: the option is a mapped conversion (reuse the mdast node for every ProseMirror node a transaction did not touch, keyed on node identity), which touches the one conversion every consumer shares and the id stability its `sidecar` depends on — not a change to make inside a review-fix task. Revisit: when the rendered p95 the latency spec reports at 10 k words rises above the 16 ms of task 1.10's phase check on a machine of this class, or when Phase 3 modes add a second per-keystroke consumer of the conversion; the spec's ratio assertion is the tripwire that says it has become superlinear.
- [review-1-r1 G7, Claude 4] `vitest.config.ts` coverage `include` is `packages/*/src/**` only; `apps/desktop/src/**` (the dev-route wiring: store, bindings, cursor carry, flush, copy — where r0's F3 and F4 and r1's G1 landed) is measured at zero and reported as nothing; the Playwright suite is its only coverage. 1.26 adds the comment that says so in the file. Revisit: Phase 2 planning, when that wiring moves into the shell — add the glob with an honest per-file threshold measured then, not a number copied from `packages/`. Hard stop: `2.verify`.
- [review-1-r1 G9, Claude 6] `e2e/web/latency.spec.ts`: the source view's `pane` metric reports `n=0` by design after 1.17 and the deferred whole-buffer `parse` at the burst boundary is measured nowhere. Revisit: the first task that changes `bindCodeMirror`'s commit path after 1.24 adds a third reported line (`commit: p50/p95, n=<bursts>`, mutations arriving after the last press attributed to the deferred commit), evidence only, never an assertion. Hard stop: Phase 2's autosave task, which is the first consumer of that commit.
- [review-1-r1 G10, Claude 7] The `[review-1-r0 F5, 1.17]` trigger above reads "when the rendered p95 the latency spec reports at 10 k words rises above 16 ms on a machine of this class": the measurement it reads is a **quiet single-worker run** (`playwright test latency.spec.ts --workers=1`, no build or suite running beside it) on a machine of the r0 class — the number a task's journal or a reviewer's isolated rerun records (r1: 9.5 ms in 1.17's container, 11.7 ms Claude, 10.2 ms Sol). The shared CI runners' numbers (r1: 25.4 / 23.6 / 19.9 ms ubuntu / windows / macos; 51–54 ms inside a contended two-worker run) are evidence of contention, never the trigger's input; a reviewer reports them beside the isolated number with the protocol and the worker count named (Sol's r1 lesson).
- [review-1-r1 G11, Claude 8] One line of Michael's 1.9 record (DECISIONS #002-outline-feel), carried nowhere until now: "Editing a bold span edits the characters inside the delimiters rather than the delimiters themselves … I expected to be able to change the formatting and instead got a character edit inside it." It is a question about 1.4's reveal decorations — are revealed delimiters editable text or ornament? Revisit: the Phase 2 shell's first source-view/reveal visual pass (beside `[review-1-r0, Sol 7]` above) decides the interaction; Phase 3's Rewrite mode consumes the answer. Hard stop: Phase 3 planning.
- [review-1-r1, reviewer record] The reviewer-prompt severity sentence option (`[review-1-r0, Michael]` above) keeps its condition. Counted at #review-1-r1 over fourteen shared findings: six disagreements, low-rater instances Grok 4, Claude 2, Sol 2 — a signal, not a verdict (Michael). The calibration fix taken instead is the severity definition line in `docs/reviews/TEMPLATE.md` (one defect drew blocker / should-fix / nit at r1); if the r2 reports still spread three ways on one defect, the phase-boundary docker/ change is the next step. Grok's inputs are not touched.
- [review-1-r1, turn budget] `ralph/lib/run.mjs:108` caps `loop` tasks at 50 turns (`docker/compose.yml`'s `RALPH_MAX_TURNS` default is a fallback the runner always overrides). Phase 1: 10 of 23 loop tasks needed a second attempt, nine of them `NO-JOURNAL` at the cap, each a consumed attempt and a manual restart. Decided at #review-1-r1 (Michael): the r2 chain runs at 50 as single-concern tasks with the journal stubbed first (promoted via 1.verify.r2); the raise is **conditional** — proposed 80 for `loop` tasks from the transcript evidence in #review-1-r1 (capped attempts at 54–67 tool uses, finished ones at 52–56, all in the verification tail), taken as one `fix(runner)` commit with its conformance leg in the Phase 2 boundary window only on Michael's OK after the r2 chain's data at 50, read by the transcript measure (tool uses and `num_turns`), never by the journal's self-reported counts. Hard stop: before Phase 2's first `loop` task.
- [review-1-r1 F1 family, Claude risk 1] The `&#x20;` family's emphasis-edge member — `emphasis[text("b ")]` serialises as `a *b&#x20;*&#x20;c` — is unreachable while Phase 1 binds no mark-toggle command. Revisit: the Phase 3 task that first binds a mark toggle or Rewrite edits inside a mark enumerates the mark-edge boundaries the way 1.25 enumerates the line boundaries, with a guard per edge asserting bytes. Hard stop: `3.verify`.
- [review-1-r1, Michael] Stated invariants without a test: 1.19 turned the "history extension not installed" comment into `no-codemirror-history.test.ts`, which fails a build instead of relying on memory. Sweep the tree for every other "we deliberately do not do X" comment (`grep -rn "deliberately\|never\|not installed\|must not" packages apps` is the starting list) and give each one a test or a written reason it cannot have one. Revisit: Phase 2 planning appends it as a test-only task; hard stop `2.verify`.
- [review-1-r2 H8, Sol 2 — correction of the `[review-1-r1 F1 family, Claude risk 1]` line above] The emphasis-edge member is **reachable now**: load `a *b* c`, put the rendered caret after `b`, type a space → `a *b&#x20;*&#x20;c` by ordinary keyboard events (Sol's `/report/probe.mjs`); a caret at the end of an emphasised word sits inside the mark, and marks come from input rules and from every loaded file. Taken in task 1.30 (the mark-edge clause of the ownership rule, one guard per edge and mark, a corpus leg that types a space at the end of every marked run). The Phase 3 trigger above still stands for the edges a mark-toggle command or a Rewrite edit can reach that a caret cannot (a mark applied to a selection that starts or ends in whitespace). Hard stop unchanged: `3.verify`.
- [review-1-r2 H7, Claude 4 / Grok 2] `ralph/PROMPT.md` lines 25–34 (the per-task wrapper's "Iteration" list) still say implement, then one post-hoc journal entry, one commit, while `CLAUDE.md`/`AGENTS.md` carry the six-step stub-first list `1.verify.r2` installed (RUNNER-SPEC §10); it cost `1.verify.r2.g1` one attempt. Not a fix task: the wrapper is a runner file rendered from the host checkout (`docker/entrypoints/claude-task:20`), fixed by one atomic `fix(runner)` with its conformance test. Taken at the Phase 1→2 boundary, from the host checkout with the runner idle (rotate.md step 5, first case): the wrapper's steps 2–6 replaced by CLAUDE.md's steps 2–5 verbatim, and `tests/agent-rules.test.ts` widened to assert the wrapper's iteration list against CLAUDE.md's. Until then every r3-chain task text names the reading that stands (CLAUDE.md's). Hard stop: the first Phase 2 `loop` task (`2.1`).
- [review-1-r2, Claude risk 1] The `&#x20;` / bare-`\` family has been found one member at a time (r0: a typed trailing space; r1: after a hard break, both sides of a soft break; r2: the atom itself at a block end, the mark edge) because each fix enumerated the previous symptom's boundaries. The invariant is general — every tree the editor can build that `parse` cannot produce makes the serializer emit bytes no file contains — and 1.29's deletion leg and 1.30's mark-edge leg assert it for the typed, deleted and marked shapes. What no leg asserts yet: `pmToMdast`'s output is in the image of `parse` after an **arbitrary** editing transaction (splits, joins, structural moves, pastes). Revisit: Phase 2 planning, beside the `[review-1-r1, Michael]` stated-invariant sweep — a property-shaped corpus test over random ProseMirror transactions, test-only, with the found members enumerated as named guards. Hard stop: `2.verify`.
- [review-1-r2, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r2 chain ran eight `loop` tasks at 50 turns: five passed at attempt 1 (1.24 at `num_turns` 60, 1.26 44, 1.27 39, 1.verify.r2 29, 1.28 33); 1.23 and 1.25 capped at a1 (`error_max_turns`, 50 and 56 tool uses; 1.25 `NO-JOURNAL`, one restart) and passed at a2 (26, 52); 1.verify.r2.g1 a1 stopped by choice at 11 (the H7 wrapper drift) and passed at a2 (25). Only 1.28 and 1.verify.r2.g1 ran with the journal-stub rule in the tree: zero caps and zero `NO-JOURNAL` under it, two data points. The r3 chain (1.29–1.32, 1.verify.r3) runs at 50 under the stub rule; the 80-turn proposal stays conditional on Michael's OK at the Phase 2 boundary and is read by the transcript measure (`num_turns`, tool-use blocks), never the journal's self-report.
- [1.29, found outside scope] A multi-line **plain-text paste into a table cell** does not keep its lines in the cell: `prosemirror-view` turns pasted plain text into one `<p>` per line and its fitter, unable to place a second paragraph in an inline-only `table_cell`, closes the cell, the row and the table — the first line lands in the cell, the rest becomes a paragraph after the table, and the table is cut where the caret was (`e2e/web/editor-trailing-break.spec.ts`, the paste case, asserts only the round-trip properties of the result and not that shape). Not a round-trip defect (the result is a fixed point), a paste-fitting one: a paste into a cell should never restructure the document. Revisit: Phase 2 planning, with the table-editing commands (a `transformPastedText`/`clipboardTextParser` that joins lines with a space when the caret is in a cell, or a cell-aware `handlePaste`); hard stop `2.verify`.
- [1.29, found outside scope] **Shift-Enter in the rendered view inserts nothing** — `essaydownKeymap` and `baseKeymap` bind neither `Shift-Enter` nor a hard-break command, and the browser's default line-break insertion leaves the document unchanged under ProseMirror — so there is no keyboard route to a `hard_break` in the rendered view; the only way to build one is a rich-text paste carrying `<br>` (the H5 case's seed) or the source view. Revisit: Phase 2 planning, when the rendered view's editing keymap is specified (PRD §6.5's key list), as one bound `Shift-Enter` → `hard_break` with its test; hard stop `2.verify`.
- [1.30, found outside scope] **A `hard_break` last inside a flanking mark's run, mid-block** — `emphasis[text("b"), break], text(" c")` — is the atom member of the mark-edge family the whitespace clause of task 1.30 does not touch: the serializer writes the break's line ending as `&#xA;` inside the delimiters (`a *b\&#xA;*&#x63;`, probed, a fixed point but a character reference no file holds), because a closing `*` after a line ending is not right-flanking any more than one after a space. Not reachable by a keystroke today (Shift-Enter is unbound, the line above), only by a rich-text paste carrying `<br>` inside `<em>`; a break *last in the block* is already dropped by 1.29's end-of-block clause whatever its marks. Revisit: the Phase 2 task that binds `Shift-Enter` → `hard_break` (the line above) extends the mark-edge clause to the atom — a break at a run's edge leaves the mark the way its whitespace does — with a guard asserting bytes; hard stop `2.verify`.
- [1.30, found outside scope] **Unicode whitespace at a mark edge.** The mark-edge clause moves *ASCII* whitespace, the class the task names and the class every other clause of the strip uses; CommonMark §6.2's flanking rules read *Unicode* whitespace (Zs, so U+00A0 too), and the serializer encodes that class (`micromark-util-classify-character`): `emphasis[text("b ")]` serialises as `a *b&#xA0;*&#x63;`, a fixed point (the reference decodes to the same tree) but a reference, and Option+Space on macOS types U+00A0. Revisit: the next review attempt's reconciliation decides whether the clause's class widens to §6.2's (moving a no-break space out of a mark changes what a writer typed on purpose, which is why 1.30 did not decide it); hard stop `2.verify`.
- [1.32, found outside scope] **A fourth instance of the `x === x` count tautology** (task 1.21 F9c, 1.26 G8, Grok r2 finding 1 → 1.32): `packages/editor/test/editor-fixed-point.test.ts:763` asserts `expect(names.length).toBe(Object.keys(index).length)` where `names` is `Object.keys(index).sort()` — the same shape 1.32 removed from `position-map-editor-leg.test.ts`, found by the grep 1.10.r2d's lesson asks for (`grep -rn 'names.length).toBe(Object.keys'` over the tree; two hits, one in scope). That file is not in 1.32's scope, so the line stays; its `toBeGreaterThan(0)` sibling and the per-fixture `it` cases are the coverage that holds. Revisit: the next review attempt's reconciliation (or any task that next edits `editor-fixed-point.test.ts`) drops the line; hard stop `2.verify`.
- [plan.1.verify.r3h.r0, found outside scope] **Four rendered-view caret placements through `ControlOrMeta+Home`/`ControlOrMeta+End` pass on macOS by accident of the seed's click** — `e2e/web/editor-soft-line-breaks.spec.ts:85` (G4's first-line space: `ControlOrMeta+Home` then five `ArrowRight`; its assertion is "bytes unchanged", which a space typed at the paragraph's end also satisfies, so on macOS the case proves nothing about the first line) and `:107`, `e2e/web/editor-trailing-break.spec.ts:125` and `:138` (`ControlOrMeta+End` after a click that already leaves the caret at the document end) — the construct DECISIONS #022 removes from `editor-mark-edge.spec.ts` in task 1.33. The source-view uses of bare `Home`/`End` (`editor-cursor.spec.ts:36`, `editor-toggle.spec.ts:359`, `:436`) are CodeMirror's own keymap, handled in JS before the OS layer, and are not members. Revisit: the r3 reconciliation if a reviewer raises it; otherwise Phase 2 planning as one test-only task replacing the four with Blink-only motions and adding a guard over `e2e/web/` (no `Home`/`End`/`ControlOrMeta+Home|End` in a rendered-view spec, as a test that fails the build — the `no-codemirror-history` shape Michael named at #021). Hard stop: `2.verify`.
- [review-1-r3, DECISIONS #023] **Boundary `fix(runner)` for the transient-rerun path**: `ralph/lib/doctor.mjs` gains the clause a rerun produces (a `rejected` attempt `n` on a `passed` gate with `attempts > n` is recorded — the rerun could only have started from its recorded failure); `ciGate` refuses `--resume a<n>` when `accepted.json` names a later attempt or the gate is `passed` ("a<n> is superseded by a<m>; nothing to resume"), so doctor's remedy can never overwrite an accepted record; one conformance test per guard, and the existing transient-rerun test ends with a `doctor` clean assertion. Until then, after any `gate.sh rerun` that accepts: never run the `--resume` doctor names; `scripts/gate.sh abandon <gate> a<n> --reason "… (DECISIONS #023)"`, once per rerun. Taken at the Phase 1→2 boundary from the host checkout with the runner idle (rotate.md step 5, first case), beside the `[review-1-r2 H7]` fix. Hard stop: the first Phase 2 `loop` task (`2.1`).
- [review-1-r3 I7, Claude 4 — conditional, Michael's OK] `scripts/gate.sh` fetches artifacts only on `conclusion === success` (#020), so the a1 hang of `1.verify.r3.g1h` (windows-latest, `editor-clipboard.spec.ts:126`, `setInputFiles` 30 s timeout before any assertion) left no trace or `error-context.md`, and nothing distinguishes a Vite optimizer reload from an app that failed to mount. Candidate: fetch `playwright-report` on failure too (the `merge-logs` job already uploads it), kept beside the `a<n>/` record and never read as acceptance. A change to #020's rule and to `gate.sh`: proposed with this one lost trace as the evidence, decided at the Phase 1→2 boundary on Michael's OK, taken (if at all) as one `fix(runner)` with its conformance leg. Hard stop: `2.1`.
- [review-1-r3 I7, Claude 4 — flake signature] `locator.setInputFiles: Test timeout of 30000ms exceeded … waiting for getByTestId('fixture-file')` inside `load()` in an `e2e/web` spec, on any runner, in a file unchanged since its last green run there — one occurrence so far (`1.verify.r3.g1h` a1, windows-latest, run 34572776730). Revisit: a second occurrence → a test-only fix task that makes `load()` wait for the app to mount (the dev bar's file input attached and enabled) before `setInputFiles`, or pins the Vite optimizer's dependencies for `e2e/web`; until then a same-SHA rerun is the answer only when the signature matches exactly and the file is unchanged (RUNNER-SPEC §2, #023). Hard stop: `2.verify`.
- [review-1-r3 I1, Claude 1 — correction of the `[1.30, found outside scope]` break-inside-a-mark line above] The member is **reachable now**: load a paragraph holding a hard break inside emphasis (`*roses are red\` newline `violets are blue* — anon`; every loaded file and the source view supply the `<br>`), caret after `blue`, Backspace to the break → `*roses are red\&#xA;*— anon` on the pane and in the copy, the break gone and `&#xA;` now text; for strikethrough and for a setext heading the bytes are not even a fixed point. The reachability sentence was written from the insertion side (Shift-Enter unbound) and never probed by a deletion. Taken in task 1.34 (the mark-edge clause extended to the atom, one guard per position and mark, a second deletion range set to the end of every marked run, a fixture with a break inside emphasis). The Phase 2 Shift-Enter line above stands for the insertion route.
- [review-1-r3 I10, Claude 8 / Grok — decision on the `[1.30, found outside scope]` Unicode-whitespace line above] Decided at the r3 reconciliation: the mark-edge clause's class stays **ASCII** in Phase 1. A no-break space inside a mark is what Option+Space typed on purpose, `&#xA0;` is a fixed point that decodes to the same tree, and no gate criterion is touched. Revisit re-pointed: the Phase 3 task that first binds a mark toggle (the `[review-1-r1 F1 family]` trigger) decides whether §6.2's Zs class applies to the edges a toggle can create. Hard stop: `3.verify`.
- [review-1-r3 I8 — disposition of the `[1.32, found outside scope]` tautology line above] Trigger fired at the r3 reconciliation (Claude 6, Sol 4, Grok 1); the line is dropped in task 1.34, which edits `editor-fixed-point.test.ts` (the `toBeGreaterThan(0)` sibling and the per-fixture cases carry the claim). No further line.
- [review-1-r3 I6 — disposition of the `[plan.1.verify.r3h.r0, found outside scope]` chord line above] Trigger fired (Claude 5, Sol 3 Required, Grok 2): task 1.38 replaces the four rendered-view chords with Blink-only motions, asserts the `:85` case's caret offset before it types, and adds the tree-wide guard in `tests/` (no `ControlOrMeta+Home|End` under `e2e/web/`; bare `Home`/`End` only at the three allowlisted source-view sites). No further line.
- [review-1-r3, Sol risk 1] The class behind I2: `blockToMdast` copies every parsed block attribute (`spread` on `list` and `list_item`, `checked`, `align`, `start`, `lang`/`meta`) from the ProseMirror node's attrs, and a structural edit (a split, a join, a paste of blocks, a list item moved) can leave one describing a structure that no longer exists; 1.36 re-derives `spread`. Revisit: Phase 2 planning, beside the `[review-1-r2, Claude risk 1]` property-shaped test — one sweep over the attributes the serializer reads, each either re-derived from the children or shown to be invariant under every editing command bound in Phase 2, with a list/table-shaped destructive leg. Hard stop: `2.verify`.
- [review-1-r3, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r3 chain ran seven `loop` tasks at 50 turns under the journal-stub rule: 1.31 (`num_turns` 49, 48 tool uses), 1.32 (38), 1.verify.r3 (35), 1.33 (22), 1.verify.r3.g1 (24) at attempt 1; 1.29 a1 `error_max_turns` at 51 / 50 tool uses (stub present, no stop), a2 `success` at 21 with DONE and `NO-JOURNAL` (procedure: the stub completed in place), a3 passed at 12; 1.30 a1 `error_max_turns` at 51 / 50, a2 passed at 27. Phase 1 to date: 15 of 38 loop tasks needed more than one attempt; `NO-JOURNAL` stops 11. Of the six opus fix tasks in the r2 and r3 chains, three ran to 49–51 turns at a1. The 80-turn proposal stays conditional on Michael's OK at the Phase 2 boundary, read by the transcript measure.
- [review-1-r3, reviewer record] Counted at #review-1-r3: unique findings Claude 6 of 8, Sol 2 of 4, Grok 0 of 2; two shared findings (one agreed, one with Grok low); cumulative over seventeen shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2; Grok PASS with blockers in the tree at r2 and r3. The `[review-1-r1, reviewer record]` condition (three-way spread on one defect) has not fired (I6 spreads over two classes). No change inside a review set; these counts are the input to Michael's boundary decision on the reviewer-prompt option.
- [1.37, deletion leg (review-1-r3 I5, Sol 2)] `cursorMap.toSource` (`packages/editor/src/toggle.ts`) answers a position the block correspondence covers but no inline child does with the block's *start*: `innermostAt` finds the block, a block has no spelling table, so the `range.startLine` branch fires. Two editor-reachable shapes, both found by the position-map family's deletion leg and confirmed headless: (a) a paragraph left ending in a `hard_break` after the continuation is deleted (`abc  ⏎def` → Backspace over `def`) — the strip drops the trailing break, the mdast block is narrower than the editor's, and the cursor after the break maps to the paragraph's start (bytes: `3:0`, expected `3:2` for `ab`); (b) the empty paragraph a split opens inside a list item — `isPlaceholder` skips it, the item's correspondence still covers it, and the cursor maps to the item's start (`1:0`, expected the end of the paragraph before it, `1:10` for `- item one`), where the same placeholder at document level goes through `afterLastNode` to the previous block's end. Deferred: the fix is in `toggle.ts`, outside a test-only task's scope. Revisit: Phase 2 planning, beside `[review-1-r3, Sol risk 1]` — the first Phase 2 task that binds a rendered-view deletion (Backspace/Delete) or touches `cursorMap` folds both shapes into the leg's clause 3 (delete the width branch, the split collector and the `it.fails` case). Hard stop: `2.verify`; and the `known defect` `it.fails` case in `position-map-editor-leg.test.ts`, which turns the file red the moment `toSource` answers correctly.
- [plan.1.verify.r4h.r0, found outside scope] **Two rendered-view caret placements through `toDocumentEnd` (`ArrowDown` once per visual line) remain in `e2e/web/editor-trailing-break.spec.ts`** (H1 `toDocumentEnd(page, 2)`, H5 `toDocumentEnd(page, 1)`), the construct #024 replaces in `editor-soft-line-breaks.spec.ts` in task 1.39. Each is followed by 1.38's end-of-block assertion (`"two"` at 3, `"one"` at 3) and passed on ubuntu, macos and windows at `1.verify.r4h` a1, so they are proven for those geometries; they pass because the click already leaves the caret at the end. Revisit: the first red landing assertion on any runner, or the next task that edits the file, replaces them with the anchor-and-count route (the click's end-of-block anchor asserted, then counted horizontal arrows); `tests/no-caret-chords-in-e2e.test.ts` does not cover vertical arrows and a guard for them is Phase 2 planning's call. Hard stop: `2.verify`.
- [review-1-r4, DECISIONS #025] **Boundary `fix(runner)` for a single-reviewer retry**: `stepReview`, when it starts a strict subset of a review set (`todo.length < 3`), gives the started reviewer a view of the attempt directory holding only its own subdirectory and the four metadata files; the entrypoints' `check_report` refuses a report whose transcript read a sibling's `report.md`; one conformance test per guard (a retried reviewer cannot read a sibling's report; a full-set start is unchanged; the siblings are back before `passed` is written), and the existing retry test ends with `doctor` clean. Until then, before any `ralph.sh retry <set>.<attempt><x>`: #025's manual procedure (the failed attempt's own `report.md`/`transcript.log` moved aside with an attempt suffix; the sibling directories moved to `<attempt>.held/` and restored before the reconciliation); a Grok 402 is Michael's call, never retried on the principal's initiative. Taken at the Phase 1→2 boundary from the host checkout with the runner idle, beside the `[review-1-r2 H7]` and `[review-1-r3, DECISIONS #023]` fixes. Hard stop: `2.1`.
- [review-1-r4 J3 — disposition of the `[1.37, deletion leg]` cursor-map line above] Trigger fired at the r4 reconciliation (Claude 3 should-fix, Sol 2 should-fix Required; reproduced headless: `abc\`+LF+`def` minus `def` → `toSource` of the end is the paragraph's start; Sol's browser probe types `Xabc`). Taken in task 1.43 (`toggle.ts:351` → `afterLastNode`; the width branch, the split collector and the `it.fails` case removed; both shapes folded into the deletion leg's clause 3; Sol's delete → toggle → type reproduction as a Playwright case) rather than Phase 2 planning: a test-only task's scope is a fact about the task, not a deferral reason for a defect on an existing Phase 1 action. No further line.
- [review-1-r4 J6 — correction of the `[plan.1.verify.r4h.r0, found outside scope]` vertical-arrow line above] The construct (a vertical arrow to a block's edge in a block that renders more than one line) is at **three** sites, not two: the two `toDocumentEnd` uses in `e2e/web/editor-trailing-break.spec.ts` (`:85` helper, H1 and H5) and `e2e/web/editor-break-in-mark.spec.ts:126` (`ArrowDown` ×2 in the verse block with its `<br>`, task 1.34). `editor-link-edge.spec.ts:143`/`:163` (`ArrowDown` ×2 / `ArrowUp` ×2) and `editor-mark-edge.spec.ts:90`/`:106`/`:123` are one-line blocks, inside the promoted rule. All carry a caret precondition and passed on three runners. The revisit trigger above covers all three sites; `tests/no-caret-chords-in-e2e.test.ts` still cannot see a vertical arrow (1.44 widens it to modifier chains only). Hard stop: `2.verify`.
- [review-1-r4 J7, Claude 7] The heading input rule (`packages/editor/src/input.ts`, `HEADING`) applied at the start of a hard-wrapped paragraph (`alpha`+LF+`beta gamma`, the soft line break typing preserves since 1.25) makes a heading whose text holds a `\n`; at depth 1–2 `format` writes it setext, at depth ≥ 3 as `### alpha&#xA;beta gamma` (reproduced at the r4 reconciliation) — a fixed point that decodes to the same tree, so no criterion is touched, but the entity is what the writer sees in the source view. Revisit: the Phase 3 task that first binds a heading conversion replaces line endings and breaks by a space when the target depth is ≥ 3, the way cells do (`CELL_LINE_ENDING`), with a guard per depth. Hard stop: `3.verify`.
- [review-1-r4, Claude risk 1] The serializer half of the fixed-point property is owned by no test: `toMarkdownExtensions()` (`packages/core/src/format.ts`) takes `gfmTableToMarkdown()`, `gfmStrikethroughToMarkdown()` and `gfmAutolinkLiteralToMarkdown()` as-is, and only the strikethrough handler failed the flanking walk (J1, 1.40); tables (`|`, line endings in cells), autolink literals and inline code were clean in Claude's 61,682-transaction sweep, but nothing in the tree reads the handlers. Revisit: the task that adds the next `mdast-util` extension (none is planned inside PRD §4; footnotes and task lists stay plain text) reads its handler for flanking and escaping logic and adds a fixture per delimiter beside a letter to the identity corpus, the way 1.40 adds `strikethrough-punctuation.md`. Hard stop: the task that adds one.
- [review-1-r4, Claude risk 2 — evidence on the `[review-1-r2, Claude risk 1]` property-test line above] A throwaway sweep of every keystroke-shaped single transaction (Backspace, Delete, word-Backspace, Enter, Tab, Shift-Tab, a typed letter/space/period/asterisk/tilde with the position's marks, a two-line plain paste) at every inline position of every fixture — 61,682 transactions, plus 11,060 pairs — ran in under five minutes in the container and found J1 in one run while confirming every r3 fix; the committed legs found nothing beyond their enumerated ranges. The line's hard stop stands (`2.verify`): whether a five-minute sweep joins every suite run on three runners, or runs as a separate job, is Phase 2 planning's cost decision with this evidence; 1.41's neighbour leg is the targeted member for Phase 1.
- [review-1-r4, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r4 chain ran nine `loop` tasks at 50 turns under the journal-stub rule: 1.34, 1.35, 1.36, 1.37 each a1 `error_max_turns` at 51 (stub present, no stop), a2 passed at 26 / 20 / 25 / 36; 1.38 (35), 1.verify.r4 (40), 1.39 (18), 1.verify.r4.g1 (26) at attempt 1. Of the opus fix tasks in the r2–r4 chains that edited `packages/editor/src/schema.ts` (1.25, 1.29, 1.30, 1.34, 1.35, 1.36), six of six capped at a1. Phase 1 to date: 19 of 46 loop tasks needed more than one attempt; `NO-JOURNAL` stops 11 (none in the r4 chain). Claude's r4 review ran 110 turns (r3: 85). The 80-turn proposal stays conditional on Michael's OK at the Phase 2 boundary, read by the transcript measure; the r5 chain runs at 50 with each fix task scoped to one file.
- [review-1-r4, reviewer record] Counted at #review-1-r4 from the two independent reports (Grok's r4 report derived, DECISIONS #025, not counted): unique findings Claude 7 of 9, Sol 0 of 2; two shared findings, both agreed by class (J1 blocker/blocker; J3 should-fix with the Required flag differing); cumulative over nineteen shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2, unchanged; Grok's PASS-with-blockers count unchanged at 2 of 2 counted attempts. Michael's decision (#025): no paying for Grok, the one reset spent, a recurrence his call. Open for him before `1.10.r5a/b/c` start: whether Grok reviews r5 at all (the r5 rows are written for three reviewers as the generator expects; a no is a planning commit that supersedes `1.10.r5c`, never an edit). No change inside a review set.
- [1.40, found outside scope] **`formatWithMap` (`packages/core/src/positions.ts`, task 1.2) reports a text node as `unresolved` when `containerPhrasing` encoded its edge character after the node's handler returned** — the `outside` case of `mdast-util-to-markdown/lib/util/encode-info.js` (a letter or digit directly beside a punctuation edge of an attention run): `x *a.*b` → `x *a.*&#x62;` and `x **a.**b` → `x **a.**&#x62;` at the branch base with the built-in handlers, unresolved path `0.2` (and `b*.a* x` → `&#x62;*.a* x`, path `0.0`, on the opening side), and `~~a.~~&#x62;` the same way once 1.40's `delete` handler encodes as the built-ins do. Cause: `placeChildren` looks each child's emission up in its parent's output by `indexOf`, and the emission (`b`) is not in the output (`&#x62;` is); the parent rewrote it in `lib/util/container-phrasing.js` (`encodeAfter`, `encodingInfo.before`) after the handler returned, which the instrumentation cannot see. Consequence for the Phase 1 user path: the position map is missing a range for that one text node, so `nodeAt` over its bytes answers the paragraph and the source-view ⇄ rendered cursor map falls to its block clause there; every other node of the document is placed. Not fixed in 1.40 because `positions.ts` is outside its scope (`format.ts`, `packages/core/test/**`, the fixture) and the corpus reached the shape for the first time through the fixture the task requires; pinned in `packages/core/test/positions-corpus.test.ts` as the `known defect` `it.fails` case over `OUTSIDE_ENCODED_NEIGHBOUR` (one assertion, the fixture's other position clauses green). Fix shape: `placeChildren` (or `wrapHandle`) resynchronises a `text` child whose value is not found by also trying the value with its first and/or last character replaced by its `&#x…;` reference — the only two rewrites `containerPhrasing` makes — and records the spelling in the node's `SpellingTable` the way an escaped character already is. Revisit: the r5 reconciliation dispositions it by its consequence for the phase's user path (a task's scope decides who fixes it, never whether — lesson [1.10.r4d]); the fix removes `OUTSIDE_ENCODED_NEIGHBOUR` and the `it.fails` case, folding the fixture into the ordinary case. Hard stop: `1.close`; and the `it.fails` case itself, which turns `positions-corpus.test.ts` red the moment the node is placed.
- [review-1-r5 K2 — disposition of the `[1.40, found outside scope]` position-map line above] Trigger fired at the r5 reconciliation (Claude 2, Sol 2, Grok 1 — should-fix Required by all three; reproduced headless: `x *a.*&#x62;` unresolved `0.2`, `~~a.~~&#x62;` `0.1`, `&#x62;*.a* x` `0.0`; through `cursorMap` every position inside `gamma` of `Alpha ~~beta.~~&#x67;amma delta` answers the struck run's end, and the position after `b` of `x *a.*&#x62;` answers `{1, 5}` — Sol's `x *a.X*b`). Taken in task 1.46 (`placeChildren` retries a missed text child with its first and/or last code point replaced by its `&#x…;` reference, the reference recorded in the node's `SpellingTable`; `OUTSIDE_ENCODED_NEIGHBOUR` and the `it.fails` case deleted; a clause in the deletion leg asserting `toSource` monotone over the encoded neighbour and `toRendered` its inverse; the toggle-then-type route as a Playwright case), after 1.45 so the retry matches the code-point reference the wrapper writes. No further line.
- [review-1-r5 K6, Claude 6 — the half not taken in 1.47] `tests/no-caret-chords-in-e2e.test.ts` cannot see a Home/End key handed to `keyboard.press` as an identifier (`press(key)` with the name held in a `const`) or as a template literal; 1.47 widens `CHORD` and `BARE_PRESS` to the three quote forms only. The identifier pattern Claude proposes (`press\(\s*[A-Za-z_$][\w$]*\s*\)`) is not taken because every counted-press helper under `e2e/web/` (`press(page, key, times)`, whose body is `keyboard.press(key)`) is that pattern and would need an allowlist of its own. Revisit: the next spec that presses a key held in a `const` or a template literal, or a `prettier --check e2e/web` step joining `pnpm lint`, whichever comes first. Hard stop: `2.verify`.
- [review-1-r5, Claude risk 1] The serializer's flanking chain (`encode-info.js`, `container-phrasing.js`, `micromark-util-classify-character`, the app's port and `formatWithMap`'s spelling tables) counts UTF-16 units, and until 1.45 the corpus held no non-BMP character. 1.47's `astral-neighbour.md` seeds one at a mark's edge on both sides and in plain text (after 1.46, because the deletion leg puts an encoded neighbour last in a block); the positions it does not reach — inside a link's text at the link's edge, a table cell's edge, a heading's edge, either side of a soft line break, the edge of an inline code span — have no astral member in any leg's enumeration, and the promoted rule (a) asks each neighbour-class enumeration for one. Revisit: the first Phase 2 task that touches a corpus leg or the fixture index adds an astral member to each of those positions (one fixture, `astral-positions.md`, or one paragraph each in the existing fixtures' canonical siblings), and the sweep line `[review-1-r4, Claude risk 2]` seeds its transactions with a non-BMP character at every inline position class. Hard stop: `2.verify`.
- [review-1-r5, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r5 chain ran six `loop` tasks at 50 turns under the journal-stub rule; counts from the transcripts' result lines: 1.40 (opus) a1 51 capped with the work done (#026), a2 23 / a3 13 conflict stops, `retry` passed at 29; 1.41 (sonnet) 51 / 51 / 51 capped re-verifying a finished branch, `retry` a1 51, a2 16 on a session-limit 429 (#027), a3 52 `STUCK (no journal entry)`, second `retry` passed at 38 (seven transcripts); 1.42 (sonnet) 51 / 51 / 51, `retry` passed at 18; 1.43 (opus) 42 / 13 / 8 DONE withheld on a stale clause (#028), `retry` passed at 15; 1.44 (sonnet) 35 and 1.verify.r5 (sonnet) 43 at attempt 1. Four of five fix tasks reached `STUCK` with the work complete; every retry was a procedure lesson. Phase 1 to date: 23 of 52 loop tasks needed more than one attempt; `NO-JOURNAL` stops 12 plus one `STUCK (no journal entry)`. Claude's r5 review ran 113 turns (r4: 110), Grok's 28. The 80-turn proposal stays conditional on Michael's OK at the Phase 2 boundary; the r6 chain runs at 50 with every task text naming its one journal line and its one suite run.
- [review-1-r5, reviewer record] Counted at #review-1-r5 from three independent reports (Grok started with its siblings; #025's condition met): unique findings Claude 5 of 8, Sol 0 of 2, Grok 1 of 3; three shared findings, all agreed by class and severity (the blocker Claude–Sol; the position map all three at Required; the whitespace cell Claude–Grok at nit); cumulative over twenty-two shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2, unchanged; Grok's PASS-with-blockers count stays 2 of 3 counted attempts. Michael's decision (#027): Grok reviewed r5 on the usage reset; the r6 rows are written for three reviewers; a 402 during r6 is his call (#025). No change inside a review set.
- [1.46, found outside scope] **The toggle chord reads a rendered caret one rendering step stale after a native arrow motion** — `toggle` in `apps/desktop/src/dev/DevEditor.tsx` reads `pm.state.selection.head`, and ProseMirror's state takes a native caret move (an `ArrowLeft`/`ArrowRight` Blink resolved on its own) from the document's `selectionchange` event, which Blink dispatches in the rendering update *after* the key; `prosemirror-view`'s keydown `forceFlush` (1.42.3, `editHandlers.keydown`) flushes only a pending mutation, not the selection. A `ControlOrMeta+/` sent before that step (1.46's Playwright case at attempt 1: `ArrowRight` ×3 then the chord in the same input burst) carried the caret from `gamma`'s start, not from `gam|`, and the bytes were `Alpha \~\~beta.\~\~Xgamma delta` with the product's map right — the same spec passes with the observer given a tick, which is the precedent `e2e/web/editor-toggle.spec.ts` (line ~132) already uses. A human cannot press two keys inside one frame, so no user path is known to reach it; the exposure is the same for every keymap command that reads the selection after a native caret motion. Revisit: the first Phase 2 task that touches `toggle` in the desktop editor (the real app's toggle, PRD §6.5) settles the DOM selection before reading it (read `view.domSelectionRange()` through `view.posAtDOM`, or flush the observer) and its e2e drops the tick; or the first three-OS CI run where a spec fails on the caret after a chord. Hard stop: `2.verify`.
- [1.46, evidence — third instance of the class above] The `1.verify.r7.g1h` gate (ci.yml run 35685069545 at `5a39abd`, DECISIONS #036, task 1.56) is a third instance of a native motion reverted before the next key on windows-latest, by a third route: `editor-soft-line-breaks.spec.ts:164`'s assertion after five `ArrowRight` from an asserted offset 0 received offset **11**, not 5 — the caret had reverted to 6 (its pre-motion reading) before the rights began, i.e. ProseMirror had not yet observed the native `ArrowUp` that preceded them when the next key arrived. Task 1.56 routes that one case around the class with a click at a computed point rather than a second key after a native motion; revisit trigger and hard stop unchanged (`2.verify`).
- [1.47, letter leg's model] `typeInsideEveryBlock`'s corpus guard (`packages/editor/test/editor-fixed-point.test.ts`, "a letter is typed inside every block") checks `out.split(letter).join("") === canonical`: a letter absent from the fixture's canonical bytes, deleted everywhere from the typed output, gives the canonical bytes back. The model cannot predict the case where the inserted letter lands flush against a flanking mark's own opening delimiter and the serializer's flanking classification (`encodeInfo`) then has to encode that letter as a character reference — the split-and-join no longer isolates only the inserted occurrences from the ones the encoding might also touch. `astral-neighbour.md`'s second paragraph therefore reads `x &#x10400;*(a)*` rather than `&#x10400;*(a)*` directly: the leading `x ` keeps the leg's first insertion point (right after the block's first code point) away from the mark's edge. Revisit: the task that first needs a fixture whose first block character sits flush against an opening flanking mark gives the leg a fixture-aware skip, or predicts the encoded form instead of assuming the raw letter survives untouched. Hard stop: `2.verify`.
- [review-1-r6 L9, Claude 8] **URL-shaped text the editor holds as text is not a fixed point of `parse ∘ format`**: typed in the rendered view, `See https://example.com/a_b for details.` serialises as `See https\://example.com/a\_b for details.\n` (`gfmAutolinkLiteralToMarkdown()`'s `unsafe` patterns in `packages/core/src/format.ts`), and the app's own `parse` re-links it anyway — `mdast-util-gfm-autolink-literal`'s `fromMarkdown` transform runs `find-and-replace` over decoded text — so `format(parse(copied))` is `See <https://example.com/a_b> for details.\n` ≠ copied; the source view shows backslashes the user never typed, and a later source-view edit turns the text into a link. Reproduced by the r6 reconciliation on a hand-built text node. Not the blocker class: the copied bytes are a valid representation of the typed text, a foreign reader honours `\:`, no character is lost, and the live app does not relink on a toggle without an edit. Neither fix is small — dropping the escapes leaves the fixed point unmet, and applying the autolink transform in `pmToMdast` makes typed URLs links in the editor's model (a representation decision). Revisit: the first Phase 2 task that loads a file from disk into the editor decides the representation once (the editor's tree equals the parser's — text → `link`, bytes `<…>` — or the escapes are dropped and the fixed point is asserted on the re-linked form), states it in DECISIONS.md, and adds a corpus leg that types a URL into every block (the typing legs type letters only). Hard stop: `2.verify`.
- [review-1-r6 L11, Claude 10] Two adjacent attention runs from the parser's own tree are not a fixed point: `parse("*x.*_y_\n")` holds two `emphasis` siblings and `format` writes `*x.**y*`, whose parse differs, and the next pass writes `\*x.\**y*` (the built-in `emphasis` handler, upstream). Reproduced by the r6 reconciliation. Unreachable from the editor (adjacent same-mark text merges into one run); reachable from a file. Revisit: the first Phase 2 task that loads a file from disk into the editor adds a fixture with two adjacent same-mark runs and decides between an app handler that merges adjacent same-mark siblings before serialising and an upstream fix. Hard stop: `2.verify`.
- [review-1-r6 L2 — correction of the `[review-1-r4, Claude risk 1]` line above] That line's trigger ("the task that adds the next `mdast-util` extension") could not fire for a defect inside an extension already installed, and L2 (the autolink backslash, task 1.50) is one; L3 (inline html before a soft break, task 1.51) and L11 (adjacent runs) are two more members of the class the line deferred. 1.50 adds `autolink-backslash.md` and 1.51 `inline-html.md` to the corpus; the r7 review rows seed one probe per installed handler's delimiter characters (a table cell holding `|`, a code span holding a backtick at its edge, a URL holding each character `safe()` rewrites). Revisit for the remaining members: the first Phase 2 task that touches the fixture index adds one fixture per installed third-party handler with the delimiter characters that handler escapes beside a letter and at a block edge. Hard stop: `2.verify`.
- [review-1-r6 L1 — note on the `[review-1-r5, Claude risk 1]` line above] Task 1.49's position-class guards cover the *two-run* astral member in a heading, a table cell, a link's text, a blockquote and a list item, and `astral-between-runs.md` seeds a heading; the one-mark positions that line names (a link's edge, a cell's edge, a heading's edge, either side of a soft break, an inline code span's edge) stay its own, trigger and hard stop unchanged.
- [review-1-r6, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r6 chain ran five `loop` tasks at 50 turns under the journal-stub rule; counts from the transcripts' result lines: 1.45 (opus) a1 2 turns on a 429 session limit (#027's second instance, `NO-JOURNAL 1.45` by the harness), a2 37; 1.46 (opus) a1 51 capped, a2 44; 1.47 (sonnet) a1 51 capped without a stub, a2 51, a3 51 with the work complete, STUCK, retry 42; 1.48 (sonnet) 54; 1.verify.r6 (sonnet) 38. Phase 1 to date from `[ralph] <id> attempt` lines: 26 of 57 loop tasks needed more than one attempt; NO-JOURNAL with a Phase 1 id 14 (two 429s); STUCK 7. Reviewers: Claude 123 turns, Grok 25, Sol no count. The 80-turn raise stays conditional on Michael's OK at the boundary (DECISIONS #review-1-r6).
- [review-1-r6, reviewer record] Counted at #review-1-r6 from three independent reports (Grok started with its siblings; #025's condition met): unique findings Claude 8 of 10, Sol 0 of 3, Grok 0 of 1; three shared findings, all agreed by class (the two-run blocker Sol–Grok, Claude absent; the html rewrite Claude–Sol and the counted press Claude–Sol, each with the Required flag differing — the J3 precedent, not a disagreement); two blocker families, one shared and one unique to Claude; cumulative over twenty-five shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2 — unchanged; Grok's PASS-with-blockers count stays 2 of 4 counted attempts.
- [1.51, two position-map editor-leg models met by a paragraph beside inline html] Found by the #030 retry, outside 1.51's scope (`packages/editor/test/position-map-editor-leg.test.ts` is not in it), on the Decision-1 fixture's second paragraph `*a* <b>x</b> y`; neither is about html. (a) The letter leg ("a letter is typed inside every block") asserts the typed letter is the character before `toSource(after)` — but when the block's first text run is one character inside a mark, the letter is typed at the mark's end (`insertText` inherits the inclusive mark), and `cursorMap.toSource` at a boundary hands the position to the later node (`innermostAt`: "a boundary belongs to the later node"), so the source cursor lands after the closing `*` and the character before it is the delimiter: `*aQ* …` answers ch 4, the leg expects ch 3. `*a* y` alone fails the same way; the `[1.47, letter leg's model]` line above is the same class (a fixture shaped to keep the leg's first insertion point away from a mark's edge), and the product question — whether a caret at the end of an emphasised run toggles to before or after the closing delimiter — is open. (b) The deletion leg's clause-3 oracle (`byteEndOf`) answers a block whose reparsed last leaf is not text with that leaf's *start*, "the rule `cursorMap`'s doc comment states for every node that is not text"; when the deletion leaves ` ` after `</b>` and the conversion strips it, the block is narrowed and clause 2 (`lastNodeEnd`) answers the atom's *end* (ch 12 against the oracle's ch 8). That is the trailing-atom block end DECISIONS #030 Decision 2 gives to task 1.52 (L5), whose rule ("at or past a node's end, answer the end") sides with clause 2, so the oracle's non-text branch is the assertion that yields when 1.52 lands, and it is outside 1.52's named scope too. Revisit: (a) with the `[1.47, letter leg's model]` line's trigger (the leg gets a fixture-aware skip or predicts the boundary rule), and the first Phase 2 task that types at a mark's end in the real app decides which side of the closing delimiter the toggled caret takes; (b) task 1.52 or its reconciliation widens `byteEndOf`'s non-text branch to the end for a block narrowed past a trailing atom. Hard stop: `2.verify`.
- [1.52, code block interior] Found by 1.52's corpus inverse leg (`packages/editor/test/position-map-inverse.test.ts`), outside 1.52's scope: every caret position inside a fenced code block maps to the block's start and back to the position *before* the block — `code-fence-basic.md` position 1 (the first position inside the `code_block` at 0, whose bytes open with `` ```js ``) → `toSource` 1:0 → `toRendered` 0, and every one of its 13 interior positions the same; `code-fence-nested.md` (91 positions), `essay-fixture.md` (970 positions, the block at 8679 → 45:0 → 8679). The cause is the map's third clause by design (task 1.7): a leaf block (`code`, `thematicBreak`, `html`) is placed but never entered, because `formatWithMap` (`packages/core/src/positions.ts`) builds a spelling table only for `text` and (since 1.51) `inlineCode`, and a `code` node's bytes are not its value (a fence line and an info string precede it, indentation inside a list item prefixes every line). A user who toggles from inside a fence lands on the fence line; from a source caret inside the fence body, `toRendered` lands before the block. Not one of the members 1.52 names (the same-line container end, the trailing leaf), and not `toggle.ts`'s branch: the leg asserts the three fixtures as a named exclusion asserted positively (DECISIONS #032, which decided the `it.fails` form yields so the suite holds no expected fail): the ordinary case on each pins that every non-identity position is inside a `code_block`, nothing else is, and at least one exists, and both legs' reach cases count the excluded positions as exactly the `code_block` interiors of exactly those three fixtures. The class is L4's block-level sibling (the value-bearing *inline* node has its table since 1.51, the value-bearing *block* node does not) and the remaining member of Claude's r6 risk 2, decided so the r7 reviewers read it as such. Revisit trigger: the first task that gives `code` a spelling table in `positions.ts` (a per-line table: the value's line `n` at the fence body's line `n`, after the container prefix) removes the exclusion and its positive guards (`CODE_BLOCK_MEMBERS` and the reach cases' counts) together. Hard stop: `2.verify`.
- [1.53, a raw source keystroke before a punctuation-edged run] Found by 1.53's boundary matrix (`packages/editor/test/toggle.test.ts`, "a boundary between marked and unmarked text is resolved by `$pos.marks()`"), outside 1.53's scope (the cursor map places the caret; the source view is CodeMirror over the bytes): at the run's *start* of `Alpha *(beta.)* gamma` (and the `**`, `~~` siblings) both views put the caret before the opening delimiter and give a typed letter no mark, but the bytes cannot agree at any column — the rendered view keeps the run by writing the letter as `&#x58;` (the encoded-neighbour rule, tasks 1.45/1.49: `Alpha &#x58;*(beta.)* gamma`), while the raw keystroke in the source view yields `Alpha X*(beta.)* gamma`, whose `*` is not left-flanking (CommonMark §6.2: followed by punctuation, preceded by a letter), so the reparse un-forms the run and the pane shows `Alpha X\*(beta.)\* gamma`. The letter-edge siblings (`X*beta*`) and every run's-end tuple agree byte-for-byte. Not a caret defect and possibly not a defect at all (the source view is the Markdown, and the user typed those bytes); the matrix pins each view's bytes and their shared semantics (the letter unmarked, no run holding it) for the three tuples. Revisit trigger: the next reconciliation that reads the two-view instrument (r7), or the first task that gives the source view any keystroke-time rewriting; hard stop `2.verify`. **After side, member N8 (DECISIONS #review-1-r8, found by the r8 reconciliation):** once task 1.63 places the source caret after a block-final run's closing delimiter on the cleared-marks route, the corpus's eight block-final marked runs that still disagree (`astral-between-runs.md` ×3, `astral-neighbour.md`, `hard-break-in-emphasis.md`, `spanning-mark-last-boundary.md`, `spanning-mark-strikethrough.md`, `strikethrough-punctuation.md`) all end in punctuation: the rendered view writes the letter as `&#x58;` (`a ~~(b)~~&#x58;`) and the raw source keystroke after the delimiter dissolves the run (`a \~\~(b)\~\~X`) — the same class on the run's other side; 1.63's corpus leg names it as its one exclusion and bounds it positively. Trigger and hard stop unchanged.
- [#030 product, a soft or hard break before inline html] **A user's line break before inline html does not survive a save unchanged.** Upstream's assembler (`mdast-util-to-markdown/lib/util/container-phrasing.js` 60–80) replaces the previous child's trailing line ending by one space before an `html` child so the html is not read as an html block; the app keeps that rewrite for a *soft* break (`alpha\n<i>beta</i>` is written `alpha <i>beta</i>`, a text value changed — DECISIONS #030 Decision 1: safe, over-broad, since a paragraph continuation line cannot start an html block of type 7), and — after task 1.58 (DECISIONS #review-1-r7 M2) — keeps a *hard* break's `\`+LF exactly when the pair reparses to itself, writing one space and no backslash when the html value can start an html block (CommonMark §4.6 conditions 1–6: `<div>`, `<!-- -->`, `<script>`, `<?…?>`, `<![CDATA[`; a documented loss, the break gone, never a stray byte; the bytes before 1.58 were `\ `, a literal backslash as text). Both are product decisions for Phase 2, with real files. Revisit: the first Phase 2 task that loads a file from disk into the editor decides whether the app pre-empts the rewrite altogether (a `containerPhrasing` wrapper that keeps the line ending whenever the html child cannot start a block — the soft-break fixture then enters the index byte-identical, invariant B's comparison unchanged) or keeps it and names the exception in invariant B's comparison (a text node's trailing line ending before an `html` sibling reads as a space; the block-capable hard-break member stays a named loss); the `[1.51]` and `[1.52]` lines above and `hard-break-before-inline-html.md` are its inputs. Hard stop: `2.verify`.
- [1.10.r7d, hard break inside a container] Found by the r7 reconciliation while probing the M2 fixture's blockquote member (DECISIONS #review-1-r7 M11), outside every reviewer's findings and outside 1.60's scope: after a hard break inside a blockquote or a list item — `> alpha\`+LF+`> beta`, `- alpha\`+LF+`  beta` — the ProseMirror position right after the `hard_break` maps through `cursorMap.toSource` to `{ line: 2, ch: 0 }` (the column before the continuation prefix `> ` or the item's indentation: the break's range ends at its own line ending, and `isLeafEnd` answers the leaf's last byte, `packages/editor/src/toggle.ts`) and `toRendered` of that answers the paragraph's *start*; the same with inline html after the break, and the identity at top level (`alpha\`+LF+`beta`) and after an image before a soft break. No fixture holds a hard break inside a container (`hard-break.md` and `hard-break-in-emphasis.md` are top-level), so the corpus inverse leg never met it; the M2 fixture was shaped to stay top-level. Fix shape: the leaf-end branch's member for a leaf whose range ends at a line ending answers the following node's start — the next line's first content column, after the container prefix. Revisit: the first task that adds a fixture holding a hard break inside a blockquote or list item (it will meet the inverse leg at that position and names this line), or the first Phase 2 task that touches `isLeafEnd` / the leaf-end branch of `toSource`, whichever comes first. Hard stop: `2.verify`.
- [review-1-r7 M7, Claude 6] `installAutolinkFallback`'s `link.peek` (`packages/core/src/format.ts`) serialises the whole link — children, `safe()`, the nested `containerPhrasing` — to answer one look-ahead character, and the real call serialises it again (a third time on the resource fallback); under `formatWithMap` each peek records and discards a full emission subtree. Correct (the answer is the same either way), three serialisations per link. Revisit: the first Phase 2 task that measures or touches the source `commit` path or `installAutolinkFallback` asks `originalPeek` first and serialises only when it answers `<`. Hard stop: `2.verify`.
- [review-1-r6 L11 — member M10, Claude's r7 finding 9] `p *a.*https://x.y/b**a.** q` — the GFM autolink literal swallows `**a` into the url, so the parser's tree is `emphasis[text "a.", emphasis[link, text "."]]`; `format` writes `p *a.*<https://x.y/b**a>.** q`, whose parse dissolves the inner run, and the second pass writes `.\*\* q`: invariant A fails on the parser's own tree (reproduced by the r7 reconciliation). A URL glued to a `**` run with no space; the same class as the line above (attention resolution against the serializer's escaping, upstream), the same trigger and hard stop (`2.verify`).
- [1.46, evidence — fourth instance of the class, on Linux] Task 1.54's integrated attempt (`/logs/tasks/1.54/1.log`, tool 33; DECISIONS #review-1-r7 M3) ran the e2e/web suite once in the container, in parallel with its own build, and `editor-soft-line-breaks.spec.ts` G4 read offset **11** where 5 was expected after eleven polled `ArrowLeft` presses — the same shape as the windows-latest failures of #029, #035 and #036, on ubuntu, so the class is not a windows-only one and "the container cannot observe that runner" (#035, #036) was the wrong premise: a native caret motion reverted by ProseMirror before the next synthetic key reproduces wherever the run is contended. 1.56's click route is the right route for that reason too; revisit trigger and hard stop unchanged (`2.verify`).
- [review-1-r7, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r7 chain ran at 50 turns under the journal-stub rule; counts from the transcripts' result lines: 1.49 (opus) 38 at a1; 1.50 (opus) a1 51 capped, a2 25; 1.51 (opus) a1 51 capped, a2 43 and a3 9 withheld on #030's conflict, STUCK, retry a1 37 withheld on #030's bytes (#031), a2 13, a3 19; 1.52 (opus) a1 51 capped, a2 22 withheld (#032), a3 23; 1.53 (opus) a1 39 and a2 1 on the session limit (429, #027's third and fourth instances), a3 27 withheld (#033), STUCK, retry a1 13; 1.54 (sonnet) 51 / 51 (no stub) / 51, STUCK, retry a1 51 capped and integrated (M3); 1.verify.r7 (sonnet) 41; 1.55 (opus) 33; 1.verify.r7.g1 (sonnet) 24; 1.56 (sonnet) 50; 1.verify.r7.g2 (sonnet) 21. Phase 1 to date from `[ralph] <id> attempt … (loop` lines (unique ids, verify and gate-repair tasks included): 68 loop task ids, 31 needing more than one attempt; NO-JOURNAL with a Phase 1 id 16 (four 429s); STUCK 10. Reviewers: Claude 128 turns, Grok 26, Sol no count. The 80-turn raise stays conditional on Michael's OK at the boundary (DECISIONS #review-1-r7).
- [review-1-r7, reviewer record] Counted at #review-1-r7 from three independent reports (Grok started with its siblings; #025's condition met): unique findings Claude 8 of 9, Sol 1 of 2, Grok 0 of 0; one shared finding (the hard break before html, Claude–Sol, blocker / blocker, agreed); two blocker families, one shared by Claude and Sol and one unique to Claude, Grok absent from both; cumulative over twenty-six shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2 — unchanged; Grok's PASS-with-blockers count becomes 3 of 5 counted attempts (r2, r3, r7). Whether Grok's read-only mode continues is Michael's call between review sets (the r8 rows are written for three reviewers; a change is a planning commit, never an edit); no change inside a review set.
- [1.59, found outside scope] **A caret inside an inline code span does not survive the toggle back from the source view**: it comes back at the span's start. `canonicalCursor` (`packages/editor/src/toggle.ts` 638–664) translates the carried source position through a spelling table only when the innermost live node is a `text` node (`inside.node.type === "text"`, line 650); for every other node type it falls to `{ line: range.startLine, ch: range.startCol - 1 }` (line 658), and for an `inlineCode` node that is the opening fence. Pipe-independent and table-independent, reproduced headlessly at this task's tree: `canonicalCursor("| h      |\n| ------ |\n| `a\|b` |\n", { line: 3, ch: 3 })` (after `a`) answers `{ line: 3, ch: 2 }`, and `canonicalCursor("z `ab` y\n", { line: 1, ch: 4 })` (after `a`, no table and no pipe) answers `{ line: 1, ch: 2 }`, while a plain-text position on the same line is exact. In the browser the consequence is a keystroke after the round trip landing at the span's start (`e2e/web/editor-toggle-cell-code-pipe.spec.ts`, the second case, asserts it positively and by name: `` `a\|b` `` + `X` gives `` `Xa\|b` ``, not `` `aX\|b` ``). Not a byte or round-trip defect — every result is a parse∘format fixed point — and not task 1.59's M4 family, whose own direction (`cursorMap.toSource`, rendered → source) this task fixed and whose fix is the prerequisite for the repair: `inlineCodeSpelling` now yields the in-cell table `canonicalCursor` would need. The repair is one branch beside line 650, reading `spellingOffsets`' inline-code counterpart for the live bytes, and it belongs with M5's block-final inline-code end (task 1.60), which touches the same two views' disagreement about a code span's edges. Revisit: task 1.60's planning, or Phase 2 planning with the cursor-map work if 1.60's scope does not reach it; hard stop `2.verify`.
- [1.62, evidence — a second race of the `[1.46, found outside scope]` class] The 1.verify.r8h gate (ci.yml run 35755695990 at `b7f45a5`: ubuntu-latest 97/1, windows-latest 96/2, macos-latest 98/98) failed two preconditions that clicked to a caret beside a reveal widget — `editor-toggle-code-span-end.spec.ts:127` (`{ text: "foo", offset: 3 }` expected, `{ text: null, offset: 4 }` received on both) and `editor-toggle-cell-code-pipe.spec.ts:167` in `seedWithCaretAfterA` (windows-latest only, `{ text: "a|b", offset: 0 }` expected, `{ text: null, offset: 0 }` received) — not the observer reading a native motion late, as `[1.46, found outside scope]` is, but the DOM selection re-spelled after a click at a widget boundary: prosemirror-view's own `NodeViewDesc.domFromPos` (1.42.3, line 922) spells a caret beside a `.essaydown-delimiter` reveal widget (task 1.4; `contentEditable = "false"`, a positive `side` before the opening backtick and a negative one after the closing) on the parent element rather than the `<code>` text node Blink's hit test anchors on, and which spelling the DOM carries depends on whether `handlers.focus`'s 20 ms `selectionToDOM` resync (line 3904) ran before the poll — a race neither a longer poll nor a later read settles, because both spellings are stable and both resolve to the same document position. This task's fix (the dev bar's `data-testid="selection"` readout, `apps/desktop/src/dev/DevEditor.tsx`) reads the editor's own selection instead of the DOM's, which removes the race for these two specs; the class remains open for any future Playwright precondition that reads a DOM selection at a run's edge rather than the editor's own state. Revisit and hard stop unchanged (`2.verify`).

- [review-1-r8 N5, React 19] `apps/desktop/package.json` holds `react` and `react-dom` at `^19.1.0` while PRD §4's Frontend row says React 18, and `docs/dependencies.json` justifies both entries by that row; `scripts/check-deps.mjs` certifies each allowlisted name against its cited row and never the major version, so the drift has passed "zero dependencies beyond PRD §4" at every attempt (Claude's r8 finding 3, nit). Nothing is broken: React 19 is what the Tauri scaffold installed before the phase. Two conditional items for the phase boundary, Michael's OK: a DECISIONS note amending the §4 row to React 19 (as #review-1-r0 (a) ratified `zod`), and a `chore` making `check-deps` compare the installed major against the row it cites so a silent bump is red. Revisit: the boundary idle window after `1.close`, or the first Phase 2 task that touches `apps/desktop/package.json`. Hard stop: `2.verify`.
- [review-1-r8 N7, ci.yml merge step] `.github/workflows/ci.yml`'s `merge-logs` job carries `if: always()` and its second merge step (the `playwright-report` artifact) does not, so a failed `test-logs` merge skips it and a gate's fetch fails on a missing artifact that the evidence cannot tell from an empty report (Claude's r8 finding 5, nit). Not reachable in any accepted run (both artifacts present, digests matched, nine Phase 1 gates). `ci.yml` belongs to passed task 0.2 and a workflow change moves the SHA, so it is never edited inside a chain. Revisit: the next `.g<n>` repair that touches `ci.yml`, or the boundary idle window after `1.close` (a `chore(0.2)`-shaped commit on the next phase branch, Michael's OK). Hard stop: `2.verify`.
- [review-1-r8, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r8 chain ran at 50 turns under the journal-stub rule; counts from the transcripts' result lines (handoffs 029, 030): 1.57 (opus) a1 51 capped with the work built, a2 32; 1.58 (opus) 51, 31; 1.59 (opus) 51, 44 (two probe files swept onto the branch by the recovery commit, removed at a2); 1.60 (opus) 51, 34; 1.61 (sonnet) 51, 61; 1.verify.r8 (sonnet) 43; 1.62 (sonnet, the #037 gate repair) 51 capped, 51 with DONE on the last turn; 1.verify.r8.g1 (sonnet) 25. Every fix task of the chain and its repair capped at a1 with the work built and passed at a2 — with the r7 table, ten of eleven opus fix tasks over two chains; no `retry`, no 429, no `STUCK`, no `NO-JOURNAL` in the chain. Phase 1 to date from `[ralph] <id> attempt … (loop` lines (unique ids, verify and gate-repair tasks included): 76 loop task ids, 37 needed more than one attempt; `NO-JOURNAL` 16; `STUCK` 10. Reviewers: Claude 113 turns, Grok 22, Sol uncounted. The raise stays conditional on Michael's OK at the boundary.
- [review-1-r8, reviewer record] Counted at #review-1-r8 from three independent reports (started within 198 ms; #025's condition met): unique findings Claude 5 of 5, Sol 2 of 2, Grok 0 of 0; no shared finding, no disagreement; cumulative over twenty-six shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2 — unchanged; Grok's PASS-with-blockers count stays 3 of 5 (no blocker in any r8 report). New fact, evidence not decision: Grok's r8 PASS carries zero findings beside two should-fix marked Required that reproduce headlessly (Sol's), the first attempt with that shape; the r9c row asks Grok which read of the tree would have found each. Whether Grok's read-only mode continues is Michael's call between review sets; no change inside a review set.

- [1.63, found outside scope] **The block-*start* twin of task 1.60's block-end inline-code rule.** `isLeafEnd` (`packages/editor/src/toggle.ts`) is a rule for a leaf's *end* — an atom's, an `inlineCode` run's — and has no start twin, and task 1.63's block-edge rule is about the marks *enclosing* a leaf, which an `inlineCode` run at a block's start has none of. So on the input-rule route (stored marks `[]`) at a block start whose first inline node is a bare `inline_code` run, the rendered view types plain text *before* the span while `toSource` answers inside the run's value, past its opening fence, and the two views write different bytes for one caret. Found by task 1.63's own corpus leg (`packages/editor/test/toggle.test.ts`, "the block-edge rule over the corpus"), where it is the second named, positively bounded exclusion — three positions in two fixtures: `code-span-references.md` line 7 col 1 and `table-inline-code-pipe.md` line 3 col 3 and line 4 col 19. Not in task 1.63's scope: its task text says the leaf's own rule (`isLeafEnd`, 1.52/1.60) is unchanged and its item (1) is the one rule in `toSource` for enclosing marks, and `isLeafEnd`'s signature is fixed by the task. The fix is the mirror clause — at a block start a leaf with a spelling table is answered by that table only when the typed marks carry the leaf's mark, and by the leaf's *start* (before the opening fence) when they do not — with the guard family enumerated over both edges rather than the end alone. Revisit: the next review set that reopens the cursor map (the first `1.10.r<n>d` reconciliation after r8), or the first Phase 2 task that touches `isLeafEnd`. Hard stop: nothing else in Phase 1 is changed for it, and the exclusion is never widened to hide another class — a new member of it fails the leg.

- [1.64, found outside scope] **A block-final mark whose last child is a link: the rendered caret types inside the run, the source column is past its closing delimiter.** Found by task 1.64's new corpus leg `deleteAtEveryBlockStart` (`packages/editor/test/position-map-inverse.test.ts`), which sweeps both views' bytes rather than the inverse alone, and reproduced **unchanged at the branch base `5fb4163`** (the three positions are in blocks where the conversion drops nothing, so 1.64's correspondence change is provably the identity there — it is not this task's defect and not this task's scope, whose item (1) is the kept-character map and whose item (2) is the leaf-interior arithmetic). The shape: a block ending `*…[text](essay.md)*` (`link-in-emphasis.md`, positions 30, 162, 262 after the leg's transaction). At the block's end `doc.resolve(pos).marks()` is `[emphasis]` — the `link` mark is `inclusive: false` in `schema.ts` and drops, the `emphasis` does not — so task 1.63's `markEdgePoint` correctly finds the enclosing mark *carried* and answers `null`, and the older `containerEnd`/`isLeafEnd` rule then places the caret at the **emphasis entry's own range end**, one column past the run's closing `*`. The rendered view types inside (`[opening line ](essay.md)X*`, the run kept); the raw source keystroke lands after the delimiter, where `*` is preceded by punctuation and followed by a letter, so CommonMark §6.2 makes it non-closing, the run dissolves, and the pane shows `\*…\*X`. The right column is the one *before* the closing delimiter — the position the carried mark implies — which is a rule for the *carried* branch of 1.63's `markEdgePoint`, the branch that currently returns `null` and defers. Revisit: the first Phase 2 task that touches `markEdgePoint` or `containerEnd` gives the carried branch its own answer (the innermost carried entry's range end minus that mark's closing delimiter width, read from the delimiter table rather than assumed) and asserts it over the corpus on both of 1.64's destructive legs; until then 1.64's leg bounds the three positions positively as members of this class (DECISIONS #032) and never as expected failures. Hard stop: `2.verify`.

- [1.65, found outside scope] **`breakSurvivesBeforeHtml`'s non-paragraph guard is unreachable by construction, and is the one statement of `packages/core/src/format.ts` no test covers.** `if (block.type !== "paragraph") return false;` (format.ts:297 at this task's tree, task 1.58's probe) can never be true: the probe string the function parses is `` `x${breakOutput}${htmlValue} y\n` `` and begins with the letter `x`, so its first block is a paragraph for every value of the two interpolations — an html value that can open an html block (conditions 1–6) ends the paragraph at the break and is caught one line earlier by `root.children.length !== 1`. Read out of the coverage JSON at this task's tree (`coverage-final.json`, the `s`/`b` maps, not the text row): the only uncovered statement in the file is that `return false` and the only uncovered branch is its true side, 0 of 273 hits; at the branch base `feefa73` the same statement is uncovered at line 253 beside the two this task covered (432 `locateChild`'s `return undefined`, 0 of 266; 515 `widenSplitSurrogateReferences`' `break`, 0 of 69,923). Not this task's scope — its item (3) is the walk's give-up and its item (4) `applyEdits`' overlap skip, and `breakSurvivesBeforeHtml` is passed task 1.58's, whose record is not edited. Nothing is broken: the guard is defensive and its false side runs on every fixture that reaches the probe. The repair is a decision between two shapes, which is why it is filed rather than taken here: either delete the guard and state the invariant in the doc comment (the probe's own first character makes the block a paragraph), or keep it and give it a test by exporting the probe and handing it a `breakOutput` that does not leave `x` in a paragraph — which no serializer output is. Revisit: the first Phase 2 task that touches `breakSurvivesBeforeHtml` or the hard-break-before-html family, or the boundary idle window after `1.close`. Hard stop: `2.verify`.

- [review-1-r9, CI timing] **A per-test budget for a whole-document per-position oracle measured in this container does not transfer to GitHub's runners.** Task 1.63's block-edge corpus leg (`packages/editor/test/toggle.test.ts`, "the block-edge rule over the corpus") ran `typedInRendered`/`typedInSource` — each a full `format ∘ pmToMdast` or `format ∘ parse` of the *whole document* — twice per textblock edge and per route, and the essay fixture alone carries a few hundred edges: 7,784 ms in this container (task 1.63's own run) against 59,906 ms on the accepted macOS runner, 86,341 ms on ubuntu and 96,152 ms on windows at `1.verify.r9h`'s CI gate (ci.yml run 35894799995 at `29d2e08`) — 8–12× the container's own reading, over the 60,000 ms budget the leg had copied from one machine (the same class as lesson [1.64] and DECISIONS #review-1-r7 M6). Task 1.66 fixed it the way task 1.64 fixed its own leg: `blockAlone` (lifted into the new shared `packages/editor/test/block-alone.ts`) cuts each swept textblock out with its containers kept as open ancestors, so one `format`/`pmToMdast`/`cursorMap` of the block alone answers every edge and route of that block, and the whole-document bytes are recovered by bridging each compared position through a per-block, per-line constant shift (`packages/editor/test/toggle.test.ts`'s `shifts` map, keyed by the block's own position beside its block-local line — not by the block-local line alone, which collides across a fixture's unrelated blocks holding the same line number). The essay-fixture leg now runs in 159 ms in this container, inside the family's usual 30,000 ms budget on any of the three runners. **After this fix, `packages/editor/test` has no remaining whole-document per-position oracle**: `grep -n 'it.each(names)\|for (const name of names)' packages/editor/test/*.ts` finds nine corpus loops, and every one either calls `map.toSource`/`toRendered` alone (`toggle.test.ts`'s "cursorMap over the corpus", a strided sweep with no serialization per position) or a bounded number of whole-document `format`/`parse` calls per fixture — once or twice per transaction, never per position (`editor-fixed-point.test.ts`'s two loops, `position-map-inverse.test.ts`'s two `deleteAtEveryBlockEnd`/`deleteBesideEveryMarkedRun` and `deleteAtEveryBlockStart` (already block-local, task 1.64) describe blocks, `position-map-editor-leg.test.ts`'s two loops, `schema-roundtrip.test.ts`'s one). Filed as a decision record, not a defect to fix: nothing here is broken, and the class is closed for `packages/editor/test` as it stands today. Revisit: before `2.verify`, and whenever a new corpus leg is added under `packages/editor/test`, its author reads this line and checks the new leg is block-local from the start rather than discovering the timing gap at a CI gate. Hard stop: `2.verify`.

- [review-1-r9 O1 — correction of the `[review-1-r6 L11]` line above] That line's "unreachable from the editor (adjacent same-mark text merges into one run)" is true of **same**-mark runs and false of **different**-mark runs, which nothing merges: `emphasis("(b)") + strong("z")` serialises `*(b)***z**` and parses to `text("*(b)*") + strong("z")`, reached by one Backspace at the start of the second of two paragraphs or one Delete of the separating space (Claude's r9 finding 1, driven in Chromium at `287c884`). The different-mark class is task **1.67**'s (app `emphasis`/`strong` handlers, the 75-pair sweep, the join and separator-deletion legs); the same-mark member reachable from a file keeps L11's trigger and hard stop, and 1.67's journal records whether its handler makes L11's own reproduction a fixed point. Lesson carried: a deferral that says "unreachable from the editor" names the commands it was read against, and the next review re-drives the route through the keymap the app installs.
- [review-1-r9 O2] **The block-local byte legs' bridge to the document is asserted against itself** (Claude's r9 finding 2, should-fix, backlogged under DECISIONS #041 D1): `packages/editor/test/position-map-inverse.test.ts:583` and `packages/editor/test/toggle.test.ts:2009` compute `shift = { line: source.line, ch: source.ch - inBlock.ch }` and assert only that it is the same across the routes and positions sharing a block-local line, so a per-line constant error in the document map (a container prefix, an ordered marker widening at item 10) passes both legs. True today by Claude's document-level sweeps A and B (5,309 comparisons, 0 disagreements outside the recorded classes): guard power, not a live defect. Fix: assert the bridge against the document's bytes (`document.split("\n")[source.line - 1].slice(shift.ch)` starts the block-alone line's text) or run `typedInSource(documentText, source)` once per block line. Revisit: the first Phase 2 task that touches either leg or the document map's container prefixes, or `2.verify`. Hard stop: `2.verify`.
- [review-1-r9 O4] **`buildMap` never reports an `overlapping-edit` give-up in any test** (Claude's r9 finding 4, should-fix, backlogged under D1): `packages/core/src/positions.ts:1060`'s ternary arm for `overlapping-edit` is the file's uncovered branch — `format-widening-giveup.test.ts:145,154` call `applyEdits` directly — so a mutation deleting that half of the reporting loop stays green, the N3 promote bullet's own clause unmet for one of its two reasons; nothing reaches the branch today. Fix: drive one `overlapping-edit` through `formatWithMap` (a seam handing `buildMap` a recorded give-up, or a `RecordedChild` list that becomes two overlapping edits), or collapse the reason to a recorded-unreachable at `applyEdits` and say so where the map is built. Revisit: the first Phase 2 task that touches `applyEdits`, `buildMap` or the widening walk, or `2.verify`. Hard stop: `2.verify`.
- [review-1-r9 O5–O6] **Four uncovered defensive arms in the cursor map** (Claude's r9 nits 5 and 6): `packages/editor/src/toggle.ts:414` and `:420` (`leafOffset`/`leafPosition`'s `entry.inline === undefined`, defended by a doc-comment sentence — N3's shape) and `packages/editor/src/schema.ts:1042` and `:1055` (`liveOf`'s `offset < 0`, `liveEndOf`'s `offset <= 0`, no caller reaches them). Fix: narrow the parameter types and drop the arms, or give each the one guard that reaches it and state the ownership answer it encodes. Revisit: the first Phase 2 task that touches either function, or `2.verify`. Hard stop: `2.verify`.
- [review-1-r9 O7] **An interior boundary whose typed marks match neither neighbouring run is assigned to the following run** (Sol's r9 finding 1, should-fix Required, backlogged under D1: both views round-trip; the typed character's formatting differs between the views, visible in the pane as typed): `packages/editor/src/toggle.ts:508` (`innermostAt` distinguishes "equals before" from everything else). Native route: load `q [ab](u)*cd* r`, click inside the link, ArrowRight to its end (readout `before: "q ab", marks: [], stored: null`); direct `X` copies `q [ab](u)X*cd* r`, toggle then `X` copies `q [ab](u)*Xcd* r`. Carried-subset twin: `q *x [ab](u)* r` at the link's end copies `q *x [ab](u)X* r` direct and `q \*x [ab](u)\*X r` after the toggle. Evidence `.evidence/reviews/1/r9/sol/{boundary-browser.mjs,boundary-browser.log,sweep.json}`. Fix: resolve an interior boundary per carried mark; guards for a non-inclusive link followed by each mark, shared enclosing marks, both edges, astral text, both routes. Revisit: the first Phase 2 task that touches `toSource`'s interior-boundary branch, or `2.verify`. Hard stop: `2.verify`.
- [review-1-r9 O8] **The dropped-whitespace shortcut discards a mark the caret still carries** (Sol's r9 finding 2, should-fix Required, backlogged under D1, severity as O7): `packages/editor/src/toggle.ts:675` (`beyondDroppedWhitespace` → `betweenNodes` returns the outer emphasis's start and bypasses `markEdgePoint`). Native route: load `` *x `bc`* ``, ArrowUp to the paragraph start, Delete `x`, ArrowRight past the surviving space (readout `before: " ", marks: ["emphasis"], stored: null`); direct `X` copies `` *X`bc`* ``, toggle then `X` copies `` X\*`bc`\* ``. Evidence `.evidence/reviews/1/r9/sol/{marked-gap-browser.mjs,marked-gap-browser.log}`; strong, delete and nested members in Sol's sweep. Fix: carry the typed-mark set through the dropped-gap resolution; destructive two-view guards across the textblock containers, bytes beside the inverse. Revisit: as O7. Hard stop: `2.verify`.
- [review-1-r9 O9 — supersedes the `[1.63, found outside scope]` line's trigger, which fired at #review-1-r9] **The leaf-start twin of the code-span end rule, reached by an ordinary Delete** (Sol's r9 finding 3, should-fix Required, backlogged under D1, severity as O7): `packages/editor/src/toggle.ts:692`. Native route: load `` a`bc` ``, ArrowUp, Delete `a` (readout `marks: ["inline_code"], stored: []`); direct `X` copies `` X`bc` ``, toggle then `X` copies `` `Xbc` ``. Evidence `.evidence/reviews/1/r9/sol/{code-start-browser.mjs,code-start-browser.log}`. The fix is the `[1.63]` line's mirror clause; 1.63's corpus exclusion 2 becomes equivalence after it, with the native Delete route, both edges and routes, each textblock container and non-BMP content. Revisit: the first Phase 2 task that touches `isLeafEnd` or the block-edge rule, or `2.verify`. Hard stop: `2.verify`.
- [repair-1.9 (c) — fired at #review-1-r9] The next observation gate is `1.9.r1` (DECISIONS #040), whose text names two readers with different engines: Typora and BBEdit's preview with its CommonMark (cmark) processor. Markdown.pl, this line's other example, reads no GFM and was not chosen.
- [review-1-r9, turn budget — evidence only; the `[review-1-r1, turn budget]` decision unchanged] The r9 chain ran at 50 turns; counts from the transcripts' result lines, the moved-aside logs included: 1.63 (opus) a1 51 capped with the work built, a2 40; 1.64 (opus) six caps at 51 across two `retry`s (#038), then 25; 1.65 (opus) 51, 26; 1.verify.r9 (sonnet) 45; 1.66 (sonnet, the #039 gate repair) 51, 51, 31; 1.verify.r9.g1 (sonnet) 29. Every opus fix task capped at a1 — thirteen of fourteen over the r7–r9 chains. Reviewers: Claude 113 turns, Grok 20, Sol uncounted. The raise stays conditional on Michael's OK at the boundary.
- [review-1-r9, reviewer record] Counted at #review-1-r9 from three independent reports (started within 221 ms): unique findings Claude 6 of 6, Sol 3 of 3, Grok 0 of 0; no shared finding, no disagreement; cumulative unchanged (twenty-six shared, eight disagreements, low-rater Grok 6, Claude 2, Sol 2); Grok's PASS-with-blockers count becomes 4 of 6 counted attempts (r2, r3, r7, r9). Fact, not decision: Grok's third consecutive zero-finding PASS (r7, r8, r9), with no build, test or browser run at r9. Whether Grok's read-only mode continues is Michael's call at the boundary; no change inside a review set.
- [1.67, found outside scope] **The `~~` twin of #review-1-r9 O1: two adjacent `delete` runs still share one delimiter run, and `~` has no second marker to take.** Task 1.67 gave `emphasis` and `strong` app handlers that open with `_`/`__` when the run beside them just closed with the `*`/`**` they would have taken; `handleDelete` (task 1.40) cannot do the same, because GFM's strikethrough construct has exactly one marker. So a tree holding `delete("a.") + delete("b")` as two siblings still serialises `~~a.~~~~b~~`, which parses to a single `delete` whose text is `a.~~~~b`. **Positively bounded, and out of every route this task owns**: neither the parser nor the editor can build that tree — micromark reads `~~a.~~~~b~~` as one run (probed at this task's tree: `format(parse("~~a.~~~~b~~\n"))` is `~~a.\~\~\~\~b~~\n`, one `delete`), and ProseMirror merges adjacent text nodes with equal mark sets, so a Backspace join or a Delete of the separator between `~~a.~~` and `~~b~~` yields one run, which is exactly the same-mark half of `[review-1-r6 L11]`'s premise that stands. Task 1.67's sweep therefore cannot reach it: its `delete + delete` cells are one merged run in the editor's own tree, asserted as such. The class is reachable only from a hand-built mdast tree — an importer, a coach edit, a future transform that splices two `delete` nodes. Fix, when a route exists: encode the first character of the second run's content as a character reference (`~~a.~~~~&#x62;~~` does not parse as one run) or separate the two runs' delimiters the way the `*` handlers separate theirs, with the guard set enumerated over both neighbour classes on both runs. Revisit: the first task that introduces an mdast producer other than `parse` and `pmToMdast` (a Phase 3 import path, the coach's patch application), or `2.verify`, whichever comes first. Hard stop: `2.verify` — nothing in Phase 1 is changed for it.
