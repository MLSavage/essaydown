# progress.md — Essay Down

## Current state (≤ 6 lines; once the loop runs this file is regenerated only in reconciliation commits)

Phase: pre-bootstrap. Branch: main (planning docs only). Last passing task: none. Next step: docs/PREFLIGHT.md checklist, then `ralph/bootstrap.sh` (task 0.0).
Blockers: none in planning — LOCKED 2026-09-05 at v14; next is docs/PREFLIGHT.md §0 on the Mac Mini, then handoff 001. PREFLIGHT items open (repos, CLI logins, fixture essay).
Open human gates: none yet.

## Entries (planning-phase journal; once the loop starts, agents append to docs/progress/journal-main.md and this file is regenerated by ralph.sh)

- [plan] 2026-09-03T15:00:00+03:00 PRD v1 written; independent Opus review returned 20 findings (6 blockers); v2 applied all 20; v3 added execution model (§12), container, Sol/Grok reviewers, claude-cli coach provider, tasks 0.0, 5.3c, 6.5. Iterations: n/a. First attempt: n/a.
- [plan] 2026-09-03T18:30:00+03:00 Sol PRD review (19 findings, 9 blockers) applied → v4: review-set macro (Claude+Sol+Grok+reconcile, one commit each), Milkdown bake-off deleted, 1.2 = position map, 13-char hash, Phase 2 Mac human gate 2.9, 6.2 re-planning gate, Phase 6 split into 6.4–6.9, keyring for secrets, yaml opaque-except-app-keys, deterministic anchoring, claude-cli flags pinned, LibreOffice smoke test dropped, sections renumbered. 62 tasks, graph validated. Lessons mined from takeoff-engine and quote-engine into lessons.md; BUILD-DEFAULTS 1.1.0.
- [plan] 2026-09-03T21:00:00+03:00 Sol PRD audit #2 (20 findings, 18 blockers, 5 gaps) applied → v5: WorkspaceRoot path authority + escape tests; honest duplicate-anchor semantics with `pos`; `execution` field (loop/interactive-principal/human), blockedOnHuman only on human tasks; task/<id> branches + ralph.sh integration + wip squash to one acceptance commit; container never pushes (6.6h, 6.9h human push tasks; logs/ci via host script); Phase 5 decoupled from release, provider===null gating, 5.3b/5.3d/5.3e split; `grok -p` verified against docs.x.ai; externalBin two base entries + shell arg scope; sidecars.lock.json + poppler pinned; rename rewrites image URLs, delete trashes trio, collision-safe image names; key commands in §6.4 with no secret over IPC; 3.1 injected-mutation undo test; nested-paragraph scope and replacement semantics stated; toggle no longer pushes undo snapshot; stream order fixed. 66 raw tasks → 87 expanded, acyclic, human flags consistent.
- [plan] 2026-09-03T23:30:00+03:00 Sol PRD audit #3 (20 findings, 11 blockers) applied → v6: needsCI CI-gate expansion (11 gates), N.close runner tasks for all 7 phases with next-phase branches cut from main, /work/.evidence mounted as /logs, parallel reviewers from one review_sha with reports outside the repo, severity rule with replacement review sets, flock + CAS integration, rc2 packaging before the install gate, v0.1.0 tag on the main SHA at 6.close, Phase 5 optional track (6.1 depends on 4.close), root-relative path contract, coach anchors required (no essay scope), YAML rewrite boundary + 5 new fixtures (45 total), 1.3/1.7 undo fixes, slices in scripts/.cache, DOCX validation with pinned tooling, 6.9a/6.9b/6.9h split, revert-with-dependents rule, journals + generated progress.md, 0.0 boundary tests in a fixture repo. 77 raw → 109 expanded, acyclic.
- [plan] 2026-09-04T09:00:00+03:00 Sol PRD audit #4 (20 findings, 13 blockers) applied → v7: docs/RUNNER-SPEC.md created as the canonical protocol (immutable spec + .evidence/state, execution modes loop/loop-external/reviewer/interactive-principal/human/runner, integration transaction with detached candidate + CAS, three SHAs, gate.sh + record tasks, N.verify CI gates, phase close semantics, branch topology with post/0.1); PRD §10.2 rewritten as a summary; coach ships in v0.2.0; release chronology fixed (notes before review, rc2 tag on bump commit, v0.1.0 at 6.close, post phase for retro); context-only claude-cli sandbox; baseline approval gate 6.3v; Phase 4 actor fixes; 0.h prerequisites gate; fixture counts 45; terminology sweep. 88 raw → 149 expanded (88 raw + 21 review + 30 CI gate&record + 10 human record), acyclic. BUILD-DEFAULTS 1.4.0.
- [plan] 2026-09-04T12:00:00+03:00 Sol joint audit #5 (20 findings, 16 blockers) applied → v8 + RUNNER-SPEC 1.1: phases strictly sequential with one lock; 0.0 is `bootstrap` (host, after docs/PREFLIGHT.md), 0.h removed from the graph; human gates evidence-only, copied into DECISIONS by reconciliation under `recordTarget` (no record commits → verification SHA = implementation SHA, asserted); review attempts `<id>.rN`; sync-state after planning commits; recovery commands; `ci` objects on all 15 CI gates; `replan` mode for 6.2m; versions set before tags (6.6 → 0.1.0, 5.version → 0.2.0) with the release invariant at close; Phase 5 = coach + retrospective after 6.close on phase/5 → v0.2.0 (no post/ branch, no v0.1.1); loop-external transaction; coach sandbox with no settings sources and user-level hook tests; 1.1 count fixed. 87 raw → 123 expanded, acyclic. BUILD-DEFAULTS 1.5.0.
- [plan] 2026-09-04T15:00:00+03:00 Sol joint audit #6 (18 findings, 10 blockers) applied → v9 + RUNNER-SPEC 1.2: transactional close (source-branch check, detached test, one ref transaction for main + tag, evidence after); per-attempt verifiers N.verify.r<k> with CI gates; verdict separate from task status; CI gates recordPolicy evidence-only; immutable ci/<id>/r<n> refs; gate outcomes ACCEPT/REJECT/GATE-FAILED with principal re-plan; split release invariant (close-time vs publication); fast-forward-only closes; replanTarget on 6.2m; external fields on post.3; 5.0 pins the CLI argv (DECISIONS #007) and 5.3c cites it; 1.9/5.3b/post.2 evidence-only; RETRO-CARRYFORWARD for Phase 5 review lessons; sequential implementation (no --stream); paths normalized to r<k>. 88 raw → 124 expanded, acyclic. BUILD-DEFAULTS 1.6.0.
- [plan] 2026-09-04T18:00:00+03:00 Sol joint audit #7 (20 findings, 11 blockers) applied → v10 + RUNNER-SPEC 1.3: runner-native plan-gate; outcome in runtime state (dependency = passed + ACCEPT); r0-based attempts with next = k+1; idempotent close (intent record, one ref transaction incl. next branch and tag with zero-OID expected-old, finalise after, safe retry); phases.json base records; releaseVersion + approvedCandidateGate resolved from newest ACCEPT; verifier CI gates of release phases are final-package gates (release-candidate.yml on ci refs); hotfix mini-phase for post-tag failures; 6.1 sets rc1 versions; 5.0 depends on 6.6h; gateKind observation/approval/ci/publication; ci refTemplate ci/<id>/a{n} with gate_attempts ordinal; bootstrap initialises external state and phase base; verifier_id cited by reviewers; generic release-notes finalisation; 4.0 spike ref wording; close tasks 0–4 rewritten; journal-main everywhere; no per-iteration rebase. 88 raw → 124 expanded, acyclic. BUILD-DEFAULTS 1.7.0.
- [plan] 2026-09-04T21:00:00+03:00 Sol joint audit #8 (14 findings, 9 blockers) applied → v11 + RUNNER-SPEC 1.4: nextPhase/nextBranch on closes (4→6→5→terminal); plan requests as first-class state with branch, states and recovery; a<n> execution attempts vs .r<n> graph revisions with exact rewrite rules; accepted.json manifests for CI and human evidence, all consumers updated; candidate resolved from the newest passing attempt's verifier with sha == verification_sha; honest candidate/tag statement; hotfix as a planned phase via plan request with supersession; versioned close-record schema; physical r0 review ids; risk-triggered reviews removed; conformance suite in bootstrap (§12); open questions resolved into docs/DECISIONS.md #000; PRD sweep (paths, §11 refs, entry points). 88 raw → 124 expanded, acyclic. BUILD-DEFAULTS 1.8.0.
- [plan] 2026-09-05T00:30:00+03:00 Sol joint audit #9 (14 findings, 6 blockers) applied → v12 + RUNNER-SPEC 1.5: publication gates carry outcomes and CI evidence; verifier repairs in g<n>, review attempts in r<n>; patch phases created by `plan --new-phase` on a runner-created branch, successor-branch advance in the close transaction, no `open` task; plan-request transition table with blocked/integration-failed and provenance fields; RC packaging on ci refs (no RC tags anywhere; 6.1h/6.3bh now ci gates); completed-close re-entry; human accepted.json schema with payload fields (5.3d/5.4/post.3/3.2/6.3a read named fields); scoped host rule; stream order; r0/review-N-r<k> references; RUNNER-SPEC in reviewer inputs. 88 raw → 124 expanded, acyclic. BUILD-DEFAULTS 1.9.0.
- [plan] 2026-09-05T03:00:00+03:00 Sol joint audit #10 (15 findings, 8 blockers) applied → v13 + RUNNER-SPEC 1.6: patch-phase machinery removed (publication gates trailing with outcomes ACCEPT/GATE-FAILED→halt; 5.0 depends on 6.close; PUBLICATION-FAILED halts for a principal decision); integration/external/gate-transition intents with re-entry rules; verifier grammar N.verify[.r<k>][.g<n>] with rewiring of whichever unstarted attempt; plan requests gate-repair only; 6.2 measure.yml downloads rc1 artifacts from the producer run by id with hash checks; 6.6h attempt-scoped evidence; runtime row, host exceptions, API-key wording, reviewer inputs, r0 anchors. 88 raw → 124 expanded, acyclic. BUILD-DEFAULTS 1.10.0.
- [plan] 2026-09-05T06:00:00+03:00 Sol joint audit #11 (9 findings, 5 blockers, all in the runner) applied by scope cuts per the audit budget → v14 + RUNNER-SPEC 1.7: publication gates removed (6.6h, post.h deleted; manual docs/PUBLISH.md; no `publication` gateKind); intents removed (candidate/<id> tag before every ref move, run.json before every remote wait, `ralph.sh doctor` + `admin mark-integrated|retry|resolve-request`, `gate.sh --resume|abandon|gc`); run.json/accepted.json with run_id and digests verified by consumers; gates never commit (6.2 commits ci-inputs/measure.json); reruns transient-only, workflow fixes are .g<n>; one namespace statement; §8 macro aliases and 0.0 command list cleaned; verifier_id grammar in §5; §6 stream fork. 86 raw → 122 expanded, acyclic. BUILD-DEFAULTS 1.11.0. Recommendation: Lock.

## Entry template

- [<task-id>] <ISO> Task: <one line>. Status: Complete | Partial | Debugging | Blocked | HUMAN_GATE. Files: <list>. Tests: <counts, pass/fail>. Commit: <sha> "<msg>". Iterations used: <n>. First attempt passed: y/n. Tool calls: <n>. Reviewer overturned: y/n/pending. Notes: <what the next iteration needs>.
