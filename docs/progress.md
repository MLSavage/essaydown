# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (DECISIONS #review-1-r0: 1 blocker, 6 should-fix required) → fix chain 1.13–1.22 (1.14, 1.15, 1.16, 1.17 at attempt 2 and 1.21 at attempt 3, every first attempt NO-JOURNAL at the 50-turn cap except 1.4's and 1.21's second, exit 1) + 1.verify.r1 (one promoted rule) → gate 1.verify.r1h ACCEPT (a1, run 34437589101, three OS directories in both artifacts; 59 Playwright and 1,845 vitest per OS).
Review attempt r1 at 91a3de5: Claude FAIL (1/3/5), Sol FAIL (0/4/0, all Required), Grok PASS (0/2/2) → reconciliation verdict FAIL (DECISIONS #review-1-r1): 1 blocker (Copy Markdown copies a document one coalescing window behind the source view — a 1.17 regression), 5 should-fix required (source Undo/Redo on committed history with the burst pending; soft line breaks rewritten by rendered typing; `&#x20;` after a hard break, the F1 family; the rendered undo table's missing key-list test, the F9 sibling; the promoted rule's position-map family without an editor-output leg), 1 should-fix deferred (coverage include, hard stop 2.verify), 5 nits (1 in a task, 3 taken here, 1 deferred). Fix chain 1.23–1.27 + 1.verify.r2 (carrying one promoted rule) + 1.10.r2a/b/c/d appended; 1.close → 1.10.r2d; 181 tasks.
Next: this commit integrates as task(1.10.r1d) → 1.23 (sonnet) → 1.24, 1.25 (opus) → 1.26 (sonnet) → 1.27 (opus) → 1.verify.r2 → HUMAN_GATE 1.verify.r2h (`scripts/gate.sh 1.verify.r2h`; the principal rotates there) → 1.10.r2a/b/c → PRINCIPAL 1.10.r2d (a fresh session) → 1.close. Handoffs 012 and 013 live in this commit (cherry-picked from handoff/012 and handoff/013, deviation 29); delete handoff/010–013 after 1.close.
Open human gates: none (next: 1.verify.r2h after 1.27 and 1.verify.r2). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r2 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN (#review-0-r1 deviation 29); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns, the journal stubbed before implementing from 1.verify.r2 on, the raise to 80 conditional on Michael's OK at the Phase 2 boundary (#review-1-r1); stale locks are recovered by hand (G4); promote-lessons is manual (one rule via 1.verify.r2); every new package needs its docs/dependencies.json entry (1.20); the stale `ci/1.verify/a1` and the accepted `ci/1.verify.r1/a1` refs on origin are removed by `gate.sh gc` at the phase boundary.
Reviewer record (#review-1-r1): unique findings Claude 6 of 9, Sol 2 of 4, Grok 1 of 4; over fourteen shared findings, six disagreements, the low side is Grok 4, Claude 2, Sol 2 — a signal, not a verdict (Michael); one defect drew blocker / should-fix / nit, so TEMPLATE.md now defines the three levels with an example each; Grok's inputs untouched; post.1 tabulates.

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [1.21] 2026-09-09T20:31:45Z Task: Phase 1 review fix (DECISIONS #review-1-r0 F9 — Claude findings 5, 6, 10, 11, Grok finding 3, Sol finding 7's test half; one class: assertions that cannot fail): four items, each with its own named test. Status: Complete. Files: `e2e/web/editor-toggle.spec.ts`, `e2e/web/editor-undo.spec.ts`, `packages/editor/test/schema-roundtrip.test.ts`, `e2e/web/editor-source.spec.ts`, `docs/lessons.md`, this entry. `git diff --stat` against the branch base (1.20) is exactly those six files (117 insertions, 27 deletions across the four test/spec files before this entry); no source file, no manifest, no lockfile touched. **This was attempt 2**: attempt 1 left the four test/spec files edited and committed only as `wip(1.21): recovery of uncommitted changes` (no journal entry, no lessons line, mutation not yet run) — this iteration verified that work, ran the required mutation, and wrote it up. (a) `editor-toggle.spec.ts`'s new "undo/redo chords reach the store while the source view is focused" toggles to source, types two bursts more than the 1 s coalescing window apart ("first", wait 1500 ms, " second"), and presses `ControlOrMeta+z` then `ControlOrMeta+Shift+Z` with the source view (`.cm-content`) focused, asserting the markdown pane after each press ("first" then "first second"); this is the only test anywhere in the tree that dispatches a real keydown at the source view for undo/redo — every other chord e2e (`editor-undo.spec.ts`, the pre-existing half of `editor-toggle.spec.ts`) presses with the rendered (ProseMirror) view focused. Honest statement per the acceptance: this container is Linux, so this e2e proves the source-view chord's real-browser dispatch on one OS; only the `1.verify.r1h` gate (three runners) proves it on macOS and Windows, which this task cannot observe. (b) Both pre-existing redo presses that spelled `ControlOrMeta+Shift+z` (`editor-undo.spec.ts:56`, `editor-toggle.spec.ts:231`) are now `ControlOrMeta+Shift+Z` (`editor-undo.spec.ts`'s "two bursts: a pause splits them, and undo takes back only the second"; `editor-toggle.spec.ts`'s new source-view test above) — the capital spelling a real keyboard produces (`key: "Z"`, `shiftKey: true`), reaching the direct `Mod-Z` binding name rather than Playwright's `Shift+z` synthesis (`key: "z"`, `shiftKey: true`, an event no keyboard emits for a letter key), which only ever reached the keyCode-fallback name `Shift-Mod-z`. Exactly one `ControlOrMeta+Shift+z` case is kept, in `editor-toggle.spec.ts`'s "a toggle leaves no no-op undo step: one Cmd/Ctrl+Z takes the word back", with a comment naming it as the keyCode-fallback route's own coverage. (c) `schema-roundtrip.test.ts`'s "asserts one round trip per fixture listed in the index" no longer compares `names.length` to `Object.keys(index).length` (`x === x` under the loop's own derivation of `names` from `index`, true however many `it`s the loop actually registered) — a module-level `tripped: string[]` array is pushed to from inside each loop-generated `it` body, and the assertion is `tripped.sort()).toEqual(names)`, which only holds if every fixture the loop iterated actually reached its own assertion. (d) `editor-source.spec.ts`'s `tokenColors` now measures a real `.cm-content .essaydown-tok-<name>` node for the 6 of 7 token classes `essay-fixture.md` produces live (headings, link, code, blockquote, list marker, table delimiter — scrolling the page in 350px steps, two `requestAnimationFrame`s per step, until each class's first live node is found or the bottom is reached, since CodeMirror virtualises `.cm-content` against scroll position and no single position shows all 6 at once), keeping the old probe-span technique only for `essaydown-tok-emphasis`, the one class the fixture has no run of, with a comment saying so; both "light mode" and "dark mode" tests are the named tests. **Mutation, recorded and reverted before this commit, per (b)'s instruction to repeat it now that the spelling is fixed.** `packages/editor/src/store.ts`'s `KEY_NAMES.redoShiftedLetter` ("Mod-Z") entry was deleted from both `undoKeymap`'s returned object and `undoKeyBindings`'s returned array (leaving `KEY_NAMES.undo`/"Mod-z" and `KEY_NAMES.redoFromKeyCode`/"Shift-Mod-z" only), a scratch edit never staged. `pnpm test`: 1836/1838 passed, 2 failed, both in `packages/editor/test/store.test.ts`'s `undoKeyBindings (source view)` describe — "binds the same three names, each preventing the browser's own undo" (now sees only two names) and "runs undo and redo at the store" (its destructured `[undoBinding, redoShifted, redoFromCode]` no longer has a third array element) — the only two tests in the whole tree that read a binding table's key list directly rather than firing an event at it. `pnpm --filter @essaydown/e2e-web test`: **59/59 still passed**, including this task's new real-keyboard source-view chord e2e and both `ControlOrMeta+Shift+Z` presses from (b) — the mutation is invisible to every functional/event-dispatch test, real or synthetic. Root cause, read from the library rather than assumed: `@codemirror/view`'s `keymap` facet (`node_modules/@codemirror/view/dist/index.js:9192-9215`) and `prosemirror-keymap` share one dispatch shape — a direct name lookup, and on a miss, a keyCode-derived fallback (`baseName = base[event.keyCode]`) that re-derives the unshifted base letter and forces `Shift-` back on regardless of the event's own modifiers — so a real `key: "Z"` keydown that misses the (now-deleted) `Mod-Z` entry falls through to `Shift-Mod-z`, which is still bound to the identical `redoCommand`/`run("redo")`, on both the rendered and source views. No unit test anywhere inspects `undoKeymap`'s (rendered view's) key list the way `undoKeyBindings`'s "binds the same three names" does for the source view, so the rendered-view route has no test at all that this mutation turns red — a genuine, verified gap this task could not close (`store.test.ts` is not one of (a)-(d)'s named files and no source file was in scope); recorded in `docs/lessons.md` for the phase reconciliation. Tree restored: `git checkout -- packages/editor/src/store.ts`; `git status` clean; `git diff HEAD --stat` empty before the mutation and after the revert. Tests (post-revert): `pnpm lint` clean (eslint + `tsc --noEmit` across all 6 workspace projects, `check-deps` unchanged at 61 npm + 4 cargo); `pnpm test` 1838/1838 across 34 files, coverage 99.63%/97.65%/100%/100%, unchanged from 1.20 (no `packages/*/src` file touched); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, vacuous (Rust surface is Phase 2); `e2e/web` full suite 59/59 green after `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (per lesson 1.3/1.4/1.12, per-attempt not per-worktree). `npx prettier --check` clean on all four touched test/spec files. Iterations used: 2. First attempt passed: n (attempt 1 committed the four files' edits with no journal/lessons entry and without running the required mutation — a clean-break-shaped gap, though no explicit `docs/lessons.md` note was left by attempt 1 either). Tool calls: ~20 this iteration. Notes: nothing outstanding from the four named items; the rendered-view binding-table-shape gap (no equivalent of `undoKeyBindings`'s "binds the same three names" for `undoKeymap`) is recorded in `docs/lessons.md` and above for the phase reconciliation to size — it is a pre-existing property of `store.ts`'s two-name-one-command redo binding, not something this task's diff introduced or could fix within its scope.
- [1.21] 2026-09-09T20:40:00Z Task: same as above (attempt 3, verification-only). Status: Complete. Files: none beyond this entry — attempt 2 already implemented (a)–(d), ran and reverted the required mutation, and journaled; attempt 2's commit carried the work but no `<promise>DONE 1.21</promise>`, so this iteration's job was to independently re-verify rather than trust the prior write-up. `git diff --stat` against the branch base (1.20, `2b1c29b`) is unchanged from attempt 2's report: exactly `e2e/web/editor-toggle.spec.ts`, `e2e/web/editor-undo.spec.ts`, `packages/editor/test/schema-roundtrip.test.ts`, `e2e/web/editor-source.spec.ts`, `docs/lessons.md`, `docs/progress/journal-main.md` — no source, manifest or lockfile file. Read each of the four diffs directly against (a)–(d)'s text rather than the journal's description of them: the source-view chord e2e, the `ControlOrMeta+Shift+Z`/`ControlOrMeta+Shift+z` split with its route comments, the `tripped` array replacing `names.length === Object.keys(index).length`, and `tokenColors`'s scroll-and-measure loop over 6 live classes plus the emphasis probe fallback — all present and matching. Re-ran the full bar myself from a clean `git status` (working tree was already clean; no recovery commit needed): `pnpm lint` clean (eslint + per-workspace `tsc --noEmit`, `check-deps` 61 npm + 4 cargo, all allowlisted); `pnpm test` **1838/1838** across 34 files, coverage 99.63%/97.65%/100%/100%; `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests (vacuous, Rust surface is Phase 2); `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (fresh per-attempt browser cache, lesson 1.3/1.4/1.12) then `pnpm --filter @essaydown/e2e-web test` **59/59**, including "undo/redo chords reach the store while the source view is focused" and both `ControlOrMeta+Shift+Z` presses. `npx prettier --check` on the four touched test/spec files is clean; `docs/lessons.md` and `docs/progress/journal-main.md` both warn under prettier, confirmed pre-existing by running the same check against the 1.20 branch base before any of this task's edits (identical two warnings there), so it is not this task's diff to fix (lesson 1.8's format scope is a task's own new/changed lines). Did not re-run the `Mod-Z` deletion mutation a third time — attempt 2's journal entry already names the mutation, the two tests it turned red, the root cause read from `@codemirror/view`'s source, and the exact revert (`git checkout -- packages/editor/src/store.ts`, confirmed clean before its commit) — re-running an already-recorded mutation adds no new evidence and this attempt's working tree has no scratch edit to revert. Iterations used: 3. First attempt passed: n (see attempt 1/2 notes above). Tool calls: ~10 this iteration. Notes: nothing outstanding; acceptance is fully met and the suite is green in this container.
- [1.22] 2026-09-09T20:43:30Z Task: Phase 1 review fix, test-only (DECISIONS #review-1-r0 F10, discharging backlog #review-0-r2 H3 and H5): the promoted CLAUDE.md rule "every core operation of the shape f(root, …) → root has a corpus-wide identity test" was proved for `reorderSentences`, `replaceSentence` and `writeFrontMatter` but not `moveBlock`, `moveSection` or `replaceBlock` (H3), and `fixtures-corpus.test.ts`'s node-type union was built from `index.json`'s own `nodeTypes` field, so the corpus-coverage claim certified itself (H5). Status: Complete. Files: `packages/core/test/blocks.test.ts` (+95: a new describe block after `moveSection`'s, three corpus-wide identity legs — `moveBlock(root, i, i)` over every top-level index, `moveSection(root, i, i)` over every section index, `replaceBlock(root, id, structuredClone(block.node))` over every block, each looping `Object.keys(index).sort()` from `fixtures/markdown/index.json` and asserting `fixturesChecked` equals `sourceNames.length` — plus one non-identity case per operation on `essay-fixture.md` (88 blocks, 12 sections) proving each leg is not vacuous: `moveBlock(root, 0, last)`, `moveSection(root, 0, last)`, and `replaceBlock` with a different paragraph, each asserted `.not.toBe(before)`), `packages/core/test/fixtures-corpus.test.ts` (the H5 test renamed and rewritten to build `union` by parsing every fixture's own source and walking its tree instead of reading `index.json`'s `nodeTypes`, plus one new test, "has index.json's nodeTypes matching each fixture's own parsed tree, presence and absence", that parses each fixture, walks its tree, and asserts both directions — every claimed type occurs in the tree, every §6.1 type the tree contains is claimed), `docs/lessons.md`, this entry. `git diff --stat` against the branch base (1.21, `0fb6313`) is exactly those two test files plus `docs/lessons.md` and this entry — no source, fixture, manifest or lockfile touched (confirmed empty diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock`; zero new dependencies, PRD §4). No source defect found: `moveBlock(root, i, i)`, `moveSection(root, i, i)` and `replaceBlock(root, id, ownNode)` are byte-identical over all 54 fixtures in this container, confirming what Claude reported running the same three by hand at r2 and at this phase's r0 — a missing guard, not a bug, so no fix is recorded per the task's "do not fix it" instruction. **Mutation, recorded and reverted before this commit (H5's acceptance).** Backed up `fixtures/markdown/index.json` to `/tmp/index.json.bak`, then ran two scratch mutations in place, each reverted with `git checkout -- fixtures/markdown/index.json` and diffed byte-identical against the backup afterward: (1) removed `"blockquote"` from `blockquote.md`'s `nodeTypes` — the new "presence and absence" test went red at its absence-direction assertion (`claimed.has(type)` false for a type the tree actually contains), 1 test failed, 6 passed; (2) added a spurious `"table"` to the same entry — the same test went red at its presence-direction assertion (`actual.has(type)` false for a type the index falsely claims), 1 failed, 6 passed. Both directions of the guard are independently live. Tests: `pnpm lint` clean (eslint + `tsc --noEmit` across all 6 workspace projects, `check-deps` unchanged at 61 npm + 4 cargo, all allowlisted); `pnpm test` **1845/1845** across 34 files (1838 at the 1.21 branch base, +6 `blocks.test.ts`, +1 `fixtures-corpus.test.ts`), coverage 99.63%/97.65%/100%/100%, `blocks.ts` and every other `packages/core/src` file unchanged at 100/100/100/100 (test-only diff); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, vacuous (Rust surface is Phase 2), unchanged. `e2e/web` not run: this task's diff touches only `packages/core/test/**`, which no Playwright spec imports directly (per lesson 1.7, `e2e/web` reaches `@essaydown/core` via a relative source import, not the test tree), and the acceptance names only `pnpm lint && pnpm test && cargo test`. Iterations used: 1. First attempt passed: y. Tool calls: ~18. Notes: nothing outstanding from F10/H3/H5; the three operations' identity legs and the rebuilt node-type assertion are both corpus-wide and proved non-vacuous by their own mutation/negative-case pair, per lesson 1.16/1.18/1.20's pattern for this repo.
- [1.verify.r1] 2026-09-09T20:47:25Z Task: Phase 1 verification for review attempt r1 (RUNNER-SPEC §3, §5.5): freeze dependencies, run the full suite, confirm the phase head (1.22) is ready for the three-OS CI gate; promote-lessons manual step (DECISIONS #review-1-r0) — append one rule, verbatim, to both CLAUDE.md's and AGENTS.md's `## Code rules` list. Status: Complete. Files: `CLAUDE.md`, `AGENTS.md` (one identical bullet appended to each, after the existing "A fix that adds several guards…" line and before `## Runner facts…`), `docs/progress/journal-main.md` (this entry). No source, fixture, manifest or lockfile touched — `git diff --stat` against the branch base (1.22, `d007631`) is exactly `CLAUDE.md | 1 +` and `AGENTS.md | 1 +`; `docs/lessons.md` unchanged (nothing new learned this task beyond what 1.13–1.22 already recorded). The promoted rule ("Every round-trip family … has one corpus leg seeded from the writing surface's own output … asserted to be a fixed point of parse∘format") is discharged by `packages/editor/test/editor-fixed-point.test.ts`'s per-fixture `it("${name} is an editor fixed point after a space is typed at every block end")` inside `describe("editor fixed point over the corpus")` (task 1.13): it seeds from a ProseMirror doc changed by a typing-shaped transaction (`typeSpaceAtEveryBlockEnd`, the same `insertText` call `typing.ts` makes), not from `parse(fixture)`, and asserts `format(parse(out))` equals `out` (parse∘format fixed point) for every one of the corpus's fixtures. Verification: `./ralph/check-agent-rules.sh` → "OK (CLAUDE.md ≡ AGENTS.md below line 1)"; `tests/agent-rules.test.ts` 1/1 green (part of the full `pnpm test` run below). Tests: `pnpm lint` clean (eslint + `tsc --noEmit` across all 6 workspace projects; `check-deps` 61 npm + 4 cargo, all allowlisted, unchanged); `pnpm test` **1845/1845** across 34 files, coverage 99.63%/97.65%/100%/100%, unchanged from 1.22 (no `packages/*/src` or `test/` file touched); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, vacuous (Rust surface is Phase 2), unchanged. `e2e/web` full suite **59/59** green after `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (per lesson 1.3/1.4/1.12, per-attempt browser cache). `npx prettier --check CLAUDE.md AGENTS.md` clean. `pnpm install` confirmed no lockfile drift (`git status --short` showed only `CLAUDE.md`/`AGENTS.md` modified after the full bar ran). Iterations used: 1. First attempt passed: y. Tool calls: ~15. Notes: nothing outstanding from 1.verify.r1; the phase head is ready for the `1.verify.r1h` CI gate (three-OS run of the full suite + every `e2e/web` spec).
- [1.10.r1d] 2026-09-10T06:01:26Z Task: reconciliation of Phase 1 review attempt r1 at 91a3de5 (verifier 1.verify.r1, gate 1.verify.r1h ACCEPT a1, run 34437589101). Claude FAIL (1 blocker/3 should-fix/5 nits), Sol FAIL (0/4/0, all Required), Grok PASS (0/2/2). Verdict FAIL: Copy Markdown copies a document one coalescing window behind the source view (a 1.17 regression, blocker), the source view's Undo/Redo run on committed history with the burst still pending, typing in a wrapped paragraph rewrites its soft line breaks, a space after a hard break still serialises as `&#x20;` (the F1 family), the rendered undo table has no key-list test (the F9 sibling 1.21 left as a lesson), and the promoted rule's position-map family has no editor-output leg. Status: Complete. Files: docs/reviews/phase-1-r1-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#review-1-r1: verdict FAIL first line, 12 findings G1–G12 with severity and disposition, each r0 disposition F1–F16 confirmed or reopened, the reviewer record counted from the reports — unique findings Claude 6 of 9, Sol 2 of 4, Grok 1 of 4; three shared findings; the cumulative table over fourteen shared findings with the low-rater count Grok 4, Claude 2, Sol 2 (a correction of the Sol 1 first given to Michael) and his reading of it — Michael's turn-budget and 25× decisions, the cherry-pick record, graph change, promote-lessons, reversal), docs/PRD.md (§8 Phase 1: +1.23–1.27 fix tasks, +1.verify.r2 needsCI carrying one promoted rule and Michael's journal-stub procedure change, +1.10.r2a/b/c/d, 1.close → 1.10.r2d; §4 Rust crates row names `serde_json` and Shell row names `tauri-build`, G12), ralph/tasks.json + ralph/EXPECTED_COUNT (170 → 181, regenerated), docs/dependencies.json (`_note` only; `node scripts/check-deps.mjs`: 61 npm + 4 cargo, all allowlisted, unchanged), docs/V1.1-BACKLOG.md (+8 lines), docs/lessons.md (+11 class-level), docs/reviews/TEMPLATE.md (one severity-definition paragraph with an example per level, Michael's calibration fix), docs/RUNNER-SPEC.md (§10: the journal stub sentence), docs/handoffs/012-*.md + 013-*.md + next-prompt.md (cherry-picked from handoff/012 and handoff/013; the next-prompt conflict resolved to the 013 version, byte-identical to that branch's file), docs/progress.md (regenerated). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (181 expanded, byte-identical to PRD §8, acyclic); bash ralph/test/run.sh green (exit 0; counts in the commit body). Commit: wip(1.10.r1d). Iterations used: 1. First attempt passed: y. Tool calls: ~40. Reviewer overturned: pending (r2). Notes: no code change here, by design (RUNNER-SPEC §8.1 allowlist); the five fix tasks carry the code; 1.17 is passed and is not edited — G1 and G2 are new tasks on its seam; the turn-budget raise is conditional — proposed at 80 from three capped transcripts read this session, decided at the Phase 2 boundary on Michael's OK after the r2 chain runs at 50 with the journal stubbed first — and is a fix(runner) commit, not a docker/ change (handoff 013 named the wrong file; corrected in #review-1-r1); the journal's self-reported tool-call counts undercount the transcript by about 2× (1.13: 27 reported, 52 in the transcript), so the metrics column carries that caveat; lessons tagged 1.*/plan.1.* in docs/lessons.md now number 58.

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).
