# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (#review-1-r0) → fix chain 1.13–1.22 + 1.verify.r1 → gate 1.verify.r1h ACCEPT → review r1 FAIL (#review-1-r1) → fix chain 1.23–1.27 + 1.verify.r2 → gate 1.verify.r2h GATE-FAILED (#021) → 1.28 + 1.verify.r2.g1 → gate 1.verify.r2.g1h ACCEPT → review r2 FAIL (#review-1-r2) → fix chain 1.29–1.32 (1.29 at attempt 3: a1 capped, a2 NO-JOURNAL on procedure — the `chore(1.29)` lesson; 1.30 at attempt 2, a1 capped) + 1.verify.r3 (one promoted rule) → gate 1.verify.r3h GATE-FAILED (#022, the macOS Home chord) → 1.33 + 1.verify.r3.g1 → gate 1.verify.r3.g1h ACCEPT at a2 after the build's first transient rerun (#023; a1 a windows `setInputFiles` timeout before any assertion; run 34573685490, three OS directories in both artifacts; 79 Playwright and 2,196 vitest per OS).
Review attempt r3 at f90ed50: Claude FAIL (1/4/3), Sol FAIL (1/2/1), Grok PASS (0/0/2) → reconciliation verdict FAIL (DECISIONS #review-1-r3): 2 blockers (a `hard_break` last in a flanking mark's run with unmarked text after it serialises as `\&#xA;` inside the delimiters — the F1/G4/H1/H8 family's atom-at-a-mark-edge member, deferred by 1.30 on a false reachability claim; a list item keeps its parsed `spread` after a two-line paste adds a paragraph — a block-level class), 3 should-fix required (1.30's mark-edge clause invents a second link from a link's edge whitespace inside a mark, a regression; the position-map editor leg is insertion-only against the promoted rule; the four rendered-view Home/End chords, #022's trigger fired), 1 should-fix taken with its sibling (a link's edge whitespace at a block end, pre-existing), 1 should-fix recorded (the a2 rerun, answered by #023), 3 nits (the fourth tautology taken in 1.34; the owner's address in the Phase 0 r0 report copy redacted here; Unicode whitespace at a mark edge decided ASCII, re-pointed at Phase 3). Fix chain 1.34–1.38 (all opus) + 1.verify.r4 (two promoted rules, one recorded) + 1.10.r4a/b/c/d appended; 1.close → 1.10.r4d; 208 tasks.
Next: this commit integrates as task(1.10.r3d) → 1.34 (the mark-edge clause at an atom; the deletion leg to the end of every marked run; the tautology) → 1.35 (the link predicate at both trims) → 1.36 (`spread` re-derived; the list-split leg) → 1.37 (the position-map deletion leg) → 1.38 (the four chords; the tree-wide guard) → 1.verify.r4 → HUMAN_GATE 1.verify.r4h (`scripts/gate.sh 1.verify.r4h`; the principal rotates there) → 1.10.r4a/b/c → PRINCIPAL 1.10.r4d (a fresh session) → 1.close. Handoffs 017, 018 and 019 live in this commit (cherry-picked from handoff/017–019, deviation 29); delete handoff/010–019 after 1.close.
Open human gates: none (next: 1.verify.r4h after 1.38 and 1.verify.r4). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r4 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN (#review-0-r1 deviation 29); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns with the journal stubbed first, the raise to 80 conditional on Michael's OK at the Phase 2 boundary (#review-1-r1; r2 and r3 chain counts on the backlog); stale locks are recovered by hand (G4); promote-lessons is manual (two rules via 1.verify.r4); every new package needs its docs/dependencies.json entry (1.20); after any `gate.sh rerun` that accepts, `doctor` names `--resume a<n>` — never run it, use #023's abandon once per rerun, until the boundary `fix(runner)`; the `ralph/PROMPT.md` iteration list is aligned to CLAUDE.md by a `fix(runner)` at the Phase 1→2 boundary (H7) and until then every r4 task text names CLAUDE.md's list as the one that stands; a task that lost an attempt on procedure gets its principal lesson as `chore(<id>)` on its task branch (docs/lessons.md only); the stale `ci/*` refs on origin (verify with `git ls-remote origin 'refs/heads/ci/*'`) are removed by `gate.sh gc` at the phase boundary.
Reviewer record (#review-1-r3): unique findings Claude 6 of 8, Sol 2 of 4, Grok 0 of 2; both blockers unique to a probing reviewer, each reproduced headlessly by the reconciliation; two shared findings (the tautology, agreed at nit; the chords, Grok low); cumulative over seventeen shared findings, eight disagreements, the low side is Grok 6, Claude 2, Sol 2 — still a signal, not a verdict; Grok PASS with blockers in the tree at r2 and r3; the three-way-spread condition has not fired; no reviewer change inside a review set; the counts are Michael's input at the boundary; post.1 tabulates.

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [1.verify.r3] 2026-09-11T04:44:49Z Task: Phase 1 verification for review attempt r3 (RUNNER-SPEC §3, §5.5): freeze dependencies, run the full suite in the container, confirm the phase head is ready for the three-OS CI run, and promote the invariant-corpus lesson into CLAUDE.md/AGENTS.md. Status: Complete. `git status` clean at start (base 2273cdb, task 1.32), no recovery commit. **Procedure note (DECISIONS #review-1-r2 H7):** this task's generated `ralph/PROMPT.md` wrapper still lists the pre-1.verify.r2 iteration order (implement, one journal entry, one commit); CLAUDE.md's stub-first `## Every iteration` list stands per the task text and 1.10.r2d's lesson, and the mismatch is not a conflict to stop on — stub first (this line, `wip(1.verify.r3): journal stub`), then implement, then complete the stub in place. **The promotion.** One line appended verbatim, in the same position, to the `## Code rules` list of both `CLAUDE.md` and `AGENTS.md`, as the last bullet before `## Runner facts you rely on`: "A fix for one member of a stated invariant asserts the invariant over the corpus and enumerates instances only as named guards on top of it; a corpus leg that claims to be seeded from the writing surface's own output includes at least one destructive transaction (a deletion, a split, a join), because the trees a parser cannot produce are usually reached by taking something away." `ralph/check-agent-rules.sh` exits 0 (files identical below line 1) and `tests/agent-rules.test.ts` passes. **The tests that discharge the clause**, both in `packages/editor/test/editor-fixed-point.test.ts` and named per the task text: 1.29's deletion leg, `${name} is an editor fixed point after deletion: the continuation after every hard break, then the last character of every block's last run` (plus its two non-vacuous reach guards, `at least one fixture in the index holds a hard break the deletion leg leaves last in its block` and `at least one fixture in the index has a block whose last run the deletion leg deletes from`); 1.30's mark-edge leg, `${name} is an editor fixed point after a space is typed inside every marked run, at its end (the mark edge)` (plus its reach guard, `at least one fixture in the index holds a marked run the mark-edge leg types into`) — both are corpus legs seeded from a ProseMirror transaction (deletion; an insertion at a mark's end) rather than only from `parse(fixture)`, and both are destructive or mark-boundary transactions, not typing-only. No procedure change beyond the promotion; no code, test or fixture touched. **Verification, this container (Linux; no other OS claimed):** `pnpm lint` clean (eslint, `check-deps` 61 npm + 4 cargo direct dependencies all allowlisted, `tsc --noEmit` across all 6 typechecked projects); `pnpm test` **2196/2196** across 35 files, coverage 99.64% statements / 97.73% branches / 100% functions / 100% lines, `schema.ts`/`undo.ts`/`store.ts` 100% on every column; `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests (vacuous, Rust surface is Phase 2 — no cargo project changed since 1.31/1.32 confirmed this); `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (per-attempt browser cache) then the full e2e/web suite **79/79** green, every spec file under `e2e/web/` including the 1.17 latency spec. `git diff --stat` against the branch base (1.32, 2273cdb): exactly `CLAUDE.md` (+1), `AGENTS.md` (+1), `docs/progress/journal-main.md` (this entry) — no dependency-manifest or lockfile change (`package.json` × 9, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, every `Cargo.toml`, `Cargo.lock` all zero-line diff), zero new dependencies (PRD §4), `docs/lessons.md` untouched (nothing learned beyond what the fix chain already recorded). Files: CLAUDE.md (+1), AGENTS.md (+1), docs/progress/journal-main.md (the stub commit, now this completion). Tests: as above, all green. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: nothing outstanding; the phase head (this commit, once squashed) is ready for the `1.verify.r3` CI gate (three-OS run) and the review-set r3 that follows; the two Phase-2 backlog lines from 1.29/1.32 remain open for their named hard stops.
- [plan.1.verify.r3h.r0] 2026-09-11T05:14:16Z Task: planning commit for the 1.verify.r3h GATE-FAILED (ci.yml run 34563964087 at 32552e0: macos-latest 1 e2e/web case red at the Playwright step — task 1.30's H8 case in editor-mark-edge.spec.ts, the pane polling for `a *b*  c` and receiving the seed's `a *b* c` because `ControlOrMeta+Home` is not a document-start motion on macOS and the `<em>` assertion normalised whitespace; ubuntu and windows green on everything, 79 Playwright cases each; artifacts [] because the gate fetches only on success). Status: Complete. Files: docs/PRD.md (§8 Phase 1: +1.33 the caret placed through Blink-only motions with the rule in the doc comment, byte-exact `<em>` assertions, four grep guards, the paragraph's-end mutation, the reproduction quoted; +1.verify.r3.g1 needsCI repair with refTemplate ci/1.verify.r3.g1/a{n}; 1.10.r3a/b/c → 1.verify.r3.g1 with their accepted-artifact path moved to /logs/ci/1.verify.r3.g1h/accepted/), ralph/tasks.json + ralph/EXPECTED_COUNT (194 → 197, regenerated; the generator makes the reviewers depend on 1.verify.r3.g1h), docs/DECISIONS.md (#022: what happened, cause, scope decision, graph change, model choice, reviewer notes, reversal), docs/lessons.md, docs/V1.1-BACKLOG.md (the four other rendered-view Home/End uses, hard stop 2.verify). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (197 tasks); bash ralph/test/run.sh run before the commit. Commit: wip(plan.1.verify.r3h.r0). Iterations used: 1. First attempt passed: y. Tool calls: ~22. Reviewer overturned: pending. Notes: the runner integrates this as plan(plan.1.verify.r3h.r0), supersedes 1.verify.r3h, then runs 1.33 and 1.verify.r3.g1; next human gate is scripts/gate.sh 1.verify.r3.g1h; the macOS Playwright log joins the Windows one as the first unknown of that round.
- [1.33] 2026-09-11T05:18:44Z Task: Phase 1 verification-gate repair, test-only (repair after the 1.verify.r3h GATE-FAILED, ci.yml run 34563964087 at 32552e0: ubuntu-latest and windows-latest green on every unit test and every e2e/web spec, 79 Playwright cases each; macos-latest green on everything except `e2e/web/editor-mark-edge.spec.ts` › H8; DECISIONS #022) (attempt 1). Status: Complete. `git status` clean at start, no recovery commit; stub committed first (`wip(1.33): journal stub`) per CLAUDE.md's list, which stands over the wrapper's older steps (DECISIONS #review-1-r2 H7). **Reproduction, before the change.** `grep -c 'ControlOrMeta+Home' e2e/web/editor-mark-edge.spec.ts` was **2** (H8's two seeds, spec lines 68 and 87) and `grep -c 'ControlOrMeta+End'` was **1** (the absence case, line 101). This container is Linux and cannot run macOS, so the failure is quoted from /logs/ci/1.verify.r3h/a1/workflow.log, step `test (macos-latest)` › `Playwright (e2e/web)` (lines 6267 and 6317–6339): `✘  42 editor-mark-edge.spec.ts:60:3 › a space typed at a mark edge never reaches the Markdown as a character reference › H8 (Sol's reproduction): \`a *b* c\`, the caret after \`b\`, one space — the space lands inside the mark and the pane is clean (5.3s)`, then `Error: expect(received).toBe(expected) // Object.is equality` / `- Expected  - 1` / `+ Received  + 1` / `- a *b*  c` / `+ a *b* c` / `↵` / `Timeout 5000ms exceeded while waiting on the predicate` at `> 53 |   await expect.poll(() => markdown(page)).toBe(bytes);` in `expectClean`, called from `editor-mark-edge.spec.ts:78:11` — the H8 case's first `expectClean(page, "a *b*  c\n")`, receiving the seed's own bytes. The same case is `ok 25` on windows-latest (line 1372) and `✓ 23` on ubuntu-latest (line 4195); the absence and input-rule cases are green on all three. The principal's reading is consistent with everything the log can show: the one red case is the one whose caret placement is a modifier chord (`ControlOrMeta+Home` → Meta+Home on macOS, resolved by Cocoa's key-binding layer as a scroll, not a document motion), the received bytes are exactly what the trailing-whitespace clause of `stripUnparsableWhitespace` leaves when the space is typed at the paragraph's end (where `seedFromSource`'s click leaves the caret), and the absence case passed on macOS only because its chord was a no-op at a caret already at the end. Confirmed from the spec too, not assumed: the old `expect(page.locator(".ProseMirror em")).toHaveText("b ")` at line 72 is a normalising matcher (Playwright's `toHaveText` with a string normalises whitespace), so `<em>b</em>` satisfied it and the pane assertion was the first one able to fail; the mutation below shows the byte-exact read returning `b` in that state. **Change**, `e2e/web/editor-mark-edge.spec.ts` only (+39/-13). (1) Caret placement: the H8 case's two `ControlOrMeta+Home` become `ArrowUp` (a one-line block, so Blink's line-start motion reaches the block's start on every OS), followed by the unchanged three `ArrowRight`; the absence case's `ControlOrMeta+End` becomes `ArrowDown` (Blink's line-end motion), followed by the unchanged `ArrowLeft`. The container showed `ArrowUp`/`ArrowDown` reaching the block's edges (the three cases green, and the mutation below shows the placement is live), so the bounding-box click fallback the task names was not needed and is not in the file. The rule is stated once, in words, in the file's doc comment ("Never a Home or End key and never a modifier chord for caret motion in the rendered view", with the Cocoa reason); the `ControlOrMeta+/` toggle in `seedFromSource` is the app's own view-toggle keymap, not a caret motion, and stays. (2) Byte-exact `<em>` reads through a new `emText(page)` helper (`page.locator(".ProseMirror em").textContent()`): H8 `expect(await emText(page)).toBe("b ")` after each of its two spaces; the absence case `toBe("b")`; the input-rule case `toBe("word")` before the space, `toMatch(/^word ?$/)` after the space (the two trees the title admits, named), `toBe("word")` after the `x`. No `toHaveText` on the `<em>` remains. The pane assertions `a *b*  c\n`, `a *b* c\n` and `a *b*  c\n`, `expectClean`'s `not.toContain("&#x20;")` and its `format(parse(pane))` fixed point are unchanged; no timeout option added; no `process.platform`; no `.skip`/`.only`; `stripUnparsableWhitespace` and DevEditor untouched. **Able to fail, in the container** (case title: `H8 (Sol's reproduction): \`a *b* c\`, the caret after \`b\`, one space — the space lands inside the mark and the pane is clean`): scratch edit backed up to /tmp/mark-edge.bak, the first seed's `ArrowUp` + three `ArrowRight` replaced by one `ArrowDown` (the paragraph's end, the position the macOS run typed at) and the `<em>` expect made `expect.soft` so both assertions report in one run; result **red on both**: the byte-exact `<em>` assertion `Expected: "b "` / `Received: "b"` (line 94) and the pane assertion `- a *b*  c` / `+ a *b* c` / `Timeout 5000ms exceeded while waiting on the predicate` (the identical diff to the macOS log). Reverted with `cp` in the same tool call (lesson [1.25]); `grep -c SCRATCH` 0 and `git status --short` showing only this task's files before the commit. **Acceptance greps, after the change**, on the spec: `grep -cE 'ControlOrMeta\+(Home|End)|"(Home|End)"'` **0**; `grep -c 'em")).toHaveText'` **0**; `grep -c 'process.platform'` **0**; `grep -cE '\.(skip|only)\(|runIf|skipIf'` **0**; `grep -c timeout` **0**. Files: `e2e/web/editor-mark-edge.spec.ts`, `docs/lessons.md` (+1), this entry. `git diff --stat` against the branch base (plan.1.verify.r3h.r0, de5dcf2) is exactly those three files; no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock` change — zero new dependencies (PRD §4); no source, config or workflow file touched; no `docs/V1.1-BACKLOG.md` line (no defect found outside this scope; the two sibling spec files' chords are already the #022 backlog line and were not touched). Tests: `pnpm install --frozen-lockfile` (fresh worktree, no lockfile change), `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (lessons 1.3/1.4/1.12), then the full `e2e/web` suite **79/79** green including this file's three cases (baseline before the edit also 3/3 here — the miss is macOS-only, as the log says); `pnpm lint` clean (eslint, check-deps, `tsc --noEmit` on all typechecked projects); `pnpm test` **2196/2196** across 35 files; `cargo test` 0/0/0 (Rust surface is Phase 2), all unchanged from the base since no unit-tested file was touched; `npx prettier --check e2e/web/editor-mark-edge.spec.ts` clean. Iterations used: 1. First attempt passed: y. Tool calls: ~21. **Honest statement:** this container is Linux; the repaired route uses only motions Blink decides (`ArrowUp`/`ArrowDown`/`ArrowRight`/`ArrowLeft`), but the only proof of a macOS pass is the `1.verify.r3.g1h` gate green on ubuntu, macos and windows, which this task cannot observe and does not claim. Notes: nothing outstanding from 1.33; next is 1.verify.r3.g1 and then the `scripts/gate.sh 1.verify.r3.g1h` three-OS gate, where the macos-latest Playwright leg of this file is the first unknown; the same chord construct in editor-soft-line-breaks.spec.ts (lines 85, 107) and editor-trailing-break.spec.ts (lines 125, 138) stays on the #022 backlog line and passes on macOS only by the seed's caret position, so a seed change in those files would surface it.
- [1.verify.r3.g1] 2026-09-11T05:25:32Z Task: Phase 1 verification for review attempt r3, repair 1 of 1.verify.r3 after the 1.verify.r3h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #022): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. Status: Complete. `git status` clean at start (base 1.33, 440472e), no recovery commit; stub committed first (`wip(1.verify.r3.g1): journal stub`) per CLAUDE.md's list. **No procedure change and no code, test or fixture edit.** The promote-lessons step (the invariant-over-the-corpus rule appended to `CLAUDE.md`/`AGENTS.md`) was carried by `1.verify.r3` and is already integrated at `32552e0`; not repeated here. `bash ralph/check-agent-rules.sh` exits 0 (`check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`) and `tests/agent-rules.test.ts` passes (inside the `pnpm test` run below). **Verification, this container (Linux; no other OS claimed).** `pnpm lint` clean: eslint, `check-deps` (61 npm + 4 cargo direct dependencies, all allowlisted), `tsc --noEmit` across all 6 typechecked projects (`packages/coach`, `packages/core`, `packages/export`, `packages/modes`, `packages/editor`, `apps/desktop`); the fresh worktree's `pnpm install` (760-entry lockfile, supply-chain policy check passed) added `node_modules` only, `git status --short` and `git diff --stat pnpm-lock.yaml` both empty after. `pnpm test` **2196/2196** across 35 files, coverage 99.64% statements / 97.73% branches / 100% functions / 100% lines — identical to `1.verify.r3`'s numbers, since no source file changed since. `cargo test` (workspace: `desktop_lib`, `desktop`, doc-tests) **0/0/0** (vacuous, Rust surface is Phase 2; `git diff --stat Cargo.lock` empty). `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (per-attempt browser cache, lessons 1.3/1.4/1.12) then the full `e2e/web` suite (all 14 spec files under `e2e/web/`: `editor-clipboard`, `editor-cursor`, `editor-input`, `editor-mark-edge`, `editor-reveal`, `editor-soft-line-breaks`, `editor-source`, `editor-table-empty-cell`, `editor-toggle`, `editor-trailing-break`, `editor-trailing-space`, `editor-undo`, `latency`, `outline-produce`) **79/79 passed**, including the 1.17 latency spec's two measurements and 1.33's repaired `editor-mark-edge.spec.ts` H8 case (`ArrowUp`/`ArrowDown` caret placement, byte-exact `<em>` reads) — the fix that failed only on `macos-latest` in the `1.verify.r3h` GATE-FAILED run is exercised green in this Linux container as it was at `1.33`; the only unknown this task cannot resolve is the macOS leg itself, left to the `1.verify.r3.g1h` three-OS gate. **`git diff --stat` against the branch base (1.33, `440472e`)** is exactly `docs/progress/journal-main.md` (the stub commit, now this completion) — no dependency-manifest or lockfile change (`package.json` × 9, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, every `Cargo.toml`, `Cargo.lock` all zero-line diff), zero new dependencies (PRD §4); `docs/lessons.md` untouched (nothing learned beyond what 1.33 and 1.verify.r3 already recorded). Files: `docs/progress/journal-main.md` only. Tests: as above, all green. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: nothing outstanding; the phase head (this commit, once squashed) is ready for the `1.verify.r3.g1h` three-OS CI gate, where the macOS Playwright leg of `editor-mark-edge.spec.ts` (1.33's repair) is the first unknown; the review-set r3 and the two Phase-2 backlog lines from 1.29/1.32 remain open for their named hard stops.
- [1.10.r3d] 2026-09-12T04:54:13Z Task: reconciliation of Phase 1 review attempt r3 at f90ed50 (verifier 1.verify.r3.g1, gate 1.verify.r3.g1h ACCEPT a2 after a transient rerun, run 34573685490, DECISIONS #023). Claude FAIL (1 blocker/4 should-fix/3 nits), Sol FAIL (1/2/1), Grok PASS (0/0/2). Verdict FAIL: a `hard_break` last in an emphasis/strong/strikethrough run with unmarked text after it serialises as `\&#xA;` inside the delimiters — the break gone and the entity now text, and not even a fixed point for strikethrough or a setext heading (Claude, one Backspace inside an emphasised verse; reproduced headlessly here for all three marks); a list item keeps its parsed `spread` after a two-line paste adds a paragraph, so Copy Markdown's bytes are not a fixed point (Sol, `nested-lists.md` + `X\nY`; reproduced headlessly here); 1.30's mark-edge clause invents a second link from a link's edge whitespace inside a mark (a regression, reproduced) and the block-end trims drop a link's edge whitespace (pre-existing, reproduced); the position-map editor leg is insertion-only against the promoted rule; the four rendered-view `ControlOrMeta+Home/End` chords (#022's trigger fired, 3 of 3 reviewers). Status: Complete. Files: docs/reviews/phase-1-r3-{claude,sol,grok}.md (byte-identical copies, `cmp` clean), docs/DECISIONS.md (#review-1-r3: verdict FAIL first line, 10 findings I1–I10 with severity and disposition, each r2 disposition H1–H9 confirmed or reopened — H1's family → I1, H8 → I3 and I1 — the reviewer record counted from the reports: unique findings Claude 6 of 8, Sol 2 of 4, Grok 0 of 2; two shared findings, one with Grok low; cumulative over seventeen shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2; the `chore(1.29)` commit audited as a #016 deviation and sanctioned as the shape; #022's scope decision read against I6; #023 quoted against Claude 4; the turn-budget counts from the state file and the transcripts — 15 of 38 loop tasks at more than one attempt, a correction of handoff 019's 16; the three promote candidates weighed for 1.verify.r4; the cherry-pick record; graph change; reversal), docs/PRD.md (§8 Phase 1: +1.34–1.38 fix tasks, all opus; +1.verify.r4 needsCI carrying two promoted rules and one recorded; +1.10.r4a/b/c/d; 1.close → 1.10.r4d), ralph/tasks.json + ralph/EXPECTED_COUNT (197 → 208, regenerated: 162 raw + 21 review-set + 25 CI gates), docs/V1.1-BACKLOG.md (+10 lines: #023's boundary fix(runner), the conditional fetch-on-failure candidate, the flake signature, four dispositions of fired triggers, Sol's risk-1 class, turn budget, reviewer record), docs/lessons.md (+10 class-level), docs/reviews/phase-0-r0-claude.md (I9: the owner's address at line 26 replaced by `<owner address>`, recorded in #review-1-r3 as the one edit ever made to a review copy), docs/handoffs/017-*.md + 018-*.md + 019-*.md + next-prompt.md (cherry-picked from handoff/017, handoff/018 and both commits of handoff/019; the two next-prompt conflicts resolved to the incoming version, byte-identical to handoff/019's file), docs/progress.md (regenerated). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (208 expanded, byte-identical to PRD §8, acyclic); bash ralph/test/run.sh green (83 pass / 0 fail); the two headless reproductions were scratch vitest files under packages/editor/test, run and deleted before this commit (`git status` shows no test file). Commit: wip(1.10.r3d). Iterations used: 1. First attempt passed: y. Tool calls: ~30. Reviewer overturned: pending (r4). Notes: no code change here, by design (RUNNER-SPEC §8.1 allowlist); the five fix tasks carry the code; the boundary window after 1.close holds #023's fix(runner), H7's fix(runner), the conditional fetch-on-failure and turn-budget changes on Michael's OK, `gate.sh gc` and the phase/1 push; lessons tagged 1.*/plan.1.* in docs/lessons.md now number 116.

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.23 | 2 | ~22 (a1) + ~12 (a2) | n (a1 `error_max_turns` at 51, exit 1, journal present — no stop signal) | n (its three copy cases read the OS clipboard: the windows CRLF gate failure, #021, repaired by 1.28) |
| 1.24 | 1 | ~30 | y | y (H2: the pending segment keeps one keystroke time, so a flush splits a burst longer than the window) |
| 1.25 | 2 | n/a (the fields are absent from its entries) | n (NO-JOURNAL at a1, `error_max_turns` at 51) | y (H1/H5: the atom itself at a block end; H6: a cell's line ending) |
| 1.26 | 1 | ~24 | y | n |
| 1.27 | 1 | ~20 | y | n (H9: one tautology, nit, taken in 1.32) |
| 1.verify.r2 | 1 | ~14 | y | partial (H7: the wrapper `ralph/PROMPT.md` not carried along — a runner file, fixed at the boundary) |
| 1.28 | 1 | ~18 | y | partial (H4: the spy removed the suite's last real clipboard write — #021's scope decision) |
| 1.verify.r2.g1 | 2 | ~12 (a1) + ~20 (a2) | n (a1 exit 0 at `num_turns` 11, stopped by choice on H7; no stop signal) | n |
| 1.10.r2a (Claude) / r2b (Sol) / r2c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.29 | 3 | ~50 (a1) + ~16 (a2) + ~10 (a3) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal; a2 `success` at 21 with DONE and NO-JOURNAL — the stub completed in place, `chore(1.29)`) | partial (I1: the atom at a *mark's* edge, deferred by 1.30's backlog line; the deletion leg's ranges stop at the block's end) |
| 1.30 | 2 | ~50 (a1) + ~20 (a2) | n (a1 `error_max_turns` at 51, journal present — no stop signal) | y (I3: the mark-edge clause splits a link-marked run, a regression; I1's deferral written by this task on a false reachability claim) |
| 1.31 | 1 | ~34 (49 turns, 48 tool uses — one under the cap) | y | n |
| 1.32 | 1 | ~30 | y | n (I8: the fourth tautology, in a file outside its scope, filed on the backlog as asked) |
| 1.verify.r3 | 1 | ~20 | y | partial (I5: the promoted rule discharged at the neighbouring family only; the position map stayed insertion-only) |
| 1.33 | 1 | ~21 | y | n (gate 1.verify.r3h GATE-FAILED on 1.30's chord, #022, repaired here; the four sibling chords are I6 by #022's own trigger) |
| 1.verify.r3.g1 | 1 | ~20 | y | n (gate 1.verify.r3.g1h ACCEPT at a2 after a transient rerun, #023; I7 recorded) |
| 1.10.r3a (Claude) / r3b (Sol) / r3c (Grok) | 1 / 1 / 1 | 85 turns / — / 19 turns | y / y / y | — |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).

Phase 1 review r2: findings as reconciled — 2 blockers (in tasks 1.29 and 1.31), 3 should-fix required (in tasks 1.30 and 1.32), 1 should-fix taken as a runner chore at the phase boundary (H7, `fix(runner)` with a conformance test), 3 nits (2 taken in task 1.29, 1 in task 1.32); r1 dispositions reopened: 3 of 12 as siblings or families (G1's attestation → H4, G2 → H2, G4 → H1/H5/H6), 9 confirmed; the r1 backlog deferral of F1's emphasis-edge member reopened (H8, its reachability premise false). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 92 (docs/lessons.md lines tagged 1.*, plan.1.*; 84 before this commit + 8). Lessons promoted to CLAUDE.md/AGENTS.md: 5 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2) + 1 pending (via 1.verify.r3). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 10 (the nine of r1 plus 1.25 at a1); other second attempts: 1.23 (capped, journal present) and 1.verify.r2.g1 (stopped by choice), neither a stop signal. Gate reruns: 0. Gate repairs: 2 (1.verify.g1, 1.verify.r2.g1). Coverage at r2 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r1: 0, 0, 0, 0 (1.26–1.28 and the verifiers touched no `packages/*/src`; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.2 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 20.6 / 22.7 / 11.9 ms on the ubuntu / windows / macos runners of the accepted gate. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.28: 14 of 26 (10 of 20 before the r2 chain, 4 of 6 in it). By the transcript measure the two capped first attempts this chain ended at 50 and 56 tool uses and the five that finished first at 28–59 — the r1 reading (the cap sits on the median of the work) stands; only 1.28 and 1.verify.r2.g1 ran under the journal-stub rule, with no cap and no NO-JOURNAL between them.

Phase 1 review r3: findings as reconciled — 2 blockers (in tasks 1.34 and 1.36), 3 should-fix required (in tasks 1.35, 1.37 and 1.38), 1 should-fix taken with its sibling in 1.35 (I4), 1 should-fix recorded (I7, the a2 rerun answered by #023, with a conditional boundary candidate and a flake-signature line), 3 nits (1 taken in task 1.34, 1 taken in the reconciliation — the address redaction, 1 decided — Unicode whitespace stays ASCII); r2 dispositions reopened: 2 of 9 as families or members (H1 → I1, H8 → I3 and I1), 7 confirmed; the `[1.30]` backlog deferral of the break-inside-a-mark member reopened (its reachability premise false, as H8's was at r2). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 116 (docs/lessons.md lines tagged 1.*, plan.1.*; 106 before this commit + 10). Lessons promoted to CLAUDE.md/AGENTS.md: 6 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3) + 2 pending (via 1.verify.r4). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 11 (the ten of r2 plus 1.29 at a2 — procedure, not budget); capped first attempts this chain: 1.29 and 1.30 (both continued under the stub rule, no stop signal). Gate reruns: 1 (1.verify.r3.g1h a1 → a2, transient, #023). Gate repairs: 3 (1.verify.g1, 1.verify.r2.g1, 1.verify.r3.g1). Coverage at r3 (three CI legs and both cold builds agree): statements 99.64 %, branches 97.73 %, functions 100 %, lines 100 %; delta vs r2: +0.01, +0.08, 0, 0 (1.29–1.31 added 153 tests; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 11.5 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 26.4 / 20.7 / 14.3 ms on the ubuntu / windows / macos runners of the accepted gate; source commit p95 56.1 / 59.9 ms isolated (G9, evidence only). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.33: 18 of 31 (14 of 26 before the r3 chain, 4 of 5 in it: 1.31, 1.32, 1.33 and the verifiers at a1; 1.29 and 1.30 capped). By the transcript measure the two capped first attempts this chain ended at 50 tool uses each and the three that finished first at 48 (1.31, one turn under the cap), 38 and 22; the whole chain ran under the journal-stub rule, which turned both caps into continued attempts rather than NO-JOURNAL stops. Turn-budget decision unchanged: 80 conditional on Michael's OK at the Phase 2 boundary.
