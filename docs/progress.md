# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (#review-1-r0) → fix chain 1.13–1.22 + 1.verify.r1 → gate 1.verify.r1h ACCEPT → review r1 FAIL (#review-1-r1) → fix chain 1.23–1.27 + 1.verify.r2 → gate 1.verify.r2h GATE-FAILED (#021) → 1.28 + 1.verify.r2.g1 → gate 1.verify.r2.g1h ACCEPT → review r2 FAIL (#review-1-r2) → fix chain 1.29–1.32 + 1.verify.r3 → gate 1.verify.r3h GATE-FAILED (#022) → 1.33 + 1.verify.r3.g1 → gate 1.verify.r3.g1h ACCEPT a2 (#023) → review r3 FAIL (#review-1-r3) → fix chain 1.34–1.38 + 1.verify.r4 → gate 1.verify.r4h GATE-FAILED (#024) → 1.39 + 1.verify.r4.g1 → gate 1.verify.r4.g1h ACCEPT a1 → review r4 FAIL (#review-1-r4; Grok derived, #025) → fix chain 1.40–1.44 (four of five to STUCK with the work done, each recovered by a `retry` with a lesson; #026–#028) + 1.verify.r5 → gate 1.verify.r5h ACCEPT a1 → review r5 (three independent reviewers, Grok on the usage reset, #027) → this reconciliation. → fix chain 1.45–1.48 + 1.verify.r6 → gate 1.verify.r6h ACCEPT a2 after a transient rerun (#029) → review r6 FAIL (#review-1-r6) → fix chain 1.49–1.54 + 1.verify.r7 (four principal decisions in the chain, #030–#034; 1.54 integrated from a capped attempt, #review-1-r7 M3) → gate 1.verify.r7h GATE-FAILED (#035) → 1.55 + 1.verify.r7.g1 → gate 1.verify.r7.g1h GATE-FAILED (#036) → 1.56 + 1.verify.r7.g2 → gate 1.verify.r7.g2h ACCEPT a1 → review r7 FAIL (#review-1-r7) → fix chain 1.57–1.61 + 1.verify.r8 → gate 1.verify.r8h GATE-FAILED (#037) → 1.62 + 1.verify.r8.g1 → gate 1.verify.r8.g1h ACCEPT a1 → review r8 FAIL (#review-1-r8: Claude PASS, Sol FAIL, Grok PASS) → fix chain 1.63–1.65 + 1.verify.r9 (1.64 six caps across two `retry`s, #038) → gate 1.verify.r9h GATE-FAILED (#039) → 1.66 + 1.verify.r9.g1 → gate 1.verify.r9.g1h ACCEPT a1 → review r9 FAIL (#review-1-r9: Claude FAIL, Sol FAIL, Grok PASS; dispositions per Michael's D1, #041) → 1.9.r1 ACCEPT a1 (on 287c884, #002-outline-feel) + fix chain 1.67–1.68 + 1.verify.r10 → gate 1.verify.r10h ACCEPT a2 after a transient rerun (#042) → review r10 PASS (#review-1-r10; Sol on a #025 retry) (this commit) → 1.close.
Review attempt r10 at 3e4a3f6, scoped by Michael’s D1 (#041) to confirming O1/O3 and the r9 dispositions, blockers only: Claude PASS (0/0/0), Sol PASS (0/0/0), Grok PASS (0/0/0) → reconciliation verdict PASS (DECISIONS #review-1-r10). The human round trip (1.9.r1) was ~250 words on 287c884 in BBEdit (cmark) and VS Code, none broken; the closing tree’s delta (1.67’s format.ts rule) is evidenced by the 100-cell pair sweep on three OSes. No promotion into 2.verify; seven backlog lines.
Next: this commit integrates as task(1.10.r10d) → 1.close (runner execution; preconditions close.mjs:59–66) → CLOSED 1 → the Phase 2 boundary items of handoff 038 (D3–D5 of proposal 001 on Michael’s word, the boundary fix(runner) list, branch and worktree cleanup).
Open human gates: none. Plan requests: none. Blocked: none. Open asks to Michael at the boundary: gate.sh gc, the phase/1 push, the turn-budget raise, the 429 and reviewer usage-limit signal, Grok’s read-only mode.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r8 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN, stacked on the newest unmerged one (#review-0-r1 deviation 29, handoff 026); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns with the journal stubbed first by `printf` and the suite run once (the 80-turn raise conditional on Michael's OK at the Phase 2 boundary); after a `gate.sh rerun` that accepts, never run the `--resume` doctor names — #023's abandon once per rerun (two instances, #029); before any `retry`, the capped logs moved aside and a principal lesson line that orients without widening scope (#027, #028); a 429 in a transcript is the harness's stop, not the agent's — wait for the reset Michael names; before any single-reviewer retry, #025's procedure, and a Grok 402 is never retried without Michael. No lessons line, task text, DECISIONS entry, backlog line or handoff written from #review-1-r8 on quotes the promise literal — "print the DONE promise for <id>" is the phrase; the files whose job is to say what to print (CLAUDE.md/AGENTS.md step 6, RUNNER-SPEC, the runner's prompt line) carry it by design (#review-1-r8 N6 restating #review-1-r7 M3 (a)); `transcriptHasDone` reads only the assistant's own text blocks at the boundary `fix(runner)` (M3 (b)).
Reviewer record (#review-1-r10): three independent reports (Sol alone on a #025 retry); findings 0 / 0 / 0, no unique, shared or disagreeing finding; cumulative unchanged (twenty-six shared, eight disagreements, low-rater Grok 6, Claude 2, Sol 2); Grok’s PASS-with-blockers count 4 of 6 counted attempts, its fourth consecutive zero-finding PASS (r7–r10).

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [1.67] 2026-09-24T09:01:47Z Task: Phase 1 review fix (DECISIONS #review-1-r9 O1 — Claude r9 finding 1, blocker, taken under Michael's stop rule DECISIONS #041 D1; packages/core/src/format.ts, the serializer of passed tasks 1.1, 1.40, 1.57 and 1.58, whose records are not edited — a new task per DECISIONS #016): two flanking runs of different marks made adjacent, the first ending in punctuation, serialise their delimiters as one run and the bytes parse to a different tree. Status: In progress.
- [1.67] 2026-09-24T09:21:46Z Task: Phase 1 review fix (DECISIONS #review-1-r9 O1 — Claude r9 finding 1, blocker, taken under Michael's stop rule DECISIONS #041 D1): two flanking runs of different marks made adjacent, the first ending in punctuation, serialise their delimiters as one run and the bytes parse to a different tree. Status: Complete. `git status` clean at start (HEAD `4043e7d`, attempt 1's own recovery commit), so no recovery commit was needed; this attempt's own stub was appended by one `printf` and committed first (`wip(1.67): journal stub (attempt 2)`) — attempt 1's stub (09:01:47Z) is unedited. **This attempt is the remaining step and nothing else**: attempt 1 built the whole deliverable (its two commits carry the handlers, the sweep, the corpus legs, the backlog line and its lesson) and capped before the mutation, the two suite runs, the count readings and the journal line, so this attempt verified the carried-over diff against every acceptance sentence rather than redoing it (lesson [1.57]) and ran the six things no attempt had reached. No scope file was edited this attempt. `node_modules` was present, so `pnpm install --frozen-lockfile` was not re-run; no manifest and no lockfile changed (`check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`, the same figures 1.verify.r9.g1 recorded — zero new dependencies, PRD §4). **(1) The installed handlers, read before anything (task step 1).** `node_modules/.pnpm/mdast-util-to-markdown@2.1.2/node_modules/mdast-util-to-markdown/lib/handle/emphasis.js` and `strong.js`: the **marker is chosen** at `emphasis.js:20` (`const marker = checkEmphasis(state)`) and `strong.js:20` (`checkStrong(state)`), and each utility reads `state.options` alone — `util/check-emphasis.js:10` and `util/check-strong.js:10`, both `state.options.<construct> || '*'`, with a throw at lines 12–18 for any marker but `*` or `_`; nothing in either utility looks at the node, its parent or the bytes already written, which is the defect's whole cause. The **flanking neighbours are encoded** at `emphasis.js:33–48` and `strong.js:33–48` — `encodeInfo(info.before.charCodeAt(info.before.length - 1), betweenHead, marker)` then `encodeInfo(info.after.charCodeAt(0), betweenTail, marker)`, each followed by the `open.inside` / `close.inside` character-reference rewrite of the run's own first/last character — and the neighbours *outside* the run are reported at `:54–57` (`state.attentionEncodeSurroundingInfo = { after: close.outside, before: open.outside }`). So both built-ins encode the outside neighbour's flanking and neither can see that its outside neighbour is itself an adjacent run's delimiter. `util/encode-info.js:41–70` is the table the app mirrors, and `:51` (`marker === '_'`) is its one stricter column, the letter-outside/letter-inside cell that encodes **both** letters for `_` where `*` encodes neither. **(2) The diff, read against the task's steps (2)–(4).** `packages/core/src/format.ts`: `encodeInfo` takes the `marker` third argument the installed table takes, so the `_` column exists here too (`handleDelete`'s two calls pass its `~`, whose behaviour is unchanged — `~` takes the `*` branches); `opensBesideAttentionRun` (exported) is the predicate, and its doc comment states the rule once with its zero-width ownership answer (a childless run's `between` is `""`, whose `charCodeAt` is `NaN`, the built-ins' own "other" class, so the rule reads `info.before`'s last character alone and encodes nothing); `handleAttention` is the built-in's body mirrored — same `state.enter(construct)`, `createTracker`, `containerPhrasing` with the single-character `after`, the two `encodeInfo` calls, `state.attentionEncodeSurroundingInfo` — with the marker taken from the predicate instead of `state.options`; `handleEmphasis` (width 1) and `handleStrong` (width 2) wrap it and each carry `peek` returning `*`; both are registered in `toMarkdownExtensions` **after** every extension, as `handleDelete` is, in the last entry `{ handlers: { emphasis: handleEmphasis, strong: handleStrong } }` (`lib/configure.js` merges with `Object.assign` in extension order, and the built-ins are the base it starts from — the doc comment says so). **The rule is a function of the tree and of the bytes together**, and that is the correction attempt 1's lesson records: the bytes half alone (`info.before` ends in `*`) fires on three different things — a sibling run's closing delimiter, this node's own parent's opening delimiter (`***a***`), and an escaped literal `\*` in text — so the predicate also requires the previous *sibling* to be an `emphasis` or `strong` node (`ATTENTION_TYPES`). Legality at the positions the rule fires at is read from the parser's own rules, not assumed: the character before is `*`, which CommonMark §6.2 classifies as punctuation, and an `_` run preceded by punctuation is left-flanking-and-openable whatever follows; the stricter cells `_` has against letters are exactly the ones `encodeInfo`'s `marker === "_"` column encodes, which is why `emphasis("(b)") + strong("z") + text("w")` is written `*(b)*__&#x7A;__&#x77;` and is a fixed point. **(3) The sweep and its count, read from the run.** `packages/editor/test/adjacent-runs.test.ts` (new, 344 lines) generates `PAIRS` from its axis arrays — `MARK_NAMES` = {emphasis, strong, delete, link, inline_code} (**5**) × the same 5 as the second mark × `TAILS` (**4**: `ascii punctuation` `)`, `ascii letter` `b`, `astral symbol` U+1F600, `astral letter` U+10330 — CLAUDE.md's astral rule, one non-BMP symbol and one non-BMP letter) — and its first `it` asserts `PAIRS.toHaveLength(MARK_NAMES.length * MARK_NAMES.length * TAILS.length)`, never a literal. **Both numbers, from a temporary `console.log(JSON.stringify({...}))` in the summary test run once under `npx vitest run packages/editor/test/adjacent-runs.test.ts --reporter=verbose` and reverted from a copy (`git status --short` empty after): `{"pairs":100,"markNames":5,"tails":4,"product":100,"fixtures":65,"joins":42,"spaces":0,"fixturesWithJoins":11,"fixturesWithSpaces":0}` — 100 = 5 × 5 × 4, the product the test asserts.** Deviation recorded, not a conflict: the task text's parenthetical says "75 at the review's axes", which is 5 × 5 × 3 counting `astral` as one class; the same sentence names two astral members (a non-BMP symbol and a non-BMP letter) and CLAUDE.md's astral rule asks for both, so they are two values of the axis and the product is 100. The acceptance binds on "the sweep's pair count equal to the product of its axis lengths", which 100 = 100 satisfies; the parenthetical is the only number that differs and no axis was dropped. Each pair asserts, through `pmToMdast` of the editor's own document and `format`, that `format(parse(out)) === out` **and** that `parse(out)` holds the tree the editor made, compared by node types and every leaf's text (`shape`, `toEqual`) — DECISIONS #022, the bytes cannot tell the two trees apart — and it asserts it twice, once on the adjacent tree and once on the same tree reached by the Delete keystroke (`tr.delete` of the single space), with the two byte strings asserted equal. **(4) The named guards** (their titles, from the file's `describe` "the two pairs DECISIONS #review-1-r9 O1 names, each by the keystroke that reaches it"): "Backspace joining `*(b)*` and `**z**` writes `*(b)*__z__`, not `*(b)***z**`"; "Delete of the single space in `*(b)* **z**` writes `*(b)*__z__`, not `*(b)***z**`"; "Backspace joining `**(b)**` and `*z*` writes `**(b)**_z_`, not `**(b)***z*`"; "Delete of the single space in `**(b)** *z*` writes `**(b)**_z_`, not `**(b)***z*`" — the two pairs the review found, each by both of the two one-keystroke routes the task names (the join is `tr.join` at the second paragraph's start, what `baseKeymap`'s `joinBackward` performs) — beside four instances on top of the invariant: "a third run beside the second takes `*` again, because its neighbour closed with `_`" (`*a)*__z__*y*`), "a letter after the second run is encoded, because `_` forbids the intraword run" (`*(b)*__&#x7A;__&#x77;`, the one cell only the `_` column reaches), "a nested run is not an adjacent one: `strong[emphasis]` stays `***a***`" and "an escaped `*` in text is not a run's delimiter: `text('*') + emphasis` stays `\**z*`" — the last two are attempt 1's lesson's two other classes, asserted so the tree half of the rule cannot be dropped. The nested guard asserts node **types** only (`***a***` parses as emphasis-around-strong where the editor nests strong-around-emphasis, the ambiguity `schema.test.ts` already pins), which is why it is the one guard that reads `types` rather than `shape`. **(5) Legs (a) and (b) over the index, with their transaction counts from the same run.** Both legs read the keys of `fixtures/markdown/index.json` (**65** fixtures) and both ran on every one of them (the summary test asserts `joinCounts.size` and `spaceCounts.size` are `names.length`). Leg (a), the join of every adjacent pair of paragraphs in one parent — the transaction `joinBackward` performs at the second's start: **42 joins over 11 of the 65 fixtures**, asserted positive over the corpus (`toBeGreaterThan(0)`). Leg (b), the deletion of every single space that is the only character between two marked runs: **0 members at this index** — the corpus holds no such space, which the summary test says in place and which the task's own sentence provides for: the sweep of (3) is that leg's instance, running the same Delete keystroke on all 100 of its pairs and asserting the block it leaves, and the positivity is asserted over the two together (`spaces + PAIRS.length`) with the count read from the run rather than pinned, so a fixture that later adds a member is swept rather than ignored. After each transaction the changed block is cut out with `blockAlone` from `packages/editor/test/block-alone.ts` (task 1.66's shared helper) and asserted a fixed point of `parse ∘ format` whose parse holds the block's own tree — block-local, so the family stays inside the 30_000 ms budget on every runner (DECISIONS #039); the file's whole 240 tests ran in the suite without approaching it. **(6) The mutation (task step 5), run this attempt and not committed.** The single registration line `{ handlers: { emphasis: handleEmphasis, strong: handleStrong } }` removed from `toMarkdownExtensions` by a scripted edit, `npx vitest run packages/editor/test/adjacent-runs.test.ts packages/core/test/adjacent-attention.test.ts --reporter=verbose` → **Tests 10 failed | 234 passed (244)**, then restored from a copy in the same tool call (`git status --short` empty after). The ten, with their received bytes: the sweep's two punctuation-edged cells — `emphasis ending in ascii punctuation then strong` received `\*a)\***z**\n` for `*a)***z**\n` and `strong ending in ascii punctuation then emphasis` received `\*\*a)\*\**z*\n` for `**a)***z*\n` (the escaping is the second pass's: the first pass's `*a)***z**` re-parses as text, so the fixed-point leg is what catches it); the four O1 route guards — `*(b)***z**\n` for `*(b)*__z__\n` twice (join and Delete) and `**(b)***z*\n` for `**(b)**_z_\n` twice; the third-run guard — `*a)***z***y*\n` for `*a)*__z__*y*\n`; the intraword-letter guard — `*(b)***z**w\n` for `*(b)*__&#x7A;__&#x77;\n`; and **two corpus members of leg (a)** — `astral-between-runs.md: join at 9` received `*a.*&#x1F600;\*(b)\***a.**&#x10400;~~…` for `*a.*&#x1F600;*(b)***a.**&#x10400;~~(b…` and `astral-neighbour.md: join at 15` received `x 𐐀\*(a)\***a.**&#x1D552; 😀\n` for `x &#x10400;*(a)***a.**&#x1D552; 😀\n`. The nested-`***a***` and escaped-`\**z*` guards stayed **green** under the mutation, as they must: they pin behaviour the fix does not change, and they are the guards that would go red if the rule were stated on the bytes alone. `adjacent-attention.test.ts` also stayed green — it exercises the predicate directly, which the mutation leaves exported and correct; the mutation isolates the registration, which is the defect's own surface. **(7) L11's reproduction (task step 6), evidence only.** `format(parse("*x.*_y_\n"))` is `"*x.*_y_\n"` — **a fixed point after the change**, and its parse is `root,paragraph,emphasis,text,emphasis,text`, two runs. The rule does not fire on it (the character before the second run is `_`, not `*`), and the second run keeps the `_` the parser gave it because a marker is chosen per run from what precedes it. `[review-1-r6 L11]`'s line and its revisit trigger stand either way, as the task says. For the record beside it: `format(parse("*(b)***z**\n"))` is `"\\*(b)\\***z**\n"`, because `*(b)***z**` parses as `text("*(b)*") + strong("z")` — the defect's own bytes, confirmed at this tree — while `format(parse("*(b)*__z__\n"))` and `format(parse("**(b)**_z_\n"))` are both fixed points whose parses are `emphasis + strong` and `strong + emphasis`, the trees the fix writes. **(8) The class found outside scope, filed and positively bounded, never fixed here** (`docs/V1.1-BACKLOG.md`, `[1.67, found outside scope]`, 1 line): the `~~` twin — two adjacent `delete` siblings still share one delimiter run and `handleDelete` has no second marker to take, because GFM's strikethrough construct has exactly one. It is bounded out of every route this task owns: micromark reads `~~a.~~~~b~~` as one run (probed: `format(parse("~~a.~~~~b~~\n"))` is `~~a.\~\~\~\~b~~\n`, one `delete`) and ProseMirror merges adjacent text nodes with equal mark sets, so the sweep's `delete + delete` cells are one run in the editor's own tree and are asserted as such — the same-mark half of L11's premise, which stands. The line carries the fix to apply when a route exists, its revisit trigger (the first mdast producer other than `parse` and `pmToMdast`) and its hard stop `2.verify`. No other disagreement was found: legs (a) and (b) are green over the whole index with no exclusion and no narrowing, and nothing outside the adjacent-pair class turned red. **(9) The suite, run once each this attempt, each one foreground call.** `pnpm lint && pnpm test && cargo test` exit **0**: eslint clean, `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`, `tsc --noEmit` across every typechecked workspace project; **Test Files 47 passed (47), Tests 4816 passed (4816)**, no skip (45 files / 4572 tests at the branch base — the two new files and their 244 tests); coverage 99.52 % statements / 97.25 % branches / 100 % functions / 100 % lines, `format.ts` 99.57 / 99.15 / 100 / 100; `cargo test` `test result: ok` ×3 (0/0/0 — the Rust surface is Phase 2). Then `e2e/web/node_modules/.bin/playwright install chromium` (lesson [1.50]) and the full e2e/web suite once: **102 passed**, 0 failed, 33.6 s, over **32** spec files (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`, DECISIONS #review-1-r6 L10 — the nested `e2e/web/tests/placeholder.spec.ts` counted; no spec added or removed by this task, which touches no file under `e2e/`). This container is Linux; no macOS or Windows pass is claimed. **Acceptance greps, all run this attempt.** `grep -c 'export function handleEmphasis\|export function handleStrong' packages/core/src/format.ts`: **2**. The registration line in `toMarkdownExtensions`: **1** (both names in it). `grep -c 'it.fails' packages/editor/test/adjacent-runs.test.ts`: **0**. `grep -c '\.skip(' packages/editor/test/adjacent-runs.test.ts`: **0**. Both on `packages/core/test/adjacent-attention.test.ts`: **0**, and `it.fails` in `format.ts`: **0** (lesson [1.63] — no prose line cites the token a `grep -c … is 0` clause greps for). `git diff --stat` against branch base `faaab4b` (`git merge-base HEAD phase/1`) touches exactly the scope files and nothing else: `packages/core/src/format.ts` (+189/−17), `packages/editor/test/adjacent-runs.test.ts` (new, 344), `packages/core/test/adjacent-attention.test.ts` (new, 44), `docs/V1.1-BACKLOG.md` (+1), `docs/lessons.md` (+1) and `docs/progress/journal-main.md` (+2: attempt 1's stub and this line) — no file under `packages/editor/src`, `apps/`, `e2e/` or `fixtures/`, no fixture added, no config or workflow file, and `git diff --name-only faaab4b HEAD | grep -E 'package.json|lock|Cargo'` prints nothing: no manifest or lockfile change, zero new dependencies (PRD §4). The `packages/core/test` file is the allowance the scope sentence makes ("a handler unit guard only if the new file cannot reach a branch"): `opensBesideAttentionRun`'s `parent === undefined` branch is the parent `mdast-util-to-markdown` types as possibly absent and only `root` is ever dispatched with, so no phrasing node in the editor's tree can reach it — its four `it`s are one per clause of the predicate, enumerated from the diff (CLAUDE.md), and the other three clauses are reached through the editor's own trees by the sweep as well. Procedure note (DECISIONS #review-1-r2 H7): PROMPT.md's pre-1.verify.r2 iteration list is superseded by CLAUDE.md's stub-first `## Every iteration` list, which this attempt followed. Files: packages/core/src/format.ts, packages/editor/test/adjacent-runs.test.ts, packages/core/test/adjacent-attention.test.ts, docs/V1.1-BACKLOG.md, docs/lessons.md, docs/progress/journal-main.md (this line; attempt 1's line at 09:01:47Z is unedited). Tests: the 100-pair sweep and its product assertion in adjacent-runs.test.ts, the eight named guards beside it (the four O1 routes, the third run, the intraword letter, the nested run, the escaped literal), legs (a) and (b) over all 65 fixtures of the index with the block-local `blockAlone` oracle, the summary test's count and positivity assertions, the four clause guards of `opensBesideAttentionRun` in packages/core/test/adjacent-attention.test.ts, the full `pnpm test` suite (4816 tests, 47 files), `cargo test` (vacuous, Phase 2), the full e2e/web Playwright suite (102 tests over 32 spec files) — all green, all run by me this attempt, and the mutation run and reverted by me this attempt. Iterations used: 2. First attempt passed: n. Tool calls: 17. Notes: nothing outstanding for 1.67; the `~~` twin is the one filed class and is out of every route the product has (backlog, hard stop 2.verify). Next in PRD §8 is 1.68, then 1.verify.r10.
- [1.68] 2026-09-24T09:33:33Z Task: Phase 1 review fix (DECISIONS #review-1-r9 O3 — Claude r9 finding 3, should-fix, taken under DECISIONS #041 D1; e2e/web/editor-soft-line-breaks.spec.ts, a bounded re-click for the computed-point-click caret precondition at line 181). Status: Complete. `git status` clean at start (HEAD `114e204`, task 1.67's integrated commit), so no recovery commit was needed; this attempt's own stub (09:33:33Z) was appended by one `printf` and committed first (`wip(1.68): journal stub`, `ed2f1bb`). **The fix.** `e2e/web/editor-soft-line-breaks.spec.ts` gains one helper, `clickAtComputedPoint(page, geometry, expected)` (new, after `caret`, lines 116–142): up to three tries, each awaiting `geometry()` immediately before its own `page.mouse.click`, then `expect.poll(() => caret(page)).toEqual(expected)` with no custom timeout (Playwright's own default, so no timing magnitude from any one run enters the test, per #037/#039's lesson) — the first try whose poll resolves returns; a try that times out is caught and retried with freshly re-read geometry; three tries exhausted falls through with no throw. The G4 first-line case's only computed-point click (was inline at lines 160–178, `clickPoint`) is now `geometry`, a closure over the same `page.evaluate` (unchanged body: the `Range` over the last character of `alpha`, `rect.right - 1`/`rect.top + rect.height / 2`), called through the new helper with `expectedCaret = { kind: "text", text: "alpha\nbeta gamma", offset: "alpha".length }`; the call site's own `expect.poll(() => caret(page)).toEqual(expectedCaret)` immediately after is unchanged in form and is what still asserts the precondition for real (a genuinely lost click still fails there, at Playwright's full per-assertion timeout) — exactly as the task text requires. The other `mouse.click` site this file has (line 61's `.click()` and line 238's `.click()` are locator clicks, not computed-point clicks) is none: `grep -c 'mouse.click'` at the branch base (`114e204`) is **1** (the one call site); after, **2** (the one call site plus one mention inside the new helper's own doc comment, `\`page.mouse.click\``, line 120) — one real call site, unchanged. No arrow route added, no timeout lengthened. **Verification, this container (Linux only).** `pnpm install --frozen-lockfile` first (`git status --short` empty after — no manifest or lockfile change). `pnpm lint` exit 0 (eslint clean over the whole tree, `check-deps` unchanged at 61 npm + 4 cargo, `tsc --noEmit` across all 8 typechecked workspace projects — e2e/web has no typecheck script, consistent with its existing `package.json`). `pnpm test`: **Test Files 47 passed (47), Tests 4816 passed (4816)**, no skip, including `tests/no-caret-chords-in-e2e.test.ts` (81 tests, unaffected — no bare Home/End or chord added). `cargo test`: `test result: ok` ×3 (0/0/0, the Rust surface is Phase 2). `e2e/web/node_modules/.bin/playwright install chromium` (lesson [1.50]) then this spec alone with `--repeat-each=5`: **20 passed** (0 failed, 4.1s; 4 tests × 5 repeats), read from the list reporter. The full e2e/web suite once, from the repo root: **102 passed**, 0 failed, 31.8s, over **32** spec files (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`, DECISIONS #review-1-r6 L10's form — no spec added or removed this task). **Acceptance greps, all run this attempt.** `grep -c '\.skip(\|\.only('` on the spec: **0**. `git diff --stat` against branch base `114e204` touches exactly `e2e/web/editor-soft-line-breaks.spec.ts` and `docs/progress/journal-main.md` (this stub and its completion) — no file under `packages/`, `apps/`, `fixtures/`, any other spec, any config or workflow file, no manifest or lockfile; zero new dependencies (PRD §4). No defect found outside this task's scope, so `docs/V1.1-BACKLOG.md` is untouched; nothing new was learned beyond what #037/#039 already carry, so `docs/lessons.md` is untouched this attempt (permitted — the acceptance's scope list allows it unedited). Procedure note (DECISIONS #review-1-r2 H7): PROMPT.md's pre-1.verify.r2 iteration list is superseded by CLAUDE.md's stub-first `## Every iteration` list, which this attempt followed. Files: e2e/web/editor-soft-line-breaks.spec.ts, docs/progress/journal-main.md (this line; the 09:33:33Z stub is unedited). Tests: `clickAtComputedPoint` and its one call site (the G4 first-line case), `tests/no-caret-chords-in-e2e.test.ts` (81 tests, unaffected), the full `pnpm test` suite (4816 tests, 47 files), `cargo test` (vacuous, Phase 2), this spec alone at `--repeat-each=5` (20/20), the full e2e/web Playwright suite (102 tests over 32 spec files) — all green, all run by me this attempt. Iterations used: 1. First attempt passed: y. Tool calls: 19. Notes: nothing outstanding for 1.68; next in PRD §8 is 1.verify.r10.
- [1.verify.r10] 2026-09-24T09:42:58Z Task: Phase 1 verification for review attempt r10 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change allowed), run the full suite in the container, confirm the phase head is ready for the three-OS CI run that the CI gate performs, and carry the promote-lessons step for the r10 chain — nine candidates weighed at DECISIONS #review-1-r9, one promoted and eight recorded. Status: Complete. `git status` clean at start (HEAD `0ff43df`, task 1.68's integrated commit), so no recovery commit was needed; this attempt's own stub was appended by one `printf` and committed first (`wip(1.verify.r10): journal stub`, `b4ab75c`). `pnpm install --frozen-lockfile` was run once (lesson [0.4]); `git status --short` was empty after — no manifest or lockfile change. **Promote-lessons (a), verbatim and identical, appended to both CLAUDE.md and AGENTS.md as the next `## Code rules` bullet after the r8 N1/N2/N3 bullets** (`ralph/check-agent-rules.sh` confirmed identical below line 1 after the edit; `tests/agent-rules.test.ts` green in the suite below). (a) O1's bullet — "A round-trip family's corpus legs seeded from the writing surface include one transaction that joins two blocks and one that deletes the whole separator between two inline nodes, and the family enumerates ordered pairs of the schema's mark kinds × the first run's edge-character class (punctuation, letter, astral), because a leg that edits one position inside one block never makes two nodes adjacent (DECISIONS #review-1-r9 O1)." — discharged by 1.67's pair sweep and its two corpus legs in `packages/editor/test/adjacent-runs.test.ts`: the sweep's own product assertion, `describe("the sweep: every ordered pair of adjacent marked runs, over every class of the first run's last character", …)` > `it("the sweep is exactly the product of its axes", …)`, and its 100 per-pair cases, each titled `` `${first} ending in ${tail.key} then ${second}: the bytes hold both runs, by both routes` ``; the two corpus legs under `describe("the writing surface's own destructive transactions over the corpus", …)` — leg (a), the block-join transaction, titled per fixture `` `${name}: leg (a), every adjacent pair of paragraphs joined as joinBackward joins them` ``, and leg (b), the separator-deletion transaction, titled per fixture `` `${name}: leg (b), every single space between two marked runs deleted` `` — plus the summary guard `it("the two legs ran over the whole index, and leg (a) is positive over the corpus", …)`. (b) Recorded, not promoted: Claude's r9 lesson that a deferral reading "unreachable from the editor" names the commands it was read against addresses backlog authors and reviewers — the `[review-1-r9 O1]` correction line in `docs/V1.1-BACKLOG.md` carries it. (c) Recorded, not promoted: Claude's r9 lesson on a block-local oracle's bridge to the document is finding O2's, backlogged as `[review-1-r9 O2]` with a `2.verify` trigger. (d) Recorded, not promoted: Sol's three r9 lessons (a neither-neighbour mark set assigned to the following run; an early cursor-map return discharging the carried-mark invariant one route short; the native action that reaches a state varies the result) are findings O7–O9's, backlogged with a `2.verify` trigger each. (e) Recorded, not promoted: Grok's three r9 lessons restate rules already in CLAUDE.md — the r8 N1 (cursor rule at both block edges, every mark kind) and N2 (correspondence anchored on the live document) bullets, and the DECISIONS #039 turn-cap procedure; no new rule text needed, so no CLAUDE.md edit for these three. No other procedure change this attempt: no source, test, spec, fixture or config file was touched. **Verification, this container (Linux; no other OS claimed), each suite run once.** `pnpm lint` exit 0 (eslint clean; `check-deps: 61 npm + 4 cargo direct dependencies, all allowlisted`; `tsc --noEmit` across all 8 typechecked workspace projects). `pnpm test` **Test Files 47 passed (47), Tests 4816 passed (4816)**, no skip — `tests/agent-rules.test.ts` (1 test) and `tests/check-deps.test.ts` (64 tests) among them, both green; coverage 99.52 % statements / 97.25 % branches / 100 % functions / 100 % lines, `format.ts` 99.57 / 99.15 / 100 / 100. `cargo test` (workspace `desktop_lib`, `desktop`, doc-tests) `test result: ok` ×3 (0/0/0 — the Rust surface is Phase 2). `e2e/web/node_modules/.bin/playwright install chromium` (lesson [1.50]) then the full e2e/web suite once: **102 passed**, 0 failed, 32.1s, over **32** spec files (`find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l`, DECISIONS #review-1-r6 L10 — the nested `e2e/web/tests/placeholder.spec.ts` counted; no spec added or removed this task), `latency.spec.ts` green among them. **Acceptance greps.** `grep -c 'it.fails' packages/core/test/*.ts packages/editor/test/*.ts` — 0 for every one of the 44 matched files. `git diff --stat` against `0ff43df` (task 1.68, the previous task) touches exactly `CLAUDE.md`, `AGENTS.md` and this journal — no dependency manifest and no lockfile in the tree changed, zero new dependencies (verify-task rule, PRD §4). `grep -c 'review-1-r9 O1' CLAUDE.md` and the same on `AGENTS.md`: **1** each. `bash ralph/check-agent-rules.sh` exits 0; `tests/agent-rules.test.ts` passed (counted in the 4816 above). No defect found outside this task's scope; `docs/lessons.md` is untouched this attempt — (b)–(e) are recorded in this journal line itself, as the task text's "record, do not promote" asks, and nothing new was learned beyond what the r9-chain lessons already carry. The phase-1 head (this task's squashed commit) is ready for the CI gate's three-OS run. Procedure note (DECISIONS #review-1-r2 H7): PROMPT.md's pre-1.verify.r2 iteration list is superseded by CLAUDE.md's stub-first `## Every iteration` list, which this attempt followed. Files: CLAUDE.md, AGENTS.md, docs/progress/journal-main.md (this line). Tests: the full `pnpm test` suite (4816 tests, 47 files, including `tests/agent-rules.test.ts` and `tests/check-deps.test.ts`), `cargo test` (vacuous, Phase 2), the full e2e/web Playwright suite (102 tests over 32 spec files) — all green, all run by me this attempt. Iterations used: 1. First attempt passed: y. Tool calls: 15. Notes: nothing outstanding for 1.verify.r10; next is the 1.verify.r10h CI gate's three-OS run, then review set 1.10.r10a/b/c.
- [1.10.r10d] 2026-09-24T15:46:34Z Task: reconciliation of Phase 1 review attempt r10 at 3e4a3f6 (verifier 1.verify.r10, gate 1.verify.r10h ACCEPT a2 after a transient rerun — DECISIONS #042 —, run 35984824027; review scope per Michael's D1, #041). Claude PASS (0/0/0), Grok PASS (0/0/0), Sol PASS (0/0/0) on a #025 single-reviewer retry after a Codex usage limit, independent by construction. Verdict PASS; one Fable brief (the verdict question: PASS, high confidence). Status: Complete. Two commits of phase/1..handoff/038 cherry-picked first, docs only, no conflict; reports copied to docs/reviews/phase-1-r10-{claude,sol,grok}.md (cmp clean); 1.9.r1's accepted record appended to DECISIONS #002-outline-feel with its four deviations (tree 287c884, ~250 words, VS Code for Typora, the pnpm --filter command); DECISIONS.md#review-1-r10 written (verdict: PASS, per-criterion verdict, O1/O3 and the r9 dispositions confirmed, reviewer risks read against D1's blocker test, the Sol retry, the reviewer record, turn budget, gate numbers, no promotion into 2.verify with the nine candidates recorded); seven backlog lines appended; docs/progress.md regenerated; no task appended, EXPECTED_COUNT 285; generate-tasks --check byte-identical, validate-tasks OK 285, ralph/test/run.sh 83 pass / 0 fail.

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.23 | 2 | ~22 (a1) + ~12 (a2) | n (a1 `error_max_turns` at 51, exit 1, journal present — no stop signal) | n (its three copy cases read the OS clipboard: the windows CRLF gate failure, #021, repaired by 1.28) |
| 1.24 | 1 | ~30 | y | y (H2: the pending segment keeps one keystroke time, so a flush splits a burst longer than the window) |
| 1.25 | 2 | n/a (the fields are absent from its entries) | n (NO-JOURNAL at a1, `error_max_turns` at 51) | y (H1/H5: the atom itself at a block end; H6: a cell's line ending) |
| 1.26 | 1 | ~24 | y | n |
| 1.27 | 1 | ~20 | y | n (H9: one tautology, nit, taken in 1.32) |
| 1.verify.r2 | 1 | ~14 | y | partial (H7: the wrapper `ralph/PROMPT.md` not carried along — a runner file, fixed at the boundary) |
| 1.28 | 1 | ~18 | y | partial (H4: the spy removed the suite's last real clipboard write — #021's scope decision) |
| 1.verify.r2.g1 | 2 | ~12 (a1) + ~20 (a2) | n (a1 exit 0 at `num_turns` 11, stopped by choice on H7; no stop signal) | n |
| 1.10.r2a (Claude) / r2b (Sol) / r2c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.29 | 3 | ~50 (a1) + ~16 (a2) + ~10 (a3) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal; a2 `success` at 21 with DONE and NO-JOURNAL — the stub completed in place, `chore(1.29)`) | partial (I1: the atom at a *mark's* edge, deferred by 1.30's backlog line; the deletion leg's ranges stop at the block's end) |
| 1.30 | 2 | ~50 (a1) + ~20 (a2) | n (a1 `error_max_turns` at 51, journal present — no stop signal) | y (I3: the mark-edge clause splits a link-marked run, a regression; I1's deferral written by this task on a false reachability claim) |
| 1.31 | 1 | ~34 (49 turns, 48 tool uses — one under the cap) | y | n |
| 1.32 | 1 | ~30 | y | n (I8: the fourth tautology, in a file outside its scope, filed on the backlog as asked) |
| 1.verify.r3 | 1 | ~20 | y | partial (I5: the promoted rule discharged at the neighbouring family only; the position map stayed insertion-only) |
| 1.33 | 1 | ~21 | y | n (gate 1.verify.r3h GATE-FAILED on 1.30's chord, #022, repaired here; the four sibling chords are I6 by #022's own trigger) |
| 1.verify.r3.g1 | 1 | ~20 | y | n (gate 1.verify.r3.g1h ACCEPT at a2 after a transient rerun, #023; I7 recorded) |
| 1.10.r3a (Claude) / r3b (Sol) / r3c (Grok) | 1 / 1 / 1 | 85 turns / — / 19 turns | y / y / y | — |
| 1.34 | 2 | ~50 (a1) + ~25 (a2) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal) | partial (J1: the atom clause holds; the family's next member is the serializer's, `format.ts`) |
| 1.35 | 2 | ~50 (a1) + ~19 (a2) | n (a1 `error_max_turns` at 51, stub present) | n (J1's link member runs through its `outermostMark` rule as designed; the serializer is at fault) |
| 1.36 | 2 | ~50 (a1) + ~24 (a2) | n (a1 `error_max_turns` at 51, stub present) | partial (J2: the paragraph → setext-heading pair missing from `needsBlankLine`; the kind table lacked it) |
| 1.37 | 2 | ~50 (a1) + ~35 (a2) | n (a1 `error_max_turns` at 51, stub present — its stub carries no timestamp, J8) | partial (J3: the defect it found and pinned as `it.fails`, deferred on scope; taken in 1.43) |
| 1.38 | 1 | ~34 | y | partial (J4/J5: the guard's regex misses Shift/Alt chains and its allowlist reason is false; gate 1.verify.r4h GATE-FAILED on its `ArrowUp` route, #024, repaired by 1.39) |
| 1.verify.r4 | 1 | ~39 | y | n |
| 1.39 | 1 | ~17 | y | n (J6: the `[plan.1.verify.r4h.r0]` line it left names two of three sites — a reconciliation correction) |
| 1.verify.r4.g1 | 1 | ~25 | y | n (gate 1.verify.r4.g1h ACCEPT at a1) |
| 1.10.r4a (Claude) / r4b (Sol) / r4c (Grok) | 1 / 1 / 2 container runs (a 402, then `retry` alone — #025) | 110 turns / — / 19 turns (13 on the 402 run) | y / y / n (STUCK 1.10.r4 on the 402) | — (Grok's report derived) |
| 1.40 | 3 → STUCK, retry 1 | ~50 (a1) + ~23 (a2) + ~13 (a3) + ~29 (retry) | n (a1 `error_max_turns` at 51 with the work done; a2/a3 stopped on the task-text conflict #026 decided) | partial (K1: the astral neighbour, a Phase 0 gap its port extended to `~~`; K2: the map defect it found and pinned, taken in 1.46) |
| 1.41 | 3 → STUCK, retry 3 → STUCK, retry 1 | ~51 ×4 + 16 (429) + 52 (no new journal line) + ~38 | n (every cap on re-verification of a finished branch; #027) | partial (K4: the decoded-text assertion its text names is absent) |
| 1.42 | 3 → STUCK, retry 1 | ~51 ×3 + ~18 | n (caps on re-verification) | n (K5's second half: the depth-3 clause has no `KINDS` feeder — 1.48) |
| 1.43 | 3 → STUCK, retry 1 | ~42 / ~13 / ~8 + ~15 | n (DONE withheld on the clause #028 re-read) | n |
| 1.44 | 1 | ~35 | y | n (K6/K8: the guard's quote forms and title — 1.48) |
| 1.verify.r5 | 1 | ~43 | y | n (K7: its journal's spec-file count) |
| 1.10.r5a (Claude) / r5b (Sol) / r5c (Grok) | 1 / 1 / 1 | 113 turns / — / 28 turns | y / y / y | — |
| 1.45 | 2 | ~2 (a1, a 429 session limit — #027's second instance, `NO-JOURNAL 1.45` by the harness) + ~37 | n | partial (L1: the two-run join its `SPLIT_PAIR` never named — 1.49) |
| 1.46 | 2 | ~51 (a1 capped) + ~44 | n | partial (L3: the html-before-soft-break branch its enumeration missed — 1.51) |
| 1.47 | 3 → STUCK, retry 1 | ~51 ×3 (a1 without a stub) + ~42 | n (every cap with the work complete on the branch; the retry after a principal lesson line, #027's procedure) | partial (L8: the corpus-callers clause re-read, recorded at #review-1-r6; L10: its journal's spec-file count) |
| 1.48 | 1 | ~54 | y | n |
| 1.verify.r6 | 1 | ~38 | y | n |
| 1.10.r6a (Claude) / r6b (Sol) / r6c (Grok) | 1 / 1 / 1 | 123 turns / — / 25 turns | y / y / y | — |
| 1.49 | 1 | ~38 | y | partial (M1: its third form widened every occurrence in the join, a code span's literal bytes included — 1.57) |
| 1.50 | 2 | ~51 (a1 capped) + ~25 | n | n (M7: `link.peek`'s three serialisations per link — backlog) |
| 1.51 | 3 → STUCK, retry 3 | ~51 (a1 capped) + ~43 / ~9 (withheld on the conflict #030 decided) + ~37 (withheld on #030's bytes, #031) / ~13 / ~19 | n | partial (M2: the `break` member of its third branch, pinned as-is — 1.58; M4: its `inlineCode` table reads the base handler, not the table extension's — 1.59; M8: its false `peek` comment — 1.59) |
| 1.52 | 3 | ~51 (a1 capped) + ~22 (withheld on the expected-fail contradiction, #032) + ~23 | n | partial (M5: its block-final code-span rule — 1.60) |
| 1.53 | 3 → STUCK, retry 1 | ~39 (429) / ~1 (429, `NO-JOURNAL 1.53`) / ~27 (withheld on the 1.46 pin, #033) + ~13 | n | n (M5 is L6's last tuple; its guard pinned the column only — 1.60) |
| 1.54 | 3 → STUCK, retry 1 | ~51 ×3 (a2 without a stub) + ~51 (capped; integrated on the promise echoed from lessons.md — M3) | n | partial (M3: the record never written and the paste leg's missing assertion — 1.61) |
| 1.verify.r7 | 1 | ~41 | y | n (gate 1.verify.r7h GATE-FAILED a1: two spec defects on windows, #035) |
| 1.55 | 1 | ~33 | y | n (gate 1.verify.r7.g1h GATE-FAILED a1: G4's third route, #036) |
| 1.verify.r7.g1 | 1 | ~24 | y | n |
| 1.56 | 1 | ~50 | y | n (gate 1.verify.r7.g2h ACCEPT at a1) |
| 1.verify.r7.g2 | 1 | ~21 | y | n |
| 1.10.r7a (Claude) / r7b (Sol) / r7c (Grok) | 1 / 1 / 1 | 128 turns / — / 26 turns | y / y / y | — |
| 1.57 | 2 | ~51 (a1 capped, the work built) + ~32 | n | partial (N3: the walk's give-up path unguarded, its doc-comment argument false — 1.65) |
| 1.58 | 2 | ~51 (a1 capped) + ~31 | n | n |
| 1.59 | 2 | ~51 (a1 capped; two probe files swept onto the branch by the recovery commit, removed at a2) + ~44 | n | n |
| 1.60 | 2 | ~51 (a1 capped) + ~34 | n | partial (N1: the typed-marks rule applied to `inlineCode` only, every other mark at both block edges — 1.63) |
| 1.61 | 2 | ~51 (a1 capped) + ~61 | n | n |
| 1.verify.r8 | 1 | ~43 | y | n (gate 1.verify.r8h GATE-FAILED a1: the widget-boundary caret spelling in the two new specs, #037) |
| 1.62 | 2 | ~51 (a1 capped) + ~51 (DONE on the last turn) | n | n (gate 1.verify.r8.g1h ACCEPT at a1) |
| 1.verify.r8.g1 | 1 | ~25 | y | n |
| 1.10.r8a (Claude) / r8b (Sol) / r8c (Grok) | 1 / 1 / 1 | 113 turns / — / 22 turns | y / y / y | — |
| 1.63 | 2 | ~51 (a1 capped, the work built) + ~40 | n | n |
| 1.64 | 7 | 6 × ~51 (three caps, `retry`, three caps, `retry`; #038) + ~25 | n | n |
| 1.65 | 2 | ~51 (a1 capped) + ~26 | n | partial (O4: the `overlapping-edit` reason never reported through `buildMap` — backlogged) |
| 1.verify.r9 | 1 | ~45 | y | n (gate 1.verify.r9h GATE-FAILED a1: the whole-document block-edge oracle over the runners' budget, #039) |
| 1.66 | 3 | ~51 + ~51 + ~31 | n | partial (O2: the block-local bridge asserted against itself — backlogged) |
| 1.verify.r9.g1 | 1 | ~29 | y | n (gate 1.verify.r9.g1h ACCEPT at a1) |
| 1.10.r9a (Claude) / r9b (Sol) / r9c (Grok) | 1 / 1 / 1 | 113 turns / — / 20 turns | y / y / y | — |
| 1.67 | 2 | ~51 (a1 capped, the work built) + ~21 | n | n |
| 1.68 | 1 | ~46 | y | n |
| 1.verify.r10 | 1 | ~36 | y | n (gate 1.verify.r10h ACCEPT a2 after a transient rerun, #042) |
| 1.10.r10a (Claude) / r10b (Sol) / r10c (Grok) | 1 / 1 (a #025 retry after a Codex usage limit) / 1 | 91 turns / — / 22 turns | y / y / y | — |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).

Phase 1 review r2: findings as reconciled — 2 blockers (in tasks 1.29 and 1.31), 3 should-fix required (in tasks 1.30 and 1.32), 1 should-fix taken as a runner chore at the phase boundary (H7, `fix(runner)` with a conformance test), 3 nits (2 taken in task 1.29, 1 in task 1.32); r1 dispositions reopened: 3 of 12 as siblings or families (G1's attestation → H4, G2 → H2, G4 → H1/H5/H6), 9 confirmed; the r1 backlog deferral of F1's emphasis-edge member reopened (H8, its reachability premise false). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 92 (docs/lessons.md lines tagged 1.*, plan.1.*; 84 before this commit + 8). Lessons promoted to CLAUDE.md/AGENTS.md: 5 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2) + 1 pending (via 1.verify.r3). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 10 (the nine of r1 plus 1.25 at a1); other second attempts: 1.23 (capped, journal present) and 1.verify.r2.g1 (stopped by choice), neither a stop signal. Gate reruns: 0. Gate repairs: 2 (1.verify.g1, 1.verify.r2.g1). Coverage at r2 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r1: 0, 0, 0, 0 (1.26–1.28 and the verifiers touched no `packages/*/src`; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.2 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 20.6 / 22.7 / 11.9 ms on the ubuntu / windows / macos runners of the accepted gate. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.28: 14 of 26 (10 of 20 before the r2 chain, 4 of 6 in it). By the transcript measure the two capped first attempts this chain ended at 50 and 56 tool uses and the five that finished first at 28–59 — the r1 reading (the cap sits on the median of the work) stands; only 1.28 and 1.verify.r2.g1 ran under the journal-stub rule, with no cap and no NO-JOURNAL between them.

Phase 1 review r3: findings as reconciled — 2 blockers (in tasks 1.34 and 1.36), 3 should-fix required (in tasks 1.35, 1.37 and 1.38), 1 should-fix taken with its sibling in 1.35 (I4), 1 should-fix recorded (I7, the a2 rerun answered by #023, with a conditional boundary candidate and a flake-signature line), 3 nits (1 taken in task 1.34, 1 taken in the reconciliation — the address redaction, 1 decided — Unicode whitespace stays ASCII); r2 dispositions reopened: 2 of 9 as families or members (H1 → I1, H8 → I3 and I1), 7 confirmed; the `[1.30]` backlog deferral of the break-inside-a-mark member reopened (its reachability premise false, as H8's was at r2). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 116 (docs/lessons.md lines tagged 1.*, plan.1.*; 106 before this commit + 10). Lessons promoted to CLAUDE.md/AGENTS.md: 6 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3) + 2 pending (via 1.verify.r4). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 11 (the ten of r2 plus 1.29 at a2 — procedure, not budget); capped first attempts this chain: 1.29 and 1.30 (both continued under the stub rule, no stop signal). Gate reruns: 1 (1.verify.r3.g1h a1 → a2, transient, #023). Gate repairs: 3 (1.verify.g1, 1.verify.r2.g1, 1.verify.r3.g1). Coverage at r3 (three CI legs and both cold builds agree): statements 99.64 %, branches 97.73 %, functions 100 %, lines 100 %; delta vs r2: +0.01, +0.08, 0, 0 (1.29–1.31 added 153 tests; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 11.5 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 26.4 / 20.7 / 14.3 ms on the ubuntu / windows / macos runners of the accepted gate; source commit p95 56.1 / 59.9 ms isolated (G9, evidence only). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.33: 18 of 31 (14 of 26 before the r3 chain, 4 of 5 in it: 1.31, 1.32, 1.33 and the verifiers at a1; 1.29 and 1.30 capped). By the transcript measure the two capped first attempts this chain ended at 50 tool uses each and the three that finished first at 48 (1.31, one turn under the cap), 38 and 22; the whole chain ran under the journal-stub rule, which turned both caps into continued attempts rather than NO-JOURNAL stops. Turn-budget decision unchanged: 80 conditional on Michael's OK at the Phase 2 boundary.

Phase 1 review r4: findings as reconciled — 1 blocker (in tasks 1.40 and 1.41, the serializer half of the flanking family), 1 should-fix required (in task 1.43, the cursor map — the `[1.37]` backlog line's trigger fired), 2 should-fix taken (1.42 the setext pair; 1.44 the chord guard's regex), 5 nits (1 taken in 1.44 — the allowlist reason, 2 taken in the reconciliation — the vertical-arrow line's third site and the timestamp-less stub recorded, 1 backlog — the heading entity, 1 evidence — the turn budget); r3 dispositions reopened: 1 of 10 as a family (I1 → J1, on the serializer's side), 9 confirmed. Verdict FAIL. Reviewer record: two independent reports and one derived (Grok, DECISIONS #025 — a single-reviewer retry after a 402 read its siblings' finished reports; unique findings 0 by construction, verdict outside the count). Lessons written by Phase 1 agents and the principal: 131 (docs/lessons.md lines tagged 1.*, plan.1.*; 124 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 8 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3, 2 via 1.verify.r4); 2 more carried by 1.verify.r5 (the two-halves ownership of a fixed-point family; a kind table derived from the rule's branches), 1 recorded. STUCK signals this attempt: 1 (1.10.r4, Grok's 402). Turn budget: four of the five r4 opus fix tasks capped at a1 (six of six schema.ts opus fix tasks across r2–r4); 19 of 46 Phase 1 loop tasks needed more than one attempt; the raise stays conditional on Michael's OK.

Phase 1 review r5: findings as reconciled — 1 blocker (in tasks 1.45 and 1.47, the astral neighbour of a flanking mark — the flanking family's non-BMP member, the encode decision right and its width wrong), 1 should-fix required by all three reviewers (in task 1.46, the position map's encoded neighbour — 1.40's pin, the `[1.40, found outside scope]` line's trigger fired), 1 should-fix discharged by the reconciliation's cherry-pick (#026–#028 into the tree), 1 should-fix taken (in task 1.47, the decoded-text assertion), 4 nits (3 taken in 1.48, 1 recorded); r4 dispositions: J1 reopened for astral neighbours, J2–J9 confirmed; reviewer record: three independent reports, unique findings Claude 5 of 8, Sol 0 of 2, Grok 1 of 3, three shared findings all agreed.

Phase 1 review r6: findings as reconciled — 2 blockers (in task 1.49, the astral character between two flanking runs — the two-run join of the flanking family; in task 1.50, the autolink literal's backslash — invariant A on the parser's own tree), 2 should-fix required (in task 1.51: the position map's html-before-soft-break rewrite and the `inlineCode` spelling table), 4 should-fix taken (1.52 the cell's end with the corpus inverse leg; 1.53 the boundary side by `$pos.marks()`; 1.54 the counted-press helpers and the corpus callers' text instrument, with 1.47's re-read clause recorded), 1 should-fix deferred with a trigger (URL-shaped text, hard stop `2.verify`), 2 nits (1 backlog — adjacent attention runs, 1 recorded — 1.47's spec count); r5 dispositions: K1 reopened for the two-run join, K2 reopened for the third branch, K3–K9 confirmed; J3 reopened for two node classes; reviewer record: three independent reports, unique findings Claude 8 of 10, Sol 0 of 3, Grok 0 of 1, three shared findings agreed by class, two blocker families (one shared by Sol and Grok, one unique to Claude). Verdict FAIL. Gate reruns this attempt: 1 (1.verify.r6h a1 → a2, #029, the second instance of #023). STUCK stops in the r6 chain: 1 (1.47); NO-JOURNAL: 2 (1.45 on a 429, 1.47 a1 without a stub).

Phase 1 review r7: findings as reconciled — 2 blockers (in task 1.57, the join widening rewriting a code span's and inline html's literal bytes — the ownership of a serializer post-processor; in task 1.58, a hard break before inline html written as backslash-space — a rewrite named for the map and never read for its bytes), 2 should-fix required (task 1.54 integrated from a capped attempt — the record, the rule and the boundary `fix(runner)`, its paste leg in task 1.61; in task 1.59 the `inlineCode` spelling table against the table extension's pipe escape), 2 should-fix taken (1.60 the block-final code-span end by the typed marks with the stored-marks override; 1.61 the destructive legs' timeout), 4 nits (1 taken in 1.59 — the false `peek` comment, 1 taken in the reconciliation — the #030 product backlog line, 2 backlog — `link.peek`, a GFM-literal member of L11), 1 found by the reconciliation and deferred with a trigger (a hard break inside a container); r6 dispositions: L1 fixed and its fix M1's cause, L3 fixed with M2 as its branch's break member, L4 reopened for the configured handler, L5's block-final rule reopened as M5, L2, L6, L7 fixed, L8 half met, L9–L11 as recorded; K1 and K2 reopened, K3–K9 confirmed; J3 reopened a third time; reviewer record: three independent reports, unique findings Claude 8 of 9, Sol 1 of 2, Grok 0 of 0, one shared finding agreed, Grok PASS with two blocker families in the tree (3 of 5 counted attempts). Verdict FAIL. Gate rounds this attempt: 3 (1.verify.r7h GATE-FAILED #035, 1.verify.r7.g1h GATE-FAILED #036, 1.verify.r7.g2h ACCEPT a1), no rerun. STUCK stops in the r7 chain: 3 (1.51, 1.53, 1.54); NO-JOURNAL: 2 (1.53 on a 429, 1.54 a2 without a stub); principal decisions in the chain: 5 (#030–#034); integrations from a capped attempt: 1 (1.54).

Phase 1 review r8: findings as reconciled — 0 blockers, 2 should-fix required (in task 1.60's block-edge branch, the typed-marks rule stopped at `inlineCode` while every other mark and both block edges answered the innermost text's spelling table — N1, Sol, with the link members the reconciliation found; in task 1.7's `walkInline`, the correspondences built from the normalised tree's widths while the conversion drops characters the live document keeps — N2, Sol), 1 should-fix taken (N3, Claude: the walk's give-up path in 1.57, unreachable and unguarded — 1.65), 1 should-fix as a criterion paragraph and an ask to Michael (N4, Claude: the `1.9` record at ca1cd3d, stale for two §7 clauses now evidenced by machine), 3 nits (N5 React 19 — backlog, Michael's OK; N6 M3 (a) restated as satisfiable — taken here; N7 `ci.yml`'s merge step — backlog), 1 found by the reconciliation (N8: the `[1.53]` class's after-side member on eight punctuation-edged block-final runs — a member sentence, no new line). Three fix tasks 1.63–1.65, no fixture; the r9 chain and 1.verify.r9's promote step (a)–(c) appended.

Phase 1 review r9: findings as reconciled — 1 blocker (in task 1.67, two adjacent different-mark runs sharing one delimiter run — the `[review-1-r6 L11]` premise false for different marks), 1 should-fix taken (in task 1.68, the fifth caret-precondition race), 5 should-fix backlogged under Michael's stop rule (O2, O4, O7–O9 — Sol's three Required among them: the typed character's formatting differs between views, both views round-trip), 2 nits backlogged (O5–O6); every backlog line carries a `2.verify` trigger (DECISIONS #review-1-r9, #041 D1).

Phase 1 review r10: findings as reconciled — none (0 blockers, 0 should-fix, 0 nits from three independent reviewers under D1’s blockers-only scope); verdict PASS (DECISIONS #review-1-r10); reviewer risks read against D1’s blocker test and filed as backlog lines (leg (b)’s 0 members, the 1.9.r1 editing feedback beside O7–O9, the [1.46] sixth instance, the pnpm dev text for 2.9); no promotion into 2.verify.
