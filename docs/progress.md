# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (#review-1-r0) → fix chain 1.13–1.22 + 1.verify.r1 → gate 1.verify.r1h ACCEPT → review r1 FAIL (#review-1-r1) → fix chain 1.23–1.27 + 1.verify.r2 → gate 1.verify.r2h GATE-FAILED (#021) → 1.28 + 1.verify.r2.g1 → gate 1.verify.r2.g1h ACCEPT → review r2 FAIL (#review-1-r2) → fix chain 1.29–1.32 + 1.verify.r3 → gate 1.verify.r3h GATE-FAILED (#022) → 1.33 + 1.verify.r3.g1 → gate 1.verify.r3.g1h ACCEPT a2 (#023) → review r3 FAIL (#review-1-r3) → fix chain 1.34–1.38 + 1.verify.r4 → gate 1.verify.r4h GATE-FAILED (#024) → 1.39 + 1.verify.r4.g1 → gate 1.verify.r4.g1h ACCEPT a1 → review r4 FAIL (#review-1-r4; Grok derived, #025) → fix chain 1.40–1.44 (four of five to STUCK with the work done, each recovered by a `retry` with a lesson; #026–#028) + 1.verify.r5 → gate 1.verify.r5h ACCEPT a1 → review r5 (three independent reviewers, Grok on the usage reset, #027) → this reconciliation.
Review attempt r5 at 3dd9c4b: Claude FAIL (1/3/4), Sol FAIL (1/1/0), Grok FAIL (0/1/2) → reconciliation verdict FAIL (DECISIONS #review-1-r5): 1 blocker (an astral, non-BMP character directly outside a flanking mark whose inner edge is punctuation or whitespace is written as a lone-surrogate character reference and reparses to U+FFFD — `container-phrasing.js` encodes one UTF-16 unit of the pair; the encode decision itself is right in the parser's code-unit terms; Claude's typed emoji, Sol's Backspace route and two supplementary letters, reproduced headlessly for 3 marks × 3 characters × both sides), 1 should-fix required by all three (`placeChildren` leaves the encoded neighbour unresolved, so the toggle types inside the marked run — the `[1.40, found outside scope]` line's trigger fired; reproduced headlessly), 1 should-fix discharged by the cherry-pick of #026–#028, 1 should-fix taken (the decoded-text assertion 1.41's legs lack), 4 nits (3 taken, 1 recorded); r4 dispositions: J1 reopened for astral neighbours, 8 confirmed.
Next: this commit integrates as task(1.10.r5d) → 1.45 (the `containerPhrasing` wrapper and app `root` handler in packages/core/src/format.ts, astral guards over three marks, one typed Playwright case) → 1.46 (`placeChildren`'s code-point retry, the surrogate-pair spelling rule, 1.40's pin deleted, the deletion-leg clause, a toggle-then-type Playwright case) → 1.47 (the `astral-neighbour.md` fixture, the legs' code-point rule, the text-equality assertion) → 1.48 (the whitespace × whitespace cell, the depth-3 `KINDS` entry, the chord guard's quote forms and title) → 1.verify.r6 → HUMAN_GATE 1.verify.r6h (`scripts/gate.sh 1.verify.r6h`; the principal rotates there) → 1.10.r6a/b/c → PRINCIPAL 1.10.r6d (a fresh session) → 1.close on PASS.
Open human gates: none (next: 1.verify.r6h after 1.48 and 1.verify.r6). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r6 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN, stacked on the newest unmerged one (#review-0-r1 deviation 29, handoff 024); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns with the journal stubbed first by `printf` and the suite run once (the 80-turn raise conditional on Michael's OK at the Phase 2 boundary); after a `gate.sh rerun` that accepts, never run the `--resume` doctor names — #023's abandon once per rerun; before any `retry`, every attempt log moved aside with a suffix (#027); before any single-reviewer retry, #025's procedure; a Grok 402 is Michael's call; a 429 in a transcript waits for the reset Michael names; a mid-chain task-text conflict is decided by naming the sentence that yields, never by widening scope through a lesson line (#026, #028); the boundary fix(runner) items (#023, #025, #027, H7) land after 1.close with the runner idle on phase/2.
Reviewer record (#review-1-r5): three independent reports; unique findings Claude 5 of 8, Sol 0 of 2, Grok 1 of 3; three shared findings all agreed by class and severity (the blocker Claude–Sol; the position map all three at Required; the whitespace cell Claude–Grok at nit); cumulative over twenty-two shared findings, eight disagreements, the low side is Grok 6, Claude 2, Sol 2 — unchanged, still a signal, not a verdict; Grok's PASS-with-blockers count stays 2 of 3 counted attempts; no change inside a review set; the r6 rows are written for three reviewers, a 402 during r6 is Michael's call.

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [1.43] 2026-09-12T15:43:16Z Task: Phase 1 review fix (DECISIONS #review-1-r4 J3 — Claude finding 3 should-fix, Sol finding 2 should-fix Required; `cursorMap.toSource` in packages/editor/src/toggle.ts answers a position that a block's correspondence covers but no inline child does with the block's start instead of `afterLastNode`) (attempt 3). Status: Blocked (task-text conflict, unchanged from attempts 1 and 2; re-confirmed cheaply from the failing clause, DONE withheld). `git status` clean at start, no recovery commit. **Inputs unchanged since attempt 2, checked first:** no commit on this branch after attempt 2's `9b5a3cb` other than this attempt's own stub; `/logs/state/tasks.json` for 1.43 is `attempts: 2, notes: "no DONE promise"`, no principal note; `docs/lessons.md` has no `[1.43]` principal line after attempt 2's; `docs/DECISIONS.md` still has no `#026` header (`grep -n '^## #0[2-9]'` lists #020–#025); `ralph/tasks.json` still holds the narrow scope; no `/logs/human/1.43/`. **Conflict, one sentence (attempt 1's, unchanged):** the acceptance's "`pnpm test`'s summary shows no expected fail", the scope's "No other source change" / `git diff --stat ... touches only the scope files` clause, and CLAUDE.md's "scope is the task text" cannot all hold, because the one remaining `it.fails` is task 1.40's pin at `packages/core/test/positions-corpus.test.ts:114` (a passed task's record of the open `docs/V1.1-BACKLOG.md:76` defect, hard stop `1.close`), outside this task's scope. Resolutions (unchanged): (i) read the clause as the description's own "the `it.fails` case removed — the suite then reports no expected failure" — no expected fail *from the pin this task removes*, which the file-level grep in the same acceptance checks and which is 0 — and print DONE on the branch as it stands; (ii) a planning commit widens 1.43 or writes the fix task the backlog line names. Not decided here (CLAUDE.md: a task conflicting with the rules is written to the journal, not decided). **Re-confirmed this attempt (CLAUDE.md step 3, nothing copied forward):** `grep -c 'it.fails' packages/editor/test/position-map-editor-leg.test.ts` = 0; `grep -rn 'it\.fails' packages/*/test e2e` finds only `positions-corpus.test.ts` (doc comment and line 114); `grep -cE 'ControlOrMeta\+(Home|End)|"(Home|End)"'` on `e2e/web/editor-toggle-trailing-break.spec.ts` = 0. `git diff --stat 0412eac HEAD` outside the journal and lessons touches exactly `e2e/web/editor-toggle-trailing-break.spec.ts` (+140), `packages/editor/src/toggle.ts` (+84/−13 lines changed), `packages/editor/test/position-map-editor-leg.test.ts` (+88 lines changed); `pnpm-lock.yaml`, `Cargo.lock`, every `package.json` untouched, zero new dependencies. `pnpm lint` exit 0. `pnpm test` exit 0: **38 files, 3055 passed, 1 expected fail (3056)** — carried by `positions-corpus.test.ts` (412 tests); `toggle.test.ts` (132) and `position-map-editor-leg.test.ts` (179) green, so the cursor, toggle and 200-seeded-position suites are green unchanged. `cargo test` 0/0/0 (Phase 2 surface, vacuous). e2e/web not re-run: `git diff --stat 9b5a3cb HEAD -- e2e packages` is empty (byte-identical to the state attempt 1 ran at 86/86) and the runner re-runs the suite on the candidate at integration. The able-to-fail mutation is attempt 1's record (14 fixtures, shapes (a) and (b) listed there), not re-derived (lesson [1.40] 12:36:44Z). Files: `docs/progress/journal-main.md` (this line only). Tests: as above. Iterations used: 3. First attempt passed: n (conflict, all three attempts). Tool calls: 6. Notes: nothing to implement and nothing new to learn (no lesson appended); this is the third attempt on unchanged inputs, so the runner's cap now routes the task to the principal — the decision needed is the reading of the "no expected fail" clause: under (i) the next attempt stubs its own journal line, runs `pnpm lint && pnpm test && cargo test` and the e2e/web suite (install chromium first), completes the line and prints DONE; under (ii) it is a different task (the `placeChildren` core fix, `packages/core/src/positions.ts` and `positions-corpus.test.ts`).
- [1.43] 2026-09-12T15:46:39Z Task: Phase 1 review fix (DECISIONS #review-1-r4 J3 — Claude finding 3 should-fix, Sol finding 2 should-fix Required; `cursorMap.toSource` in packages/editor/src/toggle.ts answers a position that a block's correspondence covers but no inline child does with the block's start instead of `afterLastNode`) (attempt 4, the retry after DECISIONS #028) Status: Complete. `git status` clean at start, no recovery commit. **The retry after DECISIONS #028** (`docs/DECISIONS.md` on `handoff/023`, entry `#028-correction-of-026-decision-2`, verified to exist there by header — not taken from the lessons line alone, per lesson [1.43] 15:42:30Z): the conflict the three earlier attempts recorded is decided the way attempt 1's resolution (i) put it — the acceptance clause "`pnpm test`'s summary shows no expected fail" reads, for this task, as "no expected fail from the pin this task removes" (`grep -c 'it.fails' packages/editor/test/position-map-editor-leg.test.ts` = 0), and the one remaining expected fail in the suite is 1.40's pin at `packages/core/test/positions-corpus.test.ts:114` (the `[1.40, found outside scope]` `formatWithMap` backlog defect), which STAYS per DECISIONS #028 and is a task for the next planning-capable commit; #026's lesson-line widening of this task's scope to packages/core is withdrawn. This attempt changed no source and no test, as #028 instructs; per the standing lessons ([1.41] 14:55:00Z, 14:52:39Z) it still ran the whole suite itself in this container rather than copying earlier counts forward. **The three deliverables, by file (all from attempt 1, unchanged since):** (1) `packages/editor/src/toggle.ts` — `toSource`'s no-spelling branch for an entered block answers the end of the block's last descendant ending at or before `pos` (`lastNodeEnd`, bounded to the block's own descendants; `afterLastNode` a wrapper over it), a block with no preceding descendant falls through to its start, leaf blocks and inline atoms keep their own start, the doc comment stating the three clauses; (2) `packages/editor/test/position-map-editor-leg.test.ts` — the width branch and the split collector deleted, both shapes folded into clause 3 of the deletion leg as ordinary assertions, the `it.fails` case removed, the reach counts `blocksEndingPastCorrespondence > 0` and `splitParagraphsPastCorrespondence > 0` kept; (3) `e2e/web/editor-toggle-trailing-break.spec.ts` — the case "Sol's reproduction: `abc\\` + LF + `def`, the caret after `def`, Backspace ×3, Cmd/Ctrl+/ then `X` — the source caret is after `abc`, the pane and the copy are `abcX`", the caret placed by a click and asserted `{ text: "def", offset: 3 }` before any key, pane and `writeText` spy `abcX\n` with `toBe`; `grep -cE 'ControlOrMeta\+(Home|End)|"(Home|End)"'` on the spec = 0. **Able to fail** (attempt 1's record, not re-run this attempt per #028): `:351`'s old answer restored under a test-local shim (`if (false && isEntered(inside.node))`) turned the deletion leg red on 14 fixtures — shape (a), the dropped break: `hard-break.md`, `hard-break-in-emphasis.md`, `soft-line-breaks.md`, `strikethrough-punctuation.md`, `table-escaped-pipes.md`; shape (b), the split item's empty paragraph: `list-unordered.md`, `list-ordered.md`, `list-mixed-ordered-unordered.md`, `ordered-list-start-number.md`, `nested-lists.md`, `task-list-as-text.md`, `tabs.md`, `blockquote-nested.md`, `essay-fixture.md` — and the Playwright case red (`Xabc\n`); restored in the same tool call, never committed. **Verification this attempt.** `git diff --stat` against the branch base (`0412eac`, task 1.42) excluding the journal and lessons touches exactly `e2e/web/editor-toggle-trailing-break.spec.ts` (+140), `packages/editor/src/toggle.ts` (+84/−13 net), `packages/editor/test/position-map-editor-leg.test.ts` (+88 lines changed); `pnpm-lock.yaml`, `Cargo.lock` and every `package.json` diff empty, zero new dependencies. `pnpm lint` exit 0 (eslint, check-deps, typecheck ×6). `pnpm test` 38 files passed, 3055 passed, 1 expected fail (1.40's pin, per #028), 0 failed; `toggle.test.ts` 132 (cursorMap over the corpus 59), `position-map-editor-leg.test.ts` 179 (the seeded leg 119, the deletion leg 60), the cursor, toggle and seeded-position suites green unchanged. `cargo test` 0/0/0 (Rust surface is Phase 2, vacuous). e2e/web (chromium installed fresh in this container) 86/86 passed. Files: `docs/progress/journal-main.md` (this line only). Tests: as above, all green, all run by me this attempt. Iterations used: 4 (three earlier attempts withheld DONE on the conflict; this is the fourth journal line, `grep -cF -- '- [1.43] '` = 4). First attempt passed: n (attempt 1 completed the work and withheld DONE on the task-text conflict, correctly per #028). Tool calls: 12. Notes: nothing outstanding for 1.43; the `formatWithMap` fix behind 1.40's pin is the next planning-capable commit's task (DECISIONS #028), not this one's.
- [1.44] 2026-09-12T15:51:01Z Task: Phase 1 review fix, test-only (DECISIONS #review-1-r4 J4 -- Claude finding 4 should-fix; J5 -- Claude finding 5 nit; tests/no-caret-chords-in-e2e.test.ts). Status: Complete. `git status` clean at start, no recovery commit. (J4) `CHORD` is now `/"(?:[A-Za-z]+\+)+(Home|End)"/g` — any chain of modifiers ending in `Home` or `End`, in the quoted key-name form `keyboard.press` takes — replacing `/\b(ControlOrMeta|Control|Meta)\+(Home|End)\b/g`, which missed `Shift+End`, `Alt+Home` and `ControlOrMeta+Shift+End`. A new describe block, "CHORD matches every modifier chain ending in Home or End, not only ControlOrMeta/Control/Meta", feeds the regex the four matching spellings (`"Shift+End"`, `"Alt+Home"`, `"ControlOrMeta+Shift+End"`, `"ControlOrMeta+Home"`, each asserted count 1) and two non-matching ones (`"Home"`, `"ArrowLeft"`, each asserted count 0). The guard's ability to fail was re-recorded, not assumed: a scratch spec `e2e/web/zzscratch-chord.spec.ts` with one `page.keyboard.press("Shift+End")` line turned "zzscratch-chord.spec.ts has no ControlOrMeta, Control or Meta chord with Home or End" red (`expected 1 to be +0`) under the new regex; deleted in the same tool call, confirmed absent by `git status --short` (only `tests/no-caret-chords-in-e2e.test.ts` modified), never committed. (J5) Both `ALLOWED_BARE_PRESSES` reason strings and the file's doc comment reworded from the false "CodeMirror's own keymap … handled in JS before the OS layer" to the true mechanism: Blink's native line motion in CodeMirror's contenteditable, no key-binding extension installed (`pnpm why @codemirror/commands` is empty, confirmed this attempt; packages/editor/src/source.ts installs none), proven by the three-OS gate (green on macos-latest at 1.verify.r4.g1h a1). `grep -c 'keymap' tests/no-caret-chords-in-e2e.test.ts` = 0 (the word "keymap" itself is gone, not just the false claim — the first reword used "no keymap is installed" and still matched the grep, corrected to "no key-binding extension is installed"). Allowlist counts unchanged and read from the tree, not assumed: `editor-cursor.spec.ts` 1, `editor-toggle.spec.ts` 2 (`grep -c '"\(Home\|End\)"'` on each). No defect found outside scope; no `docs/V1.1-BACKLOG.md` line added. Files: `tests/no-caret-chords-in-e2e.test.ts`, `docs/lessons.md` (+1), `docs/progress/journal-main.md` (this line). `git diff --stat` against the branch base (`ece548c`, task 1.43) excluding the journal and lessons touches exactly `tests/no-caret-chords-in-e2e.test.ts` (+31/−7); `pnpm-lock.yaml`, `Cargo.lock`, every `package.json` diff empty, zero new dependencies. Tests: `pnpm lint` exit 0 (eslint, check-deps: 61 npm + 4 cargo direct deps all allowlisted, `pnpm -r typecheck`); `pnpm test` 38 files, 3061 passed, 1 expected fail (1.40's `positions-corpus.test.ts` pin, per DECISIONS #028, unchanged by this task); `cargo test` 0/0/0 (Phase 2 surface, vacuous); e2e/web 86/86 (playwright chromium installed fresh in this container). Iterations used: 1. First attempt passed: y. Tool calls: 15. Notes: nothing outstanding for 1.44; the next task is whatever `docs/PRD.md` §8 names after this in the r4 chain.
- [1.verify.r5] 2026-09-12T15:56:52Z Task: Phase 1 verification for review attempt r5 (RUNNER-SPEC §3, §5.5): freeze dependencies, run the full suite in the container, confirm the phase head is ready for the three-OS CI run, and carry the promote-lessons step for the r5 chain — three candidates weighed at DECISIONS #review-1-r4, two promoted and one recorded. Status: Complete. `git status` clean at start (base: task 1.44, `d871506`), no recovery commit; stub committed first (`wip(1.verify.r5): journal stub`) per CLAUDE.md's list. **(a) Promoted**, one bullet appended verbatim as the second-to-last line of `## Code rules` in both `CLAUDE.md` and `AGENTS.md`, identical in both files: "A fix for a mark-edge or delimiter defect reads the serializer's handler for that mark before touching the editor's tree — both halves of every fixed-point family are owned, the tree the editor builds and the bytes the serializer writes for it — and every mark-edge guard is enumerated over both neighbour classes CommonMark §6.2 names (whitespace and punctuation) on every flanking mark (DECISIONS #review-1-r4 J1)." **Discharging tests**, by title: `packages/core/test/strikethrough-flanking.test.ts`'s `describe("strikethrough flanking (task 1.40, J1): the \`delete\` handler encodes the neighbour a \`~~\` run cannot form against")` — the closing-side and opening-side sweeps (`it(\`(closing, ${neighbour} outside, ${inner} inside): bytes, one delete, fixed point\`)` / the opening-side equivalent, over both neighbour classes CommonMark §6.2 names, whitespace and punctuation) and the two whitespace-inside-the-run cases; and `packages/editor/test/editor-fixed-point.test.ts`'s neighbour leg (task 1.41), `it(\`${name} is an editor fixed point after the whitespace beside every marked run is deleted (the mark's neighbour)\`)` and `it(\`${name} is an editor fixed point after a period is typed at the end of every marked run and the whitespace after it deleted (the mark's neighbour, the certain case)\`)`. **(b) Promoted**, the next bullet, verbatim: "An enumeration that mirrors a third-party rule derives its kind table from that rule's own branches, read in the installed package, never from the type list — a pair the table lacks is a pair the enumeration cannot fail on (DECISIONS #review-1-r4 J2)." **Discharging test**, by title: `packages/editor/test/list-spread.test.ts`'s two setext `KINDS` entries (`"setext heading, depth 1, a line ending in its text"`, `"setext heading, depth 2, a break"`, task 1.42) exercised by every case of `describe("the two parse(format(·)) confirmations the derivation is built on (task 1.36)")`'s pairwise sweep, `it(\`confirmation 1, ${leftName} then ${rightName}: the item is spread exactly when the tight bytes do not reparse to the tight pair, and the bytes are the tree\`)` (both setext names as `leftName` and as `rightName`, against every other kind), plus the four named `it("clause (setext heading), …")` cases. **(c) Recorded, not promoted**: Sol's r4 lesson — a test-only task's scope is not a deferral reason for a product defect on the phase's user path — addresses the reconciliation, not the coding agent (the agent's own rule, a backlog line with a trigger and a hard stop, already stands unchanged); it lands in `docs/PRINCIPAL.md` at the boundary, which this zero-dependency verify task (scoped to `CLAUDE.md`, `AGENTS.md`, `docs/lessons.md` and the journal) does not edit, so it is recorded here rather than promoted into the agent files. `bash ralph/check-agent-rules.sh` exits 0 (`check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`) and `tests/agent-rules.test.ts` passes. No other procedure change this attempt. **Verification, this container (Linux; no other OS claimed).** `pnpm install --frozen-lockfile` (fresh worktree, 760-entry lockfile, supply-chain policy check passed) added `node_modules` only; `git status --short pnpm-lock.yaml` and `git diff --stat pnpm-lock.yaml` both empty after. `pnpm lint` clean: eslint, `check-deps` (61 npm + 4 cargo direct dependencies, all allowlisted), `pnpm -r typecheck` across all 6 typechecked projects. `pnpm test` 38 files, 3061 passed, 1 expected fail (1.40's `positions-corpus.test.ts` pin, per DECISIONS #028, unchanged by this task), `tests/agent-rules.test.ts` and `tests/no-caret-chords-in-e2e.test.ts` (50 tests) both green, coverage 99.56%/97.66%/100%/100%. `cargo test` (workspace: `desktop_lib`, `desktop`, doc-tests) 0/0/0 (vacuous, Rust surface is Phase 2; `git diff --stat Cargo.lock` empty). `pnpm --filter @essaydown/e2e-web exec playwright install chromium` then the full `e2e/web` suite 86/86 passed, every one of the 23 spec files under `e2e/web/` including the 1.17 latency spec. `git diff --stat` against the branch base (1.44, `d871506`) is exactly `CLAUDE.md` (+2), `AGENTS.md` (+2), `docs/progress/journal-main.md` (this entry) — no `package.json` (×9), `pnpm-lock.yaml`, `pnpm-workspace.yaml`, any `Cargo.toml` or `Cargo.lock` change, zero new dependencies (PRD §4). Secrets scan over the whole working tree (key-shaped patterns, `node_modules`/`target`/`test-results`/`.git` excluded): 0 new hits, nothing touched by this branch. No defect found outside scope; no `docs/V1.1-BACKLOG.md` line. Files: `CLAUDE.md`, `AGENTS.md`, `docs/progress/journal-main.md`. Tests: as above, all green, all run by me this attempt. Iterations used: 1. First attempt passed: y. Tool calls: 24. Notes: nothing outstanding from 1.verify.r5; next is the `1.verify.r5h` HUMAN_GATE (the three-OS `ci.yml` run at this SHA, artifacts `test-logs` and `playwright-report`), then `1.10.r5a/b/c` and `1.10.r5d`.
- [1.10.r5d] 2026-09-12T17:52:51Z Task: reconciliation of Phase 1 review attempt r5 at 3dd9c4b (verifier 1.verify.r5, gate 1.verify.r5h ACCEPT a1, run 34705334786; three independent reviewers, started together — Grok on the usage reset, DECISIONS #027). Claude FAIL (1 blocker/3 should-fix/4 nits), Sol FAIL (1/1/0), Grok FAIL (0/1/2). Verdict FAIL: an astral (non-BMP) character directly outside a flanking mark whose inner edge is punctuation or whitespace is written as a lone-surrogate character reference — `container-phrasing.js` encodes one UTF-16 unit of the pair — and reparses to U+FFFD, so Copy Markdown reports "Copied Markdown" with the character destroyed and the toggle carries it into the document, for `*`, `**` and `~~`, both sides, the parser's own tree included (Claude, a typed `*wow!*` then an emoji; Sol, the r4 Backspace route and two supplementary letters; reproduced headlessly here for 3 marks × 3 characters × both sides × two inner classes, 27 trees, and the encode decision confirmed right in the parser's code-unit terms — a raw astral neighbour never closes the run — so only the width of the encoding is wrong); `placeChildren` leaves the encoded neighbour unresolved so the toggle puts a typed character inside the marked run (all three reviewers, Required; the `[1.40, found outside scope]` line's trigger fired; reproduced headlessly: unresolved on both sides for three marks, and `cursorMap` answering the run's end for every position inside `gamma` of `Alpha ~~beta.~~&#x67;amma delta`); #026–#028 absent from the tree (discharged by the cherry-pick); the decoded-text assertion missing from 1.41's legs; three nits taken, two recorded. Status: Complete. Files: docs/reviews/phase-1-r5-{claude,sol,grok}.md (byte-identical copies, `cmp` clean), docs/DECISIONS.md (#review-1-r5: verdict FAIL first line, the cause read in the installed package and the fix shape decided from a prototype — `state.containerPhrasing` wrapped once per serialization so the two rewrites widen to the whole scalar, 96 of 96 prototype trees fixed points; 9 findings K1–K9 with severity and disposition; J1–J9 confirmed with J1 reopened for astral neighbours; the reviewer record — unique findings Claude 5 of 8, Sol 0 of 2, Grok 1 of 3, three shared findings all agreed, cumulative twenty-two shared / eight disagreements / low-rater Grok 6, Claude 2, Sol 2 —; the turn-budget counts from the transcripts; the promote candidates for 1.verify.r6; the cherry-pick record; graph change; reversal), docs/PRD.md (§8 Phase 1: +1.45 the containerPhrasing wrapper and app root handler in format.ts with astral guards over three marks and one typed Playwright case (opus); +1.46 placeChildren's code-point retry, the surrogate-pair spelling rule, the pin deleted, the deletion-leg clause and a toggle-then-type Playwright case (opus); +1.47 the astral-neighbour.md fixture, the legs' code-point rule and the text-equality assertion (sonnet, test-only); +1.48 the whitespace×whitespace cell, the depth-3 KINDS entry and the chord guard's quote forms and title (sonnet, test-only); +1.verify.r6 needsCI carrying two promoted rules and one recorded; +1.10.r6a/b/c/d; 1.close → 1.10.r6d), ralph/tasks.json + ralph/EXPECTED_COUNT (222 → 232, regenerated: 183 raw + 21 review-set + 28 CI gates), docs/V1.1-BACKLOG.md (+5 lines: the [1.40] line dispositioned into 1.46, K6's identifier half, the astral class beyond the fixture's positions, the turn-budget counts, the reviewer record), docs/lessons.md (+6), docs/progress.md (regenerated), docs/handoffs/023-*, 024-* and next-prompt.md with DECISIONS #026–#028 (cherry-picked from handoff/024, five commits, no conflict). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (232 tasks); bash ralph/test/run.sh run before the commit (result in the commit body); the reproductions and the 1.45 prototype ran in a detached scratch worktree at 3dd9c4b (throwaway vitest files and a prototype patch, removed with the worktree; its full unit suite ran three times: 14 reds with `html` wrongly listed as phrasing, then 5 reds all on the astral fixture — the deletion leg's encoded neighbour at a block end (K2) and the letter leg cutting a surrogate pair — which is why the fixture is 1.47's and the legs get a code-point rule), never in this worktree — no node_modules here. Commit: wip(1.10.r5d). Iterations used: 1. First attempt passed: y. Tool calls: ~45. Reviewer overturned: pending. Notes: the runner integrates this as task(1.10.r5d) and runs 1.45; the next human gate is scripts/gate.sh 1.verify.r6h (the principal rotates there); a 402 during the r6 set is Michael's call (#025, #027); the e2e/web directory holds 20 spec files at 3dd9c4b (K7).

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.23 | 2 | ~22 (a1) + ~12 (a2) | n (a1 `error_max_turns` at 51, exit 1, journal present — no stop signal) | n (its three copy cases read the OS clipboard: the windows CRLF gate failure, #021, repaired by 1.28) |
| 1.24 | 1 | ~30 | y | y (H2: the pending segment keeps one keystroke time, so a flush splits a burst longer than the window) |
| 1.25 | 2 | n/a (the fields are absent from its entries) | n (NO-JOURNAL at a1, `error_max_turns` at 51) | y (H1/H5: the atom itself at a block end; H6: a cell's line ending) |
| 1.26 | 1 | ~24 | y | n |
| 1.27 | 1 | ~20 | y | n (H9: one tautology, nit, taken in 1.32) |
| 1.verify.r2 | 1 | ~14 | y | partial (H7: the wrapper `ralph/PROMPT.md` not carried along — a runner file, fixed at the boundary) |
| 1.28 | 1 | ~18 | y | partial (H4: the spy removed the suite's last real clipboard write — #021's scope decision) |
| 1.verify.r2.g1 | 2 | ~12 (a1) + ~20 (a2) | n (a1 exit 0 at `num_turns` 11, stopped by choice on H7; no stop signal) | n |
| 1.10.r2a (Claude) / r2b (Sol) / r2c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.29 | 3 | ~50 (a1) + ~16 (a2) + ~10 (a3) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal; a2 `success` at 21 with DONE and NO-JOURNAL — the stub completed in place, `chore(1.29)`) | partial (I1: the atom at a *mark's* edge, deferred by 1.30's backlog line; the deletion leg's ranges stop at the block's end) |
| 1.30 | 2 | ~50 (a1) + ~20 (a2) | n (a1 `error_max_turns` at 51, journal present — no stop signal) | y (I3: the mark-edge clause splits a link-marked run, a regression; I1's deferral written by this task on a false reachability claim) |
| 1.31 | 1 | ~34 (49 turns, 48 tool uses — one under the cap) | y | n |
| 1.32 | 1 | ~30 | y | n (I8: the fourth tautology, in a file outside its scope, filed on the backlog as asked) |
| 1.verify.r3 | 1 | ~20 | y | partial (I5: the promoted rule discharged at the neighbouring family only; the position map stayed insertion-only) |
| 1.33 | 1 | ~21 | y | n (gate 1.verify.r3h GATE-FAILED on 1.30's chord, #022, repaired here; the four sibling chords are I6 by #022's own trigger) |
| 1.verify.r3.g1 | 1 | ~20 | y | n (gate 1.verify.r3.g1h ACCEPT at a2 after a transient rerun, #023; I7 recorded) |
| 1.10.r3a (Claude) / r3b (Sol) / r3c (Grok) | 1 / 1 / 1 | 85 turns / — / 19 turns | y / y / y | — |
| 1.34 | 2 | ~50 (a1) + ~25 (a2) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal) | partial (J1: the atom clause holds; the family's next member is the serializer's, `format.ts`) |
| 1.35 | 2 | ~50 (a1) + ~19 (a2) | n (a1 `error_max_turns` at 51, stub present) | n (J1's link member runs through its `outermostMark` rule as designed; the serializer is at fault) |
| 1.36 | 2 | ~50 (a1) + ~24 (a2) | n (a1 `error_max_turns` at 51, stub present) | partial (J2: the paragraph → setext-heading pair missing from `needsBlankLine`; the kind table lacked it) |
| 1.37 | 2 | ~50 (a1) + ~35 (a2) | n (a1 `error_max_turns` at 51, stub present — its stub carries no timestamp, J8) | partial (J3: the defect it found and pinned as `it.fails`, deferred on scope; taken in 1.43) |
| 1.38 | 1 | ~34 | y | partial (J4/J5: the guard's regex misses Shift/Alt chains and its allowlist reason is false; gate 1.verify.r4h GATE-FAILED on its `ArrowUp` route, #024, repaired by 1.39) |
| 1.verify.r4 | 1 | ~39 | y | n |
| 1.39 | 1 | ~17 | y | n (J6: the `[plan.1.verify.r4h.r0]` line it left names two of three sites — a reconciliation correction) |
| 1.verify.r4.g1 | 1 | ~25 | y | n (gate 1.verify.r4.g1h ACCEPT at a1) |
| 1.10.r4a (Claude) / r4b (Sol) / r4c (Grok) | 1 / 1 / 2 container runs (a 402, then `retry` alone — #025) | 110 turns / — / 19 turns (13 on the 402 run) | y / y / n (STUCK 1.10.r4 on the 402) | — (Grok's report derived) |
| 1.40 | 3 → STUCK, retry 1 | ~50 (a1) + ~23 (a2) + ~13 (a3) + ~29 (retry) | n (a1 `error_max_turns` at 51 with the work done; a2/a3 stopped on the task-text conflict #026 decided) | partial (K1: the astral neighbour, a Phase 0 gap its port extended to `~~`; K2: the map defect it found and pinned, taken in 1.46) |
| 1.41 | 3 → STUCK, retry 3 → STUCK, retry 1 | ~51 ×4 + 16 (429) + 52 (no new journal line) + ~38 | n (every cap on re-verification of a finished branch; #027) | partial (K4: the decoded-text assertion its text names is absent) |
| 1.42 | 3 → STUCK, retry 1 | ~51 ×3 + ~18 | n (caps on re-verification) | n (K5's second half: the depth-3 clause has no `KINDS` feeder — 1.48) |
| 1.43 | 3 → STUCK, retry 1 | ~42 / ~13 / ~8 + ~15 | n (DONE withheld on the clause #028 re-read) | n |
| 1.44 | 1 | ~35 | y | n (K6/K8: the guard's quote forms and title — 1.48) |
| 1.verify.r5 | 1 | ~43 | y | n (K7: its journal's spec-file count) |
| 1.10.r5a (Claude) / r5b (Sol) / r5c (Grok) | 1 / 1 / 1 | 113 turns / — / 28 turns | y / y / y | — |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).

Phase 1 review r2: findings as reconciled — 2 blockers (in tasks 1.29 and 1.31), 3 should-fix required (in tasks 1.30 and 1.32), 1 should-fix taken as a runner chore at the phase boundary (H7, `fix(runner)` with a conformance test), 3 nits (2 taken in task 1.29, 1 in task 1.32); r1 dispositions reopened: 3 of 12 as siblings or families (G1's attestation → H4, G2 → H2, G4 → H1/H5/H6), 9 confirmed; the r1 backlog deferral of F1's emphasis-edge member reopened (H8, its reachability premise false). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 92 (docs/lessons.md lines tagged 1.*, plan.1.*; 84 before this commit + 8). Lessons promoted to CLAUDE.md/AGENTS.md: 5 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2) + 1 pending (via 1.verify.r3). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 10 (the nine of r1 plus 1.25 at a1); other second attempts: 1.23 (capped, journal present) and 1.verify.r2.g1 (stopped by choice), neither a stop signal. Gate reruns: 0. Gate repairs: 2 (1.verify.g1, 1.verify.r2.g1). Coverage at r2 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r1: 0, 0, 0, 0 (1.26–1.28 and the verifiers touched no `packages/*/src`; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.2 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 20.6 / 22.7 / 11.9 ms on the ubuntu / windows / macos runners of the accepted gate. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.28: 14 of 26 (10 of 20 before the r2 chain, 4 of 6 in it). By the transcript measure the two capped first attempts this chain ended at 50 and 56 tool uses and the five that finished first at 28–59 — the r1 reading (the cap sits on the median of the work) stands; only 1.28 and 1.verify.r2.g1 ran under the journal-stub rule, with no cap and no NO-JOURNAL between them.

Phase 1 review r3: findings as reconciled — 2 blockers (in tasks 1.34 and 1.36), 3 should-fix required (in tasks 1.35, 1.37 and 1.38), 1 should-fix taken with its sibling in 1.35 (I4), 1 should-fix recorded (I7, the a2 rerun answered by #023, with a conditional boundary candidate and a flake-signature line), 3 nits (1 taken in task 1.34, 1 taken in the reconciliation — the address redaction, 1 decided — Unicode whitespace stays ASCII); r2 dispositions reopened: 2 of 9 as families or members (H1 → I1, H8 → I3 and I1), 7 confirmed; the `[1.30]` backlog deferral of the break-inside-a-mark member reopened (its reachability premise false, as H8's was at r2). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 116 (docs/lessons.md lines tagged 1.*, plan.1.*; 106 before this commit + 10). Lessons promoted to CLAUDE.md/AGENTS.md: 6 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3) + 2 pending (via 1.verify.r4). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 11 (the ten of r2 plus 1.29 at a2 — procedure, not budget); capped first attempts this chain: 1.29 and 1.30 (both continued under the stub rule, no stop signal). Gate reruns: 1 (1.verify.r3.g1h a1 → a2, transient, #023). Gate repairs: 3 (1.verify.g1, 1.verify.r2.g1, 1.verify.r3.g1). Coverage at r3 (three CI legs and both cold builds agree): statements 99.64 %, branches 97.73 %, functions 100 %, lines 100 %; delta vs r2: +0.01, +0.08, 0, 0 (1.29–1.31 added 153 tests; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 11.5 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 26.4 / 20.7 / 14.3 ms on the ubuntu / windows / macos runners of the accepted gate; source commit p95 56.1 / 59.9 ms isolated (G9, evidence only). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.33: 18 of 31 (14 of 26 before the r3 chain, 4 of 5 in it: 1.31, 1.32, 1.33 and the verifiers at a1; 1.29 and 1.30 capped). By the transcript measure the two capped first attempts this chain ended at 50 tool uses each and the three that finished first at 48 (1.31, one turn under the cap), 38 and 22; the whole chain ran under the journal-stub rule, which turned both caps into continued attempts rather than NO-JOURNAL stops. Turn-budget decision unchanged: 80 conditional on Michael's OK at the Phase 2 boundary.

Phase 1 review r4: findings as reconciled — 1 blocker (in tasks 1.40 and 1.41, the serializer half of the flanking family), 1 should-fix required (in task 1.43, the cursor map — the `[1.37]` backlog line's trigger fired), 2 should-fix taken (1.42 the setext pair; 1.44 the chord guard's regex), 5 nits (1 taken in 1.44 — the allowlist reason, 2 taken in the reconciliation — the vertical-arrow line's third site and the timestamp-less stub recorded, 1 backlog — the heading entity, 1 evidence — the turn budget); r3 dispositions reopened: 1 of 10 as a family (I1 → J1, on the serializer's side), 9 confirmed. Verdict FAIL. Reviewer record: two independent reports and one derived (Grok, DECISIONS #025 — a single-reviewer retry after a 402 read its siblings' finished reports; unique findings 0 by construction, verdict outside the count). Lessons written by Phase 1 agents and the principal: 131 (docs/lessons.md lines tagged 1.*, plan.1.*; 124 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 8 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3, 2 via 1.verify.r4); 2 more carried by 1.verify.r5 (the two-halves ownership of a fixed-point family; a kind table derived from the rule's branches), 1 recorded. STUCK signals this attempt: 1 (1.10.r4, Grok's 402). Turn budget: four of the five r4 opus fix tasks capped at a1 (six of six schema.ts opus fix tasks across r2–r4); 19 of 46 Phase 1 loop tasks needed more than one attempt; the raise stays conditional on Michael's OK.

Phase 1 review r5: findings as reconciled — 1 blocker (in tasks 1.45 and 1.47, the astral neighbour of a flanking mark — the flanking family's non-BMP member, the encode decision right and its width wrong), 1 should-fix required by all three reviewers (in task 1.46, the position map's encoded neighbour — 1.40's pin, the `[1.40, found outside scope]` line's trigger fired), 1 should-fix discharged by the reconciliation's cherry-pick (#026–#028 into the tree), 1 should-fix taken (in task 1.47, the decoded-text assertion), 4 nits (3 taken in 1.48, 1 recorded); r4 dispositions: J1 reopened for astral neighbours, J2–J9 confirmed; reviewer record: three independent reports, unique findings Claude 5 of 8, Sol 0 of 2, Grok 1 of 3, three shared findings all agreed.
