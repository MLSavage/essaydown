# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (#review-1-r0) → fix chain 1.13–1.22 + 1.verify.r1 → gate 1.verify.r1h ACCEPT → review r1 FAIL (#review-1-r1) → fix chain 1.23–1.27 + 1.verify.r2 → gate 1.verify.r2h GATE-FAILED (#021) → 1.28 + 1.verify.r2.g1 → gate 1.verify.r2.g1h ACCEPT → review r2 FAIL (#review-1-r2) → fix chain 1.29–1.32 + 1.verify.r3 → gate 1.verify.r3h GATE-FAILED (#022) → 1.33 + 1.verify.r3.g1 → gate 1.verify.r3.g1h ACCEPT a2 after a transient rerun (#023) → review r3 FAIL (#review-1-r3) → fix chain 1.34–1.38 + 1.verify.r4 → gate 1.verify.r4h GATE-FAILED (#024) → 1.39 + 1.verify.r4.g1 → gate 1.verify.r4.g1h ACCEPT a1 → review r4 (Grok retried alone after a 402, its report derived, #025) → this reconciliation.
Review attempt r4 at 6c0e601: Claude FAIL (1/3/5), Sol FAIL (1/1/0), Grok FAIL (1/3/2, derived — outside the count) → reconciliation verdict FAIL (DECISIONS #review-1-r4): 1 blocker family (a `delete` run whose edge is punctuation beside unmarked alphanumeric text serialises to `~~` delimiters GFM cannot open or close — `mdast-util-gfm-strikethrough`'s handler has no flanking logic; the serializer half of the fixed-point property, wrong for the parser's own tree too; Claude's and Sol's routes both one Backspace, reproduced headlessly), 1 should-fix taken (the paragraph → setext-heading pair in `needsBlankLine`), 1 should-fix required (`toSource` answers a block's start for a cursor past every known inline node — the `[1.37]` line's trigger fired), 1 should-fix taken with its nit (the chord guard's regex and its false allowlist reason), 4 nits (1 backlog, 3 taken or recorded in the reconciliation); r3 dispositions reopened: 1 of 10 as a family (I1 → J1, on the serializer's side), 9 confirmed.
Next: this commit integrates as task(1.10.r4d) → 1.40 (the `delete` handler in packages/core/src/format.ts; guards per side × neighbour × inner class; the invariant-A fixture) → 1.41 (the neighbour leg; Claude's and Sol's browser reproductions) → 1.42 (the setext pair) → 1.43 (`toSource`'s third clause; the `it.fails` case folded) → 1.44 (the chord guard's modifier chain and reason) → 1.verify.r5 → HUMAN_GATE 1.verify.r5h (`scripts/gate.sh 1.verify.r5h`; the principal rotates there) → 1.10.r5a/b/c (Michael decides first whether Grok reviews r5, #025) → PRINCIPAL 1.10.r5d (a fresh session) → 1.close on PASS.
Open human gates: none (next: 1.verify.r5h after 1.44 and 1.verify.r5). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r5 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN (#review-0-r1 deviation 29); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns with the journal stubbed first (the 80-turn raise conditional on Michael's OK at the Phase 2 boundary); after a `gate.sh rerun` that accepts, never run the `--resume` doctor names — #023's abandon once per rerun; before any single-reviewer retry, #025's procedure (hold the sibling directories, clear the failed attempt's own report.md); a Grok 402 is Michael's call; the boundary fix(runner) items (#023, #025, H7) land after 1.close with the runner idle on phase/2.
Reviewer record (#review-1-r4): two independent reports (Claude, Sol) and one derived (Grok, #025); unique findings Claude 7 of 9, Sol 0 of 2; the blocker shared and agreed, the cursor map shared with the Required flag differing; cumulative over nineteen shared findings, eight disagreements, the low side is Grok 6, Claude 2, Sol 2 — unchanged, still a signal, not a verdict; Grok's PASS-with-blockers count stays 2 of 2 counted attempts; no change inside a review set; whether Grok reviews r5 is Michael's question, the r5 rows are written for three reviewers.

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [1.verify.r4] 2026-09-12T06:38:07Z Task: Phase 1 verification for review attempt r4 (RUNNER-SPEC §3, §5.5): freeze dependencies, run the full suite in the container, confirm the phase head is ready for the three-OS CI run, and carry the promote-lessons step for the r4 chain. Status: Complete. `git status` clean at start (base: task 1.38), no recovery commit; stub committed first (`wip(1.verify.r4): journal stub`) per CLAUDE.md's list. **Three candidates weighed at DECISIONS #review-1-r3, dispositioned here as the task text says.** **(a) Promoted**, one sentence appended verbatim to steps 2 and 4 of `## Every iteration` in both `CLAUDE.md` and `AGENTS.md`, identical in both files: "Each attempt appends its own `- [<id>] ` journal line; completing an earlier attempt's stub and appending nothing is `NO-JOURNAL` even with DONE printed and the suite green (lesson [1.29]; the runner's stop-check counts journal lines per attempt)." **Discharging test**: `ralph/test/conformance.test.mjs`, `test("stop-check: STUCK after 3 attempts without DONE; retry resets; NO-JOURNAL stops the run")` — it stubs `0.1` with `journal: false`, asserts the run reports `NO-JOURNAL 0.1` and the task status stays `running` rather than progressing, i.e. it is the runner's own stop-check counting journal lines per attempt. **(b) Promoted**, one bullet appended verbatim as the last line of `## Code rules` in both files, before `## Runner facts you rely on`: "An e2e places the rendered caret only through motions Blink alone decides — arrow keys (`ArrowUp`/`ArrowDown` reach a one-line block's edges on every OS, then counted `ArrowRight`/`ArrowLeft`) or a click at a computed point — never a Home/End key or a `ControlOrMeta+` chord in a contenteditable, because Cocoa resolves those before Blink sees them; a mark's text is asserted byte-exact with `textContent` and `toBe`, never a normalising matcher; a case whose bytes cannot tell two trees apart asserts the tree (DECISIONS #022)." **Discharging tests**: 1.33's acceptance greps on `e2e/web/editor-mark-edge.spec.ts` (`grep -cE 'ControlOrMeta\+(Home|End)|"(Home|End)"'` **0**, `grep -c 'em")).toHaveText'` **0** — the file's own H8 and absence cases are the byte-exact/tree-vs-bytes members), plus `tests/no-caret-chords-in-e2e.test.ts`'s tree-wide guard, `describe("no e2e/web file places the caret with a modifier chord on Home or End")` (per-file `it` "`${file}` has no ControlOrMeta, Control or Meta chord with Home or End") and `describe("a bare Home or End press appears only at the allowlisted source-view sites")` (per-file `it`s "`${file}` has no bare Home or End press" and "`${file}` has exactly `${allowed.count}` bare press(es) — `${allowed.reason}`") — together these name every file the rule binds, not only the one 1.33 touched. **(c) Recorded, not promoted**: the `[0.0]` doctor lesson (a doctor remedy is read against `gate.mjs` before anyone runs it; every conformance scenario that ends in a new state ends with a doctor-clean assertion) addresses the principal and the runner's own conformance suite, which this verify task cannot edit (it is zero-dependency and scoped to `docs/lessons.md`, the journal and the two agent-rule files) — it lands in the boundary `fix(runner)` DECISIONS #023 scopes and in `docs/PRINCIPAL.md` at the boundary, so it is recorded here rather than promoted into CLAUDE.md/AGENTS.md. `bash ralph/check-agent-rules.sh` exits **0** (`check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`) and `tests/agent-rules.test.ts` passes. No other procedure change this attempt; no code, fixture, spec or config file touched beyond CLAUDE.md, AGENTS.md and the journal. **Verification, this container (Linux; no other OS claimed):** `pnpm lint` clean (eslint, `check-deps` 61 npm + 4 cargo direct dependencies all allowlisted, `tsc --noEmit` across all 6 typechecked projects; the fresh worktree's `pnpm install` — 760-entry lockfile, supply-chain policy check passed — added `node_modules` only, `git status --short` and `git diff --stat pnpm-lock.yaml` both empty after); `pnpm test` **2798 passed, 1 expected fail** (1.37's `it.fails` case) across 37 files, `tests/agent-rules.test.ts` and `tests/no-caret-chords-in-e2e.test.ts` (40 tests) both green, coverage 99.66% statements / 97.91% branches / 100% functions / 100% lines; `cargo test` (workspace: `desktop_lib`, `desktop`, doc-tests) **0/0/0** (vacuous, Rust surface is Phase 2); `pnpm --filter @essaydown/e2e-web exec playwright install chromium` then the full `e2e/web` suite **83/83 passed**, every spec file under `e2e/web/` including the 1.17 latency spec. `git diff --stat` against the branch base (1.38, `04517a8`): exactly `CLAUDE.md` (+5/−2), `AGENTS.md` (+5/−2), `docs/progress/journal-main.md` (this entry) — no `package.json` (× 9), `pnpm-lock.yaml`, `pnpm-workspace.yaml`, any `Cargo.toml` or `Cargo.lock` change, zero new dependencies (PRD §4); `npx prettier --check CLAUDE.md AGENTS.md` clean (the pre-existing `docs/lessons.md`/`docs/progress/journal-main.md` prettier warnings predate this attempt, confirmed by stashing this attempt's diff and re-running the check, and are outside `pnpm lint`'s scope, which runs eslint/check-deps/tsc only). Secrets scan over the whole working tree (key- and address-shaped patterns, `node_modules`/`target`/`test-results` excluded): the same pre-existing hits noted at 1.10.r3d/1.38 (the `.invalid` git-identity domains in `docker/Dockerfile`, `ralph/test/harness.mjs`, `ralph/lib/close.mjs` and this journal's own quoted CI log line; the lockfile's `glob` deprecation notice; the `@2x` icon path), none touched by this branch, no personal identifier. No defect found outside scope; no `docs/V1.1-BACKLOG.md` line. Files: `CLAUDE.md`, `AGENTS.md`, `docs/progress/journal-main.md`. Tests: as above, all green. Iterations used: 1. First attempt passed: y. Tool calls: ~26. Notes: nothing outstanding from 1.verify.r4; next is the `1.verify.r4h` HUMAN_GATE (the three-OS `ci.yml` run at this SHA, artifacts `test-logs` and `playwright-report`), then `1.10.r4a/b/c` and `1.10.r4d`, then `1.close`.
- [plan.1.verify.r4h.r0] 2026-09-12T09:40:45Z Task: planning commit for the 1.verify.r4h GATE-FAILED (ci.yml run 34685866034 at 4c133e0: ubuntu-latest and windows-latest 1 e2e/web case red at the Playwright step, the same on both — task 1.38's G4 first-line case in editor-soft-line-breaks.spec.ts, its caret precondition receiving offset 8 and 16 instead of 5 because `ArrowUp` in a two-line paragraph is a visual-line motion that lands by x-position and font metrics; macos-latest green on all 83 cases; every unit test, cargo and lint step green on all three; artifacts [] because the gate fetches only on success). Status: Complete. Files: docs/PRD.md (§8 Phase 1: +1.39 the anchor-and-count caret route in editor-soft-line-breaks.spec.ts only — the block's end asserted, then counted `ArrowLeft` — the doc comment rewritten, the shortened-count mutation, the reproduction quoted; +1.verify.r4.g1 needsCI repair with refTemplate ci/1.verify.r4.g1/a{n}; 1.10.r4a/b/c → 1.verify.r4.g1 with their accepted-artifact path moved to /logs/ci/1.verify.r4.g1h/accepted/), ralph/tasks.json + ralph/EXPECTED_COUNT (208 → 211, regenerated: 164 raw + 21 review-set + 26 CI gates; the generator makes the reviewers depend on 1.verify.r4.g1h), docs/DECISIONS.md (#024: what happened, cause, scope decision, graph change, model choice, reviewer notes, reversal), docs/lessons.md (+1), docs/V1.1-BACKLOG.md (the two `toDocumentEnd` uses in editor-trailing-break.spec.ts, hard stop 2.verify). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (211 tasks); bash ralph/test/run.sh green (83 pass / 0 fail) before the commit. Commit: wip(plan.1.verify.r4h.r0). Iterations used: 1. First attempt passed: y. Tool calls: ~8. Reviewer overturned: pending. Notes: the runner integrates this as plan(plan.1.verify.r4h.r0), supersedes 1.verify.r4h, then runs 1.39 and 1.verify.r4.g1; next human gate is scripts/gate.sh 1.verify.r4.g1h; the ubuntu and windows Playwright legs of editor-soft-line-breaks.spec.ts are the first unknown of that round.
- [1.39] 2026-09-12T09:46:55Z Task: Phase 1 verification-gate repair, test-only (repair after the 1.verify.r4h GATE-FAILED, ci.yml run 34685866034 at 4c133e0: ubuntu-latest and windows-latest red on one e2e/web case — `e2e/web/editor-soft-line-breaks.spec.ts` G4 first-line caret precondition, expected offset 5, received 8 and 16; DECISIONS #024) (attempt 1). Status: Complete. `git status` clean at start (base: plan.1.verify.r4h.r0), no recovery commit; stub committed first (`wip(1.39): journal stub`) per CLAUDE.md's list, which stands over the wrapper's older steps (DECISIONS #review-1-r2 H7). **Reproduction, before the change.** `grep -c 'toDocumentStart\|toDocumentEnd' e2e/web/editor-soft-line-breaks.spec.ts` was **5** (the two definitions, the two uses, and the doc comment's mention — at least the 4 the acceptance names). This container is Linux and cannot run the ubuntu-latest or windows-latest geometry, so the failure is quoted from /logs/ci/1.verify.r4h/a1/workflow.log: step `test (ubuntu-latest)` › `Playwright (e2e/web)` (lines 2693, 2734–2759): `✘  45 editor-soft-line-breaks.spec.ts:119:3 › … › G4: a space typed at the end of the first line of a wrapped paragraph carries no entity (5.5s)`, then `Error: expect(received).toEqual(expected) // deep equality` / `- Expected  - 1` / `+ Received  + 1` / `Object {` / `"kind": "text",` / `-   "offset": 5,` / `+   "offset": 8,` / `"text": "alpha` / `beta gamma",` / `Call Log:` / `- Timeout 5000ms exceeded while waiting on the predicate` / `> 135 |       .toEqual({ kind: "text", text: "alpha\nbeta gamma", offset: "alpha".length });` at `e2e/web/editor-soft-line-breaks.spec.ts:135:8`; step `test (windows-latest)` › `Playwright (e2e/web)` (lines 5821, 5861–5886): `x  48 editor-soft-line-breaks.spec.ts:119:3 › … (5.7s)`, the identical diff with `-   "offset": 5,` / `+   "offset": 16,`, the same `Timeout 5000ms exceeded while waiting on the predicate` at `e2e\web\editor-soft-line-breaks.spec.ts:135:8`. The same case is `✓ 53` on macos-latest (line 4236); the file's other three cases are green on all three runners (ubuntu 2681/2696/2697, macos 4235/4237/4238, windows 5811/5823/5824). Confirmed against the spec before anything changed: line 131 was `toDocumentStart(page, 2)` (two `ArrowUp`) then five `ArrowRight`, so the received 8 is `ArrowUp`×2 landing at 3 and 16 is it landing at 11 — a visual-line motion by x-position in a two-line paragraph, the principal's reading; the poll timed out before the space was typed, so the app's whitespace strip was never exercised on those runners and is not implicated. **Change**, `e2e/web/editor-soft-line-breaks.spec.ts` only (+41/−42). (1) Caret placement: `toDocumentStart`/`toDocumentEnd` are deleted; the first-line case now asserts the anchor first — `expect.poll(() => caret(page)).toEqual({ kind: "text", text: "alpha\nbeta gamma", offset: "alpha\nbeta gamma".length })` (16, the paragraph's end where `seedFromSource` leaves the caret) — then presses `ArrowLeft` `"beta gamma".length + 1` times (11; the LF is one character in the text node under `white-space: pre-wrap`) and keeps its existing offset-5 assertion before the space; the hard-break case keeps its `.ProseMirror` click, its end-of-block assertion (`"Second line after a hard break."` at 31) and its 31 `ArrowLeft`, and loses the `toDocumentEnd(page, 2)` between the click and the assertion — the click alone is the anchor, and the container's run shows it lands at the block's end. `press` (the counted-key helper) and `caret` are unchanged. (2) The doc comment's caret-placement paragraph is rewritten to state the rule once: in the rendered view a position is reached by counted horizontal arrows from an anchor the case asserts first (the block's end where the seed's click leaves the caret; the count is a property of the document's characters, an LF and a `<br>` one position each); a vertical arrow only in a one-line block, and why the gate went red in a two-line one (offset 3 on ubuntu, 11 on windows); never a Home or End key or a modifier chord, the chord named in prose only (lesson [1.30]); "Blink-only" means decided from the document's characters alone, not merely handled by Blink. The prose says "a vertical arrow (up or down)" rather than the key names so the acceptance grep for the two vertical keys is 0 over the whole file. No new helper; no source, DevEditor, config or workflow file touched; `e2e/web/editor-trailing-break.spec.ts` untouched (its two `toDocumentEnd` uses stay the `[plan.1.verify.r4h.r0]` backlog line, hard stop 2.verify); `tests/no-caret-chords-in-e2e.test.ts` untouched and green (40 tests). **Able to fail, in the container**, the shortened-count mutation: the spec copied to /tmp, `"beta gamma".length + 1` → `"beta gamma".length` (10 `ArrowLeft` instead of 11), run, restored with `cp` in the same tool call (lesson [1.25]) — **red on the location assertion**, the case `G4: a space typed at the end of the first line of a wrapped paragraph carries no entity`: `- Expected  - 1` / `+ Received  + 1` / `-   "offset": 5,` / `+   "offset": 6,` / `- Timeout 5000ms exceeded while waiting on the predicate` at `> 133 |       .toEqual({ kind: "text", text: "alpha\nbeta gamma", offset: "alpha".length });` (`editor-soft-line-breaks.spec.ts:133:8`), 1 failed / 3 passed; after the restore `git diff --stat` is the intended 41/42 diff and `grep -c '"beta gamma".length + 1'` is 1. **Acceptance greps, after the change**, on the spec: `grep -c 'toDocumentStart\|toDocumentEnd\|ArrowUp\|ArrowDown'` **0**; the first-line case asserts offset 16 in `alpha\nbeta gamma` (spec line 128) before its `ArrowLeft` (line 130) and offset 5 after it (line 133); the hard-break case asserts its end-of-block anchor (line 160) before its `ArrowLeft` count (line 162); `grep -cE 'ControlOrMeta\+(Home|End)|"(Home|End)"'` **0**; `grep -c 'process.platform'` **0**; `grep -cE '\.(skip|only)\(|runIf|skipIf'` **0**; `grep -c timeout` **0** (no timeout option added, no poll widened); `npx prettier --check e2e/web/editor-soft-line-breaks.spec.ts` clean. **A note on the acceptance's count:** it says "all five cases of this file"; the file has four `test(` cases (G3, the two G4s, the absence case) since 1.25 and 83 e2e/web cases in total, unchanged by 1.38 and by this task — no case was added, because none is asked for; the four are the "all". Secrets scan over the whole working tree (key-shaped patterns, `node_modules`/`target`/`test-results`/`.git` excluded): 0 files hit; `git status --short` empty before the commit. No defect found outside this scope; no `docs/V1.1-BACKLOG.md` line. Files: `e2e/web/editor-soft-line-breaks.spec.ts`, `docs/lessons.md` (+1), this entry. `git diff --stat` against the branch base (plan.1.verify.r4h.r0, `0251db3`) is exactly those three files; no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock` change — zero new dependencies (PRD §4). Tests: `pnpm install --frozen-lockfile` (fresh worktree, no lockfile change), `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (lessons 1.3/1.4/1.12; Chrome Headless Shell 153), then this spec **4/4** (baseline before the edit not run — the miss is ubuntu/windows geometry, as the log says) and the full `e2e/web` suite **83/83** green including the 1.17 latency spec; `pnpm lint` exit 0 (eslint, check-deps, `tsc --noEmit` on all six typechecked projects); `pnpm test` **2798 passed, 1 expected fail** (1.37's `it.fails`) across 37 files, `tests/no-caret-chords-in-e2e.test.ts` 40/40; `cargo test` 0/0/0 (Rust surface is Phase 2). Iterations used: 1. First attempt passed: y. Tool calls: 14. **Honest statement:** this container is Linux; the repaired route uses only counted character motions from an asserted anchor, but the only proof of an ubuntu-latest and windows-latest pass is the `1.verify.r4.g1h` gate green on all three runners, which this task cannot observe and does not claim. Notes: nothing outstanding from 1.39; next is 1.verify.r4.g1 and then the `scripts/gate.sh 1.verify.r4.g1h` three-OS gate, where the ubuntu and windows Playwright legs of this file are the first unknown; if either is red on an anchor assertion, the received `{ kind, text, offset }` names where the seed's click or the source-view toggle left the caret on that runner — the anchor, not the count, would be the wrong assumption.
- [1.verify.r4.g1] 2026-09-12T09:49:06Z Task: Phase 1 verification for review attempt r4, repair 1 of 1.verify.r4 after the 1.verify.r4h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #024): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. Status: Complete. `git status` clean at start (base: task 1.39, `af8a62c`), no recovery commit; stub committed first (`wip(1.verify.r4.g1): journal stub`) per CLAUDE.md's list. **No procedure change and no code, test, fixture or config edit.** The promote-lessons step carried by `1.verify.r4` (the journal-line-per-attempt sentence in `## Every iteration` and the Blink-only caret rule in `## Code rules` of CLAUDE.md/AGENTS.md) is already integrated at `4c133e0` and is not repeated here; `bash ralph/check-agent-rules.sh` exits **0** (`check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`) and `tests/agent-rules.test.ts` passes (inside the `pnpm test` run below). **Verification, this container (Linux; no other OS claimed).** `pnpm install --frozen-lockfile` (fresh worktree, 760-entry lockfile, supply-chain policy check passed) added `node_modules` only, `git status --short` and `git diff --stat pnpm-lock.yaml` both empty after. `pnpm lint` clean: eslint, `check-deps` (61 npm + 4 cargo direct dependencies, all allowlisted), `tsc --noEmit` across all 6 typechecked projects (`packages/coach`, `packages/core`, `packages/export`, `packages/modes`, `packages/editor`, `apps/desktop`). `pnpm test` **2798 passed, 1 expected fail** (1.37's `it.fails` case) across 37 files, `tests/agent-rules.test.ts` and `tests/no-caret-chords-in-e2e.test.ts` (40 tests) both green, coverage 99.66% statements / 97.91% branches / 100% functions / 100% lines — identical to `1.verify.r4`'s numbers, since no source file has changed since (1.39 touched only `e2e/web/editor-soft-line-breaks.spec.ts`, outside `pnpm test`'s scope). `cargo test` (workspace: `desktop_lib`, `desktop`, doc-tests) **0/0/0** (vacuous, Rust surface is Phase 2; `git diff --stat Cargo.lock` empty). `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (per-attempt browser cache, lessons 1.3/1.4/1.12) then the full `e2e/web` suite **83/83 passed**, every spec file under `e2e/web/` including the 1.17 latency spec and 1.39's repaired `editor-soft-line-breaks.spec.ts` (the anchor-and-count G4 first-line case among the four green cases in that file) — the case that failed only on `ubuntu-latest` and `windows-latest` in the `1.verify.r4h` GATE-FAILED run is exercised green in this Linux container as it was at `1.39`; the only unknown this task cannot resolve is the ubuntu-latest/windows-latest legs themselves, left to the `1.verify.r4.g1h` three-OS gate. `git diff --stat` against the branch base (1.39, `af8a62c`) is exactly `docs/progress/journal-main.md` (the stub commit, now this completion) — no dependency-manifest or lockfile change (`package.json` × 9, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, every `Cargo.toml`, `Cargo.lock` all zero-line diff), zero new dependencies (PRD §4); `docs/lessons.md` untouched (nothing learned beyond what 1.39 and 1.verify.r4 already recorded). Secrets scan over the whole working tree (key-shaped patterns, `node_modules`/`target`/`test-results`/`.git` excluded): 0 new hits; nothing touched by this branch. Files: `docs/progress/journal-main.md` only. Tests: as above, all green. Iterations used: 1. First attempt passed: y. Tool calls: ~14. Notes: nothing outstanding; the phase head (this commit, once squashed) is ready for the `1.verify.r4.g1h` three-OS CI gate, where the ubuntu-latest and windows-latest Playwright legs of `editor-soft-line-breaks.spec.ts` (1.39's repair) are the first unknown; the review-set r4 and the open Phase-2 backlog lines (1.29, 1.32, 1.37's deletion leg, the two `toDocumentEnd` uses in `editor-trailing-break.spec.ts`) remain open for their named hard stops.
- [1.10.r4d] 2026-09-12T11:55:27Z Task: reconciliation of Phase 1 review attempt r4 at 6c0e601 (verifier 1.verify.r4.g1, gate 1.verify.r4.g1h ACCEPT a1, run 34687540740; Grok retried alone after a 402 and read its siblings' reports, DECISIONS #025). Claude FAIL (1 blocker/3 should-fix/5 nits), Sol FAIL (1/1/0), Grok FAIL (1/3/2) — derived, outside the count. Verdict FAIL: a strikethrough run whose edge is punctuation beside unmarked alphanumeric text serialises to `~~` delimiters GFM cannot open or close, because `mdast-util-gfm-strikethrough`'s handler has none of the `encodeInfo` logic the built-in emphasis/strong handlers have — the serializer half of the fixed-point property, wrong for the parser's own tree too (Claude, one Backspace after `~~beta.~~`; Sol, one Backspace after `~~a [b ](u)~~`; reproduced headlessly here for both sides, four neighbour classes and three inner classes, and from `link-in-emphasis.md` by one deletion); the paragraph → setext-heading pair missing from `needsBlankLine` (reproduced); `toSource` answering a block's start for a cursor past every known inline node (the `[1.37]` line's trigger fired, Sol Required; reproduced); the chord guard's regex missing Shift/Alt chains and its allowlist reason false (confirmed). Status: Complete. Files: docs/reviews/phase-1-r4-{claude,sol,grok}.md (byte-identical copies, `cmp` clean), docs/DECISIONS.md (#review-1-r4: verdict FAIL first line, 9 findings J1–J9 with severity and disposition, I1–I10 confirmed with I1's family reopened as J1 on the serializer's side, the reviewer record counted from the two independent reports with Grok derived per #025 — unique findings Claude 7 of 9, Sol 0 of 2; two shared findings, both agreed; cumulative over nineteen shared findings, eight disagreements, low-rater instances Grok 6, Claude 2, Sol 2 —, Michael's Grok decision cited and the r5 question recorded for him, #025 quoted, the turn-budget counts from the state file and the transcripts — 19 of 46 loop tasks at more than one attempt, six of six schema.ts opus fix tasks capped at a1 —, the three promote candidates weighed for 1.verify.r5, the cherry-pick record, graph change, reversal), docs/PRD.md (§8 Phase 1: +1.40 core `delete` handler (opus), +1.41 neighbour leg and browser reproductions (sonnet, test-only), +1.42 setext pair (sonnet), +1.43 cursor map (opus), +1.44 chord guard (sonnet, test-only); +1.verify.r5 needsCI carrying two promoted rules and one recorded; +1.10.r5a/b/c/d; 1.close → 1.10.r5d), ralph/tasks.json + ralph/EXPECTED_COUNT (211 → 222, regenerated: 174 raw + 21 review-set + 27 CI gates), docs/V1.1-BACKLOG.md (+8 lines: #025's boundary fix(runner), the [1.37] line dispositioned, the vertical-arrow line's third site, J7's heading entity, the serializer-handler class, the sweep evidence, the turn-budget counts, the reviewer record), docs/lessons.md (+7), docs/progress.md (regenerated), docs/handoffs/020-*, 021-*, 022-* and next-prompt.md (cherry-picked from handoff/020, handoff/021 and handoff/022 with #025, the next-prompt conflict resolved to the incoming version twice). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (222 tasks); bash ralph/test/run.sh run before the commit (result in the commit body); the reproductions ran in a detached scratch worktree at 6c0e601 (two throwaway vitest files, removed with the worktree), never in this worktree — no node_modules here. Commit: wip(1.10.r4d). Iterations used: 1. First attempt passed: y. Tool calls: ~60. Reviewer overturned: pending. Notes: the runner integrates this as task(1.10.r4d) and runs 1.40; the next human gate is scripts/gate.sh 1.verify.r5h (the principal rotates there); before 1.10.r5a/b/c start Michael decides whether Grok reviews r5 (#025); a single-reviewer retry follows #025's manual procedure.

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.23 | 2 | ~22 (a1) + ~12 (a2) | n (a1 `error_max_turns` at 51, exit 1, journal present — no stop signal) | n (its three copy cases read the OS clipboard: the windows CRLF gate failure, #021, repaired by 1.28) |
| 1.24 | 1 | ~30 | y | y (H2: the pending segment keeps one keystroke time, so a flush splits a burst longer than the window) |
| 1.25 | 2 | n/a (the fields are absent from its entries) | n (NO-JOURNAL at a1, `error_max_turns` at 51) | y (H1/H5: the atom itself at a block end; H6: a cell's line ending) |
| 1.26 | 1 | ~24 | y | n |
| 1.27 | 1 | ~20 | y | n (H9: one tautology, nit, taken in 1.32) |
| 1.verify.r2 | 1 | ~14 | y | partial (H7: the wrapper `ralph/PROMPT.md` not carried along — a runner file, fixed at the boundary) |
| 1.28 | 1 | ~18 | y | partial (H4: the spy removed the suite's last real clipboard write — #021's scope decision) |
| 1.verify.r2.g1 | 2 | ~12 (a1) + ~20 (a2) | n (a1 exit 0 at `num_turns` 11, stopped by choice on H7; no stop signal) | n |
| 1.10.r2a (Claude) / r2b (Sol) / r2c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.29 | 3 | ~50 (a1) + ~16 (a2) + ~10 (a3) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal; a2 `success` at 21 with DONE and NO-JOURNAL — the stub completed in place, `chore(1.29)`) | partial (I1: the atom at a *mark's* edge, deferred by 1.30's backlog line; the deletion leg's ranges stop at the block's end) |
| 1.30 | 2 | ~50 (a1) + ~20 (a2) | n (a1 `error_max_turns` at 51, journal present — no stop signal) | y (I3: the mark-edge clause splits a link-marked run, a regression; I1's deferral written by this task on a false reachability claim) |
| 1.31 | 1 | ~34 (49 turns, 48 tool uses — one under the cap) | y | n |
| 1.32 | 1 | ~30 | y | n (I8: the fourth tautology, in a file outside its scope, filed on the backlog as asked) |
| 1.verify.r3 | 1 | ~20 | y | partial (I5: the promoted rule discharged at the neighbouring family only; the position map stayed insertion-only) |
| 1.33 | 1 | ~21 | y | n (gate 1.verify.r3h GATE-FAILED on 1.30's chord, #022, repaired here; the four sibling chords are I6 by #022's own trigger) |
| 1.verify.r3.g1 | 1 | ~20 | y | n (gate 1.verify.r3.g1h ACCEPT at a2 after a transient rerun, #023; I7 recorded) |
| 1.10.r3a (Claude) / r3b (Sol) / r3c (Grok) | 1 / 1 / 1 | 85 turns / — / 19 turns | y / y / y | — |
| 1.34 | 2 | ~50 (a1) + ~25 (a2) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal) | partial (J1: the atom clause holds; the family's next member is the serializer's, `format.ts`) |
| 1.35 | 2 | ~50 (a1) + ~19 (a2) | n (a1 `error_max_turns` at 51, stub present) | n (J1's link member runs through its `outermostMark` rule as designed; the serializer is at fault) |
| 1.36 | 2 | ~50 (a1) + ~24 (a2) | n (a1 `error_max_turns` at 51, stub present) | partial (J2: the paragraph → setext-heading pair missing from `needsBlankLine`; the kind table lacked it) |
| 1.37 | 2 | ~50 (a1) + ~35 (a2) | n (a1 `error_max_turns` at 51, stub present — its stub carries no timestamp, J8) | partial (J3: the defect it found and pinned as `it.fails`, deferred on scope; taken in 1.43) |
| 1.38 | 1 | ~34 | y | partial (J4/J5: the guard's regex misses Shift/Alt chains and its allowlist reason is false; gate 1.verify.r4h GATE-FAILED on its `ArrowUp` route, #024, repaired by 1.39) |
| 1.verify.r4 | 1 | ~39 | y | n |
| 1.39 | 1 | ~17 | y | n (J6: the `[plan.1.verify.r4h.r0]` line it left names two of three sites — a reconciliation correction) |
| 1.verify.r4.g1 | 1 | ~25 | y | n (gate 1.verify.r4.g1h ACCEPT at a1) |
| 1.10.r4a (Claude) / r4b (Sol) / r4c (Grok) | 1 / 1 / 2 container runs (a 402, then `retry` alone — #025) | 110 turns / — / 19 turns (13 on the 402 run) | y / y / n (STUCK 1.10.r4 on the 402) | — (Grok's report derived) |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).

Phase 1 review r2: findings as reconciled — 2 blockers (in tasks 1.29 and 1.31), 3 should-fix required (in tasks 1.30 and 1.32), 1 should-fix taken as a runner chore at the phase boundary (H7, `fix(runner)` with a conformance test), 3 nits (2 taken in task 1.29, 1 in task 1.32); r1 dispositions reopened: 3 of 12 as siblings or families (G1's attestation → H4, G2 → H2, G4 → H1/H5/H6), 9 confirmed; the r1 backlog deferral of F1's emphasis-edge member reopened (H8, its reachability premise false). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 92 (docs/lessons.md lines tagged 1.*, plan.1.*; 84 before this commit + 8). Lessons promoted to CLAUDE.md/AGENTS.md: 5 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2) + 1 pending (via 1.verify.r3). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 10 (the nine of r1 plus 1.25 at a1); other second attempts: 1.23 (capped, journal present) and 1.verify.r2.g1 (stopped by choice), neither a stop signal. Gate reruns: 0. Gate repairs: 2 (1.verify.g1, 1.verify.r2.g1). Coverage at r2 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r1: 0, 0, 0, 0 (1.26–1.28 and the verifiers touched no `packages/*/src`; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.2 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 20.6 / 22.7 / 11.9 ms on the ubuntu / windows / macos runners of the accepted gate. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.28: 14 of 26 (10 of 20 before the r2 chain, 4 of 6 in it). By the transcript measure the two capped first attempts this chain ended at 50 and 56 tool uses and the five that finished first at 28–59 — the r1 reading (the cap sits on the median of the work) stands; only 1.28 and 1.verify.r2.g1 ran under the journal-stub rule, with no cap and no NO-JOURNAL between them.

Phase 1 review r3: findings as reconciled — 2 blockers (in tasks 1.34 and 1.36), 3 should-fix required (in tasks 1.35, 1.37 and 1.38), 1 should-fix taken with its sibling in 1.35 (I4), 1 should-fix recorded (I7, the a2 rerun answered by #023, with a conditional boundary candidate and a flake-signature line), 3 nits (1 taken in task 1.34, 1 taken in the reconciliation — the address redaction, 1 decided — Unicode whitespace stays ASCII); r2 dispositions reopened: 2 of 9 as families or members (H1 → I1, H8 → I3 and I1), 7 confirmed; the `[1.30]` backlog deferral of the break-inside-a-mark member reopened (its reachability premise false, as H8's was at r2). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 116 (docs/lessons.md lines tagged 1.*, plan.1.*; 106 before this commit + 10). Lessons promoted to CLAUDE.md/AGENTS.md: 6 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3) + 2 pending (via 1.verify.r4). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 11 (the ten of r2 plus 1.29 at a2 — procedure, not budget); capped first attempts this chain: 1.29 and 1.30 (both continued under the stub rule, no stop signal). Gate reruns: 1 (1.verify.r3.g1h a1 → a2, transient, #023). Gate repairs: 3 (1.verify.g1, 1.verify.r2.g1, 1.verify.r3.g1). Coverage at r3 (three CI legs and both cold builds agree): statements 99.64 %, branches 97.73 %, functions 100 %, lines 100 %; delta vs r2: +0.01, +0.08, 0, 0 (1.29–1.31 added 153 tests; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 11.5 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 26.4 / 20.7 / 14.3 ms on the ubuntu / windows / macos runners of the accepted gate; source commit p95 56.1 / 59.9 ms isolated (G9, evidence only). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.33: 18 of 31 (14 of 26 before the r3 chain, 4 of 5 in it: 1.31, 1.32, 1.33 and the verifiers at a1; 1.29 and 1.30 capped). By the transcript measure the two capped first attempts this chain ended at 50 tool uses each and the three that finished first at 48 (1.31, one turn under the cap), 38 and 22; the whole chain ran under the journal-stub rule, which turned both caps into continued attempts rather than NO-JOURNAL stops. Turn-budget decision unchanged: 80 conditional on Michael's OK at the Phase 2 boundary.

Phase 1 review r4: findings as reconciled — 1 blocker (in tasks 1.40 and 1.41, the serializer half of the flanking family), 1 should-fix required (in task 1.43, the cursor map — the `[1.37]` backlog line's trigger fired), 2 should-fix taken (1.42 the setext pair; 1.44 the chord guard's regex), 5 nits (1 taken in 1.44 — the allowlist reason, 2 taken in the reconciliation — the vertical-arrow line's third site and the timestamp-less stub recorded, 1 backlog — the heading entity, 1 evidence — the turn budget); r3 dispositions reopened: 1 of 10 as a family (I1 → J1, on the serializer's side), 9 confirmed. Verdict FAIL. Reviewer record: two independent reports and one derived (Grok, DECISIONS #025 — a single-reviewer retry after a 402 read its siblings' finished reports; unique findings 0 by construction, verdict outside the count). Lessons written by Phase 1 agents and the principal: 131 (docs/lessons.md lines tagged 1.*, plan.1.*; 124 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 8 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3, 2 via 1.verify.r4); 2 more carried by 1.verify.r5 (the two-halves ownership of a fixed-point family; a kind table derived from the rule's branches), 1 recorded. STUCK signals this attempt: 1 (1.10.r4, Grok's 402). Turn budget: four of the five r4 opus fix tasks capped at a1 (six of six schema.ts opus fix tasks across r2–r4); 19 of 46 Phase 1 loop tasks needed more than one attempt; the raise stays conditional on Michael's OK.
