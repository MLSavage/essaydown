# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (#review-1-r0) → fix chain 1.13–1.22 + 1.verify.r1 → gate 1.verify.r1h ACCEPT → review r1 FAIL (#review-1-r1) → fix chain 1.23–1.27 (1.23 and 1.25 at attempt 2, both capped at a1) + 1.verify.r2 (one promoted rule; the journal-stub procedure) → gate 1.verify.r2h GATE-FAILED (#021, windows clipboard CRLF) → 1.28 + 1.verify.r2.g1 (attempt 2; a1 stopped by choice on the `ralph/PROMPT.md` drift) → gate 1.verify.r2.g1h ACCEPT (a1, run 34509206442, three OS directories in both artifacts; 68 Playwright and 2,043 vitest per OS).
Review attempt r2 at c9408ee: Claude FAIL (1/3/2), Sol FAIL (1/0/1), Grok PASS (0/0/2) → reconciliation verdict FAIL (DECISIONS #review-1-r2): 2 blockers (a `hard_break` left as a block's last node serialises to a bare trailing `\` that does not round-trip — the F1/G4 family, an atom this time; a flush inside a source burst that continues past one coalescing window splits undo history), 3 should-fix required (the toggle's flush has no test that fails without it; no test exercises the real clipboard write after 1.28; the r1 deferral of the emphasis-edge `&#x20;` member rested on a false reachability claim — reachable by one caret keystroke), 1 should-fix taken as a boundary runner chore (`ralph/PROMPT.md`'s stale iteration list, H7), 3 nits (2 in 1.29, 1 in 1.32). Fix chain 1.29–1.32 (all opus) + 1.verify.r3 (carrying one promoted rule) + 1.10.r3a/b/c/d appended; 1.close → 1.10.r3d; 194 tasks.
Next: this commit integrates as task(1.10.r2d) → 1.29 (the strip at atoms and the deletion leg) → 1.30 (mark edges) → 1.31 (the segment's first and last keystroke times; G9's commit metric) → 1.32 (toggle and real-clipboard cases, the tautology) → 1.verify.r3 → HUMAN_GATE 1.verify.r3h (`scripts/gate.sh 1.verify.r3h`; the principal rotates there) → 1.10.r3a/b/c → PRINCIPAL 1.10.r3d (a fresh session) → 1.close. Handoffs 014, 015 and 016 live in this commit (cherry-picked from handoff/014–016, deviation 29); delete handoff/010–016 after 1.close.
Open human gates: none (next: 1.verify.r3h after 1.32 and 1.verify.r3). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r3 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN (#review-0-r1 deviation 29); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns with the journal stubbed before implementing, the raise to 80 conditional on Michael's OK at the Phase 2 boundary (#review-1-r1, r2-chain counts on the backlog); stale locks are recovered by hand (G4); promote-lessons is manual (one rule via 1.verify.r3); every new package needs its docs/dependencies.json entry (1.20); the `ralph/PROMPT.md` iteration list is aligned to CLAUDE.md by a `fix(runner)` with its conformance test at the Phase 1→2 boundary (H7) and until then every r3 task text names CLAUDE.md's list as the one that stands; the stale `ci/1.verify/a1` and `ci/1.verify.r2/a1` refs on origin are removed by `gate.sh gc` at the phase boundary.
Reviewer record (#review-1-r2): unique findings Claude 5 of 6, Sol 2 of 2, Grok 1 of 2; both blockers unique to a probing reviewer, each a browser reproduction; one shared finding (H7: Claude should-fix, Grok nit); cumulative over fifteen shared findings, seven disagreements, the low side is Grok 5, Claude 2, Sol 2 — still a signal, not a verdict; Grok's PASS with two blockers in the tree is asked about in the 1.10.r3c text; no reviewer change inside a review set; post.1 tabulates.

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [plan.1.verify.r2h.r0] 2026-09-10T16:40:30Z Task: planning commit for the 1.verify.r2h GATE-FAILED (ci.yml run 34499653269 at 6d9345c: windows-latest 3 e2e/web specs red at the Playwright step, all three the Copy Markdown cases of 1.23 in editor-clipboard.spec.ts, each receiving a carriage return before the newline from `navigator.clipboard.readText()`; ubuntu and macos green on everything; artifacts [] because the merge job and the artifact check were never reached). Status: Complete. Files: docs/PRD.md (§8 Phase 1: +1.28 copy cases assert the string handed to `writeText` through an init-script spy, byte-exact, no `readText`, no platform branch, no carriage-return escape, G1's flush-removal mutation re-run against the spy; +1.verify.r2.g1 needsCI repair with refTemplate ci/1.verify.r2.g1/a{n}; 1.10.r2a/b/c → 1.verify.r2.g1 with their accepted-artifact path moved to /logs/ci/1.verify.r2.g1h/accepted/), ralph/tasks.json + ralph/EXPECTED_COUNT (181 → 184, regenerated; the generator makes the reviewers depend on 1.verify.r2.g1h), docs/DECISIONS.md (#021: what happened, cause, scope decision, graph change, model choice, reviewer notes, reversal), docs/lessons.md. Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (184 tasks); bash ralph/test/run.sh run before the commit. Commit: wip(plan.1.verify.r2h.r0). Iterations used: 1. First attempt passed: y. Tool calls: ~24. Reviewer overturned: pending. Notes: the runner integrates this as plan(plan.1.verify.r2h.r0), supersedes 1.verify.r2h, then runs 1.28 and 1.verify.r2.g1; next human gate is scripts/gate.sh 1.verify.r2.g1h; the Windows Playwright log stays the first unknown of that round.
- [1.28] 2026-09-10T16:58:00Z Task: Phase 1 verification-gate repair, test-only (repair after the 1.verify.r2h GATE-FAILED, ci.yml run 34499653269 at 6d9345c: ubuntu-latest and macos-latest green on every unit test and every e2e/web spec; windows-latest green on every unit test and on every e2e/web spec except three, all in `e2e/web/editor-clipboard.spec.ts` › `Copy Markdown flushes a pending source burst before it reads the store`; DECISIONS #021). Status: Complete. **Reproduction, before the change.** `grep -c 'navigator.clipboard.readText' e2e/web/editor-clipboard.spec.ts` was **1** — one occurrence, line 227, the last statement of `clickCopyMarkdown` (`return page.evaluate(() => navigator.clipboard.readText());`), the helper all three named tests call and the only way any of them learned what had been copied; so all three did read the OS clipboard back through it, confirmed by reading the file and not assumed. This container is Linux and cannot run Windows, so the failure itself is quoted from /logs/ci/1.verify.r2h/a1/workflow.log, step `test (windows-latest)` › `Playwright (e2e/web)` (lines 6274–6346; ubuntu at 2608–2612 and macos at 4657–4659 show the same three tests green, which is the other half of the reproduction). All three diffs have the identical shape — `Error: expect(received).toBe(expected) // Object.is equality`, `- Expected  - 1` / `+ Received  + 1`, then the expected word, the received word, a blank line and a lone `↵` (U+21B5, the reporter's rendering of the invisible character the received string carries and the expected one does not) — at (1) `> 241 |     expect(clipboard).toBe("alpha\n");` (`- alpha` / `+ alpha`), (2) `> 255 |     expect(clipboard).toBe("beta\n");` (`- beta` / `+ beta`), (3) `> 267 |     expect(clipboard).toBe("gamma\n");` (`- gamma` / `+ gamma`), each `at D:\a\essaydown\essaydown\e2e\web\editor-clipboard.spec.ts`. The principal's reading is consistent with the log in every detail the log can show: the three failures are exactly the three tests that go through `clickCopyMarkdown`, every other `editor-clipboard.spec.ts` case — including the foreign plain-text paste, which also touches the real clipboard but *writes* it rather than reading back what the app wrote — is green on windows-latest, the received string differs from the expected one by one invisible character before the line feed, and the app's own bytes are `format(root)`, asserted as `"<word>\n"` by the Markdown-pane poll in two of the three tests on the same runner. The log cannot name the character (the reporter prints it as a glyph), which is why the fix does not depend on its identity: it removes the read-back instead of compensating for it. **Change.** `e2e/web/editor-clipboard.spec.ts` only (+48/-13): `installWriteTextSpy(page)` added (a `page.addInitScript` that, before the page's own scripts run, puts `{ calls: [] }` on `window.__writeText` and replaces `navigator.clipboard.writeText` with a function pushing its argument and resolving), called as the first statement of `openEditor` — before its `page.goto("/dev/editor")`, which is the ordering the init script needs and is why the spy lives in that helper; `clickCopyMarkdown` now clicks, keeps its `expect(page.getByTestId("status")).toHaveText("Copied Markdown")` assertion (so the app's real path flush → `format` → `writeText` → status is still the thing exercised, and `copyMarkdown` sets the status only after the write resolves, so a recorded call is there to read by the time it appears), then reads `window.__writeText.calls`, asserts `toHaveLength(1)` and returns the one string; the three tests drop their `context` fixture and their `context.grantPermissions(["clipboard-read", "clipboard-write"])` line, which the spy makes unnecessary, and assert the recorded string byte-exact and unnormalised — `toBe("alpha\n")`, `toBe("beta\n")`, `toBe("gamma\n")`, unchanged from before. The rule is stated once, in the section's doc comment (the one introducing this describe block and its helpers): a copy case asserts the string the app handed `writeText`; a paste of foreign content is the one case that needs the real clipboard, which is why the plain-text case keeps it and its permission grant; the carriage return is named there in words and never appears as an escape in code. `openEditor` is called by these three tests and nothing else, so no other case in the file sees the spy; the foreign-paste case and the `DataTransfer` round-trip cases are untouched. **Acceptance greps, after the change**, on `e2e/web/editor-clipboard.spec.ts`: `navigator.clipboard.readText` **0**; `addInitScript` **1**; `navigator.clipboard.writeText` **4** (the spy's assignment, the foreign-paste case's real write, and two prose mentions); `process.platform` **0**; `\r` (`grep -c '\\r'`) **0**; `grep -cE '\.(skip|only)\(|runIf|skipIf'` **0**. **G1 mutation — still able to fail.** Backed up `apps/desktop/src/dev/DevEditor.tsx` to /tmp/DevEditor.tsx.bak, deleted only the `sourceBinding.current?.flush();` line from `copyMarkdown` (leaving its comment, so the deletion is the one statement), ran `playwright test editor-clipboard.spec.ts -g "Copy Markdown flushes"`: **presence red** ("presence: copying inside the coalescing window includes the just-typed text, with no pause and no toggle" — `Expected: "alpha\n"`, `Received: ""`, i.e. the spy recorded the empty string the un-flushed store still held, which is G1's defect seen through the new assertion rather than through the OS), **absence green** ("absence: copying after the coalescing window has elapsed reads the same committed text") and **rendered-view green** ("the rendered view's copy is unchanged: every keystroke there is already committed, so an immediate copy needs no flush") — exactly the 1-of-3 split the acceptance names. Restored with `cp /tmp/DevEditor.tsx.bak apps/desktop/src/dev/DevEditor.tsx` in the same tool call that made the mutation (lesson [1.25]); `git diff --stat apps/desktop/src/dev/DevEditor.tsx` empty and `git status --short` showing only this task's files, both confirmed before the commit. Files: `e2e/web/editor-clipboard.spec.ts` (+48/-13), `docs/lessons.md` (+1), this entry. `git diff --stat` against the branch base (plan.1.verify.r2h.r0, `6a39a92`) is exactly those three files; zero-line diff over every `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` and `Cargo.lock` — zero new dependencies (PRD §4); no source, config or workflow file touched; no `docs/V1.1-BACKLOG.md` line (no defect found outside this task's scope). Tests: `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (lessons 1.3/1.4/1.12, per-attempt browser cache) then the full `e2e/web` suite **68/68** green, including all three named tests; `pnpm lint` clean (eslint + `check-deps` 61 npm + 4 cargo all allowlisted + `tsc --noEmit` across all 6 typechecked projects); `pnpm test` **2043/2043** across 35 files, coverage 99.63%/97.65%/100%/100%, unchanged from the branch base (no `packages/*/src` or unit test file touched); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, vacuous (Rust surface is Phase 2), unchanged. `npx prettier --check e2e/web/editor-clipboard.spec.ts` clean (`docs/lessons.md` is not prettier-clean and was not before this task either — checked on a stashed tree — and the file is append-only, so no old line was reformatted). Iterations used: 1. First attempt passed: y. Tool calls: ~18. **Honest statement about the OS:** this container is Linux. The spy makes the assertion independent of any OS clipboard convention — it never touches the system clipboard at all in these three cases — but that is an argument, not an observation: the only proof of a Windows pass is the `1.verify.r2.g1h` gate green on ubuntu-latest, macos-latest and windows-latest, which this task cannot run and does not claim. Notes: nothing outstanding from 1.28; next is 1.verify.r2.g1 and then the `scripts/gate.sh 1.verify.r2.g1h` three-OS gate, where the windows-latest Playwright leg is still the first unknown.
- [1.verify.r2.g1] 2026-09-10T16:53:30Z Task: Phase 1 verification for review attempt r2, repair 1 of 1.verify.r2 after the 1.verify.r2h GATE-FAILED — freeze dependencies, run the full suite, confirm the phase head is ready for the three-OS CI gate. Status: Blocked. **This iteration performed no implementation, by design.** Orientation (CLAUDE.md → `/logs/state/summary.md` → this task's `ralph/tasks.json` entry → `docs/lessons.md`) surfaced a real conflict between this attempt's own prompt wrapper (`PROMPT.md`, "one Ralph iteration (task 1.verify.r2.g1, attempt 1)") and `CLAUDE.md`'s `## Every iteration` list. `CLAUDE.md`'s step 2 — added by task 1.verify.r2 itself, integrated at `6d9345c`, and confirmed still in force by `docs/progress.md`'s Current-state line ("the journal stubbed before implementing from 1.verify.r2 on") — requires appending a stub journal entry and committing it as `wip(<id>): journal stub` *before* any implementation, specifically so a turn-capped attempt leaves a checkpoint (RUNNER-SPEC §4.3), then completing that same stub in place later (never a second entry). `PROMPT.md`'s `## Iteration` list instead has step 2 as "Implement the task" directly, step 4 as a single "Append one journal entry" written after implementation, and step 6 as one `git add -A && git commit -m "wip(1.verify.r2.g1): <what this iteration did>"` — no stub, no pre-implementation commit, one commit per iteration instead of two. These are not reconcilable by silently picking a superset: `PROMPT.md`'s own numbered step 2 is "Implement", not "stub, commit, then implement", so following CLAUDE.md's newer procedure means deviating from PROMPT.md's steps as literally written, and CLAUDE.md is explicit at the top of the file: "These rules outrank task text: if a task conflicts with them, stop, write the conflict to the journal, and do not decide." Per that instruction, this iteration stopped here instead of choosing a procedure. `git status` was clean at start (no recovery commit needed). No source, test, config or dependency file touched; this entry and the paired `docs/lessons.md` line are the only diff. Tests: not run — the task's verification work (freeze dependencies, `pnpm lint`/`pnpm test`/`cargo test`/`e2e/web`, `ralph/check-agent-rules.sh`) was not attempted this iteration. Iterations used: 1. First attempt passed: n. Tool calls: ~12. Notes: the next iteration (or the principal, at a `PRINCIPAL`/`REPLAN`-shaped stop) needs to resolve which iteration procedure governs this task — most likely `PROMPT.md`'s generator needs to pick up the journal-stub procedure change that task 1.verify.r2 itself wrote into `CLAUDE.md`/`AGENTS.md`, so that a task created after that integration is handed a wrapper that agrees with it; until that's resolved, the actual verification (dependency freeze, full suite, `check-agent-rules.sh`) for `1.verify.r2.g1` is still outstanding.
- [1.verify.r2.g1] 2026-09-10T16:56:08Z Task: Phase 1 verification for review attempt r2, repair 1 of 1.verify.r2 after the 1.verify.r2h GATE-FAILED (RUNNER-SPEC §2 rewrite rule (a), §3, §5.5; DECISIONS #021): freeze dependencies (no lockfile change allowed), run the full suite in the container, and confirm the phase head is ready for the three-OS CI run that the CI gate performs. Status: Complete. **This is attempt 2.** Attempt 1 (previous journal entry, same id, 2026-09-10T16:53:30Z) stopped with no implementation, having found this attempt's `PROMPT.md` wrapper ("Every iteration" steps: implement, then one post-hoc journal entry, then one commit) contradicting `CLAUDE.md`'s current `## Every iteration` procedure (steps: stub-journal-first, implement, complete-the-stub — the procedure task 1.verify.r2 itself wrote into `CLAUDE.md`/`AGENTS.md`, integrated at `6d9345c`) and, per `CLAUDE.md`'s own top-of-file rule, stopped rather than choose. This attempt re-read both files: the mismatch is unchanged (`PROMPT.md`'s step 2 is still literally "Implement the task", `CLAUDE.md`'s step 2 is still the pre-implementation stub). It did not repeat attempt 1's stop, for two reasons read from the documents themselves rather than decided fresh: (1) this task's own `ralph/tasks.json` description states as a fact, not a question, that "the CLAUDE.md/AGENTS.md procedure change carried by 1.verify.r2 are already integrated at 6d9345c" — i.e. the task text this attempt was handed already presumes the stub-first procedure is the one in force; (2) `CLAUDE.md`'s own opening line is itself the resolution rule, not a reason to stop short of one: "These rules outrank task text." `PROMPT.md` is a generated per-task wrapper, not the task's substance (`ralph/tasks.json`'s description/acceptance, which is what CLAUDE.md's conflict clause is written to protect — scope, dependencies, non-negotiables); the mismatch is mechanical (commit granularity and checkpoint timing), not a request to violate a non-negotiable, so applying CLAUDE.md's already-stated precedence is following the rules, not silently picking between them. This attempt therefore appended the stub (committed `wip(1.verify.r2.g1): journal stub`, 8457b7a) before doing any implementation, exactly as CLAUDE.md's current step 2 requires, then did the verification work, then is completing that same stub in place now (this edit), never a second entry — CLAUDE.md's step 4. Files this iteration: `docs/progress/journal-main.md` (the stub commit, now this completion), `docs/lessons.md` (this entry's paired lesson). No source, fixture, manifest or lockfile file touched — `git diff --stat` against the branch base (1.28, `c794f27`) is empty except for `docs/progress/journal-main.md` and `docs/lessons.md`; confirmed no `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `**/Cargo.toml` or `Cargo.lock` change anywhere in the tree — zero new dependencies (PRD §4), dependencies frozen as the task requires. **Verification, this container:** `pnpm lint` clean (eslint + `check-deps.mjs`: 61 npm + 4 cargo, all allowlisted + `tsc --noEmit` across all 6 workspace projects); `pnpm test` **2043/2043** across 35 files, coverage 99.63%/97.65%/100%/100%, unchanged from 1.28 (no `packages/*/src` or `test/` file touched); `cargo test` 0/0/0 across `desktop_lib`, `desktop` and doc-tests, vacuous (Rust surface is Phase 2), unchanged. `pnpm --filter @essaydown/e2e-web exec playwright install chromium` (per lesson 1.3/1.4/1.12, per-attempt browser cache) then the full `e2e/web` suite **68/68** green, including all three of 1.28's spy-based Copy Markdown cases and 1.24's two Undo/Redo reproduction cases. `bash ralph/check-agent-rules.sh` → "OK (CLAUDE.md ≡ AGENTS.md below line 1)", exit 0. `git status --short` clean before this entry's own edit. Honest statement about the OS, unchanged from 1.28: this container is Linux; the phase head (1.28) is ready for the three-OS `1.verify.r2.g1h` CI gate, but only that gate — not this task — can prove the windows-latest and macos-latest legs, including the Copy Markdown spy's behaviour on Windows, which is the first unknown of this round. Iterations used: 2. First attempt passed: n (attempt 1 stopped on the PROMPT.md/CLAUDE.md conflict, no implementation, no tests run). Tool calls: ~12 (attempt 1) + ~20 (this attempt). Notes: nothing outstanding from the verification itself; the runner's per-task `PROMPT.md` generation still has not picked up the stub-first procedure task 1.verify.r2 wrote into `CLAUDE.md`/`AGENTS.md` (lesson [1.verify.r2.g1] from attempt 1, unresolved), so a later task in this same chain may hit the identical mismatch — this attempt's resolution (CLAUDE.md's own stated precedence over a generated wrapper, applied because the mismatch is mechanical rather than a non-negotiable conflict) is offered as the precedent, not a fix to the generator itself; next is the `scripts/gate.sh 1.verify.r2.g1h` three-OS gate.
- [1.10.r2d] 2026-09-11T03:26:49Z Task: reconciliation of Phase 1 review attempt r2 at c9408ee (verifier 1.verify.r2.g1, gate 1.verify.r2.g1h ACCEPT a1, run 34509206442). Claude FAIL (1 blocker/3 should-fix/2 nits), Sol FAIL (1/0/1), Grok PASS (0/0/2). Verdict FAIL: a `hard_break` left as a block's last node serialises to a bare trailing `\` that does not round-trip (Claude, three Backspaces from `hard-break.md`'s shape; the F1/G4 family, an atom this time), a flush inside a source burst that continues past one coalescing window splits undo history (Sol, `a`, Copy, `bcdef` at 300 ms, Undo leaves `a`), the r1 backlog deferral of the emphasis-edge `&#x20;` member rested on a false reachability claim (Sol: caret after `b` in `a *b* c`, one space), the toggle's flush has no test that fails without it, no test exercises the real clipboard write after 1.28, and one tautology survived into a third file. Status: Complete. Files: docs/reviews/phase-1-r2-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#review-1-r2: verdict FAIL first line, 9 findings H1–H9 with severity and disposition, each r1 disposition G1–G12 confirmed or reopened — G2 → H2, G4 → H1/H5/H6, G1's attestation → H4, F1's emphasis-edge deferral → H8 — the reviewer record counted from the reports: unique findings Claude 5 of 6, Sol 2 of 2, Grok 1 of 2; one shared finding, H7, Grok low; cumulative over fifteen shared findings, seven disagreements, low-rater instances Grok 5, Claude 2, Sol 2; the `ralph/PROMPT.md` drift disposition (a boundary `fix(runner)` with a conformance test, never on `phase/1` mid-flight, the reading that stands named in every r3 task text); the turn-budget note with counts read from `.evidence/state/tasks.json` and the transcripts (three second attempts this chain: 1.23 and 1.25 capped before the stub rule, 1.verify.r2.g1 by choice; zero caps under the stub rule in two data points; the 80-turn proposal conditional, unchanged); the cherry-pick record (handoff/014, /015, /016 in order; `next-prompt.md` resolved to the incoming version twice, byte-identical to handoff/016's); graph change; promote-lessons; reversal), docs/PRD.md (§8 Phase 1: +1.29–1.32 fix tasks, all opus, +1.verify.r3 needsCI carrying one promoted rule, +1.10.r3a/b/c/d, 1.close → 1.10.r3d), ralph/tasks.json + ralph/EXPECTED_COUNT (184 → 194, regenerated: 150 raw + 21 review-set + 23 CI gates), docs/V1.1-BACKLOG.md (+4 lines: the H8 correction, the H7 boundary chore with its hard stop, the image-of-parse assertion for Phase 2 planning, the r2-chain turn counts), docs/lessons.md (+8 class-level), docs/handoffs/014-*.md + 015-*.md + 016-*.md + next-prompt.md (cherry-picked), docs/progress.md (regenerated). Tests: node ralph/generate-tasks.mjs --check and node ralph/validate-tasks.mjs green (194 expanded, byte-identical to PRD §8, acyclic); bash ralph/test/run.sh green (exit 0; counts in the commit body). Commit: wip(1.10.r2d). Iterations used: 1. First attempt passed: y. Tool calls: ~45. Reviewer overturned: pending (r3). Notes: no code change here, by design (RUNNER-SPEC §8.1 allowlist); the four fix tasks carry the code; 1.13/1.25 (the strip) and 1.17/1.24 (the deferred commit) are passed and are not edited — H1/H5/H6/H8 and H2 are new tasks on their seams; H7 is not a fix task (a runner file, fixed at the Phase 1→2 boundary by `fix(runner)`; each r3 task text says CLAUDE.md's stub-first list stands over the stale wrapper, which is `1.verify.r2.g1`'s second lessons line); G9's trigger fires in 1.31 (the commit metric line); this session was autonomous, so no decision in #review-1-r2 is Michael's and the conditional items (turn budget, `gate.sh gc`, the `phase/1` push, `git branch -D handoff/010…016`) wait for him at the boundary; lessons tagged 1.*/plan.1.* in docs/lessons.md now number 92.

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.23 | 2 | ~22 (a1) + ~12 (a2) | n (a1 `error_max_turns` at 51, exit 1, journal present — no stop signal) | n (its three copy cases read the OS clipboard: the windows CRLF gate failure, #021, repaired by 1.28) |
| 1.24 | 1 | ~30 | y | y (H2: the pending segment keeps one keystroke time, so a flush splits a burst longer than the window) |
| 1.25 | 2 | n/a (the fields are absent from its entries) | n (NO-JOURNAL at a1, `error_max_turns` at 51) | y (H1/H5: the atom itself at a block end; H6: a cell's line ending) |
| 1.26 | 1 | ~24 | y | n |
| 1.27 | 1 | ~20 | y | n (H9: one tautology, nit, taken in 1.32) |
| 1.verify.r2 | 1 | ~14 | y | partial (H7: the wrapper `ralph/PROMPT.md` not carried along — a runner file, fixed at the boundary) |
| 1.28 | 1 | ~18 | y | partial (H4: the spy removed the suite's last real clipboard write — #021's scope decision) |
| 1.verify.r2.g1 | 2 | ~12 (a1) + ~20 (a2) | n (a1 exit 0 at `num_turns` 11, stopped by choice on H7; no stop signal) | n |
| 1.10.r2a (Claude) / r2b (Sol) / r2c (Grok) | 1 / 1 / 1 | — | y / y / y | — |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).

Phase 1 review r2: findings as reconciled — 2 blockers (in tasks 1.29 and 1.31), 3 should-fix required (in tasks 1.30 and 1.32), 1 should-fix taken as a runner chore at the phase boundary (H7, `fix(runner)` with a conformance test), 3 nits (2 taken in task 1.29, 1 in task 1.32); r1 dispositions reopened: 3 of 12 as siblings or families (G1's attestation → H4, G2 → H2, G4 → H1/H5/H6), 9 confirmed; the r1 backlog deferral of F1's emphasis-edge member reopened (H8, its reachability premise false). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 92 (docs/lessons.md lines tagged 1.*, plan.1.*; 84 before this commit + 8). Lessons promoted to CLAUDE.md/AGENTS.md: 5 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2) + 1 pending (via 1.verify.r3). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 10 (the nine of r1 plus 1.25 at a1); other second attempts: 1.23 (capped, journal present) and 1.verify.r2.g1 (stopped by choice), neither a stop signal. Gate reruns: 0. Gate repairs: 2 (1.verify.g1, 1.verify.r2.g1). Coverage at r2 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r1: 0, 0, 0, 0 (1.26–1.28 and the verifiers touched no `packages/*/src`; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.2 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 20.6 / 22.7 / 11.9 ms on the ubuntu / windows / macos runners of the accepted gate. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.28: 14 of 26 (10 of 20 before the r2 chain, 4 of 6 in it). By the transcript measure the two capped first attempts this chain ended at 50 and 56 tool uses and the five that finished first at 28–59 — the r1 reading (the cap sits on the median of the work) stands; only 1.28 and 1.verify.r2.g1 ran under the journal-stub rule, with no cap and no NO-JOURNAL between them.
