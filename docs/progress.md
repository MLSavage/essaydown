# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 1 (rendered editor, source toggle, Outline prototype), branch phase/1. Implementation 1.1–1.8 passed; 1.9 observation gate ACCEPT a2 (#repair-1.9); 1.verify → gate 1.verifyh GATE-FAILED (#020) → 1.11 + 1.12 + 1.verify.g1 → gate 1.verify.g1h ACCEPT → review r0 FAIL (#review-1-r0) → fix chain 1.13–1.22 + 1.verify.r1 → gate 1.verify.r1h ACCEPT → review r1 FAIL (#review-1-r1) → fix chain 1.23–1.27 + 1.verify.r2 → gate 1.verify.r2h GATE-FAILED (#021) → 1.28 + 1.verify.r2.g1 → gate 1.verify.r2.g1h ACCEPT → review r2 FAIL (#review-1-r2) → fix chain 1.29–1.32 + 1.verify.r3 → gate 1.verify.r3h GATE-FAILED (#022) → 1.33 + 1.verify.r3.g1 → gate 1.verify.r3.g1h ACCEPT a2 (#023) → review r3 FAIL (#review-1-r3) → fix chain 1.34–1.38 + 1.verify.r4 → gate 1.verify.r4h GATE-FAILED (#024) → 1.39 + 1.verify.r4.g1 → gate 1.verify.r4.g1h ACCEPT a1 → review r4 FAIL (#review-1-r4; Grok derived, #025) → fix chain 1.40–1.44 (four of five to STUCK with the work done, each recovered by a `retry` with a lesson; #026–#028) + 1.verify.r5 → gate 1.verify.r5h ACCEPT a1 → review r5 (three independent reviewers, Grok on the usage reset, #027) → this reconciliation. → fix chain 1.45–1.48 + 1.verify.r6 → gate 1.verify.r6h ACCEPT a2 after a transient rerun (#029) → review r6 FAIL (#review-1-r6) → fix chain 1.49–1.54 + 1.verify.r7 (this commit).
Review attempt r6 at c2f6526: Claude FAIL (1/7/2), Sol FAIL (1/2/0), Grok FAIL (1/0/0) → reconciliation verdict FAIL (DECISIONS #review-1-r6): 2 blocker families — an astral character that is the closing neighbour of one flanking run and the opening neighbour of the next is written as two lone-surrogate references (`container-phrasing.js`'s two edge encodings both fire on the one-character child; 1.45's `SPLIT_PAIR` names a reference beside a raw mate only; Sol and Grok, reproduced headlessly for nine mark pairs × three astral characters and five position classes) and an autolink literal whose URL holds a backslash doubles it on every `parse ∘ format` (the text child passes through `safe()` while nothing inside `<…>` decodes; Claude) —, 2 should-fix required (the position map misses the html-before-soft-break rewrite, Claude and Sol; `inlineCode` has no spelling table, Claude), 4 should-fix taken (a cell's end maps to the row's start; the run-end boundary side disagrees with `$pos.marks()`; the blind counted `press` helpers — the r6h a1 rerun's instrument, Sol Required; the corpus callers' text instrument with 1.47's clause recorded), 1 should-fix deferred with a trigger (URL-shaped text), 2 nits (1 backlog — adjacent attention runs, 1 recorded — 1.47's spec count); K1 and K2 reopened for the two-run join and the third branch, J3 reopened for two node classes, the rest confirmed.
Next: this commit integrates as task(1.10.r6d) → 1.49 (the third `SPLIT_PAIR` form, pair guards, the `astral-between-runs.md` fixture, Sol's browser route) → 1.50 (the per-node autolink resource fallback, guards from `safe()`'s branches, the `autolink-backslash.md` fixture) → 1.51 (the html-rewrite candidate and the `inlineCode` spelling table, the `inline-html.md` fixture, Sol's toggle route) → 1.52 (the cell-end rule, the corpus inverse leg over every text position of every fixture, two Playwright cases) → 1.53 (the boundary side by `ResolvedPos.marks()`) → 1.54 (the per-step caret poll in every counted-press helper; the whitespace-removed text equality in every `expectNoForbiddenEntity` caller) → 1.verify.r7 → HUMAN_GATE 1.verify.r7h (`scripts/gate.sh 1.verify.r7h`; the principal rotates there) → 1.10.r7a/b/c → PRINCIPAL 1.10.r7d (a fresh session) → 1.close on PASS.
Open human gates: none (next: 1.verify.r7h after 1.54 and 1.verify.r7). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (#review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17; none is in the r7 chain); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN, stacked on the newest unmerged one (#review-0-r1 deviation 29, handoff 026); reviewers run with RALPH_REVIEW_MAX_TURNS 160; loop tasks at 50 turns with the journal stubbed first by `printf` and the suite run once (the 80-turn raise conditional on Michael's OK at the Phase 2 boundary); after a `gate.sh rerun` that accepts, never run the `--resume` doctor names — #023's abandon once per rerun (two instances, #029); before any `retry`, the capped logs moved aside and a principal lesson line that orients without widening scope (#027, #028); a 429 in a transcript is the harness's stop, not the agent's — wait for the reset Michael names; before any single-reviewer retry, #025's procedure, and a Grok 402 is never retried without Michael.
Reviewer record (#review-1-r6): three independent reports; unique findings Claude 8 of 10, Sol 0 of 3, Grok 0 of 1; three shared findings all agreed by class (the two-run blocker Sol–Grok, Claude absent; the html rewrite and the counted press Claude–Sol, the Required flag differing under the J3 precedent); two blocker families, one shared and one unique to Claude; cumulative over twenty-five shared findings, eight disagreements, the low side is Grok 6, Claude 2, Sol 2 — unchanged, still a signal, not a verdict; Grok's PASS-with-blockers count stays 2 of 4 counted attempts; no change inside a review set; the r7 rows are written for three reviewers, a 402 during r7 is Michael's call.

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [1.47] 2026-09-12T20:27:14Z Task: Phase 1 review fix, test-only (DECISIONS #review-1-r5 K1 corpus half and K4): the astral-neighbour.md fixture, the typing-legs.ts code-point rule and the mark-neighbour legs' text-equality assertion, attempt 3 (attempt 2's work recovered uncommitted, journal never completed). Status: In progress.
- [1.47] 2026-09-12T20:34:07Z Task: Phase 1 review fix, test-only (DECISIONS #review-1-r5 K1 corpus half and K4): the astral-neighbour.md fixture, the typing-legs.ts code-point rule and the mark-neighbour legs' text-equality assertion, attempt 4 (retry after three capped attempts; work already complete on branch per docs/lessons.md). Status: Complete. `git status` clean at start (base: task 1.46, `5163835`), no recovery commit; stub committed first (`wip(1.47): journal stub`, this attempt's own line — the two attempts above are earlier attempts' and were not edited). Attempts 1–3 capped at 51 turns each without printing DONE (`NO-JOURNAL` twice, one journal line left `In progress`), but their combined work is on this branch, verified by me this attempt rather than assumed from the two principal lesson lines above (2026-09-12T20:13:35Z, 2026-09-12T20:31:51Z): those lines gave orientation and a procedure only; every acceptance sentence below was checked directly in this container. **(1)** `fixtures/markdown/astral-neighbour.md` and its `.canonical.md` sibling, byte-identical (three paragraphs: `~~a.~~&#x1F600;` — closing-side symbol neighbour; `x &#x10400;*(a)*` — opening-side letter neighbour, `x ` first per the letter-leg's model limit; `**a.**&#x1D552; 😀` — closing-side letter neighbour then a raw astral in plain text), with its `index.json` entry (`nodeTypes` delete/emphasis/paragraph/root/strong/text, `blockCount` 3). **(2)** `typing-legs.ts`: `typeInsideEveryBlock` types after the first *code point* (`([...text][0] as string).length`, not `first + 1`); `grep -c 'first + 1' packages/editor/test/typing-legs.ts` is 0; the rule stated once in the file's doc comment (line 40). `editor-fixed-point.test.ts`'s astral-first guard (`a paragraph whose first character is an astral one gets the letter after both of its surrogate units`, line 1381) is green; its mutation (`first + 1`) was recorded red in attempt 1's transcript (`/logs/tasks/1.47/1.a1-capped-nostub.log`, `😀` split, U+FFFD in the output) — not re-run this attempt, per the two principal lines above, since the code has not changed since. **(3)** The K4 text-equality assertion (`expectTextMatchesEditor`, comparing `inlineBlockTexts(parse(out))` to `editorBlockTexts(doc)`) is present in both mark's-neighbour legs' guards (`deleteBesideEveryMarkedRun`'s and `punctuateThenDeleteAfterEveryMarkedRun`'s `it` blocks) and green over every fixture; the reach test (`the corpus fixture set whose text is compared by every leg above includes an astral neighbour shape`) names `astral-neighbour.md` directly (`expect(names).toContain("astral-neighbour.md")`). Its able-to-fail was run in attempt 3 (not committed): `installSurrogateWidening`'s early-return disabled in `packages/core/src/format.ts` (backed up to `/tmp/format.ts.bak` first), `editor-fixed-point.test.ts` re-run, exactly the two `astral-neighbour.md` mark's-neighbour cases went red (`AssertionError: expected '~~a.~~��\n...' to be '~~a.~~&#xD83D;�\n...'`, 2 failed | 570 skipped), then `packages/core/src/format.ts` restored from the backup (`git status --short packages/core/src/format.ts` and `git diff --stat` clean afterward, confirmed again by me this attempt: `git diff --stat 5163835 HEAD` today touches no file under `packages/core/src`). The task description's clause "and in `expectNoForbiddenEntity`'s corpus callers" was not added beyond the two named legs — attempt 3's reading, confirmed here: the acceptance sentence names "both neighbour legs" only, and the space-typing legs' editor text holds whitespace the strip drops, so a per-block equality there would be a new, unscoped assertion. **(4)** `docs/V1.1-BACKLOG.md`: the `[1.47, letter leg's model]` line, present with a trigger and hard stop `2.verify`. **Verification, this container (Linux; no other OS claimed), the suite run once by me this attempt:** `pnpm lint` exit 0 (eslint, `check-deps` 61 npm + 4 cargo direct dependencies all allowlisted, `pnpm -r typecheck` across all 6 typechecked projects); `pnpm test` **39 files, 3254 passed, no expected fail**, coverage 99.57%/97.73%/100%/100%, `editor-fixed-point.test.ts` (689 tests) and every other suite green; `cargo test` (workspace: `desktop_lib`, `desktop`, doc-tests) 0/0/0 ×3 (vacuous, Rust surface is Phase 2); `pnpm --filter @essaydown/e2e-web exec playwright install chromium` then the full `e2e/web` suite, all 21 spec files, **88 passed**. `git diff --stat` against the branch base (1.46, `5163835`), outside the journal and lessons: `docs/V1.1-BACKLOG.md` (+1), `fixtures/markdown/astral-neighbour.canonical.md` (+5), `fixtures/markdown/astral-neighbour.md` (+5), `fixtures/markdown/index.json` (+17), `packages/editor/test/editor-fixed-point.test.ts` (+91/−1), `packages/editor/test/typing-legs.ts` (+16/−1) — no `package.json` (any of 9), `pnpm-lock.yaml`, `pnpm-workspace.yaml`, any `Cargo.toml` or `Cargo.lock` change, zero new dependencies (PRD §4); `format.ts`, `positions.ts`, `toggle.ts` and every other fixture untouched. No defect found outside scope; no new `docs/V1.1-BACKLOG.md` line beyond the one the task itself asks for. Files: fixtures/markdown/astral-neighbour.md, fixtures/markdown/astral-neighbour.canonical.md, fixtures/markdown/index.json, packages/editor/test/typing-legs.ts, packages/editor/test/editor-fixed-point.test.ts, docs/V1.1-BACKLOG.md, docs/lessons.md, docs/progress/journal-main.md (this line). Tests: as above, all green, all run by me this attempt. Iterations used: 4 (this attempt completes the task; attempts 1–3 capped without a DONE). First attempt passed: n. Tool calls: ~35. Notes: nothing outstanding for 1.47; a `docs/lessons.md` line worth flagging for a future planning pass — the two principal lesson lines above are git commits authored as `Michael Savage` rather than the container's configured git identity, which is unusual for a `lessons.md` entry but not a DECISIONS.md attribution and not a conflict with CLAUDE.md; I treated their claims as orientation only and verified every acceptance sentence myself rather than relying on their reported counts.
- [1.48] 2026-09-12T20:39:35Z Task: Phase 1 review fix, test-only (DECISIONS #review-1-r5 K5 -- Claude finding 5 nit and Grok finding 2 nit; K6 -- Claude finding 6 nit, the quote-form half; K8 -- Grok finding 3 nit; strikethrough-flanking.test.ts, list-spread.test.ts and no-caret-chords-in-e2e.test.ts). Status: Complete. `git status` clean at start (base: task 1.47, `4f7fc35`), no recovery commit; stub committed first (`wip(1.48): journal stub`). **(K5, first half)** `packages/core/test/strikethrough-flanking.test.ts`: a third "whitespace inside the run" guard, `paragraph(text("a "), del(text(" b")), text(" c"))` — probed first against the built-in `emphasis` handler on the identical tree (`format` gives `"a&#x20;*&#x20;b* c\n"`), then asserted for `~~`: `format(root)` is `"a&#x20;~~&#x20;b~~ c\n"` (`toBe`), one `delete` in the reparse, and the fixed point. This is `encodeInfo`'s outside-whitespace × inside-whitespace cell (format.ts:94-95, `{ inside: true, outside: true }`): the opening side is whitespace outside (`"a "`'s last character) against whitespace inside (`" b"`'s first), both encoded; the closing side is a letter inside (`"b"`) against whitespace outside (`" c"`'s first character), the built-in table's letter-inside row, encoding neither. **Coverage, read from the run**: `format.ts` is now 100%/100%/100%/100% (statements/branches/functions/lines), no uncovered line, where before this guard line 94's `{ inside: true, outside: true }` branch (reached only when `outsideKind === "whitespace"`) was the file's one gap — the two existing guards hit the `outsideKind === "other"` and the final `insideKind === "whitespace"` branches only. **(K5, second half)** `packages/editor/test/list-spread.test.ts`: one `KINDS` entry, `"ATX heading, depth 3, a line ending in its text"` (`{ type: "heading", depth: 3, children: [text("h\nx")] }`), grows the pairwise `confirmation 1` sweep by one row and one column from the loop (`Object.keys(KINDS)`, never a literal count) — `schema.ts:711`'s `if (node.depth > 2) return false` in `isSetextHeading` was previously unexecuted with a line ending present (only depth-1 and depth-2 setext `KINDS` entries existed). One named guard, `"clause (setext heading), depth clause: …"`: a tight item `[paragraph("a"), heading(depth 3, "h\nx"), list([item(false, paragraph("i"))])]` — probed first with a scratch vitest file (deleted after, `git status --short` clean) to read the actual bytes rather than assume them — gives `format(through(root))` = `"- a\n  ### h&#xA;x\n  - i\n"` (`toBe`) and the fixed point: unlike the depth-1 twin (`"clause (setext heading), present"`, which asserts the blank line before the heading and before the nested list), the depth-3 heading is not setext (`needsBlankLine`'s `setext` flag false), so there is no blank line before the heading (`paragraph` → non-setext `heading` matches none of `needsBlankLine`'s paragraph-side clauses) nor before the nested list after it (`heading` matches none of `needsBlankLine`'s left-side clauses, falls to the final `return false`). **(K6, quote-form half)** `tests/no-caret-chords-in-e2e.test.ts`: `CHORD` and `BARE_PRESS` widened from the double-quote-only form to `/(['"\`])(?:[A-Za-z]+\+)+(Home|End)\1/g` and `/(['"\`])(Home|End)\1/g` — a capture group for the opening quote and a backreference for the closing one, so `'Shift+End'` and a backtick-quoted `Shift+End` match but a mismatched pair would not (not itself asserted, out of the task's four named cases). Doc comment above both regexes reworded to name the three quote forms and the backreference. Two new sample-based describe blocks: `CHORD`'s `matching` array gained `'Shift+End'` and `` `Shift+End` `` (both asserted count 1) alongside the four existing double-quoted samples and the two non-matching (`"Home"`, `"ArrowLeft"`, count 0); a new `BARE_PRESS` describe block with `matching` = `"Home"`, `"End"`, `'Home'`, `` `End` `` (count 1 each) and `nonMatching` = `"ArrowLeft"` (count 0) — `BARE_PRESS` had no sample-based unit test before this task, only its use inside the per-file allowlist sweep. Allowlist counts unchanged, read from the tree: `editor-cursor.spec.ts` 1, `editor-toggle.spec.ts` 2 (confirmed no file under `e2e/web/` holds a single-quote or backtick Home/End before this task: `grep -rnE "'(Home|End)'|\`(Home|End)\`"` and the chord equivalent both empty). Ability to fail re-recorded, not assumed: a scratch spec `e2e/web/zzscratch-chord.spec.ts` with one `page.keyboard.press('Shift+End')` line (single-quoted) turned "zzscratch-chord.spec.ts has no modifier chord ending in Home or End" red (`expected 1 to be +0`) under the widened `CHORD`; deleted in the same tool call, `git status --short` clean afterward, never committed. **(K8)** The per-file `it` title in the "no e2e/web file places the caret with a modifier chord on Home or End" describe retitled from `` `${file} has no ControlOrMeta, Control or Meta chord with Home or End` `` to `` `${file} has no modifier chord ending in Home or End` ``, matching the class `CHORD` now matches; `grep -c 'ControlOrMeta, Control or Meta' tests/no-caret-chords-in-e2e.test.ts` = 0. The file's doc comment (lines 6-25) and the other three describe blocks' titles checked against the widened regex: none named `ControlOrMeta`/`Control`/`Meta` specifically as the exhaustive set (the top doc comment already spoke of "a modifier chord" generically), so no further rewording was needed there. The identifier-argument form of a caret-chord press (`press(key)` with the name held in a `const`) is not taken, per the `[review-1-r5 K6]` backlog line's disposition — unchanged this task. No defect found outside scope; no `docs/V1.1-BACKLOG.md` line added. **Verification, this container (Linux; no other OS claimed).** `pnpm install --frozen-lockfile` (fresh worktree, 760-entry lockfile, supply-chain policy passed), `git status --short pnpm-lock.yaml` empty after. `pnpm lint` exit 0 (eslint, `check-deps` 61 npm + 4 cargo direct dependencies all allowlisted, `pnpm -r typecheck` across all 6 typechecked projects). `pnpm test` **39 files, 3298 passed, no expected fail**; `format.ts` 100%/100%/100%/100% (statements/branches/functions/lines), no uncovered line — the sole target of K5's first half; overall coverage 99.68%/97.9%/100%/100%, the remaining uncovered lines (`positions.ts` 194/515, `sentences.ts`, `sidecar.ts`, `schema.ts` 722-723 — an unrelated pre-existing `image.alt ?? ""` nullish-coalescing gap, not `isSetextHeading`'s depth clause — `toggle.ts`) all pre-existing and outside this task's three-file scope. `cargo test` 0/0/0 ×3 (Phase 2 surface, vacuous). `pnpm --filter @essaydown/e2e-web exec playwright install chromium` then the full `e2e/web` suite, all spec files, **88 passed**. `git diff --stat 4f7fc35 HEAD` outside the journal touches exactly `packages/core/test/strikethrough-flanking.test.ts` (+15), `packages/editor/test/list-spread.test.ts` (+16), `tests/no-caret-chords-in-e2e.test.ts` (+45/−10); no `pnpm-lock.yaml`, `Cargo.lock` or `package.json` (any of 9) change, zero new dependencies (PRD §4); no fixture, no source file (`format.ts`, `schema.ts`), no e2e, no config or workflow file touched. Files: packages/core/test/strikethrough-flanking.test.ts, packages/editor/test/list-spread.test.ts, tests/no-caret-chords-in-e2e.test.ts, docs/lessons.md (+1), docs/progress/journal-main.md (this line). Tests: as above, all green, all run by me this attempt. Iterations used: 1. First attempt passed: y. Tool calls: ~30. Notes: nothing outstanding for 1.48; the `schema.ts:722-723` image-alt nullish-coalescing gap and the K6 identifier-argument form are both out of this task's scope and already covered by an existing backlog line (K6) or are pre-existing (the image-alt gap, not raised by any reviewer finding).
- [1.verify.r6] 2026-09-12T20:46:46Z Task: Phase 1 verification for review attempt r6 (freeze dependencies, run full suite, confirm phase head ready for three-OS CI, promote-lessons for the r6 chain). Status: Complete. `git status` clean at start (base: task 1.48, `749a51e`), no recovery commit; stub committed first (`wip(1.verify.r6): journal stub`). **Promote-lessons, the three candidates DECISIONS #review-1-r5 weighed for this task.** **(a) Promoted** verbatim as a `## Code rules` bullet in both CLAUDE.md and AGENTS.md: the character-classification/astral-enumeration rule (DECISIONS #review-1-r5 K1). Discharged by 1.45's `describe("astral neighbours (task 1.45, K1): a non-BMP character outside a flanking mark is encoded as one scalar, never as a lone surrogate", …)` in `packages/core/test/strikethrough-flanking.test.ts` (mark × side × inner-class × neighbour, plus the absence case and the parser-vs-raw decision case) and 1.47's `fixtures/markdown/astral-neighbour.md` entry in `fixtures/markdown/index.json`. **(b) Promoted** verbatim as the next bullet: the position-map-instruments-rewrites rule (DECISIONS #review-1-r5 K2). Discharged by 1.46's `it.each(names)("%s: maps every node of the tree, and nothing is unresolved", …)` in `packages/core/test/positions-corpus.test.ts` (the pin gone — `grep -rn 'OUTSIDE_ENCODED_NEIGHBOUR\|it\.fails' packages tests` = 0, confirmed this attempt) and its deletion-leg clause, `` it(`${name}: the cursor map over every text node the deletion beside every marked run left against a flanking mark (task 1.46)`, …) `` in `packages/editor/test/position-map-editor-leg.test.ts`. **(c) Recorded, not promoted:** Grok's r5 lesson (a read-only review's first two reads are the handler walk and the pin's neighbouring branch) addresses reviewers and the r6 review-set rows carry it; Claude's and Sol's r5 lessons (mid-chain decisions outside the tree; dispositioning a pinned defect by its user-visible consequence) address the principal and land in `docs/PRINCIPAL.md` at the boundary — this task adds no bullet for (c), per DECISIONS #review-1-r5's own disposition. `ralph/check-agent-rules.sh` → `check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`; `tests/agent-rules.test.ts` passes (1 test, part of the suite run below). **Dependency freeze:** `git diff --stat 749a51e -- . ':!docs/progress/journal-main.md' ':!docs/lessons.md'` touches exactly `AGENTS.md` (+2) and `CLAUDE.md` (+2) — no `package.json` (any of 9), `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `Cargo.toml` or `Cargo.lock` change; `pnpm lint`'s own install reported "Lockfile is up to date, resolution step is skipped" and `git status --short pnpm-lock.yaml` empty after. **Verification, this container (Linux; no other OS claimed), the suite run once this attempt:** `pnpm lint` exit 0 (eslint, `check-deps` 61 npm + 4 cargo direct dependencies all allowlisted, `pnpm -r typecheck` across all 6 typechecked projects). `pnpm test` **39 files, 3298 passed, no expected fail** (coverage 99.68%/97.9%/100%/100%, unchanged from 1.48's run — no source file touched this task). `cargo test` (workspace: `desktop_lib`, `desktop`, doc-tests) 0/0/0 ×3 (vacuous, Rust surface is Phase 2). `pnpm --filter @essaydown/e2e-web exec playwright install chromium` then the full e2e/web suite: spec-file count from the directory, `find e2e/web -name '*.spec.ts' -not -path '*/node_modules/*' | wc -l` = **22** (not a literal carried over from a prior task — DECISIONS #review-1-r5 K7's own caution), **88 passed**, 0 failed. Phase head (`749a51e`, task 1.48) is ready for the three-OS CI run: nothing in this task's own diff (CLAUDE.md/AGENTS.md only) touches code, tests or dependencies, so the CI gate's three-OS result is a re-run of the same green suite, not new surface. Procedure note (DECISIONS #review-1-r2 H7, restated in this task's text): `PROMPT.md`'s pre-1.verify.r2 iteration list (implement, one journal entry, one commit) is superseded by CLAUDE.md's stub-first `## Every iteration` list, which this attempt followed (stub committed before implementation) — a mechanical mismatch in commit granularity, not a conflict to stop on. No defect found outside scope; no `docs/V1.1-BACKLOG.md` line added. Files: CLAUDE.md, AGENTS.md, docs/progress/journal-main.md (this line). Tests: as above, all green, all run by me this attempt. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: nothing outstanding for 1.verify.r6; the review-set for r6 and its principal reconciliation are the next work on this phase.
- [1.10.r6d] 2026-09-21T07:30:54Z Task: reconciliation of Phase 1 review attempt r6 at c2f6526 (verifier 1.verify.r6, gate 1.verify.r6h ACCEPT a2 after a transient rerun — DECISIONS #029 —, run 34737435949; three independent reviewers, started together). Claude FAIL (1 blocker/7 should-fix/2 nits), Sol FAIL (1/2/0), Grok FAIL (1/0/0). Verdict FAIL, two blocker families: (L1, Sol and Grok) an astral character that is the closing neighbour of one flanking run and the opening neighbour of the next is written as two lone-surrogate references (`*a.*&#x1F600;*(b)*` → `*a.*&#xD83D;&#xDE00;*(b)*`, reparsing to U+FFFD U+FFFD) because `container-phrasing.js`'s two edge encodings both fire on the one-character child and 1.45's `SPLIT_PAIR` matches a reference beside a raw mate only — reproduced headlessly for nine mark pairs × three astral characters and five position classes, 32 trees, none a fixed point, every one unresolved in the map; (L2, Claude) an autolink literal whose URL holds a backslash is written in `<…>` form with the backslash doubled and every `parse ∘ format` doubles it again, because the text child still passes through `safe()` while nothing inside an autolink decodes — reproduced on `https://x.y\` and `Foo\_bar`; both fix shapes prototyped in a detached scratch worktree (a third `SPLIT_PAIR` form; a per-node resource-form fallback whose criterion is the byte-for-byte round trip of the url) with the suite green. Required should-fix: the position map misses `container-phrasing.js`'s third branch (a soft break before inline html, unresolved; Claude 3 = Sol 2) and has no spelling table for `inlineCode` (Claude 2), reproduced as the inverse `toRendered(toSource(p))` failing over the text positions; taken: a table cell's end mapping to the row's start, the run-end boundary side disagreeing with `$pos.marks()`, the blind counted `press` helpers (the r6h a1 rerun's instrument, Sol 3 = Claude 6), the corpus callers' text instrument (1.47's clause recorded under 1.47 as L8); deferred with triggers: URL-shaped text (L9), adjacent attention runs (L11); recorded: 1.47's spec count (L10). Status: Complete. Files: docs/reviews/phase-1-r6-{claude,sol,grok}.md (byte-identical copies, `cmp` clean), docs/DECISIONS.md (#review-1-r6: verdict FAIL first line; the r6h a1/a2 record quoted from #029; the two causes read in the installed package and the two prototyped shapes; 11 findings L1–L11 with severity and disposition; K1–K9 confirmed with K1 and K2 reopened for the two-run join and the third branch, J3 reopened for two node classes; the reviewer record — unique findings Claude 8 of 10, Sol 0 of 3, Grok 0 of 1, three shared findings agreed by class, cumulative twenty-five shared / eight disagreements / low-rater Grok 6, Claude 2, Sol 2 —; the turn-budget counts from the transcripts; the promote candidates for 1.verify.r7; the cherry-pick record; graph change; reversal), docs/PRD.md (§8 Phase 1: +1.49 the third `SPLIT_PAIR` form with pair guards over nine mark pairs, five position classes, the `astral-between-runs.md` fixture and Sol's browser route (opus); +1.50 the per-node autolink fallback with guards enumerated from `safe()`, the `autolink-backslash.md` fixture and a `formatWithMap` case (opus); +1.51 the html-rewrite candidate and the `inlineCode` spelling table with the `inline-html.md` fixture and Sol's toggle route (opus); +1.52 the cell-end rule, the corpus inverse leg over every text position of every fixture with a destructive-transaction seed, two Playwright cases (opus); +1.53 the boundary side by `ResolvedPos.marks()` with two-view agreement guards (opus); +1.54 the per-step caret poll in every counted-press helper and the whitespace-removed text equality in every `expectNoForbiddenEntity` caller (sonnet, test-only); +1.verify.r7 with needsCI `ci/1.verify.r7/a{n}` and the two promote-lessons candidates verbatim; +1.10.r7a/b/c with the r6 dispositions, the two recorded deferrals and the r6 class to search beyond, and 1.10.r7d; 1.close re-pointed at 1.10.r7d), ralph/tasks.json and ralph/EXPECTED_COUNT (232 → 244: 194 raw + 21 review-set + 29 CI gates; `generate-tasks --check` and `validate-tasks` green), docs/V1.1-BACKLOG.md (L9, L11, the correction of the r4 autolink deferral, a note on the r5 astral-positions line, the turn-budget and reviewer-record evidence lines), docs/lessons.md (six `[1.10.r6d]` lessons), docs/progress.md (regenerated: current state, last five journal entries, the r6-chain metrics rows, the review r6 paragraph), handoffs 025 and 026 and DECISIONS #029 cherry-picked from `handoff/026` (three commits, no conflict). Reproductions and prototypes ran in a detached scratch worktree at c2f6526 (`pnpm install --frozen-lockfile` there), removed before this commit; `.wt/1.10.r6d` holds no `node_modules`. `bash ralph/test/run.sh` green (count in the commit body). No human record copied (1.verify.r6h is evidence-only).

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.

Per task, Phase 1 (attempts from .evidence/state, tool calls from the journal, first attempt from runner.log, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 1.1 | 1 | ~25 | y | y (F1 trailing space, F2 no parseDOM) |
| 1.2 | 1 | ~30 | y | y (F3 empty cells unmapped) |
| 1.3 | 2 | 18 | n (NO-JOURNAL at a1) | n (its empty table row is F3's shape) |
| 1.4 | 2 | 12 | n (a1 exit 1) | n |
| 1.5 | 2 | ~20 | n (NO-JOURNAL at a1) | partial (F9 probe-span colour test; F15 depth wording) |
| 1.6 | 1 | 27 | y | partial (F9 redo spelling) |
| 1.7 | 2 | 22 | n (NO-JOURNAL at a1) | y (F4 cursor coordinates, F5 source-view parse per keystroke) |
| 1.8 | 2 | 14 | n (NO-JOURNAL at a1) | y (F6 bare YAML topic) |
| 1.9 (human) | a2 (a1 re-recorded, #repair-1.9) | — | n | — (its record is F1's source; F13 provenance, F16 count) |
| 1.verify | 1 | 14 | y | n (gate 1.verifyh GATE-FAILED on 1.6/1.7's tests, #020) |
| 1.11 | 1 | 22 | y | partial (F9: the CodeMirror half untested) |
| 1.12 | 1 | ~24 | y | n |
| 1.verify.g1 | 1 | 15 | y | n |
| 1.10.r0a (Claude) / r0b (Sol) / r0c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.13 | 1 | 27 | y | partial (G4: the strip stops at an atom; the family has more members) |
| 1.14 | 2 | 20 | n (NO-JOURNAL at a1) | partial (G3: the whitespace fix covers the clipboard parse, not the typing parse) |
| 1.15 | 2 | 26 | n (NO-JOURNAL at a1) | n |
| 1.16 | 2 | 31 | n (NO-JOURNAL at a1) | n (G3 recorded by it as a lesson, not caused by it) |
| 1.17 | 2 | 24 | n (NO-JOURNAL at a1) | y (G1 copy button, G2 source Undo/Redo: readers of the deferred commit) |
| 1.18 | 1 | ~24 | y | n |
| 1.19 | 1 | ~20 | y | n |
| 1.20 | 1 | ~30 | y | n (G12 citation nit, taken in the reconciliation) |
| 1.21 | 3 | ~10 (a3) | n (NO-JOURNAL at a1, exit 1 at a2) | partial (G5: the rendered-view sibling left as a lesson) |
| 1.22 | 1 | ~18 | y | n |
| 1.verify.r1 | 1 | ~15 | y | partial (G6: the promoted rule's position-map family discharged by a test that does not call it) |
| 1.10.r1a (Claude) / r1b (Sol) / r1c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.23 | 2 | ~22 (a1) + ~12 (a2) | n (a1 `error_max_turns` at 51, exit 1, journal present — no stop signal) | n (its three copy cases read the OS clipboard: the windows CRLF gate failure, #021, repaired by 1.28) |
| 1.24 | 1 | ~30 | y | y (H2: the pending segment keeps one keystroke time, so a flush splits a burst longer than the window) |
| 1.25 | 2 | n/a (the fields are absent from its entries) | n (NO-JOURNAL at a1, `error_max_turns` at 51) | y (H1/H5: the atom itself at a block end; H6: a cell's line ending) |
| 1.26 | 1 | ~24 | y | n |
| 1.27 | 1 | ~20 | y | n (H9: one tautology, nit, taken in 1.32) |
| 1.verify.r2 | 1 | ~14 | y | partial (H7: the wrapper `ralph/PROMPT.md` not carried along — a runner file, fixed at the boundary) |
| 1.28 | 1 | ~18 | y | partial (H4: the spy removed the suite's last real clipboard write — #021's scope decision) |
| 1.verify.r2.g1 | 2 | ~12 (a1) + ~20 (a2) | n (a1 exit 0 at `num_turns` 11, stopped by choice on H7; no stop signal) | n |
| 1.10.r2a (Claude) / r2b (Sol) / r2c (Grok) | 1 / 1 / 1 | — | y / y / y | — |
| 1.29 | 3 | ~50 (a1) + ~16 (a2) + ~10 (a3) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal; a2 `success` at 21 with DONE and NO-JOURNAL — the stub completed in place, `chore(1.29)`) | partial (I1: the atom at a *mark's* edge, deferred by 1.30's backlog line; the deletion leg's ranges stop at the block's end) |
| 1.30 | 2 | ~50 (a1) + ~20 (a2) | n (a1 `error_max_turns` at 51, journal present — no stop signal) | y (I3: the mark-edge clause splits a link-marked run, a regression; I1's deferral written by this task on a false reachability claim) |
| 1.31 | 1 | ~34 (49 turns, 48 tool uses — one under the cap) | y | n |
| 1.32 | 1 | ~30 | y | n (I8: the fourth tautology, in a file outside its scope, filed on the backlog as asked) |
| 1.verify.r3 | 1 | ~20 | y | partial (I5: the promoted rule discharged at the neighbouring family only; the position map stayed insertion-only) |
| 1.33 | 1 | ~21 | y | n (gate 1.verify.r3h GATE-FAILED on 1.30's chord, #022, repaired here; the four sibling chords are I6 by #022's own trigger) |
| 1.verify.r3.g1 | 1 | ~20 | y | n (gate 1.verify.r3.g1h ACCEPT at a2 after a transient rerun, #023; I7 recorded) |
| 1.10.r3a (Claude) / r3b (Sol) / r3c (Grok) | 1 / 1 / 1 | 85 turns / — / 19 turns | y / y / y | — |
| 1.34 | 2 | ~50 (a1) + ~25 (a2) | n (a1 `error_max_turns` at 51, journal stub present — no stop signal) | partial (J1: the atom clause holds; the family's next member is the serializer's, `format.ts`) |
| 1.35 | 2 | ~50 (a1) + ~19 (a2) | n (a1 `error_max_turns` at 51, stub present) | n (J1's link member runs through its `outermostMark` rule as designed; the serializer is at fault) |
| 1.36 | 2 | ~50 (a1) + ~24 (a2) | n (a1 `error_max_turns` at 51, stub present) | partial (J2: the paragraph → setext-heading pair missing from `needsBlankLine`; the kind table lacked it) |
| 1.37 | 2 | ~50 (a1) + ~35 (a2) | n (a1 `error_max_turns` at 51, stub present — its stub carries no timestamp, J8) | partial (J3: the defect it found and pinned as `it.fails`, deferred on scope; taken in 1.43) |
| 1.38 | 1 | ~34 | y | partial (J4/J5: the guard's regex misses Shift/Alt chains and its allowlist reason is false; gate 1.verify.r4h GATE-FAILED on its `ArrowUp` route, #024, repaired by 1.39) |
| 1.verify.r4 | 1 | ~39 | y | n |
| 1.39 | 1 | ~17 | y | n (J6: the `[plan.1.verify.r4h.r0]` line it left names two of three sites — a reconciliation correction) |
| 1.verify.r4.g1 | 1 | ~25 | y | n (gate 1.verify.r4.g1h ACCEPT at a1) |
| 1.10.r4a (Claude) / r4b (Sol) / r4c (Grok) | 1 / 1 / 2 container runs (a 402, then `retry` alone — #025) | 110 turns / — / 19 turns (13 on the 402 run) | y / y / n (STUCK 1.10.r4 on the 402) | — (Grok's report derived) |
| 1.40 | 3 → STUCK, retry 1 | ~50 (a1) + ~23 (a2) + ~13 (a3) + ~29 (retry) | n (a1 `error_max_turns` at 51 with the work done; a2/a3 stopped on the task-text conflict #026 decided) | partial (K1: the astral neighbour, a Phase 0 gap its port extended to `~~`; K2: the map defect it found and pinned, taken in 1.46) |
| 1.41 | 3 → STUCK, retry 3 → STUCK, retry 1 | ~51 ×4 + 16 (429) + 52 (no new journal line) + ~38 | n (every cap on re-verification of a finished branch; #027) | partial (K4: the decoded-text assertion its text names is absent) |
| 1.42 | 3 → STUCK, retry 1 | ~51 ×3 + ~18 | n (caps on re-verification) | n (K5's second half: the depth-3 clause has no `KINDS` feeder — 1.48) |
| 1.43 | 3 → STUCK, retry 1 | ~42 / ~13 / ~8 + ~15 | n (DONE withheld on the clause #028 re-read) | n |
| 1.44 | 1 | ~35 | y | n (K6/K8: the guard's quote forms and title — 1.48) |
| 1.verify.r5 | 1 | ~43 | y | n (K7: its journal's spec-file count) |
| 1.10.r5a (Claude) / r5b (Sol) / r5c (Grok) | 1 / 1 / 1 | 113 turns / — / 28 turns | y / y / y | — |
| 1.45 | 2 | ~2 (a1, a 429 session limit — #027's second instance, `NO-JOURNAL 1.45` by the harness) + ~37 | n | partial (L1: the two-run join its `SPLIT_PAIR` never named — 1.49) |
| 1.46 | 2 | ~51 (a1 capped) + ~44 | n | partial (L3: the html-before-soft-break branch its enumeration missed — 1.51) |
| 1.47 | 3 → STUCK, retry 1 | ~51 ×3 (a1 without a stub) + ~42 | n (every cap with the work complete on the branch; the retry after a principal lesson line, #027's procedure) | partial (L8: the corpus-callers clause re-read, recorded at #review-1-r6; L10: its journal's spec-file count) |
| 1.48 | 1 | ~54 | y | n |
| 1.verify.r6 | 1 | ~38 | y | n |
| 1.10.r6a (Claude) / r6b (Sol) / r6c (Grok) | 1 / 1 / 1 | 123 turns / — / 25 turns | y / y / y | — |

Phase 1 review r0: findings as reconciled — 1 blocker, 6 should-fix required (in tasks 1.13–1.19), 2 should-fix taken as new tasks (1.20 enforcement, 1.22 test-only from backlog triggers), 5 nits (1 taken in the reconciliation, 2 deferred, 2 recorded), 1 recorded. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 41 (docs/lessons.md lines tagged 1.*, plan.1.*). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (Phase 0) + 1 pending (via 1.verify.r1). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 4 (1.3, 1.5, 1.7, 1.8, each passed at a2). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r0 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.68 %, functions 100 %, lines 100 %; delta vs Phase 0 r2: +0.18, +0.70, 0, 0 (thresholds hold; coverage counts packages/*/src only, so apps/desktop/src/dev/** is covered by the 36 Playwright specs alone). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11, 1.12: 5 of 10.

Phase 1 review r1: findings as reconciled — 1 blocker, 5 should-fix required (in tasks 1.23–1.27), 1 should-fix deferred with a backlog line (G7, hard stop 2.verify), 5 nits (1 taken in task 1.26, 3 taken in the reconciliation, 1 deferred); r0 dispositions reopened: 2 of 16 as families or siblings (F1 → G4, F9 → G5), 14 confirmed. Verdict FAIL. Lessons written by Phase 1 agents and the principal: 58 (docs/lessons.md lines tagged 1.*, plan.1.*; 47 before this commit + 11). Lessons promoted to CLAUDE.md/AGENTS.md: 4 (3 in Phase 0, 1 via 1.verify.r1) + 1 pending (via 1.verify.r2). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 9 (1.3, 1.5, 1.7, 1.8, 1.14, 1.15, 1.16, 1.17, 1.21, each at a1; 1.21 also exit 1 at a2 and passed at a3). Gate reruns: 0. Gate repairs: 1 (1.verify.g1). Coverage at r1 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r0: 0, −0.03, 0, 0 (thresholds hold; apps/desktop/src/** is still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.5 ms (1.17's container), 11.7 ms (Claude, single worker), 10.2 ms (Sol, single worker); 25.4 / 23.6 / 19.9 ms on the ubuntu / windows / macos runners; 51–54 ms inside a contended two-worker run. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.22: 10 of 20 (5 of 10 before the r0 chain, 5 of 10 in it). The "tool calls" column is the journal's self-report and undercounts the transcript by about 2× (1.13: 27 reported, 52 tool-use blocks; 1.18: ~24 reported, 56); by the transcript measure the capped first attempts ended at 54–67 tool uses and the finished ones at 52–56 — the cap sits on the median of the work, not above it (#review-1-r1).

Phase 1 review r2: findings as reconciled — 2 blockers (in tasks 1.29 and 1.31), 3 should-fix required (in tasks 1.30 and 1.32), 1 should-fix taken as a runner chore at the phase boundary (H7, `fix(runner)` with a conformance test), 3 nits (2 taken in task 1.29, 1 in task 1.32); r1 dispositions reopened: 3 of 12 as siblings or families (G1's attestation → H4, G2 → H2, G4 → H1/H5/H6), 9 confirmed; the r1 backlog deferral of F1's emphasis-edge member reopened (H8, its reachability premise false). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 92 (docs/lessons.md lines tagged 1.*, plan.1.*; 84 before this commit + 8). Lessons promoted to CLAUDE.md/AGENTS.md: 5 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2) + 1 pending (via 1.verify.r3). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 10 (the nine of r1 plus 1.25 at a1); other second attempts: 1.23 (capped, journal present) and 1.verify.r2.g1 (stopped by choice), neither a stop signal. Gate reruns: 0. Gate repairs: 2 (1.verify.g1, 1.verify.r2.g1). Coverage at r2 (three CI legs and both cold builds agree): statements 99.63 %, branches 97.65 %, functions 100 %, lines 100 %; delta vs r1: 0, 0, 0, 0 (1.26–1.28 and the verifiers touched no `packages/*/src`; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 9.2 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 20.6 / 22.7 / 11.9 ms on the ubuntu / windows / macos runners of the accepted gate. First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.28: 14 of 26 (10 of 20 before the r2 chain, 4 of 6 in it). By the transcript measure the two capped first attempts this chain ended at 50 and 56 tool uses and the five that finished first at 28–59 — the r1 reading (the cap sits on the median of the work) stands; only 1.28 and 1.verify.r2.g1 ran under the journal-stub rule, with no cap and no NO-JOURNAL between them.

Phase 1 review r3: findings as reconciled — 2 blockers (in tasks 1.34 and 1.36), 3 should-fix required (in tasks 1.35, 1.37 and 1.38), 1 should-fix taken with its sibling in 1.35 (I4), 1 should-fix recorded (I7, the a2 rerun answered by #023, with a conditional boundary candidate and a flake-signature line), 3 nits (1 taken in task 1.34, 1 taken in the reconciliation — the address redaction, 1 decided — Unicode whitespace stays ASCII); r2 dispositions reopened: 2 of 9 as families or members (H1 → I1, H8 → I3 and I1), 7 confirmed; the `[1.30]` backlog deferral of the break-inside-a-mark member reopened (its reachability premise false, as H8's was at r2). Verdict FAIL. Lessons written by Phase 1 agents and the principal: 116 (docs/lessons.md lines tagged 1.*, plan.1.*; 106 before this commit + 10). Lessons promoted to CLAUDE.md/AGENTS.md: 6 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3) + 2 pending (via 1.verify.r4). STUCK stops in Phase 1: 0; NO-JOURNAL stops: 11 (the ten of r2 plus 1.29 at a2 — procedure, not budget); capped first attempts this chain: 1.29 and 1.30 (both continued under the stub rule, no stop signal). Gate reruns: 1 (1.verify.r3.g1h a1 → a2, transient, #023). Gate repairs: 3 (1.verify.g1, 1.verify.r2.g1, 1.verify.r3.g1). Coverage at r3 (three CI legs and both cold builds agree): statements 99.64 %, branches 97.73 %, functions 100 %, lines 100 %; delta vs r2: +0.01, +0.08, 0, 0 (1.29–1.31 added 153 tests; thresholds hold; apps/desktop/src/** still outside the include — G7). Latency under the 1.17 protocol at 10 k words, rendered pane p95: 11.5 ms (Claude, single worker) and 9.8 ms (Sol, isolated rerun); 26.4 / 20.7 / 14.3 ms on the ubuntu / windows / macos runners of the accepted gate; source commit p95 56.1 / 59.9 ms isolated (G9, evidence only). First-attempt pass rate, implementation tasks 1.1–1.8, 1.11–1.33: 18 of 31 (14 of 26 before the r3 chain, 4 of 5 in it: 1.31, 1.32, 1.33 and the verifiers at a1; 1.29 and 1.30 capped). By the transcript measure the two capped first attempts this chain ended at 50 tool uses each and the three that finished first at 48 (1.31, one turn under the cap), 38 and 22; the whole chain ran under the journal-stub rule, which turned both caps into continued attempts rather than NO-JOURNAL stops. Turn-budget decision unchanged: 80 conditional on Michael's OK at the Phase 2 boundary.

Phase 1 review r4: findings as reconciled — 1 blocker (in tasks 1.40 and 1.41, the serializer half of the flanking family), 1 should-fix required (in task 1.43, the cursor map — the `[1.37]` backlog line's trigger fired), 2 should-fix taken (1.42 the setext pair; 1.44 the chord guard's regex), 5 nits (1 taken in 1.44 — the allowlist reason, 2 taken in the reconciliation — the vertical-arrow line's third site and the timestamp-less stub recorded, 1 backlog — the heading entity, 1 evidence — the turn budget); r3 dispositions reopened: 1 of 10 as a family (I1 → J1, on the serializer's side), 9 confirmed. Verdict FAIL. Reviewer record: two independent reports and one derived (Grok, DECISIONS #025 — a single-reviewer retry after a 402 read its siblings' finished reports; unique findings 0 by construction, verdict outside the count). Lessons written by Phase 1 agents and the principal: 131 (docs/lessons.md lines tagged 1.*, plan.1.*; 124 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 8 (3 in Phase 0, 1 via 1.verify.r1, 1 via 1.verify.r2, 1 via 1.verify.r3, 2 via 1.verify.r4); 2 more carried by 1.verify.r5 (the two-halves ownership of a fixed-point family; a kind table derived from the rule's branches), 1 recorded. STUCK signals this attempt: 1 (1.10.r4, Grok's 402). Turn budget: four of the five r4 opus fix tasks capped at a1 (six of six schema.ts opus fix tasks across r2–r4); 19 of 46 Phase 1 loop tasks needed more than one attempt; the raise stays conditional on Michael's OK.

Phase 1 review r5: findings as reconciled — 1 blocker (in tasks 1.45 and 1.47, the astral neighbour of a flanking mark — the flanking family's non-BMP member, the encode decision right and its width wrong), 1 should-fix required by all three reviewers (in task 1.46, the position map's encoded neighbour — 1.40's pin, the `[1.40, found outside scope]` line's trigger fired), 1 should-fix discharged by the reconciliation's cherry-pick (#026–#028 into the tree), 1 should-fix taken (in task 1.47, the decoded-text assertion), 4 nits (3 taken in 1.48, 1 recorded); r4 dispositions: J1 reopened for astral neighbours, J2–J9 confirmed; reviewer record: three independent reports, unique findings Claude 5 of 8, Sol 0 of 2, Grok 1 of 3, three shared findings all agreed.

Phase 1 review r6: findings as reconciled — 2 blockers (in task 1.49, the astral character between two flanking runs — the two-run join of the flanking family; in task 1.50, the autolink literal's backslash — invariant A on the parser's own tree), 2 should-fix required (in task 1.51: the position map's html-before-soft-break rewrite and the `inlineCode` spelling table), 4 should-fix taken (1.52 the cell's end with the corpus inverse leg; 1.53 the boundary side by `$pos.marks()`; 1.54 the counted-press helpers and the corpus callers' text instrument, with 1.47's re-read clause recorded), 1 should-fix deferred with a trigger (URL-shaped text, hard stop `2.verify`), 2 nits (1 backlog — adjacent attention runs, 1 recorded — 1.47's spec count); r5 dispositions: K1 reopened for the two-run join, K2 reopened for the third branch, K3–K9 confirmed; J3 reopened for two node classes; reviewer record: three independent reports, unique findings Claude 8 of 10, Sol 0 of 3, Grok 0 of 1, three shared findings agreed by class, two blocker families (one shared by Sol and Grok, one unique to Claude). Verdict FAIL. Gate reruns this attempt: 1 (1.verify.r6h a1 → a2, #029, the second instance of #023). STUCK stops in the r6 chain: 1 (1.47); NO-JOURNAL: 2 (1.45 on a 429, 1.47 a1 without a stub).
