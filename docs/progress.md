# progress.md — Essay Down

## Current state (≤ 6 lines; regenerated only in reconciliation commits, RUNNER-SPEC §10)

Phase 0 (core model & round-trip), branch phase/0. Implementation 0.0–0.11 passed; 0.verify → gate 0.verifyh GATE-FAILED (Windows CRLF) → 0.13 + 0.verify.g1 → gate ACCEPT → review r0 FAIL (DECISIONS #review-0-r0) → fix chain 0.14–0.22 + 0.verify.r1 → gate 0.verify.r1h ACCEPT → review r1 FAIL (DECISIONS #review-0-r1) → fix chain 0.23–0.26 + 0.verify.r2, all at attempt 1 → gate 0.verify.r2h ACCEPT (a1, run 34112753627, three OS directories).
Review attempt r2 at 98bee5a: Claude PASS (0/4/4, 110 of 160 turns, one container run), Sol PASS (0/0/0), Grok PASS (0/0/1) → reconciliation verdict PASS (DECISIONS #review-0-r2): 1 should-fix taken (RUNNER-SPEC §1 lock wording), 3 should-fix deferred to the backlog with revisit points (H1 unidentifiable lock holder, H2 YAML implicit-type quoting, H3 corpus identity for block operations), 5 nits (1 taken, 4 recorded). Every r1 disposition G1–G13 confirmed; none reopened. Graph unchanged (150 tasks).
Next: 0.close (moves `main` from the seed 660f3aa to this reconciliation commit, creates phase/1); then the principal rotates (handoff 008) and the runner restarts with --phase 1 → 1.1 (opus). Handoff 007 lives in this commit (cherry-picked from handoff/007, deviation 29); delete handoff/006 and handoff/007 after 0.close.
Open human gates: none (next: Phase 1's 1.verifyh after 1.1–1.9 and 1.verify). Plan requests: none. Blocked: none.
Manual procedures in force: one runner process (DECISIONS #review-0-r0 F14); rebuild the image after any docker/ change before the next container task that can observe it (F17, reading recorded in #review-0-r2 deviation 34); host-checkout commits only per #017; handoff commits at a verifier gate go on handoff/NNN (#review-0-r1 deviation 29); reviewers run with RALPH_REVIEW_MAX_TURNS 160 (default from 0.26); stale locks are recovered by hand (G4, from 0.25); promote-lessons is manual (RUNNER-SPEC §5.5; none named for 1.verify).
First Phase 1 idle window (before 1.1 starts, or between tasks, never after 1.verify): the chore(0.0) commit on phase/1 for .claude/commands/rotate.md step 5 and docs/PRINCIPAL.md (handoff 007 Next Steps 6) and, on Michael's OK, the H1 fix(runner) commit with its conformance leg (backlog).

## Last 5 journal entries (docs/progress/journal-main.md is the full, append-only journal)

- [0.24] 2026-09-07T06:56:00Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G3, Sol finding 3, should-fix required): `packages/core/src/sentences.ts` rebuilt the paragraph from per-sentence slices even when the argument asked for no change, so a mark spanning a sentence boundary came back split. Status: Complete. Files: `packages/core/src/sentences.ts` (two short-circuits and the doc comments), `packages/core/test/sentences-spanning-marks.test.ts` (new, 24 tests), six new fixtures under `fixtures/markdown/` with canonical siblings (`spanning-mark-strong`, `-emphasis`, `-link`, `-strikethrough`, `-first-boundary`, `-last-boundary`), `fixtures/markdown/index.json` (six additive entries), `docs/progress/journal-main.md`, `docs/lessons.md`. No other file: `git diff --cached --stat` is those files only, and the same diff over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty (zero new dependencies), so the edits stay inside the three trees the task text names. The three things asked for. (1) The no-op is the identity: `reorderSentences` returns the argument root when `order` is the identity permutation, and `replaceSentence` when `text` equals `sentenceMarkdown(paragraph, index, options)` — both after the full validation a real edit gets (unknown block, non-top-level block, non-permutation, out-of-range index all still throw, tested), and both decided from the validated argument rather than from a comparison of the rebuilt output's bytes (a test replaces `*One.*` with `_One._`, which serialises to the same bytes, and asserts the call is *not* the identity and takes the slicing path). The identity permutation is the only permutation of the empty sentence list, so the old `sentences.length === 0` branch is now unreachable and was removed rather than left as dead code under the coverage thresholds. (2) The corpus grew by six LF fixtures, one paragraph each, covering `strong`, `emphasis`, `link` and `delete` across a boundary plus a mark spanning the first boundary and one spanning the last (both in four-sentence paragraphs, so first, middle and last are distinguishable); each `.canonical.md` is `format(parse(source))` and is byte-identical to its source, and the `index.json` entries were computed the way the existing ones are (`nodeTypes` from the parsed canonical, `blockCount` = root's direct children + each `listItem` + each `tableRow`, `sectionCount` = heading count, `paragraphStartLines` from the canonical's own parse); the diff is purely additive, no existing entry touched. The new test reads the fixtures out of `index.json` and asserts the shape is present (one test per mark type, one for the first/middle/last boundary positions) rather than naming files, so deleting a fixture fails the suite. (3) The rule for a real edit is stated in `sliceInline`'s doc comment and referenced from both operations: no sentence keeps a spanning mark — every sentence the mark covers keeps its own copy over its own share of the text, and the boundary whitespace is unwrapped from the mark and left in place — which is the rule under which the bytes of every untouched sentence are unchanged, since an untouched sentence's own Markdown is the same slice of the same mark before and after. That is tested at the first, middle and last position for both operations (`**One. Two. Three.**` replaced at 0/1/2; `**One. Two. Three. Four.**` swapped at the first, a middle and the last position) and corpus-wide over every spanning-mark fixture. The doc comment also says why the alternative rule (keep one whole mark around the edited span) is not available in general: a mark covering only part of the sentences at its ends has no span left once those sentences move apart. Tests: reproduction first — with the new test file and the six fixtures in place and `sentences.ts` restored to HEAD, `npx vitest run packages/core/test/sentences-spanning-marks.test.ts` was 5 failed / 19 passed (both Sol probes, the mark-stays-whole assertion, the empty-permutation identity and the corpus no-op test; the rule-for-real-edits tests pass either way, because the rule is the one the code already implemented and this task only documents and pins it), and `npx vitest run packages/core/test/sentences-zero-width.test.ts packages/core/test/roundtrip.test.ts` was 7 failed / 222 passed — all six new fixtures failing the 0.14 corpus-wide identity invariant, which is the gap Sol named. After restoring the fix both files are green. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729 across 21 files (675 + 24 new + 18 roundtrip A/B/C for the 6 fixtures + 6 corpus identity + 6 front-matter-boundary corpus cases, every one of those four growths read from the index or the directory, not a literal), coverage 99.45%/96.98%/100%/100%, thresholds met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. The 30 cases of `sentences.cases.json` and the existing `sentences.test.ts` (91) and `sentences-zero-width.test.ts` (70) tests pass unedited — no test of a passed task was rewritten. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: one design reading a reviewer should check — the short-circuits return the *argument root object*, not a structurally equal copy, which is what `expect(next).toBe(root)` pins; it follows 0.23's `writeFrontMatter` precedent and means an identity reorder no longer strips `position` from the paragraph it did not change. `applyReorderSentences` (sidecar.ts) passes that same root to `reanchor`, whose anchors are unmoved under the identity permutation; its 73 tests are green. Nothing outstanding from 0.24.
- [0.25] 2026-09-07T07:07:46Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G4 and G5, Sol finding 1 and Claude finding 6, should-fix required; runner code, conformance tests required): (1) `breakStaleLock` renamed a lock it judged stale before it compared bytes, so the lock path was free between the rename and the compare and a third entrant could create it and run beside a live holder (Sol's probe `/logs/reviews/0/r1/sol/lock-probe.{mjs,log}`: "third entrant inside critical section while live holder active: true"); (2) `writeAtomic` swallowed a failed directory fsync in a bare catch, so a close intent published without directory durability left no trace. Status: Complete. Files: `ralph/lib/util.mjs`, `ralph/lib/doctor.mjs`, `ralph/lib/state.mjs` (2 lines, see the deviation below), `ralph/test/util.test.mjs` (rewritten to the new contract, 13 tests including subtests, was 6), `ralph/test/conformance.test.mjs` (+1 scenario, 34 tests, was 33), `docs/progress/journal-main.md`, `docs/lessons.md`. `docs/RUNNER-SPEC.md` untouched — §1 already carries the amendment the 0.12.r1d reconciliation made. Zero new dependencies: `git diff --stat` over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty. What changed. (1) Automatic stale-lock breaking is cut to a manual procedure, as CLAUDE.md prefers over more machinery: `breakStaleLock` and the `hooks.beforeBreak` interleaving point are deleted (the identifier no longer appears anywhere under `ralph/lib/`), and `withLock` now never renames or unlinks a lock it did not create. A lock whose holder pid is not running makes `withLock` throw a `RalphError` naming the file, the dead pid and the recovery; `doctor` reports the same lock as `stale-lock <pid> (<path>)` with the identical repair sentence, because both quote one exported helper (`staleLockRepair`) and read the holder through one exported helper (`lockHolder`), so the refusal and the drift report cannot describe different procedures. The token re-read after acquisition and the token-scoped release are kept unchanged; `hooks.afterCreate` is the one interleaving point left. A lock file whose first field is not a positive integer now counts as held-by-something-unidentifiable (`stale-lock unknown`) rather than as breakable — the old code treated `Number("garbage")` as "no pid" and broke it. (2) `writeAtomic` takes `{ fs, audit }`; the directory-fsync catch now calls `audit("writeAtomic: directory fsync failed <path> <code>")` and continues. The sink defaults to `auditNote`, which appends to the state layer's own `state/audit.log`: the `Ctx` constructor hands util.mjs that path via `setAuditLog` (util.mjs sits below the state layer and must not re-derive an evidence path), and with no path named yet the line goes to stderr rather than being lost. `<path>` is the file the call published, not its parent directory — the parent is `dirname` of it, and the fact worth recording is which intent lacks durability; the reading is stated in the doc comment. Tests: reproduction first. With both new test files in place and `ralph/lib/` restored to HEAD, `node --test ralph/test/util.test.mjs` was `# tests 13 / # pass 5 / # fail 8` and `node --test ralph/test/conformance.test.mjs` was `# tests 34 / # pass 33 / # fail 1`. The 8 + 1 red cases are the findings: the stale-lock refusal and its message, the "every later entrant sees the same lock file (same inode)" case, Sol's three-actor sequence, the two `writeAtomic` audit-line cases (directory open throws, directory fsync throws), and the CLI-level scenario. Honest note on which of the new cases do *not* reproduce a finding: "a third entrant driven through `hooks.afterCreate`" and "a directory fsync that succeeds writes no line at all" pass on the old code too — they are the presence/absence partners of the cases that do reproduce, kept so the rule reads as a property rather than a single symptom. After the fix `bash ralph/test/run.sh` is `# tests 79 / # pass 79 / # fail 0` (was 71/71). The three-actor test represents the live holder A by a lock file carrying a running pid rather than by a second process, because a holder's identity is exactly what `withLock` reads from that file, and it asserts the outcome Sol's probe wanted by inode: the live holder's lock file is the same inode before and after the third entrant's attempt (so the path was never freed), the third entrant waits the full timeout and throws `held by pid <pid>`, and no critical section ran beside the holder. The conformance scenario drives the same contract through the real entry points: with a dead-pid lock in place, `ralph.sh sync-state` exits 1 with the recovery message and `digestDir(.evidence/state)` is byte-identical before and after; `ralph.sh doctor` exits 3 with `stale-lock <pid> (<path>) → manual repair: … pgrep … remove <path> by hand` and `DOCTOR 1 findings`; `ralph.sh run` refuses and selects nothing; the lock file's bytes are unchanged throughout; and after removing the lock by hand `doctor` is clean and `run` integrates 0.1 normally. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729 across 21 files, coverage 99.45%/96.98%/100%/100%, unchanged (this task touches no `packages/` source); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests; `RALPH_ROOT=/work ralph doctor` with this worktree's code prints `doctor: clean`, exit 0. Rewritten tests of a passed task, with the reason (0.15's precedent): `ralph/test/util.test.mjs`'s first two subtests from 0.20 — "a stale lock is broken and taken, and released on the way out" and "a concurrent breaker between the staleness check and the break … the other stays the sole holder" — asserted the exact behaviour this finding calls wrong (that a stale lock *is* broken, and that `hooks.beforeBreak` exists), so "the existing tests still pass" and the fix could not both hold. They are replaced in place by the refusal cases; the two tests that survive unchanged are the post-acquisition token re-read and the token-scoped release, and the `writeAtomic` call-order test is unchanged. DEVIATION (declared): the task text's file list is `ralph/lib/util.mjs`, `ralph/lib/doctor.mjs`, `ralph/test/` and the journal, and this diff also touches `ralph/lib/state.mjs` — two lines, an import and `setAuditLog(this.paths.audit)` in the `Ctx` constructor. The task requires the durability line to land in "the audit log the state layer already writes", and only the state layer knows that path; the alternative (util.mjs re-deriving `<root>/<RALPH_EVIDENCE_DIR>/state/audit.log` for itself) duplicates the layout in a second place, which is the class of drift G1/G5 are both about. Second declared deviation, same as 0.20's: `docs/lessons.md` is not in the task's file list, and CLAUDE.md step 3 requires a lessons line, so two were appended. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: two readings a reviewer should check. First, `setAuditLog` is module-level mutable state in `ralph/lib/util.mjs` (CLAUDE.md's "no module-level config" rule is scoped to `packages/core`); the CLI builds exactly one `Ctx` per process, and it follows the existing `registerRun`/`globalThis.__ralphRun` precedent in this tree. Second, the refusal is unconditional — there is no `--force` and no timeout after which the runner breaks a lock anyway; a crashed holder now stops the loop until a human removes the file, which is the trade the reconciliation chose (recovery is rarer than the race it caused). Nothing outstanding from 0.25.
- [0.26] 2026-09-07T07:15:34Z Task: Phase 0 review fix (DECISIONS #review-0-r1 G6 and deviation 30, Claude finding 2 and Grok finding 2, should-fix required; runner tests and compose only): the review-start block of `ralph/test/conformance.test.mjs` covered a missing `accepted.json` and a tampered artifact (both refused inside `readAcceptedCi`) but never reached the two guards `stepReview` runs after it (`acc.sha !== verificationSha`, `acc.sha !== implementationSha`); and the three review services shared the task services' 50-turn default with no reviewer-scaled budget. Status: Complete. Files: `ralph/test/conformance.test.mjs` (one new leg, (c), inserted between the existing tampered-artifact and intact-evidence legs of the "review start consumes the verifier gate's evidence" test — same test, no new top-level test), `docker/compose.yml`, `docs/progress/journal-main.md`. No other file: `git diff --stat` is exactly those two files (plus this journal entry), and the same diff over every manifest, `pnpm-lock.yaml`, `Cargo.lock` and `pnpm-workspace.yaml` is empty (zero new dependencies). (1) The new leg copies the pristine `accepted.json` and artifact bytes captured at the top of the test, restores the artifact (undoing leg (b)'s tamper), flips `accepted.json`'s `sha` field to a different well-formed 40-hex value (first hex digit toggled 0↔1, so `readAcceptedCi`'s digest check still passes — the artifact's own `sha256` entries are untouched), runs `ralph.sh run --phase 0`, and asserts non-zero exit, a message naming `0.verifyh` and matching `accepted sha .* != (verification|implementation)_sha`, and `nothingRecorded()` (the existing closure: no `phase_base_sha`/`verifier_id`/`verification_sha`/`implementation_sha` files, no reviewer directories, the three `0.9.r0{a,b,c}` tasks left `pending`). It restores `accepted.json`'s original bytes afterward so the following intact-evidence leg (renamed (c)→(d), unchanged otherwise) still starts the review normally. Reproduction first: with the two guards (`ralph/lib/run.mjs`, the `acc.sha !== verificationSha` / `acc.sha !== implementationSha` lines) temporarily deleted and the new leg in place, `node --test ralph/test/conformance.test.mjs` reported the "review start consumes…" test `not ok` (an unrelated later signal, `PRINCIPAL 0.9.r0d`, since with both guards gone the review actually starts and the fixture runs out of scripted CI/task responses — this is a real behaviour change downstream of the missing guard, not a broken test); with the guards restored the same run is green (34/34). (2) `docker/compose.yml` gets a second env anchor `x-review-env: &review-env` that merges `*env` (`<<: *env`) and overrides `RALPH_MAX_TURNS: ${RALPH_REVIEW_MAX_TURNS:-160}`; `claude-review`, `codex-review` and `grok-review` use `environment: *review-env` in place of `*env`; `claude-task` and `boundary-check` are untouched (still `*env`, default 50). Honest note: this container has no `docker` binary (host-side `ralph/bootstrap.sh` is what runs `docker compose`), so the acceptance's literal `docker compose config` command could not be executed here; the merge was instead verified by hand-resolving the two anchors from the file text (a scratch, uncommitted Node script, deleted after use) against plain YAML merge-key semantics (explicit keys override merged-in ones) — `x-env`'s `RALPH_MAX_TURNS` resolves to `50` with neither variable set, `x-review-env`'s to `160`, and only the three review services reference `*review-env`. Tests: `node --test ralph/test/conformance.test.mjs` 34/34 (unchanged test count — the new leg is inside an existing test, not a new one); `bash ralph/test/run.sh` 79/79 (unchanged from 0.25, same reason); full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729, coverage 99.45%/96.98%/100%/100%, unchanged (this task touches no `packages/` source); `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. Iterations used: 1. First attempt passed: y. Tool calls: ~20. Notes: nothing outstanding from 0.26; this was the last of the four r1 fix tasks named ahead of 0.verify.r2 (0.23, 0.24, 0.25, 0.26) per the 0.12.r1d reconciliation's PRD §8 additions.
- [0.verify.r2] 2026-09-07T07:25:00Z Task: Phase 0 verification for review attempt r2 (RUNNER-SPEC §3, §5.5): freeze dependencies (no lockfile change), run the full suite in the container, confirm the phase head (0.26) is ready for the three-OS CI gate, and carry out the `promote-lessons` manual step (DECISIONS #review-0-r1) — append the one named rule, verbatim, to the `## Code rules` list of both CLAUDE.md and AGENTS.md. Status: Complete. Files: `CLAUDE.md` and `AGENTS.md` (each +1 line at the end of the `## Code rules` list, identical text in both, appended after the zero-width-ownership bullet and before the `## Runner facts` heading), `docs/progress/journal-main.md` (this entry). No source file, fixture, or dependency manifest touched. Tests: `git diff --stat` shows exactly `AGENTS.md | 1 +` and `CLAUDE.md | 1 +`, 2 insertions, 0 deletions, no other file; `git status --porcelain` after `pnpm lint`, `pnpm test` and `cargo test` (all three trigger no install/build artefacts under version control) confirms no `pnpm-lock.yaml`, `Cargo.lock`, `package.json` or `Cargo.toml` diff anywhere in the tree. `bash ralph/check-agent-rules.sh` prints `check-agent-rules: OK (CLAUDE.md ≡ AGENTS.md below line 1)`, exit 0. Full bar in this container: `pnpm lint` clean (eslint + the 6 typecheck scripts); `pnpm test` 729/729 across 21 files including `tests/agent-rules.test.ts`, coverage 99.45%/96.98%/100%/100%, unchanged from 0.26, every threshold met; `cargo test` 0/0/0 across desktop_lib, desktop and doc-tests, unchanged. Also ran, though not named by this task's acceptance, as a check that the phase head is CI-ready after 0.26's runner/compose changes: `bash ralph/test/run.sh` 79/79, unchanged from 0.26. The task's `ci`/`ciAcceptance` fields (three-OS run via `ci/0.verify.r2/a{n}`) are the CI gate's job to execute, not this loop task's acceptance; nothing here claims that run happened, and this container is Linux so no macOS or Windows pass is claimed. Iterations used: 1. First attempt passed: y. Tool calls: 11. Notes: the promoted line is exactly the task text's wording, appended after the existing last Code-rules bullet (zero-width partitioning) and before the `## Runner facts` heading, in both files identically; the promoting task naming, per the newly-promoted rule itself, which test discharges the rule it promotes: the rule is a process rule about future fix tasks' guard-level tests, not a code behaviour, so it has no discharging test of its own in this task — `ralph/check-agent-rules.sh` and `tests/agent-rules.test.ts` discharge the identity requirement (CLAUDE.md ≡ AGENTS.md below line 1) that lets the promotion land in both files at once. Nothing outstanding from 0.verify.r2; next is gate `scripts/gate.sh 0.verify.r2h`, then review attempt r2 (0.12.r2a/b/c/d).
- [0.12.r2d] 2026-09-07T11:11:39Z Task: reconciliation of Phase 0 review attempt r2 at 98bee5a (verifier 0.verify.r2). Claude PASS (0 blockers/4 should-fix/4 nits, 110 of 160 turns, one container run), Sol PASS (0/0/0), Grok PASS (0/0/1). Verdict PASS: no blocker and no should-fix marked required (RUNNER-SPEC §5.4); every r1 disposition G1–G13 confirmed by all three reviewers against the tree, none reopened; findings H1–H9 reconciled — 1 should-fix taken here (RUNNER-SPEC §1 named the lock as `flock`; now the O_EXCL file, H4), 3 should-fix deferred with revisit points (unidentifiable lock holder folded into "dead", H1; YAML implicit-type quoting in the front-matter writer, H2; corpus-wide identity for the three block operations, H3), 5 nits (PRD corpus counts taken, H6; four recorded). Status: Complete. Files: docs/reviews/phase-0-r2-{claude,sol,grok}.md (byte-identical copies), docs/DECISIONS.md (#review-0-r2: verdict, 9 findings with severity and disposition, r1 dispositions confirmed, deviations 33–34 and two observations, reversal; #009's lock bullet carries a marked amendment), docs/RUNNER-SPEC.md (§1 lock primitive), docs/PRD.md (§7 gate row count; 1.1 and 1.2 acceptances drop the literal 45), ralph/tasks.json (regenerated; EXPECTED_COUNT 150 unchanged; graph unchanged), docs/V1.1-BACKLOG.md (+7 lines), docs/lessons.md (+7 class-level), docs/progress.md (regenerated), docs/handoffs/007-* and next-prompt.md (cherry-picked from handoff/007, deviation 29). Tests: node ralph/generate-tasks.mjs --check, node ralph/validate-tasks.mjs (150), bash ralph/test/run.sh green. Commit: wip(0.12.r2d). Iterations used: 1. First attempt passed: y. Tool calls: ~25. Reviewer overturned: n/a. Notes: after integration the runner runs 0.close (main → this commit, phase/1 created at it); the principal rotates (handoff 008) before the runner restarts with --phase 1; first Phase 1 idle window: the chore(0.0) rotate.md/PRINCIPAL.md commit (handoff 007 Next Steps 6) and, on Michael's OK, the H1 fix(runner) commit with its conformance leg; no image rebuild pending (no docker/ change since 0.26).

## Metrics (PRD §10.4)

Per task, Phase 0 (attempts from .evidence/state, tool calls and first-attempt from the journal, overturned = a review finding traced to the task):

| task | attempts | tool calls | first attempt passed | reviewer overturned |
|---|---|---|---|---|
| 0.0 | 1 |  | y | y (F5–F10) |
| 0.1 | 1 | ~20 | n | y (F3, F11) |
| 0.2 | 3 | ~10 | n | y (F4) |
| 0.3 | 1 | ~20 | y | partial (F12) |
| 0.4 | 1 | ~45 | y | n |
| 0.5 | 1 | ~25 | y | n |
| 0.6 | 1 | ~15 | y | n |
| 0.7 | 1 | ~20 | y | n |
| 0.8 | 1 | ~25 | y | y (F1) |
| 0.9 | 1 | ~18 | y | n |
| 0.10 | 3 | 9 | n | y (F2) |
| 0.11 | 1 | ~20 | y | n (F16 deferred) |
| 0.verify | 1 | 9 | y | n |
| 0.13 | 1 | 9 | y | n |
| 0.verify.g1 | 1 | 8 | y | n |
| 0.14 | 1 | 12 | y | n |
| 0.15 | 1 | 17 | y | y (G1, G2, G7) |
| 0.16 | 1 | ~28 | y | n |
| 0.17 | 1 | 12 | y | n |
| 0.18 | 1 | 20 | y | y (G6) |
| 0.19 | 1 | 26 | y | n |
| 0.20 | 1 | 16 | y | y (G4, G5) |
| 0.21 | 1 | ~22 | y | n (G10 backlog) |
| 0.22 | 1 | ~24 | y | n (G9 docs) |
| 0.verify.r1 | 1 | 12 | y | partial (G8: promoted rule without its test) |
| 0.12.r1a (Claude reviewer) | 3 container runs (retry resets attempts) | 96 turns on the third | n | — |
| 0.12.r1b (Sol) / 0.12.r1c (Grok) | 1 / 1 | — | y / y | — |
| 0.23 | 1 | ~25 | y | n (H2 is a new class in the same file, deferred) |
| 0.24 | 1 | ~20 | y | n (H3 names sibling operations, deferred) |
| 0.25 | 1 | ~20 | y | partial (H1, H7) |
| 0.26 | 1 | ~20 | y | n |
| 0.verify.r2 | 1 | 11 | y | n |
| 0.12.r2a (Claude reviewer) | 1 | 110 turns | y | — |
| 0.12.r2b (Sol) / 0.12.r2c (Grok) | 1 / 1 | — | y / y | — |

Phase 0 review r0: findings as reconciled — 2 blockers, 12 should-fix required (10 fixed in tasks 0.14–0.22, 2 taken in the reconciliation), 3 should-fix deferred, 4 nits (2 taken, 2 recorded). Lessons written by Phase 0 agents and the principal: 54 (docs/lessons.md lines tagged 0.*, plan.0.*, both). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1). STUCK signals: 1 (0.5, three attempts, re-cut per DECISIONS #016). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r0 (cold build): statements 99.38 %, branches 96.15 %, functions 100 %, lines 99.82 %; blocks.ts 100 %. First-attempt pass rate, implementation tasks 0.1–0.11 + 0.13: 10 of 13 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r1: findings as reconciled — 2 blockers, 5 should-fix required (all in tasks 0.23–0.26), 1 should-fix taken in the reconciliation, 4 nits (3 taken in the reconciliation, 1 recorded); r0 dispositions reopened: 0 of 21. Lessons written by Phase 0 agents and the principal: 75 (docs/lessons.md lines tagged 0.*, plan.0.*). Lessons promoted to CLAUDE.md/AGENTS.md: 2 (via 0.verify.r1) + 1 pending (via 0.verify.r2). STUCK signals: 2 (0.5, three attempts, re-cut per DECISIONS #016; 0.12.r1 twice, Claude reviewer max_turns, retried with a larger budget). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r1 (cold build and all three CI legs): statements 99.0 %, branches 96.3 %, functions 100 %, lines 99.67 %; blocks.ts 100 %; delta vs r0: statements −0.38, branches +0.15, lines −0.15 (thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.22: 19 of 22 (0.1, 0.2, 0.10 needed more than one attempt).

Phase 0 review r2: findings as reconciled — 0 blockers, 0 should-fix required, 1 should-fix taken in the reconciliation, 3 should-fix deferred, 5 nits (1 taken, 4 recorded); r1 dispositions reopened: 0 of 13. Verdict PASS; the phase closes at 0.close. Lessons written by Phase 0 agents and the principal: 90 (docs/lessons.md lines tagged 0.*, plan.0.*; 83 before this commit + 7). Lessons promoted to CLAUDE.md/AGENTS.md: 3 (2 via 0.verify.r1, 1 via 0.verify.r2); none named for 1.verify. STUCK stops over the phase: 4 (0.2; 0.5, re-cut per DECISIONS #016; 0.12.r1 twice for the Claude reviewer's turn budget, one of them absent from runner.log per deviation 31). Gate reruns: 0. Gate repairs: 1 (0.verify.g1). Coverage at r2 (cold build and all three CI legs): statements 99.45 %, branches 96.98 %, functions 100 %, lines 100 %; blocks.ts 100 %; delta vs r1: +0.45, +0.68, 0, +0.33 (every metric up; thresholds hold). First-attempt pass rate, implementation tasks 0.1–0.11, 0.13–0.26: 23 of 26 (0.1, 0.2, 0.10 needed more than one attempt); fix-chain tasks 0.13–0.26: 14 of 14. Review blockers per attempt: r0 2, r1 2, r2 0; should-fix required per attempt: r0 12, r1 5, r2 0 — the PRD §10.4 direction (blockers falling) holds within the phase.
